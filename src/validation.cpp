// Copyright (c) 2009-2010 Satoshi Nakamoto
// Copyright (c) 2009-present The Bitcoin Core developers
// Distributed under the MIT software license, see the accompanying
// file COPYING or http://www.opensource.org/licenses/mit-license.php.

#include <bitcoin-build-config.h> // IWYU pragma: keep

#include <validation.h>

#include <arith_uint256.h>
#include <chain.h>
#include <checkqueue.h>
#include <clientversion.h>
#include <consensus/amount.h>
#include <consensus/consensus.h>
#include <consensus/merkle.h>
#include <consensus/tx_check.h>
#include <consensus/tx_verify.h>
#include <consensus/validation.h>
#include <cuckoocache.h>
#include <flatfile.h>
#include <hash.h>
#include <kernel/chain.h>
#include <kernel/chainparams.h>
#include <kernel/coinstats.h>
#include <kernel/disconnected_transactions.h>
#include <kernel/mempool_entry.h>
#include <kernel/messagestartchars.h>
#include <kernel/notifications_interface.h>
#include <kernel/warning.h>
#include <logging.h>
#include <logging/timer.h>
#include <node/blockstorage.h>
#include <node/matmul_trusted_attestations.h>
#include <node/utxo_snapshot.h>
#include <policy/coin_age_priority.h>
#include <policy/ephemeral_policy.h>
#include <policy/policy.h>
#include <policy/rbf.h>
#include <policy/settings.h>
#include <policy/truc_policy.h>
#include <matmul/matmul_v4_rc.h>
#include <matmul/matmul_v4_rc_stage3_consensus.h>
#include <matmul/matmul_v4_rc_stage3_producer.h>
#include <pow.h>
#include <primitives/block.h>
#include <primitives/transaction.h>
#include <random.h>
#include <script/script.h>
#include <script/sigcache.h>
#include <signet.h>
#include <stats/stats.h>
#include <tinyformat.h>
#include <txdb.h>
#include <txmempool.h>
#include <uint256.h>
#include <undo.h>
#include <util/check.h>
#include <util/fs.h>
#include <util/fs_helpers.h>
#include <util/hasher.h>
#include <util/ioprio.h>
#include <util/mempressure.h>
#include <util/moneystr.h>
#include <util/overflow.h>
#include <util/rbf.h>
#include <util/readwritefile.h>
#include <util/result.h>
#include <util/signalinterrupt.h>
#include <util/strencodings.h>
#include <util/string.h>
#include <util/threadpool.h>
#include <util/time.h>
#include <util/trace.h>
#include <util/translation.h>
#include <validationinterface.h>

#include <shielded/smile2/verify_dispatch.h>
#include <shielded/ringct/ring_selection.h>
#include <shielded/recovery_exit.h>
#include <shielded/v2_ingress.h>
#include <shielded/v2_proof.h>
#include <shielded/v2_types.h>

#include <algorithm>
#include <atomic>
#include <cassert>
#include <chrono>
#include <cinttypes>
#include <deque>
#include <limits>
#include <list>
#include <numeric>
#include <optional>
#include <ranges>
#include <set>
#include <span>
#include <string>
#include <tuple>
#include <utility>
#include <vector>

using kernel::CCoinsStats;
using kernel::CoinStatsHashType;
using kernel::ComputeUTXOStats;
using kernel::Notifications;

using fsbridge::FopenFn;
using node::BlockManager;
using node::BlockMap;
using node::CBlockIndexHeightOnlyComparator;
using node::CBlockIndexWorkComparator;
using node::SnapshotMetadata;
using node::ShieldedSnapshotSectionHeader;

/** Time window to wait between writing blocks/block index and chainstate to disk.
 *  Randomize writing time inside the window to prevent a situation where the
 *  network over time settles into a few cohorts of synchronized writers.
*/
static constexpr auto DATABASE_WRITE_INTERVAL_MIN{50min};
static constexpr auto DATABASE_WRITE_INTERVAL_MAX{70min};
/** Maximum age of our tip for us to be considered current for fee estimation */
static constexpr std::chrono::hours MAX_FEE_ESTIMATION_TIP_AGE{3};

// Return whether the completed full flush should compact chainstate.
static bool ShouldCompactChainstate(bool in_ibd)
{
    static constexpr uint32_t flush_ratio{320}; // Roughly every 2 weeks with hourly flushes.
    return !in_ibd && FastRandomContext().randrange(flush_ratio) == 0;
}

const std::vector<std::string> CHECKLEVEL_DOC {
    "level 0 reads the blocks from disk",
    "level 1 verifies block validity",
    "level 2 verifies undo data",
    "level 3 checks disconnection of tip blocks",
    "level 4 tries to reconnect the blocks",
    "each level includes the checks of the previous levels",
};

SpkReuseModes SpkReuseMode;

namespace {
std::atomic<uint64_t> g_reorg_protection_observed_reorgs{0};
std::atomic<uint32_t> g_reorg_protection_deepest_observed_reorg_depth{0};
std::atomic<uint32_t> g_reorg_protection_last_observed_reorg_depth{0};
std::atomic<int32_t> g_reorg_protection_last_observed_tip_height{0};
std::atomic<int32_t> g_reorg_protection_last_observed_fork_height{0};
std::atomic<int32_t> g_reorg_protection_last_observed_candidate_height{0};
std::atomic<int64_t> g_reorg_protection_last_observed_unix{0};
std::atomic<uint64_t> g_reorg_protection_rejected_reorgs{0};
std::atomic<uint32_t> g_reorg_protection_deepest_rejected_reorg_depth{0};
std::atomic<uint32_t> g_reorg_protection_last_rejected_reorg_depth{0};
std::atomic<uint32_t> g_reorg_protection_last_rejected_max_reorg_depth{0};
std::atomic<int32_t> g_reorg_protection_last_rejected_tip_height{0};
std::atomic<int32_t> g_reorg_protection_last_rejected_fork_height{0};
std::atomic<int32_t> g_reorg_protection_last_rejected_candidate_height{0};
std::atomic<int64_t> g_reorg_protection_last_rejected_unix{0};
std::atomic<uint64_t> g_reorg_protection_deferred_reorgs{0};
std::atomic<uint32_t> g_reorg_protection_deepest_deferred_reorg_depth{0};
std::atomic<uint32_t> g_reorg_protection_last_deferred_reorg_depth{0};
std::atomic<uint32_t> g_reorg_protection_last_deferred_required_work_margin{0};
std::atomic<int32_t> g_reorg_protection_last_deferred_tip_height{0};
std::atomic<int32_t> g_reorg_protection_last_deferred_fork_height{0};
std::atomic<int32_t> g_reorg_protection_last_deferred_candidate_height{0};
std::atomic<int64_t> g_reorg_protection_last_deferred_unix{0};

[[nodiscard]] bool UseNoncedShieldedBridgeTags(const Consensus::Params& consensus, int32_t height)
{
    return consensus.IsShieldedBridgeTagUpgradeActive(height);
}

[[nodiscard]] bool UseSingleUseSettlementAnchors(const Consensus::Params& consensus, int32_t height)
{
    return consensus.IsShieldedMatRiCTDisabled(height);
}

[[nodiscard]] bool UseSettlementAnchorMaturityRule(const Consensus::Params& consensus, int32_t height)
{
    return consensus.GetShieldedSettlementAnchorMaturityDepth(height) > 0;
}

[[nodiscard]] bool UseNettingManifestFreshnessBinding(const Consensus::Params& consensus, int32_t height)
{
    return consensus.IsShieldedMatRiCTDisabled(height);
}

[[nodiscard]] bool UseZeroFeeRebalanceConsensusRule(const Consensus::Params& consensus, int32_t height)
{
    return consensus.IsShieldedMatRiCTDisabled(height);
}

[[nodiscard]] bool UseAccountRegistryAppendRateLimit(const Consensus::Params& consensus, int32_t height)
{
    return consensus.IsShieldedMatRiCTDisabled(height);
}

[[nodiscard]] bool UseAccountRegistryEntryCountLimit(const Consensus::Params& consensus, int32_t height)
{
    return consensus.IsShieldedMatRiCTDisabled(height);
}

[[nodiscard]] bool WouldExceedAccountRegistryEntryCountLimit(const Consensus::Params& consensus,
                                                             uint64_t current_entries,
                                                             uint64_t new_entries)
{
    return current_entries > consensus.nMaxShieldedAccountRegistryEntries ||
           new_entries > consensus.nMaxShieldedAccountRegistryEntries - current_entries;
}

[[nodiscard]] bool RejectDisabledShieldedPoolCredit(const CShieldedBundle& bundle,
                                                    const Consensus::Params& consensus,
                                                    int32_t validation_height,
                                                    std::string& reject_reason)
{
    if (!consensus.IsShieldedPoolCreditDisabled(validation_height)) {
        return true;
    }

    std::string value_balance_reject;
    const auto value_balance = TryGetShieldedStateValueBalance(bundle, value_balance_reject);
    if (!value_balance.has_value()) {
        reject_reason = value_balance_reject;
        return false;
    }
    if (*value_balance < 0) {
        reject_reason = "bad-shielded-pool-credit-disabled";
        return false;
    }

    if (!bundle.HasV2Bundle()) {
        return true;
    }
    const auto* v2_bundle = bundle.GetV2Bundle();
    if (v2_bundle == nullptr) {
        reject_reason = "bad-shielded-v2-bundle";
        return false;
    }

    switch (shielded::v2::GetBundleSemanticFamily(*v2_bundle)) {
    case shielded::v2::TransactionFamily::V2_SETTLEMENT_ANCHOR: {
        const auto& payload = std::get<shielded::v2::SettlementAnchorPayload>(v2_bundle->payload);
        if (!payload.reserve_deltas.empty() || !payload.anchored_netting_manifest_id.IsNull()) {
            reject_reason = "bad-shielded-v2-settlement-disabled";
            return false;
        }
        return true;
    }
    case shielded::v2::TransactionFamily::V2_REBALANCE:
        // DS-5 fix makes a rebalance pool-neutral (state value_balance == 0), so the negative-credit
        // predicate above no longer catches it. But a rebalance is reserve/netting rollover machinery
        // that mints new shielded reserve notes and manifests, so the height gate disables the family
        // structurally (matching this gate's no-new-shielded-control-plane intent).
        reject_reason = "bad-shielded-v2-rebalance-disabled";
        return false;
    case shielded::v2::TransactionFamily::V2_SEND:
    case shielded::v2::TransactionFamily::V2_INGRESS_BATCH:
    case shielded::v2::TransactionFamily::V2_EGRESS_BATCH:
    case shielded::v2::TransactionFamily::V2_GENERIC:
    case shielded::v2::TransactionFamily::V2_LIFECYCLE:
    case shielded::v2::TransactionFamily::V2_SPEND_PATH_RECOVERY:
    case shielded::v2::TransactionFamily::V2_RECOVERY_EXIT:
        return true;
    }

    reject_reason = "bad-shielded-v2-contextual";
    return false;
}

// Derive the (commitment, nullifier) a V2_RECOVERY_EXIT bundle retires, deterministically from its
// revealed payload. Used identically by ConnectBlock (validate + collect) and DisconnectBlock (undo), so
// the same note retires the same two identifiers on connect and disconnect. Returns false if the bundle
// is not a well-formed recovery-exit claim (the caller treats that as a consensus error in ConnectBlock).
[[nodiscard]] bool GetRecoveryExitIdentifiersFromBundle(const CShieldedBundle& bundle,
                                                        shielded::recovery::RecoveryExitClaim& out_claim,
                                                        shielded::recovery::RecoveryExitIdentifiers& out_ids)
{
    if (!bundle.HasV2Bundle()) return false;
    const auto* v2b = bundle.GetV2Bundle();
    if (v2b == nullptr) return false;
    if (shielded::v2::GetBundleSemanticFamily(*v2b) != shielded::v2::TransactionFamily::V2_RECOVERY_EXIT) return false;
    if (!std::holds_alternative<shielded::v2::RecoveryExitPayload>(v2b->payload)) return false;
    const auto& p = std::get<shielded::v2::RecoveryExitPayload>(v2b->payload);
    out_claim.value = p.value;
    out_claim.note_commitment = p.note_commitment;
    out_claim.recipient_pk_hash = p.recipient_pk_hash;
    out_claim.rho = p.rho;
    out_claim.rcm = p.rcm;
    out_claim.spend_pubkey = p.spend_pubkey;
    out_claim.ownership_sig = p.ownership_sig;
    out_claim.membership_proof = p.membership_proof;
    std::string rr;
    return shielded::recovery::DeriveRecoveryExitIdentifiers(out_claim, out_ids, rr);
}

[[nodiscard]] bool IsRecoveryExitBundle(const CShieldedBundle& bundle)
{
    if (!bundle.HasV2Bundle()) return false;
    const auto* v2b = bundle.GetV2Bundle();
    return v2b != nullptr &&
        shielded::v2::GetBundleSemanticFamily(*v2b) == shielded::v2::TransactionFamily::V2_RECOVERY_EXIT;
}

[[nodiscard]] bool IsRecoveryExitTransaction(const CTransaction& tx)
{
    return tx.HasShieldedBundle() && IsRecoveryExitBundle(tx.GetShieldedBundle());
}

[[nodiscard]] bool CollectRecoveryExitRetirements(const CShieldedBundle& bundle,
                                                  std::vector<Nullifier>* nullifiers,
                                                  std::vector<uint256>* commitments)
{
    if (!IsRecoveryExitBundle(bundle)) return true;

    shielded::recovery::RecoveryExitClaim re_claim;
    shielded::recovery::RecoveryExitIdentifiers re_ids;
    if (!GetRecoveryExitIdentifiersFromBundle(bundle, re_claim, re_ids)) {
        return false;
    }
    if (nullifiers) {
        nullifiers->push_back(re_ids.nullifier);
    }
    if (commitments) {
        commitments->push_back(re_ids.commitment);
    }
    return true;
}

[[nodiscard]] uint256 RecoveryExitMembershipRoot(const Consensus::Params& consensus,
                                                 int32_t validation_height,
                                                 const shielded::ShieldedMerkleTree& current_tree)
{
    if (!consensus.IsShieldedRecoveryExitActive(validation_height)) return uint256{};
    if (!consensus.nShieldedRecoveryExitFrozenRoot.IsNull()) {
        return consensus.nShieldedRecoveryExitFrozenRoot;
    }
    if (!consensus.IsShieldedSunsetActive(validation_height)) return uint256{};
    return current_tree.Root();
}

[[nodiscard]] bool TrySumTransparentOutputs(const std::vector<CTxOut>& vout,
                                            CAmount& out,
                                            std::string& reject_reason)
{
    CAmount total{0};
    for (const CTxOut& txout : vout) {
        if (!MoneyRange(txout.nValue) || txout.nValue > MAX_MONEY - total) {
            reject_reason = "bad-txns-vout-toolarge";
            return false;
        }
        total += txout.nValue;
    }
    out = total;
    return true;
}

[[nodiscard]] bool TryComputeRecoveryExitFee(CAmount claim_value,
                                             CAmount transparent_out,
                                             CAmount& fee,
                                             std::string& reject_reason)
{
    if (!MoneyRange(claim_value)) {
        reject_reason = "bad-recovery-exit-value";
        return false;
    }
    if (!MoneyRange(transparent_out)) {
        reject_reason = "bad-txns-vout-toolarge";
        return false;
    }
    const auto computed_fee = CheckedAdd(claim_value, -transparent_out);
    if (!computed_fee.has_value()) {
        reject_reason = "bad-recovery-exit-fee";
        return false;
    }
    fee = *computed_fee;
    return true;
}

[[nodiscard]] bool RejectShieldedSunsetViolation(const CTransaction& tx,
                                                 const Consensus::Params& consensus,
                                                 int32_t validation_height,
                                                 std::string& reject_reason)
{
    if (!consensus.IsShieldedSunsetActive(validation_height)) {
        return true;
    }
    const CShieldedBundle& bundle = tx.GetShieldedBundle();

    // Outflow-only sunset (legacy-balance preservation). At/after the sunset height the ONLY permitted
    // shielded transaction is a strict OUTFLOW exit of existing legacy value: a V2_SEND/recovery spend
    // whose net pool effect is value LEAVING the pool (state value_balance > 0), that actually delivers
    // value to TRANSPARENT outputs (a real unshield), and that appends NO new shielded outputs. This lets
    // legacy holders unshield out while rejecting every value-IN credit, rollover, bridge, control op,
    // pool-neutral/private transfer, and shielded change/recipient append.
    // NOTE: value_balance > 0 alone is INSUFFICIENT -- a private z->z transfer has value_balance == fee
    // (> 0) yet no transparent output, so we additionally require positive transparent output value. The
    // pool is therefore strictly monotone-decreasing after sunset and can never be re-credited. The zero
    // shielded-output rule also makes the live commitment tree root immutable after the sunset, which lets
    // RECOVERY_EXIT verify against the frozen live root even before a later release hardcodes that root.
    std::string value_balance_reject;
    const auto value_balance = TryGetShieldedStateValueBalance(bundle, value_balance_reject);
    if (!value_balance.has_value()) {
        reject_reason = value_balance_reject;
        return false;
    }
    if (*value_balance <= 0) {
        // Pool credit (< 0) or pool-neutral private transfer / control op (== 0): not an exit.
        reject_reason = "bad-shielded-sunset-non-exit";
        return false;
    }
    CAmount transparent_out{0};
    if (!TrySumTransparentOutputs(tx.vout, transparent_out, reject_reason)) {
        return false;
    }
    if (transparent_out <= 0) {
        // A fee-only private spend can still have positive state value_balance.
        // Require an actual transparent payout so the post-sunset allow-list is
        // strictly z->t/recovery-exit, never z->z.
        reject_reason = "bad-shielded-sunset-non-exit";
        return false;
    }
    if (!bundle.HasV2Bundle()) {
        reject_reason = "bad-shielded-sunset-non-exit";
        return false;
    }
    const auto* v2_bundle = bundle.GetV2Bundle();
    if (v2_bundle == nullptr) {
        reject_reason = "bad-shielded-v2-bundle";
        return false;
    }
    const auto family = shielded::v2::GetBundleSemanticFamily(*v2_bundle);
    // V2_RECOVERY_EXIT is the second permitted post-sunset operation: a transparent claim of a stranded
    // pre-snapshot note (reveals the note, retires both its commitment AND its canonical SMILE2
    // nullifier, pays out transparent). It is gated by its OWN activation height
    // (nShieldedRecoveryExitActivationHeight >= sunset). The full claim validation + atomic dual
    // retirement runs in ConnectBlock / mempool (CheckRecoveryExitClaim); here we only admit it to the
    // post-sunset allow-list when its fork is on.
    if (family == shielded::v2::TransactionFamily::V2_RECOVERY_EXIT) {
        if (!consensus.IsShieldedRecoveryExitActive(validation_height)) {
            reject_reason = "bad-shielded-v2-recovery-exit-disabled";
            return false;
        }
        if (bundle.GetShieldedOutputCount() != 0) {
            reject_reason = "bad-shielded-sunset-shielded-output";
            return false;
        }
        return true;
    }
    // Only a V2_SEND real unshield is a true outflow exit. V2_SPEND_PATH_RECOVERY has
    // value_balance == fee (it re-shields stranded value rather than sending it transparent) and its
    // DS-4 nullifier binding is not yet implemented, so it is rejected here until that binding lands
    // (stranded notes are recoverable before the sunset height); re-enable it once bound.
    if (family != shielded::v2::TransactionFamily::V2_SEND ||
        !std::holds_alternative<shielded::v2::SendPayload>(v2_bundle->payload)) {
        reject_reason = "bad-shielded-sunset-non-exit";
        return false;
    }
    // The spend must deliver real value to transparent, not merely pay a fee while keeping value
    // shielded. A V2_SEND's state value_balance == transparent_out + fee, so a real unshield has
    // value_balance > fee while a pool-neutral z->z private transfer has value_balance == fee. (The
    // builder only emits a transparent outflow at/after C-002 height 123000, <= the sunset height, so a
    // value_balance > fee V2_SEND at the sunset is genuinely a public unshield.)
    const auto& send_payload = std::get<shielded::v2::SendPayload>(v2_bundle->payload);
    if (*value_balance <= send_payload.fee) {
        reject_reason = "bad-shielded-sunset-non-exit";
        return false;
    }
    if (bundle.GetShieldedOutputCount() != 0) {
        reject_reason = "bad-shielded-sunset-shielded-output";
        return false;
    }
    return true;  // strict outflow unshield of legacy value: allowed for the life of the wind-down
}

[[nodiscard]] bool IsV2SendZeroOutputExitBundle(const CShieldedBundle& bundle)
{
    if (!bundle.HasV2Bundle()) return false;
    const auto* v2_bundle = bundle.GetV2Bundle();
    if (v2_bundle == nullptr ||
        shielded::v2::GetBundleSemanticFamily(*v2_bundle) != shielded::v2::TransactionFamily::V2_SEND ||
        !std::holds_alternative<shielded::v2::SendPayload>(v2_bundle->payload)) {
        return false;
    }
    const auto& payload = std::get<shielded::v2::SendPayload>(v2_bundle->payload);
    return payload.outputs.empty();
}

[[nodiscard]] bool RejectDisabledV2SendZeroOutputExit(const CShieldedBundle& bundle,
                                                      const Consensus::Params& consensus,
                                                      int32_t validation_height,
                                                      std::string& reject_reason)
{
    if (!IsV2SendZeroOutputExitBundle(bundle) ||
        !consensus.IsShieldedSunsetActive(validation_height) ||
        consensus.IsShieldedV2SendZeroOutputExitActive(validation_height)) {
        return true;
    }
    reject_reason = "bad-shielded-v2-send-zero-output-exit-disabled";
    return false;
}

[[nodiscard]] bool RejectShieldedHeightGateViolation(const CTransaction& tx,
                                                     const Consensus::Params& consensus,
                                                     int32_t validation_height,
                                                     std::string& reject_reason)
{
    if (!RejectShieldedSunsetViolation(tx, consensus, validation_height, reject_reason)) {
        return false;
    }
    if (!RejectDisabledShieldedPoolCredit(tx.GetShieldedBundle(), consensus, validation_height, reject_reason)) {
        return false;
    }
    if (!RejectDisabledV2SendZeroOutputExit(tx.GetShieldedBundle(), consensus, validation_height, reject_reason)) {
        return false;
    }
    return true;
}

[[nodiscard]] bool RejectPostForkTransparentFundingV2SendContext(const CTransaction& tx,
                                                                 const CCoinsViewCache& view,
                                                                 const Consensus::Params& consensus,
                                                                 int32_t validation_height,
                                                                 std::string& reject_reason)
{
    (void)view;
    if (!consensus.IsShieldedDirectSendPublicFlowDisabled(validation_height) || !tx.HasShieldedBundle()) {
        return true;
    }

    const CShieldedBundle& bundle = tx.GetShieldedBundle();
    if (!bundle.HasV2Bundle() ||
        bundle.GetTransactionFamily() != shielded::v2::TransactionFamily::V2_SEND) {
        return true;
    }

    const auto* v2_bundle = bundle.GetV2Bundle();
    if (v2_bundle == nullptr) {
        return true;
    }

    const auto& payload = std::get<shielded::v2::SendPayload>(v2_bundle->payload);
    if (!payload.spends.empty() || tx.vin.empty()) {
        return true;
    }

    if (!payload.lifecycle_controls.empty()) {
        reject_reason = "bad-shielded-v2-send-lifecycle-control";
        return false;
    }

    if (!tx.vout.empty()) {
        reject_reason = "bad-shielded-v2-send-public-flow-disabled";
        return false;
    }

    reject_reason = "bad-shielded-v2-send-public-flow-disabled";
    return false;
}

[[nodiscard]] bool ApplyShieldedStateEffects(const CShieldedBundle& bundle,
                                             bool use_nonced_bridge_tag,
                                             const uint256& block_hash,
                                             shielded::ShieldedMerkleTree& tree,
                                             ShieldedPoolBalance& pool,
                                             std::vector<Nullifier>* all_nullifiers = nullptr,
                                             std::map<uint256, smile2::CompactPublicAccount>* public_accounts = nullptr,
                                             std::map<uint256, uint256>* account_leaf_commitments = nullptr,
                                             std::vector<uint256>* recovery_exit_commitments = nullptr);

[[nodiscard]] std::optional<ShieldedStateMutationMarker::PreparedSnapshot>
MakePreparedShieldedSnapshot(const shielded::ShieldedMerkleTree& tree,
                             const ShieldedPoolBalance& pool,
                             const shielded::registry::ShieldedAccountRegistryState& account_registry)
{
    const auto commitment_index_digest = tree.CommitmentIndexDigest();
    if (!commitment_index_digest.has_value()) {
        return std::nullopt;
    }

    const auto account_registry_snapshot = account_registry.ExportPersistedSnapshot();
    if (!account_registry_snapshot.IsValid()) {
        return std::nullopt;
    }

    ShieldedStateMutationMarker::PreparedSnapshot prepared_snapshot;
    prepared_snapshot.tree = tree;
    prepared_snapshot.pool_balance = pool.GetBalance();
    prepared_snapshot.commitment_index_digest = *commitment_index_digest;
    prepared_snapshot.account_registry_snapshot = account_registry_snapshot;
    const auto journaled_account_payloads =
        account_registry.ExportPreparedPayloadEntries();
    if (!journaled_account_payloads.has_value()) {
        return std::nullopt;
    }
    uint64_t cumulative_payload_bytes{0};
    for (const auto& entry : *journaled_account_payloads) {
        const uint64_t payload_size{entry.account_leaf_payload.size()};
        if (payload_size >
            ShieldedStateMutationMarker::MAX_JOURNALED_ACCOUNT_PAYLOAD_BYTES -
                cumulative_payload_bytes) {
            return std::nullopt;
        }
        cumulative_payload_bytes += payload_size;
    }
    prepared_snapshot.journaled_account_payloads =
        *journaled_account_payloads;
    if (!prepared_snapshot.IsValid()) {
        return std::nullopt;
    }
    return prepared_snapshot;
}

[[nodiscard]] ShieldedStateMutationMarker MakeShieldedMutationMarker(const CBlockIndex* tip)
{
    ShieldedStateMutationMarker marker;
    marker.target_tip_hash = tip ? tip->GetBlockHash() : uint256{};
    marker.target_tip_height = tip ? tip->nHeight : -1;
    return marker;
}

[[nodiscard]] std::optional<ShieldedStateMutationMarker> MakePreparedShieldedMutationMarker(
    const CBlockIndex* source_tip,
    const CBlockIndex* target_tip,
    const shielded::ShieldedMerkleTree& target_tree,
    const ShieldedPoolBalance& target_pool,
    const shielded::registry::ShieldedAccountRegistryState& target_account_registry)
{
    const auto prepared_snapshot =
        MakePreparedShieldedSnapshot(target_tree, target_pool, target_account_registry);
    if (!prepared_snapshot.has_value()) {
        return std::nullopt;
    }

    ShieldedStateMutationMarker marker;
    marker.version = ShieldedStateMutationMarker::PREPARED_TRANSITION_VERSION;
    marker.stage = ShieldedStateMutationMarker::PREPARED_STAGE;
    marker.source_tip_hash = source_tip ? source_tip->GetBlockHash() : uint256{};
    marker.source_tip_height = source_tip ? source_tip->nHeight : -1;
    marker.target_tip_hash = target_tip ? target_tip->GetBlockHash() : uint256{};
    marker.target_tip_height = target_tip ? target_tip->nHeight : -1;
    marker.prepared_target_snapshot = *prepared_snapshot;
    if (!marker.IsValid()) {
        return std::nullopt;
    }
    return marker;
}

[[nodiscard]] bool WriteShieldedMutationMarker(NullifierSet* nullifiers,
                                               const ShieldedStateMutationMarker& marker) EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);
    if (nullifiers == nullptr) return false;
    return nullifiers->WriteMutationMarker(marker);
}

[[nodiscard]] bool TryCountShieldedAccountRegistryAppends(const CShieldedBundle& bundle,
                                                          const Consensus::Params& consensus,
                                                          int32_t height,
                                                          uint64_t& out_count)
{
    const auto account_leaf_commitments =
        CollectShieldedOutputAccountLeafCommitments(bundle,
                                                    UseNoncedShieldedBridgeTags(consensus, height));
    if (!account_leaf_commitments.has_value()) return false;
    out_count = static_cast<uint64_t>(account_leaf_commitments->size());
    return true;
}

[[nodiscard]] bool IsNettingManifestFresh(const ConfirmedNettingManifestState& manifest_state,
                                          int32_t validation_height)
{
    if (!manifest_state.IsValid() || validation_height < manifest_state.created_height) {
        return false;
    }
    const auto age = static_cast<uint64_t>(validation_height - manifest_state.created_height);
    return age < manifest_state.settlement_window;
}

[[nodiscard]] bool IsSettlementAnchorMature(const ConfirmedSettlementAnchorState& anchor_state,
                                            const Consensus::Params& consensus,
                                            int32_t validation_height)
{
    const uint32_t maturity_depth = consensus.GetShieldedSettlementAnchorMaturityDepth(validation_height);
    if (maturity_depth == 0) {
        return true;
    }
    if (!anchor_state.IsValid() || validation_height <= anchor_state.created_height) {
        return false;
    }
    const auto depth = static_cast<uint32_t>(validation_height - anchor_state.created_height);
    return depth >= maturity_depth;
}

[[nodiscard]] bool PrepareShieldedCommitmentIndex(const fs::path& db_path,
                                                  bool retain_commitment_index,
                                                  bool wipe_data)
{
    shielded::ShieldedMerkleTree::ResetCommitmentIndexStore();

    if (retain_commitment_index) {
        return shielded::ShieldedMerkleTree::ConfigureCommitmentIndexStore(
            db_path,
            8 << 20,
            262144,
            /*memory_only=*/false,
            wipe_data);
    }

    // Slice 14 production default: retain nullifier state on disk, but
    // externalize the commitment-position index unless the explicit full-
    // retention dev/audit profile is requested.
    try {
        if (fs::exists(db_path)) {
            fs::remove_all(db_path);
        }
    } catch (const fs::filesystem_error& e) {
        LogPrintf("PrepareShieldedCommitmentIndex: failed to remove retained commitment index store %s: %s\n",
                  fs::PathToString(db_path),
                  e.what());
        return false;
    }

    if (wipe_data && fs::exists(db_path)) {
        LogPrintf("PrepareShieldedCommitmentIndex: retained commitment index store still exists after wipe %s\n",
                  fs::PathToString(db_path));
        return false;
    }

    return true;
}

[[nodiscard]] bool PrepareShieldedAccountRegistryPayloadStore(const fs::path& db_path,
                                                              bool wipe_data)
{
    shielded::registry::ShieldedAccountRegistryState::ResetPayloadStore();
    return shielded::registry::ShieldedAccountRegistryState::ConfigurePayloadStore(
        db_path,
        8 << 20,
        /*memory_only=*/false,
        wipe_data);
}

struct ShieldedStateDirectoryBackupEntry {
    fs::path live_path;
    fs::path backup_path;
};

struct ShieldedStateDirectoryBackup {
    fs::path backup_root;
    std::vector<fs::path> managed_paths;
    std::vector<ShieldedStateDirectoryBackupEntry> entries;
};

constexpr std::string_view SHIELDED_SNAPSHOT_BACKUP_PREFIX{
    "shielded_state_snapshot_backup_"};
constexpr std::string_view SHIELDED_SNAPSHOT_TXN_FILENAME{
    "shielded_state_snapshot_transaction"};

enum class ShieldedSnapshotTxnPhase : uint8_t {
    PREPARING_BACKUP = 1,
    BACKUP_COMPLETE = 2,
    ROLLING_BACK = 3,
};

struct ShieldedSnapshotTxnMarker {
    static constexpr uint32_t VERSION{1};
    uint32_t version{VERSION};
    uint8_t phase{static_cast<uint8_t>(
        ShieldedSnapshotTxnPhase::PREPARING_BACKUP)};
    std::string backup_name;

    SERIALIZE_METHODS(ShieldedSnapshotTxnMarker, obj)
    {
        READWRITE(obj.version, obj.phase, obj.backup_name);
    }

    [[nodiscard]] bool IsValid() const
    {
        const std::string_view name{backup_name};
        const std::string_view suffix{
            name.size() >= SHIELDED_SNAPSHOT_BACKUP_PREFIX.size()
                ? name.substr(SHIELDED_SNAPSHOT_BACKUP_PREFIX.size())
                : std::string_view{}};
        return version == VERSION &&
               (phase == static_cast<uint8_t>(ShieldedSnapshotTxnPhase::PREPARING_BACKUP) ||
                phase == static_cast<uint8_t>(ShieldedSnapshotTxnPhase::BACKUP_COMPLETE) ||
                phase == static_cast<uint8_t>(ShieldedSnapshotTxnPhase::ROLLING_BACK)) &&
               name.starts_with(SHIELDED_SNAPSHOT_BACKUP_PREFIX) &&
               suffix.size() == 64 &&
               std::ranges::all_of(suffix, [](char c) {
                   return (c >= '0' && c <= '9') ||
                          (c >= 'a' && c <= 'f');
               }) &&
               backup_name.find('/') == std::string::npos &&
               backup_name.find('\\') == std::string::npos;
    }
};

[[nodiscard]] fs::path ShieldedSnapshotTxnPath(const fs::path& datadir)
{
    return datadir / fs::PathFromString(
        std::string{SHIELDED_SNAPSHOT_TXN_FILENAME});
}

[[nodiscard]] bool WriteShieldedSnapshotTxnMarker(
    const fs::path& datadir, const ShieldedSnapshotTxnMarker& marker)
{
    if (!marker.IsValid()) return false;
    const fs::path path{ShieldedSnapshotTxnPath(datadir)};
    const fs::path tmp{path + ".tmp"};
    FILE* raw{fsbridge::fopen(tmp, "wb")};
    AutoFile file{raw};
    if (file.IsNull()) return false;
    file << marker;
    const bool committed{file.Commit()};
    const int close_result{file.fclose()};
    if (!committed || close_result != 0 || !RenameOver(tmp, path)) {
        std::error_code ec;
        fs::remove(tmp, ec);
        return false;
    }
    DirectoryCommit(datadir);
    return true;
}

[[nodiscard]] bool ClearShieldedSnapshotTxnMarker(const fs::path& datadir)
{
    std::error_code ec;
    const fs::path path{ShieldedSnapshotTxnPath(datadir)};
    const bool removed{fs::remove(path, ec)};
    if (ec) return false;
    if (removed) DirectoryCommit(datadir);
    return true;
}

[[nodiscard]] std::optional<ShieldedSnapshotTxnMarker>
ReadShieldedSnapshotTxnMarker(const fs::path& datadir)
{
    const fs::path path{ShieldedSnapshotTxnPath(datadir)};
    if (!fs::exists(path)) return std::nullopt;
    try {
        AutoFile file{fsbridge::fopen(path, "rb")};
        if (file.IsNull()) return std::nullopt;
        ShieldedSnapshotTxnMarker marker;
        file >> marker;
        const int64_t position{file.tell()};
        file.seek(0, SEEK_END);
        if (position != file.tell() || !marker.IsValid()) return std::nullopt;
        return marker;
    } catch (const std::exception&) {
        return std::nullopt;
    }
}

[[nodiscard]] std::vector<fs::path> GetShieldedStateStorePaths(const fs::path& datadir)
{
    const fs::path shielded_state_dir = datadir / "shielded_state";
    return {
        shielded_state_dir / "nullifiers",
        shielded_state_dir / "commitments",
        shielded_state_dir / "account_registry",
    };
}

[[nodiscard]] bool RestoreShieldedStateDirectoryBackup(ShieldedStateDirectoryBackup& backup,
                                                       bool remove_partial_live_paths = true)
{
    bool restored{true};
    if (remove_partial_live_paths) {
        for (auto it = backup.managed_paths.rbegin(); it != backup.managed_paths.rend(); ++it) {
            try {
                if (fs::exists(*it)) {
                    fs::remove_all(*it);
                    DirectoryCommit(it->parent_path());
                }
            } catch (const fs::filesystem_error& e) {
                LogPrintf("RestoreShieldedStateDirectoryBackup: failed removing partial shielded state path %s: %s\n",
                          fs::PathToString(*it),
                          fsbridge::get_filesystem_error_message(e));
                restored = false;
            }
        }
    }
    for (auto it = backup.entries.rbegin(); it != backup.entries.rend(); ++it) {
        try {
            if (!fs::exists(it->backup_path)) {
                LogPrintf("RestoreShieldedStateDirectoryBackup: missing backup path %s for %s\n",
                          fs::PathToString(it->backup_path),
                          fs::PathToString(it->live_path));
                restored = false;
                continue;
            }
            fs::rename(it->backup_path, it->live_path);
            DirectoryCommit(it->backup_path.parent_path());
            DirectoryCommit(it->live_path.parent_path());
        } catch (const fs::filesystem_error& e) {
            LogPrintf("RestoreShieldedStateDirectoryBackup: failed restoring %s from %s: %s\n",
                      fs::PathToString(it->live_path),
                      fs::PathToString(it->backup_path),
                      fsbridge::get_filesystem_error_message(e));
            restored = false;
        }
    }

    // Never delete the only remaining old-state copy after a partial restore.
    // Keep the root plus ROLLING_BACK marker so startup can retry idempotently.
    if (restored) {
        try {
            if (!backup.backup_root.empty() && fs::exists(backup.backup_root)) {
                fs::remove_all(backup.backup_root);
                DirectoryCommit(backup.backup_root.parent_path());
            }
        } catch (const fs::filesystem_error& e) {
            LogPrintf("RestoreShieldedStateDirectoryBackup: failed removing backup root %s: %s\n",
                      fs::PathToString(backup.backup_root),
                      fsbridge::get_filesystem_error_message(e));
            restored = false;
        }
    } else {
        LogPrintf("RestoreShieldedStateDirectoryBackup: preserving incomplete backup root %s for retry\n",
                  fs::PathToString(backup.backup_root));
    }
    if (restored) {
        backup.managed_paths.clear();
        backup.entries.clear();
    }
    return restored;
}

[[nodiscard]] bool DiscardShieldedStateDirectoryBackup(ShieldedStateDirectoryBackup& backup)
{
    try {
        if (!backup.backup_root.empty() && fs::exists(backup.backup_root)) {
            fs::remove_all(backup.backup_root);
            DirectoryCommit(backup.backup_root.parent_path());
        }
    } catch (const fs::filesystem_error& e) {
        LogPrintf("DiscardShieldedStateDirectoryBackup: failed removing backup root %s: %s\n",
                  fs::PathToString(backup.backup_root),
                  fsbridge::get_filesystem_error_message(e));
        return false;
    }
    backup.managed_paths.clear();
    backup.entries.clear();
    return true;
}

[[nodiscard]] std::optional<ShieldedStateDirectoryBackup> BackupShieldedStateDirectories(
    const fs::path& datadir)
{
    ShieldedStateDirectoryBackup backup;
    backup.backup_root = datadir / fs::PathFromString(
        strprintf("%s%s", SHIELDED_SNAPSHOT_BACKUP_PREFIX,
                  GetRandHash().ToString()));
    backup.managed_paths = GetShieldedStateStorePaths(datadir);

    ShieldedSnapshotTxnMarker marker{
        .phase = static_cast<uint8_t>(
            ShieldedSnapshotTxnPhase::PREPARING_BACKUP),
        .backup_name = fs::PathToString(backup.backup_root.filename()),
    };
    if (!WriteShieldedSnapshotTxnMarker(datadir, marker)) {
        LogPrintf("BackupShieldedStateDirectories: failed to persist snapshot transaction marker\n");
        return std::nullopt;
    }

    try {
        for (const fs::path& live_path : backup.managed_paths) {
            if (!fs::exists(live_path)) continue;
            if (backup.entries.empty()) {
                fs::create_directories(backup.backup_root);
                DirectoryCommit(backup.backup_root.parent_path());
            }
            const fs::path backup_path = backup.backup_root / live_path.filename();
            fs::rename(live_path, backup_path);
            DirectoryCommit(live_path.parent_path());
            DirectoryCommit(backup_path.parent_path());
            backup.entries.push_back({live_path, backup_path});
        }
    } catch (const fs::filesystem_error& e) {
        LogPrintf("BackupShieldedStateDirectories: failed staging shielded state backup under %s: %s\n",
                  fs::PathToString(backup.backup_root),
                  fsbridge::get_filesystem_error_message(e));
        if (RestoreShieldedStateDirectoryBackup(
                backup, /*remove_partial_live_paths=*/false)) {
            (void)ClearShieldedSnapshotTxnMarker(datadir);
        }
        return std::nullopt;
    }
    marker.phase = static_cast<uint8_t>(
        ShieldedSnapshotTxnPhase::BACKUP_COMPLETE);
    if (!WriteShieldedSnapshotTxnMarker(datadir, marker)) {
        LogPrintf("BackupShieldedStateDirectories: failed to commit snapshot backup marker\n");
        if (RestoreShieldedStateDirectoryBackup(
                backup, /*remove_partial_live_paths=*/false)) {
            (void)ClearShieldedSnapshotTxnMarker(datadir);
        }
        return std::nullopt;
    }
    return backup;
}

[[nodiscard]] bool RecoverInterruptedShieldedSnapshotTransaction(
    const fs::path& datadir)
{
    const fs::path marker_path{ShieldedSnapshotTxnPath(datadir)};
    std::vector<fs::path> backup_roots;
    try {
        if (fs::exists(datadir)) {
            for (const auto& entry : fs::directory_iterator{datadir}) {
                const std::string name{
                    fs::PathToString(entry.path().filename())};
                if (name.starts_with(SHIELDED_SNAPSHOT_BACKUP_PREFIX)) {
                    backup_roots.push_back(entry.path());
                }
            }
        }
    } catch (const fs::filesystem_error& e) {
        LogPrintf("RecoverInterruptedShieldedSnapshotTransaction: failed enumerating backups: %s\n",
                  fsbridge::get_filesystem_error_message(e));
        return false;
    }
    if (backup_roots.size() > 1) {
        LogPrintf("RecoverInterruptedShieldedSnapshotTransaction: multiple shielded-state backups found\n");
        return false;
    }
    if (!fs::exists(marker_path)) {
        if (!backup_roots.empty()) {
            LogPrintf("RecoverInterruptedShieldedSnapshotTransaction: orphan shielded-state backup without marker: %s\n",
                      fs::PathToString(backup_roots.front()));
            return false;
        }
        return true;
    }
    const auto marker{ReadShieldedSnapshotTxnMarker(datadir)};
    if (!marker.has_value()) {
        LogPrintf("RecoverInterruptedShieldedSnapshotTransaction: malformed transaction marker\n");
        return false;
    }
    const fs::path expected_root{datadir / fs::PathFromString(marker->backup_name)};
    if (!backup_roots.empty() && backup_roots.front() != expected_root) {
        LogPrintf("RecoverInterruptedShieldedSnapshotTransaction: marker/backup name mismatch\n");
        return false;
    }
    const auto snapshot_dir{node::FindSnapshotChainstateDir(datadir)};
    const bool snapshot_committed{
        snapshot_dir.has_value() &&
        fs::exists(*snapshot_dir / node::SNAPSHOT_BLOCKHASH_FILENAME)};
    if (backup_roots.empty()) {
        // A PREPARING marker is durable before the first rename. Crashing in
        // that narrow window leaves all live stores untouched and no backup to
        // restore, so clearing the marker is safe. A ROLLING_BACK marker with
        // no root means every rename and root removal completed before marker
        // cleanup. A committed snapshot similarly makes the new live state
        // authoritative. Other cases are ambiguous and fail closed.
        if (snapshot_committed ||
            marker->phase == static_cast<uint8_t>(
                                 ShieldedSnapshotTxnPhase::PREPARING_BACKUP) ||
            marker->phase == static_cast<uint8_t>(
                                 ShieldedSnapshotTxnPhase::ROLLING_BACK)) {
            return ClearShieldedSnapshotTxnMarker(datadir);
        }
        LogPrintf("RecoverInterruptedShieldedSnapshotTransaction: transaction marker has no backup root\n");
        return false;
    }
    if (fs::exists(expected_root)) {
        try {
            const auto managed{GetShieldedStateStorePaths(datadir)};
            for (const auto& entry : fs::directory_iterator{expected_root}) {
                const bool known{std::ranges::any_of(
                    managed, [&](const fs::path& live_path) {
                        return entry.path().filename() == live_path.filename();
                    })};
                if (!known) {
                    LogPrintf("RecoverInterruptedShieldedSnapshotTransaction: unexpected backup entry %s\n",
                              fs::PathToString(entry.path()));
                    return false;
                }
            }
        } catch (const fs::filesystem_error& e) {
            LogPrintf("RecoverInterruptedShieldedSnapshotTransaction: malformed backup root: %s\n",
                      fsbridge::get_filesystem_error_message(e));
            return false;
        }
    }

    ShieldedStateDirectoryBackup backup;
    backup.backup_root = expected_root;
    for (const fs::path& live_path : GetShieldedStateStorePaths(datadir)) {
        const fs::path backup_path{expected_root / live_path.filename()};
        if (fs::exists(backup_path)) {
            backup.entries.push_back({live_path, backup_path});
        }
    }

    if (snapshot_committed) {
        if (marker->phase == static_cast<uint8_t>(
                                 ShieldedSnapshotTxnPhase::PREPARING_BACKUP)) {
            LogPrintf("RecoverInterruptedShieldedSnapshotTransaction: snapshot committed before backup completion marker\n");
            return false;
        }
        if (!DiscardShieldedStateDirectoryBackup(backup) ||
            !ClearShieldedSnapshotTxnMarker(datadir)) {
            return false;
        }
        LogPrintf("Recovered committed shielded snapshot transaction\n");
        return true;
    }

    if (marker->phase == static_cast<uint8_t>(
                             ShieldedSnapshotTxnPhase::BACKUP_COMPLETE)) {
        ShieldedSnapshotTxnMarker rolling{*marker};
        rolling.phase = static_cast<uint8_t>(
            ShieldedSnapshotTxnPhase::ROLLING_BACK);
        if (!WriteShieldedSnapshotTxnMarker(datadir, rolling)) return false;
        // This is the first rollback attempt: remove every imported live store,
        // including paths that did not exist in the previous state.
        backup.managed_paths = GetShieldedStateStorePaths(datadir);
    } else {
        // PREPARING_BACKUP has not created imported state; ROLLING_BACK may
        // already have restored some paths. Touch only entries still in backup.
        for (const auto& entry : backup.entries) {
            backup.managed_paths.push_back(entry.live_path);
        }
    }
    if (!RestoreShieldedStateDirectoryBackup(backup) ||
        !ClearShieldedSnapshotTxnMarker(datadir)) {
        return false;
    }
    LogPrintf("Rolled back interrupted shielded snapshot transaction\n");
    return true;
}
} // namespace

bool ChainstateManager::RecoverInterruptedShieldedSnapshotForTest(
    const fs::path& datadir)
{
    return RecoverInterruptedShieldedSnapshotTransaction(datadir);
}

ReorgProtectionRuntimeStats ProbeReorgProtectionRuntimeStats()
{
    return ReorgProtectionRuntimeStats{
        .observed_reorgs = g_reorg_protection_observed_reorgs.load(std::memory_order_relaxed),
        .deepest_observed_reorg_depth = g_reorg_protection_deepest_observed_reorg_depth.load(std::memory_order_relaxed),
        .last_observed_reorg_depth = g_reorg_protection_last_observed_reorg_depth.load(std::memory_order_relaxed),
        .last_observed_tip_height = g_reorg_protection_last_observed_tip_height.load(std::memory_order_relaxed),
        .last_observed_fork_height = g_reorg_protection_last_observed_fork_height.load(std::memory_order_relaxed),
        .last_observed_candidate_height = g_reorg_protection_last_observed_candidate_height.load(std::memory_order_relaxed),
        .last_observed_unix = g_reorg_protection_last_observed_unix.load(std::memory_order_relaxed),
        .rejected_reorgs = g_reorg_protection_rejected_reorgs.load(std::memory_order_relaxed),
        .deepest_rejected_reorg_depth = g_reorg_protection_deepest_rejected_reorg_depth.load(std::memory_order_relaxed),
        .last_rejected_reorg_depth = g_reorg_protection_last_rejected_reorg_depth.load(std::memory_order_relaxed),
        .last_rejected_max_reorg_depth = g_reorg_protection_last_rejected_max_reorg_depth.load(std::memory_order_relaxed),
        .last_rejected_tip_height = g_reorg_protection_last_rejected_tip_height.load(std::memory_order_relaxed),
        .last_rejected_fork_height = g_reorg_protection_last_rejected_fork_height.load(std::memory_order_relaxed),
        .last_rejected_candidate_height = g_reorg_protection_last_rejected_candidate_height.load(std::memory_order_relaxed),
        .last_rejected_unix = g_reorg_protection_last_rejected_unix.load(std::memory_order_relaxed),
        .deferred_reorgs = g_reorg_protection_deferred_reorgs.load(std::memory_order_relaxed),
        .deepest_deferred_reorg_depth = g_reorg_protection_deepest_deferred_reorg_depth.load(std::memory_order_relaxed),
        .last_deferred_reorg_depth = g_reorg_protection_last_deferred_reorg_depth.load(std::memory_order_relaxed),
        .last_deferred_required_work_margin = g_reorg_protection_last_deferred_required_work_margin.load(std::memory_order_relaxed),
        .last_deferred_tip_height = g_reorg_protection_last_deferred_tip_height.load(std::memory_order_relaxed),
        .last_deferred_fork_height = g_reorg_protection_last_deferred_fork_height.load(std::memory_order_relaxed),
        .last_deferred_candidate_height = g_reorg_protection_last_deferred_candidate_height.load(std::memory_order_relaxed),
        .last_deferred_unix = g_reorg_protection_last_deferred_unix.load(std::memory_order_relaxed),
    };
}

void ResetReorgProtectionRuntimeStats()
{
    g_reorg_protection_observed_reorgs.store(0, std::memory_order_relaxed);
    g_reorg_protection_deepest_observed_reorg_depth.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_observed_reorg_depth.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_observed_tip_height.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_observed_fork_height.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_observed_candidate_height.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_observed_unix.store(0, std::memory_order_relaxed);
    g_reorg_protection_rejected_reorgs.store(0, std::memory_order_relaxed);
    g_reorg_protection_deepest_rejected_reorg_depth.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_rejected_reorg_depth.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_rejected_max_reorg_depth.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_rejected_tip_height.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_rejected_fork_height.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_rejected_candidate_height.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_rejected_unix.store(0, std::memory_order_relaxed);
    g_reorg_protection_deferred_reorgs.store(0, std::memory_order_relaxed);
    g_reorg_protection_deepest_deferred_reorg_depth.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_deferred_reorg_depth.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_deferred_required_work_margin.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_deferred_tip_height.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_deferred_fork_height.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_deferred_candidate_height.store(0, std::memory_order_relaxed);
    g_reorg_protection_last_deferred_unix.store(0, std::memory_order_relaxed);
}

void RecordObservedReorgDepth(
    const uint32_t reorg_depth,
    const int32_t old_tip_height,
    const int32_t fork_height,
    const int32_t candidate_height)
{
    g_reorg_protection_observed_reorgs.fetch_add(1, std::memory_order_relaxed);

    uint32_t deepest_seen = g_reorg_protection_deepest_observed_reorg_depth.load(std::memory_order_relaxed);
    while (reorg_depth > deepest_seen &&
           !g_reorg_protection_deepest_observed_reorg_depth.compare_exchange_weak(
               deepest_seen,
               reorg_depth,
               std::memory_order_relaxed,
               std::memory_order_relaxed)) {
    }

    g_reorg_protection_last_observed_reorg_depth.store(reorg_depth, std::memory_order_relaxed);
    g_reorg_protection_last_observed_tip_height.store(old_tip_height, std::memory_order_relaxed);
    g_reorg_protection_last_observed_fork_height.store(fork_height, std::memory_order_relaxed);
    g_reorg_protection_last_observed_candidate_height.store(candidate_height, std::memory_order_relaxed);
    g_reorg_protection_last_observed_unix.store(GetTime(), std::memory_order_relaxed);
}

void RecordRejectedReorgDepth(
    const uint32_t reorg_depth,
    const uint32_t max_reorg_depth,
    const int32_t old_tip_height,
    const int32_t fork_height,
    const int32_t candidate_height)
{
    g_reorg_protection_rejected_reorgs.fetch_add(1, std::memory_order_relaxed);

    uint32_t deepest_seen = g_reorg_protection_deepest_rejected_reorg_depth.load(std::memory_order_relaxed);
    while (reorg_depth > deepest_seen &&
           !g_reorg_protection_deepest_rejected_reorg_depth.compare_exchange_weak(
               deepest_seen,
               reorg_depth,
               std::memory_order_relaxed,
               std::memory_order_relaxed)) {
    }

    g_reorg_protection_last_rejected_reorg_depth.store(reorg_depth, std::memory_order_relaxed);
    g_reorg_protection_last_rejected_max_reorg_depth.store(
        max_reorg_depth != kernel::REORG_PROTECTION_DEPTH_DISABLED ? max_reorg_depth : 0,
        std::memory_order_relaxed);
    g_reorg_protection_last_rejected_tip_height.store(old_tip_height, std::memory_order_relaxed);
    g_reorg_protection_last_rejected_fork_height.store(fork_height, std::memory_order_relaxed);
    g_reorg_protection_last_rejected_candidate_height.store(candidate_height, std::memory_order_relaxed);
    g_reorg_protection_last_rejected_unix.store(GetTime(), std::memory_order_relaxed);
}

void RecordDeferredReorgDepth(
    const uint32_t reorg_depth,
    const uint32_t required_work_margin,
    const int32_t old_tip_height,
    const int32_t fork_height,
    const int32_t candidate_height)
{
    g_reorg_protection_deferred_reorgs.fetch_add(1, std::memory_order_relaxed);

    uint32_t deepest_seen = g_reorg_protection_deepest_deferred_reorg_depth.load(std::memory_order_relaxed);
    while (reorg_depth > deepest_seen &&
           !g_reorg_protection_deepest_deferred_reorg_depth.compare_exchange_weak(
               deepest_seen,
               reorg_depth,
               std::memory_order_relaxed,
               std::memory_order_relaxed)) {
    }

    g_reorg_protection_last_deferred_reorg_depth.store(reorg_depth, std::memory_order_relaxed);
    g_reorg_protection_last_deferred_required_work_margin.store(required_work_margin, std::memory_order_relaxed);
    g_reorg_protection_last_deferred_tip_height.store(old_tip_height, std::memory_order_relaxed);
    g_reorg_protection_last_deferred_fork_height.store(fork_height, std::memory_order_relaxed);
    g_reorg_protection_last_deferred_candidate_height.store(candidate_height, std::memory_order_relaxed);
    g_reorg_protection_last_deferred_unix.store(GetTime(), std::memory_order_relaxed);
}

TRACEPOINT_SEMAPHORE(validation, block_connected);
TRACEPOINT_SEMAPHORE(utxocache, flush);
TRACEPOINT_SEMAPHORE(mempool, replaced);
TRACEPOINT_SEMAPHORE(mempool, rejected);

namespace {

[[nodiscard]] bool BlockHasShieldedBundle(const CBlock& block)
{
    return std::any_of(block.vtx.begin(), block.vtx.end(), [](const CTransactionRef& tx) {
        return tx->HasShieldedBundle();
    });
}

[[nodiscard]] bool TryCountShieldedOutputs(const CBlock& block, uint64_t& out_count)
{
    out_count = 0;
    for (const auto& txref : block.vtx) {
        if (!txref->HasShieldedBundle()) continue;
        const CShieldedBundle& bundle = txref->GetShieldedBundle();
        const uint64_t bundle_output_count = bundle.GetShieldedOutputCount();
        if (out_count > std::numeric_limits<uint64_t>::max() - bundle_output_count) {
            return false;
        }
        out_count += bundle_output_count;
    }
    return true;
}

[[nodiscard]] bool CollectRecentShieldedOutputCounts(const Chainstate& chainstate,
                                                     const CBlockIndex* tip,
                                                     std::vector<uint64_t>& counts)
{
    counts.clear();
    const CBlockIndex* cursor = tip;
    for (int depth = 0; depth < SHIELDED_ANCHOR_DEPTH && cursor != nullptr; ++depth) {
        CBlock block;
        if (!chainstate.m_blockman.ReadBlock(block, *cursor)) {
            LogError("CollectRecentShieldedOutputCounts: failed to read block %s\n",
                     cursor->GetBlockHash().ToString());
            return false;
        }
        uint64_t output_count{0};
        if (!TryCountShieldedOutputs(block, output_count)) {
            LogError("CollectRecentShieldedOutputCounts: output count overflow at block %s\n",
                     cursor->GetBlockHash().ToString());
            return false;
        }
        counts.push_back(output_count);
        cursor = cursor->pprev;
    }
    return true;
}

[[nodiscard]] size_t ExpectedShieldedRecentHistoryCount(const CBlockIndex* tip)
{
    size_t count{0};
    const CBlockIndex* cursor = tip;
    while (count < static_cast<size_t>(SHIELDED_ANCHOR_DEPTH) && cursor != nullptr) {
        ++count;
        cursor = cursor->pprev;
    }
    return count;
}

[[nodiscard]] bool UsesShieldedSpendAuthChecks(const CShieldedBundle& bundle)
{
    if (!bundle.HasShieldedInputs()) return false;
    if (!bundle.HasV2Bundle()) return true;
    return bundle.GetTransactionFamily() == shielded::v2::TransactionFamily::V2_SEND;
}

[[nodiscard]] size_t GetShieldedProofPlausibilityOutputCount(const CShieldedBundle& bundle)
{
    if (!bundle.HasV2Bundle()) return bundle.GetShieldedOutputCount();

    const auto* v2_bundle = bundle.GetV2Bundle();
    if (v2_bundle == nullptr) return bundle.GetShieldedOutputCount();

    switch (shielded::v2::GetBundleSemanticFamily(*v2_bundle)) {
    case shielded::v2::TransactionFamily::V2_INGRESS_BATCH: {
        const auto& payload = std::get<shielded::v2::IngressBatchPayload>(v2_bundle->payload);
        return payload.reserve_outputs.size() + payload.ingress_leaves.size();
    }
    default:
        return bundle.GetShieldedOutputCount();
    }
}

[[nodiscard]] bool IsSmile2ProofPlausible(size_t proof_size,
                                          size_t num_inputs,
                                          size_t num_outputs)
{
    static constexpr size_t kSmileMinBase{1024};
    static constexpr size_t kSmileMinPerInput{4096};
    static constexpr size_t kSmileMinPerOutput{2048};

    size_t min_expected = kSmileMinBase;
    const size_t input_bytes = num_inputs * kSmileMinPerInput;
    if (input_bytes / kSmileMinPerInput != num_inputs) return false;
    min_expected += input_bytes;
    const size_t output_bytes = num_outputs * kSmileMinPerOutput;
    if (output_bytes / kSmileMinPerOutput != num_outputs) return false;
    min_expected += output_bytes;

    return proof_size >= min_expected && proof_size <= smile2::MAX_SMILE2_PROOF_BYTES;
}

[[nodiscard]] bool RejectRetiredMatRiCTEnvelopeAfterDisable(const CShieldedBundle& bundle,
                                                            const Consensus::Params& consensus,
                                                            int32_t validation_height,
                                                            std::string& reject_reason)
{
    if (!bundle.HasV2Bundle() || !consensus.IsShieldedMatRiCTDisabled(validation_height)) {
        return true;
    }

    const auto* v2_bundle = bundle.GetV2Bundle();
    if (v2_bundle == nullptr) {
        LogShieldedV2ContextReject("retired-matrict-v2-bundle-null", bundle);
        reject_reason = "bad-shielded-v2-contextual";
        return false;
    }

    const auto& envelope = v2_bundle->header.proof_envelope;
    const bool allow_recovery_matrict =
        shielded::v2::BundleHasSemanticFamily(*v2_bundle, shielded::v2::V2_SPEND_PATH_RECOVERY) &&
        consensus.IsShieldedSpendPathRecoveryActive(validation_height) &&
        envelope.proof_kind == shielded::v2::ProofKind::DIRECT_MATRICT &&
        envelope.membership_proof_kind == shielded::v2::ProofComponentKind::MATRICT &&
        envelope.amount_proof_kind == shielded::v2::ProofComponentKind::RANGE &&
        envelope.balance_proof_kind == shielded::v2::ProofComponentKind::BALANCE;
    if (allow_recovery_matrict) {
        return true;
    }
    if (envelope.proof_kind == shielded::v2::ProofKind::DIRECT_MATRICT ||
        envelope.proof_kind == shielded::v2::ProofKind::BATCH_MATRICT ||
        envelope.membership_proof_kind == shielded::v2::ProofComponentKind::MATRICT ||
        envelope.amount_proof_kind == shielded::v2::ProofComponentKind::RANGE ||
        envelope.balance_proof_kind == shielded::v2::ProofComponentKind::BALANCE) {
        reject_reason = "bad-shielded-matrict-disabled";
        return false;
    }
    return true;
}

[[nodiscard]] bool PrecheckShieldedRingPositions(const CShieldedBundle& bundle,
                                                 uint64_t tree_size,
                                                 std::string& reject_reason)
{
    if (!bundle.HasShieldedInputs()) return true;

    const auto check_ring_positions = [&](const auto& spends, bool allow_singleton) {
        for (const auto& spend : spends) {
            if (allow_singleton) {
                if (spend.ring_positions.size() != 1) {
                    reject_reason = "bad-shielded-ring-positions";
                    return false;
                }
            } else if (!shielded::lattice::IsSupportedRingSize(spend.ring_positions.size())) {
                reject_reason = "bad-shielded-ring-positions";
                return false;
            }
            const size_t required_unique_members = static_cast<size_t>(std::min<uint64_t>(
                tree_size,
                static_cast<uint64_t>(spend.ring_positions.size())));
            std::set<uint64_t> unique_positions;
            for (const uint64_t pos : spend.ring_positions) {
                if (pos >= tree_size) {
                    reject_reason = "bad-shielded-ring-member-position";
                    return false;
                }
                unique_positions.insert(pos);
            }
            if (!allow_singleton && unique_positions.size() < required_unique_members) {
                reject_reason = "bad-shielded-ring-member-insufficient-diversity";
                return false;
            }
        }
        return true;
    };

    if (bundle.HasV2Bundle()) {
        const auto* v2_bundle = bundle.GetV2Bundle();
        if (v2_bundle == nullptr) {
            LogShieldedV2ContextReject("precheck-ring-positions-v2-bundle-null", bundle);
            reject_reason = "bad-shielded-v2-contextual";
            return false;
        }

        switch (shielded::v2::GetBundleSemanticFamily(*v2_bundle)) {
        case shielded::v2::TransactionFamily::V2_SEND: {
            auto witness = shielded::v2::proof::ParseV2SendWitness(*v2_bundle, reject_reason);
            if (!witness.has_value()) return false;
            return check_ring_positions(witness->spends, /*allow_singleton=*/false);
        }
        case shielded::v2::TransactionFamily::V2_INGRESS_BATCH: {
            auto witness = shielded::v2::ParseV2IngressWitness(*v2_bundle, reject_reason);
            if (!witness.has_value()) return false;
            for (const auto& shard : witness->shards) {
                if (!check_ring_positions(shard.spends, /*allow_singleton=*/false)) return false;
            }
            return true;
        }
        case shielded::v2::V2_SPEND_PATH_RECOVERY: {
            auto witness = shielded::v2::proof::ParseSpendPathRecoveryWitness(*v2_bundle, reject_reason);
            if (!witness.has_value()) return false;
            return check_ring_positions(witness->spends, /*allow_singleton=*/true);
        }
        default:
            LogShieldedV2ContextReject("precheck-ring-positions-unsupported-family", bundle);
            reject_reason = "bad-shielded-v2-contextual";
            return false;
        }
    }

    return check_ring_positions(bundle.shielded_inputs, /*allow_singleton=*/false);
}

[[nodiscard]] bool RejectShieldedSmallPrivacyPool(const CShieldedBundle& bundle,
                                                  uint64_t tree_size,
                                                  const Consensus::Params& consensus,
                                                  int32_t validation_height,
                                                  std::string& reject_reason)
{
    if (!bundle.HasShieldedInputs() ||
        !bundle.HasV2Bundle() ||
        !consensus.IsShieldedMatRiCTDisabled(validation_height)) {
        return true;
    }

    const auto* v2_bundle = bundle.GetV2Bundle();
    if (v2_bundle == nullptr) {
        LogShieldedV2ContextReject("reject-small-privacy-pool-v2-bundle-null", bundle);
        reject_reason = "bad-shielded-v2-contextual";
        return false;
    }
    const auto violates_minimum_tree_size = [&](size_t ring_size) {
        const uint64_t minimum_tree_size = shielded::ringct::GetMinimumPrivacyTreeSize(ring_size);
        return minimum_tree_size > 0 && tree_size < minimum_tree_size;
    };

        switch (shielded::v2::GetBundleSemanticFamily(*v2_bundle)) {
        case shielded::v2::TransactionFamily::V2_SEND: {
            auto witness = shielded::v2::proof::ParseV2SendWitness(*v2_bundle, reject_reason);
            if (!witness.has_value()) return false;
            const size_t ring_size = witness->spends.empty() ? 0 : witness->spends.front().ring_positions.size();
        if (violates_minimum_tree_size(ring_size)) {
            reject_reason = "bad-shielded-anonymity-pool-size";
            return false;
        }
        return true;
    }
    case shielded::v2::TransactionFamily::V2_INGRESS_BATCH: {
        auto witness = shielded::v2::ParseV2IngressWitness(*v2_bundle, reject_reason);
        if (!witness.has_value()) return false;
        for (const auto& shard : witness->shards) {
            for (const auto& spend : shard.spends) {
                if (violates_minimum_tree_size(spend.ring_positions.size())) {
                    reject_reason = "bad-shielded-anonymity-pool-size";
                    return false;
                }
            }
        }
        return true;
    }
    default:
        return true;
    }
}

[[nodiscard]] bool PrecheckShieldedProofPlausibility(const CShieldedBundle& bundle,
                                                     std::string& reject_reason)
{
    if (!bundle.HasShieldedInputs()) return true;
    if (bundle.GetProofSize() == 0) {
        reject_reason = "bad-shielded-proof-missing";
        return false;
    }

    if (bundle.HasV2Bundle()) {
        const auto* v2_bundle = bundle.GetV2Bundle();
        if (v2_bundle != nullptr) {
            const auto proof_kind = v2_bundle->header.proof_envelope.proof_kind;
            const auto semantic_family = shielded::v2::GetBundleSemanticFamily(*v2_bundle);
            const bool uses_smile_plausibility =
                proof_kind == shielded::v2::ProofKind::DIRECT_SMILE ||
                proof_kind == shielded::v2::ProofKind::BATCH_SMILE ||
                proof_kind == shielded::v2::ProofKind::GENERIC_SMILE ||
                (proof_kind == shielded::v2::ProofKind::GENERIC_OPAQUE &&
                 (semantic_family == shielded::v2::TransactionFamily::V2_SEND ||
                  semantic_family == shielded::v2::TransactionFamily::V2_INGRESS_BATCH));
            if (uses_smile_plausibility) {
                if (!IsSmile2ProofPlausible(bundle.GetProofSize(),
                                            bundle.GetShieldedInputCount(),
                                            GetShieldedProofPlausibilityOutputCount(bundle))) {
                    reject_reason = "bad-shielded-proof-implausible";
                    return false;
                }
                return true;
            }
        }
    }

    // MatRiCT plausibility (legacy path):
    // Cheap DoS guard: reject implausibly-small proofs before expensive decode/verify.
    // Conservative lower bounds (well below actual proof sizes) to catch trivially
    // fake proofs while not rejecting valid max-size (16-in/16-out) proofs within
    // the MAX_SHIELDED_PROOF_BYTES limit of 1.5 MB.
    //   Ring signature: ~50 KB/input (MODULE_RANK × RING_SIZE polynomial vectors)
    //   Range proof: ~15 KB/output (VALUE_BITS bit-proof challenge/response pairs)
    //   Balance proof + binding: ~2 KB base overhead
    static constexpr size_t kMinBaseBytes{2048};
    static constexpr size_t kMinBytesPerInput{51200};
    static constexpr size_t kMinBytesPerOutput{15360};
    size_t min_expected{kMinBaseBytes};
    const size_t input_bytes = bundle.GetShieldedInputCount() * kMinBytesPerInput;
    if (input_bytes > std::numeric_limits<size_t>::max() - min_expected) {
        reject_reason = "bad-shielded-proof-implausible";
        return false;
    }
    min_expected += input_bytes;
    const size_t output_bytes = GetShieldedProofPlausibilityOutputCount(bundle) * kMinBytesPerOutput;
    if (output_bytes > std::numeric_limits<size_t>::max() - min_expected) {
        reject_reason = "bad-shielded-proof-implausible";
        return false;
    }
    min_expected += output_bytes;

    if (bundle.GetProofSize() < min_expected) {
        reject_reason = "bad-shielded-proof-implausible";
        return false;
    }
    return true;
}

template <typename Spend>
[[nodiscard]] bool ValidateShieldedAccountRegistrySpends(
    Span<const Spend> spends,
    const uint256& account_registry_anchor,
    const ChainstateManager& chainman,
    std::string& reject_reason)
    EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);
    if (spends.empty()) {
        return true;
    }
    if (!chainman.IsShieldedAccountRegistryRootValid(account_registry_anchor)) {
        reject_reason = "bad-shielded-account-registry-anchor";
        return false;
    }

    for (const auto& spend : spends) {
        if (spend.account_leaf_commitment.IsNull() ||
            !spend.account_registry_proof.IsValid() ||
            spend.account_registry_proof.account_leaf_commitment != spend.account_leaf_commitment ||
            !shielded::registry::VerifyShieldedAccountRegistrySpendWitness(
                spend.account_registry_proof,
                chainman.GetShieldedAccountRegistry(),
                account_registry_anchor)) {
            reject_reason = "bad-shielded-account-registry-proof";
            return false;
        }
    }
    return true;
}

[[nodiscard]] bool ValidateShieldedAccountRegistryRefs(const CShieldedBundle& bundle,
                                                       const ChainstateManager& chainman,
                                                       std::string& reject_reason)
    EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);
    if (!bundle.HasV2Bundle()) return true;

    const auto* v2_bundle = bundle.GetV2Bundle();
    if (v2_bundle == nullptr) {
        LogShieldedV2ContextReject("account-registry-refs-v2-bundle-null", bundle);
        reject_reason = "bad-shielded-v2-contextual";
        return false;
    }

    switch (shielded::v2::GetBundleSemanticFamily(*v2_bundle)) {
    case shielded::v2::TransactionFamily::V2_SEND: {
        const auto& payload = std::get<shielded::v2::SendPayload>(v2_bundle->payload);
        return ValidateShieldedAccountRegistrySpends(
            Span<const shielded::v2::SpendDescription>{payload.spends.data(), payload.spends.size()},
            payload.account_registry_anchor,
            chainman,
            reject_reason);
    }
    case shielded::v2::TransactionFamily::V2_INGRESS_BATCH: {
        const auto& payload = std::get<shielded::v2::IngressBatchPayload>(v2_bundle->payload);
        return ValidateShieldedAccountRegistrySpends(
            Span<const shielded::v2::ConsumedAccountLeafSpend>{payload.consumed_spends.data(),
                                                               payload.consumed_spends.size()},
            payload.account_registry_anchor,
            chainman,
            reject_reason);
    }
    case shielded::v2::V2_SPEND_PATH_RECOVERY:
    case shielded::v2::TransactionFamily::V2_RECOVERY_EXIT:
    case shielded::v2::TransactionFamily::V2_EGRESS_BATCH:
    case shielded::v2::TransactionFamily::V2_REBALANCE:
    case shielded::v2::TransactionFamily::V2_SETTLEMENT_ANCHOR:
    case shielded::v2::TransactionFamily::V2_LIFECYCLE:
    case shielded::v2::TransactionFamily::V2_GENERIC:
        return true;
    }
    LogShieldedV2ContextReject("account-registry-refs-unsupported-family", bundle);
    reject_reason = "bad-shielded-v2-contextual";
    return false;
}

[[nodiscard]] bool RebuildShieldedState(const Chainstate& chainstate,
                                        const CBlockIndex* tip,
                                        shielded::ShieldedMerkleTree& tree,
                                        ShieldedPoolBalance& pool,
                                        std::vector<Nullifier>* all_nullifiers = nullptr,
                                        std::map<uint256, smile2::CompactPublicAccount>* public_accounts = nullptr,
                                        std::map<uint256, uint256>* account_leaf_commitments = nullptr,
                                        std::vector<uint256>* recovery_exit_commitments = nullptr,
                                        ShieldedUnshieldVelocity* unshield_velocity = nullptr)
{
    const auto storage_mode = tree.GetIndexStorageMode();
    const auto& consensus = chainstate.m_chainman.GetConsensus();
    tree = shielded::ShieldedMerkleTree{storage_mode};
    pool = ShieldedPoolBalance{};
    if (all_nullifiers) all_nullifiers->clear();
    if (public_accounts) public_accounts->clear();
    if (account_leaf_commitments) account_leaf_commitments->clear();
    if (recovery_exit_commitments) recovery_exit_commitments->clear();
    if (unshield_velocity) unshield_velocity->Clear();
    if (tip == nullptr) return true;

    std::vector<const CBlockIndex*> chain;
    chain.reserve(tip->nHeight + 1);
    for (const CBlockIndex* pindex = tip; pindex != nullptr; pindex = pindex->pprev) {
        chain.push_back(pindex);
    }
    std::reverse(chain.begin(), chain.end());

    // Replaying the whole chain to rebuild shielded state is O(chain) and can take minutes-to-hours
    // on large chains or slow disks, during which RPC stays at -28. Log periodic progress so a long
    // (or stalled) rebuild is observable rather than looking dead -- if the height stops advancing
    // with no disk/CPU, the last line shows exactly where it got stuck.
    const size_t total_blocks = chain.size();
    const auto rebuild_start = std::chrono::steady_clock::now();
    auto last_progress_log = rebuild_start;
    size_t blocks_processed = 0;
    LogPrintf("RebuildShieldedState: replaying %u blocks (genesis -> height %d) to rebuild shielded state...\n",
              static_cast<unsigned int>(total_blocks),
              tip->nHeight);

    for (const CBlockIndex* pindex : chain) {
        const bool use_nonced_bridge_tag =
            UseNoncedShieldedBridgeTags(consensus, pindex->nHeight);
        CBlock block;
        if (!chainstate.m_blockman.ReadBlock(block, *pindex)) {
            LogError("RebuildShieldedState: failed to read block %s\n", pindex->GetBlockHash().ToString());
            return false;
        }
        const CAmount pool_start = pool.GetBalance();
        for (const auto& txref : block.vtx) {
            const CTransaction& tx = *txref;
            if (!tx.HasShieldedBundle()) continue;
            const CShieldedBundle& bundle = tx.GetShieldedBundle();
            std::string height_gate_reject;
            if (!RejectShieldedHeightGateViolation(tx,
                                                   consensus,
                                                   pindex->nHeight,
                                                   height_gate_reject)) {
                LogError("RebuildShieldedState: shielded height-gate violation at %d: %s\n",
                         pindex->nHeight,
                         height_gate_reject);
                return false;
            }

            if (!ApplyShieldedStateEffects(bundle,
                                           use_nonced_bridge_tag,
                                           pindex->GetBlockHash(),
                                           tree,
                                           pool,
                                           all_nullifiers,
                                           public_accounts,
                                           account_leaf_commitments,
                                           recovery_exit_commitments)) {
                return false;
            }
        }
        if (unshield_velocity &&
            consensus.IsShieldedUnshieldVelocityCapActive(pindex->nHeight)) {
            const CAmount pool_end = pool.GetBalance();
            const CAmount block_net_egress = pool_start > pool_end ? pool_start - pool_end : 0;
            unshield_velocity->RecordBlock(pindex->nHeight, block_net_egress);
            unshield_velocity->Prune(pindex->nHeight -
                2 * static_cast<int32_t>(consensus.nShieldedUnshieldVelocityWindowBlocks));
        }

        ++blocks_processed;
        const auto now = std::chrono::steady_clock::now();
        if (now - last_progress_log >= std::chrono::seconds{15}) {
            last_progress_log = now;
            const double pct = total_blocks > 0
                                   ? 100.0 * static_cast<double>(blocks_processed) / static_cast<double>(total_blocks)
                                   : 100.0;
            LogPrintf("RebuildShieldedState: replaying shielded state %u/%u (height=%d, %.1f%%)\n",
                      static_cast<unsigned int>(blocks_processed),
                      static_cast<unsigned int>(total_blocks),
                      pindex->nHeight,
                      pct);
        }
    }
    const auto elapsed_s = std::chrono::duration_cast<std::chrono::seconds>(
                               std::chrono::steady_clock::now() - rebuild_start)
                               .count();
    LogPrintf("RebuildShieldedState: replayed %u blocks to height %d in %llds\n",
              static_cast<unsigned int>(total_blocks),
              tip->nHeight,
              static_cast<long long>(elapsed_s));
    return true;
}

[[nodiscard]] bool ApplyShieldedStateEffects(const CShieldedBundle& bundle,
                                             bool use_nonced_bridge_tag,
                                             const uint256& block_hash,
                                             shielded::ShieldedMerkleTree& tree,
                                             ShieldedPoolBalance& pool,
                                             std::vector<Nullifier>* all_nullifiers,
                                             std::map<uint256, smile2::CompactPublicAccount>* public_accounts,
                                             std::map<uint256, uint256>* account_leaf_commitments,
                                             std::vector<uint256>* recovery_exit_commitments)
{
    if (all_nullifiers) {
        const auto nullifiers = CollectShieldedNullifiers(bundle);
        all_nullifiers->insert(all_nullifiers->end(), nullifiers.begin(), nullifiers.end());
    }
    if (!CollectRecoveryExitRetirements(bundle, all_nullifiers, recovery_exit_commitments)) {
        LogError("ApplyShieldedStateEffects: failed to derive recovery-exit retirements at block %s\n",
                 block_hash.ToString());
        return false;
    }
    if (public_accounts) {
        for (const auto& [commitment, account] : CollectShieldedOutputSmileAccounts(bundle)) {
            (*public_accounts)[commitment] = account;
        }
    }
    if (account_leaf_commitments) {
        const auto leaf_entries =
            CollectShieldedOutputAccountLeafEntries(bundle, use_nonced_bridge_tag);
        if (!leaf_entries.has_value()) {
            LogError("ApplyShieldedStateEffects: failed to rebuild account-leaf entries at block %s\n",
                     block_hash.ToString());
            return false;
        }
        for (const auto& [commitment, account_leaf_commitment] : *leaf_entries) {
            (*account_leaf_commitments)[commitment] = account_leaf_commitment;
        }
    }

    try {
        for (const auto& commitment : CollectShieldedOutputCommitments(bundle)) {
            tree.Append(commitment);
        }
    } catch (const std::exception& e) {
        LogError("ApplyShieldedStateEffects: failed to append shielded commitment at block %s: %s\n",
                 block_hash.ToString(),
                 e.what());
        return false;
    }
    std::string reject_reason;
    const auto value_balance = TryGetShieldedStateValueBalance(bundle, reject_reason);
    if (!value_balance.has_value()) {
        LogError("ApplyShieldedStateEffects: failed to derive pool balance delta at block %s: %s\n",
                 block_hash.ToString(),
                 reject_reason);
        return false;
    }
    if (!pool.ApplyValueBalance(*value_balance)) {
        LogError("ApplyShieldedStateEffects: turnstile invariant failed at block %s\n",
                 block_hash.ToString());
        return false;
    }
    return true;
}

[[nodiscard]] std::string DescribeShieldedAuditFamily(const CShieldedBundle& bundle)
{
    if (const auto family = bundle.GetTransactionFamily()) {
        return shielded::v2::GetTransactionFamilyName(*family);
    }
    if (bundle.IsShieldOnly()) return "legacy_shield";
    if (bundle.IsUnshieldOnly()) return "legacy_unshield";
    if (bundle.IsFullyShielded()) return "legacy_direct";
    return "legacy_mixed";
}

[[nodiscard]] std::string DescribeShieldedAuditProofKind(const CShieldedBundle& bundle)
{
    const auto* v2_bundle = bundle.GetV2Bundle();
    if (v2_bundle == nullptr) return "legacy";
    return shielded::v2::GetProofKindName(v2_bundle->header.proof_envelope.proof_kind);
}

[[nodiscard]] static bool ReadShieldedRebuildBlock(const Chainstate& chainstate,
                                                   const CBlockIndex& index,
                                                   CBlock& block)
{
    if (chainstate.m_blockman.ReadBlock(block, index)) {
        return true;
    }

    std::optional<FlatFilePos> fallback_pos;
    {
        LOCK(::cs_main);
        if ((index.nStatus & BLOCK_HAVE_DATA) == 0 &&
            index.nDataPos >= node::BLOCK_SERIALIZATION_HEADER_SIZE) {
            fallback_pos = FlatFilePos(index.nFile, index.nDataPos);
        }
    }
    if (!fallback_pos.has_value()) {
        return false;
    }

    return chainstate.m_blockman.ReadBlock(block, *fallback_pos, index.GetBlockHash());
}

[[nodiscard]] static bool ShieldedGapRecoveryCanSkipBlockBody(
    const CBlockIndex& index)
{
    // CheckTransaction rejects a shielded bundle in coinbase. Therefore an
    // accepted one-transaction block cannot change any shielded state. This
    // lets bounded startup recovery repair a torn tip pin even when an
    // unclean stop lost the recent block-file position from the block index.
    // Any block that could contain a second transaction still requires its
    // body and fails closed when the body is unavailable.
    return index.nTx == 1;
}

[[nodiscard]] static bool ShieldedRebuildBlockAvailable(const Chainstate& chainstate,
                                                        const CBlockIndex& index)
    EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);

    if (chainstate.m_blockman.IsBlockPruned(index)) {
        return false;
    }
    if ((index.nStatus & BLOCK_HAVE_DATA) == 0 &&
        index.nDataPos < node::BLOCK_SERIALIZATION_HEADER_SIZE) {
        return false;
    }
    return true;
}

[[nodiscard]] static bool ShieldedRebuildHistoryBlocksAvailable(const Chainstate& chainstate,
                                                                const CBlockIndex* tip)
    EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);

    const CBlockIndex* cursor = tip;
    for (int depth = 0; depth < SHIELDED_ANCHOR_DEPTH && cursor != nullptr; ++depth) {
        if (!ShieldedGapRecoveryCanSkipBlockBody(*cursor) &&
            !ShieldedRebuildBlockAvailable(chainstate, *cursor)) {
            return false;
        }
        cursor = cursor->pprev;
    }
    return true;
}

[[nodiscard]] static bool ShieldedFullRebuildBlocksAvailable(const Chainstate& chainstate,
                                                             const CBlockIndex* tip)
    EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);

    for (const CBlockIndex* cursor = tip; cursor != nullptr; cursor = cursor->pprev) {
        if (!ShieldedRebuildBlockAvailable(chainstate, *cursor)) {
            return false;
        }
    }
    return true;
}

[[nodiscard]] static bool RebuildRecentShieldedUnshieldVelocity(const Chainstate& chainstate,
                                                                const CBlockIndex* tip,
                                                                const Consensus::Params& consensus,
                                                                ShieldedUnshieldVelocity& velocity,
                                                                std::string& error)
    EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);
    velocity.Clear();
    error.clear();
    if (tip == nullptr) return true;

    const int64_t keep_from_height =
        static_cast<int64_t>(tip->nHeight) -
        2 * static_cast<int64_t>(consensus.nShieldedUnshieldVelocityWindowBlocks);
    const int64_t first_height = std::max<int64_t>(0, keep_from_height);
    std::vector<const CBlockIndex*> chain;
    for (const CBlockIndex* cursor = tip;
         cursor != nullptr && cursor->nHeight >= first_height;
         cursor = cursor->pprev) {
        if (!ShieldedGapRecoveryCanSkipBlockBody(*cursor) &&
            !ShieldedRebuildBlockAvailable(chainstate, *cursor)) {
            error = strprintf("block unavailable at height=%d hash=%s",
                              cursor->nHeight,
                              cursor->GetBlockHash().ToString());
            return false;
        }
        chain.push_back(cursor);
    }
    std::reverse(chain.begin(), chain.end());

    for (const CBlockIndex* pindex : chain) {
        if (!consensus.IsShieldedUnshieldVelocityCapActive(pindex->nHeight)) {
            continue;
        }
        if (ShieldedGapRecoveryCanSkipBlockBody(*pindex)) {
            velocity.RecordBlock(pindex->nHeight, 0);
            velocity.Prune(pindex->nHeight -
                2 * static_cast<int32_t>(consensus.nShieldedUnshieldVelocityWindowBlocks));
            continue;
        }
        CBlock block;
        if (!ReadShieldedRebuildBlock(chainstate, *pindex, block)) {
            error = strprintf("failed to read block at height=%d hash=%s",
                              pindex->nHeight,
                              pindex->GetBlockHash().ToString());
            return false;
        }
        CAmount block_value_balance{0};
        for (const auto& txref : block.vtx) {
            if (!txref->HasShieldedBundle()) continue;
            std::string reject_reason;
            const auto value_balance =
                TryGetShieldedStateValueBalance(txref->GetShieldedBundle(), reject_reason);
            if (!value_balance.has_value()) {
                error = strprintf("failed to derive value balance at height=%d hash=%s: %s",
                                  pindex->nHeight,
                                  pindex->GetBlockHash().ToString(),
                                  reject_reason);
                return false;
            }
            const auto next_value_balance = CheckedAdd(block_value_balance, *value_balance);
            if (!next_value_balance || !MoneyRangeSigned(*next_value_balance)) {
                error = strprintf("value balance overflow at height=%d hash=%s",
                                  pindex->nHeight,
                                  pindex->GetBlockHash().ToString());
                return false;
            }
            block_value_balance = *next_value_balance;
        }
        // Match ConnectBlock/RebuildShieldedState exactly: once the velocity
        // cap is active every connected block has a deterministic log entry,
        // including transparent zero-egress blocks. The zero entries are part
        // of the state pin, make disconnect undo exact, and prevent fast-start
        // repair from producing a different serialized velocity state.
        velocity.RecordBlock(pindex->nHeight, block_value_balance > 0 ? block_value_balance : 0);
        velocity.Prune(pindex->nHeight -
            2 * static_cast<int32_t>(consensus.nShieldedUnshieldVelocityWindowBlocks));
    }

    velocity.Prune(tip->nHeight -
        2 * static_cast<int32_t>(consensus.nShieldedUnshieldVelocityWindowBlocks));
    return true;
}

[[nodiscard]] static bool ShieldedUnshieldVelocityStateRequiredAtTip(
    const Consensus::Params& consensus,
    int32_t tip_height)
{
    if (consensus.nShieldedUnshieldVelocityActivationHeight == std::numeric_limits<int32_t>::max()) {
        return false;
    }
    if (tip_height < consensus.nShieldedUnshieldVelocityActivationHeight) {
        return false;
    }
    if (consensus.IsShieldedUnshieldVelocityCapActive(tip_height)) {
        return true;
    }
    if (consensus.nShieldedUnshieldVelocityEndHeight == std::numeric_limits<int32_t>::max()) {
        return true;
    }
    if (consensus.nMaxReorgDepth == std::numeric_limits<uint32_t>::max()) {
        return true;
    }
    const int64_t retention_limit =
        static_cast<int64_t>(consensus.nShieldedUnshieldVelocityEndHeight) +
        static_cast<int64_t>(consensus.nMaxReorgDepth);
    return static_cast<int64_t>(tip_height) < retention_limit;
}

//! Pin a prune-retention lock so the fast-path shielded disconnect of any reorg up to nMaxReorgDepth
//! never needs a pruned block (issue #35 defense-in-depth). No-op unless this is the active
//! chainstate, pruning is enabled, AND shielded state is live. Pruning POLICY ONLY -- it can only
//! prevent pruning of in-window block files, never change validation/consensus and never cause extra
//! pruning. The deeper genesis-walk full-rebuild fallback is handled separately by the
//! ShieldedFullRebuildBlocksAvailable retain guards; this lock makes the common fast path robust and
//! is load-bearing on chains where nMaxReorgDepth + SHIELDED_ANCHOR_DEPTH exceeds MIN_BLOCKS_TO_KEEP.
static void UpdateShieldedPruneRetentionLock(Chainstate& chainstate) EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);
    ChainstateManager& chainman = chainstate.m_chainman;
    if (&chainstate != &chainman.ActiveChainstate()) return;   // active chain only
    if (!chainstate.m_blockman.IsPruneMode()) return;          // no-op when not pruning
    if (!chainman.HasShieldedState()) return;                  // no-op on non-shielded chains
    const CBlockIndex* tip = chainstate.m_chain.Tip();
    if (tip == nullptr) return;

    const uint32_t max_reorg = chainman.GetConsensus().nMaxReorgDepth;
    const uint64_t reorg_window = (max_reorg == std::numeric_limits<uint32_t>::max())
        ? MIN_BLOCKS_TO_KEEP
        : static_cast<uint64_t>(max_reorg) + static_cast<uint64_t>(SHIELDED_ANCHOR_DEPTH);
    const uint64_t window = std::max<uint64_t>(MIN_BLOCKS_TO_KEEP, reorg_window);
    const uint64_t tip_height = static_cast<uint64_t>(tip->nHeight);
    const uint64_t height_first = (tip_height > window) ? (tip_height - window) : 1;

    node::PruneLockInfo lock_info;
    lock_info.desc = "shielded anchor retention (issue #35)";
    lock_info.height_first = height_first;
    // height_last left default (max): we only pin a lower bound. temporary (default): recomputed each
    // ConnectTip from the tip height, so it never persists a stale height and always allows pruning of
    // block files older than the window.
    chainstate.m_blockman.UpdatePruneLock("shielded", lock_info);
}

[[nodiscard]] static bool CollectShieldedAnchorHistoryFromTree(
    const Chainstate& chainstate,
    const CBlockIndex* tip,
    const shielded::ShieldedMerkleTree& current_tree,
    std::deque<uint256>& rebuilt_roots)
{
    rebuilt_roots.clear();
    const uint256 current_root = current_tree.Root();
    if (!current_root.IsNull()) {
        rebuilt_roots.push_back(current_root);
    }

    shielded::ShieldedMerkleTree cursor_tree = current_tree;
    if (!cursor_tree.DetachToMemoryOnly()) {
        LogError("CollectShieldedAnchorHistoryFromTree: failed to detach scratch tree at height=%d hash=%s\n",
                 tip != nullptr ? tip->nHeight : -1,
                 tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
        return false;
    }

    const CBlockIndex* cursor = tip;
    for (int depth = 0; depth < SHIELDED_ANCHOR_DEPTH && cursor != nullptr; ++depth) {
        if (ShieldedGapRecoveryCanSkipBlockBody(*cursor)) {
            const uint256 rebuilt_root = cursor_tree.Root();
            if (!rebuilt_root.IsNull()) rebuilt_roots.push_back(rebuilt_root);
            cursor = cursor->pprev;
            continue;
        }
        CBlock block;
        if (!ReadShieldedRebuildBlock(chainstate, *cursor, block)) {
            LogError("CollectShieldedAnchorHistoryFromTree: failed to read block %s\n",
                     cursor->GetBlockHash().ToString());
            return false;
        }
        uint64_t output_count{0};
        if (!TryCountShieldedOutputs(block, output_count)) {
            LogError("CollectShieldedAnchorHistoryFromTree: output count overflow at block %s\n",
                     cursor->GetBlockHash().ToString());
            return false;
        }
        if (output_count > 0) {
            if (!cursor_tree.RemoveLast(output_count)) {
                LogError("CollectShieldedAnchorHistoryFromTree: failed to remove %" PRIu64 " outputs at block %s\n",
                         output_count,
                         cursor->GetBlockHash().ToString());
                return false;
            }
        }
        const uint256 rebuilt_root = cursor_tree.Root();
        if (!rebuilt_root.IsNull()) {
            rebuilt_roots.push_back(rebuilt_root);
        }
        cursor = cursor->pprev;
    }
    return true;
}

[[nodiscard]] static bool NeedsShieldedProofCheck(const CShieldedBundle& bundle)
{
    return bundle.HasShieldedInputs() ||
           (bundle.HasV2Bundle() &&
            (bundle.GetTransactionFamily() == shielded::v2::TransactionFamily::V2_SEND ||
             bundle.GetTransactionFamily() == shielded::v2::V2_SPEND_PATH_RECOVERY ||
             bundle.GetTransactionFamily() == shielded::v2::TransactionFamily::V2_EGRESS_BATCH ||
             bundle.GetTransactionFamily() == shielded::v2::TransactionFamily::V2_SETTLEMENT_ANCHOR ||
             bundle.GetTransactionFamily() == shielded::v2::TransactionFamily::V2_LIFECYCLE));
}

[[nodiscard]] static bool NeedsShieldedProofCheck(const CShieldedBundle& bundle,
                                                  const Consensus::Params& consensus,
                                                  int32_t validation_height)
{
    if (NeedsShieldedProofCheck(bundle)) return true;
    return bundle.HasV2Bundle() &&
           bundle.GetTransactionFamily() == shielded::v2::TransactionFamily::V2_REBALANCE &&
           consensus.IsShieldedMatRiCTDisabled(validation_height);
}

static void RefreshShieldedValidationSnapshots(
    const ChainstateManager& chainman,
    std::shared_ptr<const shielded::ShieldedMerkleTree>& shielded_tree_snapshot,
    std::shared_ptr<const std::map<uint256, smile2::CompactPublicAccount>>&
        shielded_smile_public_accounts_snapshot,
    std::shared_ptr<const std::map<uint256, uint256>>&
        shielded_account_leaf_commitments_snapshot) EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);
    shielded_tree_snapshot =
        std::make_shared<const shielded::ShieldedMerkleTree>(chainman.GetShieldedMerkleTree());
    shielded_smile_public_accounts_snapshot = chainman.GetShieldedSmilePublicAccountsSnapshot();
    shielded_account_leaf_commitments_snapshot =
        chainman.GetShieldedAccountLeafCommitmentsSnapshot();
}

[[nodiscard]] static bool IsRecoverableShieldedProofReject(const std::string& reject_reason)
{
    return reject_reason == "bad-shielded-proof" ||
           reject_reason == "bad-shielded-ring-member-position" ||
           reject_reason == "bad-shielded-ring-tree-unavailable" ||
           reject_reason == "bad-smile2-ring-member-account" ||
           reject_reason == "bad-smile2-ring-member-public-account" ||
           reject_reason == "bad-smile2-ring-member-account-leaf" ||
           reject_reason == "bad-smile2-shared-ring";
}

[[nodiscard]] static bool TryAutoRepairShieldedAnchorHistory(
    ChainstateManager& chainman,
    const char* context,
    const uint256& work_id) EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);
    if (!chainman.MarkShieldedAutoRepairAttempt(ShieldedAutoRepairKind::ANCHOR_HISTORY)) {
        LogPrintf("%s: skipping shielded anchor-history repair for %s at unchanged shielded state generation\n",
                  context,
                  work_id.ToString());
        return false;
    }
    LogPrintf("%s: attempting one-shot shielded anchor-history repair for %s\n",
              context,
              work_id.ToString());
    chainman.RunShieldedAutoRepairHookForTest(ShieldedAutoRepairKind::ANCHOR_HISTORY);
    if (!chainman.RepairShieldedAnchorHistoryFromActiveChain()) {
        LogPrintf("%s: shielded anchor-history repair failed for %s\n",
                  context,
                  work_id.ToString());
        return false;
    }
    LogPrintf("%s: shielded anchor-history repair completed for %s\n",
              context,
              work_id.ToString());
    return true;
}

[[nodiscard]] static bool TryAutoRebuildShieldedState(
    ChainstateManager& chainman,
    const char* context,
    const uint256& work_id) EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);
    if (!chainman.MarkShieldedAutoRepairAttempt(ShieldedAutoRepairKind::STATE_REBUILD)) {
        LogPrintf("%s: skipping shielded state rebuild for %s at unchanged shielded state generation\n",
                  context,
                  work_id.ToString());
        return false;
    }
    LogPrintf("%s: attempting one-shot shielded state rebuild for %s\n",
              context,
              work_id.ToString());
    chainman.RunShieldedAutoRepairHookForTest(ShieldedAutoRepairKind::STATE_REBUILD);
    if (!chainman.RebuildShieldedStateFromActiveChain()) {
        LogPrintf("%s: shielded state rebuild failed for %s\n",
                  context,
                  work_id.ToString());
        return false;
    }
    LogPrintf("%s: shielded state rebuild completed for %s\n",
              context,
              work_id.ToString());
    return true;
}

[[nodiscard]] static std::optional<std::string> RevalidateBlockShieldedProofsAfterRepair(
    const CBlock& block,
    const Consensus::Params& consensus,
    int32_t height,
    const std::shared_ptr<const shielded::ShieldedMerkleTree>& shielded_tree_snapshot,
    const std::shared_ptr<const std::map<uint256, smile2::CompactPublicAccount>>&
        shielded_smile_public_accounts_snapshot,
    const std::shared_ptr<const std::map<uint256, uint256>>&
        shielded_account_leaf_commitments_snapshot) EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);
    for (const auto& tx_ref : block.vtx) {
        const CTransaction& tx = *tx_ref;
        if (!tx.HasShieldedBundle()) continue;
        const CShieldedBundle& bundle = tx.GetShieldedBundle();
        if (!NeedsShieldedProofCheck(bundle, consensus, height)) continue;

        std::shared_ptr<const shielded::ringct::MatRiCTProof> parsed_spend_auth_proof;
        if (bundle.HasShieldedInputs() && UsesShieldedSpendAuthChecks(bundle)) {
            std::string spend_auth_reject;
            auto parsed = ParseShieldedSpendAuthProof(bundle, spend_auth_reject);
            if (!parsed.has_value()) {
                return spend_auth_reject;
            }
            parsed_spend_auth_proof = *parsed;
        }

        CShieldedProofCheck proof_check(tx,
                                        consensus,
                                        height,
                                        shielded_tree_snapshot,
                                        shielded_smile_public_accounts_snapshot,
                                        shielded_account_leaf_commitments_snapshot,
                                        parsed_spend_auth_proof);
        if (auto err = proof_check()) {
            return err;
        }
    }
    return std::nullopt;
}

[[nodiscard]] bool RebuildShieldedSmilePublicAccountState(
    const Chainstate& chainstate,
    const CBlockIndex* tip,
    std::map<uint256, smile2::CompactPublicAccount>& public_accounts)
{
    public_accounts.clear();
    if (tip == nullptr) return true;

    std::vector<const CBlockIndex*> chain;
    chain.reserve(tip->nHeight + 1);
    for (const CBlockIndex* pindex = tip; pindex != nullptr; pindex = pindex->pprev) {
        chain.push_back(pindex);
    }
    std::reverse(chain.begin(), chain.end());

    for (const CBlockIndex* pindex : chain) {
        CBlock block;
        if (!ReadShieldedRebuildBlock(chainstate, *pindex, block)) {
            LogError("RebuildShieldedSmilePublicAccountState: failed to read block %s\n",
                     pindex->GetBlockHash().ToString());
            return false;
        }
        for (const auto& txref : block.vtx) {
            if (!txref->HasShieldedBundle()) continue;
            for (const auto& [commitment, account] :
                 CollectShieldedOutputSmileAccounts(txref->GetShieldedBundle())) {
                public_accounts[commitment] = account;
            }
        }
    }
    return true;
}

[[nodiscard]] bool RebuildShieldedAccountLeafCommitmentState(
    const Chainstate& chainstate,
    const CBlockIndex* tip,
    std::map<uint256, uint256>& account_leaf_commitments)
{
    account_leaf_commitments.clear();
    if (tip == nullptr) return true;

    std::vector<const CBlockIndex*> chain;
    chain.reserve(tip->nHeight + 1);
    for (const CBlockIndex* pindex = tip; pindex != nullptr; pindex = pindex->pprev) {
        chain.push_back(pindex);
    }
    std::reverse(chain.begin(), chain.end());

    for (const CBlockIndex* pindex : chain) {
        const bool use_nonced_bridge_tag =
            UseNoncedShieldedBridgeTags(chainstate.m_chainman.GetConsensus(), pindex->nHeight);
        CBlock block;
        if (!ReadShieldedRebuildBlock(chainstate, *pindex, block)) {
            LogError("RebuildShieldedAccountLeafCommitmentState: failed to read block %s\n",
                     pindex->GetBlockHash().ToString());
            return false;
        }
        for (const auto& txref : block.vtx) {
            if (!txref->HasShieldedBundle()) continue;
            const auto leaf_entries =
                CollectShieldedOutputAccountLeafEntries(txref->GetShieldedBundle(),
                                                       use_nonced_bridge_tag);
            if (!leaf_entries.has_value()) {
                LogError("RebuildShieldedAccountLeafCommitmentState: failed to rebuild account-leaf entries for tx %s in block %s\n",
                         txref->GetHash().ToString(),
                         pindex->GetBlockHash().ToString());
                return false;
            }
            for (const auto& [commitment, account_leaf_commitment] : *leaf_entries) {
                account_leaf_commitments[commitment] = account_leaf_commitment;
            }
        }
    }
    return true;
}

[[nodiscard]] bool RebuildShieldedAccountRegistryState(
    const Chainstate& chainstate,
    const CBlockIndex* tip,
    shielded::registry::ShieldedAccountRegistryState& account_registry)
{
    const bool use_configured_payload_store = account_registry.HasAttachedPayloadStore();
    account_registry = use_configured_payload_store
        ? shielded::registry::ShieldedAccountRegistryState::WithConfiguredPayloadStore()
        : shielded::registry::ShieldedAccountRegistryState{};
    if (tip == nullptr) return true;

    std::vector<const CBlockIndex*> chain;
    chain.reserve(tip->nHeight + 1);
    for (const CBlockIndex* pindex = tip; pindex != nullptr; pindex = pindex->pprev) {
        chain.push_back(pindex);
    }
    std::reverse(chain.begin(), chain.end());

    // This rebuild can be reached after an interrupted shielded-state mutation. Account-registry
    // payloads are fully chain-derived and the startup mutation marker keeps partially rebuilt
    // state from being trusted after a crash, so rebuild-time payload appends do not need to
    // fsync every transaction. Normal block-connect appends keep their default synced writes.
    const size_t total_blocks = chain.size();
    const auto rebuild_start = std::chrono::steady_clock::now();
    auto last_progress_log = rebuild_start;
    size_t blocks_processed = 0;
    size_t shielded_txs_processed = 0;
    LogPrintf("RebuildShieldedAccountRegistryState: replaying %u blocks (genesis -> height %d) to rebuild account registry...\n",
              static_cast<unsigned int>(total_blocks),
              tip->nHeight);

    for (const CBlockIndex* pindex : chain) {
        const bool use_nonced_bridge_tag =
            UseNoncedShieldedBridgeTags(chainstate.m_chainman.GetConsensus(), pindex->nHeight);
        CBlock block;
        if (!ReadShieldedRebuildBlock(chainstate, *pindex, block)) {
            LogError("RebuildShieldedAccountRegistryState: failed to read block %s\n",
                     pindex->GetBlockHash().ToString());
            return false;
        }
        for (const auto& txref : block.vtx) {
            if (!txref->HasShieldedBundle()) continue;
            const auto account_leaves =
                shielded::registry::CollectShieldedOutputAccountLeaves(txref->GetShieldedBundle(),
                                                                      use_nonced_bridge_tag);
            if (!account_leaves.has_value()) {
                LogError("RebuildShieldedAccountRegistryState: failed to rebuild account leaves for tx %s in block %s\n",
                         txref->GetHash().ToString(),
                         pindex->GetBlockHash().ToString());
                return false;
            }
            try {
                if (!account_registry.Append(
                        Span<const shielded::registry::ShieldedAccountLeaf>{
                            account_leaves->data(),
                            account_leaves->size()},
                        /*inserted_indices=*/nullptr,
                        /*sync_payload_store=*/false)) {
                    LogError("RebuildShieldedAccountRegistryState: failed to append account leaves for tx %s in block %s\n",
                             txref->GetHash().ToString(),
                             pindex->GetBlockHash().ToString());
                    return false;
                }
                ++shielded_txs_processed;
            } catch (const std::exception& e) {
                LogError("RebuildShieldedAccountRegistryState: account-registry append exception for tx %s in block %s: %s\n",
                         txref->GetHash().ToString(),
                         pindex->GetBlockHash().ToString(),
                         e.what());
                return false;
            }
        }
        ++blocks_processed;
        const auto now = std::chrono::steady_clock::now();
        if (now - last_progress_log >= std::chrono::seconds{15}) {
            last_progress_log = now;
            const double pct = total_blocks > 0
                                   ? 100.0 * static_cast<double>(blocks_processed) / static_cast<double>(total_blocks)
                                   : 100.0;
            LogPrintf("RebuildShieldedAccountRegistryState: replaying account registry %u/%u (height=%d, %.1f%%, shielded_txs=%u, entries=%u)\n",
                      static_cast<unsigned int>(blocks_processed),
                      static_cast<unsigned int>(total_blocks),
                      pindex->nHeight,
                      pct,
                      static_cast<unsigned int>(shielded_txs_processed),
                      static_cast<unsigned int>(account_registry.Size()));
        }
    }
    const auto elapsed_s = std::chrono::duration_cast<std::chrono::seconds>(
                               std::chrono::steady_clock::now() - rebuild_start)
                               .count();
    LogPrintf("RebuildShieldedAccountRegistryState: replayed %u blocks to height %d in %llds (shielded_txs=%u entries=%u)\n",
              static_cast<unsigned int>(total_blocks),
              tip->nHeight,
              static_cast<long long>(elapsed_s),
              static_cast<unsigned int>(shielded_txs_processed),
              static_cast<unsigned int>(account_registry.Size()));
    return true;
}

// Single genesis->tip walk that rebuilds every chain-derived shielded consumer used at
// startup repair time. Archive nodes carry ~720 KB MatMul ExactReplay payloads per block, so
// four independent full-history passes (state / account-registry / settlement / netting) read
// the block store ~4x; fusing them is the floor for a genuine from-genesis rebuild.
struct ShieldedChainDerivedRebuildResult {
    shielded::ShieldedMerkleTree tree{shielded::ShieldedMerkleTree::IndexStorageMode::MEMORY_ONLY};
    ShieldedPoolBalance pool{};
    ShieldedUnshieldVelocity unshield_velocity{};
    std::vector<Nullifier> nullifiers;
    std::vector<uint256> recovery_exit_commitments;
    std::map<uint256, smile2::CompactPublicAccount> public_accounts;
    std::map<uint256, uint256> account_leaf_commitments;
    shielded::registry::ShieldedAccountRegistryState account_registry;
    std::vector<ConfirmedSettlementAnchorState> settlement_anchors;
    std::vector<ConfirmedNettingManifestState> netting_manifests;
};

enum class ShieldedChainDerivedRebuildParts : uint32_t {
    NONE = 0,
    STATE = 1u << 0,
    ACCOUNT_REGISTRY = 1u << 1,
    SETTLEMENT_ANCHORS = 1u << 2,
    NETTING_MANIFESTS = 1u << 3,
    ALL = (1u << 0) | (1u << 1) | (1u << 2) | (1u << 3),
};

[[nodiscard]] constexpr ShieldedChainDerivedRebuildParts operator|(
    ShieldedChainDerivedRebuildParts lhs, ShieldedChainDerivedRebuildParts rhs)
{
    return static_cast<ShieldedChainDerivedRebuildParts>(
        static_cast<uint32_t>(lhs) | static_cast<uint32_t>(rhs));
}

[[nodiscard]] constexpr bool HasShieldedChainDerivedRebuildPart(
    ShieldedChainDerivedRebuildParts parts, ShieldedChainDerivedRebuildParts bit)
{
    return (static_cast<uint32_t>(parts) & static_cast<uint32_t>(bit)) != 0;
}

[[nodiscard]] bool RebuildShieldedChainDerivedState(
    const Chainstate& chainstate,
    const CBlockIndex* tip,
    ShieldedChainDerivedRebuildResult& out,
    ShieldedChainDerivedRebuildParts parts = ShieldedChainDerivedRebuildParts::ALL,
    shielded::ShieldedMerkleTree::IndexStorageMode storage_mode =
        shielded::ShieldedMerkleTree::IndexStorageMode::MEMORY_ONLY,
    bool account_registry_use_payload_store = false,
    kernel::Notifications* notifications = nullptr)
{
    const bool want_state = HasShieldedChainDerivedRebuildPart(parts, ShieldedChainDerivedRebuildParts::STATE);
    const bool want_registry =
        HasShieldedChainDerivedRebuildPart(parts, ShieldedChainDerivedRebuildParts::ACCOUNT_REGISTRY);
    const bool want_settlement =
        HasShieldedChainDerivedRebuildPart(parts, ShieldedChainDerivedRebuildParts::SETTLEMENT_ANCHORS);
    const bool want_netting =
        HasShieldedChainDerivedRebuildPart(parts, ShieldedChainDerivedRebuildParts::NETTING_MANIFESTS);

    const auto& consensus = chainstate.m_chainman.GetConsensus();
    out = ShieldedChainDerivedRebuildResult{};
    if (want_state) {
        out.tree = shielded::ShieldedMerkleTree{storage_mode};
    }
    if (want_registry) {
        out.account_registry = account_registry_use_payload_store
            ? shielded::registry::ShieldedAccountRegistryState::WithConfiguredPayloadStore()
            : shielded::registry::ShieldedAccountRegistryState{};
    }
    if (tip == nullptr) return true;

    std::vector<const CBlockIndex*> chain;
    chain.reserve(tip->nHeight + 1);
    for (const CBlockIndex* pindex = tip; pindex != nullptr; pindex = pindex->pprev) {
        chain.push_back(pindex);
    }
    std::reverse(chain.begin(), chain.end());

    const size_t total_blocks = chain.size();
    const auto rebuild_start = std::chrono::steady_clock::now();
    auto last_progress_log = rebuild_start;
    size_t blocks_processed = 0;
    size_t bytes_read = 0;
    size_t shielded_txs_processed = 0;
    LogPrintf("RebuildShieldedChainDerivedState: replaying %u blocks (genesis -> height %d) for shielded state+registry+bridge metadata in one pass...\n",
              static_cast<unsigned int>(total_blocks),
              tip->nHeight);
    if (notifications != nullptr) {
        notifications->progress(_("Rebuilding shielded state…"), 0, false);
    }

    std::map<uint256, ConfirmedSettlementAnchorState> active_settlement_anchors;
    for (const CBlockIndex* pindex : chain) {
        const bool use_nonced_bridge_tag = UseNoncedShieldedBridgeTags(consensus, pindex->nHeight);
        CBlock block;
        if (!ReadShieldedRebuildBlock(chainstate, *pindex, block)) {
            LogError("RebuildShieldedChainDerivedState: failed to read block %s\n",
                     pindex->GetBlockHash().ToString());
            return false;
        }
        bytes_read += ::GetSerializeSize(TX_WITH_WITNESS(block));

        const CAmount pool_start = want_state ? out.pool.GetBalance() : 0;
        for (const auto& txref : block.vtx) {
            const CTransaction& tx = *txref;
            if (want_settlement || want_netting) {
                std::string reject_reason;
                if (want_settlement) {
                    auto created_anchors =
                        ExtractCreatedShieldedSettlementAnchors(tx, pindex->nHeight, reject_reason);
                    if (!created_anchors.has_value()) {
                        LogError("RebuildShieldedChainDerivedState: failed to rebuild settlement anchors for tx %s in block %s: %s\n",
                                 tx.GetHash().ToString(),
                                 pindex->GetBlockHash().ToString(),
                                 reject_reason);
                        return false;
                    }
                    for (const auto& anchor : *created_anchors) {
                        active_settlement_anchors.insert_or_assign(
                            anchor,
                            ConfirmedSettlementAnchorState{anchor, pindex->nHeight});
                    }
                }
                if (want_netting) {
                    auto created_manifests =
                        ExtractCreatedShieldedNettingManifestStates(tx, pindex->nHeight, reject_reason);
                    if (!created_manifests.has_value()) {
                        LogError("RebuildShieldedChainDerivedState: failed to rebuild netting manifests for tx %s in block %s: %s\n",
                                 tx.GetHash().ToString(),
                                 pindex->GetBlockHash().ToString(),
                                 reject_reason);
                        return false;
                    }
                    out.netting_manifests.insert(out.netting_manifests.end(),
                                                 created_manifests->begin(),
                                                 created_manifests->end());
                }
            }

            if (!tx.HasShieldedBundle()) continue;
            const CShieldedBundle& bundle = tx.GetShieldedBundle();
            ++shielded_txs_processed;

            if (want_settlement && UseSingleUseSettlementAnchors(consensus, pindex->nHeight)) {
                for (const auto& consumed_anchor : CollectShieldedSettlementAnchorRefs(bundle)) {
                    active_settlement_anchors.erase(consumed_anchor);
                }
            }

            if (want_state) {
                std::string height_gate_reject;
                if (!RejectShieldedHeightGateViolation(tx,
                                                       consensus,
                                                       pindex->nHeight,
                                                       height_gate_reject)) {
                    LogError("RebuildShieldedChainDerivedState: shielded height-gate violation at %d: %s\n",
                             pindex->nHeight,
                             height_gate_reject);
                    return false;
                }
                if (!ApplyShieldedStateEffects(bundle,
                                               use_nonced_bridge_tag,
                                               pindex->GetBlockHash(),
                                               out.tree,
                                               out.pool,
                                               &out.nullifiers,
                                               &out.public_accounts,
                                               &out.account_leaf_commitments,
                                               &out.recovery_exit_commitments)) {
                    return false;
                }
            }

            if (want_registry) {
                const auto account_leaves =
                    shielded::registry::CollectShieldedOutputAccountLeaves(bundle,
                                                                          use_nonced_bridge_tag);
                if (!account_leaves.has_value()) {
                    LogError("RebuildShieldedChainDerivedState: failed to rebuild account leaves for tx %s in block %s\n",
                             tx.GetHash().ToString(),
                             pindex->GetBlockHash().ToString());
                    return false;
                }
                try {
                    if (!out.account_registry.Append(
                            Span<const shielded::registry::ShieldedAccountLeaf>{
                                account_leaves->data(),
                                account_leaves->size()},
                            /*inserted_indices=*/nullptr,
                            /*sync_payload_store=*/false)) {
                        LogError("RebuildShieldedChainDerivedState: failed to append account leaves for tx %s in block %s\n",
                                 tx.GetHash().ToString(),
                                 pindex->GetBlockHash().ToString());
                        return false;
                    }
                } catch (const std::exception& e) {
                    LogError("RebuildShieldedChainDerivedState: account-registry append exception for tx %s in block %s: %s\n",
                             tx.GetHash().ToString(),
                             pindex->GetBlockHash().ToString(),
                             e.what());
                    return false;
                }
            }
        }

        if (want_state &&
            consensus.IsShieldedUnshieldVelocityCapActive(pindex->nHeight)) {
            const CAmount pool_end = out.pool.GetBalance();
            const CAmount block_net_egress = pool_start > pool_end ? pool_start - pool_end : 0;
            out.unshield_velocity.RecordBlock(pindex->nHeight, block_net_egress);
            out.unshield_velocity.Prune(pindex->nHeight -
                2 * static_cast<int32_t>(consensus.nShieldedUnshieldVelocityWindowBlocks));
        }

        ++blocks_processed;
        const auto now = std::chrono::steady_clock::now();
        if (now - last_progress_log >= std::chrono::seconds{15}) {
            last_progress_log = now;
            const double pct = total_blocks > 0
                                   ? 100.0 * static_cast<double>(blocks_processed) / static_cast<double>(total_blocks)
                                   : 100.0;
            const auto elapsed_s = std::chrono::duration_cast<std::chrono::seconds>(now - rebuild_start).count();
            LogPrintf("RebuildShieldedChainDerivedState: replaying %u/%u (height=%d, %.1f%%, elapsed=%llds, bytes_read=%llu, shielded_txs=%u, registry_entries=%u, anchors=%u, manifests=%u)\n",
                      static_cast<unsigned int>(blocks_processed),
                      static_cast<unsigned int>(total_blocks),
                      pindex->nHeight,
                      pct,
                      static_cast<long long>(elapsed_s),
                      static_cast<unsigned long long>(bytes_read),
                      static_cast<unsigned int>(shielded_txs_processed),
                      static_cast<unsigned int>(want_registry ? out.account_registry.Size() : 0),
                      static_cast<unsigned int>(active_settlement_anchors.size()),
                      static_cast<unsigned int>(out.netting_manifests.size()));
            if (notifications != nullptr) {
                notifications->progress(_("Rebuilding shielded state…"),
                                        std::max(1, std::min(99, static_cast<int>(pct))),
                                        false);
            }
        }
    }

    if (want_settlement) {
        out.settlement_anchors.reserve(active_settlement_anchors.size());
        for (const auto& [_, anchor_state] : active_settlement_anchors) {
            out.settlement_anchors.push_back(anchor_state);
        }
    }

    const auto elapsed_s = std::chrono::duration_cast<std::chrono::seconds>(
                               std::chrono::steady_clock::now() - rebuild_start)
                               .count();
    LogPrintf("RebuildShieldedChainDerivedState: replayed %u blocks to height %d in %llds (bytes_read=%llu, shielded_txs=%u, nullifiers=%u, registry_entries=%u, anchors=%u, manifests=%u)\n",
              static_cast<unsigned int>(total_blocks),
              tip->nHeight,
              static_cast<long long>(elapsed_s),
              static_cast<unsigned long long>(bytes_read),
              static_cast<unsigned int>(shielded_txs_processed),
              static_cast<unsigned int>(out.nullifiers.size()),
              static_cast<unsigned int>(want_registry ? out.account_registry.Size() : 0),
              static_cast<unsigned int>(out.settlement_anchors.size()),
              static_cast<unsigned int>(out.netting_manifests.size()));
    if (notifications != nullptr) {
        notifications->progress(_("Rebuilding shielded state…"), 100, false);
    }
    return true;
}

[[nodiscard]] bool LoadRebuiltShieldedBridgeMetadata(
    NullifierSet& nullifier_set,
    const std::vector<ConfirmedSettlementAnchorState>& settlement_anchors,
    const std::vector<ConfirmedNettingManifestState>& netting_manifests)
{
    if (!settlement_anchors.empty() &&
        !nullifier_set.InsertSettlementAnchors(settlement_anchors)) {
        return false;
    }
    if (!netting_manifests.empty() &&
        !nullifier_set.InsertNettingManifests(netting_manifests)) {
        return false;
    }
    return true;
}

[[nodiscard]] bool CollectShieldedAccountRegistryHistoryFromState(
    const Chainstate& chainstate,
    const CBlockIndex* tip,
    const shielded::registry::ShieldedAccountRegistryState& current_registry,
    std::deque<uint256>& rebuilt_roots)
{
    rebuilt_roots.clear();
    const uint256 current_root = current_registry.Root();
    if (!current_root.IsNull()) {
        rebuilt_roots.push_back(current_root);
    }

    shielded::registry::ShieldedAccountRegistryState cursor_registry = current_registry;
    const CBlockIndex* cursor = tip;
    for (int depth = 0; depth < SHIELDED_ANCHOR_DEPTH && cursor != nullptr; ++depth) {
        if (ShieldedGapRecoveryCanSkipBlockBody(*cursor)) {
            const uint256 rebuilt_root = cursor_registry.Root();
            if (!rebuilt_root.IsNull()) rebuilt_roots.push_back(rebuilt_root);
            cursor = cursor->pprev;
            continue;
        }
        const bool use_nonced_bridge_tag =
            UseNoncedShieldedBridgeTags(chainstate.m_chainman.GetConsensus(), cursor->nHeight);
        CBlock block;
        if (!ReadShieldedRebuildBlock(chainstate, *cursor, block)) {
            LogError("CollectShieldedAccountRegistryHistoryFromState: failed to read block %s\n",
                     cursor->GetBlockHash().ToString());
            return false;
        }
        uint64_t leaf_count{0};
        for (const auto& txref : block.vtx) {
            if (!txref->HasShieldedBundle()) continue;
            const auto account_leaf_commitments =
                CollectShieldedOutputAccountLeafCommitments(txref->GetShieldedBundle(),
                                                            use_nonced_bridge_tag);
            if (!account_leaf_commitments.has_value()) {
                LogError("CollectShieldedAccountRegistryHistoryFromState: failed to rebuild account leaves for tx %s in block %s\n",
                         txref->GetHash().ToString(),
                         cursor->GetBlockHash().ToString());
                return false;
            }
            const auto next_leaf_count =
                CheckedAdd(leaf_count, static_cast<uint64_t>(account_leaf_commitments->size()));
            if (!next_leaf_count.has_value()) {
                LogError("CollectShieldedAccountRegistryHistoryFromState: account-leaf count overflow at block %s\n",
                         cursor->GetBlockHash().ToString());
                return false;
            }
            leaf_count = *next_leaf_count;
        }
        if (leaf_count > 0) {
            const uint64_t cursor_size = static_cast<uint64_t>(cursor_registry.Size());
            if (leaf_count > cursor_size ||
                !cursor_registry.Truncate(
                    static_cast<size_t>(cursor_size - leaf_count),
                    shielded::registry::ShieldedAccountRegistryState::PayloadPruneMode::KEEP)) {
                LogError("CollectShieldedAccountRegistryHistoryFromState: failed to remove %" PRIu64 " leaves at block %s\n",
                         leaf_count,
                         cursor->GetBlockHash().ToString());
                return false;
            }
        }
        const uint256 rebuilt_root = cursor_registry.Root();
        if (!rebuilt_root.IsNull()) {
            rebuilt_roots.push_back(rebuilt_root);
        }
        cursor = cursor->pprev;
    }
    return true;
}

[[nodiscard]] bool LoadShieldedAccountRegistryRootWindow(
    const std::vector<uint256>& roots,
    const uint256& current_root,
    std::optional<size_t> expected_size,
    const char* context,
    std::deque<uint256>& root_window)
{
    if (current_root.IsNull()) {
        LogError("%s: current account-registry root is null\n", context);
        return false;
    }
    if (roots.empty()) {
        LogError("%s: account-registry root window is empty\n", context);
        return false;
    }
    if (roots.size() > static_cast<size_t>(SHIELDED_ANCHOR_DEPTH) + 1U) {
        LogError("%s: account-registry root window has %u entries, maximum is %u\n",
                 context,
                 static_cast<unsigned int>(roots.size()),
                 static_cast<unsigned int>(static_cast<size_t>(SHIELDED_ANCHOR_DEPTH) + 1U));
        return false;
    }
    if (expected_size.has_value() && roots.size() != *expected_size) {
        LogError("%s: account-registry root window has %u entries, expected %u\n",
                 context,
                 static_cast<unsigned int>(roots.size()),
                 static_cast<unsigned int>(*expected_size));
        return false;
    }
    if (roots.front() != current_root) {
        LogError("%s: account-registry root window current root mismatch expected=%s actual=%s\n",
                 context,
                 current_root.ToString(),
                 roots.front().ToString());
        return false;
    }
    if (std::any_of(roots.begin(), roots.end(), [](const uint256& root) { return root.IsNull(); })) {
        LogError("%s: account-registry root window contains a null root\n", context);
        return false;
    }

    root_window.assign(roots.begin(), roots.end());
    return true;
}

[[nodiscard]] bool DeriveShieldedAccountRegistryHistoryFromV6Snapshot(
    const shielded::ShieldedMerkleTree& tree,
    const shielded::registry::ShieldedAccountRegistryState& current_registry,
    const std::vector<uint64_t>& recent_output_counts,
    std::deque<uint256>& rebuilt_roots)
{
    rebuilt_roots.clear();

    uint64_t recent_output_total{0};
    for (const uint64_t output_count : recent_output_counts) {
        const auto next_total = CheckedAdd(recent_output_total, output_count);
        if (!next_total.has_value()) {
            LogError("DeriveShieldedAccountRegistryHistoryFromV6Snapshot: recent output count overflow\n");
            return false;
        }
        recent_output_total = *next_total;
    }

    const uint64_t tree_size = tree.Size();
    const uint64_t registry_size = static_cast<uint64_t>(current_registry.Size());
    if (recent_output_total > tree_size) {
        LogError("DeriveShieldedAccountRegistryHistoryFromV6Snapshot: recent outputs=%" PRIu64 " exceed commitment count=%" PRIu64 "\n",
                 recent_output_total,
                 tree_size);
        return false;
    }
    if (recent_output_total > registry_size) {
        LogError("DeriveShieldedAccountRegistryHistoryFromV6Snapshot: recent outputs=%" PRIu64 " exceed account-registry entries=%" PRIu64 "\n",
                 recent_output_total,
                 registry_size);
        return false;
    }

    const uint64_t commitment_offset = tree_size - recent_output_total;
    const uint64_t registry_offset = registry_size - recent_output_total;
    for (uint64_t i = 0; i < recent_output_total; ++i) {
        const auto commitment = tree.CommitmentAt(commitment_offset + i);
        if (!commitment.has_value()) {
            LogError("DeriveShieldedAccountRegistryHistoryFromV6Snapshot: missing commitment at position=%" PRIu64 "\n",
                     commitment_offset + i);
            return false;
        }

        const auto entry = current_registry.MaterializeEntry(registry_offset + i);
        if (!entry.has_value()) {
            LogError("DeriveShieldedAccountRegistryHistoryFromV6Snapshot: missing account-registry entry at index=%" PRIu64 "\n",
                     registry_offset + i);
            return false;
        }
        if (entry->spent) {
            LogError("DeriveShieldedAccountRegistryHistoryFromV6Snapshot: account-registry entry at index=%" PRIu64 " is spent\n",
                     registry_offset + i);
            return false;
        }

        const auto leaf = shielded::registry::DeserializeShieldedAccountLeafPayload(
            Span<const uint8_t>{entry->account_leaf_payload.data(),
                                entry->account_leaf_payload.size()});
        if (!leaf.has_value()) {
            LogError("DeriveShieldedAccountRegistryHistoryFromV6Snapshot: invalid account-registry leaf payload at index=%" PRIu64 "\n",
                     registry_offset + i);
            return false;
        }
        if (leaf->note_commitment != *commitment) {
            LogError("DeriveShieldedAccountRegistryHistoryFromV6Snapshot: note commitment mismatch at recent offset=%" PRIu64 " commitment=%s leaf=%s\n",
                     i,
                     commitment->ToString(),
                     leaf->note_commitment.ToString());
            return false;
        }
    }

    shielded::registry::ShieldedAccountRegistryState cursor_registry = current_registry;
    const uint256 current_root = cursor_registry.Root();
    if (current_root.IsNull()) {
        LogError("DeriveShieldedAccountRegistryHistoryFromV6Snapshot: current account-registry root is null\n");
        return false;
    }
    rebuilt_roots.push_back(current_root);
    for (const uint64_t output_count : recent_output_counts) {
        const uint64_t cursor_size = static_cast<uint64_t>(cursor_registry.Size());
        if (output_count > cursor_size) {
            LogError("DeriveShieldedAccountRegistryHistoryFromV6Snapshot: output count=%" PRIu64 " exceeds cursor registry size=%" PRIu64 "\n",
                     output_count,
                     cursor_size);
            return false;
        }
        if (output_count > 0 &&
            !cursor_registry.Truncate(
                static_cast<size_t>(cursor_size - output_count),
                shielded::registry::ShieldedAccountRegistryState::PayloadPruneMode::KEEP)) {
            LogError("DeriveShieldedAccountRegistryHistoryFromV6Snapshot: failed to remove %" PRIu64 " account-registry leaves\n",
                     output_count);
            return false;
        }
        const uint256 rebuilt_root = cursor_registry.Root();
        if (rebuilt_root.IsNull()) {
            LogError("DeriveShieldedAccountRegistryHistoryFromV6Snapshot: derived null account-registry root\n");
            return false;
        }
        rebuilt_roots.push_back(rebuilt_root);
    }
    return true;
}

[[nodiscard]] [[maybe_unused]] bool RebuildShieldedSettlementAnchorState(const node::BlockManager& blockman,
                                                        const Consensus::Params& consensus,
                                                        const CBlockIndex* tip,
                                                        std::vector<ConfirmedSettlementAnchorState>& settlement_anchors)
{
    settlement_anchors.clear();
    if (tip == nullptr) return true;

    std::vector<const CBlockIndex*> chain;
    chain.reserve(tip->nHeight + 1);
    for (const CBlockIndex* pindex = tip; pindex != nullptr; pindex = pindex->pprev) {
        chain.push_back(pindex);
    }
    std::reverse(chain.begin(), chain.end());

    // Full-chain settlement-anchor rebuild is O(height) and can read hundreds of GB on archive
    // nodes. Emit the same style of rate-limited progress used by RebuildShieldedState so a long
    // pass is distinguishable from a hang in debug.log / operator diagnostics.
    const size_t total_blocks = chain.size();
    const auto rebuild_start = std::chrono::steady_clock::now();
    auto last_progress_log = rebuild_start;
    size_t blocks_processed = 0;
    LogPrintf("RebuildShieldedSettlementAnchorState: scanning %u blocks (genesis -> height %d) for settlement anchors...\n",
              static_cast<unsigned int>(total_blocks),
              tip->nHeight);

    std::map<uint256, ConfirmedSettlementAnchorState> active_settlement_anchors;
    for (const CBlockIndex* pindex : chain) {
        CBlock block;
        if (!blockman.ReadBlock(block, *pindex)) {
            LogError("RebuildShieldedSettlementAnchorState: failed to read block %s\n",
                     pindex->GetBlockHash().ToString());
            return false;
        }
        for (const auto& txref : block.vtx) {
            std::string reject_reason;
            auto created = ExtractCreatedShieldedSettlementAnchors(*txref, pindex->nHeight, reject_reason);
            if (!created.has_value()) {
                LogError("RebuildShieldedSettlementAnchorState: failed to rebuild settlement anchors for tx %s in block %s: %s\n",
                         txref->GetHash().ToString(),
                         pindex->GetBlockHash().ToString(),
                         reject_reason);
                return false;
            }
            for (const auto& anchor : *created) {
                active_settlement_anchors.insert_or_assign(anchor,
                                                           ConfirmedSettlementAnchorState{
                                                               anchor,
                                                               pindex->nHeight});
            }
            if (UseSingleUseSettlementAnchors(consensus, pindex->nHeight)) {
                for (const auto& consumed_anchor : CollectShieldedSettlementAnchorRefs(txref->GetShieldedBundle())) {
                    active_settlement_anchors.erase(consumed_anchor);
                }
            }
        }
        ++blocks_processed;
        const auto now = std::chrono::steady_clock::now();
        if (now - last_progress_log >= std::chrono::seconds{15}) {
            last_progress_log = now;
            const double pct = total_blocks > 0
                                   ? 100.0 * static_cast<double>(blocks_processed) / static_cast<double>(total_blocks)
                                   : 100.0;
            const auto elapsed_s = std::chrono::duration_cast<std::chrono::seconds>(now - rebuild_start).count();
            LogPrintf("RebuildShieldedSettlementAnchorState: scanning settlement anchors %u/%u (height=%d, %.1f%%, elapsed=%llds, anchors=%u)\n",
                      static_cast<unsigned int>(blocks_processed),
                      static_cast<unsigned int>(total_blocks),
                      pindex->nHeight,
                      pct,
                      static_cast<long long>(elapsed_s),
                      static_cast<unsigned int>(active_settlement_anchors.size()));
        }
    }
    settlement_anchors.reserve(active_settlement_anchors.size());
    for (const auto& [anchor, anchor_state] : active_settlement_anchors) {
        settlement_anchors.push_back(anchor_state);
    }
    const auto elapsed_s = std::chrono::duration_cast<std::chrono::seconds>(
                               std::chrono::steady_clock::now() - rebuild_start)
                               .count();
    LogPrintf("RebuildShieldedSettlementAnchorState: scanned %u blocks to height %d in %llds (anchors=%u)\n",
              static_cast<unsigned int>(total_blocks),
              tip->nHeight,
              static_cast<long long>(elapsed_s),
              static_cast<unsigned int>(settlement_anchors.size()));
    return true;
}

[[nodiscard]] [[maybe_unused]] bool RebuildShieldedNettingManifestState(const node::BlockManager& blockman,
                                                       const CBlockIndex* tip,
                                                       std::vector<ConfirmedNettingManifestState>& manifests)
{
    manifests.clear();
    if (tip == nullptr) return true;

    std::vector<const CBlockIndex*> chain;
    chain.reserve(tip->nHeight + 1);
    for (const CBlockIndex* pindex = tip; pindex != nullptr; pindex = pindex->pprev) {
        chain.push_back(pindex);
    }
    std::reverse(chain.begin(), chain.end());

    const size_t total_blocks = chain.size();
    const auto rebuild_start = std::chrono::steady_clock::now();
    auto last_progress_log = rebuild_start;
    size_t blocks_processed = 0;
    LogPrintf("RebuildShieldedNettingManifestState: scanning %u blocks (genesis -> height %d) for netting manifests...\n",
              static_cast<unsigned int>(total_blocks),
              tip->nHeight);

    for (const CBlockIndex* pindex : chain) {
        CBlock block;
        if (!blockman.ReadBlock(block, *pindex)) {
            LogError("RebuildShieldedNettingManifestState: failed to read block %s\n",
                     pindex->GetBlockHash().ToString());
            return false;
        }
        for (const auto& txref : block.vtx) {
            std::string reject_reason;
            auto created = ExtractCreatedShieldedNettingManifestStates(*txref, pindex->nHeight, reject_reason);
            if (!created.has_value()) {
                LogError("RebuildShieldedNettingManifestState: failed to rebuild manifests for tx %s in block %s: %s\n",
                         txref->GetHash().ToString(),
                         pindex->GetBlockHash().ToString(),
                         reject_reason);
                return false;
            }
            manifests.insert(manifests.end(), created->begin(), created->end());
        }
        ++blocks_processed;
        const auto now = std::chrono::steady_clock::now();
        if (now - last_progress_log >= std::chrono::seconds{15}) {
            last_progress_log = now;
            const double pct = total_blocks > 0
                                   ? 100.0 * static_cast<double>(blocks_processed) / static_cast<double>(total_blocks)
                                   : 100.0;
            const auto elapsed_s = std::chrono::duration_cast<std::chrono::seconds>(now - rebuild_start).count();
            LogPrintf("RebuildShieldedNettingManifestState: scanning netting manifests %u/%u (height=%d, %.1f%%, elapsed=%llds, manifests=%u)\n",
                      static_cast<unsigned int>(blocks_processed),
                      static_cast<unsigned int>(total_blocks),
                      pindex->nHeight,
                      pct,
                      static_cast<long long>(elapsed_s),
                      static_cast<unsigned int>(manifests.size()));
        }
    }
    const auto elapsed_s = std::chrono::duration_cast<std::chrono::seconds>(
                               std::chrono::steady_clock::now() - rebuild_start)
                               .count();
    LogPrintf("RebuildShieldedNettingManifestState: scanned %u blocks to height %d in %llds (manifests=%u)\n",
              static_cast<unsigned int>(total_blocks),
              tip->nHeight,
              static_cast<long long>(elapsed_s),
              static_cast<unsigned int>(manifests.size()));
    return true;
}

enum class PersistedShieldedMetadataSyncMode {
    STRICT_CHAIN_CONVERGENCE,
    PRESERVE_PERSISTED_EXTRAS,
};

[[nodiscard]] bool SyncShieldedSettlementAnchorStateFromRebuilt(
    NullifierSet& nullifier_set,
    std::vector<ConfirmedSettlementAnchorState> rebuilt_settlement_anchors,
    PersistedShieldedMetadataSyncMode sync_mode)
{
    const auto settlement_anchor_less = [](const ConfirmedSettlementAnchorState& lhs,
                                           const ConfirmedSettlementAnchorState& rhs) {
        return lhs.anchor < rhs.anchor;
    };
    std::sort(rebuilt_settlement_anchors.begin(),
              rebuilt_settlement_anchors.end(),
              settlement_anchor_less);
    rebuilt_settlement_anchors.erase(std::unique(rebuilt_settlement_anchors.begin(),
                                                 rebuilt_settlement_anchors.end(),
                                                 [](const ConfirmedSettlementAnchorState& lhs,
                                                    const ConfirmedSettlementAnchorState& rhs) {
                                                     return lhs.anchor == rhs.anchor;
                                                 }),
                                     rebuilt_settlement_anchors.end());

    std::vector<ConfirmedSettlementAnchorState> persisted_settlement_anchors;
    if (!nullifier_set.ForEachPersistedSettlementAnchorState([&](const ConfirmedSettlementAnchorState& anchor) {
            persisted_settlement_anchors.push_back(anchor);
            return true;
        })) {
        return false;
    }
    std::sort(persisted_settlement_anchors.begin(),
              persisted_settlement_anchors.end(),
              settlement_anchor_less);
    persisted_settlement_anchors.erase(std::unique(persisted_settlement_anchors.begin(),
                                                   persisted_settlement_anchors.end(),
                                                   [](const ConfirmedSettlementAnchorState& lhs,
                                                      const ConfirmedSettlementAnchorState& rhs) {
                                                       return lhs.anchor == rhs.anchor &&
                                                              lhs.created_height == rhs.created_height;
                                                   }),
                                       persisted_settlement_anchors.end());

    if (persisted_settlement_anchors == rebuilt_settlement_anchors) {
        return true;
    }

    std::vector<uint256> stale_settlement_anchors;
    std::vector<ConfirmedSettlementAnchorState> refreshed_settlement_anchors;
    size_t persisted_index = 0;
    size_t rebuilt_index = 0;
    while (persisted_index < persisted_settlement_anchors.size() &&
           rebuilt_index < rebuilt_settlement_anchors.size()) {
        const auto& persisted = persisted_settlement_anchors[persisted_index];
        const auto& rebuilt = rebuilt_settlement_anchors[rebuilt_index];
        if (persisted.anchor < rebuilt.anchor) {
            if (sync_mode == PersistedShieldedMetadataSyncMode::STRICT_CHAIN_CONVERGENCE) {
                stale_settlement_anchors.push_back(persisted.anchor);
            }
            ++persisted_index;
            continue;
        }
        if (rebuilt.anchor < persisted.anchor) {
            refreshed_settlement_anchors.push_back(rebuilt);
            ++rebuilt_index;
            continue;
        }
        if (persisted != rebuilt) {
            refreshed_settlement_anchors.push_back(rebuilt);
        }
        ++persisted_index;
        ++rebuilt_index;
    }
    while (persisted_index < persisted_settlement_anchors.size()) {
        if (sync_mode == PersistedShieldedMetadataSyncMode::STRICT_CHAIN_CONVERGENCE) {
            stale_settlement_anchors.push_back(persisted_settlement_anchors[persisted_index].anchor);
        }
        ++persisted_index;
    }
    while (rebuilt_index < rebuilt_settlement_anchors.size()) {
        refreshed_settlement_anchors.push_back(rebuilt_settlement_anchors[rebuilt_index]);
        ++rebuilt_index;
    }
    if (!stale_settlement_anchors.empty() &&
        !nullifier_set.RemoveSettlementAnchors(stale_settlement_anchors)) {
        return false;
    }

    if (!refreshed_settlement_anchors.empty() &&
        !nullifier_set.InsertSettlementAnchors(refreshed_settlement_anchors)) {
        return false;
    }

    return true;
}

[[nodiscard]] bool SyncShieldedNettingManifestStateFromRebuilt(
    NullifierSet& nullifier_set,
    std::vector<ConfirmedNettingManifestState> rebuilt_manifest_states,
    PersistedShieldedMetadataSyncMode sync_mode)
{
    std::sort(rebuilt_manifest_states.begin(),
              rebuilt_manifest_states.end(),
              [](const ConfirmedNettingManifestState& lhs, const ConfirmedNettingManifestState& rhs) {
                  return lhs.manifest_id < rhs.manifest_id;
              });
    rebuilt_manifest_states.erase(
        std::unique(rebuilt_manifest_states.begin(),
                    rebuilt_manifest_states.end(),
                    [](const ConfirmedNettingManifestState& lhs, const ConfirmedNettingManifestState& rhs) {
                        return lhs.manifest_id == rhs.manifest_id;
                    }),
        rebuilt_manifest_states.end());

    std::vector<ConfirmedNettingManifestState> persisted_manifest_states;
    if (!nullifier_set.ForEachPersistedNettingManifestState([&](const ConfirmedNettingManifestState& manifest_state) {
            persisted_manifest_states.push_back(manifest_state);
            return true;
        })) {
        return false;
    }
    std::sort(persisted_manifest_states.begin(),
              persisted_manifest_states.end(),
              [](const ConfirmedNettingManifestState& lhs, const ConfirmedNettingManifestState& rhs) {
                  return lhs.manifest_id < rhs.manifest_id;
              });
    persisted_manifest_states.erase(
        std::unique(persisted_manifest_states.begin(),
                    persisted_manifest_states.end(),
                    [](const ConfirmedNettingManifestState& lhs, const ConfirmedNettingManifestState& rhs) {
                        return lhs.manifest_id == rhs.manifest_id;
                    }),
        persisted_manifest_states.end());

    if (persisted_manifest_states == rebuilt_manifest_states) {
        return true;
    }

    std::vector<uint256> stale_manifest_ids;
    if (sync_mode == PersistedShieldedMetadataSyncMode::STRICT_CHAIN_CONVERGENCE) {
        for (const auto& persisted : persisted_manifest_states) {
            const auto it = std::lower_bound(
                rebuilt_manifest_states.begin(),
                rebuilt_manifest_states.end(),
                persisted.manifest_id,
                [](const ConfirmedNettingManifestState& candidate, const uint256& manifest_id) {
                    return candidate.manifest_id < manifest_id;
                });
            if (it == rebuilt_manifest_states.end() || it->manifest_id != persisted.manifest_id) {
                stale_manifest_ids.push_back(persisted.manifest_id);
            }
        }
    }
    if (!stale_manifest_ids.empty() &&
        !nullifier_set.RemoveNettingManifests(stale_manifest_ids)) {
        return false;
    }

    std::vector<ConfirmedNettingManifestState> missing_or_updated_manifests;
    for (const auto& rebuilt : rebuilt_manifest_states) {
        const auto it = std::lower_bound(
            persisted_manifest_states.begin(),
            persisted_manifest_states.end(),
            rebuilt.manifest_id,
            [](const ConfirmedNettingManifestState& candidate, const uint256& manifest_id) {
                return candidate.manifest_id < manifest_id;
            });
        if (it == persisted_manifest_states.end() ||
            it->manifest_id != rebuilt.manifest_id ||
            it->created_height != rebuilt.created_height ||
            it->settlement_window != rebuilt.settlement_window) {
            missing_or_updated_manifests.push_back(rebuilt);
        }
    }
    if (!missing_or_updated_manifests.empty() &&
        !nullifier_set.InsertNettingManifests(missing_or_updated_manifests)) {
        return false;
    }

    return true;
}


[[nodiscard]] bool SyncShieldedNettingManifestState(const Chainstate& chainstate,
                                                    const CBlockIndex* tip,
                                                    NullifierSet& nullifier_set,
                                                    PersistedShieldedMetadataSyncMode sync_mode =
                                                        PersistedShieldedMetadataSyncMode::STRICT_CHAIN_CONVERGENCE,
                                                    kernel::Notifications* notifications = nullptr)
{
    ShieldedChainDerivedRebuildResult rebuilt;
    if (!RebuildShieldedChainDerivedState(
            chainstate,
            tip,
            rebuilt,
            ShieldedChainDerivedRebuildParts::NETTING_MANIFESTS,
            shielded::ShieldedMerkleTree::IndexStorageMode::MEMORY_ONLY,
            /*account_registry_use_payload_store=*/false,
            notifications)) {
        return false;
    }
    return SyncShieldedNettingManifestStateFromRebuilt(nullifier_set,
                                                       std::move(rebuilt.netting_manifests),
                                                       sync_mode);
}

// Converge settlement + netting metadata with a single genesis->tip block-store walk.
[[nodiscard]] bool SyncShieldedBridgeMetadataState(const Chainstate& chainstate,
                                                   const CBlockIndex* tip,
                                                   NullifierSet& nullifier_set,
                                                   PersistedShieldedMetadataSyncMode sync_mode =
                                                       PersistedShieldedMetadataSyncMode::STRICT_CHAIN_CONVERGENCE,
                                                   kernel::Notifications* notifications = nullptr)
{
    ShieldedChainDerivedRebuildResult rebuilt;
    if (!RebuildShieldedChainDerivedState(
            chainstate,
            tip,
            rebuilt,
            ShieldedChainDerivedRebuildParts::SETTLEMENT_ANCHORS |
                ShieldedChainDerivedRebuildParts::NETTING_MANIFESTS,
            shielded::ShieldedMerkleTree::IndexStorageMode::MEMORY_ONLY,
            /*account_registry_use_payload_store=*/false,
            notifications)) {
        return false;
    }
    return SyncShieldedSettlementAnchorStateFromRebuilt(nullifier_set,
                                                        std::move(rebuilt.settlement_anchors),
                                                        sync_mode) &&
           SyncShieldedNettingManifestStateFromRebuilt(nullifier_set,
                                                       std::move(rebuilt.netting_manifests),
                                                       sync_mode);
}

[[nodiscard]] bool AuditShieldedStateAgainstChain(const Chainstate& chainstate,
                                                  std::string& error,
                                                  bool include_proof_audit = true)
    EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);
    if (&chainstate != &chainstate.m_chainman.ActiveChainstate()) return true;
    if (!chainstate.m_chainman.HasShieldedState()) return true;
    if (chainstate.m_from_snapshot_blockhash) return true;

    // One fused genesis->tip walk replaces the previous four independent rebuild passes.
    ShieldedChainDerivedRebuildResult rebuilt;
    if (!RebuildShieldedChainDerivedState(
            chainstate,
            chainstate.m_chain.Tip(),
            rebuilt,
            ShieldedChainDerivedRebuildParts::ALL,
            shielded::ShieldedMerkleTree::IndexStorageMode::MEMORY_ONLY,
            /*account_registry_use_payload_store=*/false,
            &chainstate.m_chainman.GetNotifications())) {
        error = "failed to rebuild expected shielded state";
        return false;
    }

    const auto& rebuilt_tree = rebuilt.tree;
    const auto& rebuilt_pool = rebuilt.pool;
    auto& rebuilt_nullifiers = rebuilt.nullifiers;
    auto& rebuilt_recovery_exit_commitments = rebuilt.recovery_exit_commitments;
    const auto& rebuilt_velocity = rebuilt.unshield_velocity;
    const auto& rebuilt_registry = rebuilt.account_registry;

    const auto& chain_tree = chainstate.m_chainman.GetShieldedMerkleTree();
    if (rebuilt_tree.Root() != chain_tree.Root() ||
        rebuilt_tree.Size() != chain_tree.Size()) {
        error = "merkle tree root/size mismatch";
        return false;
    }
    std::deque<uint256> rebuilt_anchor_roots;
    if (!CollectShieldedAnchorHistoryFromTree(chainstate,
                                              chainstate.m_chain.Tip(),
                                              rebuilt_tree,
                                              rebuilt_anchor_roots)) {
        error = "failed to rebuild expected shielded anchor history";
        return false;
    }
    if (rebuilt_anchor_roots != chainstate.m_chainman.GetShieldedAnchorRoots()) {
        error = "anchor history mismatch";
        return false;
    }
    if (rebuilt_pool.GetBalance() != chainstate.m_chainman.GetShieldedPoolBalance()) {
        error = "pool balance mismatch";
        return false;
    }
    const CBlockIndex* const tip = chainstate.m_chain.Tip();
    if (tip != nullptr) {
        const auto& consensus = chainstate.m_chainman.GetConsensus();
        const int32_t next_block_height =
            tip->nHeight == std::numeric_limits<int32_t>::max() ? tip->nHeight : tip->nHeight + 1;
        if (consensus.IsShieldedUnshieldVelocityCapActive(next_block_height) ||
            ShieldedUnshieldVelocityStateRequiredAtTip(consensus, tip->nHeight)) {
            ShieldedUnshieldVelocity persisted_velocity;
            if (!chainstate.m_chainman.ReadShieldedUnshieldVelocity(persisted_velocity)) {
                error = "failed to read persisted unshield velocity";
                return false;
            }
            const uint32_t window_blocks = consensus.nShieldedUnshieldVelocityWindowBlocks;
            const CAmount expected_window_egress =
                rebuilt_velocity.WindowTotal(next_block_height, window_blocks);
            const CAmount persisted_window_egress =
                persisted_velocity.WindowTotal(next_block_height, window_blocks);
            if (expected_window_egress != persisted_window_egress) {
                error = strprintf("unshield velocity window mismatch (db=%lld expected=%lld)",
                                  static_cast<long long>(persisted_window_egress),
                                  static_cast<long long>(expected_window_egress));
                return false;
            }
        }
    }

    if (rebuilt_registry.Root() != chainstate.m_chainman.GetShieldedAccountRegistryRoot() ||
        rebuilt_registry.Size() != chainstate.m_chainman.GetShieldedAccountRegistryEntryCount()) {
        error = "account registry root/size mismatch";
        return false;
    }

    std::map<uint256, smile2::CompactPublicAccount> rebuilt_public_accounts;
    std::map<uint256, uint256> rebuilt_account_leaf_commitments;
    if (!shielded::registry::BuildRegistryAccountState(rebuilt_registry,
                                                       rebuilt_public_accounts,
                                                       rebuilt_account_leaf_commitments)) {
        error = "failed to rebuild expected account registry views";
        return false;
    }
    const auto& persisted_public_accounts =
        chainstate.m_chainman.GetShieldedSmilePublicAccounts();
    if (rebuilt_public_accounts.size() != persisted_public_accounts.size()) {
        error = "account registry public-account count mismatch";
        return false;
    }
    for (const auto& [account_id, rebuilt_account] : rebuilt_public_accounts) {
        const auto persisted_it = persisted_public_accounts.find(account_id);
        if (persisted_it == persisted_public_accounts.end()) {
            error = strprintf("missing public account %s", account_id.ToString());
            return false;
        }
        if (smile2::ComputeCompactPublicAccountHash(rebuilt_account) !=
            smile2::ComputeCompactPublicAccountHash(persisted_it->second)) {
            error = strprintf("account registry public-account mismatch %s",
                              account_id.ToString());
            return false;
        }
    }
    if (rebuilt_account_leaf_commitments !=
        chainstate.m_chainman.GetShieldedAccountLeafCommitments()) {
        error = "account registry leaf-commitment map mismatch";
        return false;
    }

    std::sort(rebuilt_nullifiers.begin(), rebuilt_nullifiers.end());
    rebuilt_nullifiers.erase(std::unique(rebuilt_nullifiers.begin(), rebuilt_nullifiers.end()), rebuilt_nullifiers.end());
    const uint64_t db_count = chainstate.m_chainman.GetShieldedNullifierCount();
    if (db_count != rebuilt_nullifiers.size()) {
        error = strprintf("nullifier count mismatch (db=%llu expected=%llu)",
                          static_cast<unsigned long long>(db_count),
                          static_cast<unsigned long long>(rebuilt_nullifiers.size()));
        return false;
    }
    for (const auto& nf : rebuilt_nullifiers) {
        if (!chainstate.m_chainman.IsShieldedNullifierSpent(nf)) {
            error = strprintf("missing nullifier %s", nf.ToString());
            return false;
        }
    }

    std::sort(rebuilt_recovery_exit_commitments.begin(), rebuilt_recovery_exit_commitments.end());
    rebuilt_recovery_exit_commitments.erase(std::unique(rebuilt_recovery_exit_commitments.begin(),
                                                        rebuilt_recovery_exit_commitments.end()),
                                            rebuilt_recovery_exit_commitments.end());
    std::vector<uint256> persisted_recovery_exit_commitments;
    if (!chainstate.m_chainman.ForEachShieldedRecoveryExitCommitment(
            [&](const uint256& commitment) {
                persisted_recovery_exit_commitments.push_back(commitment);
                return true;
            })) {
        error = "failed to enumerate persisted recovery-exit commitments";
        return false;
    }
    std::sort(persisted_recovery_exit_commitments.begin(),
              persisted_recovery_exit_commitments.end());
    persisted_recovery_exit_commitments.erase(std::unique(persisted_recovery_exit_commitments.begin(),
                                                          persisted_recovery_exit_commitments.end()),
                                              persisted_recovery_exit_commitments.end());
    for (const auto& commitment : rebuilt_recovery_exit_commitments) {
        if (!std::binary_search(persisted_recovery_exit_commitments.begin(),
                                persisted_recovery_exit_commitments.end(),
                                commitment)) {
            error = strprintf("missing recovery-exit commitment %s", commitment.ToString());
            return false;
        }
    }
    for (const auto& commitment : persisted_recovery_exit_commitments) {
        if (!std::binary_search(rebuilt_recovery_exit_commitments.begin(),
                                rebuilt_recovery_exit_commitments.end(),
                                commitment)) {
            error = strprintf("extra recovery-exit commitment %s", commitment.ToString());
            return false;
        }
    }

    auto& rebuilt_settlement_anchors = rebuilt.settlement_anchors;
    std::sort(rebuilt_settlement_anchors.begin(),
              rebuilt_settlement_anchors.end(),
              [](const ConfirmedSettlementAnchorState& lhs,
                 const ConfirmedSettlementAnchorState& rhs) {
                  return lhs.anchor < rhs.anchor;
              });
    rebuilt_settlement_anchors.erase(std::unique(rebuilt_settlement_anchors.begin(),
                                                 rebuilt_settlement_anchors.end(),
                                                 [](const ConfirmedSettlementAnchorState& lhs,
                                                    const ConfirmedSettlementAnchorState& rhs) {
                                                     return lhs.anchor == rhs.anchor;
                                                 }),
                                     rebuilt_settlement_anchors.end());
    for (const auto& settlement_anchor : rebuilt_settlement_anchors) {
        if (!chainstate.m_chainman.IsShieldedSettlementAnchorValid(settlement_anchor.anchor)) {
            error = strprintf("missing settlement anchor %s", settlement_anchor.anchor.ToString());
            return false;
        }
        const auto persisted_anchor_state =
            chainstate.m_chainman.GetShieldedSettlementAnchorState(settlement_anchor.anchor);
        if (!persisted_anchor_state.has_value() ||
            persisted_anchor_state->created_height != settlement_anchor.created_height) {
            error = strprintf("settlement anchor metadata mismatch %s",
                              settlement_anchor.anchor.ToString());
            return false;
        }
    }

    auto& rebuilt_manifest_states = rebuilt.netting_manifests;
    std::sort(rebuilt_manifest_states.begin(),
              rebuilt_manifest_states.end(),
              [](const ConfirmedNettingManifestState& lhs, const ConfirmedNettingManifestState& rhs) {
                  return lhs.manifest_id < rhs.manifest_id;
              });
    rebuilt_manifest_states.erase(
        std::unique(rebuilt_manifest_states.begin(),
                    rebuilt_manifest_states.end(),
                    [](const ConfirmedNettingManifestState& lhs, const ConfirmedNettingManifestState& rhs) {
                        return lhs.manifest_id == rhs.manifest_id;
                    }),
        rebuilt_manifest_states.end());
    for (const auto& manifest_state : rebuilt_manifest_states) {
        if (!chainstate.m_chainman.HasShieldedNettingManifest(manifest_state.manifest_id)) {
            error = strprintf("missing netting manifest %s", manifest_state.manifest_id.ToString());
            return false;
        }
        const auto persisted_manifest_state =
            chainstate.m_chainman.GetShieldedNettingManifestState(manifest_state.manifest_id);
        if (!persisted_manifest_state.has_value() || *persisted_manifest_state != manifest_state) {
            error = strprintf("netting manifest metadata mismatch %s",
                              manifest_state.manifest_id.ToString());
            return false;
        }
    }

    if (include_proof_audit) {
        shielded::audit::ProofAuditArchive proof_archive;
        if (!BuildShieldedProofAuditArchive(chainstate, chainstate.m_chain.Tip(), proof_archive, error)) {
            return false;
        }
    }
    return true;
}

} // namespace

bool BuildShieldedProofAuditArchive(const Chainstate& chainstate,
                                    const CBlockIndex* tip,
                                    shielded::audit::ProofAuditArchive& archive,
                                    std::string& error)
{
    auto make_non_owning_snapshot = [](const auto& value) {
        using Value = std::decay_t<decltype(value)>;
        return std::shared_ptr<const Value>(&value, [](const Value*) {});
    };

    archive.entries.clear();
    archive.verified_count = 0;
    archive.failed_count = 0;
    error.clear();

    if (tip == nullptr) return true;

    shielded::ShieldedMerkleTree tree{
        shielded::ShieldedMerkleTree::IndexStorageMode::MEMORY_ONLY};
    ShieldedPoolBalance pool;
    std::map<uint256, smile2::CompactPublicAccount> public_accounts;
    std::map<uint256, uint256> account_leaf_commitments;

    std::vector<const CBlockIndex*> chain;
    chain.reserve(tip->nHeight + 1);
    for (const CBlockIndex* pindex = tip; pindex != nullptr; pindex = pindex->pprev) {
        chain.push_back(pindex);
    }
    std::reverse(chain.begin(), chain.end());

    for (const CBlockIndex* pindex : chain) {
        const bool use_nonced_bridge_tag =
            UseNoncedShieldedBridgeTags(chainstate.m_chainman.GetConsensus(), pindex->nHeight);
        CBlock block;
        if (!chainstate.m_blockman.ReadBlock(block, *pindex)) {
            error = strprintf("failed to read block %s for proof audit", pindex->GetBlockHash().ToString());
            return false;
        }
        for (const auto& txref : block.vtx) {
            if (!txref->HasShieldedBundle()) continue;

            const CShieldedBundle& bundle = txref->GetShieldedBundle();
            shielded::audit::ProofAuditEntry entry;
            entry.block_hash = pindex->GetBlockHash();
            entry.txid = txref->GetHash();
            entry.height = pindex->nHeight;
            entry.family = DescribeShieldedAuditFamily(bundle);
            entry.proof_kind = DescribeShieldedAuditProofKind(bundle);
            entry.verify_units = GetShieldedVerifyCost(bundle);

            CShieldedProofCheck proof_check(
                *txref,
                chainstate.m_chainman.GetConsensus(),
                pindex->nHeight,
                make_non_owning_snapshot(tree),
                make_non_owning_snapshot(public_accounts),
                make_non_owning_snapshot(account_leaf_commitments));
            if (const auto reject_reason = proof_check(); reject_reason.has_value()) {
                entry.reject_reason = *reject_reason;
                archive.failed_count += 1;
                archive.entries.push_back(entry);
                error = strprintf("shielded proof audit failed txid=%s height=%d family=%s reject=%s",
                                  entry.txid.ToString(),
                                  entry.height,
                                  entry.family,
                                  entry.reject_reason);
                return false;
            }
            entry.verified = true;
            archive.verified_count += 1;
            archive.entries.push_back(std::move(entry));

            std::string height_gate_reject;
            if (!RejectShieldedHeightGateViolation(*txref,
                                                   chainstate.m_chainman.GetConsensus(),
                                                   pindex->nHeight,
                                                   height_gate_reject)) {
                error = strprintf("shielded height-gate audit failed txid=%s height=%d reject=%s",
                                  txref->GetHash().ToString(),
                                  pindex->nHeight,
                                  height_gate_reject);
                return false;
            }
            if (!ApplyShieldedStateEffects(bundle,
                                           use_nonced_bridge_tag,
                                           pindex->GetBlockHash(),
                                           tree,
                                           pool,
                                           /*all_nullifiers=*/nullptr,
                                           &public_accounts,
                                           &account_leaf_commitments)) {
                error = strprintf("failed to apply shielded state effects while auditing block %s",
                                  pindex->GetBlockHash().ToString());
                return false;
            }
        }
    }

    return true;
}

const CBlockIndex* Chainstate::FindForkInGlobalIndex(const CBlockLocator& locator) const
{
    AssertLockHeld(cs_main);

    // Find the latest block common to locator and chain - we expect that
    // locator.vHave is sorted descending by height.
    for (const uint256& hash : locator.vHave) {
        const CBlockIndex* pindex{m_blockman.LookupBlockIndex(hash)};
        if (pindex) {
            if (m_chain.Contains(pindex)) {
                return pindex;
            }
            if (pindex->GetAncestor(m_chain.Height()) == m_chain.Tip()) {
                return m_chain.Tip();
            }
        }
    }
    return m_chain.Genesis();
}

bool CheckInputScripts(const CTransaction& tx, TxValidationState& state,
                       const CCoinsViewCache& inputs, unsigned int flags, bool cacheSigStore,
                       bool cacheFullScriptStore, PrecomputedTransactionData& txdata,
                       ValidationCache& validation_cache,
                       std::vector<CScriptCheck>* pvChecks = nullptr)
                       EXCLUSIVE_LOCKS_REQUIRED(cs_main);

bool CheckFinalTxAtTip(const CBlockIndex& active_chain_tip, const CTransaction& tx)
{
    AssertLockHeld(cs_main);

    // CheckFinalTxAtTip() uses active_chain_tip.Height()+1 to evaluate
    // nLockTime because when IsFinalTx() is called within
    // AcceptBlock(), the height of the block *being*
    // evaluated is what is used. Thus if we want to know if a
    // transaction can be part of the *next* block, we need to call
    // IsFinalTx() with one more than active_chain_tip.Height().
    int nBlockHeight{0};
    if (active_chain_tip.nHeight == std::numeric_limits<int>::max()) return false;
    nBlockHeight = active_chain_tip.nHeight + 1;

    // BIP113 requires that time-locked transactions have nLockTime set to
    // less than the median time of the previous block they're contained in.
    // When the next block is created its previous block will be the current
    // chain tip, so we use that to calculate the median time passed to
    // IsFinalTx().
    const int64_t nBlockTime{active_chain_tip.GetMedianTimePast()};

    return IsFinalTx(tx, nBlockHeight, nBlockTime);
}

namespace {
bool TryGetNextBlockHeight(int current_height, int& next_height)
{
    if (current_height == std::numeric_limits<int>::max()) return false;
    next_height = current_height + 1;
    return true;
}

int NextBlockHeightOrLimit(int current_height)
{
    int next_height{0};
    return TryGetNextBlockHeight(current_height, next_height)
        ? next_height
        : std::numeric_limits<int>::max();
}

bool CrossesShieldedMempoolHeightGate(const Consensus::Params& consensus,
                                      int32_t previous_next_block_height,
                                      int32_t current_next_block_height)
{
    const auto crosses = [&](int32_t gate_height) {
        return gate_height != std::numeric_limits<int32_t>::max() &&
               previous_next_block_height < gate_height &&
               current_next_block_height >= gate_height;
    };

    return crosses(consensus.nShieldedTxBindingActivationHeight) ||
           crosses(consensus.nShieldedBridgeTagActivationHeight) ||
           crosses(consensus.nShieldedSmileRiceCodecDisableHeight) ||
           crosses(consensus.nShieldedMatRiCTDisableHeight) ||
           crosses(consensus.nShieldedSpendPathRecoveryActivationHeight) ||
           crosses(consensus.nShieldedC002ActivationHeight) ||
           crosses(consensus.nShieldedPQ128UpgradeHeight) ||
           crosses(consensus.nShieldedPoolCreditDisableHeight) ||
           crosses(consensus.nShieldedSunsetHeight) ||
           crosses(consensus.nShieldedDirectSendPublicFlowDisableHeight) ||
           crosses(consensus.nShieldedV2SendZeroOutputExitActivationHeight) ||
           crosses(consensus.nShieldedRecoveryExitActivationHeight) ||
           crosses(consensus.nShieldedUnshieldVelocityActivationHeight) ||
           crosses(consensus.nShieldedUnshieldVelocityEndHeight);
}

constexpr int32_t MLDSA_EMERGENCY_RELAY_WINDOW_BLOCKS{960};

/**
 * A helper which calculates heights of inputs of a given transaction.
 *
 * @param[in] tip    The current chain tip. If an input belongs to a mempool
 *                   transaction, we assume it will be confirmed in the next block.
 * @param[in] coins  Any CCoinsView that provides access to the relevant coins.
 * @param[in] tx     The transaction being evaluated.
 *
 * @returns A vector of input heights or nullopt, in case of an error.
 */
std::optional<std::vector<int>> CalculatePrevHeights(
    const CBlockIndex& tip,
    const CCoinsView& coins,
    const CTransaction& tx)
{
    int mempool_confirmation_height{0};
    if (!TryGetNextBlockHeight(tip.nHeight, mempool_confirmation_height)) return std::nullopt;

    std::vector<int> prev_heights;
    prev_heights.resize(tx.vin.size());
    for (size_t i = 0; i < tx.vin.size(); ++i) {
        if (auto coin{coins.GetCoin(tx.vin[i].prevout)}) {
            prev_heights[i] = coin->nHeight == MEMPOOL_HEIGHT
                              ? mempool_confirmation_height // Assume all mempool transaction confirm in the next block.
                              : coin->nHeight;
        } else {
            LogPrintf("ERROR: %s: Missing input %d in transaction \'%s\'\n", __func__, i, tx.GetHash().GetHex());
            return std::nullopt;
        }
    }
    return prev_heights;
}
} // namespace

bool IsMLDSADisallowedForMempool(const Consensus::Params& consensusParams, int next_block_height)
{
    const int32_t disable_height = consensusParams.nMLDSADisableHeight;
    if (disable_height == std::numeric_limits<int32_t>::max()) return false;

    const int64_t emergency_start = std::max<int64_t>(
        0,
        static_cast<int64_t>(disable_height) - MLDSA_EMERGENCY_RELAY_WINDOW_BLOCKS);
    return static_cast<int64_t>(next_block_height) >= emergency_start;
}

bool IsSLHDSAFips205RequiredForMempool(const Consensus::Params& consensusParams, int next_block_height)
{
    return consensusParams.IsShieldedC002Active(next_block_height);
}

// Returns the script flags which should be checked for a given block
static unsigned int GetBlockScriptFlags(const CBlockIndex& block_index, const ChainstateManager& chainman);

[[nodiscard]] unsigned int GetNextBlockMempoolConsensusScriptFlags(
    const CBlockIndex& tip,
    const ChainstateManager& chainman,
    int next_block_height)
{
    unsigned int flags{GetBlockScriptFlags(tip, chainman)};
    const auto& consensus{chainman.GetConsensus()};
    if (IsMLDSADisallowedForMempool(consensus, next_block_height)) {
        flags |= SCRIPT_VERIFY_DISALLOW_MLDSA;
    }
    if (IsSLHDSAFips205RequiredForMempool(consensus, next_block_height)) {
        flags |= SCRIPT_VERIFY_SLHDSA_FIPS205;
    }
    return flags;
}

std::optional<LockPoints> CalculateLockPointsAtTip(
    CBlockIndex* tip,
    const CCoinsView& coins_view,
    const CTransaction& tx)
{
    assert(tip);

    auto prev_heights{CalculatePrevHeights(*tip, coins_view, tx)};
    if (!prev_heights.has_value()) return std::nullopt;

    CBlockIndex next_tip;
    next_tip.pprev = tip;
    // When SequenceLocks() is called within ConnectBlock(), the height
    // of the block *being* evaluated is what is used.
    // Thus if we want to know if a transaction can be part of the
    // *next* block, we need to use one more than active_chainstate.m_chain.Height()
    if (!TryGetNextBlockHeight(tip->nHeight, next_tip.nHeight)) return std::nullopt;
    const auto [min_height, min_time] = CalculateSequenceLocks(tx, STANDARD_LOCKTIME_VERIFY_FLAGS, prev_heights.value(), next_tip);

    // Also store the hash of the block with the highest height of
    // all the blocks which have sequence locked prevouts.
    // This hash needs to still be on the chain
    // for these LockPoint calculations to be valid
    // Note: It is impossible to correctly calculate a maxInputBlock
    // if any of the sequence locked inputs depend on unconfirmed txs,
    // except in the special case where the relative lock time/height
    // is 0, which is equivalent to no sequence lock. Since we assume
    // input height of tip+1 for mempool txs and test the resulting
    // min_height and min_time from CalculateSequenceLocks against tip+1.
    int max_input_height{0};
    for (const int height : prev_heights.value()) {
        // Can ignore mempool inputs since we'll fail if they had non-zero locks
        if (height != next_tip.nHeight) {
            max_input_height = std::max(max_input_height, height);
        }
    }

    // tip->GetAncestor(max_input_height) should never return a nullptr
    // because max_input_height is always less than the tip height.
    // It would, however, be a bad bug to continue execution, since a
    // LockPoints object with the maxInputBlock member set to nullptr
    // signifies no relative lock time.
    return LockPoints{min_height, min_time, Assert(tip->GetAncestor(max_input_height))};
}

bool CheckSequenceLocksAtTip(CBlockIndex* tip,
                             const LockPoints& lock_points)
{
    assert(tip != nullptr);

    CBlockIndex index;
    index.pprev = tip;
    // CheckSequenceLocksAtTip() uses active_chainstate.m_chain.Height()+1 to evaluate
    // height based locks because when SequenceLocks() is called within
    // ConnectBlock(), the height of the block *being*
    // evaluated is what is used.
    // Thus if we want to know if a transaction can be part of the
    // *next* block, we need to use one more than active_chainstate.m_chain.Height()
    if (!TryGetNextBlockHeight(tip->nHeight, index.nHeight)) return false;

    return EvaluateSequenceLocks(index, {lock_points.height, lock_points.time});
}

/** Compute accurate total signature operation cost of a transaction.
 *  Not consensus-critical, since legacy sigops counting is always used in the protocol.
 */
int64_t GetAccurateTransactionSigOpCost(const CTransaction& tx, const CCoinsViewCache& inputs, int flags)
{
    if (tx.IsCoinBase()) {
        return 0;
    }

    unsigned int nSigOps = 0;
    for (const auto& txin : tx.vin) {
        nSigOps += txin.scriptSig.GetSigOpCount(false);
    }

    if (flags & SCRIPT_VERIFY_P2SH) {
        nSigOps += GetP2SHSigOpCount(tx, inputs);
    }

    nSigOps *= WITNESS_SCALE_FACTOR;

    if (flags & SCRIPT_VERIFY_WITNESS) {
        for (const auto& txin : tx.vin) {
            const Coin& coin = inputs.AccessCoin(txin.prevout);
            assert(!coin.IsSpent());
            const CTxOut &prevout = coin.out;
            nSigOps += CountWitnessSigOps(txin.scriptSig, prevout.scriptPubKey, &txin.scriptWitness, flags);
        }
    }

    return nSigOps;
}

void LimitMempoolSize(CTxMemPool& pool, CCoinsViewCache& coins_cache)
    EXCLUSIVE_LOCKS_REQUIRED(::cs_main, pool.cs)
{
    AssertLockHeld(::cs_main);
    AssertLockHeld(pool.cs);
    int expired = pool.Expire(GetTime<std::chrono::seconds>() - pool.m_opts.expiry);
    if (expired != 0) {
        LogDebug(BCLog::MEMPOOL, "Expired %i transactions from the memory pool\n", expired);
    }

    std::vector<COutPoint> vNoSpendsRemaining;
    pool.TrimToSize(pool.m_opts.max_size_bytes, &vNoSpendsRemaining);
    for (const COutPoint& removed : vNoSpendsRemaining)
        coins_cache.Uncache(removed);
}

bool HasInvalidShieldedAnchors(const CTransaction& tx, const ChainstateManager& chainman)
{
    AssertLockHeld(::cs_main);
    if (!tx.HasShieldedBundle()) return false;

    const int32_t validation_height = chainman.ActiveChain().Height() + 1;
    for (const auto& anchor : CollectShieldedAnchors(tx.GetShieldedBundle())) {
        if (!chainman.IsShieldedAnchorValid(anchor)) {
            return true;
        }
    }
    for (const auto& settlement_anchor : CollectShieldedSettlementAnchorRefs(tx.GetShieldedBundle())) {
        if (!chainman.IsShieldedSettlementAnchorValid(settlement_anchor)) {
            return true;
        }
    }
    for (const auto& account_registry_anchor :
         CollectShieldedAccountRegistryRefs(tx.GetShieldedBundle())) {
        if (!chainman.IsShieldedAccountRegistryRootValid(account_registry_anchor)) {
            return true;
        }
    }
    if (tx.GetShieldedBundle().HasV2Bundle()) {
        const auto* v2_bundle = tx.GetShieldedBundle().GetV2Bundle();
        if (v2_bundle != nullptr &&
            shielded::v2::BundleHasSemanticFamily(*v2_bundle,
                                                  shielded::v2::TransactionFamily::V2_EGRESS_BATCH)) {
            const auto& payload =
                std::get<shielded::v2::EgressBatchPayload>(v2_bundle->payload);
            const auto anchor_state =
                chainman.GetShieldedSettlementAnchorState(payload.settlement_anchor);
            if (!anchor_state.has_value() ||
                !IsSettlementAnchorMature(*anchor_state,
                                          chainman.GetConsensus(),
                                          validation_height)) {
                return true;
            }
        }
        if (v2_bundle != nullptr &&
            shielded::v2::BundleHasSemanticFamily(*v2_bundle,
                                                  shielded::v2::TransactionFamily::V2_SETTLEMENT_ANCHOR)) {
            const auto& payload =
                std::get<shielded::v2::SettlementAnchorPayload>(v2_bundle->payload);
            if (!payload.anchored_netting_manifest_id.IsNull() &&
                !chainman.IsShieldedNettingManifestValid(payload.anchored_netting_manifest_id,
                                                         validation_height)) {
                return true;
            }
        }
    }
    return false;
}

bool HasInvalidShieldedRecoveryExitMempoolState(const CTransaction& tx, const ChainstateManager& chainman)
{
    AssertLockHeld(::cs_main);
    if (!tx.HasShieldedBundle()) return false;

    const CShieldedBundle& bundle = tx.GetShieldedBundle();
    if (!IsRecoveryExitBundle(bundle)) return false;

    shielded::recovery::RecoveryExitClaim re_claim;
    shielded::recovery::RecoveryExitIdentifiers re_ids;
    if (!GetRecoveryExitIdentifiersFromBundle(bundle, re_claim, re_ids)) {
        return true;
    }

    return chainman.IsShieldedNullifierSpent(re_ids.nullifier) ||
           chainman.IsShieldedRecoveryExitCommitmentRetired(re_ids.commitment);
}

bool HasInvalidShieldedPoolBalanceMempoolState(const CTransaction& tx, const ChainstateManager& chainman)
{
    AssertLockHeld(::cs_main);
    if (!tx.HasShieldedBundle()) return false;

    std::string pool_balance_reject;
    const auto state_value_balance =
        TryGetShieldedStateValueBalance(tx.GetShieldedBundle(), pool_balance_reject);
    if (!state_value_balance.has_value()) return true;

    ShieldedPoolBalance projected_pool;
    return !projected_pool.SetBalance(chainman.GetShieldedPoolBalance()) ||
           !projected_pool.ApplyValueBalance(*state_value_balance);
}

bool CollectShieldedMempoolRetirementsForBlock(const CTransaction& tx,
                                               std::vector<Nullifier>& out_nullifiers,
                                               std::vector<uint256>& out_recovery_commitments)
{
    out_nullifiers.clear();
    out_recovery_commitments.clear();
    if (!tx.HasShieldedBundle()) return true;

    const CShieldedBundle& bundle = tx.GetShieldedBundle();
    out_nullifiers = CollectShieldedNullifiers(bundle);

    return CollectRecoveryExitRetirements(bundle, &out_nullifiers, &out_recovery_commitments);
}

bool CollectShieldedMempoolNullifiersForBlock(const CTransaction& tx,
                                              std::vector<Nullifier>& out_nullifiers)
{
    std::vector<uint256> recovery_exit_commitments;
    return CollectShieldedMempoolRetirementsForBlock(tx,
                                                     out_nullifiers,
                                                     recovery_exit_commitments);
}

bool HasSpentShieldedMempoolNullifier(const CTransaction& tx, const ChainstateManager& chainman)
    EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);
    std::vector<Nullifier> nullifiers;
    if (!CollectShieldedMempoolNullifiersForBlock(tx, nullifiers)) return true;
    for (const auto& nullifier : nullifiers) {
        if (chainman.IsShieldedNullifierSpent(nullifier)) {
            return true;
        }
    }
    return false;
}

static bool CollectShieldedBlockRetirements(const std::vector<CTransactionRef>& block_vtx,
                                            std::set<uint256>& out_nullifiers,
                                            std::set<uint256>& out_recovery_commitments)
{
    out_nullifiers.clear();
    out_recovery_commitments.clear();

    for (const auto& tx : block_vtx) {
        if (!tx) return false;

        std::vector<Nullifier> tx_nullifiers;
        std::vector<uint256> tx_recovery_commitments;
        if (!CollectShieldedMempoolRetirementsForBlock(*tx,
                                                       tx_nullifiers,
                                                       tx_recovery_commitments)) {
            return false;
        }
        out_nullifiers.insert(tx_nullifiers.begin(), tx_nullifiers.end());
        out_recovery_commitments.insert(tx_recovery_commitments.begin(),
                                        tx_recovery_commitments.end());
    }
    return true;
}

void RemoveShieldedMempoolConflictsForBlock(CTxMemPool& pool,
                                            CChain& chain,
                                            ChainstateManager& chainman,
                                            Chainstate* active_chainstate,
                                            const std::vector<CTransactionRef>& block_vtx)
{
    AssertLockHeld(::cs_main);
    AssertLockHeld(pool.cs);

    std::set<uint256> block_nullifiers;
    std::set<uint256> block_recovery_commitments;
    if (!CollectShieldedBlockRetirements(block_vtx, block_nullifiers, block_recovery_commitments)) {
        LogWarning("RemoveShieldedMempoolConflictsForBlock: failed to collect connected-block shielded retirements; falling back to full shielded mempool cleanup\n");
        RemoveStaleShieldedAnchorMempoolTransactions(pool, chain, chainman, active_chainstate);
        return;
    }

    if (block_nullifiers.empty() && block_recovery_commitments.empty()) return;

    std::set<Txid> txids_to_remove;
    for (const auto& nullifier : block_nullifiers) {
        const auto it = pool.m_shielded_nullifiers.find(nullifier);
        if (it != pool.m_shielded_nullifiers.end()) {
            txids_to_remove.insert(it->second);
        }
    }
    for (const auto& commitment : block_recovery_commitments) {
        const auto it = pool.m_shielded_recovery_commitments.find(commitment);
        if (it != pool.m_shielded_recovery_commitments.end()) {
            txids_to_remove.insert(it->second);
        }
    }
    if (txids_to_remove.empty()) return;

    std::vector<CTransactionRef> txs_to_remove;
    for (const auto& it : pool.GetIterSet(txids_to_remove)) {
        txs_to_remove.push_back(it->GetSharedTx());
    }
    for (const auto& tx : txs_to_remove) {
        LogDebug(BCLog::MEMPOOL,
                 "removing mempool tx %s: shielded retirement conflict with connected block at height %d\n",
                 tx->GetHash().ToString(),
                 chain.Height());
        pool.removeRecursive(*tx, MemPoolRemovalReason::CONFLICT);
    }
}

void RemoveStaleShieldedPoolBalanceMempoolTransactions(CTxMemPool& pool,
                                                       CChain& chain,
                                                       ChainstateManager& chainman)
{
    AssertLockHeld(::cs_main);
    AssertLockHeld(pool.cs);

    const auto stale_pool_filter = [&](CTxMemPool::txiter it) EXCLUSIVE_LOCKS_REQUIRED(pool.cs, ::cs_main) {
        const CTransaction& tx = it->GetTx();
        if (!tx.HasShieldedBundle()) return false;
        if (!HasInvalidShieldedPoolBalanceMempoolState(tx, chainman)) return false;

        LogDebug(BCLog::MEMPOOL,
                 "removing mempool tx %s: stale shielded pool balance at height %d\n",
                 tx.GetHash().ToString(),
                 chain.Height());
        return true;
    };
    pool.removeForReorg(chain, stale_pool_filter);
}

void RemoveStaleShieldedAnchorMempoolTransactions(CTxMemPool& pool,
                                                  CChain& chain,
                                                  ChainstateManager& chainman,
                                                  Chainstate* active_chainstate)
{
    AssertLockHeld(::cs_main);
    AssertLockHeld(pool.cs);

    const bool shielded_state_ready = chainman.EnsureShieldedStateInitialized();
    const int32_t validation_height = chainman.ActiveChain().Height() + 1;
    const auto stale_anchor_filter = [&](CTxMemPool::txiter it) EXCLUSIVE_LOCKS_REQUIRED(pool.cs, ::cs_main) {
        const CTransaction& tx = it->GetTx();
        if (tx.HasShieldedBundle()) {
            std::string height_gate_reject;
            if (!RejectShieldedHeightGateViolation(tx,
                                                   chainman.GetConsensus(),
                                                   validation_height,
                                                   height_gate_reject)) {
                return true;
            }
            if (!shielded_state_ready) return true;
            std::vector<Nullifier> tx_nullifiers;
            std::vector<uint256> tx_recovery_commitments;
            if (!pool.GetShieldedRetirementsForMempoolTx(tx, tx_nullifiers, tx_recovery_commitments)) {
                LogDebug(BCLog::MEMPOOL,
                         "removing mempool tx %s: missing cached shielded retirements at height %d\n",
                         tx.GetHash().ToString(),
                         validation_height);
                return true;
            }
            for (const auto& nullifier : tx_nullifiers) {
                if (chainman.IsShieldedNullifierSpent(nullifier)) {
                    LogDebug(BCLog::MEMPOOL,
                             "removing mempool tx %s: stale shielded nullifier at height %d\n",
                             tx.GetHash().ToString(),
                             validation_height);
                    return true;
                }
            }
            for (const auto& commitment : tx_recovery_commitments) {
                if (chainman.IsShieldedRecoveryExitCommitmentRetired(commitment)) {
                    LogDebug(BCLog::MEMPOOL,
                             "removing mempool tx %s: stale recovery-exit shielded state at height %d\n",
                             tx.GetHash().ToString(),
                             validation_height);
                    return true;
                }
            }
            if (HasInvalidShieldedAnchors(tx, chainman)) return true;
        }

        if (active_chainstate != nullptr && !tx.IsCoinBase()) {
            const CBlockIndex* tip = active_chainstate->m_chain.Tip();
            if (tip == nullptr) return true;
            const unsigned int next_block_flags =
                GetNextBlockMempoolConsensusScriptFlags(*tip, chainman, validation_height);
            CCoinsViewMemPool view_mempool{&active_chainstate->CoinsTip(), pool};
            CCoinsViewCache view{&view_mempool};
            if (!view.HaveInputs(tx)) return true;
            std::string public_flow_reject;
            if (!RejectPostForkTransparentFundingV2SendContext(tx,
                                                               view,
                                                               chainman.GetConsensus(),
                                                               validation_height,
                                                               public_flow_reject)) {
                LogDebug(BCLog::MEMPOOL,
                         "removing mempool tx %s: stale shielded public flow at height %d (%s)\n",
                         tx.GetHash().ToString(),
                         validation_height,
                         public_flow_reject);
                return true;
            }
            TxValidationState script_state;
            PrecomputedTransactionData txdata;
            if (!CheckInputScripts(tx,
                                   script_state,
                                   view,
                                   next_block_flags,
                                   /*cacheSigStore=*/true,
                                   /*cacheFullScriptStore=*/true,
                                   txdata,
                                   active_chainstate->m_chainman.m_validation_cache)) {
                LogDebug(BCLog::MEMPOOL,
                         "removing mempool tx %s: stale next-block script flags at height %d (%s)\n",
                         tx.GetHash().ToString(),
                         validation_height,
                         script_state.ToString());
                return true;
            }
        }
        return false;
    };
    pool.removeForReorg(chain, stale_anchor_filter);
}

static bool IsCurrentForFeeEstimation(Chainstate& active_chainstate) EXCLUSIVE_LOCKS_REQUIRED(cs_main)
{
    AssertLockHeld(cs_main);
    if (active_chainstate.m_chainman.IsInitialBlockDownload()) {
        return false;
    }
    if (active_chainstate.m_chain.Tip()->GetBlockTime() < count_seconds(GetTime<std::chrono::seconds>() - MAX_FEE_ESTIMATION_TIP_AGE))
        return false;
    if (active_chainstate.m_chain.Height() < active_chainstate.m_chainman.m_best_header->nHeight - 1) {
        return false;
    }
    return true;
}

void Chainstate::MaybeUpdateMempoolForReorg(
    DisconnectedBlockTransactions& disconnectpool,
    bool fAddToMempool)
{
    if (!m_mempool) return;

    AssertLockHeld(cs_main);
    AssertLockHeld(m_mempool->cs);
    std::vector<uint256> vHashUpdate;
    {
        // disconnectpool is ordered so that the front is the most recently-confirmed
        // transaction (the last tx of the block at the tip) in the disconnected chain.
        // Iterate disconnectpool in reverse, so that we add transactions
        // back to the mempool starting with the earliest transaction that had
        // been previously seen in a block.
        const auto queuedTx = disconnectpool.take();
        auto it = queuedTx.rbegin();
        while (it != queuedTx.rend()) {
            // ignore validation errors in resurrected transactions
            if (!fAddToMempool || (*it)->IsCoinBase() ||
                AcceptToMemoryPool(*this, *it, GetTime(),
                    /*bypass_limits=*/true, /*test_accept=*/false).m_result_type !=
                        MempoolAcceptResult::ResultType::VALID) {
                // If the transaction doesn't make it in to the mempool, remove any
                // transactions that depend on it (which would now be orphans).
                m_mempool->removeRecursive(**it, MemPoolRemovalReason::REORG);
            } else if (m_mempool->exists(GenTxid::Txid((*it)->GetHash()))) {
                const uint256 txid = (*it)->GetHash();
                vHashUpdate.push_back(txid);
                // Reorg-resurrected transactions were previously removed from
                // the unbroadcast set after their first successful relay. Add
                // them back so the periodic rebroadcast path can announce them
                // to fresh peers on the new active chain.
                m_mempool->AddUnbroadcastTx(txid);
            }
            ++it;
        }
    }

    // AcceptToMemoryPool/addNewTransaction all assume that new mempool entries have
    // no in-mempool children, which is generally not true when adding
    // previously-confirmed transactions back to the mempool.
    // UpdateTransactionsFromBlock finds descendants of any transactions in
    // the disconnectpool that were added back and cleans up the mempool state.
    m_mempool->UpdateTransactionsFromBlock(vHashUpdate);

    // Predicate to use for filtering transactions in removeForReorg.
    // Checks whether the transaction is still final and, if it spends a coinbase output, mature.
    // Also updates valid entries' cached LockPoints if needed.
    // If false, the tx is still valid and its lockpoints are updated.
    // If true, the tx would be invalid in the next block; remove this entry and all of its descendants.
    // Note that TRUC rules are not applied here, so reorgs may cause violations of TRUC inheritance or
    // topology restrictions.
    const auto filter_final_and_mature = [&](CTxMemPool::txiter it)
        EXCLUSIVE_LOCKS_REQUIRED(m_mempool->cs, ::cs_main) {
        AssertLockHeld(m_mempool->cs);
        AssertLockHeld(::cs_main);
        const CTransaction& tx = it->GetTx();

        // The transaction must be final.
        if (!CheckFinalTxAtTip(*Assert(m_chain.Tip()), tx)) return true;

        const LockPoints& lp = it->GetLockPoints();
        // CheckSequenceLocksAtTip checks if the transaction will be final in the next block to be
        // created on top of the new chain.
        if (!TestLockPointValidity(m_chain, lp) || !CheckSequenceLocksAtTip(m_chain.Tip(), lp)) {
            const CCoinsViewMemPool view_mempool{&CoinsTip(), *m_mempool};
            const std::optional<LockPoints> new_lock_points{CalculateLockPointsAtTip(m_chain.Tip(), view_mempool, tx)};
            if (new_lock_points.has_value() && CheckSequenceLocksAtTip(m_chain.Tip(), *new_lock_points)) {
                // Now update the mempool entry lockpoints as well.
                it->UpdateLockPoints(*new_lock_points);
            } else {
                return true;
            }
        }

        // If the transaction spends any coinbase outputs, it must be mature.
        if (it->GetSpendsCoinbase()) {
            for (const CTxIn& txin : tx.vin) {
                if (m_mempool->exists(GenTxid::Txid(txin.prevout.hash))) continue;
                const Coin& coin{CoinsTip().AccessCoin(txin.prevout)};
                assert(!coin.IsSpent());
                int mempool_spend_height{0};
                if (!TryGetNextBlockHeight(m_chain.Tip()->nHeight, mempool_spend_height)) return true;
                const int64_t coinbase_depth{static_cast<int64_t>(mempool_spend_height) - coin.nHeight};
                if (coin.IsCoinBase() && coinbase_depth < COINBASE_MATURITY) {
                    return true;
                }
            }
        }

        // Transaction is still valid and cached LockPoints are updated.
        return false;
    };

    // We also need to remove any now-immature transactions
    m_mempool->removeForReorg(m_chain, filter_final_and_mature);
    RemoveStaleShieldedAnchorMempoolTransactions(*m_mempool, m_chain, m_chainman, this);
    // Re-limit mempool size, in case we added any transactions
    LimitMempoolSize(*m_mempool, this->CoinsTip());
}

/**
* Checks to avoid mempool polluting consensus critical paths since cached
* signature and script validity results will be reused if we validate this
* transaction again during block validation.
* */
static bool CheckInputsFromMempoolAndCache(const CTransaction& tx, TxValidationState& state,
                const CCoinsViewCache& view, const CTxMemPool& pool,
                unsigned int flags, PrecomputedTransactionData& txdata, CCoinsViewCache& coins_tip,
                ValidationCache& validation_cache)
                EXCLUSIVE_LOCKS_REQUIRED(cs_main, pool.cs)
{
    AssertLockHeld(cs_main);
    AssertLockHeld(pool.cs);

    assert(!tx.IsCoinBase());
    for (const CTxIn& txin : tx.vin) {
        const Coin& coin = view.AccessCoin(txin.prevout);

        // This coin was checked in PreChecks and MemPoolAccept
        // has been holding cs_main since then.
        Assume(!coin.IsSpent());
        if (coin.IsSpent()) return false;

        // If the Coin is available, there are 2 possibilities:
        // it is available in our current ChainstateActive UTXO set,
        // or it's a UTXO provided by a transaction in our mempool.
        // Ensure the scriptPubKeys in Coins from CoinsView are correct.
        const CTransactionRef& txFrom = pool.get(txin.prevout.hash);
        if (txFrom) {
            assert(txFrom->GetHash() == txin.prevout.hash);
            assert(txFrom->vout.size() > txin.prevout.n);
            assert(txFrom->vout[txin.prevout.n] == coin.out);
        } else {
            const Coin& coinFromUTXOSet = coins_tip.AccessCoin(txin.prevout);
            assert(!coinFromUTXOSet.IsSpent());
            assert(coinFromUTXOSet.out == coin.out);
        }
    }

    // Call CheckInputScripts() to cache signature and script validity against current tip consensus rules.
    return CheckInputScripts(tx, state, view, flags, /* cacheSigStore= */ true, /* cacheFullScriptStore= */ true, txdata, validation_cache);
}

namespace {

class MemPoolAccept
{
public:
    explicit MemPoolAccept(CTxMemPool& mempool, Chainstate& active_chainstate) :
        m_pool(mempool),
        m_view(&m_dummy),
        m_viewmempool(&active_chainstate.CoinsTip(), m_pool),
        m_active_chainstate(active_chainstate)
    {
    }

    // We put the arguments we're handed into a struct, so we can pass them
    // around easier.
    struct ATMPArgs {
        const CChainParams& m_chainparams;
        const int64_t m_accept_time;
        const ignore_rejects_type& m_ignore_rejects;
        /*
         * Return any outpoints which were not previously present in the coins
         * cache, but were added as a result of validating the tx for mempool
         * acceptance. This allows the caller to optionally remove the cache
         * additions if the associated transaction ends up being rejected by
         * the mempool.
         */
        std::vector<COutPoint>& m_coins_to_uncache;
        /** When true, the transaction or package will not be submitted to the mempool. */
        const bool m_test_accept;
        /** Whether we allow transactions to replace mempool transactions. If false,
         * any transaction spending the same inputs as a transaction in the mempool is considered
         * a conflict. */
        const bool m_allow_replacement;
        /** When true, allow sibling eviction. This only occurs in single transaction package settings. */
        const bool m_allow_sibling_eviction;
        /** When true, the mempool will not be trimmed when any transactions are submitted in
         * Finalize(). Instead, limits should be enforced at the end to ensure the package is not
         * partially submitted.
         */
        const bool m_package_submission;
        /** When true, use package feerates instead of individual transaction feerates for fee-based
         * policies such as mempool min fee and min relay fee.
         */
        const bool m_package_feerates;
        /** Used for local submission of transactions to catch "absurd" fees
         * due to fee miscalculation by wallets. std:nullopt implies unset, allowing any feerates.
         * Any individual transaction failing this check causes immediate failure.
         */
        const std::optional<CFeeRate> m_client_maxfeerate;

        /** Whether CPFP carveout and RBF carveout are granted. */
        const bool m_allow_carveouts;

        /** Parameters for single transaction mempool validation. */
        static ATMPArgs SingleAccept(const CChainParams& chainparams, int64_t accept_time,
                                     const ignore_rejects_type& ignore_rejects, std::vector<COutPoint>& coins_to_uncache,
                                     bool test_accept) {
            return ATMPArgs{/* m_chainparams */ chainparams,
                            /* m_accept_time */ accept_time,
                            /* m_ignore_rejects */ ignore_rejects,
                            /* m_coins_to_uncache */ coins_to_uncache,
                            /* m_test_accept */ test_accept,
                            /* m_allow_replacement */ true,
                            /* m_allow_sibling_eviction */ true,
                            /* m_package_submission */ false,
                            /* m_package_feerates */ false,
                            /* m_client_maxfeerate */ {}, // checked by caller
                            /* m_allow_carveouts */ true,
            };
        }

        /** Parameters for test package mempool validation through testmempoolaccept. */
        static ATMPArgs PackageTestAccept(const CChainParams& chainparams, int64_t accept_time,
                                          const ignore_rejects_type& ignore_rejects, std::vector<COutPoint>& coins_to_uncache) {
            return ATMPArgs{/* m_chainparams */ chainparams,
                            /* m_accept_time */ accept_time,
                            /* m_ignore_rejects */ ignore_rejects,
                            /* m_coins_to_uncache */ coins_to_uncache,
                            /* m_test_accept */ true,
                            /* m_allow_replacement */ false,
                            /* m_allow_sibling_eviction */ false,
                            /* m_package_submission */ false, // not submitting to mempool
                            /* m_package_feerates */ false,
                            /* m_client_maxfeerate */ {}, // checked by caller
                            /* m_allow_carveouts */ false,
            };
        }

        /** Parameters for child-with-unconfirmed-parents package validation. */
        static ATMPArgs PackageChildWithParents(const CChainParams& chainparams, int64_t accept_time,
                                                std::vector<COutPoint>& coins_to_uncache, const std::optional<CFeeRate>& client_maxfeerate, const ignore_rejects_type& ignore_rejects) {
            return ATMPArgs{/* m_chainparams */ chainparams,
                            /* m_accept_time */ accept_time,
                            /* m_ignore_rejects */ ignore_rejects,
                            /* m_coins_to_uncache */ coins_to_uncache,
                            /* m_test_accept */ false,
                            /* m_allow_replacement */ true,
                            /* m_allow_sibling_eviction */ false,
                            /* m_package_submission */ true,
                            /* m_package_feerates */ true,
                            /* m_client_maxfeerate */ client_maxfeerate,
                            /* m_allow_carveouts */ false,
            };
        }

        /** Parameters for a single transaction within a package. */
        static ATMPArgs SingleInPackageAccept(const ATMPArgs& package_args) {
            return ATMPArgs{/* m_chainparams */ package_args.m_chainparams,
                            /* m_accept_time */ package_args.m_accept_time,
                            empty_ignore_rejects,
                            /* m_coins_to_uncache */ package_args.m_coins_to_uncache,
                            /* m_test_accept */ package_args.m_test_accept,
                            /* m_allow_replacement */ true,
                            /* m_allow_sibling_eviction */ true,
                            /* m_package_submission */ true, // do not LimitMempoolSize in Finalize()
                            /* m_package_feerates */ false, // only 1 transaction
                            /* m_client_maxfeerate */ package_args.m_client_maxfeerate,
                            /* m_allow_carveouts */ false,
            };
        }

    private:
        // Private ctor to avoid exposing details to clients and allowing the possibility of
        // mixing up the order of the arguments. Use static functions above instead.
        ATMPArgs(const CChainParams& chainparams,
                 int64_t accept_time,
                 const ignore_rejects_type& ignore_rejects,
                 std::vector<COutPoint>& coins_to_uncache,
                 bool test_accept,
                 bool allow_replacement,
                 bool allow_sibling_eviction,
                 bool package_submission,
                 bool package_feerates,
                 std::optional<CFeeRate> client_maxfeerate,
                 bool allow_carveouts)
            : m_chainparams{chainparams},
              m_accept_time{accept_time},
              m_ignore_rejects{ignore_rejects},
              m_coins_to_uncache{coins_to_uncache},
              m_test_accept{test_accept},
              m_allow_replacement{allow_replacement},
              m_allow_sibling_eviction{allow_sibling_eviction},
              m_package_submission{package_submission},
              m_package_feerates{package_feerates},
              m_client_maxfeerate{client_maxfeerate},
              m_allow_carveouts{allow_carveouts}
        {
            // If we are using package feerates, we must be doing package submission.
            // It also means carveouts and sibling eviction are not permitted.
            if (m_package_feerates) {
                Assume(m_package_submission);
                Assume(!m_allow_carveouts);
                Assume(!m_allow_sibling_eviction);
            }
            if (m_allow_sibling_eviction) Assume(m_allow_replacement);
        }
    };

    /** Clean up all non-chainstate coins from m_view and m_viewmempool. */
    void CleanupTemporaryCoins() EXCLUSIVE_LOCKS_REQUIRED(cs_main, m_pool.cs);

    // Single transaction acceptance
    MempoolAcceptResult AcceptSingleTransaction(const CTransactionRef& ptx, ATMPArgs& args) EXCLUSIVE_LOCKS_REQUIRED(cs_main);

    /**
    * Multiple transaction acceptance. Transactions may or may not be interdependent, but must not
    * conflict with each other, and the transactions cannot already be in the mempool. Parents must
    * come before children if any dependencies exist.
    */
    PackageMempoolAcceptResult AcceptMultipleTransactions(const std::vector<CTransactionRef>& txns, ATMPArgs& args) EXCLUSIVE_LOCKS_REQUIRED(cs_main);

    /**
     * Submission of a subpackage.
     * If subpackage size == 1, calls AcceptSingleTransaction() with adjusted ATMPArgs to avoid
     * package policy restrictions like no CPFP carve out (PackageMempoolChecks)
     * and creates a PackageMempoolAcceptResult wrapping the result.
     *
     * If subpackage size > 1, calls AcceptMultipleTransactions() with the provided ATMPArgs.
     *
     * Also cleans up all non-chainstate coins from m_view at the end.
    */
    PackageMempoolAcceptResult AcceptSubPackage(const std::vector<CTransactionRef>& subpackage, ATMPArgs& args)
        EXCLUSIVE_LOCKS_REQUIRED(cs_main, m_pool.cs);

    /**
     * Package (more specific than just multiple transactions) acceptance. Package must be a child
     * with all of its unconfirmed parents, and topologically sorted.
     */
    PackageMempoolAcceptResult AcceptPackage(const Package& package, ATMPArgs& args) EXCLUSIVE_LOCKS_REQUIRED(cs_main);

private:
    // All the intermediate state that gets passed between the various levels
    // of checking a given transaction.
    struct Workspace {
        explicit Workspace(const CTransactionRef& ptx) : m_ptx(ptx), m_hash(ptx->GetHash()) {}
        /** Txids of mempool transactions that this transaction directly conflicts with or may
         * replace via sibling eviction. */
        /** .second=true is a consensus conflict, and .second=false is a policy conflict. */
        std::map<Txid, bool> m_conflicts_incl_policy;
        /** Iterators to mempool entries that this transaction directly conflicts with or may
         * replace via sibling eviction. */
        CTxMemPool::setEntries m_iters_conflicting;
        /** All mempool ancestors of this transaction. */
        CTxMemPool::setEntries m_ancestors;
        /* Handle to the tx in the changeset */
        CTxMemPool::ChangeSet::TxHandle m_tx_handle;
        /** Whether RBF-related data structures (m_conflicts, m_iters_conflicting,
         * m_replaced_transactions) include a sibling in addition to txns with conflicting inputs. */
        bool m_sibling_eviction{false};

        /** Virtual size of the transaction as used by the mempool, calculated using serialized size
         * of the transaction and sigops. */
        int64_t m_vsize;
        /** Fees paid by this transaction: total input amounts subtracted by total output amounts. */
        CAmount m_base_fees;
        /** Base fees + any fee delta set by the user with prioritisetransaction. */
        CAmount m_modified_fees;

        /** If we're doing package validation (i.e. m_package_feerates=true), the "effective"
         * package feerate of this transaction is the total fees divided by the total size of
         * transactions (which may include its ancestors and/or descendants). */
        CFeeRate m_package_feerate{0};

        const CTransactionRef& m_ptx;
        /** Txid. */
        const Txid& m_hash;
        TxValidationState m_state;
        /** A temporary cache containing serialized transaction data for signature verification.
         * Reused across PolicyScriptChecks and ConsensusScriptChecks. */
        PrecomputedTransactionData m_precomputed_txdata;
    };

    static inline bool MaybeReject_(TxValidationResult reason, const std::string& reason_str, const std::string& debug_msg, const ignore_rejects_type& ignore_rejects, TxValidationState& state) {
        if (ignore_rejects.count(reason_str)) {
            return false;
        }

        state.Invalid(reason, reason_str, debug_msg);
        return true;
    }

#define MaybeRejectDbg(reason, reason_str, debug_msg)  do {  \
    if (MaybeReject_(reason, reason_str, debug_msg, ignore_rejects, state)) {  \
        return false;  \
    }  \
} while(0)

#define MaybeReject(reason, reason_str)  MaybeRejectDbg(reason, reason_str, "")

    // Run the policy checks on a given transaction, excluding any script checks.
    // Looks up inputs, calculates feerate, considers replacement, evaluates
    // package limits, etc. As this function can be invoked for "free" by a peer,
    // only tests that are fast should be done here (to avoid CPU DoS).
    bool PreChecks(ATMPArgs& args, Workspace& ws) EXCLUSIVE_LOCKS_REQUIRED(cs_main, m_pool.cs);

    // Run checks for mempool replace-by-fee, only used in AcceptSingleTransaction.
    bool ReplacementChecks(ATMPArgs& args, Workspace& ws) EXCLUSIVE_LOCKS_REQUIRED(cs_main, m_pool.cs);

    // Enforce package mempool ancestor/descendant limits (distinct from individual
    // ancestor/descendant limits done in PreChecks) and run Package RBF checks.
    bool PackageMempoolChecks(const ATMPArgs& args, const std::vector<CTransactionRef>& txns,
                              std::vector<Workspace>& workspaces,
                              int64_t total_vsize,
                              PackageValidationState& package_state) EXCLUSIVE_LOCKS_REQUIRED(cs_main, m_pool.cs);

    // Run the script checks using our policy flags. As this can be slow, we should
    // only invoke this on transactions that have otherwise passed policy checks.
    bool PolicyScriptChecks(const ATMPArgs& args, Workspace& ws) EXCLUSIVE_LOCKS_REQUIRED(cs_main, m_pool.cs);

    // Re-run the script checks, using consensus flags, and try to cache the
    // result in the scriptcache. This should be done after
    // PolicyScriptChecks(). This requires that all inputs either be in our
    // utxo set or in the mempool.
    bool ConsensusScriptChecks(const ATMPArgs& args, Workspace& ws) EXCLUSIVE_LOCKS_REQUIRED(cs_main, m_pool.cs);

    // Try to add the transaction to the mempool, removing any conflicts first.
    void FinalizeSubpackage(const ATMPArgs& args) EXCLUSIVE_LOCKS_REQUIRED(cs_main, m_pool.cs);

    // Submit all transactions to the mempool and call ConsensusScriptChecks to add to the script
    // cache - should only be called after successful validation of all transactions in the package.
    // Does not call LimitMempoolSize(), so mempool max_size_bytes may be temporarily exceeded.
    bool SubmitPackage(const ATMPArgs& args, std::vector<Workspace>& workspaces, PackageValidationState& package_state,
                       std::map<Wtxid, MempoolAcceptResult>& results)
         EXCLUSIVE_LOCKS_REQUIRED(cs_main, m_pool.cs);

    // Compare a package's feerate against minimum allowed.
    bool CheckFeeRate(size_t package_size, CAmount package_fee, TxValidationState& state, const ignore_rejects_type& ignore_rejects) EXCLUSIVE_LOCKS_REQUIRED(::cs_main, m_pool.cs)
    {
        AssertLockHeld(::cs_main);
        AssertLockHeld(m_pool.cs);
        CAmount mempoolRejectFee = m_pool.GetMinFee().GetFee(package_size);
        if (mempoolRejectFee > 0 && package_fee < mempoolRejectFee && !ignore_rejects.count(rejectmsg_lowfee_mempool)) {
            return state.Invalid(TxValidationResult::TX_RECONSIDERABLE, "mempool min fee not met", strprintf("%d < %d", package_fee, mempoolRejectFee));
        }

        if (package_fee < m_pool.m_opts.min_relay_feerate.GetFee(package_size) && !ignore_rejects.count(rejectmsg_lowfee_relay)) {
            return state.Invalid(TxValidationResult::TX_RECONSIDERABLE, "min relay fee not met",
                                 strprintf("%d < %d", package_fee, m_pool.m_opts.min_relay_feerate.GetFee(package_size)));
        }
        return true;
    }

    ValidationCache& GetValidationCache()
    {
        return m_active_chainstate.m_chainman.m_validation_cache;
    }

private:
    CTxMemPool& m_pool;
    CCoinsViewCache m_view;
    CCoinsViewMemPool m_viewmempool;
    CCoinsView m_dummy;

    Chainstate& m_active_chainstate;

    // Fields below are per *sub*package state and must be reset prior to subsequent
    // AcceptSingleTransaction and AcceptMultipleTransactions invocations
    struct SubPackageState {
        /** Aggregated modified fees of all transactions, used to calculate package feerate. */
        CAmount m_total_modified_fees{0};
        /** Aggregated virtual size of all transactions, used to calculate package feerate. */
        int64_t m_total_vsize{0};

        /**
         * Shielded nullifiers already observed in this staged subpackage.
         * This closes package-only double-spend gaps where conflicting
         * nullifiers are not yet reflected in the mempool index.
         */
        std::set<Nullifier> m_package_shielded_nullifiers;
        /** Recovery commitments already claimed in this staged subpackage. */
        std::set<uint256> m_package_recovery_exit_commitments;
        /** Settlement anchors already referenced in this staged subpackage. */
        std::set<uint256> m_package_settlement_anchor_refs;
        /** Account-registry appends already staged in this subpackage. */
        uint64_t m_package_shielded_account_registry_appends{0};

        // RBF-related members
        /** Whether the transaction(s) would replace any mempool transactions and/or evict any siblings.
         * If so, RBF rules apply. */
        bool m_rbf{false};
        /** Mempool transactions that were replaced. */
        std::list<CTransactionRef> m_replaced_transactions;
        /* Changeset representing adding transactions and removing their conflicts. */
        std::unique_ptr<CTxMemPool::ChangeSet> m_changeset;

        /** Total modified fees of mempool transactions being replaced. */
        CAmount m_conflicting_fees{0};
        /** Total size (in virtual bytes) of mempool transactions being replaced. */
        size_t m_conflicting_size{0};
    };

    struct SubPackageState m_subpackage;

    /** Re-set sub-package state to not leak between evaluations */
    void ClearSubPackageState() EXCLUSIVE_LOCKS_REQUIRED(cs_main, m_pool.cs)
    {
        m_subpackage = SubPackageState{};

        // And clean coins while at it
        CleanupTemporaryCoins();
    }
};

bool MemPoolAccept::PreChecks(ATMPArgs& args, Workspace& ws)
{
    AssertLockHeld(cs_main);
    AssertLockHeld(m_pool.cs);
    const CTransactionRef& ptx = ws.m_ptx;
    const CTransaction& tx = *ws.m_ptx;
    const Txid& hash = ws.m_hash;

    // Copy/alias what we need out of args
    const int64_t nAcceptTime = args.m_accept_time;
    const ignore_rejects_type& ignore_rejects = args.m_ignore_rejects;
    std::vector<COutPoint>& coins_to_uncache = args.m_coins_to_uncache;

    // Alias what we need out of ws
    TxValidationState& state = ws.m_state;
    std::optional<int32_t> shielded_validation_height;

    if (!CheckTransaction(tx, state)) {
        return false; // state filled in by CheckTransaction
    }

    // Coinbase is only valid in a block, not as a loose transaction
    if (tx.IsCoinBase())
        return state.Invalid(TxValidationResult::TX_CONSENSUS, "coinbase");

    if (tx.version == TRUC_VERSION && m_pool.m_opts.truc_policy == TRUCPolicy::Reject) {
        return state.Invalid(TxValidationResult::TX_NOT_STANDARD, "version");
    }

    // Rather not work on nonstandard transactions (unless -testnet/-regtest)
    std::string reason;
    if (m_pool.m_opts.require_standard && !IsStandardTx(tx, m_pool.m_opts, reason, ignore_rejects)) {
        return state.Invalid(TxValidationResult::TX_NOT_STANDARD, reason);
    }

    // Transactions smaller than 65 non-witness bytes are not relayed to mitigate CVE-2017-12842.
    if (!tx.HasShieldedBundle() &&
        ::GetSerializeSize(TX_NO_WITNESS(tx)) < MIN_STANDARD_TX_NONWITNESS_SIZE) {
        MaybeReject(TxValidationResult::TX_NOT_STANDARD, "tx-size-small");
    }

    // Only accept nLockTime-using transactions that can be mined in the next
    // block; we don't want our mempool filled up with transactions that can't
    // be mined yet.
    if (!CheckFinalTxAtTip(*Assert(m_active_chainstate.m_chain.Tip()), tx)) {
        MaybeReject(TxValidationResult::TX_PREMATURE_SPEND, "non-final");
    }

    if (m_pool.exists(GenTxid::Wtxid(tx.GetWitnessHash()))) {
        // Exact transaction already exists in the mempool.
        return state.Invalid(TxValidationResult::TX_CONFLICT, "txn-already-in-mempool");
    } else if (m_pool.exists(GenTxid::Txid(tx.GetHash()))) {
        // Transaction with the same non-witness data but different witness (same txid, different
        // wtxid) already exists in the mempool.
        return state.Invalid(TxValidationResult::TX_CONFLICT, "txn-same-nonwitness-data-in-mempool");
    }

    const auto maybe_add_mempool_conflict =
        [&](const CTransaction& conflicting_tx, const char* reject_reason) -> bool {
            if (!args.m_allow_replacement) {
                return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, "bip125-replacement-disallowed");
            }
            if (ws.m_conflicts_incl_policy.count(conflicting_tx.GetHash())) {
                return true;
            }

            bool allow_rbf;
            if (m_pool.m_opts.rbf_policy == RBFPolicy::Always || ignore_rejects.count("txn-mempool-conflict")) {
                allow_rbf = true;
            } else if (m_pool.m_opts.rbf_policy == RBFPolicy::Never) {
                allow_rbf = false;
            } else {
                const bool recovery_exit_family_replacement =
                    IsRecoveryExitTransaction(tx) && IsRecoveryExitTransaction(conflicting_tx);
                allow_rbf = SignalsOptInRBF(conflicting_tx) ||
                            conflicting_tx.version == TRUC_VERSION ||
                            recovery_exit_family_replacement;
            }
            if (!allow_rbf) {
                return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, reject_reason);
            }

            ws.m_conflicts_incl_policy.emplace(conflicting_tx.GetHash(), true);
            return true;
        };

    if (tx.HasShieldedBundle()) {
        const Consensus::Params& consensus = m_active_chainstate.m_chainman.GetConsensus();
        const int next_block_height = m_active_chainstate.m_chain.Height() + 1;
        shielded_validation_height = next_block_height;
        const uint64_t current_registry_entries =
            m_active_chainstate.m_chainman.GetShieldedAccountRegistryEntryCount();
        if (next_block_height < consensus.nShieldedPoolActivationHeight) {
            return state.Invalid(TxValidationResult::TX_CONSENSUS, "bad-shielded-premature");
        }
        if (::GetSerializeSize(TX_WITH_WITNESS(tx)) > consensus.nMaxShieldedTxSize) {
            return state.Invalid(TxValidationResult::TX_CONSENSUS, "bad-shielded-tx-size");
        }

        if (!m_active_chainstate.m_chainman.EnsureShieldedStateInitialized()) {
            return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, "shielded-state-init-failed");
        }
        const auto shielded_conflicts = m_pool.GetShieldedNullifierConflicts(tx);
        if (shielded_conflicts.invalid_in_tx) {
            return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, "bad-shielded-nullifier-mempool-conflict");
        }
        for (const Txid& conflict_txid : shielded_conflicts.txids) {
            const auto conflict_iter = m_pool.GetIter(conflict_txid);
            if (!conflict_iter.has_value()) continue;
            if (!maybe_add_mempool_conflict(conflict_iter.value()->GetTx(),
                                            "bad-shielded-nullifier-mempool-conflict")) {
                return false;
            }
        }
        if (UseSingleUseSettlementAnchors(consensus, next_block_height) &&
            m_pool.HasShieldedSettlementAnchorConflict(tx)) {
            return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY,
                                 "bad-shielded-v2-egress-anchor-mempool-conflict");
        }
        const CShieldedBundle& bundle = tx.GetShieldedBundle();
        std::string height_gate_reject;
        if (!RejectShieldedHeightGateViolation(tx, consensus, next_block_height, height_gate_reject)) {
            return state.Invalid(TxValidationResult::TX_CONSENSUS, height_gate_reject);
        }
        for (const auto& nullifier : CollectShieldedNullifiers(bundle)) {
            if (!m_subpackage.m_package_shielded_nullifiers.insert(nullifier).second) {
                return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, "bad-shielded-nullifier-package-conflict");
            }
        }
        if (UseSingleUseSettlementAnchors(consensus, next_block_height)) {
            for (const auto& settlement_anchor : CollectShieldedSettlementAnchorRefs(bundle)) {
                if (!m_subpackage.m_package_settlement_anchor_refs.insert(settlement_anchor).second) {
                    return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY,
                                         "bad-shielded-v2-egress-anchor-package-conflict");
                }
            }
        }
        if (UseAccountRegistryAppendRateLimit(consensus, next_block_height)) {
            uint64_t bundle_account_registry_appends{0};
            if (!TryCountShieldedAccountRegistryAppends(
                    bundle, consensus, next_block_height, bundle_account_registry_appends)) {
                return state.Invalid(TxValidationResult::TX_CONSENSUS,
                                     "bad-shielded-account-registry-rate-limit");
            }
            if (bundle_account_registry_appends >
                    consensus.nMaxBlockShieldedAccountRegistryAppends ||
                m_subpackage.m_package_shielded_account_registry_appends >
                consensus.nMaxBlockShieldedAccountRegistryAppends - bundle_account_registry_appends) {
                return state.Invalid(TxValidationResult::TX_CONSENSUS,
                                     "bad-shielded-account-registry-rate-limit");
            }
            m_subpackage.m_package_shielded_account_registry_appends += bundle_account_registry_appends;
            if (m_subpackage.m_package_shielded_account_registry_appends >
                consensus.nMaxBlockShieldedAccountRegistryAppends) {
                return state.Invalid(TxValidationResult::TX_CONSENSUS,
                                     "bad-shielded-account-registry-rate-limit");
            }
        }
        if (UseAccountRegistryEntryCountLimit(consensus, next_block_height)) {
            if (current_registry_entries > consensus.nMaxShieldedAccountRegistryEntries) {
                return state.Invalid(TxValidationResult::TX_CONSENSUS,
                                     "bad-shielded-account-registry-size-limit");
            }
            if (WouldExceedAccountRegistryEntryCountLimit(
                    consensus,
                    current_registry_entries,
                    m_subpackage.m_package_shielded_account_registry_appends)) {
                return state.Invalid(TxValidationResult::TX_CONSENSUS,
                                     "bad-shielded-account-registry-size-limit");
            }
        }

        // Finding 9 fix: validate every historical shielded anchor reference
        // carried by the bundle before mempool admission.
        for (const auto& anchor : CollectShieldedAnchors(bundle)) {
            if (!m_active_chainstate.m_chainman.IsShieldedAnchorValid(anchor)) {
                // d3 fix (completes the rebuild removal): do NOT trigger the O(chain)
                // anchor-history rebuild from the mempool path either. A mempool tx
                // referencing an anchor the chain never created is peer misbehavior, not
                // local-state corruption -- repairing here let one cheap unconfirmed tx
                // force a genesis->tip replay, re-armable per shielded block. Anchor-history
                // self-repair remains on the PoW-gated ConnectBlock path and at startup.
                LogDebug(BCLog::MEMPOOL, "bad-shielded-anchor (mempool): provided=%s txid=%s\n",
                         anchor.ToString(),
                         tx.GetHash().ToString());
                return state.Invalid(TxValidationResult::TX_CONSENSUS, "bad-shielded-anchor");
            }
        }
        if (bundle.HasV2Bundle() &&
            bundle.GetTransactionFamily() == shielded::v2::V2_SPEND_PATH_RECOVERY) {
            if (!consensus.IsShieldedSpendPathRecoveryActive(next_block_height)) {
                LogShieldedV2ContextReject("mempool-v2-spend-path-recovery-disabled", bundle);
                return state.Invalid(TxValidationResult::TX_CONSENSUS,
                                     "bad-shielded-v2-spend-path-recovery-disabled");
            }
        }
        if (bundle.HasV2Bundle() &&
            bundle.GetTransactionFamily() != shielded::v2::TransactionFamily::V2_SEND &&
            bundle.GetTransactionFamily() != shielded::v2::V2_SPEND_PATH_RECOVERY &&
            bundle.GetTransactionFamily() != shielded::v2::TransactionFamily::V2_RECOVERY_EXIT &&
            bundle.GetTransactionFamily() != shielded::v2::TransactionFamily::V2_INGRESS_BATCH &&
            bundle.GetTransactionFamily() != shielded::v2::TransactionFamily::V2_EGRESS_BATCH &&
            bundle.GetTransactionFamily() != shielded::v2::TransactionFamily::V2_SETTLEMENT_ANCHOR &&
            bundle.GetTransactionFamily() != shielded::v2::TransactionFamily::V2_REBALANCE &&
            bundle.GetTransactionFamily() != shielded::v2::TransactionFamily::V2_LIFECYCLE) {
            LogShieldedV2ContextReject("mempool-allowed-family", bundle);
            return state.Invalid(TxValidationResult::TX_CONSENSUS, "bad-shielded-v2-contextual");
        }
        std::string retired_matrict_reject;
        if (!RejectRetiredMatRiCTEnvelopeAfterDisable(bundle,
                                                      m_active_chainstate.m_chainman.GetConsensus(),
                                                      next_block_height,
                                                      retired_matrict_reject)) {
            return state.Invalid(TxValidationResult::TX_CONSENSUS, retired_matrict_reject);
        }
        std::string account_registry_reject;
        if (!ValidateShieldedAccountRegistryRefs(bundle,
                                                 m_active_chainstate.m_chainman,
                                                 account_registry_reject)) {
            return state.Invalid(TxValidationResult::TX_CONSENSUS, account_registry_reject);
        }
        std::string pool_balance_reject;
        const auto state_value_balance = TryGetShieldedStateValueBalance(bundle, pool_balance_reject);
        if (!state_value_balance.has_value()) {
            return state.Invalid(TxValidationResult::TX_CONSENSUS, pool_balance_reject);
        }
        ShieldedPoolBalance projected_pool;
        if (!projected_pool.SetBalance(m_active_chainstate.m_chainman.GetShieldedPoolBalance()) ||
            !projected_pool.ApplyValueBalance(*state_value_balance)) {
            return state.Invalid(TxValidationResult::TX_CONSENSUS, "shielded-pool-balance-negative");
        }
        if (IsRecoveryExitBundle(bundle)) {
            shielded::recovery::RecoveryExitClaim re_claim;
            shielded::recovery::RecoveryExitIdentifiers re_ids;
            if (!GetRecoveryExitIdentifiersFromBundle(bundle, re_claim, re_ids)) {
                return state.Invalid(TxValidationResult::TX_CONSENSUS, "bad-shielded-recovery-exit-malformed");
            }
            CAmount re_transparent_out{0};
            std::string re_sum_reject;
            if (!TrySumTransparentOutputs(tx.vout, re_transparent_out, re_sum_reject)) {
                return state.Invalid(TxValidationResult::TX_CONSENSUS, re_sum_reject);
            }
            CAmount re_fee{0};
            std::string re_fee_reject;
            if (!TryComputeRecoveryExitFee(re_claim.value, re_transparent_out, re_fee, re_fee_reject)) {
                return state.Invalid(TxValidationResult::TX_CONSENSUS, re_fee_reject);
            }
            const bool re_nullifier_already_spent =
                m_subpackage.m_package_shielded_nullifiers.count(re_ids.nullifier) > 0 ||
                m_active_chainstate.m_chainman.IsShieldedNullifierSpent(re_ids.nullifier);
            if (re_nullifier_already_spent) {
                return state.Invalid(TxValidationResult::TX_CONSENSUS, "bad-recovery-exit-nullifier-spent");
            }
            const bool re_commitment_already_claimed =
                m_subpackage.m_package_recovery_exit_commitments.count(re_ids.commitment) > 0 ||
                m_active_chainstate.m_chainman.IsShieldedRecoveryExitCommitmentRetired(re_ids.commitment);
            if (re_commitment_already_claimed) {
                return state.Invalid(TxValidationResult::TX_CONSENSUS, "bad-recovery-exit-commitment-claimed");
            }
            const uint256 re_tx_binding = shielded::recovery::ComputeRecoveryExitTransparentBinding(tx.vout);
            const uint256 re_binding_hash = shielded::recovery::ComputeRecoveryExitBindingHash(
                re_ids.commitment, re_ids.nullifier, re_claim.value, re_tx_binding);
            const uint256 re_membership_root = RecoveryExitMembershipRoot(
                consensus,
                next_block_height,
                m_active_chainstate.m_chainman.GetShieldedMerkleTree());
            std::string re_membership_reject;
            shielded::recovery::RecoveryExitConstraints re_c;
            re_c.value_balance = re_claim.value;
            re_c.fee = re_fee;
            re_c.transparent_out = re_transparent_out;
            re_c.transparent_input_count = tx.vin.size();
            re_c.shielded_output_count = bundle.GetShieldedOutputCount();
            re_c.pool_balance = m_active_chainstate.m_chainman.GetShieldedPoolBalance();
            re_c.validation_height = next_block_height;
            re_c.activation_height = consensus.nShieldedRecoveryExitActivationHeight;
            re_c.expiry_height = 0;
            re_c.ownership_verified = shielded::recovery::VerifyRecoveryExitOwnership(re_claim, re_binding_hash);
            re_c.membership_verified = shielded::recovery::VerifyRecoveryExitMembership(
                re_claim, re_ids.commitment, re_membership_root, re_membership_reject);
            re_c.nullifier_already_spent = re_nullifier_already_spent;
            re_c.commitment_already_claimed = re_commitment_already_claimed;
            shielded::recovery::RecoveryExitIdentifiers re_checked;
            std::string re_reject;
            if (!shielded::recovery::CheckRecoveryExitClaim(re_claim, re_c, re_checked, re_reject)) {
                return state.Invalid(TxValidationResult::TX_CONSENSUS, re_reject);
            }
            if (!m_subpackage.m_package_shielded_nullifiers.insert(re_ids.nullifier).second) {
                return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, "bad-shielded-nullifier-package-conflict");
            }
            if (!m_subpackage.m_package_recovery_exit_commitments.insert(re_ids.commitment).second) {
                return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, "bad-shielded-recovery-exit-package-conflict");
            }
        }
        if (bundle.HasShieldedInputs()) {
            std::string ring_reject;
            const uint64_t shielded_tree_size = m_active_chainstate.m_chainman.GetShieldedMerkleTree().Size();
            if (!RejectShieldedSmallPrivacyPool(bundle,
                                                shielded_tree_size,
                                                m_active_chainstate.m_chainman.GetConsensus(),
                                                next_block_height,
                                                ring_reject)) {
                return state.Invalid(TxValidationResult::TX_CONSENSUS, ring_reject);
            }
            if (!PrecheckShieldedRingPositions(bundle,
                                               shielded_tree_size,
                                               ring_reject)) {
                return state.Invalid(TxValidationResult::TX_CONSENSUS, ring_reject);
            }
            std::string proof_plausibility_reject;
            if (!PrecheckShieldedProofPlausibility(bundle, proof_plausibility_reject)) {
                return state.Invalid(TxValidationResult::TX_CONSENSUS, proof_plausibility_reject);
            }

            const auto bundle_nullifiers = CollectShieldedNullifiers(bundle);
            for (size_t spend_index = 0; spend_index < bundle.GetShieldedInputCount(); ++spend_index) {
                const auto& nullifier = bundle_nullifiers[spend_index];
                if (m_active_chainstate.m_chainman.IsShieldedNullifierSpent(nullifier)) {
                    return state.Invalid(TxValidationResult::TX_CONSENSUS, "bad-shielded-nullifier-spent");
                }
            }
        }
        if (bundle.HasV2Bundle()) {
            switch (*bundle.GetTransactionFamily()) {
            case shielded::v2::TransactionFamily::V2_EGRESS_BATCH: {
                const auto& payload =
                    std::get<shielded::v2::EgressBatchPayload>(bundle.GetV2Bundle()->payload);
                const auto anchor_state =
                    m_active_chainstate.m_chainman.GetShieldedSettlementAnchorState(payload.settlement_anchor);
                if (!anchor_state.has_value()) {
                    return state.Invalid(TxValidationResult::TX_CONSENSUS, "bad-shielded-v2-egress-unanchored");
                }
                if (UseSettlementAnchorMaturityRule(m_active_chainstate.m_chainman.GetConsensus(),
                                                    next_block_height) &&
                    !IsSettlementAnchorMature(*anchor_state,
                                              m_active_chainstate.m_chainman.GetConsensus(),
                                              next_block_height)) {
                    return state.Invalid(TxValidationResult::TX_CONSENSUS,
                                         "bad-shielded-v2-egress-immature-anchor");
                }
                break;
            }
            case shielded::v2::TransactionFamily::V2_SETTLEMENT_ANCHOR: {
                const auto& payload =
                    std::get<shielded::v2::SettlementAnchorPayload>(bundle.GetV2Bundle()->payload);
                if (!payload.anchored_netting_manifest_id.IsNull() &&
                    !m_active_chainstate.m_chainman.IsShieldedNettingManifestValid(payload.anchored_netting_manifest_id,
                                                                                   next_block_height)) {
                    return state.Invalid(TxValidationResult::TX_CONSENSUS,
                                         "bad-shielded-v2-settlement-unanchored-manifest");
                }
                break;
            }
            case shielded::v2::TransactionFamily::V2_REBALANCE:
            case shielded::v2::TransactionFamily::V2_SEND:
            case shielded::v2::TransactionFamily::V2_SPEND_PATH_RECOVERY:
            case shielded::v2::TransactionFamily::V2_RECOVERY_EXIT:
            case shielded::v2::TransactionFamily::V2_INGRESS_BATCH:
            case shielded::v2::TransactionFamily::V2_LIFECYCLE:
                break;
            case shielded::v2::TransactionFamily::V2_GENERIC:
                LogShieldedV2ContextReject("mempool-v2-generic-family", bundle);
                return state.Invalid(TxValidationResult::TX_CONSENSUS, "bad-shielded-v2-contextual");
            }
        }
    }

    auto spk_reuse_mode = SpkReuseMode;
    if (ignore_rejects.count("txn-spk-reused")) {
        spk_reuse_mode = SRM_ALLOW;
    }
    SPKStates_t mapSPK;

    // Check for conflicts with in-memory transactions
    for (const CTxIn &txin : tx.vin)
    {
        const CTransaction* ptxConflicting = m_pool.GetConflictTx(txin.prevout);
        if (ptxConflicting) {
            if (!maybe_add_mempool_conflict(*ptxConflicting, "txn-mempool-conflict")) {
                return false;
            }
        }
    }

    if (spk_reuse_mode != SRM_ALLOW) {
        for (const CTxOut& txout : tx.vout) {
            uint160 hashSPK = ScriptHashkey(txout.scriptPubKey);
            const auto& SPKUsedIn = m_pool.mapUsedSPK.find(hashSPK);
            if (SPKUsedIn != m_pool.mapUsedSPK.end()) {
                if (SPKUsedIn->second.first) {
                    ws.m_conflicts_incl_policy.emplace(SPKUsedIn->second.first->GetHash(), false);
                }
                if (SPKUsedIn->second.second) {
                    ws.m_conflicts_incl_policy.emplace(SPKUsedIn->second.second->GetHash(), false);
                }
            }
            if (mapSPK.find(hashSPK) != mapSPK.end()) {
                MaybeReject(TxValidationResult::TX_MEMPOOL_POLICY, "txn-spk-reused-twinoutputs");
            }
            mapSPK[hashSPK] = MemPool_SPK_State(mapSPK[hashSPK] | MSS_CREATED);
        }
    }

    m_view.SetBackend(m_viewmempool);

    const CCoinsViewCache& coins_cache = m_active_chainstate.CoinsTip();
    // do all inputs exist?
    for (const CTxIn& txin : tx.vin) {
        if (!coins_cache.HaveCoinInCache(txin.prevout)) {
            coins_to_uncache.push_back(txin.prevout);
        }

        // Note: this call may add txin.prevout to the coins cache
        // (coins_cache.cacheCoins) by way of FetchCoin(). It should be removed
        // later (via coins_to_uncache) if this tx turns out to be invalid.
        if (!m_view.HaveCoin(txin.prevout)) {
            // Are inputs missing because we already have the tx?
            for (size_t out = 0; out < tx.vout.size(); out++) {
                // Optimistically just do efficient check of cache for outputs
                if (coins_cache.HaveCoinInCache(COutPoint(hash, out))) {
                    return state.Invalid(TxValidationResult::TX_CONFLICT, "txn-already-known");
                }
            }
            // Otherwise assume this might be an orphan tx for which we just haven't seen parents yet
            return state.Invalid(TxValidationResult::TX_MISSING_INPUTS, "bad-txns-inputs-missingorspent");
        }
    }

    // This is const, but calls into the back end CoinsViews. The CCoinsViewDB at the bottom of the
    // hierarchy brings the best block into scope. See CCoinsViewDB::GetBestBlock().
    m_view.GetBestBlock();

    // we have all inputs cached now, so switch back to dummy (to protect
    // against bugs where we pull more inputs from disk that miss being added
    // to coins_to_uncache)
    m_view.SetBackend(m_dummy);

    assert(m_active_chainstate.m_blockman.LookupBlockIndex(m_view.GetBestBlock()) == m_active_chainstate.m_chain.Tip());

    // Only accept BIP68 sequence locked transactions that can be mined in the next
    // block; we don't want our mempool filled up with transactions that can't
    // be mined yet.
    // Pass in m_view which has all of the relevant inputs cached. Note that, since m_view's
    // backend was removed, it no longer pulls coins from the mempool.
    const std::optional<LockPoints> lock_points{CalculateLockPointsAtTip(m_active_chainstate.m_chain.Tip(), m_view, tx)};
    // NOTE: The miner doesn't check this again, so for now it may not be overridden.
    if (!lock_points.has_value() || !CheckSequenceLocksAtTip(m_active_chainstate.m_chain.Tip(), *lock_points)) {
        return state.Invalid(TxValidationResult::TX_PREMATURE_SPEND, "non-BIP68-final");
    }

    // The mempool holds txs for the next block, so pass height+1 to CheckTxInputs
    const auto block_height_current = m_active_chainstate.m_chain.Height();
    int block_height_next{0};
    if (!TryGetNextBlockHeight(block_height_current, block_height_next)) {
        return state.Invalid(TxValidationResult::TX_PREMATURE_SPEND, "bad-txns-height-overflow");
    }
    if (!Consensus::CheckTxInputs(tx, state, m_view, block_height_next, ws.m_base_fees)) {
        return false; // state filled in by CheckTxInputs
    }
    if (tx.HasShieldedBundle()) {
        const auto& bundle = tx.GetShieldedBundle();
        if (bundle.HasV2Bundle() &&
            shielded::UseShieldedCanonicalFeeBuckets(
                m_active_chainstate.m_chainman.GetConsensus(),
                block_height_next)) {
            const auto family = bundle.GetTransactionFamily();
            if (family.has_value()) switch (*family) {
            case shielded::v2::TransactionFamily::V2_SEND:
                if (!shielded::IsCanonicalShieldedFee(
                        ws.m_base_fees,
                        m_active_chainstate.m_chainman.GetConsensus(),
                        block_height_next)) {
                    return state.Invalid(TxValidationResult::TX_CONSENSUS,
                                         "bad-shielded-v2-send-fee-bucket");
                }
                break;
            case shielded::v2::TransactionFamily::V2_INGRESS_BATCH:
                if (const auto* v2_bundle = bundle.GetV2Bundle();
                    v2_bundle != nullptr &&
                    !shielded::IsCanonicalShieldedFee(
                        std::get<shielded::v2::IngressBatchPayload>(v2_bundle->payload).fee,
                        m_active_chainstate.m_chainman.GetConsensus(),
                        block_height_next)) {
                    return state.Invalid(TxValidationResult::TX_CONSENSUS,
                                         "bad-shielded-v2-ingress-fee-bucket");
                }
                break;
            case shielded::v2::TransactionFamily::V2_REBALANCE:
                if (ws.m_base_fees > 0 &&
                    !shielded::IsCanonicalShieldedFee(
                        ws.m_base_fees,
                        m_active_chainstate.m_chainman.GetConsensus(),
                        block_height_next)) {
                    return state.Invalid(TxValidationResult::TX_CONSENSUS,
                                         "bad-shielded-v2-rebalance-fee-bucket");
                }
                break;
            default:
                break;
            }
        } else if (!bundle.HasV2Bundle() &&
                   bundle.IsShieldOnly() &&
                   ws.m_base_fees > 0 &&
                   shielded::UseShieldedCanonicalFeeBuckets(
                       m_active_chainstate.m_chainman.GetConsensus(),
                       block_height_next) &&
                   !shielded::IsCanonicalShieldedFee(
                       ws.m_base_fees,
                       m_active_chainstate.m_chainman.GetConsensus(),
                       block_height_next)) {
            return state.Invalid(TxValidationResult::TX_CONSENSUS,
                                 "bad-shielded-legacy-fee-bucket");
        }
        if (bundle.HasV2Bundle() &&
            bundle.GetTransactionFamily() == shielded::v2::TransactionFamily::V2_REBALANCE &&
            UseZeroFeeRebalanceConsensusRule(m_active_chainstate.m_chainman.GetConsensus(), block_height_next) &&
            ws.m_base_fees <= 0) {
            return state.Invalid(TxValidationResult::TX_CONSENSUS, "bad-shielded-v2-rebalance-zero-fee");
        }
    }

    if (m_pool.m_opts.minrelaymaturity) {
        auto max_coin_height = block_height_next - m_pool.m_opts.minrelaymaturity;
        static_assert(std::is_signed_v<decltype(max_coin_height)>, "Unsigned max_coin_height needs a range check");
        for (const CTxIn &txin : tx.vin) {
            const Coin &coin = m_view.AccessCoin(txin.prevout);
            if (coin.nHeight > max_coin_height) {
                MaybeReject(TxValidationResult::TX_PREMATURE_SPEND, "bad-txns-input-immature-depth");
            }
        }
    }

    if (spk_reuse_mode != SRM_ALLOW) {
        for (const CTxIn& txin : tx.vin) {
            const Coin &coin = m_view.AccessCoin(txin.prevout);
            uint160 hashSPK = ScriptHashkey(coin.out.scriptPubKey);

            SPKStates_t::iterator mssit = mapSPK.find(hashSPK);
            if (mssit != mapSPK.end()) {
                if (mssit->second & MSS_CREATED) {
                    MaybeReject(TxValidationResult::TX_MEMPOOL_POLICY, "txn-spk-reused-change");
                }
            }
            const auto& SPKit = m_pool.mapUsedSPK.find(hashSPK);
            if (SPKit != m_pool.mapUsedSPK.end()) {
                if (SPKit->second.second /* Spent */) {
                    ws.m_conflicts_incl_policy.emplace(SPKit->second.second->GetHash(), false);
                }
            }
            mapSPK[hashSPK] = MemPool_SPK_State(mapSPK[hashSPK] | MSS_SPENT);
        }
    }

    if (m_pool.m_opts.require_standard && !AreInputsStandard(tx, m_view, m_pool.m_opts, "bad-txns-input-", reason, ignore_rejects)) {
        return state.Invalid(TxValidationResult::TX_INPUTS_NOT_STANDARD, reason);
    }

    if (m_pool.m_opts.datacarrier_fullcount || !m_pool.m_opts.accept_non_std_datacarrier) {
        const auto dcb = DatacarrierBytes(tx, m_view);
        if (dcb.second > 0 && !(m_pool.m_opts.accept_non_std_datacarrier || ignore_rejects.count("txn-datacarrier-nonstandard"))) {
            return state.Invalid(TxValidationResult::TX_INPUTS_NOT_STANDARD, "txn-datacarrier-nonstandard");
        }
        if (m_pool.m_opts.datacarrier_fullcount && (!ignore_rejects.count("txn-datacarrier-exceeded")) && dcb.first + dcb.second > m_pool.m_opts.max_datacarrier_bytes.value_or(0)) {
            return state.Invalid(TxValidationResult::TX_INPUTS_NOT_STANDARD, "txn-datacarrier-exceeded");
        }
    }

    // Check for non-standard witnesses.
    if (tx.HasWitness() && m_pool.m_opts.require_standard && !IsWitnessStandard(tx, m_view, "bad-witness-", reason, ignore_rejects)) {
        return state.Invalid(TxValidationResult::TX_WITNESS_MUTATED, reason);
    }

    int64_t nSigOpsCost = GetTransactionSigOpCost(tx, m_view, STANDARD_SCRIPT_VERIFY_FLAGS);

    // Keep track of transactions that spend a coinbase, which we re-scan
    // during reorgs to ensure COINBASE_MATURITY is still met.
    bool fSpendsCoinbase = false;
    for (const CTxIn &txin : tx.vin) {
        const Coin &coin = m_view.AccessCoin(txin.prevout);
        if (coin.IsCoinBase()) {
            fSpendsCoinbase = true;
            break;
        }
    }

    // Since entries arrive *after* the tip's height, their priority is for the height+1
    const auto coin_age = GetCoinAge(tx, m_view, block_height_next);

    if (coin_age.inputs_coin_age < m_pool.m_opts.minrelaycoinblocks) {
        MaybeReject(TxValidationResult::TX_PREMATURE_SPEND, "bad-txns-input-immature-coinblocks");
    }

    // Set entry_sequence to 0 when rejectmsg_zero_mempool_entry_seq is used; this allows txs from a block
    // reorg to be marked earlier than any child txs that were already in the mempool.
    const uint64_t entry_sequence = args.m_ignore_rejects.count(rejectmsg_zero_mempool_entry_seq) ? 0 : m_pool.GetSequence();
    int32_t extra_weight = CalculateExtraTxWeight(*ptx, m_view, ::g_weight_per_data_byte);
    if (!m_subpackage.m_changeset) {
        m_subpackage.m_changeset = m_pool.GetChangeSet();
    }
    ws.m_tx_handle = m_subpackage.m_changeset->StageAddition(ptx, ws.m_base_fees, nAcceptTime, block_height_current, entry_sequence, coin_age, fSpendsCoinbase, /*extra_weight=*/ extra_weight, /*sigops_cost=*/ nSigOpsCost, lock_points.value());

    if (spk_reuse_mode != SRM_ALLOW) {
        m_subpackage.m_changeset->m_to_add.modify(ws.m_tx_handle, [=](CTxMemPoolEntry& e) {
            e.mapSPK = mapSPK;
        });
    }

    // ws.m_modified_fees includes any fee deltas from PrioritiseTransaction
    ws.m_modified_fees = ws.m_tx_handle->GetModifiedFee();

    ws.m_vsize = ws.m_tx_handle->GetTxSize();

    // Enforces 0-fee for dust transactions, no incentive to be mined alone
    if (m_pool.m_opts.require_standard && !ignore_rejects.count("dust")) {
        if (!PreCheckEphemeralTx(*ptx, m_pool.m_opts.dust_relay_feerate, ws.m_base_fees, ws.m_modified_fees, state)) {
            return false; // state filled in by PreCheckEphemeralTx
        }
    }

    // To avoid rejecting low-sigop bare-multisig transactions, the sigops
    // are counted a second time more accurately.
    if ((nSigOpsCost > MAX_STANDARD_TX_SIGOPS_COST) || (nBytesPerSigOpStrict && GetAccurateTransactionSigOpCost(tx, m_view, STANDARD_SCRIPT_VERIFY_FLAGS) > ws.m_vsize * WITNESS_SCALE_FACTOR / nBytesPerSigOpStrict)) {
        MaybeRejectDbg(TxValidationResult::TX_NOT_STANDARD, "bad-txns-too-many-sigops",
                strprintf("%d", nSigOpsCost));
    }

    // No individual transactions are allowed below the min relay feerate except from disconnected blocks.
    // This requirement, unlike CheckFeeRate, cannot be bypassed using m_package_feerates because,
    // while a tx could be package CPFP'd when entering the mempool, we do not have a DoS-resistant
    // method of ensuring the tx remains bumped. For example, the fee-bumping child could disappear
    // due to a replacement.
    // The only exception is TRUC transactions.
    const int64_t relay_policy_vsize = tx.HasShieldedBundle() ? GetShieldedRelayVirtualSize(tx) : ws.m_vsize;
    const CAmount min_relay_fee = m_pool.m_opts.min_relay_feerate.GetFee(relay_policy_vsize);
    if ((ws.m_ptx->version != TRUC_VERSION || m_pool.m_opts.truc_policy != TRUCPolicy::Enforce) && ws.m_modified_fees < min_relay_fee && !args.m_ignore_rejects.count(rejectmsg_lowfee_relay)) {
        // Even though this is a fee-related failure, this result is TX_MEMPOOL_POLICY, not
        // TX_RECONSIDERABLE, because it cannot be bypassed using package validation.
        return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, "min relay fee not met",
                             strprintf("%d < %d", ws.m_modified_fees, min_relay_fee));
    }
    if (tx.HasShieldedBundle()) {
        const auto shielded_min_relay = CheckedAdd(min_relay_fee, MIN_SHIELDED_RELAY_FEE_PREMIUM);
        if (!shielded_min_relay.has_value()) {
            return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, "bad-shielded-fee-overflow");
        }
        if (ws.m_modified_fees < *shielded_min_relay) {
        return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY,
                             "min shielded relay premium not met",
                             strprintf("%d < %d",
                                       ws.m_modified_fees,
                                       *shielded_min_relay));
        }
    }
    // No individual transactions are allowed below the mempool min feerate except from disconnected
    // blocks and transactions in a package. Package transactions will be checked using package
    // feerate later.
    if (!args.m_package_feerates && !CheckFeeRate(relay_policy_vsize, ws.m_modified_fees, state, args.m_ignore_rejects)) return false;

    if (tx.HasShieldedBundle()) {
        const Consensus::Params& consensus = m_active_chainstate.m_chainman.GetConsensus();
        const int32_t next_block_height = shielded_validation_height.value_or(m_active_chainstate.m_chain.Height() + 1);
        const CShieldedBundle& bundle = tx.GetShieldedBundle();
        std::shared_ptr<const shielded::ShieldedMerkleTree> shielded_tree_snapshot;
        std::shared_ptr<const std::map<uint256, smile2::CompactPublicAccount>> shielded_smile_public_accounts_snapshot;
        std::shared_ptr<const std::map<uint256, uint256>> shielded_account_leaf_commitments_snapshot;
        std::shared_ptr<const shielded::ringct::MatRiCTProof> parsed_spend_auth_proof;

        if (bundle.HasShieldedInputs()) {
            const bool uses_spend_auth_checks = UsesShieldedSpendAuthChecks(bundle);
            shielded_tree_snapshot =
                std::make_shared<const shielded::ShieldedMerkleTree>(
                    m_active_chainstate.m_chainman.GetShieldedMerkleTree());
            shielded_smile_public_accounts_snapshot =
                m_active_chainstate.m_chainman.GetShieldedSmilePublicAccountsSnapshot();
            shielded_account_leaf_commitments_snapshot =
                m_active_chainstate.m_chainman.GetShieldedAccountLeafCommitmentsSnapshot();

            std::optional<std::vector<Nullifier>> proof_bound_nullifiers;
            if (uses_spend_auth_checks) {
                std::string spend_auth_reject;
                auto parsed = ParseShieldedSpendAuthProof(bundle, spend_auth_reject);
                if (!parsed.has_value()) {
                    return state.Invalid(TxValidationResult::TX_CONSENSUS, spend_auth_reject);
                }
                parsed_spend_auth_proof = *parsed;
                proof_bound_nullifiers = ExtractShieldedProofBoundNullifiers(
                    bundle,
                    spend_auth_reject,
                    consensus.IsShieldedSmileRiceCodecDisabled(next_block_height));
                if (!proof_bound_nullifiers.has_value()) {
                    return state.Invalid(TxValidationResult::TX_CONSENSUS, spend_auth_reject);
                }
            }

            for (size_t spend_index = 0; spend_index < bundle.GetShieldedInputCount(); ++spend_index) {
                if (uses_spend_auth_checks && proof_bound_nullifiers.has_value()) {
                    CShieldedSpendAuthCheck auth_check(tx, spend_index, (*proof_bound_nullifiers)[spend_index]);
                    if (auto err = auth_check()) {
                        return state.Invalid(TxValidationResult::TX_CONSENSUS, *err);
                    }
                }
            }
        }

        const bool needs_shielded_proof_check =
            NeedsShieldedProofCheck(bundle, consensus, next_block_height);
        std::string public_flow_reject;
        if (!RejectPostForkTransparentFundingV2SendContext(tx,
                                                           m_view,
                                                           consensus,
                                                           next_block_height,
                                                           public_flow_reject)) {
            return state.Invalid(TxValidationResult::TX_CONSENSUS, public_flow_reject);
        }
        if (needs_shielded_proof_check) {
            CShieldedProofCheck proof_check(tx,
                                            consensus,
                                            next_block_height,
                                            shielded_tree_snapshot,
                                            shielded_smile_public_accounts_snapshot,
                                            shielded_account_leaf_commitments_snapshot,
                                            parsed_spend_auth_proof);
            auto err = proof_check();
            if (err.has_value()) {
                return state.Invalid(TxValidationResult::TX_CONSENSUS, *err);
            }
        }

        if (bundle.HasV2Bundle()) {
            switch (*bundle.GetTransactionFamily()) {
            case shielded::v2::TransactionFamily::V2_SETTLEMENT_ANCHOR: {
                std::string reject_reason;
                auto created_anchors = ExtractCreatedShieldedSettlementAnchors(tx, next_block_height, reject_reason);
                if (!created_anchors.has_value()) {
                    return state.Invalid(TxValidationResult::TX_CONSENSUS, reject_reason);
                }
                for (const auto& settlement_anchor : *created_anchors) {
                    if (m_active_chainstate.m_chainman.IsShieldedSettlementAnchorValid(settlement_anchor)) {
                        return state.Invalid(TxValidationResult::TX_CONSENSUS,
                                             "bad-shielded-v2-settlement-anchor-duplicate");
                    }
                }
                break;
            }
            case shielded::v2::TransactionFamily::V2_REBALANCE: {
                std::string reject_reason;
                auto created_manifests = ExtractCreatedShieldedNettingManifests(tx, reject_reason);
                if (!created_manifests.has_value()) {
                    return state.Invalid(TxValidationResult::TX_CONSENSUS, reject_reason);
                }
                for (const auto& manifest_id : *created_manifests) {
                    if (m_active_chainstate.m_chainman.HasShieldedNettingManifest(manifest_id)) {
                        return state.Invalid(TxValidationResult::TX_CONSENSUS,
                                             "bad-shielded-v2-rebalance-manifest-duplicate");
                    }
                }
                break;
            }
            case shielded::v2::TransactionFamily::V2_EGRESS_BATCH:
            case shielded::v2::TransactionFamily::V2_SEND:
            case shielded::v2::TransactionFamily::V2_SPEND_PATH_RECOVERY:
            case shielded::v2::TransactionFamily::V2_RECOVERY_EXIT:
            case shielded::v2::TransactionFamily::V2_INGRESS_BATCH:
            case shielded::v2::TransactionFamily::V2_LIFECYCLE:
                break;
            case shielded::v2::TransactionFamily::V2_GENERIC:
                return state.Invalid(TxValidationResult::TX_CONSENSUS, "bad-shielded-v2-contextual");
            }
        }
    }

    std::set<Txid> conflicts_as_a_set;
    std::transform(ws.m_conflicts_incl_policy.begin(), ws.m_conflicts_incl_policy.end(),
                    std::inserter(conflicts_as_a_set, conflicts_as_a_set.end()),
                    [](const std::pair<Txid, bool>& pair){ return pair.first; });
    ws.m_iters_conflicting = m_pool.GetIterSet(conflicts_as_a_set);

    // Note that these modifications are only applicable to single transaction scenarios;
    // carve-outs are disabled for multi-transaction evaluations.
    CTxMemPool::Limits maybe_rbf_limits = m_pool.m_opts.limits;

    // Calculate in-mempool ancestors, up to a limit.
    if (ws.m_conflicts_incl_policy.size() == 1 && args.m_allow_carveouts) {
        // In general, when we receive an RBF transaction with mempool conflicts, we want to know whether we
        // would meet the chain limits after the conflicts have been removed. However, there isn't a practical
        // way to do this short of calculating the ancestor and descendant sets with an overlay cache of
        // changed mempool entries. Due to both implementation and runtime complexity concerns, this isn't
        // very realistic, thus we only ensure a limited set of transactions are RBF'able despite mempool
        // conflicts here. Importantly, we need to ensure that some transactions which were accepted using
        // the below carve-out are able to be RBF'ed, without impacting the security the carve-out provides
        // for off-chain contract systems (see link in the comment below).
        //
        // Specifically, the subset of RBF transactions which we allow despite chain limits are those which
        // conflict directly with exactly one other transaction (but may evict children of said transaction),
        // and which are not adding any new mempool dependencies. Note that the "no new mempool dependencies"
        // check is accomplished later, so we don't bother doing anything about it here, but if our
        // policy changes, we may need to move that check to here instead of removing it wholesale.
        //
        // Such transactions are clearly not merging any existing packages, so we are only concerned with
        // ensuring that (a) no package is growing past the package size (not count) limits and (b) we are
        // not allowing something to effectively use the (below) carve-out spot when it shouldn't be allowed
        // to.
        //
        // To check these we first check if we meet the RBF criteria, above, and increment the descendant
        // limits by the direct conflict and its descendants (as these are recalculated in
        // CalculateMempoolAncestors by assuming the new transaction being added is a new descendant, with no
        // removals, of each parent's existing dependent set). The ancestor count limits are unmodified (as
        // the ancestor limits should be the same for both our new transaction and any conflicts).
        // We don't bother incrementing m_limit_descendants by the full removal count as that limit never comes
        // into force here (as we're only adding a single transaction).
        assert(ws.m_iters_conflicting.size() == 1);
        CTxMemPool::txiter conflict = *ws.m_iters_conflicting.begin();

        maybe_rbf_limits.descendant_count += 1;
        maybe_rbf_limits.descendant_size_vbytes += conflict->GetSizeWithDescendants();
    }

    CTxMemPool::Limits limits;
    if (ignore_rejects.count("too-long-mempool-chain")) {
        limits = CTxMemPool::Limits::NoLimits();
    } else {
        limits = maybe_rbf_limits;
    }
    if (auto ancestors{m_subpackage.m_changeset->CalculateMemPoolAncestors(ws.m_tx_handle, limits)}) {
        ws.m_ancestors = std::move(*ancestors);
    } else {
        // If CalculateMemPoolAncestors fails second time, we want the original error string.
        const auto error_message{util::ErrorString(ancestors).original};

        // Carve-out is not allowed in this context; fail
        if (!args.m_allow_carveouts) {
            return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, "too-long-mempool-chain", error_message);
        }

        // Contracting/payment channels CPFP carve-out:
        // If the new transaction is relatively small (up to 40k weight)
        // and has at most one ancestor (ie ancestor limit of 2, including
        // the new transaction), allow it if its parent has exactly the
        // descendant limit descendants. The transaction also cannot be TRUC,
        // as its topology restrictions do not allow a second child.
        //
        // This allows protocols which rely on distrusting counterparties
        // being able to broadcast descendants of an unconfirmed transaction
        // to be secure by simply only having two immediately-spendable
        // outputs - one for each counterparty. For more info on the uses for
        // this, see https://lists.linuxfoundation.org/pipermail/bitcoin-dev/2018-November/016518.html
        CTxMemPool::Limits cpfp_carve_out_limits{
            .ancestor_count = 2,
            .ancestor_size_vbytes = maybe_rbf_limits.ancestor_size_vbytes,
            .descendant_count = maybe_rbf_limits.descendant_count + 1,
            .descendant_size_vbytes = maybe_rbf_limits.descendant_size_vbytes + EXTRA_DESCENDANT_TX_SIZE_LIMIT,
        };
        if (ws.m_vsize > EXTRA_DESCENDANT_TX_SIZE_LIMIT || (ws.m_ptx->version == TRUC_VERSION && m_pool.m_opts.truc_policy == TRUCPolicy::Enforce)) {
            return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, "too-long-mempool-chain", error_message);
        }
        if (auto ancestors_retry{m_subpackage.m_changeset->CalculateMemPoolAncestors(ws.m_tx_handle, cpfp_carve_out_limits)}) {
            ws.m_ancestors = std::move(*ancestors_retry);
        } else {
            return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, "too-long-mempool-chain", error_message);
        }
    }

    // Even though just checking direct mempool parents for inheritance would be sufficient, we
    // check using the full ancestor set here because it's more convenient to use what we have
    // already calculated.
    if (m_pool.m_opts.truc_policy == TRUCPolicy::Enforce && !ignore_rejects.count("truc")) {
        if (const auto err{SingleTRUCChecks(ws.m_ptx, "truc-", reason, ignore_rejects, ws.m_ancestors, conflicts_as_a_set, ws.m_vsize)}) {
            // Single transaction contexts only.
            if (args.m_allow_sibling_eviction && err->second != nullptr) {
                // We should only be considering where replacement is considered valid as well.
                Assume(args.m_allow_replacement);

                // Potential sibling eviction. Add the sibling to our list of mempool conflicts to be
                // included in RBF checks.
                ws.m_conflicts_incl_policy.emplace(err->second->GetHash(), false);
                conflicts_as_a_set.insert(err->second->GetHash());
                // Adding the sibling to m_iters_conflicting here means that it doesn't count towards
                // RBF Carve Out above. This is correct, since removing to-be-replaced transactions from
                // the descendant count is done separately in SingleTRUCChecks for TRUC transactions.
                ws.m_iters_conflicting.insert(m_pool.GetIter(err->second->GetHash()).value());
                ws.m_sibling_eviction = true;
                // The sibling will be treated as part of the to-be-replaced set in ReplacementChecks.
                // Note that we are not checking whether it opts in to replaceability via BIP125 or TRUC
                // (which is normally done in PreChecks). However, the only way a TRUC transaction can
                // have a non-TRUC and non-BIP125 descendant is due to a reorg.
            } else {
                return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, reason, err->first);
            }
        }
    }

    // A transaction that spends outputs that would be replaced by it is invalid. Now
    // that we have the set of all ancestors we can detect this
    // pathological case by making sure ws.m_conflicts and ws.m_ancestors don't
    // intersect.
    bool has_policy_conflict{false};
    if (const auto err_string{EntriesAndTxidsDisjoint(ws.m_ancestors, ws.m_conflicts_incl_policy, hash, &has_policy_conflict)}) {
        // We classify this as a consensus error because a transaction depending on something it
        // conflicts with would be inconsistent.
        return state.Invalid(TxValidationResult::TX_CONSENSUS, "bad-txns-spends-conflicting-tx", *err_string);
    }
    if (has_policy_conflict) {
        MaybeReject(TxValidationResult::TX_MEMPOOL_POLICY, "txn-spk-reused-chained");
    }

    // We want to detect conflicts in any tx in a package to trigger package RBF logic
    m_subpackage.m_rbf |= !ws.m_conflicts_incl_policy.empty();
    return true;
}

bool MemPoolAccept::ReplacementChecks(ATMPArgs& args, Workspace& ws)
{
    AssertLockHeld(cs_main);
    AssertLockHeld(m_pool.cs);

    const CTransaction& tx = *ws.m_ptx;
    const uint256& hash = ws.m_hash;
    TxValidationState& state = ws.m_state;

    CFeeRate newFeeRate(ws.m_modified_fees, ws.m_vsize);
    // Enforce Rule #6. The replacement transaction must have a higher feerate than its direct conflicts.
    // - The motivation for this check is to ensure that the replacement transaction is preferable for
    //   block-inclusion, compared to what would be removed from the mempool.
    // - This logic predates ancestor feerate-based transaction selection, which is why it doesn't
    //   consider feerates of descendants.
    // - Note: Ancestor feerate-based transaction selection has made this comparison insufficient to
    //   guarantee that this is incentive-compatible for miners, because it is possible for a
    //   descendant transaction of a direct conflict to pay a higher feerate than the transaction that
    //   might replace them, under these rules.
    if (!args.m_ignore_rejects.count("insufficient fee")) {
    if (const auto err_string{PaysMoreThanConflicts(ws.m_iters_conflicting, newFeeRate, hash)}) {
        // This fee-related failure is TX_RECONSIDERABLE because validating in a package may change
        // the result.
        return state.Invalid(TxValidationResult::TX_RECONSIDERABLE,
                             strprintf("insufficient fee%s", ws.m_sibling_eviction ? " (including sibling eviction)" : ""), *err_string);
    }
    }  // ignore_rejects

    CTxMemPool::setEntries all_conflicts;

    // Calculate all conflicting entries and enforce Rule #5.
    if (const auto err_string{GetEntriesForConflicts(tx, m_pool, ws.m_iters_conflicting, all_conflicts, args.m_ignore_rejects)}) {
        return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY,
                             strprintf("too many potential replacements%s", ws.m_sibling_eviction ? " (including sibling eviction)" : ""), *err_string);
    }
    // Enforce Rule #2.
    if (!args.m_ignore_rejects.count("replacement-adds-unconfirmed")) {
    if (const auto err_string{HasNoNewUnconfirmed(tx, m_pool, all_conflicts)}) {
        // Sibling eviction is only done for TRUC transactions, which cannot have multiple ancestors.
        Assume(!ws.m_sibling_eviction);
        return state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY,
                             strprintf("replacement-adds-unconfirmed%s", ws.m_sibling_eviction ? " (including sibling eviction)" : ""), *err_string);
    }
    }  // ignore_rejects

    // Check if it's economically rational to mine this transaction rather than the ones it
    // replaces and pays for its own relay fees. Enforce Rules #3 and #4.
    for (CTxMemPool::txiter it : all_conflicts) {
        m_subpackage.m_conflicting_fees += it->GetModifiedFee();
        m_subpackage.m_conflicting_size += it->GetTxSize();
    }
    if (!args.m_ignore_rejects.count("insufficient fee")) {
    if (const auto err_string{PaysForRBF(m_subpackage.m_conflicting_fees, ws.m_modified_fees, ws.m_vsize,
                                         m_pool.m_opts.incremental_relay_feerate, hash)}) {
        // Result may change in a package context
        return state.Invalid(TxValidationResult::TX_RECONSIDERABLE,
                             strprintf("insufficient fee%s", ws.m_sibling_eviction ? " (including sibling eviction)" : ""), *err_string);
    }
    }  // ignore_rejects

    // Add all the to-be-removed transactions to the changeset.
    for (auto it : all_conflicts) {
        m_subpackage.m_changeset->StageRemoval(it);
    }
    return true;
}

bool MemPoolAccept::PackageMempoolChecks(const ATMPArgs& args, const std::vector<CTransactionRef>& txns,
                                         std::vector<Workspace>& workspaces,
                                         const int64_t total_vsize,
                                         PackageValidationState& package_state)
{
    AssertLockHeld(cs_main);
    AssertLockHeld(m_pool.cs);

    // CheckPackageLimits expects the package transactions to not already be in the mempool.
    assert(std::all_of(txns.cbegin(), txns.cend(), [this](const auto& tx)
                       { return !m_pool.exists(GenTxid::Txid(tx->GetHash()));}));

    assert(txns.size() == workspaces.size());

    util::Result<void> result = [&]() EXCLUSIVE_LOCKS_REQUIRED(m_pool.cs) {
        if (args.m_ignore_rejects.count("package-mempool-limits")) {
            return util::Result<void>();
        } else {
            return m_pool.CheckPackageLimits(txns, total_vsize);
        }
    }();
    if (!result) {
        // This is a package-wide error, separate from an individual transaction error.
        return package_state.Invalid(PackageValidationResult::PCKG_POLICY, "package-mempool-limits", util::ErrorString(result).original);
    }

    // No conflicts means we're finished. Further checks are all RBF-only.
    if (!m_subpackage.m_rbf) return true;

    // We're in package RBF context; replacement proposal must be size 2
    if (workspaces.size() != 2 || !Assume(IsChildWithParents(txns))) {
        return package_state.Invalid(PackageValidationResult::PCKG_POLICY, "package RBF failed: package must be 1-parent-1-child");
    }

    // If the package has in-mempool ancestors, we won't consider a package RBF
    // since it would result in a cluster larger than 2.
    // N.B. To relax this constraint we will need to revisit how CCoinsViewMemPool::PackageAddTransaction
    // is being used inside AcceptMultipleTransactions to track available inputs while processing a package.
    for (const auto& ws : workspaces) {
        if (!ws.m_ancestors.empty()) {
            return package_state.Invalid(PackageValidationResult::PCKG_POLICY, "package RBF failed: new transaction cannot have mempool ancestors");
        }
    }

    // Aggregate all conflicts into one set.
    CTxMemPool::setEntries direct_conflict_iters;
    for (Workspace& ws : workspaces) {
        // Aggregate all conflicts into one set.
        direct_conflict_iters.merge(ws.m_iters_conflicting);
    }

    const auto& parent_ws = workspaces[0];
    const auto& child_ws = workspaces[1];

    // Don't consider replacements that would cause us to remove a large number of mempool entries.
    // This limit is not increased in a package RBF. Use the aggregate number of transactions.
    CTxMemPool::setEntries all_conflicts;
    if (const auto err_string{GetEntriesForConflicts(*child_ws.m_ptx, m_pool, direct_conflict_iters,
                                                     all_conflicts)}) {
        return package_state.Invalid(PackageValidationResult::PCKG_POLICY,
                                     "package RBF failed: too many potential replacements", *err_string);
    }


    for (CTxMemPool::txiter it : all_conflicts) {
        m_subpackage.m_changeset->StageRemoval(it);
        m_subpackage.m_conflicting_fees += it->GetModifiedFee();
        m_subpackage.m_conflicting_size += it->GetTxSize();
    }

    // Use the child as the transaction for attributing errors to.
    const Txid& child_hash = child_ws.m_ptx->GetHash();
    if (const auto err_string{PaysForRBF(/*original_fees=*/m_subpackage.m_conflicting_fees,
                                         /*replacement_fees=*/m_subpackage.m_total_modified_fees,
                                         /*replacement_vsize=*/m_subpackage.m_total_vsize,
                                         m_pool.m_opts.incremental_relay_feerate, child_hash)}) {
        return package_state.Invalid(PackageValidationResult::PCKG_POLICY,
                                     "package RBF failed: insufficient anti-DoS fees", *err_string);
    }

    // Ensure this two transaction package is a "chunk" on its own; we don't want the child
    // to be only paying anti-DoS fees
    const CFeeRate parent_feerate(parent_ws.m_modified_fees, parent_ws.m_vsize);
    const CFeeRate package_feerate(m_subpackage.m_total_modified_fees, m_subpackage.m_total_vsize);
    if (package_feerate <= parent_feerate) {
        return package_state.Invalid(PackageValidationResult::PCKG_POLICY,
                                     "package RBF failed: package feerate is less than or equal to parent feerate",
                                     strprintf("package feerate %s <= parent feerate is %s", package_feerate.ToString(), parent_feerate.ToString()));
    }

    // Check if it's economically rational to mine this package rather than the ones it replaces.
    // This takes the place of ReplacementChecks()'s PaysMoreThanConflicts() in the package RBF setting.
    if (const auto err_tup{ImprovesFeerateDiagram(*m_subpackage.m_changeset)}) {
        return package_state.Invalid(PackageValidationResult::PCKG_POLICY,
                                     "package RBF failed: " + err_tup.value().second, "");
    }

    LogDebug(BCLog::TXPACKAGES, "package RBF checks passed: parent %s (wtxid=%s), child %s (wtxid=%s), package hash (%s)\n",
        txns.front()->GetHash().ToString(), txns.front()->GetWitnessHash().ToString(),
        txns.back()->GetHash().ToString(), txns.back()->GetWitnessHash().ToString(),
        GetPackageHash(txns).ToString());


    return true;
}

unsigned int PolicyScriptVerifyFlags(const ignore_rejects_type& ignore_rejects)
{
    if (ignore_rejects.empty()) {
        return STANDARD_SCRIPT_VERIFY_FLAGS;
    }
    if (ignore_rejects.count("non-mandatory-script-verify-flag")) {
        return MANDATORY_SCRIPT_VERIFY_FLAGS;
    }

    unsigned int flags = STANDARD_SCRIPT_VERIFY_FLAGS;
    if (ignore_rejects.count("non-mandatory-script-verify-flag-upgradable")) {
        constexpr unsigned int upgradable_policy_flags =
            SCRIPT_VERIFY_DISCOURAGE_UPGRADABLE_NOPS |
            SCRIPT_VERIFY_DISCOURAGE_UPGRADABLE_WITNESS_PROGRAM |
            SCRIPT_VERIFY_DISCOURAGE_UPGRADABLE_TAPROOT_VERSION |
            SCRIPT_VERIFY_DISCOURAGE_OP_SUCCESS |
            SCRIPT_VERIFY_DISCOURAGE_UPGRADABLE_PUBKEYTYPE;
        flags &= ~upgradable_policy_flags;
    } else {
        if (ignore_rejects.count("non-mandatory-script-verify-flag-upgradable-nops")) {
            flags &= ~SCRIPT_VERIFY_DISCOURAGE_UPGRADABLE_NOPS;
        }
        if (ignore_rejects.count("non-mandatory-script-verify-flag-upgradable-witness_program")) {
            flags &= ~SCRIPT_VERIFY_DISCOURAGE_UPGRADABLE_WITNESS_PROGRAM;
        }
        if (ignore_rejects.count("non-mandatory-script-verify-flag-upgradable-taproot_version")) {
            flags &= ~SCRIPT_VERIFY_DISCOURAGE_UPGRADABLE_TAPROOT_VERSION;
        }
        if (ignore_rejects.count("non-mandatory-script-verify-flag-upgradable-op_success")) {
            flags &= ~SCRIPT_VERIFY_DISCOURAGE_OP_SUCCESS;
        }
        if (ignore_rejects.count("non-mandatory-script-verify-flag-upgradable-pubkeytype")) {
            flags &= ~SCRIPT_VERIFY_DISCOURAGE_UPGRADABLE_PUBKEYTYPE;
        }
    }
    if (ignore_rejects.count("non-mandatory-script-verify-flag-bip62")) {
        constexpr unsigned int bip62_policy_flags =
            SCRIPT_VERIFY_LOW_S |
            SCRIPT_VERIFY_SIGPUSHONLY |  // NOTE: not actually set ever
            SCRIPT_VERIFY_MINIMALDATA;
        flags &= ~bip62_policy_flags;
    } else {
        if (ignore_rejects.count("non-mandatory-script-verify-flag-low_s")) {
            flags &= ~SCRIPT_VERIFY_LOW_S;
        }
        if (ignore_rejects.count("non-mandatory-script-verify-flag-minimaldata")) {
            flags &= ~SCRIPT_VERIFY_MINIMALDATA;
        }
        if (ignore_rejects.count("non-mandatory-script-verify-flag-cleanstack")) {
            flags &= ~SCRIPT_VERIFY_CLEANSTACK;
        }
    }
    if (ignore_rejects.count("non-mandatory-script-verify-flag-strictenc")) {
        flags &= ~SCRIPT_VERIFY_STRICTENC;
    }
    if (ignore_rejects.count("non-mandatory-script-verify-flag-minimalif")) {
        flags &= ~SCRIPT_VERIFY_MINIMALIF;
    }
    if (ignore_rejects.count("non-mandatory-script-verify-flag-nullfail")) {
        flags &= ~SCRIPT_VERIFY_NULLFAIL;
    }
    if (ignore_rejects.count("non-mandatory-script-verify-flag-witness_pubkeytype")) {
        flags &= ~SCRIPT_VERIFY_WITNESS_PUBKEYTYPE;
    }
    if (ignore_rejects.count("non-mandatory-script-verify-flag-const_scriptcode")) {
        flags &= ~SCRIPT_VERIFY_CONST_SCRIPTCODE;
    }
    flags |= MANDATORY_SCRIPT_VERIFY_FLAGS;  // for safety
    return flags;
}

bool MemPoolAccept::PolicyScriptChecks(const ATMPArgs& args, Workspace& ws)
{
    AssertLockHeld(cs_main);
    AssertLockHeld(m_pool.cs);
    const CTransaction& tx = *ws.m_ptx;
    TxValidationState& state = ws.m_state;

    unsigned int scriptVerifyFlags = PolicyScriptVerifyFlags(args.m_ignore_rejects);
    const int next_block_height = NextBlockHeightOrLimit(m_active_chainstate.m_chain.Height());
    if (IsMLDSADisallowedForMempool(m_active_chainstate.m_chainman.GetConsensus(), next_block_height)) {
        scriptVerifyFlags |= SCRIPT_VERIFY_DISALLOW_MLDSA;
    }
    if (IsSLHDSAFips205RequiredForMempool(m_active_chainstate.m_chainman.GetConsensus(), next_block_height)) {
        scriptVerifyFlags |= SCRIPT_VERIFY_SLHDSA_FIPS205;
    }

    // Check input scripts and signatures.
    // This is done last to help prevent CPU exhaustion denial-of-service attacks.
    if (!CheckInputScripts(tx, state, m_view, scriptVerifyFlags, true, false, ws.m_precomputed_txdata, GetValidationCache())) {
        // Detect a failure due to a missing witness so that p2p code can handle rejection caching appropriately.
        if (!tx.HasWitness() && SpendsNonAnchorWitnessProg(tx, m_view)) {
            state.Invalid(TxValidationResult::TX_WITNESS_STRIPPED,
                    state.GetRejectReason(), state.GetDebugMessage());
        }
        return false; // state filled in by CheckInputScripts
    }

    return true;
}

bool MemPoolAccept::ConsensusScriptChecks(const ATMPArgs& args, Workspace& ws)
{
    AssertLockHeld(cs_main);
    AssertLockHeld(m_pool.cs);
    const CTransaction& tx = *ws.m_ptx;
    const uint256& hash = ws.m_hash;
    TxValidationState& state = ws.m_state;

    // Check again against the current block tip's script verification flags,
    // plus monotonic next-block PQ flags, to cache our script execution flags.
    // A mempool transaction can only be mined in the next block, so C-002
    // SLH-DSA/FIPS and ML-DSA disable policy must be evaluated at next height,
    // not the current tip. The cache tracks script flags for us and
    // auto-invalidates when the flag set changes.
    //
    // This is also useful in case of bugs in the standard flags that cause
    // transactions to pass as valid when they're actually invalid. For
    // instance the STRICTENC flag was incorrectly allowing certain
    // CHECKSIG NOT scripts to pass, even though they were invalid.
    //
    // There is a similar check in CreateNewBlock() to prevent creating
    // invalid blocks (using TestBlockValidity), however allowing such
    // transactions into the mempool can be exploited as a DoS attack.
    const int next_block_height = NextBlockHeightOrLimit(m_active_chainstate.m_chain.Height());
    const unsigned int nextBlockScriptVerifyFlags{
        GetNextBlockMempoolConsensusScriptFlags(*m_active_chainstate.m_chain.Tip(),
                                                m_active_chainstate.m_chainman,
                                                next_block_height)};
    if (!CheckInputsFromMempoolAndCache(tx, state, m_view, m_pool, nextBlockScriptVerifyFlags,
                                        ws.m_precomputed_txdata, m_active_chainstate.CoinsTip(), GetValidationCache())) {
        LogPrintf("BUG! PLEASE REPORT THIS! CheckInputScripts failed against latest-block but not STANDARD flags %s, %s\n", hash.ToString(), state.ToString());
        return Assume(false);
    }

    return true;
}

void MemPoolAccept::FinalizeSubpackage(const ATMPArgs& args)
{
    AssertLockHeld(cs_main);
    AssertLockHeld(m_pool.cs);

    if (!m_subpackage.m_changeset->GetRemovals().empty()) Assume(args.m_allow_replacement);
    // Remove conflicting transactions from the mempool
    for (CTxMemPool::txiter it : m_subpackage.m_changeset->GetRemovals())
    {
        std::string log_string = strprintf("replacing mempool tx %s (wtxid=%s, fees=%s, vsize=%s). ",
                                      it->GetTx().GetHash().ToString(),
                                      it->GetTx().GetWitnessHash().ToString(),
                                      it->GetFee(),
                                      it->GetTxSize());
        FeeFrac feerate{m_subpackage.m_total_modified_fees, int32_t(m_subpackage.m_total_vsize)};
        uint256 tx_or_package_hash{};
        const bool replaced_with_tx{m_subpackage.m_changeset->GetTxCount() == 1};
        if (replaced_with_tx) {
            const CTransaction& tx = m_subpackage.m_changeset->GetAddedTxn(0);
            tx_or_package_hash = tx.GetHash();
            log_string += strprintf("New tx %s (wtxid=%s, fees=%s, vsize=%s)",
                                    tx.GetHash().ToString(),
                                    tx.GetWitnessHash().ToString(),
                                    feerate.fee,
                                    feerate.size);
        } else {
            tx_or_package_hash = GetPackageHash(m_subpackage.m_changeset->GetAddedTxns());
            log_string += strprintf("New package %s with %lu txs, fees=%s, vsize=%s",
                                    tx_or_package_hash.ToString(),
                                    m_subpackage.m_changeset->GetTxCount(),
                                    feerate.fee,
                                    feerate.size);

        }
        LogDebug(BCLog::MEMPOOL, "%s\n", log_string);
        TRACEPOINT(mempool, replaced,
                it->GetTx().GetHash().data(),
                it->GetTxSize(),
                it->GetFee(),
                std::chrono::duration_cast<std::chrono::duration<std::uint64_t>>(it->GetTime()).count(),
                tx_or_package_hash.data(),
                feerate.size,
                feerate.fee,
                replaced_with_tx
        );
        m_subpackage.m_replaced_transactions.push_back(it->GetSharedTx());
    }
    m_subpackage.m_changeset->Apply();
    m_subpackage.m_changeset.reset();
}

bool MemPoolAccept::SubmitPackage(const ATMPArgs& args, std::vector<Workspace>& workspaces,
                                  PackageValidationState& package_state,
                                  std::map<Wtxid, MempoolAcceptResult>& results)
{
    AssertLockHeld(cs_main);
    AssertLockHeld(m_pool.cs);
    // Sanity check: none of the transactions should be in the mempool, and none of the transactions
    // should have a same-txid-different-witness equivalent in the mempool.
    assert(std::all_of(workspaces.cbegin(), workspaces.cend(), [this](const auto& ws){
        return !m_pool.exists(GenTxid::Txid(ws.m_ptx->GetHash())); }));

    bool all_submitted = true;
    FinalizeSubpackage(args);
    // ConsensusScriptChecks adds to the script cache and is therefore consensus-critical;
    // CheckInputsFromMempoolAndCache asserts that transactions only spend coins available from the
    // mempool or UTXO set. Submit each transaction to the mempool immediately after calling
    // ConsensusScriptChecks to make the outputs available for subsequent transactions.
    for (Workspace& ws : workspaces) {
        if (!ConsensusScriptChecks(args, ws)) {
            results.emplace(ws.m_ptx->GetWitnessHash(), MempoolAcceptResult::Failure(ws.m_state));
            // Since PolicyScriptChecks() passed, this should never fail.
            Assume(false);
            all_submitted = false;
            package_state.Invalid(PackageValidationResult::PCKG_MEMPOOL_ERROR,
                                  strprintf("BUG! PolicyScriptChecks succeeded but ConsensusScriptChecks failed: %s",
                                            ws.m_ptx->GetHash().ToString()));
        }
        // Remove the first failing transaction and every subsequent package
        // member, which may depend on outputs that are no longer valid.
        if (!all_submitted) {
            if (!m_subpackage.m_changeset) m_subpackage.m_changeset = m_pool.GetChangeSet();
            m_subpackage.m_changeset->StageRemoval(m_pool.GetIter(ws.m_ptx->GetHash()).value());
        }
    }
    if (!all_submitted) {
        Assume(m_subpackage.m_changeset);
        // This code should be unreachable; it's here as belt-and-suspenders
        // to try to ensure we have no consensus-invalid transactions in the
        // mempool.
        m_subpackage.m_changeset->Apply();
        m_subpackage.m_changeset.reset();
        return false;
    }

    std::vector<Wtxid> all_package_wtxids;
    all_package_wtxids.reserve(workspaces.size());
    std::transform(workspaces.cbegin(), workspaces.cend(), std::back_inserter(all_package_wtxids),
                   [](const auto& ws) { return ws.m_ptx->GetWitnessHash(); });

    if (!m_subpackage.m_replaced_transactions.empty()) {
        LogDebug(BCLog::MEMPOOL, "replaced %u mempool transactions with %u new one(s) for %s additional fees, %d delta bytes\n",
                 m_subpackage.m_replaced_transactions.size(), workspaces.size(),
                 m_subpackage.m_total_modified_fees - m_subpackage.m_conflicting_fees,
                 m_subpackage.m_total_vsize - static_cast<int>(m_subpackage.m_conflicting_size));
    }

    // Add successful results. The returned results may change later if LimitMempoolSize() evicts them.
    for (Workspace& ws : workspaces) {
        auto iter = m_pool.GetIter(ws.m_ptx->GetHash());
        Assume(iter.has_value());
        const auto effective_feerate = args.m_package_feerates ? ws.m_package_feerate :
            CFeeRate{ws.m_modified_fees, static_cast<uint32_t>(ws.m_vsize)};
        const auto effective_feerate_wtxids = args.m_package_feerates ? all_package_wtxids :
            std::vector<Wtxid>{ws.m_ptx->GetWitnessHash()};
        results.emplace(ws.m_ptx->GetWitnessHash(),
                        MempoolAcceptResult::Success(std::move(m_subpackage.m_replaced_transactions), ws.m_vsize,
                                         ws.m_base_fees, effective_feerate, effective_feerate_wtxids));
        if (!m_pool.m_opts.signals) continue;
        const CTransaction& tx = *ws.m_ptx;
        const auto tx_info = NewMempoolTransactionInfo(ws.m_ptx, ws.m_base_fees,
                                                       ws.m_vsize, (*iter)->GetHeight(),
                                                       args.m_ignore_rejects, args.m_package_submission,
                                                       IsCurrentForFeeEstimation(m_active_chainstate),
                                                       m_pool.HasNoInputsOf(tx));
        m_pool.m_opts.signals->TransactionAddedToMempool(tx_info, m_pool.GetAndIncrementSequence());
    }
    return all_submitted;
}

MempoolAcceptResult MemPoolAccept::AcceptSingleTransaction(const CTransactionRef& ptx, ATMPArgs& args)
{
    AssertLockHeld(cs_main);
    LOCK(m_pool.cs); // mempool "read lock" (held through m_pool.m_opts.signals->TransactionAddedToMempool())

    Workspace ws(ptx);
    const std::vector<Wtxid> single_wtxid{ws.m_ptx->GetWitnessHash()};

    if (!PreChecks(args, ws)) {
        if (ws.m_state.GetResult() == TxValidationResult::TX_RECONSIDERABLE) {
            // Failed for fee reasons. Provide the effective feerate and which tx was included.
            return MempoolAcceptResult::FeeFailure(ws.m_state, CFeeRate(ws.m_modified_fees, ws.m_vsize), single_wtxid);
        }
        return MempoolAcceptResult::Failure(ws.m_state);
    }

    m_subpackage.m_total_vsize = ws.m_vsize;
    m_subpackage.m_total_modified_fees = ws.m_modified_fees;

    // Individual modified feerate exceeded caller-defined max; abort
    if (args.m_client_maxfeerate && CFeeRate(ws.m_modified_fees, ws.m_vsize) > args.m_client_maxfeerate.value()) {
        ws.m_state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, "max feerate exceeded", "");
        return MempoolAcceptResult::Failure(ws.m_state);
    }

    if (m_pool.m_opts.require_standard) {
        Wtxid dummy_wtxid;
        if (!CheckEphemeralSpends(/*package=*/{ptx}, m_pool.m_opts.dust_relay_feerate, m_pool, ws.m_state, dummy_wtxid)) {
            return MempoolAcceptResult::Failure(ws.m_state);
        }
    }

    if (m_subpackage.m_rbf && !ReplacementChecks(args, ws)) {
        if (ws.m_state.GetResult() == TxValidationResult::TX_RECONSIDERABLE) {
            // Failed for incentives-based fee reasons. Provide the effective feerate and which tx was included.
            return MempoolAcceptResult::FeeFailure(ws.m_state, CFeeRate(ws.m_modified_fees, ws.m_vsize), single_wtxid);
        }
        return MempoolAcceptResult::Failure(ws.m_state);
    }

    // Perform the inexpensive checks first and avoid hashing and signature verification unless
    // those checks pass, to mitigate CPU exhaustion denial-of-service attacks.
    if (!PolicyScriptChecks(args, ws)) return MempoolAcceptResult::Failure(ws.m_state);

    if (!ConsensusScriptChecks(args, ws)) return MempoolAcceptResult::Failure(ws.m_state);

    const CFeeRate effective_feerate{ws.m_modified_fees, static_cast<uint32_t>(ws.m_vsize)};
    // Tx was accepted, but not added
    if (args.m_test_accept) {
        return MempoolAcceptResult::Success(std::move(m_subpackage.m_replaced_transactions), ws.m_vsize,
                                            ws.m_base_fees, effective_feerate, single_wtxid);
    }

    FinalizeSubpackage(args);

    // Limit the mempool, if appropriate.
    if (!args.m_package_submission && !args.m_ignore_rejects.count(rejectmsg_mempoolfull)) {
        LimitMempoolSize(m_pool, m_active_chainstate.CoinsTip());
        if (!m_pool.exists(GenTxid::Txid(ws.m_hash))) {
            // The tx no longer meets our (new) mempool minimum feerate but could be reconsidered in a package.
            ws.m_state.Invalid(TxValidationResult::TX_RECONSIDERABLE, "mempool full");
            return MempoolAcceptResult::FeeFailure(ws.m_state, CFeeRate(ws.m_modified_fees, ws.m_vsize), {ws.m_ptx->GetWitnessHash()});
        }
    }

    if (m_pool.m_opts.signals) {
        const CTransaction& tx = *ws.m_ptx;
        auto iter = m_pool.GetIter(tx.GetHash());
        Assume(iter.has_value());
        const auto tx_info = NewMempoolTransactionInfo(ws.m_ptx, ws.m_base_fees,
                                                       ws.m_vsize, (*iter)->GetHeight(),
                                                       args.m_ignore_rejects, args.m_package_submission,
                                                       IsCurrentForFeeEstimation(m_active_chainstate),
                                                       m_pool.HasNoInputsOf(tx));
        m_pool.m_opts.signals->TransactionAddedToMempool(tx_info, m_pool.GetAndIncrementSequence());
    }

    if (!m_subpackage.m_replaced_transactions.empty()) {
        LogDebug(BCLog::MEMPOOL, "replaced %u mempool transactions with 1 new transaction for %s additional fees, %d delta bytes\n",
                 m_subpackage.m_replaced_transactions.size(),
                 ws.m_modified_fees - m_subpackage.m_conflicting_fees,
                 ws.m_vsize - static_cast<int>(m_subpackage.m_conflicting_size));
    }

    // update mempool stats cache
    const CFeeRate min_fee_rate = std::max(m_pool.GetMinFee(), m_pool.m_opts.min_relay_feerate);
    CStats::DefaultStats()->addMempoolSample(m_pool.size(), m_pool.DynamicMemoryUsage(), min_fee_rate.GetFeePerK());

    return MempoolAcceptResult::Success(std::move(m_subpackage.m_replaced_transactions), ws.m_vsize, ws.m_base_fees,
                                        effective_feerate, single_wtxid);
}

PackageMempoolAcceptResult MemPoolAccept::AcceptMultipleTransactions(const std::vector<CTransactionRef>& txns, ATMPArgs& args)
{
    AssertLockHeld(cs_main);

    // These context-free package limits can be done before taking the mempool lock.
    PackageValidationState package_state;
    if (!IsWellFormedPackage(txns, package_state, /*require_sorted=*/true)) return PackageMempoolAcceptResult(package_state, {});

    std::vector<Workspace> workspaces{};
    workspaces.reserve(txns.size());
    std::transform(txns.cbegin(), txns.cend(), std::back_inserter(workspaces),
                   [](const auto& tx) { return Workspace(tx); });
    std::map<Wtxid, MempoolAcceptResult> results;

    LOCK(m_pool.cs);

    // Do all PreChecks first and fail fast to avoid running expensive script checks when unnecessary.
    for (Workspace& ws : workspaces) {
        if (!PreChecks(args, ws)) {
            package_state.Invalid(PackageValidationResult::PCKG_TX, "transaction failed");
            // Exit early to avoid doing pointless work. Update the failed tx result; the rest are unfinished.
            results.emplace(ws.m_ptx->GetWitnessHash(), MempoolAcceptResult::Failure(ws.m_state));
            return PackageMempoolAcceptResult(package_state, std::move(results));
        }

        // Individual modified feerate exceeded caller-defined max; abort
        // N.B. this doesn't take into account CPFPs. Chunk-aware validation may be more robust.
        if (args.m_client_maxfeerate && CFeeRate(ws.m_modified_fees, ws.m_vsize) > args.m_client_maxfeerate.value()) {
            // Need to set failure here both individually and at package level
            ws.m_state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, "max feerate exceeded", "");
            package_state.Invalid(PackageValidationResult::PCKG_TX, "transaction failed");
            // Exit early to avoid doing pointless work. Update the failed tx result; the rest are unfinished.
            results.emplace(ws.m_ptx->GetWitnessHash(), MempoolAcceptResult::Failure(ws.m_state));
            return PackageMempoolAcceptResult(package_state, std::move(results));
        }

        // Make the coins created by this transaction available for subsequent transactions in the
        // package to spend. If there are no conflicts within the package, no transaction can spend a coin
        // needed by another transaction in the package. We also need to make sure that no package
        // tx replaces (or replaces the ancestor of) the parent of another package tx. As long as we
        // check these two things, we don't need to track the coins spent.
        // If a package tx conflicts with a mempool tx, PackageMempoolChecks() ensures later that any package RBF attempt
        // has *no* in-mempool ancestors, so we don't have to worry about subsequent transactions in
        // same package spending the same in-mempool outpoints. This needs to be revisited for general
        // package RBF.
        m_viewmempool.PackageAddTransaction(ws.m_ptx);
    }

    // At this point we have all in-mempool ancestors, and we know every transaction's vsize.
    // Run the TRUC checks on the package.
    if (m_pool.m_opts.truc_policy == TRUCPolicy::Enforce) {
    std::string reason;
    for (Workspace& ws : workspaces) {
        if (auto err{PackageTRUCChecks(ws.m_ptx, ws.m_vsize, "truc-", reason, args.m_ignore_rejects, txns, ws.m_ancestors)}) {
            package_state.Invalid(PackageValidationResult::PCKG_POLICY, reason, err.value());
            return PackageMempoolAcceptResult(package_state, {});
        }
    }}

    // Transactions must meet two minimum feerates: the mempool minimum fee and min relay fee.
    // For transactions consisting of exactly one child and its parents, it suffices to use the
    // package feerate (total modified fees / total virtual size) to check this requirement.
    // Note that this is an aggregate feerate; this function has not checked that there are transactions
    // too low feerate to pay for themselves, or that the child transactions are higher feerate than
    // their parents. Using aggregate feerate may allow "parents pay for child" behavior and permit
    // a child that is below mempool minimum feerate. To avoid these behaviors, callers of
    // AcceptMultipleTransactions need to restrict txns topology (e.g. to ancestor sets) and check
    // the feerates of individuals and subsets.
    m_subpackage.m_total_vsize = std::accumulate(workspaces.cbegin(), workspaces.cend(), int64_t{0},
        [](int64_t sum, auto& ws) { return sum + ws.m_vsize; });
    m_subpackage.m_total_modified_fees = std::accumulate(workspaces.cbegin(), workspaces.cend(), CAmount{0},
        [](CAmount sum, auto& ws) { return sum + ws.m_modified_fees; });
    const CFeeRate package_feerate(m_subpackage.m_total_modified_fees, m_subpackage.m_total_vsize);
    std::vector<Wtxid> all_package_wtxids;
    all_package_wtxids.reserve(workspaces.size());
    std::transform(workspaces.cbegin(), workspaces.cend(), std::back_inserter(all_package_wtxids),
                   [](const auto& ws) { return ws.m_ptx->GetWitnessHash(); });
    TxValidationState placeholder_state;
    if (args.m_package_feerates &&
        (!args.m_ignore_rejects.count("package-fee-too-low")) &&
        !CheckFeeRate(m_subpackage.m_total_vsize, m_subpackage.m_total_modified_fees, placeholder_state, empty_ignore_rejects)) {
        package_state.Invalid(PackageValidationResult::PCKG_TX, "transaction failed");
        return PackageMempoolAcceptResult(package_state, {{workspaces.back().m_ptx->GetWitnessHash(),
            MempoolAcceptResult::FeeFailure(placeholder_state, CFeeRate(m_subpackage.m_total_modified_fees, m_subpackage.m_total_vsize), all_package_wtxids)}});
    }

    // Apply package mempool ancestor/descendant limits. Skip if there is only one transaction,
    // because it's unnecessary.
    if (txns.size() > 1 && !PackageMempoolChecks(args, txns, workspaces, m_subpackage.m_total_vsize, package_state)) {
        return PackageMempoolAcceptResult(package_state, std::move(results));
    }

    // Now that we've bounded the resulting possible ancestry count, check package for dust spends
    if (m_pool.m_opts.require_standard && !(args.m_ignore_rejects.count("dust") || args.m_ignore_rejects.count("unspent-dust") || args.m_ignore_rejects.count("missing-ephemeral-spends"))) {
        TxValidationState child_state;
        Wtxid child_wtxid;
        if (!CheckEphemeralSpends(txns, m_pool.m_opts.dust_relay_feerate, m_pool, child_state, child_wtxid)) {
            package_state.Invalid(PackageValidationResult::PCKG_TX, "unspent-dust");
            results.emplace(child_wtxid, MempoolAcceptResult::Failure(child_state));
            return PackageMempoolAcceptResult(package_state, std::move(results));
        }
    }

    for (Workspace& ws : workspaces) {
        ws.m_package_feerate = package_feerate;
        if (!PolicyScriptChecks(args, ws)) {
            // Exit early to avoid doing pointless work. Update the failed tx result; the rest are unfinished.
            package_state.Invalid(PackageValidationResult::PCKG_TX, "transaction failed");
            results.emplace(ws.m_ptx->GetWitnessHash(), MempoolAcceptResult::Failure(ws.m_state));
            return PackageMempoolAcceptResult(package_state, std::move(results));
        }
        if (args.m_test_accept) {
            const auto effective_feerate = args.m_package_feerates ? ws.m_package_feerate :
                CFeeRate{ws.m_modified_fees, static_cast<uint32_t>(ws.m_vsize)};
            const auto effective_feerate_wtxids = args.m_package_feerates ? all_package_wtxids :
                std::vector<Wtxid>{ws.m_ptx->GetWitnessHash()};
            results.emplace(ws.m_ptx->GetWitnessHash(),
                            MempoolAcceptResult::Success(std::move(m_subpackage.m_replaced_transactions),
                                                         ws.m_vsize, ws.m_base_fees, effective_feerate,
                                                         effective_feerate_wtxids));
        }
    }

    if (args.m_test_accept) return PackageMempoolAcceptResult(package_state, std::move(results));

    if (!SubmitPackage(args, workspaces, package_state, results)) {
        // PackageValidationState filled in by SubmitPackage().
        return PackageMempoolAcceptResult(package_state, std::move(results));
    }

    return PackageMempoolAcceptResult(package_state, std::move(results));
}

void MemPoolAccept::CleanupTemporaryCoins()
{
    // There are 3 kinds of coins in m_view:
    // (1) Temporary coins from the transactions in subpackage, constructed by m_viewmempool.
    // (2) Mempool coins from transactions in the mempool, constructed by m_viewmempool.
    // (3) Confirmed coins fetched from our current UTXO set.
    //
    // (1) Temporary coins need to be removed, regardless of whether the transaction was submitted.
    // If the transaction was submitted to the mempool, m_viewmempool will be able to fetch them from
    // there. If it wasn't submitted to mempool, it is incorrect to keep them - future calls may try
    // to spend those coins that don't actually exist.
    // (2) Mempool coins also need to be removed. If the mempool contents have changed as a result
    // of submitting or replacing transactions, coins previously fetched from mempool may now be
    // spent or nonexistent. Those coins need to be deleted from m_view.
    // (3) Confirmed coins don't need to be removed. The chainstate has not changed (we are
    // holding cs_main and no blocks have been processed) so the confirmed tx cannot disappear like
    // a mempool tx can. The coin may now be spent after we submitted a tx to mempool, but
    // we have already checked that the package does not have 2 transactions spending the same coin.
    // Keeping them in m_view is an optimization to not re-fetch confirmed coins if we later look up
    // inputs for this transaction again.
    for (const auto& outpoint : m_viewmempool.GetNonBaseCoins()) {
        // In addition to resetting m_viewmempool, we also need to manually delete these coins from
        // m_view because it caches copies of the coins it fetched from m_viewmempool previously.
        m_view.Uncache(outpoint);
    }
    // This deletes the temporary and mempool coins.
    m_viewmempool.Reset();
}

PackageMempoolAcceptResult MemPoolAccept::AcceptSubPackage(const std::vector<CTransactionRef>& subpackage, ATMPArgs& args)
{
    AssertLockHeld(::cs_main);
    AssertLockHeld(m_pool.cs);
    auto result = [&]() EXCLUSIVE_LOCKS_REQUIRED(::cs_main, m_pool.cs) {
        if (subpackage.size() > 1) {
            return AcceptMultipleTransactions(subpackage, args);
        }
        const auto& tx = subpackage.front();
        ATMPArgs single_args = ATMPArgs::SingleInPackageAccept(args);
        const auto single_res = AcceptSingleTransaction(tx, single_args);
        PackageValidationState package_state_wrapped;
        if (single_res.m_result_type != MempoolAcceptResult::ResultType::VALID) {
            package_state_wrapped.Invalid(PackageValidationResult::PCKG_TX, "transaction failed");
        }
        return PackageMempoolAcceptResult(package_state_wrapped, {{tx->GetWitnessHash(), single_res}});
    }();

    // Clean up m_view and m_viewmempool so that other subpackage evaluations don't have access to
    // coins they shouldn't. Keep some coins in order to minimize re-fetching coins from the UTXO set.
    // Clean up package feerate and rbf calculations
    ClearSubPackageState();

    return result;
}

PackageMempoolAcceptResult MemPoolAccept::AcceptPackage(const Package& package, ATMPArgs& args)
{
    Assert(!package.empty());
    AssertLockHeld(cs_main);
    // Used if returning a PackageMempoolAcceptResult directly from this function.
    PackageValidationState package_state_quit_early;

    // There are two topologies we are able to handle through this function:
    // (1) A single transaction
    // (2) A child-with-unconfirmed-parents package.
    // Check that the package is well-formed. If it isn't, we won't try to validate any of the
    // transactions and thus won't return any MempoolAcceptResults, just a package-wide error.

    // Context-free package checks.
    if (!IsWellFormedPackage(package, package_state_quit_early, /*require_sorted=*/true)) {
        return PackageMempoolAcceptResult(package_state_quit_early, {});
    }

    if (package.size() > 1) {
        // All transactions in the package must be a parent of the last transaction. This is just an
        // opportunity for us to fail fast on a context-free check without taking the mempool lock.
        if (!IsChildWithParents(package)) {
            package_state_quit_early.Invalid(PackageValidationResult::PCKG_POLICY, "package-not-child-with-parents");
            return PackageMempoolAcceptResult(package_state_quit_early, {});
        }

        // IsChildWithParents() guarantees the package is > 1 transactions.
        assert(package.size() > 1);
        // The package must be 1 child with all of its unconfirmed parents. The package is expected to
        // be sorted, so the last transaction is the child.
        const auto& child = package.back();
        std::unordered_set<uint256, SaltedTxidHasher> unconfirmed_parent_txids;
        std::transform(package.cbegin(), package.cend() - 1,
                       std::inserter(unconfirmed_parent_txids, unconfirmed_parent_txids.end()),
                       [](const auto& tx) { return tx->GetHash(); });

        // All child inputs must refer to a preceding package transaction or a confirmed UTXO. The only
        // way to verify this is to look up the child's inputs in our current coins view (not including
        // mempool), and enforce that all parents not present in the package be available at chain tip.
        // Since this check can bring new coins into the coins cache, keep track of these coins and
        // uncache them if we don't end up submitting this package to the mempool.
        const CCoinsViewCache& coins_tip_cache = m_active_chainstate.CoinsTip();
        for (const auto& input : child->vin) {
            if (!coins_tip_cache.HaveCoinInCache(input.prevout)) {
                args.m_coins_to_uncache.push_back(input.prevout);
            }
        }
        // Using the MemPoolAccept m_view cache allows us to look up these same coins faster later.
        // This should be connecting directly to CoinsTip, not to m_viewmempool, because we specifically
        // require inputs to be confirmed if they aren't in the package.
        m_view.SetBackend(m_active_chainstate.CoinsTip());
        const auto package_or_confirmed = [this, &unconfirmed_parent_txids](const auto& input) {
             return unconfirmed_parent_txids.count(input.prevout.hash) > 0 || m_view.HaveCoin(input.prevout);
        };
        if (!std::all_of(child->vin.cbegin(), child->vin.cend(), package_or_confirmed)) {
            package_state_quit_early.Invalid(PackageValidationResult::PCKG_POLICY, "package-not-child-with-unconfirmed-parents");
            return PackageMempoolAcceptResult(package_state_quit_early, {});
        }
        // Protect against bugs where we pull more inputs from disk that miss being added to
        // coins_to_uncache. The backend will be connected again when needed in PreChecks.
        m_view.SetBackend(m_dummy);
    }

    LOCK(m_pool.cs);
    // Stores results from which we will create the returned PackageMempoolAcceptResult.
    // A result may be changed if a mempool transaction is evicted later due to LimitMempoolSize().
    std::map<Wtxid, MempoolAcceptResult> results_final;
    // Results from individual validation which will be returned if no other result is available for
    // this transaction. "Nonfinal" because if a transaction fails by itself but succeeds later
    // (i.e. when evaluated with a fee-bumping child), the result in this map may be discarded.
    std::map<Wtxid, MempoolAcceptResult> individual_results_nonfinal;
    // Tracks whether we think package submission could result in successful entry to the mempool
    bool quit_early{false};
    std::vector<CTransactionRef> txns_package_eval;
    for (const auto& tx : package) {
        const auto& wtxid = tx->GetWitnessHash();
        const auto& txid = tx->GetHash();
        // There are 3 possibilities: already in mempool, same-txid-diff-wtxid already in mempool,
        // or not in mempool. An already confirmed tx is treated as one not in mempool, because all
        // we know is that the inputs aren't available.
        if (m_pool.exists(GenTxid::Wtxid(wtxid))) {
            // Exact transaction already exists in the mempool.
            // Node operators are free to set their mempool policies however they please, nodes may receive
            // transactions in different orders, and malicious counterparties may try to take advantage of
            // policy differences to pin or delay propagation of transactions. As such, it's possible for
            // some package transaction(s) to already be in the mempool, and we don't want to reject the
            // entire package in that case (as that could be a censorship vector). De-duplicate the
            // transactions that are already in the mempool, and only call AcceptMultipleTransactions() with
            // the new transactions. This ensures we don't double-count transaction counts and sizes when
            // checking ancestor/descendant limits, or double-count transaction fees for fee-related policy.
            const auto& entry{*Assert(m_pool.GetEntry(txid))};
            results_final.emplace(wtxid, MempoolAcceptResult::MempoolTx(entry.GetTxSize(), entry.GetFee()));
        } else if (m_pool.exists(GenTxid::Txid(txid))) {
            // Transaction with the same non-witness data but different witness (same txid,
            // different wtxid) already exists in the mempool.
            //
            // We don't allow replacement transactions right now, so just swap the package
            // transaction for the mempool one. Note that we are ignoring the validity of the
            // package transaction passed in.
            // NOTE: allow witness replacement in packages.
            const auto& entry{*Assert(m_pool.GetEntry(txid))};
            // Provide the wtxid of the mempool tx so that the caller can look it up in the mempool.
            results_final.emplace(wtxid, MempoolAcceptResult::MempoolTxDifferentWitness(entry.GetTx().GetWitnessHash()));
        } else {
            // Transaction does not already exist in the mempool.
            // Try submitting the transaction on its own.
            const auto single_package_res = AcceptSubPackage({tx}, args);
            const auto& single_res = single_package_res.m_tx_results.at(wtxid);
            if (single_res.m_result_type == MempoolAcceptResult::ResultType::VALID) {
                // The transaction succeeded on its own and is now in the mempool. Don't include it
                // in package validation, because its fees should only be "used" once.
                assert(m_pool.exists(GenTxid::Wtxid(wtxid)));
                results_final.emplace(wtxid, single_res);
            } else if (package.size() == 1 || // If there is only one transaction, no need to retry it "as a package"
                       (single_res.m_state.GetResult() != TxValidationResult::TX_RECONSIDERABLE &&
                       single_res.m_state.GetResult() != TxValidationResult::TX_MISSING_INPUTS)) {
                // Package validation policy only differs from individual policy in its evaluation
                // of feerate. For example, if a transaction fails here due to violation of a
                // consensus rule, the result will not change when it is submitted as part of a
                // package. To minimize the amount of repeated work, unless the transaction fails
                // due to feerate or missing inputs (its parent is a previous transaction in the
                // package that failed due to feerate), don't run package validation. Note that this
                // decision might not make sense if different types of packages are allowed in the
                // future.  Continue individually validating the rest of the transactions, because
                // some of them may still be valid.
                quit_early = true;
                package_state_quit_early.Invalid(PackageValidationResult::PCKG_TX, "transaction failed");
                individual_results_nonfinal.emplace(wtxid, single_res);
            } else {
                individual_results_nonfinal.emplace(wtxid, single_res);
                txns_package_eval.push_back(tx);
            }
        }
    }

    auto multi_submission_result = quit_early || txns_package_eval.empty() ? PackageMempoolAcceptResult(package_state_quit_early, {}) :
        AcceptSubPackage(txns_package_eval, args);
    PackageValidationState& package_state_final = multi_submission_result.m_state;

    // This is invoked by AcceptSubPackage() already, so this is just here for
    // clarity (since it's not permitted to invoke LimitMempoolSize() while a
    // changeset is outstanding).
    ClearSubPackageState();

    // Make sure we haven't exceeded max mempool size.
    // Package transactions that were submitted to mempool or already in mempool may be evicted.
    LimitMempoolSize(m_pool, m_active_chainstate.CoinsTip());

    for (const auto& tx : package) {
        const auto& wtxid = tx->GetWitnessHash();
        if (multi_submission_result.m_tx_results.count(wtxid) > 0) {
            // We shouldn't have re-submitted if the tx result was already in results_final.
            Assume(results_final.count(wtxid) == 0);
            // If it was submitted, check to see if the tx is still in the mempool. It could have
            // been evicted due to LimitMempoolSize() above.
            const auto& txresult = multi_submission_result.m_tx_results.at(wtxid);
            if (txresult.m_result_type == MempoolAcceptResult::ResultType::VALID && !m_pool.exists(GenTxid::Wtxid(wtxid))) {
                package_state_final.Invalid(PackageValidationResult::PCKG_TX, "transaction failed");
                TxValidationState mempool_full_state;
                mempool_full_state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, "mempool full");
                results_final.emplace(wtxid, MempoolAcceptResult::Failure(mempool_full_state));
            } else {
                results_final.emplace(wtxid, txresult);
            }
        } else if (const auto it{results_final.find(wtxid)}; it != results_final.end()) {
            // Already-in-mempool transaction. Check to see if it's still there, as it could have
            // been evicted when LimitMempoolSize() was called.
            Assume(it->second.m_result_type != MempoolAcceptResult::ResultType::INVALID);
            Assume(individual_results_nonfinal.count(wtxid) == 0);
            // Query by txid to include the same-txid-different-witness ones.
            if (!m_pool.exists(GenTxid::Txid(tx->GetHash()))) {
                package_state_final.Invalid(PackageValidationResult::PCKG_TX, "transaction failed");
                TxValidationState mempool_full_state;
                mempool_full_state.Invalid(TxValidationResult::TX_MEMPOOL_POLICY, "mempool full");
                // Replace the previous result.
                results_final.erase(wtxid);
                results_final.emplace(wtxid, MempoolAcceptResult::Failure(mempool_full_state));
            }
        } else if (const auto it{individual_results_nonfinal.find(wtxid)}; it != individual_results_nonfinal.end()) {
            Assume(it->second.m_result_type == MempoolAcceptResult::ResultType::INVALID);
            // Interesting result from previous processing.
            results_final.emplace(wtxid, it->second);
        }
    }
    Assume(results_final.size() == package.size());
    return PackageMempoolAcceptResult(package_state_final, std::move(results_final));
}

} // anon namespace

MempoolAcceptResult AcceptToMemoryPool(Chainstate& active_chainstate, const CTransactionRef& tx,
                                       int64_t accept_time, const ignore_rejects_type& ignore_rejects, bool test_accept)
{
    AssertLockHeld(::cs_main);
    const CChainParams& chainparams{active_chainstate.m_chainman.GetParams()};
    assert(active_chainstate.GetMempool() != nullptr);
    CTxMemPool& pool{*active_chainstate.GetMempool()};

    std::vector<COutPoint> coins_to_uncache;
    auto args = MemPoolAccept::ATMPArgs::SingleAccept(chainparams, accept_time, ignore_rejects, coins_to_uncache, test_accept);
    MempoolAcceptResult result = MemPoolAccept(pool, active_chainstate).AcceptSingleTransaction(tx, args);
    if (result.m_result_type != MempoolAcceptResult::ResultType::VALID) {
        // Remove coins that were not present in the coins cache before calling
        // AcceptSingleTransaction(); this is to prevent memory DoS in case we receive a large
        // number of invalid transactions that attempt to overrun the in-memory coins cache
        // (`CCoinsViewCache::cacheCoins`).

        for (const COutPoint& hashTx : coins_to_uncache)
            active_chainstate.CoinsTip().Uncache(hashTx);
        TRACEPOINT(mempool, rejected,
                tx->GetHash().data(),
                result.m_state.GetRejectReason().c_str()
        );
    }
    // After we've (potentially) uncached entries, ensure our coins cache is still within its size limits
    BlockValidationState state_dummy;
    active_chainstate.FlushStateToDisk(state_dummy, FlushStateMode::PERIODIC);
    return result;
}

PackageMempoolAcceptResult ProcessNewPackage(Chainstate& active_chainstate, CTxMemPool& pool,
                                                   const Package& package, bool test_accept, const std::optional<CFeeRate>& client_maxfeerate, const ignore_rejects_type& ignore_rejects)
{
    AssertLockHeld(cs_main);
    assert(!package.empty());
    assert(std::all_of(package.cbegin(), package.cend(), [](const auto& tx){return tx != nullptr;}));

    std::vector<COutPoint> coins_to_uncache;
    const CChainParams& chainparams = active_chainstate.m_chainman.GetParams();
    auto result = [&]() EXCLUSIVE_LOCKS_REQUIRED(cs_main) {
        AssertLockHeld(cs_main);
        if (test_accept) {
            auto args = MemPoolAccept::ATMPArgs::PackageTestAccept(chainparams, GetTime(), ignore_rejects, coins_to_uncache);
            return MemPoolAccept(pool, active_chainstate).AcceptMultipleTransactions(package, args);
        } else {
            auto args = MemPoolAccept::ATMPArgs::PackageChildWithParents(chainparams, GetTime(), coins_to_uncache, client_maxfeerate, ignore_rejects);
            return MemPoolAccept(pool, active_chainstate).AcceptPackage(package, args);
        }
    }();

    // Uncache coins pertaining to transactions that were not submitted to the mempool.
    if (test_accept || result.m_state.IsInvalid()) {
        for (const COutPoint& hashTx : coins_to_uncache) {
            active_chainstate.CoinsTip().Uncache(hashTx);
        }
    }
    // Ensure the coins cache is still within limits.
    BlockValidationState state_dummy;
    active_chainstate.FlushStateToDisk(state_dummy, FlushStateMode::PERIODIC);
    return result;
}

CAmount GetBlockSubsidy(int nHeight, const Consensus::Params& consensusParams)
{
    if (consensusParams.nSubsidyHalvingInterval <= 0) {
        return 0;
    }
    int halvings = nHeight / consensusParams.nSubsidyHalvingInterval;
    // Force block reward to zero when right shift is undefined.
    if (halvings >= 64)
        return 0;

    CAmount nSubsidy = consensusParams.nInitialSubsidy;
    nSubsidy >>= halvings;
    return nSubsidy;
}

CAmount GetBlockSubsidyForBlock(int nHeight, const CBlock& block, const CBlockIndex* pindexPrev, const Consensus::Params& consensusParams)
{
    const CAmount base_subsidy{GetBlockSubsidy(nHeight, consensusParams)};
    // Consensus can objectively detect coinbase-only blocks. It cannot prove a
    // non-coinbase transaction came from the public mempool rather than the
    // miner, so subsidy penalties deliberately avoid "real tx" heuristics that
    // would incentivize self-spam.
    if (nHeight < consensusParams.nEmptyBlockSubsidyPenaltyHeight ||
        nHeight >= consensusParams.nEmptyBlockSubsidyPenaltyEndHeight ||
        block.vtx.size() != 1 ||
        base_subsidy <= 0 || consensusParams.nEmptyBlockSubsidyMaxHalvings == 0) {
        return base_subsidy;
    }

    if (nHeight >= consensusParams.nEmptyBlockSubsidyStrictPenaltyHeight) {
        const bool follows_empty_block{pindexPrev != nullptr && pindexPrev->nTx == 1};
        return follows_empty_block ? base_subsidy / 4 : base_subsidy / 2;
    }

    uint32_t consecutive_empty_ancestors{0};
    for (const CBlockIndex* cursor = pindexPrev;
         cursor != nullptr && cursor->nTx == 1 &&
         consecutive_empty_ancestors + 1 < consensusParams.nEmptyBlockSubsidyMaxHalvings;
         cursor = cursor->pprev) {
        ++consecutive_empty_ancestors;
    }

    const uint32_t penalty_halvings{std::min<uint32_t>(consecutive_empty_ancestors + 1, consensusParams.nEmptyBlockSubsidyMaxHalvings)};
    return base_subsidy >> penalty_halvings;
}

CoinsViews::CoinsViews(DBParams db_params, CoinsViewOptions options)
    : m_dbview{std::move(db_params), std::move(options)},
      m_catcherview(&m_dbview) {}

void CoinsViews::InitCache(int32_t prevoutfetch_threads)
{
    AssertLockHeld(::cs_main);
    m_cacheview = std::make_unique<CCoinsViewCache>(&m_catcherview);
    auto thread_pool{std::make_shared<ThreadPool>("prevout")};
    if (prevoutfetch_threads > 0) {
        thread_pool->Start(prevoutfetch_threads);
        LogInfo("Block input prevout fetching uses %d additional threads\n", prevoutfetch_threads);
    }
    m_connect_block_view = std::make_unique<CoinsViewOverlay>(&*m_cacheview, std::move(thread_pool));
}

Chainstate::Chainstate(
    CTxMemPool* mempool,
    BlockManager& blockman,
    ChainstateManager& chainman,
    std::optional<uint256> from_snapshot_blockhash)
    : m_mempool(mempool),
      m_blockman(blockman),
      m_chainman(chainman),
      m_from_snapshot_blockhash(from_snapshot_blockhash) {}

const CBlockIndex* Chainstate::SnapshotBase()
{
    if (!m_from_snapshot_blockhash) return nullptr;
    if (!m_cached_snapshot_base) m_cached_snapshot_base = Assert(m_chainman.m_blockman.LookupBlockIndex(*m_from_snapshot_blockhash));
    return m_cached_snapshot_base;
}

void Chainstate::InitCoinsDB(
    size_t cache_size_bytes,
    bool in_memory,
    bool should_wipe,
    fs::path leveldb_name)
{
    if (m_from_snapshot_blockhash) {
        leveldb_name += node::SNAPSHOT_CHAINSTATE_SUFFIX;
    }

    m_coins_views = std::make_unique<CoinsViews>(
        DBParams{
            .path = m_chainman.m_options.datadir / leveldb_name,
            .cache_bytes = cache_size_bytes,
            .memory_only = in_memory,
            .wipe_data = should_wipe,
            .obfuscate = true,
            .options = m_chainman.m_options.coins_db},
        m_chainman.m_options.coins_view);

    m_coinsdb_cache_size_bytes = cache_size_bytes;
}

void Chainstate::InitCoinsCache(size_t cache_size_bytes)
{
    AssertLockHeld(::cs_main);
    assert(m_coins_views != nullptr);
    m_coinstip_cache_size_bytes = cache_size_bytes;
    m_coins_views->InitCache(m_chainman.m_options.prevoutfetch_threads_num);
}

// Note that though this is marked const, we may end up modifying `m_cached_finished_ibd`, which
// is a performance-related implementation detail. This function must be marked
// `const` so that `CValidationInterface` clients (which are given a `const Chainstate*`)
// can call it.
//
bool ChainstateManager::IsInitialBlockDownload() const
{
    // Optimization: pre-test latch before taking the lock.
    if (m_cached_finished_ibd.load(std::memory_order_relaxed))
        return false;

    LOCK(cs_main);
    if (m_cached_finished_ibd.load(std::memory_order_relaxed))
        return false;
    if (m_blockman.LoadingBlocks()) {
        return true;
    }
    CChain& chain{ActiveChain()};
    if (chain.Tip() == nullptr) {
        return true;
    }
    if (chain.Tip()->nChainWork < MinimumChainWork()) {
        return true;
    }
    if (chain.Tip()->Time() < Now<NodeSeconds>() - m_options.max_tip_age) {
        return true;
    }
    LogPrintf("Leaving InitialBlockDownload (latching to false)\n");
    m_cached_finished_ibd.store(true, std::memory_order_relaxed);
    return false;
}

void Chainstate::CheckForkWarningConditions()
{
    AssertLockHeld(cs_main);

    // Not applicable to the background chainstate. The active chain should
    // warn even during IBD, since a persistent invalid higher-work chain may
    // indicate corruption or a consensus incompatibility.
    if (this->GetRole() == ChainstateRole::BACKGROUND) {
        return;
    }

    if (m_chainman.m_best_invalid && m_chainman.m_best_invalid->nChainWork > m_chain.Tip()->nChainWork + (GetBlockProof(*m_chain.Tip()) * 6)) {
        LogWarning("Found invalid chain more than 6 blocks longer than our best chain. This could be due to database corruption or consensus incompatibility with peers.");
        m_chainman.GetNotifications().warningSet(
            kernel::Warning::LARGE_WORK_INVALID_CHAIN,
            _("Warning: Found invalid chain more than 6 blocks longer than our best chain. This could be due to database corruption or consensus incompatibility with peers."));
    } else {
        m_chainman.GetNotifications().warningUnset(kernel::Warning::LARGE_WORK_INVALID_CHAIN);
    }
}

// Called both upon regular invalid block discovery *and* InvalidateBlock
void Chainstate::InvalidChainFound(CBlockIndex* pindexNew)
{
    AssertLockHeld(cs_main);
    if (!m_chainman.m_best_invalid || pindexNew->nChainWork > m_chainman.m_best_invalid->nChainWork) {
        m_chainman.m_best_invalid = pindexNew;
    }
    SetBlockFailureFlags(pindexNew);
    if (m_chainman.m_best_header != nullptr && m_chainman.m_best_header->GetAncestor(pindexNew->nHeight) == pindexNew) {
        m_chainman.RecalculateBestHeader();
    }

    LogPrintf("%s: invalid block=%s  height=%d  log2_work=%f  date=%s\n", __func__,
      pindexNew->GetBlockHash().ToString(), pindexNew->nHeight,
      log(pindexNew->nChainWork.getdouble())/log(2.0), FormatISO8601DateTime(pindexNew->GetBlockTime()));
    CBlockIndex *tip = m_chain.Tip();
    if (tip) {
        LogPrintf("%s:  current best=%s  height=%d  log2_work=%f  date=%s\n", __func__,
          tip->GetBlockHash().ToString(), m_chain.Height(), log(tip->nChainWork.getdouble())/log(2.0),
          FormatISO8601DateTime(tip->GetBlockTime()));
    } else {
        LogPrintf("%s:  current best=(null tip)\n", __func__);
    }
    CheckForkWarningConditions();
}

// Same as InvalidChainFound, above, except not called directly from InvalidateBlock,
// which does its own setBlockIndexCandidates management.
void Chainstate::InvalidBlockFound(CBlockIndex* pindex, const BlockValidationState& state)
{
    AssertLockHeld(cs_main);
    if (state.GetResult() != BlockValidationResult::BLOCK_MUTATED) {
        pindex->nStatus |= BLOCK_FAILED_VALID;
        m_chainman.m_failed_blocks.insert(pindex);
        m_blockman.m_dirty_blockindex.insert(pindex);
        setBlockIndexCandidates.erase(pindex);
        InvalidChainFound(pindex);
    }
}

void UpdateCoins(const CTransaction& tx, CCoinsViewCache& inputs, CTxUndo &txundo, int nHeight)
{
    // mark inputs spent
    if (!tx.IsCoinBase()) {
        txundo.vprevout.reserve(tx.vin.size());
        for (const CTxIn &txin : tx.vin) {
            txundo.vprevout.emplace_back();
            bool is_spent = inputs.SpendCoin(txin.prevout, &txundo.vprevout.back());
            assert(is_spent);
        }
    }
    // add outputs
    AddCoins(inputs, tx, nHeight);
}

std::optional<std::pair<ScriptError, std::string>> CScriptCheck::operator()() {
    const CScript &scriptSig = ptxTo->vin[nIn].scriptSig;
    const CScriptWitness *witness = &ptxTo->vin[nIn].scriptWitness;
    ScriptError error{SCRIPT_ERR_UNKNOWN_ERROR};
    if (VerifyScript(scriptSig, m_tx_out.scriptPubKey, witness, nFlags, CachingTransactionSignatureChecker(ptxTo, nIn, m_tx_out.nValue, cacheStore, *m_signature_cache, *txdata), &error)) {
        return std::nullopt;
    } else {
        auto debug_str = strprintf("input %i of %s (wtxid %s), spending %s:%i", nIn, ptxTo->GetHash().ToString(), ptxTo->GetWitnessHash().ToString(), ptxTo->vin[nIn].prevout.hash.ToString(), ptxTo->vin[nIn].prevout.n);
        return std::make_pair(error, std::move(debug_str));
    }
}

ValidationCache::ValidationCache(const size_t script_execution_cache_bytes, const size_t signature_cache_bytes)
    : m_signature_cache{signature_cache_bytes}
{
    // Setup the salted hasher
    uint256 nonce = GetRandHash();
    // We want the nonce to be 64 bytes long to force the hasher to process
    // this chunk, which makes later hash computations more efficient. We
    // just write our 32-byte entropy twice to fill the 64 bytes.
    m_script_execution_cache_hasher.Write(nonce.begin(), 32);
    m_script_execution_cache_hasher.Write(nonce.begin(), 32);

    const auto [num_elems, approx_size_bytes] = m_script_execution_cache.setup_bytes(script_execution_cache_bytes);
    LogPrintf("Using %zu MiB out of %zu MiB requested for script execution cache, able to store %zu elements\n",
              approx_size_bytes >> 20, script_execution_cache_bytes >> 20, num_elems);
}

/**
 * Check whether all of this transaction's input scripts succeed.
 *
 * This involves ECDSA signature checks so can be computationally intensive. This function should
 * only be called after the cheap sanity checks in CheckTxInputs passed.
 *
 * If pvChecks is not nullptr, script checks are pushed onto it instead of being performed inline. Any
 * script checks which are not necessary (eg due to script execution cache hits) are, obviously,
 * not pushed onto pvChecks/run.
 *
 * Setting cacheSigStore/cacheFullScriptStore to false will remove elements from the corresponding cache
 * which are matched. This is useful for checking blocks where we will likely never need the cache
 * entry again.
 *
 * Note that we may set state.reason to NOT_STANDARD for extra soft-fork flags in flags, block-checking
 * callers should probably reset it to CONSENSUS in such cases.
 *
 * Non-static (and redeclared) in src/test/txvalidationcache_tests.cpp
 */
bool CheckInputScripts(const CTransaction& tx, TxValidationState& state,
                       const CCoinsViewCache& inputs, unsigned int flags, bool cacheSigStore,
                       bool cacheFullScriptStore, PrecomputedTransactionData& txdata,
                       ValidationCache& validation_cache,
                       std::vector<CScriptCheck>* pvChecks)
{
    if (tx.IsCoinBase()) return true;

    if (pvChecks) {
        pvChecks->reserve(tx.vin.size());
    }

    // First check if script executions have been cached with the same
    // flags. Note that this assumes that the inputs provided are
    // correct (ie that the transaction hash which is in tx's prevouts
    // properly commits to the scriptPubKey in the inputs view of that
    // transaction).
    uint256 hashCacheEntry;
    CSHA256 hasher = validation_cache.ScriptExecutionCacheHasher();
    hasher.Write(UCharCast(tx.GetWitnessHash().begin()), 32).Write((unsigned char*)&flags, sizeof(flags)).Finalize(hashCacheEntry.begin());
    AssertLockHeld(cs_main); //NOTE: Remove this requirement by making CuckooCache not require external locks
    if (validation_cache.m_script_execution_cache.contains(hashCacheEntry, !cacheFullScriptStore)) {
        return true;
    }

    if (!txdata.m_spent_outputs_ready) {
        std::vector<CTxOut> spent_outputs;
        spent_outputs.reserve(tx.vin.size());

        for (const auto& txin : tx.vin) {
            const COutPoint& prevout = txin.prevout;
            const Coin& coin = inputs.AccessCoin(prevout);
            assert(!coin.IsSpent());
            spent_outputs.emplace_back(coin.out);
        }
        txdata.Init(tx, std::move(spent_outputs));
    }
    assert(txdata.m_spent_outputs.size() == tx.vin.size());

    for (unsigned int i = 0; i < tx.vin.size(); i++) {

        // We very carefully only pass in things to CScriptCheck which
        // are clearly committed to by tx' witness hash. This provides
        // a sanity check that our caching is not introducing consensus
        // failures through additional data in, eg, the coins being
        // spent being checked as a part of CScriptCheck.

        // Verify signature
        CScriptCheck check(txdata.m_spent_outputs[i], tx, validation_cache.m_signature_cache, i, flags, cacheSigStore, &txdata);
        if (pvChecks) {
            pvChecks->emplace_back(std::move(check));
        } else if (auto result = check(); result.has_value()) {
            // Tx failures never trigger disconnections/bans.
            // This is so that network splits aren't triggered
            // either due to non-consensus relay policies (such as
            // non-standard DER encodings or non-null dummy
            // arguments) or due to new consensus rules introduced in
            // soft forks.
            if (flags & STANDARD_NOT_MANDATORY_VERIFY_FLAGS) {
                return state.Invalid(TxValidationResult::TX_NOT_STANDARD, strprintf("mempool-script-verify-flag-failed (%s)", ScriptErrorString(result->first)), result->second);
            } else {
                return state.Invalid(TxValidationResult::TX_CONSENSUS, strprintf("mandatory-script-verify-flag-failed (%s)", ScriptErrorString(result->first)), result->second);
            }
        }
    }

    if (cacheFullScriptStore && !pvChecks) {
        // We executed all of the provided scripts, and were told to
        // cache the result. Do so now.
        validation_cache.m_script_execution_cache.insert(hashCacheEntry);
    }

    return true;
}

bool FatalError(Notifications& notifications, BlockValidationState& state, const bilingual_str& message)
{
    notifications.fatalError(message);
    return state.Error(message.original);
}

/**
 * Restore the UTXO in a Coin at a given COutPoint
 * @param undo The Coin to be restored.
 * @param view The coins view to which to apply the changes.
 * @param out The out point that corresponds to the tx input.
 * @return A DisconnectResult as an int
 */
int ApplyTxInUndo(Coin&& undo, CCoinsViewCache& view, const COutPoint& out)
{
    bool fClean = true;

    if (view.HaveCoin(out)) fClean = false; // overwriting transaction output

    if (undo.nHeight == 0) {
        // Missing undo metadata (height and coinbase). Older versions included this
        // information only in undo records for the last spend of a transactions'
        // outputs. This implies that it must be present for some other output of the same tx.
        const Coin& alternate = AccessByTxid(view, out.hash);
        if (!alternate.IsSpent()) {
            undo.nHeight = alternate.nHeight;
            undo.fCoinBase = alternate.fCoinBase;
        } else {
            return DISCONNECT_FAILED; // adding output for transaction without known metadata
        }
    }
    // If the coin already exists as an unspent coin in the cache, then the
    // possible_overwrite parameter to AddCoin must be set to true. We have
    // already checked whether an unspent coin exists above using HaveCoin, so
    // we don't need to guess. When fClean is false, an unspent coin already
    // existed and it is an overwrite.
    view.AddCoin(out, std::move(undo), !fClean);

    return fClean ? DISCONNECT_OK : DISCONNECT_UNCLEAN;
}

/** Undo the effects of this block (with given index) on the UTXO set represented by coins.
 *  When FAILED is returned, view is left in an indeterminate state. */
DisconnectResult Chainstate::DisconnectBlock(const CBlock& block, const CBlockIndex* pindex, CCoinsViewCache& view,
                                             bool apply_shielded_state)
{
    AssertLockHeld(::cs_main);
    bool fClean = true;

    CBlockUndo blockUndo;
    if (!m_blockman.ReadBlockUndo(blockUndo, *pindex)) {
        LogError("DisconnectBlock(): failure reading undo data\n");
        return DISCONNECT_FAILED;
    }

    if (blockUndo.vtxundo.size() + 1 != block.vtx.size()) {
        LogError("DisconnectBlock(): block and undo data inconsistent\n");
        return DISCONNECT_FAILED;
    }

    // Ignore blocks that contain transactions which are 'overwritten' by later transactions,
    // unless those are already completely spent.
    // See https://github.com/bitcoin/bitcoin/issues/22596 for additional information.
    // Note: the blocks specified here are different than the ones used in ConnectBlock because DisconnectBlock
    // unwinds the blocks in reverse. As a result, the inconsistency is not discovered until the earlier
    // blocks with the duplicate coinbase transactions are disconnected.
    bool fEnforceBIP30 = !((pindex->nHeight==91722 && pindex->GetBlockHash() == uint256{"00000000000271a2dc26e7667f8419f2e15416dc6955e5a6c6cdf3f2574dd08e"}) ||
                           (pindex->nHeight==91812 && pindex->GetBlockHash() == uint256{"00000000000af0aed4792b1acee3d966af36cf5def14935db8de83d6f9306f2f"}));

    // undo transactions in reverse order
    for (size_t tx_pos = block.vtx.size(); tx_pos > 0; --tx_pos) {
        const size_t tx_index = tx_pos - 1;
        const CTransaction& tx = *(block.vtx[tx_index]);
        Txid hash = tx.GetHash();
        bool is_coinbase = tx.IsCoinBase();
        bool is_bip30_exception = (is_coinbase && !fEnforceBIP30);

        // Check that all outputs are available and match the outputs in the block itself
        // exactly.
        for (size_t o = 0; o < tx.vout.size(); o++) {
            if (!tx.vout[o].scriptPubKey.IsUnspendable()) {
                COutPoint out(hash, o);
                Coin coin;
                bool is_spent = view.SpendCoin(out, &coin);
                if (!is_spent || tx.vout[o] != coin.out || pindex->nHeight != coin.nHeight || is_coinbase != coin.fCoinBase) {
                    if (!is_bip30_exception) {
                        fClean = false; // transaction output mismatch
                    }
                }
            }
        }

        // restore inputs
        if (tx_index > 0) { // not coinbases
            CTxUndo& txundo = blockUndo.vtxundo[tx_index - 1];
            if (txundo.vprevout.size() != tx.vin.size()) {
                LogError("DisconnectBlock(): transaction and undo data inconsistent\n");
                return DISCONNECT_FAILED;
            }
            for (size_t j = tx.vin.size(); j > 0;) {
                --j;
                const COutPoint& out = tx.vin[j].prevout;
                int res = ApplyTxInUndo(std::move(txundo.vprevout[j]), view, out);
                if (res == DISCONNECT_FAILED) return DISCONNECT_FAILED;
                fClean = fClean && res != DISCONNECT_UNCLEAN;
            }
            // At this point, all of txundo.vprevout should have been moved out.
        }
    }

    const bool block_has_shielded_bundle{BlockHasShieldedBundle(block)};
    const bool block_updates_shielded_velocity{
        m_chainman.GetConsensus().IsShieldedUnshieldVelocityCapActive(pindex->nHeight)};
    if (apply_shielded_state && this == &m_chainman.ActiveChainstate() &&
        (block_has_shielded_bundle || block_updates_shielded_velocity)) {
        if (!m_chainman.EnsureShieldedStateInitialized()) {
            return DISCONNECT_FAILED;
        }

        std::vector<Nullifier> block_nullifiers;
        std::vector<uint256> block_recovery_exit_commitments;
        std::vector<uint256> block_settlement_anchors;
        std::set<uint256> block_created_settlement_anchor_set;
        std::set<uint256> block_consumed_settlement_anchor_set;
        std::vector<uint256> block_netting_manifests;
        std::vector<uint256> block_smile_commitments;
        std::vector<uint256> block_account_leaf_commitments;
        uint64_t output_count{0};
        ShieldedPoolBalance rolled_back_pool = m_chainman.m_shielded_pool_balance;
        const bool use_nonced_bridge_tag =
            UseNoncedShieldedBridgeTags(m_chainman.GetConsensus(), pindex->nHeight);

        for (const auto& txref : block.vtx) {
            if (!txref->HasShieldedBundle()) continue;
            const CShieldedBundle& bundle = txref->GetShieldedBundle();
            const auto nullifiers = CollectShieldedNullifiers(bundle);
            block_nullifiers.insert(block_nullifiers.end(), nullifiers.begin(), nullifiers.end());
            // V2_RECOVERY_EXIT: its (cm, nf) are DERIVED, not carried as bundle spend descriptors, so
            // re-derive them here to undo the retirement (nf via block_nullifiers' Remove below, cm via
            // the spent-commitment set Remove). Deterministic and identical to the ConnectBlock derivation.
            if (IsRecoveryExitBundle(bundle)) {
                shielded::recovery::RecoveryExitClaim re_claim;
                shielded::recovery::RecoveryExitIdentifiers re_ids;
                if (GetRecoveryExitIdentifiersFromBundle(bundle, re_claim, re_ids)) {
                    block_nullifiers.push_back(re_ids.nullifier);
                    block_recovery_exit_commitments.push_back(re_ids.commitment);
                } else {
                    LogError("DisconnectBlock(): failed to derive recovery-exit identifiers for tx %s\n",
                             txref->GetHash().ToString());
                    return DISCONNECT_FAILED;
                }
            }
            std::string reject_reason;
            auto created_anchors = ExtractCreatedShieldedSettlementAnchors(*txref, pindex->nHeight, reject_reason);
            if (!created_anchors.has_value()) {
                return DISCONNECT_FAILED;
            }
            block_settlement_anchors.insert(block_settlement_anchors.end(),
                                            created_anchors->begin(),
                                            created_anchors->end());
            block_created_settlement_anchor_set.insert(created_anchors->begin(), created_anchors->end());
            if (UseSingleUseSettlementAnchors(m_chainman.GetConsensus(), pindex->nHeight)) {
                const auto consumed_anchors = CollectShieldedSettlementAnchorRefs(bundle);
                block_consumed_settlement_anchor_set.insert(consumed_anchors.begin(), consumed_anchors.end());
            }
            auto created_manifests = ExtractCreatedShieldedNettingManifests(*txref, reject_reason);
            if (!created_manifests.has_value()) {
                return DISCONNECT_FAILED;
            }
            block_netting_manifests.insert(block_netting_manifests.end(),
                                           created_manifests->begin(),
                                           created_manifests->end());
            for (const auto& [commitment, _] : CollectShieldedOutputSmileAccounts(bundle)) {
                block_smile_commitments.push_back(commitment);
            }
            const auto account_leaf_commitments =
                CollectShieldedOutputAccountLeafCommitments(bundle, use_nonced_bridge_tag);
            if (!account_leaf_commitments.has_value()) {
                return DISCONNECT_FAILED;
            }
            block_account_leaf_commitments.insert(block_account_leaf_commitments.end(),
                                                  account_leaf_commitments->begin(),
                                                  account_leaf_commitments->end());
            const uint64_t bundle_output_count = bundle.GetShieldedOutputCount();
            if (output_count > std::numeric_limits<uint64_t>::max() - bundle_output_count) {
                return DISCONNECT_FAILED;
            }
            output_count += bundle_output_count;
            std::string value_balance_reject;
            const auto value_balance = TryGetShieldedStateValueBalance(bundle, value_balance_reject);
            if (!value_balance.has_value() || !rolled_back_pool.UndoValueBalance(*value_balance)) {
                return DISCONNECT_FAILED;
            }
        }

        const uint64_t tree_size = m_chainman.m_shielded_merkle_tree.Size();
        if (output_count > tree_size) return DISCONNECT_FAILED;
        const uint64_t next_tree_size = tree_size - output_count;
        const uint64_t registry_leaf_count =
            static_cast<uint64_t>(block_account_leaf_commitments.size());
        const uint64_t registry_size = m_chainman.m_shielded_account_registry.Size();
        if (registry_leaf_count > registry_size) return DISCONNECT_FAILED;
        const uint64_t next_registry_size = registry_size - registry_leaf_count;

        shielded::ShieldedMerkleTree projected_tree = m_chainman.m_shielded_merkle_tree;
        shielded::registry::ShieldedAccountRegistryState projected_account_registry =
            m_chainman.m_shielded_account_registry;
        auto projected_public_accounts = m_chainman.m_shielded_smile_public_accounts;
        auto projected_account_leaf_entries = m_chainman.m_shielded_account_leaf_commitments;
        ShieldedPoolBalance projected_pool = rolled_back_pool;

        if (!projected_tree.DetachToMemoryOnly() ||
            !projected_tree.Truncate(next_tree_size) ||
            !projected_account_registry.Truncate(
                next_registry_size,
                shielded::registry::ShieldedAccountRegistryState::PayloadPruneMode::KEEP)) {
            // Fallback for older/incomplete in-memory state where position
            // indexes are unavailable. This rebuilds shielded state by walking
            // ancestor blocks from disk; on a pruned node that walk can require a
            // block below the prune horizon (issue #35). Fail fast with an
            // actionable message instead of attempting the read and surfacing the
            // opaque "ReadRawBlock: FlatFilePos(nFile=-1)" error -- the node
            // recovers its persisted shielded state on the next restart.
            if (!ShieldedFullRebuildBlocksAvailable(*this, pindex->pprev)) {
                LogError("DisconnectBlock(): shielded undo for block %s needs a full rebuild from "
                         "ancestor blocks below the prune horizon (a reorg disconnected a block whose "
                         "shielded anchor was pruned). Refusing the prune-unsafe rebuild; restart to "
                         "recover persisted shielded state, or -reindex on an un-pruned node to reorg "
                         "past this point.\n",
                         pindex->GetBlockHash().ToString());
                return DISCONNECT_FAILED;
            }
            // Truncate indexes unavailable: rebuild note tree + account registry in one disk pass
            // (previously two full-history reads during invalidateblock/reorg undo).
            ShieldedChainDerivedRebuildResult rebuilt;
            if (!RebuildShieldedChainDerivedState(
                    *this,
                    pindex->pprev,
                    rebuilt,
                    ShieldedChainDerivedRebuildParts::STATE |
                        ShieldedChainDerivedRebuildParts::ACCOUNT_REGISTRY,
                    shielded::ShieldedMerkleTree::IndexStorageMode::MEMORY_ONLY)) {
                return DISCONNECT_FAILED;
            }
            projected_tree = std::move(rebuilt.tree);
            projected_account_registry = std::move(rebuilt.account_registry);
            projected_public_accounts = std::move(rebuilt.public_accounts);
            projected_account_leaf_entries = std::move(rebuilt.account_leaf_commitments);
            projected_pool = rebuilt.pool;
        } else {
            for (const auto& commitment : block_smile_commitments) {
                projected_public_accounts.erase(commitment);
                projected_account_leaf_entries.erase(commitment);
            }
        }

        const auto prepared_marker = MakePreparedShieldedMutationMarker(
            pindex,
            pindex->pprev,
            projected_tree,
            projected_pool,
            projected_account_registry);
        if (!prepared_marker.has_value() ||
            !WriteShieldedMutationMarker(m_chainman.m_shielded_nullifiers.get(), *prepared_marker)) {
            return DISCONNECT_FAILED;
        }

        if (!block_nullifiers.empty() && !m_chainman.m_shielded_nullifiers->Remove(block_nullifiers)) {
            return DISCONNECT_FAILED;
        }
        if (!block_recovery_exit_commitments.empty() &&
            !m_chainman.m_shielded_nullifiers->RemoveRecoveryExitCommitments(block_recovery_exit_commitments)) {
            return DISCONNECT_FAILED;
        }
        std::vector<uint256> created_unconsumed_settlement_anchors;
        created_unconsumed_settlement_anchors.reserve(block_settlement_anchors.size());
        for (const auto& anchor : block_settlement_anchors) {
            if (block_consumed_settlement_anchor_set.count(anchor) == 0) {
                created_unconsumed_settlement_anchors.push_back(anchor);
            }
        }
        if (!created_unconsumed_settlement_anchors.empty() &&
            !m_chainman.m_shielded_nullifiers->RemoveSettlementAnchors(created_unconsumed_settlement_anchors)) {
            return DISCONNECT_FAILED;
        }
        std::vector<uint256> consumed_preexisting_settlement_anchors;
        consumed_preexisting_settlement_anchors.reserve(block_consumed_settlement_anchor_set.size());
        for (const auto& anchor : block_consumed_settlement_anchor_set) {
            if (block_created_settlement_anchor_set.count(anchor) == 0) {
                consumed_preexisting_settlement_anchors.push_back(anchor);
            }
        }
        std::map<uint256, ConfirmedSettlementAnchorState> settlement_anchor_undo_metadata;
        for (const auto& anchor_state : blockUndo.consumed_settlement_anchor_states) {
            if (!anchor_state.IsValid()) {
                LogError("DisconnectBlock(): invalid settlement-anchor undo metadata for block %s anchor %s\n",
                         pindex->GetBlockHash().ToString(),
                         anchor_state.anchor.ToString());
                return DISCONNECT_FAILED;
            }
            if (!settlement_anchor_undo_metadata.emplace(anchor_state.anchor, anchor_state).second) {
                LogError("DisconnectBlock(): duplicate settlement-anchor undo metadata for block %s anchor %s\n",
                         pindex->GetBlockHash().ToString(),
                         anchor_state.anchor.ToString());
                return DISCONNECT_FAILED;
            }
        }
        if (settlement_anchor_undo_metadata.size() != consumed_preexisting_settlement_anchors.size()) {
            LogError("DisconnectBlock(): settlement-anchor undo metadata count mismatch for block %s "
                     "(expected %u, got %u); cannot prune-safely restore consumed anchors. Run -reindex "
                     "or reload a patched snapshot from an archival node\n",
                     pindex->GetBlockHash().ToString(),
                     static_cast<unsigned>(consumed_preexisting_settlement_anchors.size()),
                     static_cast<unsigned>(settlement_anchor_undo_metadata.size()));
            return DISCONNECT_FAILED;
        }
        std::vector<ConfirmedSettlementAnchorState> consumed_preexisting_settlement_anchor_states;
        consumed_preexisting_settlement_anchor_states.reserve(consumed_preexisting_settlement_anchors.size());
        for (const auto& anchor : consumed_preexisting_settlement_anchors) {
            const auto undo_it = settlement_anchor_undo_metadata.find(anchor);
            if (undo_it == settlement_anchor_undo_metadata.end()) {
                LogError("DisconnectBlock(): missing settlement-anchor undo metadata for block %s anchor %s; "
                         "cannot prune-safely restore consumed anchors. Run -reindex or reload a patched "
                         "snapshot from an archival node\n",
                         pindex->GetBlockHash().ToString(),
                         anchor.ToString());
                return DISCONNECT_FAILED;
            }
            consumed_preexisting_settlement_anchor_states.push_back(undo_it->second);
        }
        if (!consumed_preexisting_settlement_anchors.empty() &&
            !m_chainman.m_shielded_nullifiers->InsertSettlementAnchors(
                consumed_preexisting_settlement_anchor_states)) {
            return DISCONNECT_FAILED;
        }
        if (!block_netting_manifests.empty() &&
            !m_chainman.m_shielded_nullifiers->RemoveNettingManifests(block_netting_manifests)) {
            return DISCONNECT_FAILED;
        }
        if (!m_chainman.m_shielded_nullifiers->WritePoolBalance(projected_pool.GetBalance())) {
            return DISCONNECT_FAILED;
        }
        if (!projected_tree.AttachConfiguredCommitmentIndexStore()) {
            return DISCONNECT_FAILED;
        }

        m_chainman.m_shielded_merkle_tree = std::move(projected_tree);
        m_chainman.m_shielded_account_registry = std::move(projected_account_registry);
        m_chainman.m_shielded_smile_public_accounts = std::move(projected_public_accounts);
        m_chainman.m_shielded_account_leaf_commitments = std::move(projected_account_leaf_entries);
        m_chainman.InvalidateShieldedAccountStateSnapshotCaches();
        m_chainman.m_shielded_pool_balance = projected_pool;

        // v0.32.0 unshield velocity cap: erase this block's net-egress entry from the trailing window
        // (exact reorg undo). Only the entry recorded at connect time (height >= activation) exists.
        if (m_chainman.GetConsensus().IsShieldedUnshieldVelocityCapActive(pindex->nHeight)) {
            ShieldedUnshieldVelocity rolled_back_velocity = m_chainman.m_shielded_unshield_velocity;
            rolled_back_velocity.UndoBlock(pindex->nHeight);
            if (!m_chainman.m_shielded_nullifiers->WriteUnshieldVelocity(rolled_back_velocity)) {
                return DISCONNECT_FAILED;
            }
            m_chainman.m_shielded_unshield_velocity = std::move(rolled_back_velocity);
        }
    }

    if (apply_shielded_state && this == &m_chainman.ActiveChainstate() && m_chainman.m_shielded_state_initialized) {
        if (!m_chainman.m_shielded_anchor_roots.empty()) {
            m_chainman.m_shielded_anchor_roots.pop_front();
        }
        const uint256 current_root = m_chainman.m_shielded_merkle_tree.Root();
        if (m_chainman.m_shielded_anchor_roots.empty()) {
            m_chainman.RecordShieldedAnchorRoot(current_root);
        } else if (m_chainman.m_shielded_anchor_roots.front() != current_root) {
            if (!m_chainman.RebuildShieldedAnchorHistory(*this, pindex->pprev)) {
                return DISCONNECT_FAILED;
            }
        }
        if (!m_chainman.m_shielded_account_registry_roots.empty()) {
            m_chainman.m_shielded_account_registry_roots.pop_front();
        }
        const uint256 current_registry_root = m_chainman.m_shielded_account_registry.Root();
        if (m_chainman.m_shielded_account_registry_roots.empty()) {
            m_chainman.RecordShieldedAccountRegistryRoot(current_registry_root);
        } else if (m_chainman.m_shielded_account_registry_roots.front() != current_registry_root) {
            if (!m_chainman.RebuildShieldedAccountRegistryHistory(*this, pindex->pprev)) {
                return DISCONNECT_FAILED;
            }
        }
        if (!m_chainman.PersistShieldedState(pindex->pprev)) {
            return DISCONNECT_FAILED;
        }
    }

    // move best block pointer to prevout block
    view.SetBestBlock(pindex->pprev->GetBlockHash());

    return fClean ? DISCONNECT_OK : DISCONNECT_UNCLEAN;
}

/**
 * Threshold condition checker that triggers when unknown versionbits are seen on the network.
 */
class WarningBitsConditionChecker : public AbstractThresholdConditionChecker
{
private:
    const ChainstateManager& m_chainman;
    int m_bit;

public:
    explicit WarningBitsConditionChecker(const ChainstateManager& chainman, int bit) : m_chainman{chainman}, m_bit(bit) {}

    int64_t BeginTime(const Consensus::Params& params) const override { return 0; }
    int64_t EndTime(const Consensus::Params& params) const override { return std::numeric_limits<int64_t>::max(); }
    int Period(const Consensus::Params& params) const override { return params.nMinerConfirmationWindow; }
    int Threshold(const Consensus::Params& params) const override { return params.nRuleChangeActivationThreshold; }

    bool Condition(const CBlockIndex* pindex, const Consensus::Params& params) const override
    {
        return pindex->nHeight >= params.MinBIP9WarningHeight &&
               ((pindex->nVersion & VERSIONBITS_TOP_MASK) == VERSIONBITS_TOP_BITS) &&
               ((pindex->nVersion >> m_bit) & 1) != 0 &&
               ((m_chainman.m_versionbitscache.ComputeBlockVersion(pindex->pprev, params) >> m_bit) & 1) == 0;
    }
};

static unsigned int GetBlockScriptFlags(const CBlockIndex& block_index, const ChainstateManager& chainman)
{
    const Consensus::Params& consensusparams = chainman.GetConsensus();

    // BIP16 didn't become active until Apr 1 2012 (on mainnet, and
    // retroactively applied to testnet)
    // However, only one historical block violated the P2SH rules (on both
    // mainnet and testnet).
    // Similarly, only one historical block violated the TAPROOT rules on
    // mainnet.
    // For simplicity, always leave P2SH+WITNESS+TAPROOT on except for the two
    // violating blocks.
    uint32_t flags{SCRIPT_VERIFY_P2SH | SCRIPT_VERIFY_WITNESS | SCRIPT_VERIFY_TAPROOT |
                   SCRIPT_VERIFY_CHECKTEMPLATEVERIFY | SCRIPT_VERIFY_CHECKSIGFROMSTACK};
    const auto it{consensusparams.script_flag_exceptions.find(*Assert(block_index.phashBlock))};
    if (it != consensusparams.script_flag_exceptions.end()) {
        flags = it->second;
    }

    // Enforce the DERSIG (BIP66) rule
    if (DeploymentActiveAt(block_index, chainman, Consensus::DEPLOYMENT_DERSIG)) {
        flags |= SCRIPT_VERIFY_DERSIG;
    }

    // Enforce CHECKLOCKTIMEVERIFY (BIP65)
    if (DeploymentActiveAt(block_index, chainman, Consensus::DEPLOYMENT_CLTV)) {
        flags |= SCRIPT_VERIFY_CHECKLOCKTIMEVERIFY;
    }

    // Enforce CHECKSEQUENCEVERIFY (BIP112)
    if (DeploymentActiveAt(block_index, chainman, Consensus::DEPLOYMENT_CSV)) {
        flags |= SCRIPT_VERIFY_CHECKSEQUENCEVERIFY;
    }

    // Enforce BIP147 NULLDUMMY (activated simultaneously with segwit)
    if (DeploymentActiveAt(block_index, chainman, Consensus::DEPLOYMENT_SEGWIT)) {
        flags |= SCRIPT_VERIFY_NULLDUMMY;
    }

    if (consensusparams.nMLDSADisableHeight != std::numeric_limits<int32_t>::max() &&
        block_index.nHeight >= consensusparams.nMLDSADisableHeight) {
        flags |= SCRIPT_VERIFY_DISALLOW_MLDSA;
    }

    // FIPS-205 SLH-DSA activation (bundled with the C-002 fork): at/after
    // the activation height, SLH-DSA signatures are verified as finalized
    // FIPS-205 (pure mode) rather than the legacy round-3.x SPHINCS+ reference.
    // Keyed off the same height the C-002 prover/verifier branch on so the whole
    // C-002 upgrade activates atomically.
    if (consensusparams.IsShieldedC002Active(block_index.nHeight)) {
        flags |= SCRIPT_VERIFY_SLHDSA_FIPS205;
    }

    // Post-quantum only: where consensus forbids non-P2MR outputs (PQ-only chain),
    // also reject legacy secp256k1 ECDSA / Schnorr signature operations at the
    // verification layer (defense in depth; the paths are already unreachable
    // because no non-P2MR output can exist).
    if (consensusparams.fEnforceP2MROnlyOutputs) {
        flags |= SCRIPT_VERIFY_REJECT_LEGACY_SIGS;
    }

    return flags;
}

static bool CheckContentEliminationRules(const CBlock& block, int nHeight,
                                         const Consensus::Params& consensus_params,
                                         BlockValidationState& state);

/** Apply the effects of this block (with given index) on the UTXO set represented by coins.
 *  Validity checks that depend on the UTXO set are also done; ConnectBlock()
 *  can fail if those validity checks fail (among other reasons). */
bool Chainstate::ConnectBlock(const CBlock& block, BlockValidationState& state, CBlockIndex* pindex,
                              CCoinsViewCache& view, bool fJustCheck, bool apply_shielded_state)
{
    AssertLockHeld(cs_main);
    assert(pindex);

    uint256 block_hash{block.GetHash()};
    assert(*pindex->phashBlock == block_hash);
    const bool parallel_script_checks{m_chainman.m_script_check_queue_enabled && m_chainman.GetCheckQueue().HasThreads()};

    const auto time_start{SteadyClock::now()};
    const CChainParams& params{m_chainman.GetParams()};

    // Check it again in case a previous version let a bad block in
    // NOTE: We don't currently (re-)invoke ContextualCheckBlock() or
    // ContextualCheckBlockHeader() here. This means that if we add a new
    // consensus rule that is enforced in one of those two functions, then we
    // may have let in a block that violates the rule prior to updating the
    // software, and we would NOT be enforcing the rule here. Fully solving
    // upgrade from one software version to the next after a consensus rule
    // change is potentially tricky and issue-specific (see NeedsRedownload()
    // for one approach that was used for BIP 141 deployment).
    // Also, currently the rule against blocks more than 2 hours in the future
    // is enforced in ContextualCheckBlockHeader(); we wouldn't want to
    // re-enforce that rule here (at least until we make it impossible for
    // the clock to go backward).
    if (!CheckBlock(block, state, params.GetConsensus(), !fJustCheck, !fJustCheck)) {
        if (state.GetResult() == BlockValidationResult::BLOCK_MUTATED) {
            // We don't write down blocks to disk if they may have been
            // corrupted, so this should be impossible unless we're having hardware
            // problems.
            return FatalError(m_chainman.GetNotifications(), state, _("Corrupt block found indicating potential hardware failure."));
        }
        LogError("%s: Consensus::CheckBlock: %s\n", __func__, state.ToString());
        return false;
    }

    // ContextualCheckBlock is skipped by -reindex-chainstate. Re-run the
    // content-elimination block rules here so a node that upgrades after the
    // flag day cannot reconstruct a chainstate containing blocks accepted by
    // older software but invalid under the new consensus rules.
    if (!CheckContentEliminationRules(block, pindex->nHeight, params.GetConsensus(), state)) {
        return false;
    }

    // verify that the view's current state corresponds to the previous block
    uint256 hashPrevBlock = pindex->pprev == nullptr ? uint256() : pindex->pprev->GetBlockHash();
    if (hashPrevBlock != view.GetBestBlock()) {
        LogError("ConnectBlock: view best block (%s) does not match expected previous block (%s) at height %d\n",
                 view.GetBestBlock().GetHex(), hashPrevBlock.GetHex(), pindex->nHeight);
        return state.Invalid(BlockValidationResult::BLOCK_INVALID_PREV, "bad-view-bestblock",
            "view's best block does not match block's previous block");
    }

    m_chainman.num_blocks_total++;

    // BTX: genesis block transactions are connected normally so the
    // genesis coinbase reward is spendable after maturation.

    bool fScriptChecks = true;
    if (!m_chainman.AssumedValidBlock().IsNull()) {
        // We've been configured with the hash of a block which has been externally verified to have a valid history.
        // A suitable default value is included with the software and updated from time to time.  Because validity
        //  relative to a piece of software is an objective fact these defaults can be easily reviewed.
        // This setting doesn't force the selection of any particular chain but makes validating some faster by
        //  effectively caching the result of part of the verification.
        BlockMap::const_iterator it{m_blockman.m_block_index.find(m_chainman.AssumedValidBlock())};
        if (it != m_blockman.m_block_index.end()) {
            if (it->second.GetAncestor(pindex->nHeight) == pindex &&
                m_chainman.m_best_header->GetAncestor(pindex->nHeight) == pindex &&
                // Audit P0.1/C1: use AUTHENTICATED work so a forged matmul-header
                // chain cannot inflate best-header work past MinimumChainWork and
                // thereby relax (skip) expensive script verification. Equal to
                // nChainWork pre-fork and for any body-verified chain.
                m_chainman.m_best_header->nAuthenticatedChainWork >= m_chainman.MinimumChainWork()) {
                // This block is a member of the assumed verified chain and an ancestor of the best header.
                // Script verification is skipped when connecting blocks under the
                // assumevalid block. Assuming the assumevalid block is valid this
                // is safe because block merkle hashes are still computed and checked,
                // Of course, if an assumed valid block is invalid due to false scriptSigs
                // this optimization would allow an invalid chain to be accepted.
                // The equivalent time check discourages hash power from extorting the network via DOS attack
                //  into accepting an invalid block through telling users they must manually set assumevalid.
                //  Requiring a software change or burying the invalid block, regardless of the setting, makes
                //  it hard to hide the implication of the demand.  This also avoids having release candidates
                //  that are hardly doing any signature verification at all in testing without having to
                //  artificially set the default assumed verified block further back.
                // The test against the minimum chain work prevents the skipping when denied access to any chain at
                //  least as good as the expected chain.
                fScriptChecks = (GetBlockProofEquivalentTime(*m_chainman.m_best_header, *pindex, *m_chainman.m_best_header, params.GetConsensus()) <= 60 * 60 * 24 * 7 * 2);
            }
        }
    }

    const auto time_1{SteadyClock::now()};
    m_chainman.time_check += time_1 - time_start;
    LogDebug(BCLog::BENCH, "    - Sanity checks: %.2fms [%.2fs (%.2fms/blk)]\n",
             Ticks<MillisecondsDouble>(time_1 - time_start),
             Ticks<SecondsDouble>(m_chainman.time_check),
             Ticks<MillisecondsDouble>(m_chainman.time_check) / m_chainman.num_blocks_total);

    // Do not allow blocks that contain transactions which 'overwrite' older transactions,
    // unless those are already completely spent.
    // If such overwrites are allowed, coinbases and transactions depending upon those
    // can be duplicated to remove the ability to spend the first instance -- even after
    // being sent to another address.
    // See BIP30, CVE-2012-1909, and http://r6.ca/blog/20120206T005236Z.html for more information.
    // This rule was originally applied to all blocks with a timestamp after March 15, 2012, 0:00 UTC.
    // Now that the whole chain is irreversibly beyond that time it is applied to all blocks except the
    // two in the chain that violate it. This prevents exploiting the issue against nodes during their
    // initial block download.
    bool fEnforceBIP30 = !IsBIP30Repeat(*pindex);

    // Once BIP34 activated it was not possible to create new duplicate coinbases and thus other than starting
    // with the 2 existing duplicate coinbase pairs, not possible to create overwriting txs.  But by the
    // time BIP34 activated, in each of the existing pairs the duplicate coinbase had overwritten the first
    // before the first had been spent.  Since those coinbases are sufficiently buried it's no longer possible to create further
    // duplicate transactions descending from the known pairs either.
    // If we're on the known chain at height greater than where BIP34 activated, we can save the db accesses needed for the BIP30 check.

    // BIP34 requires that a block at height X (block X) has its coinbase
    // scriptSig start with a CScriptNum of X (indicated height X).  The above
    // logic of no longer requiring BIP30 once BIP34 activates is flawed in the
    // case that there is a block X before the BIP34 height of 227,931 which has
    // an indicated height Y where Y is greater than X.  The coinbase for block
    // X would also be a valid coinbase for block Y, which could be a BIP30
    // violation.  An exhaustive search of all mainnet coinbases before the
    // BIP34 height which have an indicated height greater than the block height
    // reveals many occurrences. The 3 lowest indicated heights found are
    // 209,921, 490,897, and 1,983,702 and thus coinbases for blocks at these 3
    // heights would be the first opportunity for BIP30 to be violated.

    // The search reveals a great many blocks which have an indicated height
    // greater than 1,983,702, so we simply remove the optimization to skip
    // BIP30 checking for blocks at height 1,983,702 or higher.  Before we reach
    // that block in another 25 years or so, we should take advantage of a
    // future consensus change to do a new and improved version of BIP34 that
    // will actually prevent ever creating any duplicate coinbases in the
    // future.
    static constexpr int BIP34_IMPLIES_BIP30_LIMIT = 1983702;

    // There is no potential to create a duplicate coinbase at block 209,921
    // because this is still before the BIP34 height and so explicit BIP30
    // checking is still active.

    // The final case is block 176,684 which has an indicated height of
    // 490,897. Unfortunately, this issue was not discovered until about 2 weeks
    // before block 490,897 so there was not much opportunity to address this
    // case other than to carefully analyze it and determine it would not be a
    // problem. Block 490,897 was, in fact, mined with a different coinbase than
    // block 176,684, but it is important to note that even if it hadn't been or
    // is remined on an alternate fork with a duplicate coinbase, we would still
    // not run into a BIP30 violation.  This is because the coinbase for 176,684
    // is spent in block 185,956 in transaction
    // d4f7fbbf92f4a3014a230b2dc70b8058d02eb36ac06b4a0736d9d60eaa9e8781.  This
    // spending transaction can't be duplicated because it also spends coinbase
    // 0328dd85c331237f18e781d692c92de57649529bd5edf1d01036daea32ffde29.  This
    // coinbase has an indicated height of over 4.2 billion, and wouldn't be
    // duplicatable until that height, and it's currently impossible to create a
    // chain that long. Nevertheless we may wish to consider a future soft fork
    // which retroactively prevents block 490,897 from creating a duplicate
    // coinbase. The two historical BIP30 violations often provide a confusing
    // edge case when manipulating the UTXO and it would be simpler not to have
    // another edge case to deal with.

    // testnet3 has no blocks before the BIP34 height with indicated heights
    // post BIP34 before approximately height 486,000,000. After block
    // 1,983,702 testnet3 starts doing unnecessary BIP30 checking again.
    //
    // BTX: We connect the genesis block normally (so its coinbase reward can
    // mature and become spendable). Genesis has no pprev; in that case, simply
    // skip the BIP34 optimization below.
    CBlockIndex* pindexBIP34height{nullptr};
    if (pindex->pprev) {
        pindexBIP34height = pindex->pprev->GetAncestor(params.GetConsensus().BIP34Height);
    }
    //Only continue to enforce if we're below BIP34 activation height or the block hash at that height doesn't correspond.
    fEnforceBIP30 = fEnforceBIP30 && (!pindexBIP34height || pindexBIP34height->GetBlockHash() != params.GetConsensus().BIP34Hash);

    // NOTE: Remove BIP30 checking from block height 1,983,702 on, once we have a
    // consensus change that ensures coinbases at those heights cannot
    // duplicate earlier coinbases.
    if (fEnforceBIP30 || pindex->nHeight >= BIP34_IMPLIES_BIP30_LIMIT) {
        for (const auto& tx : block.vtx) {
            for (size_t o = 0; o < tx->vout.size(); o++) {
                if (view.HaveCoin(COutPoint(tx->GetHash(), o))) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-txns-BIP30",
                                  "tried to overwrite transaction");
                }
            }
        }
    }

    // Enforce BIP68 (sequence locks)
    int nLockTimeFlags = 0;
    if (DeploymentActiveAt(*pindex, m_chainman, Consensus::DEPLOYMENT_CSV)) {
        nLockTimeFlags |= LOCKTIME_VERIFY_SEQUENCE;
    }

    // Get the script flags for this block
    unsigned int flags{GetBlockScriptFlags(*pindex, m_chainman)};

    const auto time_2{SteadyClock::now()};
    m_chainman.time_forks += time_2 - time_1;
    LogDebug(BCLog::BENCH, "    - Fork checks: %.2fms [%.2fs (%.2fms/blk)]\n",
             Ticks<MillisecondsDouble>(time_2 - time_1),
             Ticks<SecondsDouble>(m_chainman.time_forks),
             Ticks<MillisecondsDouble>(m_chainman.time_forks) / m_chainman.num_blocks_total);

    CBlockUndo blockundo;

    const bool block_has_shielded_bundle{BlockHasShieldedBundle(block)};
    const bool block_updates_shielded_velocity{
        params.GetConsensus().IsShieldedUnshieldVelocityCapActive(pindex->nHeight)};
    const bool enforce_shielded_consensus{
        apply_shielded_state && this == &m_chainman.ActiveChainstate() &&
        (block_has_shielded_bundle || block_updates_shielded_velocity)};
    if (enforce_shielded_consensus) {
        if (!m_chainman.EnsureShieldedStateInitialized()) {
            return state.Error("shielded-state-init-failed");
        }
    }

    const bool parallel_shielded_checks{
        enforce_shielded_consensus &&
        m_chainman.m_script_check_queue_enabled &&
        m_chainman.GetShieldedProofCheckQueue().HasThreads()
    };
    if (enforce_shielded_consensus && !parallel_shielded_checks) {
        LogWarning("ConnectBlock: shielded proof verification will run serially (no parallel check queue). "
                   "Block validation may be significantly slower.\n");
    }
    std::shared_ptr<const shielded::ShieldedMerkleTree> shielded_tree_snapshot;
    std::shared_ptr<const std::map<uint256, smile2::CompactPublicAccount>> shielded_smile_public_accounts_snapshot;
    std::shared_ptr<const std::map<uint256, uint256>> shielded_account_leaf_commitments_snapshot;
    if (enforce_shielded_consensus) {
        shielded_tree_snapshot = std::make_shared<const shielded::ShieldedMerkleTree>(
            m_chainman.GetShieldedMerkleTree());
        shielded_smile_public_accounts_snapshot =
            m_chainman.GetShieldedSmilePublicAccountsSnapshot();
        shielded_account_leaf_commitments_snapshot =
            m_chainman.GetShieldedAccountLeafCommitmentsSnapshot();
    }

    // Precomputed transaction data pointers must not be invalidated
    // until after `control` has run the script checks (potentially
    // in multiple threads). Preallocate the vector size so a new allocation
    // doesn't invalidate pointers into the vector, and keep txsdata in scope
    // for as long as `control`.
    std::vector<PrecomputedTransactionData> txsdata(block.vtx.size());
    CCheckQueueControl<CScriptCheck> control(fScriptChecks && parallel_script_checks ? &m_chainman.GetCheckQueue() : nullptr);
    // Shielded proof verification is a consensus check and is not relaxed by
    // assumevalid. Structural checks (nullifier uniqueness, pool balance,
    // merkle tree updates) are likewise always enforced.
    const bool fShieldedProofChecks = enforce_shielded_consensus;
    CCheckQueueControl<CShieldedProofCheck, std::string> shielded_proof_control(
        fShieldedProofChecks && parallel_shielded_checks ? &m_chainman.GetShieldedProofCheckQueue() : nullptr);
    CCheckQueueControl<CShieldedSpendAuthCheck, std::string> shielded_auth_control(
        fShieldedProofChecks && parallel_shielded_checks ? &m_chainman.GetShieldedSpendAuthCheckQueue() : nullptr);
    std::vector<int> prevheights;
    CAmount nFees = 0;
    int nInputs = 0;
    int64_t nSigOpsCost = 0;
    std::unordered_set<Nullifier, NullifierHasher> block_nullifiers;
    std::vector<Nullifier> block_nullifier_vec;
    std::vector<uint256> block_recovery_exit_commitments;
    std::set<uint256> block_settlement_anchors;
    std::vector<ConfirmedSettlementAnchorState> block_settlement_anchor_states;
    std::set<uint256> block_consumed_settlement_anchors;
    std::vector<uint256> block_consumed_settlement_anchor_vec;
    std::vector<ConfirmedSettlementAnchorState> block_consumed_settlement_anchor_states;
    std::set<uint256> block_netting_manifests;
    std::vector<uint256> block_netting_manifest_vec;
    // Finding 10 fix: track output commitments across all transactions in the
    // block to reject duplicate commitments that could corrupt the Merkle tree.
    std::set<uint256> block_output_commitments;
    std::vector<shielded::registry::ShieldedAccountLeaf> block_account_leaves;
    std::vector<std::pair<uint256, smile2::CompactPublicAccount>> block_smile_public_accounts;
    std::vector<std::pair<uint256, uint256>> block_account_leaf_entries;
    uint64_t nBlockShieldedVerifyCost{0};
    uint64_t nBlockShieldedScanUnits{0};
    uint64_t nBlockShieldedTreeUpdateUnits{0};
    uint64_t nBlockShieldedAccountRegistryAppends{0};
    const uint64_t current_registry_entries = m_chainman.GetShieldedAccountRegistryEntryCount();
    if (UseAccountRegistryEntryCountLimit(params.GetConsensus(), pindex->nHeight) &&
        current_registry_entries > params.GetConsensus().nMaxShieldedAccountRegistryEntries) {
        return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                             "bad-blk-shielded-account-registry-size-limit");
    }
    shielded::ShieldedMerkleTree next_shielded_tree = m_chainman.m_shielded_merkle_tree;
    ShieldedPoolBalance next_pool_balance = m_chainman.m_shielded_pool_balance;
    bool shielded_anchor_repair_attempted{false};
    bool shielded_state_repair_attempted{false};
    blockundo.vtxundo.reserve(block.vtx.size() - 1);
    for (unsigned int i = 0; i < block.vtx.size(); i++)
    {
        if (!state.IsValid()) break;
        const CTransaction &tx = *(block.vtx[i]);
        const bool use_nonced_bridge_tag =
            UseNoncedShieldedBridgeTags(params.GetConsensus(), pindex->nHeight);

        nInputs += tx.vin.size();

        if (tx.HasShieldedBundle()) {
            std::string height_gate_reject;
            if (!RejectShieldedHeightGateViolation(tx,
                                                   params.GetConsensus(),
                                                   pindex->nHeight,
                                                   height_gate_reject)) {
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, height_gate_reject);
                break;
            }
        }

        if (enforce_shielded_consensus && tx.HasShieldedBundle()) {
            if (pindex->nHeight < params.GetConsensus().nShieldedPoolActivationHeight) {
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-shielded-premature");
                break;
            }
            if (::GetSerializeSize(TX_WITH_WITNESS(tx)) > params.GetConsensus().nMaxShieldedTxSize) {
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-shielded-tx-size");
                break;
            }
            const CShieldedBundle& bundle = tx.GetShieldedBundle();

            const auto bundle_nullifiers = CollectShieldedNullifiers(bundle);
            for (const auto& nullifier : bundle_nullifiers) {
                if (!block_nullifiers.insert(nullifier).second) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-shielded-nullifier-duplicate");
                    break;
                }
                if (m_chainman.IsShieldedNullifierSpent(nullifier)) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-shielded-nullifier-spent");
                    break;
                }
                block_nullifier_vec.push_back(nullifier);
            }
            if (!state.IsValid()) break;

            // Finding 9 fix: validate every historical shielded anchor reference
            // carried by the bundle before any state transition. This covers both
            // legacy output anchors and v2 transaction-family spend anchors.
            for (const auto& anchor : CollectShieldedAnchors(bundle)) {
                if (!m_chainman.IsShieldedAnchorValid(anchor)) {
                    if (!shielded_anchor_repair_attempted) {
                        shielded_anchor_repair_attempted = true;
                        if (TryAutoRepairShieldedAnchorHistory(m_chainman,
                                                              "ConnectBlock",
                                                              pindex->GetBlockHash()) &&
                            m_chainman.IsShieldedAnchorValid(anchor)) {
                            continue;
                        }
                    }
                    LogDebug(BCLog::VALIDATION, "bad-shielded-anchor (block): provided=%s txid=%s\n",
                             anchor.ToString(),
                             tx.GetHash().ToString());
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-shielded-anchor");
                    break;
                }
            }
            if (!state.IsValid()) break;

            // Finding 10 fix: reject duplicate output commitments across all
            // transactions within the same block. Intra-bundle uniqueness is
            // enforced by CheckStructure(), but cross-tx duplicates could
            // corrupt the commitment tree or enable commitment-grinding.
            const auto bundle_output_commitments = CollectShieldedOutputCommitments(bundle);
            for (const auto& commitment : bundle_output_commitments) {
                if (!block_output_commitments.insert(commitment).second) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                  "bad-shielded-commitment-duplicate");
                    break;
                }
            }
            if (!state.IsValid()) break;

            // V2_RECOVERY_EXIT (fork-gated). A transparent claim of a stranded
            // pre-snapshot note: derive (cm, nf) from the revealed payload, verify the PQ ownership
            // signature and the membership proof, run the consensus predicate,
            // and collect BOTH identifiers for atomic retirement (nf into the SHARED nullifier set below,
            // cm into the spent-commitment set). No shielded outputs / no tree append; the pool debit flows
            // through the standard value_balance turnstile (TryGetShieldedStateValueBalance == +value).
            // Production activation must replace the live-root regtest stand-in below with a hard
            // consensus-pinned 125,000 commitment root.
            if (IsRecoveryExitBundle(bundle)) {
                if (!params.GetConsensus().IsShieldedRecoveryExitActive(pindex->nHeight)) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-shielded-v2-recovery-exit-disabled");
                    break;
                }
                shielded::recovery::RecoveryExitClaim re_claim;
                shielded::recovery::RecoveryExitIdentifiers re_ids;
                if (!GetRecoveryExitIdentifiersFromBundle(bundle, re_claim, re_ids)) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-shielded-recovery-exit-malformed");
                    break;
                }
                const uint256 re_tx_binding = shielded::recovery::ComputeRecoveryExitTransparentBinding(tx.vout);
                const uint256 re_binding_hash = shielded::recovery::ComputeRecoveryExitBindingHash(
                    re_ids.commitment, re_ids.nullifier, re_claim.value, re_tx_binding);
                CAmount re_transparent_out{0};
                std::string re_sum_reject;
                if (!TrySumTransparentOutputs(tx.vout, re_transparent_out, re_sum_reject)) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, re_sum_reject);
                    break;
                }
                CAmount re_fee{0};
                std::string re_fee_reject;
                if (!TryComputeRecoveryExitFee(re_claim.value, re_transparent_out, re_fee, re_fee_reject)) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, re_fee_reject);
                    break;
                }
                const bool re_nullifier_already_spent = block_nullifiers.count(re_ids.nullifier) > 0 ||
                    m_chainman.IsShieldedNullifierSpent(re_ids.nullifier);
                if (re_nullifier_already_spent) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-recovery-exit-nullifier-spent");
                    break;
                }
                const bool re_commitment_already_claimed =
                    std::find(block_recovery_exit_commitments.begin(), block_recovery_exit_commitments.end(),
                              re_ids.commitment) != block_recovery_exit_commitments.end() ||
                    m_chainman.m_shielded_nullifiers->ContainsRecoveryExitCommitment(re_ids.commitment);
                if (re_commitment_already_claimed) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-recovery-exit-commitment-claimed");
                    break;
                }
                const shielded::ShieldedMerkleTree& recovery_membership_tree =
                    shielded_tree_snapshot != nullptr ? *shielded_tree_snapshot : next_shielded_tree;
                const uint256 re_membership_root = RecoveryExitMembershipRoot(
                    params.GetConsensus(),
                    pindex->nHeight,
                    recovery_membership_tree);
                std::string re_membership_reject;
                shielded::recovery::RecoveryExitConstraints re_c;
                re_c.value_balance = re_claim.value;
                re_c.fee = re_fee;
                re_c.transparent_out = re_transparent_out;
                re_c.transparent_input_count = tx.vin.size();
                re_c.shielded_output_count = bundle_output_commitments.size();
                re_c.pool_balance = next_pool_balance.GetBalance();
                re_c.validation_height = pindex->nHeight;
                re_c.activation_height = params.GetConsensus().nShieldedRecoveryExitActivationHeight;
                re_c.expiry_height = 0;
                re_c.ownership_verified = shielded::recovery::VerifyRecoveryExitOwnership(re_claim, re_binding_hash);
                // Membership is checked against a consensus-pinned frozen root when supplied. If the root
                // is not known at release time, the post-sunset zero-output rule freezes the live tree, so
                // the current root at/after activation is the deterministic frozen membership root.
                re_c.membership_verified = shielded::recovery::VerifyRecoveryExitMembership(
                    re_claim, re_ids.commitment,
                    re_membership_root, re_membership_reject);
                re_c.nullifier_already_spent = re_nullifier_already_spent;
                re_c.commitment_already_claimed = re_commitment_already_claimed;
                shielded::recovery::RecoveryExitIdentifiers re_checked;
                std::string re_reject;
                if (!shielded::recovery::CheckRecoveryExitClaim(re_claim, re_c, re_checked, re_reject)) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, re_reject);
                    break;
                }
                if (!block_nullifiers.insert(re_ids.nullifier).second) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-shielded-nullifier-duplicate");
                    break;
                }
                block_nullifier_vec.push_back(re_ids.nullifier);
                block_recovery_exit_commitments.push_back(re_ids.commitment);
            }

            const auto bundle_output_smile_accounts = CollectShieldedOutputSmileAccounts(bundle);
            block_smile_public_accounts.insert(block_smile_public_accounts.end(),
                                              bundle_output_smile_accounts.begin(),
                                              bundle_output_smile_accounts.end());
            const auto bundle_account_leaf_entries =
                CollectShieldedOutputAccountLeafEntries(bundle, use_nonced_bridge_tag);
            if (!bundle_account_leaf_entries.has_value()) {
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-shielded-account-leaf");
                break;
            }
            block_account_leaf_entries.insert(block_account_leaf_entries.end(),
                                              bundle_account_leaf_entries->begin(),
                                              bundle_account_leaf_entries->end());
            const auto bundle_account_leaves =
                shielded::registry::CollectShieldedOutputAccountLeaves(bundle,
                                                                      use_nonced_bridge_tag);
            if (!bundle_account_leaves.has_value()) {
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-shielded-account-leaf");
                break;
            }
            if (UseAccountRegistryAppendRateLimit(params.GetConsensus(), pindex->nHeight)) {
                const uint64_t bundle_account_registry_appends =
                    static_cast<uint64_t>(bundle_account_leaves->size());
                if (bundle_account_registry_appends >
                        params.GetConsensus().nMaxBlockShieldedAccountRegistryAppends ||
                    nBlockShieldedAccountRegistryAppends >
                    params.GetConsensus().nMaxBlockShieldedAccountRegistryAppends -
                        bundle_account_registry_appends) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                  "bad-blk-shielded-account-registry-rate-limit");
                    break;
                }
                nBlockShieldedAccountRegistryAppends += bundle_account_registry_appends;
                if (nBlockShieldedAccountRegistryAppends >
                    params.GetConsensus().nMaxBlockShieldedAccountRegistryAppends) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                  "bad-blk-shielded-account-registry-rate-limit");
                    break;
                }
            }
            if (UseAccountRegistryEntryCountLimit(params.GetConsensus(), pindex->nHeight) &&
                WouldExceedAccountRegistryEntryCountLimit(params.GetConsensus(),
                                                         current_registry_entries,
                                                         nBlockShieldedAccountRegistryAppends)) {
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                              "bad-blk-shielded-account-registry-size-limit");
                break;
            }
            block_account_leaves.insert(block_account_leaves.end(),
                                        bundle_account_leaves->begin(),
                                        bundle_account_leaves->end());

            std::string pool_balance_reject;
            const auto value_balance = TryGetShieldedStateValueBalance(bundle, pool_balance_reject);
            if (!value_balance.has_value()) {
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, pool_balance_reject);
                break;
            }
            if (!next_pool_balance.ApplyValueBalance(*value_balance)) {
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "shielded-pool-balance-negative");
                break;
            }
            try {
                for (const auto& commitment : bundle_output_commitments) {
                    next_shielded_tree.Append(commitment);
                }
            } catch (const std::exception& e) {
                state.Error(strprintf("ConnectBlock(): shielded tree append failed for tx %s in block %s: %s",
                                      tx.GetHash().ToString(),
                                      pindex->GetBlockHash().ToString(),
                                      e.what()));
                break;
            }

            const auto shielded_usage = GetShieldedResourceUsage(bundle);
            // Consensus rule: enforce per-block shielded verification cost limit.
            // Prevents adversarial blocks that take excessive time to validate.
            nBlockShieldedVerifyCost += shielded_usage.verify_units;
            if (nBlockShieldedVerifyCost > params.GetConsensus().nMaxBlockShieldedVerifyCost) {
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                              "bad-blk-shielded-cost",
                              "block exceeds maximum shielded verification cost");
                break;
            }
            nBlockShieldedScanUnits += shielded_usage.scan_units;
            if (nBlockShieldedScanUnits > params.GetConsensus().nMaxBlockShieldedScanUnits) {
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                              "bad-blk-shielded-scan",
                              "block exceeds maximum shielded scan units");
                break;
            }
            nBlockShieldedTreeUpdateUnits += shielded_usage.tree_update_units;
            if (nBlockShieldedTreeUpdateUnits > params.GetConsensus().nMaxBlockShieldedTreeUpdateUnits) {
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                              "bad-blk-shielded-tree-updates",
                              "block exceeds maximum shielded tree update units");
                break;
            }

            if (bundle.HasV2Bundle() &&
                bundle.GetTransactionFamily() == shielded::v2::V2_SPEND_PATH_RECOVERY) {
                if (!params.GetConsensus().IsShieldedSpendPathRecoveryActive(pindex->nHeight)) {
                    LogShieldedV2ContextReject("block-v2-spend-path-recovery-disabled", bundle);
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                  "bad-shielded-v2-spend-path-recovery-disabled");
                    break;
                }
            }
            if (bundle.HasV2Bundle() &&
                bundle.GetTransactionFamily() != shielded::v2::TransactionFamily::V2_SEND &&
                bundle.GetTransactionFamily() != shielded::v2::V2_SPEND_PATH_RECOVERY &&
                bundle.GetTransactionFamily() != shielded::v2::TransactionFamily::V2_RECOVERY_EXIT &&
                bundle.GetTransactionFamily() != shielded::v2::TransactionFamily::V2_INGRESS_BATCH &&
                bundle.GetTransactionFamily() != shielded::v2::TransactionFamily::V2_EGRESS_BATCH &&
                bundle.GetTransactionFamily() != shielded::v2::TransactionFamily::V2_SETTLEMENT_ANCHOR &&
                bundle.GetTransactionFamily() != shielded::v2::TransactionFamily::V2_REBALANCE &&
                bundle.GetTransactionFamily() != shielded::v2::TransactionFamily::V2_LIFECYCLE) {
                LogShieldedV2ContextReject("block-allowed-family", bundle);
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-shielded-v2-contextual");
                break;
            }
            std::string retired_matrict_reject;
            if (!RejectRetiredMatRiCTEnvelopeAfterDisable(bundle,
                                                          params.GetConsensus(),
                                                          pindex->nHeight,
                                                          retired_matrict_reject)) {
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, retired_matrict_reject);
                break;
            }
            std::string account_registry_reject;
            if (!ValidateShieldedAccountRegistryRefs(bundle, m_chainman, account_registry_reject)) {
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, account_registry_reject);
                break;
            }

            std::shared_ptr<const shielded::ringct::MatRiCTProof> parsed_spend_auth_proof;
            std::optional<std::vector<Nullifier>> proof_bound_nullifiers;
            if (bundle.HasShieldedInputs()) {
                const bool uses_spend_auth_checks = UsesShieldedSpendAuthChecks(bundle);
                std::string ring_reject;
                const uint64_t shielded_tree_size = shielded_tree_snapshot->Size();
                if (!RejectShieldedSmallPrivacyPool(bundle,
                                                    shielded_tree_size,
                                                    params.GetConsensus(),
                                                    pindex->nHeight,
                                                    ring_reject)) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, ring_reject);
                    break;
                }
                if (!PrecheckShieldedRingPositions(bundle,
                                                   shielded_tree_size,
                                                   ring_reject)) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, ring_reject);
                    break;
                }
                std::string proof_plausibility_reject;
                if (!PrecheckShieldedProofPlausibility(bundle, proof_plausibility_reject)) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, proof_plausibility_reject);
                    break;
                }
                if (uses_spend_auth_checks) {
                    std::string spend_auth_reject;
                    auto parsed = ParseShieldedSpendAuthProof(bundle, spend_auth_reject);
                    if (!parsed.has_value()) {
                        state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, spend_auth_reject);
                        break;
                    }
                    parsed_spend_auth_proof = *parsed;
                    proof_bound_nullifiers = ExtractShieldedProofBoundNullifiers(
                        bundle,
                        spend_auth_reject,
                        params.GetConsensus().IsShieldedSmileRiceCodecDisabled(pindex->nHeight));
                    if (!proof_bound_nullifiers.has_value()) {
                        state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, spend_auth_reject);
                        break;
                    }
                }
            }

            const bool needs_shielded_proof_check =
                NeedsShieldedProofCheck(bundle, params.GetConsensus(), pindex->nHeight);
            std::string public_flow_reject;
            if (!RejectPostForkTransparentFundingV2SendContext(tx,
                                                               view,
                                                               params.GetConsensus(),
                                                               pindex->nHeight,
                                                               public_flow_reject)) {
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, public_flow_reject);
                break;
            }
            if (fShieldedProofChecks && needs_shielded_proof_check) {
                if (parallel_shielded_checks) {
                    std::vector<CShieldedProofCheck> proof_checks;
                    proof_checks.emplace_back(tx,
                                              params.GetConsensus(),
                                              pindex->nHeight,
                                              shielded_tree_snapshot,
                                              shielded_smile_public_accounts_snapshot,
                                              shielded_account_leaf_commitments_snapshot,
                                              parsed_spend_auth_proof);
                    shielded_proof_control.Add(std::move(proof_checks));

                    if (UsesShieldedSpendAuthChecks(bundle) && proof_bound_nullifiers.has_value()) {
                        std::vector<CShieldedSpendAuthCheck> auth_checks;
                        auth_checks.reserve(bundle.GetShieldedInputCount());
                        for (size_t spend_index = 0; spend_index < bundle.GetShieldedInputCount(); ++spend_index) {
                            auth_checks.emplace_back(tx, spend_index, (*proof_bound_nullifiers)[spend_index]);
                        }
                        shielded_auth_control.Add(std::move(auth_checks));
                    }
                } else {
                    CShieldedProofCheck proof_check(tx,
                                                    params.GetConsensus(),
                                                    pindex->nHeight,
                                                    shielded_tree_snapshot,
                                                    shielded_smile_public_accounts_snapshot,
                                                    shielded_account_leaf_commitments_snapshot,
                                                    parsed_spend_auth_proof);
                    auto err = proof_check();
                    if (err.has_value() &&
                        !shielded_state_repair_attempted &&
                        IsRecoverableShieldedProofReject(*err)) {
                        shielded_state_repair_attempted = true;
                        if (TryAutoRebuildShieldedState(m_chainman,
                                                        "ConnectBlock",
                                                        pindex->GetBlockHash())) {
                            RefreshShieldedValidationSnapshots(m_chainman,
                                                              shielded_tree_snapshot,
                                                              shielded_smile_public_accounts_snapshot,
                                                              shielded_account_leaf_commitments_snapshot);
                            CShieldedProofCheck retry_check(tx,
                                                            params.GetConsensus(),
                                                            pindex->nHeight,
                                                            shielded_tree_snapshot,
                                                            shielded_smile_public_accounts_snapshot,
                                                            shielded_account_leaf_commitments_snapshot,
                                                            parsed_spend_auth_proof);
                            err = retry_check();
                        }
                    }
                    if (err.has_value()) {
                        state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, *err);
                        break;
                    }
                    if (UsesShieldedSpendAuthChecks(bundle) && proof_bound_nullifiers.has_value()) {
                        for (size_t spend_index = 0; spend_index < bundle.GetShieldedInputCount(); ++spend_index) {
                            CShieldedSpendAuthCheck auth_check(tx, spend_index, (*proof_bound_nullifiers)[spend_index]);
                            if (auto err = auth_check()) {
                                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, *err);
                                break;
                            }
                        }
                    }
                    if (!state.IsValid()) break;
                }
            }
            if (bundle.HasV2Bundle()) {
                switch (*bundle.GetTransactionFamily()) {
                case shielded::v2::TransactionFamily::V2_EGRESS_BATCH: {
                    const auto& payload =
                        std::get<shielded::v2::EgressBatchPayload>(bundle.GetV2Bundle()->payload);
                    const bool anchor_created_in_block =
                        block_settlement_anchors.count(payload.settlement_anchor) != 0;
                    const auto anchor_state =
                        m_chainman.GetShieldedSettlementAnchorState(payload.settlement_anchor);
                    if (!anchor_state.has_value() && !anchor_created_in_block) {
                        state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-shielded-v2-egress-unanchored");
                        break;
                    }
                    if (!anchor_created_in_block &&
                        anchor_state.has_value() &&
                        !anchor_state->IsValid() &&
                        (UseSettlementAnchorMaturityRule(params.GetConsensus(), pindex->nHeight) ||
                         UseSingleUseSettlementAnchors(params.GetConsensus(), pindex->nHeight))) {
                        state.Error(strprintf("ConnectBlock(): missing settlement-anchor metadata for anchor %s "
                                              "in block %s; run -reindex or reload a patched snapshot from an "
                                              "archival node",
                                              payload.settlement_anchor.ToString(),
                                              pindex->GetBlockHash().ToString()));
                        break;
                    }
                    if (UseSettlementAnchorMaturityRule(params.GetConsensus(), pindex->nHeight)) {
                        if (anchor_created_in_block ||
                            !anchor_state.has_value() ||
                            !IsSettlementAnchorMature(*anchor_state,
                                                      params.GetConsensus(),
                                                      pindex->nHeight)) {
                            state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                          "bad-shielded-v2-egress-immature-anchor");
                            break;
                        }
                    }
                    if (UseSingleUseSettlementAnchors(params.GetConsensus(), pindex->nHeight)) {
                        if (!block_consumed_settlement_anchors.insert(payload.settlement_anchor).second) {
                            state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                          "bad-shielded-v2-egress-anchor-reused");
                            break;
                        }
                        block_consumed_settlement_anchor_vec.push_back(payload.settlement_anchor);
                        if (!anchor_created_in_block) {
                            if (!anchor_state.has_value() || !anchor_state->IsValid()) {
                                state.Error(strprintf("ConnectBlock(): missing settlement-anchor metadata for anchor %s "
                                                      "in block %s; run -reindex or reload a patched snapshot from an "
                                                      "archival node",
                                                      payload.settlement_anchor.ToString(),
                                                      pindex->GetBlockHash().ToString()));
                                break;
                            }
                            block_consumed_settlement_anchor_states.push_back(*anchor_state);
                        }
                    }
                    break;
                }
                case shielded::v2::TransactionFamily::V2_SETTLEMENT_ANCHOR: {
                    const auto& payload =
                        std::get<shielded::v2::SettlementAnchorPayload>(bundle.GetV2Bundle()->payload);
                    if (!payload.anchored_netting_manifest_id.IsNull() &&
                        !m_chainman.IsShieldedNettingManifestValid(payload.anchored_netting_manifest_id,
                                                                   pindex->nHeight) &&
                        block_netting_manifests.count(payload.anchored_netting_manifest_id) == 0) {
                        state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                      "bad-shielded-v2-settlement-unanchored-manifest");
                        break;
                    }
                    std::string reject_reason;
                    auto created_anchors = ExtractCreatedShieldedSettlementAnchors(tx, pindex->nHeight, reject_reason);
                    if (!created_anchors.has_value()) {
                        state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, reject_reason);
                        break;
                    }
                    for (const auto& settlement_anchor : *created_anchors) {
                        if (m_chainman.IsShieldedSettlementAnchorValid(settlement_anchor) ||
                            !block_settlement_anchors.insert(settlement_anchor).second) {
                            state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                          "bad-shielded-v2-settlement-anchor-duplicate");
                            break;
                        }
                        block_settlement_anchor_states.push_back(
                            ConfirmedSettlementAnchorState{settlement_anchor, pindex->nHeight});
                    }
                    break;
                }
                case shielded::v2::TransactionFamily::V2_REBALANCE: {
                    std::string reject_reason;
                    auto created_manifests = ExtractCreatedShieldedNettingManifests(tx, reject_reason);
                    if (!created_manifests.has_value()) {
                        state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, reject_reason);
                        break;
                    }
                    for (const auto& manifest_id : *created_manifests) {
                        if (m_chainman.HasShieldedNettingManifest(manifest_id) ||
                            !block_netting_manifests.insert(manifest_id).second) {
                            state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                          "bad-shielded-v2-rebalance-manifest-duplicate");
                            break;
                        }
                        block_netting_manifest_vec.push_back(manifest_id);
                    }
                    break;
                }
                case shielded::v2::TransactionFamily::V2_SEND:
                case shielded::v2::TransactionFamily::V2_SPEND_PATH_RECOVERY:
                case shielded::v2::TransactionFamily::V2_RECOVERY_EXIT:
                case shielded::v2::TransactionFamily::V2_INGRESS_BATCH:
                case shielded::v2::TransactionFamily::V2_LIFECYCLE:
                    break;
                case shielded::v2::TransactionFamily::V2_GENERIC:
                    LogShieldedV2ContextReject("block-v2-generic-family", bundle);
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-shielded-v2-contextual");
                    break;
                }
                if (!state.IsValid()) break;
            }
        }

        if (!tx.IsCoinBase())
        {
            CAmount txfee = 0;
            TxValidationState tx_state;
            if (!Consensus::CheckTxInputs(tx, tx_state, view, pindex->nHeight, txfee)) {
                // Any transaction validation failure in ConnectBlock is a block consensus failure
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                              tx_state.GetRejectReason(),
                              tx_state.GetDebugMessage() + " in transaction " + tx.GetHash().ToString());
                break;
            }
            nFees += txfee;
            if (!MoneyRange(nFees)) {
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-txns-accumulated-fee-outofrange",
                              "accumulated fee in the block out of range");
                break;
            }

            // BTX content-elimination hard fork (Pillars 2 & 3,
            // doc/btx-inscription-elimination-plan.md): once active, promote the
            // P2MR witness leaf-type allowlist and the CSFS/HTLC message bound
            // from relay policy to consensus, so a cooperating miner can no
            // longer smuggle inscription data through a non-financial witness
            // leaf. IsFinancialP2MRWitness shares the financial leaf parser
            // with relay policy but uses consensus-fixed bounds; it validates
            // only financial leaf shapes (CHECKSIG/MULTISIG/CTV/CLTV/CSV/CSFS/
            // HTLC), whitelisting large PQ signature/pubkey operands by
            // structure rather than size. It must never inherit mutable relay
            // policy such as -maxscriptsize.
            if (params.GetConsensus().IsContentEliminationActive(pindex->nHeight)) {
                std::string witness_reject;
                if (!IsFinancialP2MRWitness(tx, view, "bad-txns-witness-", witness_reject)) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                  "bad-txns-nonfinancial-witness",
                                  witness_reject + " in transaction " + tx.GetHash().ToString());
                    break;
                }
            }
            if (tx.HasShieldedBundle()) {
                const auto& bundle = tx.GetShieldedBundle();
                if (bundle.HasV2Bundle() &&
                    shielded::UseShieldedCanonicalFeeBuckets(params.GetConsensus(), pindex->nHeight)) {
                    const auto family = bundle.GetTransactionFamily();
                    if (family.has_value()) switch (*family) {
                    case shielded::v2::TransactionFamily::V2_SEND:
                        if (!shielded::IsCanonicalShieldedFee(txfee, params.GetConsensus(), pindex->nHeight)) {
                            state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                          "bad-shielded-v2-send-fee-bucket");
                        }
                        break;
                    case shielded::v2::TransactionFamily::V2_INGRESS_BATCH:
                        if (const auto* v2_bundle = bundle.GetV2Bundle();
                            v2_bundle != nullptr &&
                            !shielded::IsCanonicalShieldedFee(
                                std::get<shielded::v2::IngressBatchPayload>(v2_bundle->payload).fee,
                                params.GetConsensus(),
                                pindex->nHeight)) {
                            state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                          "bad-shielded-v2-ingress-fee-bucket");
                        }
                        break;
                    case shielded::v2::TransactionFamily::V2_REBALANCE:
                        if (txfee > 0 &&
                            !shielded::IsCanonicalShieldedFee(txfee, params.GetConsensus(), pindex->nHeight)) {
                            state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                          "bad-shielded-v2-rebalance-fee-bucket");
                        }
                        break;
                    default:
                        break;
                    }
                    if (!state.IsValid()) {
                        break;
                    }
                } else if (!bundle.HasV2Bundle() &&
                           bundle.IsShieldOnly() &&
                           txfee > 0 &&
                           shielded::UseShieldedCanonicalFeeBuckets(params.GetConsensus(), pindex->nHeight) &&
                           !shielded::IsCanonicalShieldedFee(txfee, params.GetConsensus(), pindex->nHeight)) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                  "bad-shielded-legacy-fee-bucket");
                    break;
                }
                if (bundle.HasV2Bundle() &&
                    bundle.GetTransactionFamily() == shielded::v2::TransactionFamily::V2_REBALANCE &&
                    UseZeroFeeRebalanceConsensusRule(params.GetConsensus(), pindex->nHeight) &&
                    txfee <= 0) {
                    state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-shielded-v2-rebalance-zero-fee");
                    break;
                }
            }

            // Check that transaction is BIP68 final
            // BIP68 lock checks (as opposed to nLockTime checks) must
            // be in ConnectBlock because they require the UTXO set
            prevheights.resize(tx.vin.size());
            for (size_t j = 0; j < tx.vin.size(); j++) {
                prevheights[j] = view.AccessCoin(tx.vin[j].prevout).nHeight;
            }

            if (!SequenceLocks(tx, nLockTimeFlags, prevheights, *pindex)) {
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-txns-nonfinal",
                              "contains a non-BIP68-final transaction " + tx.GetHash().ToString());
                break;
            }
        }

        // GetTransactionSigOpCost counts 3 types of sigops:
        // * legacy (always)
        // * p2sh (when P2SH enabled in flags and excludes coinbase)
        // * witness (when witness enabled in flags and excludes coinbase)
        nSigOpsCost += GetTransactionSigOpCost(tx, view, flags);
        if (nSigOpsCost > MAX_BLOCK_SIGOPS_COST) {
            state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-blk-sigops", "too many sigops");
            break;
        }

        if (!tx.IsCoinBase())
        {
            std::vector<CScriptCheck> vChecks;
            bool fCacheResults = fJustCheck; /* Don't cache results if we're actually connecting blocks (still consult the cache, though) */
            TxValidationState tx_state;
            if (fScriptChecks && !CheckInputScripts(tx, tx_state, view, flags, fCacheResults, fCacheResults, txsdata[i], m_chainman.m_validation_cache, parallel_script_checks ? &vChecks : nullptr)) {
                // Any transaction validation failure in ConnectBlock is a block consensus failure
                state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                              tx_state.GetRejectReason(), tx_state.GetDebugMessage());
                break;
            }
            control.Add(std::move(vChecks));
        }

        CTxUndo undoDummy;
        if (i > 0) {
            blockundo.vtxundo.emplace_back();
        }
        UpdateCoins(tx, view, i == 0 ? undoDummy : blockundo.vtxundo.back(), pindex->nHeight);
    }
    const auto time_3{SteadyClock::now()};
    m_chainman.time_connect += time_3 - time_2;
    LogDebug(BCLog::BENCH, "      - Connect %u transactions: %.2fms (%.3fms/tx, %.3fms/txin) [%.2fs (%.2fms/blk)]\n", (unsigned)block.vtx.size(),
             Ticks<MillisecondsDouble>(time_3 - time_2), Ticks<MillisecondsDouble>(time_3 - time_2) / block.vtx.size(),
             nInputs <= 1 ? 0 : Ticks<MillisecondsDouble>(time_3 - time_2) / (nInputs - 1),
             Ticks<SecondsDouble>(m_chainman.time_connect),
             Ticks<MillisecondsDouble>(m_chainman.time_connect) / m_chainman.num_blocks_total);

    CAmount blockReward = nFees + GetBlockSubsidyForBlock(pindex->nHeight, block, pindex->pprev, params.GetConsensus());
    if (block.vtx[0]->GetValueOut() > blockReward && state.IsValid()) {
        state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-cb-amount",
                      strprintf("coinbase pays too much (actual=%d vs limit=%d)", block.vtx[0]->GetValueOut(), blockReward));
    }

    auto parallel_result = control.Complete();
    if (parallel_result.has_value() && state.IsValid()) {
        state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, strprintf("mandatory-script-verify-flag-failed (%s)", ScriptErrorString(parallel_result->first)), parallel_result->second);
    }
    auto shielded_proof_result = shielded_proof_control.Complete();
    if (shielded_proof_result.has_value() && state.IsValid()) {
        bool repaired{false};
        if (!shielded_state_repair_attempted &&
            IsRecoverableShieldedProofReject(*shielded_proof_result)) {
            shielded_state_repair_attempted = true;
            if (TryAutoRebuildShieldedState(m_chainman,
                                            "ConnectBlock",
                                            pindex->GetBlockHash())) {
                RefreshShieldedValidationSnapshots(m_chainman,
                                                  shielded_tree_snapshot,
                                                  shielded_smile_public_accounts_snapshot,
                                                  shielded_account_leaf_commitments_snapshot);
                auto retry_result = RevalidateBlockShieldedProofsAfterRepair(
                    block,
                    params.GetConsensus(),
                    pindex->nHeight,
                    shielded_tree_snapshot,
                    shielded_smile_public_accounts_snapshot,
                    shielded_account_leaf_commitments_snapshot);
                if (!retry_result.has_value()) {
                    repaired = true;
                } else {
                    shielded_proof_result = std::move(retry_result);
                }
            }
        }
        if (!repaired) {
            state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, *shielded_proof_result);
        }
    }
    auto shielded_auth_result = shielded_auth_control.Complete();
    if (shielded_auth_result.has_value() && state.IsValid()) {
        state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, *shielded_auth_result);
    }
    if (!state.IsValid()) {
        LogInfo("Block validation error: %s", state.ToString());
        return false;
    }
    const auto time_4{SteadyClock::now()};
    m_chainman.time_verify += time_4 - time_2;
    LogDebug(BCLog::BENCH, "    - Verify %u txins: %.2fms (%.3fms/txin) [%.2fs (%.2fms/blk)]\n", nInputs - 1,
             Ticks<MillisecondsDouble>(time_4 - time_2),
             nInputs <= 1 ? 0 : Ticks<MillisecondsDouble>(time_4 - time_2) / (nInputs - 1),
             Ticks<SecondsDouble>(m_chainman.time_verify),
             Ticks<MillisecondsDouble>(m_chainman.time_verify) / m_chainman.num_blocks_total);

    if (enforce_shielded_consensus) {
        if (!fJustCheck) {
            shielded::registry::ShieldedAccountRegistryState projected_account_registry =
                m_chainman.m_shielded_account_registry;
            if (!block_account_leaves.empty()) {
                try {
                    if (!projected_account_registry.AppendPrepared(
                            Span<const shielded::registry::ShieldedAccountLeaf>{block_account_leaves.data(),
                                                                                block_account_leaves.size()})) {
                        return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                             "bad-blk-shielded-account-registry-append");
                    }
                } catch (const std::exception& e) {
                    return state.Error(strprintf("ConnectBlock(): shielded account-registry append failed in block %s: %s",
                                                 pindex->GetBlockHash().ToString(),
                                                 e.what()));
                }
                if (UseAccountRegistryEntryCountLimit(params.GetConsensus(), pindex->nHeight) &&
                    projected_account_registry.Size() >
                        params.GetConsensus().nMaxShieldedAccountRegistryEntries) {
                    return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                         "bad-blk-shielded-account-registry-size-limit");
                }
            }
            if (block_has_shielded_bundle || block_updates_shielded_velocity) {
                const auto prepared_marker = MakePreparedShieldedMutationMarker(
                    pindex->pprev,
                    pindex,
                    next_shielded_tree,
                    next_pool_balance,
                    projected_account_registry);
                if (!prepared_marker.has_value()) {
                    return state.Error("shielded-mutation-marker-write-failed");
                }
                // The payload store is append-only and its batch is atomic.
                // Publish it first: a crash here leaves an unreachable suffix,
                // while every durable PREPARED marker can subsequently rely
                // on all referenced payloads already existing. The v3 marker
                // also carries the suffix for validation/idempotent repair.
                if (m_chainman.InjectShieldedTransitionWriteFailureForTest(
                        ShieldedTransitionWriteSeam::ACCOUNT_PAYLOAD) ||
                    !projected_account_registry.CommitPreparedPayloads()) {
                    return state.Error("shielded-account-registry-payload-write-failed");
                }
                if (m_chainman.InjectShieldedTransitionWriteFailureForTest(
                        ShieldedTransitionWriteSeam::PREPARED_MARKER) ||
                    !WriteShieldedMutationMarker(m_chainman.m_shielded_nullifiers.get(), *prepared_marker)) {
                    return state.Error("shielded-mutation-marker-write-failed");
                }
            }
            if (!block_nullifier_vec.empty() &&
                (m_chainman.InjectShieldedTransitionWriteFailureForTest(
                     ShieldedTransitionWriteSeam::NULLIFIERS) ||
                 !m_chainman.m_shielded_nullifiers->Insert(block_nullifier_vec))) {
                return state.Error("shielded-nullifier-db-write-failed");
            }
            // V2_RECOVERY_EXIT: permanently retire the claimed commitments (reorg-safe; removed in
            // DisconnectBlock). The derived nullifiers were already retired via block_nullifier_vec above.
            if (!block_recovery_exit_commitments.empty() &&
                (m_chainman.InjectShieldedTransitionWriteFailureForTest(
                     ShieldedTransitionWriteSeam::RECOVERY_EXITS) ||
                 !m_chainman.m_shielded_nullifiers->InsertRecoveryExitCommitments(
                     block_recovery_exit_commitments))) {
                return state.Error("shielded-recovery-exit-db-write-failed");
            }
            if (!block_settlement_anchor_states.empty() &&
                UseSingleUseSettlementAnchors(params.GetConsensus(), pindex->nHeight)) {
                std::vector<ConfirmedSettlementAnchorState> unconsumed_settlement_anchors;
                unconsumed_settlement_anchors.reserve(block_settlement_anchor_states.size());
                for (const auto& anchor_state : block_settlement_anchor_states) {
                    if (block_consumed_settlement_anchors.count(anchor_state.anchor) == 0) {
                        unconsumed_settlement_anchors.push_back(anchor_state);
                    }
                }
                if (!unconsumed_settlement_anchors.empty() &&
                    (m_chainman.InjectShieldedTransitionWriteFailureForTest(
                         ShieldedTransitionWriteSeam::SETTLEMENT_ANCHORS) ||
                     !m_chainman.m_shielded_nullifiers->InsertSettlementAnchors(
                         unconsumed_settlement_anchors))) {
                    return state.Error("shielded-settlement-anchor-db-write-failed");
                }
            } else if (!block_settlement_anchor_states.empty() &&
                       (m_chainman.InjectShieldedTransitionWriteFailureForTest(
                            ShieldedTransitionWriteSeam::SETTLEMENT_ANCHORS) ||
                        !m_chainman.m_shielded_nullifiers->InsertSettlementAnchors(
                            block_settlement_anchor_states))) {
                return state.Error("shielded-settlement-anchor-db-write-failed");
            }
            if (!block_consumed_settlement_anchor_vec.empty() &&
                UseSingleUseSettlementAnchors(params.GetConsensus(), pindex->nHeight) &&
                (m_chainman.InjectShieldedTransitionWriteFailureForTest(
                     ShieldedTransitionWriteSeam::SETTLEMENT_ANCHORS) ||
                 !m_chainman.m_shielded_nullifiers->RemoveSettlementAnchors(
                     block_consumed_settlement_anchor_vec))) {
                return state.Error("shielded-settlement-anchor-db-write-failed");
            }
            std::vector<ConfirmedNettingManifestState> block_netting_manifest_states;
            block_netting_manifest_states.reserve(block_netting_manifest_vec.size());
            for (const auto& txref : block.vtx) {
                std::string reject_reason;
                auto created_manifests =
                    ExtractCreatedShieldedNettingManifestStates(*txref, pindex->nHeight, reject_reason);
                if (!created_manifests.has_value()) {
                    return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, reject_reason);
                }
                block_netting_manifest_states.insert(block_netting_manifest_states.end(),
                                                     created_manifests->begin(),
                                                     created_manifests->end());
            }
            if (!block_netting_manifest_states.empty() &&
                (m_chainman.InjectShieldedTransitionWriteFailureForTest(
                     ShieldedTransitionWriteSeam::NETTING_MANIFESTS) ||
                 !m_chainman.m_shielded_nullifiers->InsertNettingManifests(
                     block_netting_manifest_states))) {
                return state.Error("shielded-netting-manifest-db-write-failed");
            }

            // v0.32.0 unshield velocity cap (gated). The block's net z->t egress is the pool's net
            // decrease this block (value that left the pool); a trailing-window sum of net egress must
            // stay within cap_bps/10000 of the pool balance at block start. Computed, checked, and
            // persisted before the pool-balance commit so a violation rejects the block cleanly.
              const auto& vparams = params.GetConsensus();
              const bool velocity_active = block_updates_shielded_velocity;
            ShieldedUnshieldVelocity next_velocity;
            if (velocity_active) {
                next_velocity = m_chainman.m_shielded_unshield_velocity;
                const CAmount pool_start = m_chainman.m_shielded_pool_balance.GetBalance();
                const CAmount pool_end = next_pool_balance.GetBalance();
                const CAmount block_net_egress = pool_start > pool_end ? pool_start - pool_end : 0;
                next_velocity.RecordBlock(pindex->nHeight, block_net_egress);
                const CAmount min_cap =
                    vparams.ShieldedUnshieldVelocityMinCapForHeight(pindex->nHeight);
                if (!next_velocity.WithinCap(pindex->nHeight, pool_start,
                                             vparams.nShieldedUnshieldVelocityCapBps,
                                             vparams.nShieldedUnshieldVelocityWindowBlocks,
                                             min_cap)) {
                    return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                         "shielded-unshield-velocity-exceeded");
                }
                next_velocity.Prune(pindex->nHeight -
                    2 * static_cast<int32_t>(vparams.nShieldedUnshieldVelocityWindowBlocks));
                if (m_chainman.InjectShieldedTransitionWriteFailureForTest(
                        ShieldedTransitionWriteSeam::UNSHIELD_VELOCITY) ||
                    !m_chainman.m_shielded_nullifiers->WriteUnshieldVelocity(next_velocity)) {
                    return state.Error("shielded-velocity-db-write-failed");
                }
            }

            if (m_chainman.InjectShieldedTransitionWriteFailureForTest(
                    ShieldedTransitionWriteSeam::POOL_BALANCE) ||
                !m_chainman.m_shielded_nullifiers->WritePoolBalance(next_pool_balance.GetBalance())) {
                return state.Error("shielded-pool-db-write-failed");
            }

            m_chainman.m_shielded_merkle_tree = std::move(next_shielded_tree);
            m_chainman.m_shielded_account_registry = std::move(projected_account_registry);
            for (const auto& [commitment, account] : block_smile_public_accounts) {
                m_chainman.m_shielded_smile_public_accounts[commitment] = account;
            }
            for (const auto& [commitment, account_leaf_commitment] : block_account_leaf_entries) {
                m_chainman.m_shielded_account_leaf_commitments[commitment] = account_leaf_commitment;
            }
            if (!block_smile_public_accounts.empty() || !block_account_leaf_entries.empty()) {
                m_chainman.InvalidateShieldedAccountStateSnapshotCaches();
            }
            m_chainman.m_shielded_pool_balance = next_pool_balance;
            if (velocity_active) m_chainman.m_shielded_unshield_velocity = std::move(next_velocity);
        }
    }

    if (fJustCheck) {
        return true;
    }

    blockundo.consumed_settlement_anchor_states = std::move(block_consumed_settlement_anchor_states);
    if (!m_blockman.WriteBlockUndo(blockundo, state, *pindex)) {
        return false;
    }

    const auto time_5{SteadyClock::now()};
    m_chainman.time_undo += time_5 - time_4;
    LogDebug(BCLog::BENCH, "    - Write undo data: %.2fms [%.2fs (%.2fms/blk)]\n",
             Ticks<MillisecondsDouble>(time_5 - time_4),
             Ticks<SecondsDouble>(m_chainman.time_undo),
             Ticks<MillisecondsDouble>(m_chainman.time_undo) / m_chainman.num_blocks_total);

    if (!pindex->IsValid(BLOCK_VALID_SCRIPTS)) {
        pindex->RaiseValidity(BLOCK_VALID_SCRIPTS);
        m_blockman.m_dirty_blockindex.insert(pindex);
    }

    // add this block to the view's block chain
    view.SetBestBlock(pindex->GetBlockHash());

    if (apply_shielded_state && this == &m_chainman.ActiveChainstate() && m_chainman.m_shielded_state_initialized) {
        // PersistShieldedState() durably advances the auxiliary state and
        // clears its recovery marker before the coins view is normally synced.
        // Flush the block, undo, and their index references first so an
        // unclean stop can always reconcile that state from the older durable
        // coins tip. Without this ordering, the shielded tip can survive while
        // the corresponding CBlockIndex falls back to header-only.
        if (!m_blockman.FlushChainstateBlockFile(
                pindex->nHeight,
                /*snapshot_chainstate=*/m_from_snapshot_blockhash.has_value())) {
            return state.Error("shielded-state-block-dependency-flush-failed");
        }
        if (!m_blockman.WriteBlockIndexDB()) {
            return state.Error("shielded-state-block-index-flush-failed");
        }
        if (m_chainman.InjectShieldedTransitionWriteFailureForTest(
                ShieldedTransitionWriteSeam::BLOCK_DEPENDENCIES_DURABLE)) {
            return state.Error("shielded-state-block-dependency-test-failure");
        }
        m_chainman.RecordShieldedAnchorRoot(m_chainman.m_shielded_merkle_tree.Root());
        m_chainman.RecordShieldedAccountRegistryRoot(m_chainman.m_shielded_account_registry.Root());
        if (!m_chainman.PersistShieldedState(pindex)) {
            return state.Error("shielded-state-persist-failed");
        }
    }

    const auto time_6{SteadyClock::now()};
    m_chainman.time_index += time_6 - time_5;
    LogDebug(BCLog::BENCH, "    - Index writing: %.2fms [%.2fs (%.2fms/blk)]\n",
             Ticks<MillisecondsDouble>(time_6 - time_5),
             Ticks<SecondsDouble>(m_chainman.time_index),
             Ticks<MillisecondsDouble>(m_chainman.time_index) / m_chainman.num_blocks_total);

    TRACEPOINT(validation, block_connected,
        block_hash.data(),
        pindex->nHeight,
        block.vtx.size(),
        nInputs,
        nSigOpsCost,
        Ticks<std::chrono::nanoseconds>(time_5 - time_start)
    );

    return true;
}

CoinsCacheSizeState Chainstate::GetCoinsCacheSizeState()
{
    AssertLockHeld(::cs_main);
    return this->GetCoinsCacheSizeState(
        m_coinstip_cache_size_bytes,
        m_mempool ? m_mempool->m_opts.max_size_bytes : 0);
}

CoinsCacheSizeState Chainstate::GetCoinsCacheSizeState(
    size_t max_coins_cache_size_bytes,
    size_t max_mempool_size_bytes)
{
    AssertLockHeld(::cs_main);
    const int64_t nMempoolUsage = m_mempool ? m_mempool->DynamicMemoryUsage() : 0;
    int64_t cacheSize = CoinsTip().DynamicMemoryUsage();
    int64_t nTotalSpace =
        max_coins_cache_size_bytes + std::max<int64_t>(int64_t(max_mempool_size_bytes) - nMempoolUsage, 0);

    //! No need to periodic flush if at least this much space still available.
    static constexpr int64_t MAX_BLOCK_COINSDB_USAGE_BYTES = 10 * 1024 * 1024;  // 10MB
    int64_t large_threshold =
        std::max((9 * nTotalSpace) / 10, nTotalSpace - MAX_BLOCK_COINSDB_USAGE_BYTES);

    if (cacheSize > nTotalSpace) {
        LogPrintf("Cache size (%s) exceeds total space (%s)\n", cacheSize, nTotalSpace);
        return CoinsCacheSizeState::CRITICAL;
    } else if (cacheSize > large_threshold) {
        return CoinsCacheSizeState::LARGE;
    }
    return CoinsCacheSizeState::OK;
}


bool Chainstate::FlushStateToDisk(
    BlockValidationState &state,
    FlushStateMode mode,
    int nManualPruneHeight)
{
    LOCK(cs_main);
    assert(this->CanFlushToDisk());
    std::set<int> setFilesToPrune;
    bool full_flush_completed = false;

    [[maybe_unused]] const size_t coins_count{CoinsTip().GetCacheSize()};
    [[maybe_unused]] const size_t coins_mem_usage{CoinsTip().DynamicMemoryUsage()};

    try {
    {
        bool fFlushForPrune = false;

        CoinsCacheSizeState cache_state = GetCoinsCacheSizeState();
        LOCK(m_blockman.cs_LastBlockFile);
        if (m_blockman.IsPruneMode() && (m_blockman.m_check_for_pruning || nManualPruneHeight > 0) && m_chainman.m_blockman.m_blockfiles_indexed) {
            // make sure we don't prune above any of the prune locks bestblocks
            // pruning is height-based
            int last_prune{m_chain.Height()}; // last height we can prune

            if (nManualPruneHeight > 0) {
                LOG_TIME_MILLIS_WITH_CATEGORY("find files to prune (manual)", BCLog::BENCH);

                m_blockman.FindFilesToPruneManual(
                    setFilesToPrune,
                    std::min(last_prune, nManualPruneHeight),
                    *this, m_chainman);
            } else {
                LOG_TIME_MILLIS_WITH_CATEGORY("find files to prune", BCLog::BENCH);

                m_blockman.FindFilesToPrune(setFilesToPrune, last_prune, *this, m_chainman);
                m_blockman.m_check_for_pruning = false;
            }
            if (!setFilesToPrune.empty()) {
                fFlushForPrune = true;
                if (!m_blockman.m_have_pruned) {
                    m_blockman.m_block_tree_db->WriteFlag("prunedblockfiles", true);
                    m_blockman.m_have_pruned = true;
                }
            }
        }
        const auto nNow{NodeClock::now()};
        // The cache is large and we're within 10% and 10 MiB of the limit, but we have time now (not in the middle of a block processing).
        bool fCacheLarge = mode == FlushStateMode::PERIODIC && cache_state >= CoinsCacheSizeState::LARGE;
        bool fCacheCritical = false;
        if (mode == FlushStateMode::IF_NEEDED) {
            if (cache_state >= CoinsCacheSizeState::CRITICAL) {
                // The cache is over the limit, we have to write now.
                fCacheCritical = true;
            } else if (SystemNeedsMemoryReleased()) {
                fCacheCritical = true;
            }
        }
        // It's been a while since we wrote the block index and chain state to disk. Do this frequently, so we don't need to redownload or reindex after a crash.
        bool fPeriodicWrite = mode == FlushStateMode::PERIODIC && nNow >= m_next_write;
        // Combine all conditions that result in a write to disk.
        bool should_write = (mode == FlushStateMode::FORCE_SYNC) || (mode == FlushStateMode::FORCE_FLUSH) || fCacheLarge || fCacheCritical || fPeriodicWrite || fFlushForPrune;
        // Write blocks, block index and best chain related state to disk.
        if (should_write) {
            // Ensure we can write block index
            if (!CheckDiskSpace(m_blockman.m_opts.blocks_dir)) {
                return FatalError(m_chainman.GetNotifications(), state, _("Disk space is too low!"));
            }
            {
                LOG_TIME_MILLIS_WITH_CATEGORY("write block and undo data to disk", BCLog::BENCH);

                // First make sure all block and undo data is flushed to disk.
                if (!m_blockman.FlushChainstateBlockFile(
                        m_chain.Height(),
                        /*snapshot_chainstate=*/m_from_snapshot_blockhash.has_value())) {
                    // FlushChainstateBlockFile() already emitted the flush-error notification.
                    return state.Error("Failed to flush block or undo file");
                }
            }

            // Then update all block file information (which may refer to block and undo files).
            {
                LOG_TIME_MILLIS_WITH_CATEGORY("write block index to disk", BCLog::BENCH);

                if (!m_blockman.WriteBlockIndexDB()) {
                    return FatalError(m_chainman.GetNotifications(), state, _("Failed to write to block index database."));
                }
            }
            // Finally remove any pruned files
            if (fFlushForPrune) {
                LOG_TIME_MILLIS_WITH_CATEGORY("unlink pruned files", BCLog::BENCH);

                m_blockman.UnlinkPrunedFiles(setFilesToPrune);
            }

            if (!CoinsTip().GetBestBlock().IsNull()) {

            // Typical Coin structures on disk are around 48 bytes in size.
            // Pushing a new one to the database can cause it to be written
            // twice (once in the log, and once in the tables). This is already
            // an overestimation, as most will delete an existing entry or
            // overwrite one. Still, use a conservative safety factor of 2.
            if (!CheckDiskSpace(m_chainman.m_options.datadir, 48 * 2 * 2 * CoinsTip().GetDirtyCount())) {
                return FatalError(m_chainman.GetNotifications(), state, _("Disk space is too low!"));
            }
            // Flush the chainstate (which may refer to block index entries).
            const auto empty_cache{(mode == FlushStateMode::FORCE_FLUSH) || fCacheLarge || fCacheCritical};
            if (empty_cache ? !CoinsTip().Flush() : !CoinsTip().Sync()) {
                return FatalError(m_chainman.GetNotifications(), state, _("Failed to write to coin database."));
            }
            m_last_flushed_block = m_blockman.LookupBlockIndex(CoinsTip().GetBestBlock());
            full_flush_completed = true;
            TRACEPOINT(utxocache, flush,
                    int64_t{Ticks<std::chrono::microseconds>(NodeClock::now() - nNow)},
                   (uint32_t)mode,
                   (uint64_t)coins_count,
                   (uint64_t)coins_mem_usage,
                   (bool)fFlushForPrune);

            }
        }

        if (should_write || m_next_write == NodeClock::time_point::max()) {
            constexpr auto range{DATABASE_WRITE_INTERVAL_MAX - DATABASE_WRITE_INTERVAL_MIN};
            m_next_write = FastRandomContext().rand_uniform_delay(NodeClock::now() + DATABASE_WRITE_INTERVAL_MIN, range);
        }
    }
    if (full_flush_completed) {
        if (m_chainman.m_options.signals) {
            m_chainman.m_options.signals->ChainStateFlushed(this->GetRole(), GetLocator(m_last_flushed_block));
        }
        if (!m_chainman.m_interrupt && ShouldCompactChainstate(m_chainman.IsInitialBlockDownload())) {
            try {
                CoinsDB().CompactFull();
            } catch (const std::exception& e) {
                LogWarning("Failed to start chainstate compaction (%s)\n", e.what());
            }
        }
    }
    } catch (const std::runtime_error& e) {
        return FatalError(m_chainman.GetNotifications(), state, strprintf(_("System error while flushing: %s"), e.what()));
    }
    return true;
}

void Chainstate::ForceFlushStateToDisk(bool wipe_cache)
{
    BlockValidationState state;
    if (!this->FlushStateToDisk(state, wipe_cache ? FlushStateMode::FORCE_FLUSH : FlushStateMode::FORCE_SYNC)) {
        LogPrintf("%s: failed to flush state (%s)\n", __func__, state.ToString());
    }
}

void Chainstate::PruneAndFlush()
{
    BlockValidationState state;
    m_blockman.m_check_for_pruning = true;
    if (!this->FlushStateToDisk(state, FlushStateMode::NONE)) {
        LogPrintf("%s: failed to flush state (%s)\n", __func__, state.ToString());
    }
}

static void UpdateTipLog(
    const ChainstateManager& chainman,
    const CCoinsViewCache& coins_tip,
    const CBlockIndex* tip,
    const std::string& func_name,
    const std::string& prefix,
    const std::string& warning_messages) EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{

    AssertLockHeld(::cs_main);

    std::string frontier_suffix;
    const auto frontier{chainman.GetSignedFrontierStatus()};
    if (frontier.available) {
        frontier_suffix = strprintf(
            " signed_frontier=%d behind=%d on_active_chain=%s",
            frontier.height, frontier.blocks_behind,
            frontier.on_active_chain ? "true" : "false");
    }

    // Disable rate limiting in LogPrintLevel_ so this source location may log during IBD.
    LogPrintLevel_(BCLog::LogFlags::ALL, BCLog::Level::Info, /*should_ratelimit=*/false, "%s%s: new best=%s height=%d version=0x%08x log2_work=%f tx=%lu date='%s' progress=%f cache=%.1fMiB(%utxo)%s%s\n",
                   prefix, func_name,
                   tip->GetBlockHash().ToString(), tip->nHeight, tip->nVersion,
                   log(tip->nChainWork.getdouble()) / log(2.0), tip->m_chain_tx_count,
                   FormatISO8601DateTime(tip->GetBlockTime()),
                   chainman.GuessVerificationProgress(tip),
                   coins_tip.DynamicMemoryUsage() * (1.0 / (1 << 20)),
                   coins_tip.GetCacheSize(),
                   !warning_messages.empty() ? strprintf(" warning='%s'", warning_messages) : "",
                   frontier_suffix);
}

void Chainstate::UpdateTip(const CBlockIndex* pindexNew)
{
    AssertLockHeld(::cs_main);
    const auto& coins_tip = this->CoinsTip();

    // The remainder of the function isn't relevant if we are not acting on
    // the active chainstate, so return if need be.
    if (this != &m_chainman.ActiveChainstate()) {
        // Only log every so often so that we don't bury log messages at the tip.
        constexpr int BACKGROUND_LOG_INTERVAL = 2000;
        if (pindexNew->nHeight % BACKGROUND_LOG_INTERVAL == 0) {
            UpdateTipLog(m_chainman, coins_tip, pindexNew, __func__, "[background validation] ", "");
        }
        return;
    }

    // New best block
    if (m_mempool) {
        m_mempool->AddTransactionsUpdated(1);
    }

    std::vector<bilingual_str> warning_messages;
    if (!m_chainman.IsInitialBlockDownload()) {
        const CBlockIndex* pindex = pindexNew;
        for (int bit = 0; bit < VERSIONBITS_NUM_BITS; bit++) {
            WarningBitsConditionChecker checker(m_chainman, bit);
            ThresholdState state = checker.GetStateFor(pindex, m_chainman.GetConsensus(), m_chainman.m_warningcache.at(bit));
            if (state == ThresholdState::ACTIVE || state == ThresholdState::LOCKED_IN) {
                const bilingual_str warning = strprintf(_("WARNING: Unknown new rules activated (versionbit %i) - this software is not secure"), bit);
                m_chainman.GetNotifications().warningSet(kernel::Warning::UNKNOWN_NEW_RULES_ACTIVATED, warning);
                warning_messages.push_back(warning);
            }
        }

        // Check the version of the last 100 blocks to see if we need to upgrade:
        int unexpected_bit_count[VERSIONBITS_NUM_BITS], nonversionbit_count = 0;
        for (size_t i = 0; i < VERSIONBITS_NUM_BITS; ++i) unexpected_bit_count[i] = 0;
        // NOTE: The warning_threshold_hit* variables are static to ensure the warnings persist even after the condition changes, until the node is restarted
        static std::set<uint8_t> warning_threshold_hit_bits;
        static int32_t warning_threshold_hit_int{-1};
        for (int i = 0; i < 100 && pindex != nullptr; i++)
        {
            int32_t nExpectedVersion = m_chainman.m_versionbitscache.ComputeBlockVersion(pindex->pprev, m_chainman.GetConsensus());
            if (pindex->nVersion <= VERSIONBITS_LAST_OLD_BLOCK_VERSION) {
                // We don't care
            } else if ((pindex->nVersion & VERSIONBITS_TOP_MASK) != VERSIONBITS_TOP_BITS) {
                // Non-versionbits upgrade
                static constexpr int WARNING_THRESHOLD = 100/2;
                if (++nonversionbit_count > WARNING_THRESHOLD) {
                    if (warning_threshold_hit_int == -1) {
                        warning_threshold_hit_int = pindex->nVersion;
                    } else if (warning_threshold_hit_int != pindex->nVersion) {
                        warning_threshold_hit_int = -2;
                    }
                }
            } else if ((pindex->nVersion & ~nExpectedVersion) != 0) {
                for (int bit = 0; bit < VERSIONBITS_NUM_BITS; ++bit) {
                    const int32_t mask = 1 << bit;
                    if ((pindex->nVersion & mask) && !(nExpectedVersion & mask)) {
                        const int warning_threshold = (bit > 12 ? 75 : 50);
                        if (++unexpected_bit_count[bit] > warning_threshold) {
                            warning_threshold_hit_bits.insert(bit);
                        }
                    }
                }
            }
            pindex = pindex->pprev;
        }
        if (!warning_threshold_hit_bits.empty()) {
            const auto warning = strprintf(_("Warning: Miners are attempting to activate unknown new rules (bit %s)! You may or may not need to act to remain secure"), util::Join(warning_threshold_hit_bits, ", ", [](const uint8_t bit){ return util::ToString(int(bit)); }));
            m_chainman.GetNotifications().warningSet(kernel::Warning::UNKNOWN_NEW_RULES_SIGNAL_VBITS, warning, /*update=*/true);
            warning_messages.push_back(warning);
        }
        if (warning_threshold_hit_int != -1) {
            bilingual_str warning;
            if (warning_threshold_hit_int == -2) {
                warning = _("Warning: Unrecognised block versions are being mined! Unknown rules may or may not be in effect");
            } else {
                warning = strprintf(_("Warning: Unrecognised block version (0x%08x) is being mined! Unknown rules may or may not be in effect"), warning_threshold_hit_int);
            }
            m_chainman.GetNotifications().warningSet(kernel::Warning::UNKNOWN_NEW_RULES_SIGNAL_INTVER, warning, /*update=*/true);
            warning_messages.push_back(warning);
        }
    }

    static constexpr int32_t BIP320_MASK = 0x1fffe000UL;
    if ((pindexNew->nVersion & BIP320_MASK) && pindexNew->nVersion != m_chainman.m_versionbitscache.ComputeBlockVersion(pindexNew->pprev, m_chainman.GetConsensus())) {
        const auto warning = _("Miner violated version bit protocol");
        warning_messages.push_back(warning);
    }

    UpdateTipLog(m_chainman, coins_tip, pindexNew, __func__, "",
                 util::Join(warning_messages, Untranslated(", ")).original);
    m_chainman.NotifySignedFrontierStatus();
}

/** Disconnect m_chain's tip.
  * After calling, the mempool will be in an inconsistent state, with
  * transactions from disconnected blocks being added to disconnectpool.  You
  * should make the mempool consistent again by calling MaybeUpdateMempoolForReorg.
  * with cs_main held.
  *
  * If disconnectpool is nullptr, then no disconnected transactions are added to
  * disconnectpool (note that the caller is responsible for mempool consistency
  * in any case).
  */
bool Chainstate::DisconnectTip(BlockValidationState& state, DisconnectedBlockTransactions* disconnectpool)
{
    AssertLockHeld(cs_main);
    if (m_mempool) AssertLockHeld(m_mempool->cs);

    CBlockIndex *pindexDelete = m_chain.Tip();
    if (!pindexDelete) {
        LogError("DisconnectTip(): chain tip is null\n");
        return FatalError(m_chainman.GetNotifications(), state, _("DisconnectTip: chain tip is null."));
    }
    if (!pindexDelete->pprev) {
        LogError("DisconnectTip(): cannot disconnect genesis block\n");
        return FatalError(m_chainman.GetNotifications(), state, _("DisconnectTip: cannot disconnect genesis block."));
    }
    // Read block from disk.
    std::shared_ptr<CBlock> pblock = std::make_shared<CBlock>();
    CBlock& block = *pblock;
    if (!m_blockman.ReadBlock(block, *pindexDelete)) {
        LogError("DisconnectTip(): Failed to read block\n");
        return false;
    }
    // Apply the block atomically to the chain state.
    const auto time_start{SteadyClock::now()};
    {
        CCoinsViewCache view(&CoinsTip());
        if (view.GetBestBlock() != pindexDelete->GetBlockHash()) {
            LogError("DisconnectTip: view best block (%s) does not match block to disconnect (%s)\n",
                     view.GetBestBlock().GetHex(), pindexDelete->GetBlockHash().GetHex());
            return FatalError(m_chainman.GetNotifications(), state, _("DisconnectTip: view best block mismatch."));
        }
        if (DisconnectBlock(block, pindexDelete, view) != DISCONNECT_OK) {
            LogError("DisconnectTip(): DisconnectBlock %s failed\n", pindexDelete->GetBlockHash().ToString());
            return false;
        }
        bool flushed = view.Flush();
        if (!flushed) {
            return FatalError(m_chainman.GetNotifications(), state, _("DisconnectTip: failed to flush coins view to disk."));
        }
    }
    LogDebug(BCLog::BENCH, "- Disconnect block: %.2fms\n",
             Ticks<MillisecondsDouble>(SteadyClock::now() - time_start));

    {
        // Prune locks that began around the tip should be moved backward so they get a chance to reorg
        const uint64_t max_height_first{static_cast<uint64_t>(pindexDelete->nHeight - 1)};
        for (auto& prune_lock : m_blockman.m_prune_locks) {
            if (prune_lock.second.height_first < max_height_first) continue;

            --prune_lock.second.height_first;
            LogDebug(BCLog::PRUNE, "%s prune lock moved back to %d\n", prune_lock.first, prune_lock.second.height_first);
            // NOTE: Don't need to write to db here, since it will get synced with the rest of the chainstate
        }
    }

    // Write the chain state to disk, if necessary.
    if (!FlushStateToDisk(state, FlushStateMode::IF_NEEDED)) {
        return false;
    }

    if (disconnectpool && m_mempool) {
        for (auto it = block.vtx.rbegin(); it != block.vtx.rend(); ++it) {
            m_mempool->UpdateDependentPriorities(*(*it), pindexDelete->nHeight, false);
        }
        // Save transactions to re-add to mempool at end of reorg. If any entries are evicted for
        // exceeding memory limits, remove them and their descendants from the mempool.
        for (auto&& evicted_tx : disconnectpool->AddTransactionsFromBlock(block.vtx)) {
            m_mempool->removeRecursive(*evicted_tx, MemPoolRemovalReason::REORG);
        }
    }

    m_chain.SetTip(*pindexDelete->pprev);

    UpdateTip(pindexDelete->pprev);
    // Let wallets know transactions went from 1-confirmed to
    // 0-confirmed or conflicted:
    if (m_chainman.m_options.signals) {
        m_chainman.m_options.signals->BlockDisconnected(pblock, pindexDelete);
    }

    if (m_mempool) {
        // add mempool stats sample
        const CFeeRate min_fee_rate = std::max(m_mempool->GetMinFee(), m_mempool->m_opts.min_relay_feerate);
        CStats::DefaultStats()->addMempoolSample(m_mempool->size(), m_mempool->DynamicMemoryUsage(), min_fee_rate.GetFeePerK());
    }

    return true;
}

struct PerBlockConnectTrace {
    CBlockIndex* pindex = nullptr;
    std::shared_ptr<const CBlock> pblock;
    PerBlockConnectTrace() = default;
};
/**
 * Used to track blocks whose transactions were applied to the UTXO state as a
 * part of a single ActivateBestChainStep call.
 *
 * This class is single-use, once you call GetBlocksConnected() you have to throw
 * it away and make a new one.
 */
class ConnectTrace {
private:
    std::vector<PerBlockConnectTrace> blocksConnected;

public:
    explicit ConnectTrace() : blocksConnected(1) {}

    void BlockConnected(CBlockIndex* pindex, std::shared_ptr<const CBlock> pblock) {
        assert(!blocksConnected.back().pindex);
        assert(pindex);
        assert(pblock);
        blocksConnected.back().pindex = pindex;
        blocksConnected.back().pblock = std::move(pblock);
        blocksConnected.emplace_back();
    }

    std::vector<PerBlockConnectTrace>& GetBlocksConnected() {
        // We always keep one extra block at the end of our list because
        // blocks are added after all the conflicted transactions have
        // been filled in. Thus, the last entry should always be an empty
        // one waiting for the transactions from the next block. We pop
        // the last entry here to make sure the list we return is sane.
        assert(!blocksConnected.back().pindex);
        blocksConnected.pop_back();
        return blocksConnected;
    }
};

//! ContextualCheckBlock reports retryable local ExactReplay / trusted-quorum
//! misses as state.Error("matmul RC ExactReplay local execution incomplete: …")
//! rather than Invalid. Disk and other fatal Errors use different messages.
static constexpr const char* RETRYABLE_MATMUL_ACTIVATION_PREFIX =
    "matmul RC ExactReplay local execution incomplete";

[[nodiscard]] static bool TrustedMirrorMustDeferUnattestedConnect(
    const ChainstateManager& chainman, const CBlockIndex* pindex)
    EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    if (pindex == nullptr) return false;
    if (chainman.GetMatMulValidationMode() !=
        kernel::MatMulValidationMode::TRUSTED) {
        return false;
    }
    if (!node::matmul_trusted::IsConfigured()) return false;
    if (!chainman.GetConsensus().IsMatMulTrustedReplayAttestationActive(
            pindex->nHeight)) {
        return false;
    }
    return node::matmul_trusted::TrustedMirrorMustDeferUnattestedConnect(
        /*trusted_mirror_profile1=*/true,
        node::matmul_trusted::HasQuorum(
            pindex->GetBlockHash(), pindex->nHeight));
}

/**
 * Connect a new block to m_chain. pblock is either nullptr or a pointer to a CBlock
 * corresponding to pindexNew, to bypass loading it again from disk.
 *
 * The block is added to connectTrace if connection succeeds.
 */
bool Chainstate::ConnectTip(BlockValidationState& state, CBlockIndex* pindexNew, const std::shared_ptr<const CBlock>& pblock, ConnectTrace& connectTrace, DisconnectedBlockTransactions& disconnectpool)
{
    AssertLockHeld(cs_main);
    if (m_mempool) AssertLockHeld(m_mempool->cs);

    if (pindexNew->pprev != m_chain.Tip()) {
        LogError("ConnectTip: pindexNew->pprev (%s) != chain tip (%s)\n",
                 pindexNew->pprev ? pindexNew->pprev->GetBlockHash().GetHex() : "null",
                 m_chain.Tip() ? m_chain.Tip()->GetBlockHash().GetHex() : "null");
        return FatalError(m_chainman.GetNotifications(), state, _("ConnectTip: block's previous is not the current chain tip."));
    }
    // Trusted Profile-1 mirrors may persist tip-extending HAVE_DATA before
    // quorum (so a consensus signer can getdata the body). ConnectBlock does
    // not re-run ContextualCheckBlock, so this is the activation gate:
    // unattested MatMul must not become the active tip.
    if (TrustedMirrorMustDeferUnattestedConnect(m_chainman, pindexNew)) {
        LogDebug(BCLog::VALIDATION,
                 "ConnectTip: deferring unattested profile-1 block hash=%s height=%d until attestation quorum\n",
                 pindexNew->GetBlockHash().ToString(), pindexNew->nHeight);
        return state.Error(std::string(RETRYABLE_MATMUL_ACTIVATION_PREFIX) +
                           ": trusted attestation quorum Timeout");
    }
    // Genesis (height 0) must still connect so LoadChainTip can attach the
    // chain. After that, honor shutdown before ReadBlock / ConnectBlock so
    // ABC cannot ignore m_interrupt on the first reconnect of a reorg
    // (live: inner ABC loop held cs_main; version handshakes timed out).
    if (pindexNew->nHeight > 0 && m_chainman.m_interrupt) {
        return state.Error(std::string(RETRYABLE_MATMUL_ACTIVATION_PREFIX) +
                           ": shutdown interrupt");
    }
    // Read block from disk.
    const auto time_1{SteadyClock::now()};
    std::shared_ptr<const CBlock> pthisBlock;
    if (!pblock) {
        std::shared_ptr<CBlock> pblockNew = std::make_shared<CBlock>();
        if (!m_blockman.ReadBlock(*pblockNew, *pindexNew)) {
            return FatalError(m_chainman.GetNotifications(), state, _("Failed to read block."));
        }
        pthisBlock = pblockNew;
    } else {
        LogDebug(BCLog::BENCH, "  - Using cached block\n");
        pthisBlock = pblock;
    }
    const CBlock& blockConnecting = *pthisBlock;
    // Apply the block atomically to the chain state.
    const auto time_2{SteadyClock::now()};
    SteadyClock::time_point time_3;
    // When adding aggregate statistics in the future, keep in mind that
    // num_blocks_total may be zero until the ConnectBlock() call below.
    LogDebug(BCLog::BENCH, "  - Load block from disk: %.2fms\n",
             Ticks<MillisecondsDouble>(time_2 - time_1));
    {
        CoinsViewOverlay& view{*m_coins_views->m_connect_block_view};
        const auto reset_guard{view.StartFetching(blockConnecting)};
        bool rv = ConnectBlock(blockConnecting, state, pindexNew, view);
        if (m_chainman.m_options.signals) {
            m_chainman.m_options.signals->BlockChecked(blockConnecting, state);
        }
        if (!rv) {
            if (state.IsInvalid())
                InvalidBlockFound(pindexNew, state);
            else if (state.IsError() &&
                     (BlockHasShieldedBundle(blockConnecting) ||
                      m_chainman.GetConsensus().IsShieldedUnshieldVelocityCapActive(
                          pindexNew->nHeight))) {
                // ConnectBlock may have durably published only a prefix of
                // the auxiliary shielded transition after its PREPARED
                // journal was synced. Do not predicate shutdown on reading the
                // marker back: the same local I/O fault which interrupted the
                // transition can also make Exists()/Read() fail, and a failed
                // marker write has an indeterminate durability result. The
                // restart path will finalize a durable marker or rebuild from
                // the last fully persisted state. Keep the block valid and do
                // not retry against a possibly mixed live view.
                m_chainman.GetNotifications().fatalError(strprintf(_(
                    "A local storage error interrupted the shielded state transition for block %s. "
                    "The node is shutting down and will recover the prepared transition on restart."),
                    pindexNew->GetBlockHash().ToString()));
            }
            LogError("%s: ConnectBlock %s failed, %s\n", __func__, pindexNew->GetBlockHash().ToString(), state.ToString());
            return false;
        }
        time_3 = SteadyClock::now();
        m_chainman.time_connect_total += time_3 - time_2;
        if (m_chainman.num_blocks_total == 0) {
            LogError("ConnectTip: num_blocks_total is 0 after ConnectBlock succeeded\n");
            return FatalError(m_chainman.GetNotifications(), state, _("ConnectTip: block count invariant violated."));
        }
        LogDebug(BCLog::BENCH, "  - Connect total: %.2fms [%.2fs (%.2fms/blk)]\n",
                 Ticks<MillisecondsDouble>(time_3 - time_2),
                 Ticks<SecondsDouble>(m_chainman.time_connect_total),
                 Ticks<MillisecondsDouble>(m_chainman.time_connect_total) / m_chainman.num_blocks_total);
        bool flushed = view.Flush();
        if (!flushed) {
            return FatalError(m_chainman.GetNotifications(), state, _("ConnectTip: failed to flush coins view to disk."));
        }
    }
    const auto time_4{SteadyClock::now()};
    m_chainman.time_flush += time_4 - time_3;
    LogDebug(BCLog::BENCH, "  - Flush: %.2fms [%.2fs (%.2fms/blk)]\n",
             Ticks<MillisecondsDouble>(time_4 - time_3),
             Ticks<SecondsDouble>(m_chainman.time_flush),
             Ticks<MillisecondsDouble>(m_chainman.time_flush) / m_chainman.num_blocks_total);
    // Write the chain state to disk, if necessary.
    if (!FlushStateToDisk(state, FlushStateMode::IF_NEEDED)) {
        return false;
    }
    const auto time_5{SteadyClock::now()};
    m_chainman.time_chainstate += time_5 - time_4;
    LogDebug(BCLog::BENCH, "  - Writing chainstate: %.2fms [%.2fs (%.2fms/blk)]\n",
             Ticks<MillisecondsDouble>(time_5 - time_4),
             Ticks<SecondsDouble>(m_chainman.time_chainstate),
             Ticks<MillisecondsDouble>(m_chainman.time_chainstate) / m_chainman.num_blocks_total);
    // Remove conflicting transactions from the mempool.;
    if (m_mempool) {
        m_mempool->removeForBlock(blockConnecting.vtx, pindexNew->nHeight);
        disconnectpool.removeForBlock(blockConnecting.vtx);
    }
    // Update m_chain & related variables.
    m_chain.SetTip(*pindexNew);
    UpdateTip(pindexNew);
    // Keep the shielded prune-retention window pinned to the new tip (no-op unless pruning + shielded).
    UpdateShieldedPruneRetentionLock(*this);
    if (m_mempool) {
        RemoveShieldedMempoolConflictsForBlock(*m_mempool,
                                               m_chain,
                                               m_chainman,
                                               this,
                                               blockConnecting.vtx);
        if (BlockHasShieldedBundle(blockConnecting)) {
            RemoveStaleShieldedPoolBalanceMempoolTransactions(*m_mempool, m_chain, m_chainman);
        }
        const int32_t previous_next_block_height{pindexNew->nHeight};
        const int32_t current_next_block_height{NextBlockHeightOrLimit(pindexNew->nHeight)};
        if (CrossesShieldedMempoolHeightGate(m_chainman.GetConsensus(),
                                             previous_next_block_height,
                                             current_next_block_height)) {
            RemoveStaleShieldedAnchorMempoolTransactions(*m_mempool, m_chain, m_chainman, this);
        }
    }

    if (m_mempool) {
        // add mempool stats sample
        const CFeeRate min_fee_rate = std::max(m_mempool->GetMinFee(), m_mempool->m_opts.min_relay_feerate);
        CStats::DefaultStats()->addMempoolSample(m_mempool->size(), m_mempool->DynamicMemoryUsage(), min_fee_rate.GetFeePerK());
    }

    const auto time_6{SteadyClock::now()};
    m_chainman.time_post_connect += time_6 - time_5;
    m_chainman.time_total += time_6 - time_1;
    LogDebug(BCLog::BENCH, "  - Connect postprocess: %.2fms [%.2fs (%.2fms/blk)]\n",
             Ticks<MillisecondsDouble>(time_6 - time_5),
             Ticks<SecondsDouble>(m_chainman.time_post_connect),
             Ticks<MillisecondsDouble>(m_chainman.time_post_connect) / m_chainman.num_blocks_total);
    LogDebug(BCLog::BENCH, "- Connect block: %.2fms [%.2fs (%.2fms/blk)]\n",
             Ticks<MillisecondsDouble>(time_6 - time_1),
             Ticks<SecondsDouble>(m_chainman.time_total),
             Ticks<MillisecondsDouble>(m_chainman.time_total) / m_chainman.num_blocks_total);

    // If we are the background validation chainstate, check to see if we are done
    // validating the snapshot (i.e. our tip has reached the snapshot's base block).
    if (this != &m_chainman.ActiveChainstate()) {
        // This call may set `m_disabled`, which is referenced immediately afterwards in
        // ActivateBestChain, so that we stop connecting blocks past the snapshot base.
        m_chainman.MaybeCompleteSnapshotValidation();
    }

    connectTrace.BlockConnected(pindexNew, std::move(pthisBlock));
    return true;
}

/**
 * Return the tip of the chain with the most work in it, that isn't
 * known to be invalid (it's however far from certain to be valid).
 */
//! Nodes that track a configured attestation quorum must not let a heavier
//! competing fork occupy FindMostWorkChain. That includes trusted mirrors,
//! the local signer, and consensus miners with -matmultrustedpubkey (they
//! ExactReplay unattested tip-children and their own mining candidates; a
//! verified quorum is fork-choice and skips redundant GPU ExactReplay).
//! race may replace an unattested tip only.
static bool TrustedMirrorShouldConsiderMostWorkCandidate(
    const CBlockIndex* tip, const CBlockIndex* candidate)
    EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    if (!node::matmul_trusted::IsConfigured()) {
        return true;
    }
    if (tip == nullptr || candidate == nullptr || candidate == tip) return true;
    const bool extends_tip{
        candidate->nHeight >= tip->nHeight &&
        candidate->GetAncestor(tip->nHeight) == tip};
    const CBlockIndex* lca{LastCommonAncestor(tip, candidate)};
    const int lca_depth{lca != nullptr ? tip->nHeight - lca->nHeight : 0};
    return node::matmul_trusted::TrustedMirrorMaySelectMostWorkCandidate(
        extends_tip,
        node::matmul_trusted::TrustedMirrorIsShortTipReorg(lca_depth),
        node::matmul_trusted::HasQuorum(
            candidate->GetBlockHash(), candidate->nHeight),
        node::matmul_trusted::HasQuorum(tip->GetBlockHash(), tip->nHeight));
}

CBlockIndex* Chainstate::FindMostWorkChain()
{
    AssertLockHeld(::cs_main);
    std::vector<CBlockIndex*> hysteresis_deferred_candidates;
    std::set<CBlockIndex*> skipped_this_call;
    const auto restore_hysteresis_deferred_candidates = [&]() {
        for (CBlockIndex* candidate : hysteresis_deferred_candidates) {
            setBlockIndexCandidates.insert(candidate);
        }
        hysteresis_deferred_candidates.clear();
    };

    do {
        if (m_chainman.m_interrupt) {
            restore_hysteresis_deferred_candidates();
            return nullptr;
        }
        CBlockIndex *pindexNew = nullptr;

        // Find the best candidate header.
        {
            std::set<CBlockIndex*, CBlockIndexWorkComparator>::reverse_iterator it = setBlockIndexCandidates.rbegin();
            if (it == setBlockIndexCandidates.rend()) {
                restore_hysteresis_deferred_candidates();
                return nullptr;
            }
            pindexNew = *it;
        }

        if (CBlockIndex* abandon{const_cast<CBlockIndex*>(
                m_chainman.FindUniqueCompetingAttestedIndex())}) {
            // Unique competing attested HAVE_DATA: lost same-height race,
            // attested chain pulled ahead, heavier unattested fork,
            // dual-attested siblings with the signed frontier off this
            // chain (live 2026-08-15), or the unique attested HAVE_DATA
            // child of an already-attested tip (catch-up, LCA depth 0).
            // Do not wait for invalidateblock. A pending-attestation
            // child of an attested ancestor is not competing and is left
            // alone. Insert so ConnectTip / CheckBlockIndex see the new
            // tip in the candidate set.
            //
            // Do not re-insert a candidate this call already skipped.
            // Erase+continue then FindUniqueCompetingAttestedIndex would
            // otherwise livelock FindMostWorkChain with cs_main held
            // (PR 105 comment 5301483741: b-initload 100% CPU, 0 peers).
            if (skipped_this_call.count(abandon) == 0) {
                pindexNew = abandon;
                setBlockIndexCandidates.insert(abandon);
            }
        }

        if (m_chainman.IsOnParkedReorgBranch(pindexNew)) {
            // Mirror park-escape: if we parked our own uniquely attested
            // authority branch (self-inflicted tip-extension into the park
            // wall), unpark it. A heavier unattested rewrite (the 137-deep
            // competing miner branch) has no quorum and stays parked.
            // Abandon-fork recovery also unparks: the attested chain may
            // carry less work than the unattested tip we are leaving.
            const bool authority_escape{
                (node::matmul_trusted::IsTrustedMirror() &&
                 node::matmul_trusted::HasQuorum(
                     pindexNew->GetBlockHash(), pindexNew->nHeight) &&
                 m_chain.Tip() != nullptr &&
                 pindexNew->nChainWork >= m_chain.Tip()->nChainWork) ||
                m_chainman.IsAttestedAbandonForkCandidate(pindexNew)};
            if (!authority_escape ||
                !m_chainman.UnparkReorgBranchContainingBlock(pindexNew)) {
                skipped_this_call.insert(pindexNew);
                setBlockIndexCandidates.erase(pindexNew);
                continue;
            }
            LogWarning("%s: auto-unparked uniquely attested authority branch hash=%s height=%d (park-escape; unattested heavier rewrites stay parked)\n",
                       __func__, pindexNew->GetBlockHash().ToString(),
                       pindexNew->nHeight);
        }
        if (m_chainman.ShouldDeferLosingTipExtension(pindexNew)) {
            LogWarning("%s: deferring losing-tip extension hash=%s height=%d while authenticated reorg recovery is armed\n",
                       __func__, pindexNew->GetBlockHash().ToString(),
                       pindexNew->nHeight);
            skipped_this_call.insert(pindexNew);
            setBlockIndexCandidates.erase(pindexNew);
            continue;
        }
        if (!TrustedMirrorShouldConsiderMostWorkCandidate(m_chain.Tip(), pindexNew) &&
            !m_chainman.IsAttestedAbandonForkCandidate(pindexNew)) {
            LogDebug(BCLog::VALIDATION,
                     "FindMostWorkChain: skipping unattested competing candidate hash=%s height=%d\n",
                     pindexNew->GetBlockHash().ToString(), pindexNew->nHeight);
            skipped_this_call.insert(pindexNew);
            setBlockIndexCandidates.erase(pindexNew);
            continue;
        }

        // Configured Profile-1 nodes: do not let an unattested descendant
        // win most-work over a distinct attested tip-child. On a trusted
        // mirror ConnectTip would defer the unattested one and ABC would
        // stop, freezing the advertised height at the parent. Headers-only
        // attested siblings still count: they are often absent from
        // setBlockIndexCandidates (HAVE_DATA / HaveNumChainTxs gated).
        // Qualifier 3ed2619c: the active tip is always in the candidate
        // set and usually has quorum. It is not a sibling. A sole linear
        // child (getchaintips: height 11 active + height 12 valid-headers,
        // same parent, no competing branch) must stay selectable so
        // ConnectTip can wait for quorum instead of deferring forever.
        //
        // CONSENSUS (live 2026-08-15 2fd67f18): a followed HAVE_DATA
        // tip-child must remain selectable even when an unfollowed
        // attested sibling exists. TRUSTED mirrors must not use that
        // bypass: the first HAVE_DATA twin becomes m_best_header, so it
        // looks "followed", and skipping the overlay proposes the
        // unattested twin (bfd10a67 / 5e6df697 regression vs 34aeb78b).
        // ABC still refused to connect it; FindMostWorkChain must not
        // propose it (GBT and other callers).
        if (node::matmul_trusted::IsConfigured() &&
            m_chain.Tip() != nullptr &&
            m_chainman.GetConsensus().IsMatMulTrustedReplayAttestationActive(
                pindexNew->nHeight)) {
            const CBlockIndex* const tip{m_chain.Tip()};
            const bool extends_tip{
                pindexNew->nHeight >= tip->nHeight &&
                pindexNew->GetAncestor(tip->nHeight) == tip};
            const bool has_quorum{node::matmul_trusted::HasQuorum(
                pindexNew->GetBlockHash(), pindexNew->nHeight)};
            const CBlockIndex* attested_sibling{nullptr};
            const bool consensus_followed_tip_child{
                !node::matmul_trusted::IsTrustedMirror() &&
                m_chainman.IndexIsFollowedTipChild(tip, pindexNew)};
            if (extends_tip && !has_quorum && !consensus_followed_tip_child) {
                auto consider_attested_tip_child = [&](const CBlockIndex* alt) {
                    if (alt == nullptr) return false;
                    if (node::matmul_trusted::TrustedMirrorAttestedSiblingIsActionable(
                        /*distinct_from_candidate=*/alt != pindexNew && alt != tip,
                        /*same_parent=*/alt->pprev == tip,
                        /*same_height_as_tip_child=*/alt->nHeight == tip->nHeight + 1,
                        /*has_quorum=*/node::matmul_trusted::HasQuorum(
                            alt->GetBlockHash(), alt->nHeight),
                        /*failed=*/(alt->nStatus & BLOCK_FAILED_MASK) != 0)) {
                        return true;
                    }
                    // Dual-attested same-height siblings (live 2026-08-15):
                    // unattested children of the quorum loser must not win
                    // most-work over the attested twin of the tip.
                    if (tip->pprev == nullptr) return false;
                    return node::matmul_trusted::TrustedMirrorAttestedSiblingIsActionable(
                        /*distinct_from_candidate=*/alt != pindexNew && alt != tip,
                        /*same_parent=*/alt->pprev == tip->pprev,
                        /*same_height_as_tip_child=*/alt->nHeight == tip->nHeight,
                        /*has_quorum=*/node::matmul_trusted::HasQuorum(
                            alt->GetBlockHash(), alt->nHeight),
                        /*failed=*/(alt->nStatus & BLOCK_FAILED_MASK) != 0);
                };
                for (CBlockIndex* alt : setBlockIndexCandidates) {
                    if (consider_attested_tip_child(alt)) {
                        attested_sibling = alt;
                        break;
                    }
                }
                if (attested_sibling == nullptr) {
                    for (const auto& hint :
                         node::matmul_trusted::AttestedFrontierHints()) {
                        const CBlockIndex* const alt{
                            m_blockman.LookupBlockIndex(hint.hash)};
                        if (consider_attested_tip_child(alt)) {
                            attested_sibling = alt;
                            break;
                        }
                    }
                }
                if (attested_sibling == nullptr) {
                    for (CBlockIndex* alt :
                         m_chainman.m_reorg_authenticated_candidate_tips) {
                        if (consider_attested_tip_child(alt)) {
                            attested_sibling = alt;
                            break;
                        }
                    }
                }
            }
            if (node::matmul_trusted::TrustedMirrorDeferUnattestedMostWorkForAttestedSibling(
                    /*configured_profile1=*/true,
                    extends_tip,
                    has_quorum,
                    attested_sibling != nullptr)) {
                LogDebug(BCLog::VALIDATION,
                         "FindMostWorkChain: deferring unattested tip-child hash=%s height=%d; distinct attested tip-child hash=%s height=%d\n",
                         pindexNew->GetBlockHash().ToString(),
                         pindexNew->nHeight,
                         attested_sibling->GetBlockHash().ToString(),
                         attested_sibling->nHeight);
                skipped_this_call.insert(pindexNew);
                setBlockIndexCandidates.erase(pindexNew);
                hysteresis_deferred_candidates.push_back(pindexNew);
                continue;
            }
        }

        // Check whether all blocks on the path between the currently active chain and the candidate are valid.
        // Just going until the active chain is an optimization, as we know all blocks in it are valid already.
        CBlockIndex *pindexTest = pindexNew;
        bool fInvalidAncestor = false;
        while (pindexTest && !m_chain.Contains(pindexTest)) {
            assert(pindexTest->HaveNumChainTxs() || pindexTest->nHeight == 0);

            // Pruned nodes may have entries in setBlockIndexCandidates for
            // which block files have been deleted.  Remove those as candidates
            // for the most work chain if we come across them; we can't switch
            // to a chain unless we have all the non-active-chain parent blocks.
            bool fFailedChain = pindexTest->nStatus & BLOCK_FAILED_MASK;
            bool fMissingData = !(pindexTest->nStatus & BLOCK_HAVE_DATA);
            if (fFailedChain || fMissingData) {
                // Candidate chain is not usable (either invalid or missing data)
                if (fFailedChain && (m_chainman.m_best_invalid == nullptr || pindexNew->nChainWork > m_chainman.m_best_invalid->nChainWork)) {
                    m_chainman.m_best_invalid = pindexNew;
                }
                CBlockIndex *pindexFailed = pindexNew;
                // Remove the entire chain from the set.
                while (pindexTest != pindexFailed) {
                    if (fFailedChain) {
                        pindexFailed->nStatus |= BLOCK_FAILED_CHILD;
                        m_blockman.m_dirty_blockindex.insert(pindexFailed);
                    } else if (fMissingData) {
                        // If we're missing data, then add back to m_blocks_unlinked,
                        // so that if the block arrives in the future we can try adding
                        // to setBlockIndexCandidates again.
                        m_blockman.AddUnlinkedBlock(pindexFailed);
                    }
                    setBlockIndexCandidates.erase(pindexFailed);
                    pindexFailed = pindexFailed->pprev;
                }
                setBlockIndexCandidates.erase(pindexTest);
                fInvalidAncestor = true;
                break;
            }
            pindexTest = pindexTest->pprev;
        }
        if (!fInvalidAncestor) {
            const CBlockIndex* old_tip = m_chain.Tip();
            const CBlockIndex* fork = old_tip != nullptr ? m_chain.FindFork(pindexNew) : nullptr;
            const auto& consensus_params = m_chainman.GetConsensus();
            const auto& cm_opts = m_chainman.m_options;
            const auto profile_settings = kernel::GetReorgProtectionProfileSettings(cm_opts.reorg_protection_profile);
            const uint32_t park_depth = cm_opts.max_reorg_depth_park.value_or(profile_settings.park_depth);
            const uint32_t hysteresis_depth =
                cm_opts.reorg_hysteresis_depth.value_or(profile_settings.hysteresis_depth);
            const uint32_t hysteresis_work_margin =
                cm_opts.reorg_hysteresis_work_margin.value_or(profile_settings.hysteresis_work_margin);
            // Once the durable recovery coordinator has identified a unique,
            // authenticated shallow-race winner, that decision must bypass both
            // local finality mechanisms.  ActivateBestChainStep already lets the
            // same candidate escape PARK; applying hysteresis here first would
            // make the escape unreachable when the branch is only one-work-unit
            // ahead (the normal already-have-data recovery case).
            const bool recovery_escape{
                m_chainman.IsAutomaticReorgRecoveryCandidate(pindexNew) ||
                m_chainman.IsAttestedAbandonForkCandidate(pindexNew)};
            if (old_tip != nullptr &&
                fork != nullptr &&
                fork != old_tip &&
                old_tip->nHeight >= consensus_params.nReorgProtectionStartHeight &&
                hysteresis_depth != kernel::REORG_PROTECTION_DEPTH_DISABLED &&
                hysteresis_work_margin > 0 &&
                !recovery_escape) {
                const int reorg_depth = old_tip->nHeight - fork->nHeight;
                const bool candidate_would_park =
                    cm_opts.deep_reorg_action == kernel::DeepReorgAction::PARK &&
                    park_depth != kernel::REORG_PROTECTION_DEPTH_DISABLED &&
                    reorg_depth > static_cast<int>(park_depth);
                const arith_uint256 tip_work = GetBlockProof(*old_tip);
                if (!candidate_would_park &&
                    reorg_depth > static_cast<int>(hysteresis_depth) &&
                    tip_work > 0) {
                    const arith_uint256 required_work =
                        old_tip->nChainWork + tip_work * hysteresis_work_margin;
                    if (pindexNew->nChainWork < required_work) {
                        RecordDeferredReorgDepth(
                            static_cast<uint32_t>(reorg_depth),
                            hysteresis_work_margin,
                            old_tip->nHeight,
                            fork->nHeight,
                            pindexNew->nHeight);
                        LogWarning("Shallow reorg hysteresis deferred branch hash=%s depth=%d "
                                   "candidate_height=%d tip_height=%d required_work_margin=%u\n",
                                   pindexNew->GetBlockHash().ToString(),
                                   reorg_depth,
                                   pindexNew->nHeight,
                                   old_tip->nHeight,
                                   hysteresis_work_margin);
                        setBlockIndexCandidates.erase(pindexNew);
                        hysteresis_deferred_candidates.push_back(pindexNew);
                        continue;
                    }
                }
            }
            restore_hysteresis_deferred_candidates();
            return pindexNew;
        }
    } while(true);
}

void Chainstate::EraseParkedBlockIndexCandidates()
{
    AssertLockHeld(::cs_main);
    for (auto it = setBlockIndexCandidates.begin(); it != setBlockIndexCandidates.end();) {
        if (m_chainman.IsOnParkedReorgBranch(*it)) {
            it = setBlockIndexCandidates.erase(it);
        } else {
            ++it;
        }
    }
}

/** Delete all entries in setBlockIndexCandidates that are worse than the current tip. */
void Chainstate::PruneBlockIndexCandidates() {
    // Note that we can't delete the current block itself, as we may need to return to it later in case a
    // reorganization to a better block fails.
    std::set<CBlockIndex*, CBlockIndexWorkComparator>::iterator it = setBlockIndexCandidates.begin();
    while (it != setBlockIndexCandidates.end() && setBlockIndexCandidates.value_comp()(*it, m_chain.Tip())) {
        setBlockIndexCandidates.erase(it++);
    }
    // Either the current tip or a successor of it we're working towards is left in setBlockIndexCandidates.
    assert(!setBlockIndexCandidates.empty());
}

[[nodiscard]] static bool IsRetryableMatMulActivationError(const BlockValidationState& state)
{
    return state.IsError() &&
           state.GetRejectReason().find(RETRYABLE_MATMUL_ACTIVATION_PREFIX) != std::string::npos;
}

static std::atomic<bool> g_inject_retryable_matmul_connect_failure{false};
static std::atomic<int> g_retryable_matmul_connect_failure_attempts{0};
//! Auto-disable the test hook after this many ConnectTip injections so a
//! missed ABC-loop break cannot hang unit tests.
static constexpr int MAX_RETRYABLE_MATMUL_CONNECT_FAILURE_INJECTIONS{32};

void ChainstateManager::SetRetryableMatMulConnectFailureForTest(bool enable)
{
    g_inject_retryable_matmul_connect_failure.store(enable);
    if (enable) {
        g_retryable_matmul_connect_failure_attempts.store(0);
    }
}

int ChainstateManager::RetryableMatMulConnectFailureAttemptsForTest() const
{
    return g_retryable_matmul_connect_failure_attempts.load();
}

[[nodiscard]] static bool ConsumeRetryableMatMulConnectFailureForTest(BlockValidationState& state)
{
    if (!g_inject_retryable_matmul_connect_failure.load()) return false;
    const int attempts =
        g_retryable_matmul_connect_failure_attempts.fetch_add(1) + 1;
    if (attempts >= MAX_RETRYABLE_MATMUL_CONNECT_FAILURE_INJECTIONS) {
        g_inject_retryable_matmul_connect_failure.store(false);
    }
    state.Error(std::string(RETRYABLE_MATMUL_ACTIVATION_PREFIX) +
                ": ExactReplay: cancelled");
    return true;
}

/**
 * Try to make some progress towards making pindexMostWork the active block.
 * pblock is either nullptr or a pointer to a CBlock corresponding to pindexMostWork.
 *
 * @returns true unless a system error occurred. Retryable MatMul ExactReplay
 *          incomplete / cancelled / quorum-timeout Errors return true with
 *          state.IsError() set so ActivateBestChain can stop without
 *          busy-looping the same pindexMostWork or treating the miss as fatal.
 */
bool Chainstate::ActivateBestChainStep(BlockValidationState& state, CBlockIndex* pindexMostWork, const std::shared_ptr<const CBlock>& pblock, bool& fInvalidFound, ConnectTrace& connectTrace)
{
    AssertLockHeld(cs_main);
    if (m_mempool) AssertLockHeld(m_mempool->cs);

    const CBlockIndex* pindexOldTip = m_chain.Tip();
    const CBlockIndex* pindexFork = m_chain.FindFork(pindexMostWork);
    const Consensus::Params& consensus_params = m_chainman.GetConsensus();

    // ---------------------------------------------------------------------
    // Deep-reorg defense (issue: "no reorg-depth limit"). PER-NODE fork-choice
    // policy only; parking does not make the candidate block intrinsically
    // invalid for nodes configured with a different finality profile.
    //
    // SAFETY DESIGN. Bitcoin/Nakamoto consensus deliberately has NO maximum
    // reorg depth: the single rule "follow the most-work valid chain" is what
    // lets every honest node, no matter how it was partitioned, deterministically
    // reconverge on one chain. A naive hard cap that *refuses* reorgs deeper than
    // N turns N blocks into a finality boundary and is, in the partition case,
    // WORSE than the 51% attack it tries to stop:
    //
    //   SPLIT-RISK MEMO (why an unconditional reorg cap is dangerous)
    //   - Suppose the network partitions (or an attacker withholds and then
    //     releases a branch) so two sub-networks each extend their own tip past
    //     the common fork by more than N blocks.
    //   - When connectivity returns, neither side will adopt the other's chain:
    //     each sees the other as a ">N reorg" and refuses it. The two chains can
    //     NEVER reconverge -- a PERMANENT partition -- even though one is the
    //     legitimate most-work chain. The cap manufactures the very split it was
    //     meant to prevent, and an attacker can trigger it deliberately.
    //   - It also does not stop a sustained >50% attacker: such an attacker can
    //     keep a private chain within N of the public tip and still rewrite up to
    //     N blocks at will, or simply out-pace honest nodes that have parked.
    //   A hard cap is therefore only safe when paired with a real finality gadget
    //   (e.g. a checkpoint/finalization protocol every side agrees on). BTX has no
    //   such gadget, so we DO NOT impose a hard cap as default behavior.
    //
    // What we do instead, as default-on local fork-choice policy:
    //   - Warn early when a branch would rewrite more than the profile warning
    //     depth. This gives operators and mining infrastructure fast visibility.
    //   - Apply shallow hysteresis: late branches past the profile hysteresis
    //     depth must carry extra work before this node auto-switches to them.
    //   - Keep following most-work automatically once the branch satisfies the
    //     extra-work rule, avoiding an unattended-miner/manual-review stall.
    //   - Allow explicit operator parking with -parkdeepreorg=1 and
    //     -maxreorgdepthpark=N for deployments that intentionally prefer a
    //     local halt over automatic convergence.
    //
    // Profiles are explicit because one overloaded "max reorg" value is too
    // crude for a fragmented network: mining needs early warning, wallets and
    // services need settlement confirmation depth, and archive nodes need
    // convergence knobs.
    // The guard is armed only once the tip has reached
    // nReorgProtectionStartHeight so early/regtest chains are unaffected.
    // ---------------------------------------------------------------------
    const auto& cm_opts = m_chainman.m_options;
    const auto profile_settings = kernel::GetReorgProtectionProfileSettings(cm_opts.reorg_protection_profile);
    const uint32_t warn_depth = cm_opts.max_reorg_depth_warn.value_or(profile_settings.warn_depth);
    const uint32_t park_depth = cm_opts.max_reorg_depth_park.value_or(profile_settings.park_depth);

    if (pindexOldTip != nullptr &&
        pindexFork != nullptr &&
        pindexOldTip->nHeight >= consensus_params.nReorgProtectionStartHeight) {
        const int reorg_depth = pindexOldTip->nHeight - pindexFork->nHeight;
        const bool warn =
            warn_depth != kernel::REORG_PROTECTION_DEPTH_DISABLED &&
            reorg_depth > static_cast<int>(warn_depth);
        const bool recovery_escape{
            m_chainman.IsAutomaticReorgRecoveryCandidate(pindexMostWork) ||
            m_chainman.IsAttestedAbandonForkCandidate(pindexMostWork)};
        const bool park =
            cm_opts.deep_reorg_action == kernel::DeepReorgAction::PARK &&
            park_depth != kernel::REORG_PROTECTION_DEPTH_DISABLED &&
            reorg_depth > static_cast<int>(park_depth) &&
            !recovery_escape;

        if (warn || park) {
            RecordRejectedReorgDepth(
                static_cast<uint32_t>(reorg_depth),
                park ? park_depth : warn_depth,
                pindexOldTip->nHeight,
                pindexFork->nHeight,
                pindexMostWork->nHeight);

            const bilingual_str alarm = strprintf(
                _("Deep reorg detected: a branch would reorganize %d blocks "
                  "(profile=%s; warn=%u; park=%u; tip=%d, fork=%d, candidate=%d). %s This may indicate "
                  "a 51%% attack -- raise required confirmations and investigate."),
                reorg_depth,
                kernel::ReorgProtectionProfileName(cm_opts.reorg_protection_profile),
                warn_depth != kernel::REORG_PROTECTION_DEPTH_DISABLED ? warn_depth : 0,
                park_depth != kernel::REORG_PROTECTION_DEPTH_DISABLED ? park_depth : 0,
                pindexOldTip->nHeight, pindexFork->nHeight, pindexMostWork->nHeight,
                park ? _("Parking the branch and staying on the current chain pending operator action.")
                     : recovery_escape
                         ? _("Activating the uniquely authenticated shallow-race recovery branch.")
                         : _("Following the most-work chain (warn-only)."));

            // Loud alarm on every deep reorg, regardless of action.
            LogWarning("%s\n", alarm.original);
            m_chainman.GetNotifications().warningSet(kernel::Warning::DEEP_REORG_DETECTED, alarm);

            if (park) {
                CBlockIndex* branch_root = pindexMostWork;
                while (branch_root != nullptr && branch_root->pprev != pindexFork) {
                    branch_root = branch_root->pprev;
                }
                if (branch_root != nullptr) {
                    if (!m_chainman.ParkReorgBranch(branch_root)) {
                        return state.Error("failed to persist parked deep-reorg branch");
                    }
                }
                // Opt-in local finality (see split-risk memo above). Deep reorgs
                // are a local policy refusal, NOT a consensus invalidity. Remove
                // parked branches from best-chain selection so shallower extensions
                // of the current tip can still make progress.
                EraseParkedBlockIndexCandidates();
                if (m_chain.Tip() != nullptr) {
                    setBlockIndexCandidates.insert(m_chain.Tip());
                }
                fInvalidFound = true;
                return true;
            }
        }
        if (reorg_depth > 0) {
            RecordObservedReorgDepth(
                static_cast<uint32_t>(reorg_depth),
                pindexOldTip->nHeight,
                pindexFork->nHeight,
                pindexMostWork->nHeight);
        }
    }

    // Disconnect active blocks which are no longer in the best chain.
    bool fBlocksDisconnected = false;
    DisconnectedBlockTransactions disconnectpool{MAX_DISCONNECTED_TX_POOL_BYTES};
    while (m_chain.Tip() && m_chain.Tip() != pindexFork) {
        if (!DisconnectTip(state, &disconnectpool)) {
            // This is likely a fatal error, but keep the mempool consistent,
            // just in case. Only remove from the mempool in this case.
            MaybeUpdateMempoolForReorg(disconnectpool, false);

            // If we're unable to disconnect a block during normal operation,
            // then that is a failure of our local system -- we should abort
            // rather than stay on a less work chain.
            FatalError(m_chainman.GetNotifications(), state, _("Failed to disconnect block."));
            return false;
        }
        fBlocksDisconnected = true;
    }

    // Build list of new blocks to connect (in descending height order).
    std::vector<CBlockIndex*> vpindexToConnect;
    bool fContinue = true;
    int nHeight = pindexFork ? pindexFork->nHeight : -1;
    while (fContinue && nHeight != pindexMostWork->nHeight) {
        // Don't iterate the entire list of potential improvements toward the best tip, as we likely only need
        // a few blocks along the way.
        int nTargetHeight = std::min(nHeight + 32, pindexMostWork->nHeight);
        vpindexToConnect.clear();
        vpindexToConnect.reserve(nTargetHeight - nHeight);
        CBlockIndex* pindexIter = pindexMostWork->GetAncestor(nTargetHeight);
        while (pindexIter && pindexIter->nHeight != nHeight) {
            vpindexToConnect.push_back(pindexIter);
            pindexIter = pindexIter->pprev;
        }
        nHeight = nTargetHeight;

        // Connect new blocks.
        bool connected_this_step = false;
        for (CBlockIndex* pindexConnect : vpindexToConnect | std::views::reverse) {
            // After the first ConnectTip of this step, honor shutdown before
            // starting another ExactReplay/connect so a long reorg cannot
            // ignore m_interrupt. The first connect of the first ABC step
            // still runs so LoadChainTip can attach genesis.
            if (connected_this_step && m_chainman.m_interrupt) {
                fContinue = false;
                break;
            }
            const bool injected_retryable{
                ConsumeRetryableMatMulConnectFailureForTest(state)};
            if (injected_retryable ||
                !ConnectTip(state, pindexConnect, pindexConnect == pindexMostWork ? pblock : std::shared_ptr<const CBlock>(), connectTrace, disconnectpool)) {
                if (state.IsInvalid()) {
                    // The block violates a consensus rule.
                    if (state.GetResult() != BlockValidationResult::BLOCK_MUTATED) {
                        InvalidChainFound(vpindexToConnect.front());
                    }
                    state = BlockValidationState();
                    fInvalidFound = true;
                    fContinue = false;
                    break;
                } else if (IsRetryableMatMulActivationError(state)) {
                    // ExactReplay cancelled / local accelerator miss / trusted
                    // quorum timeout. Not a disk/system fatal and not consensus
                    // invalid: leave pindexMostWork in setBlockIndexCandidates
                    // and let ActivateBestChain return so RPC/net can complete.
                    // The scheduler retries when the GPU or quorum is ready.
                    LogWarning("ActivateBestChainStep: retryable MatMul failure connecting %s (%s); "
                               "leaving candidate in setBlockIndexCandidates\n",
                               pindexConnect->GetBlockHash().ToString(),
                               state.ToString());
                    // DisconnectTip does not maintain the candidate set.
                    // ABC CheckBlockIndex requires the new tip and every
                    // still-valid fork with >= tip work to be present.
                    if (pindexOldTip != nullptr) {
                        CBlockIndex* disconnected{
                            const_cast<CBlockIndex*>(pindexOldTip)};
                        for (; disconnected != nullptr &&
                               disconnected != m_chain.Tip();
                             disconnected = disconnected->pprev) {
                            TryAddBlockIndexCandidate(disconnected);
                        }
                    }
                    if (m_chain.Tip() != nullptr) {
                        TryAddBlockIndexCandidate(m_chain.Tip());
                    }
                    fContinue = false;
                    break;
                } else {
                    // A system error occurred (disk space, database error, ...).
                    // Make the mempool consistent with the current tip, just in case
                    // any observers try to use it before shutdown.
                    MaybeUpdateMempoolForReorg(disconnectpool, false);
                    return false;
                }
            } else {
                connected_this_step = true;
                PruneBlockIndexCandidates();
                if (!pindexOldTip || m_chain.Tip()->nChainWork > pindexOldTip->nChainWork) {
                    // We're in a better position than we were. Return temporarily to release the lock.
                    fContinue = false;
                    break;
                }
            }
        }
    }

    if (fBlocksDisconnected) {
        // If any blocks were disconnected, disconnectpool may be non empty.  Add
        // any disconnected transactions back to the mempool.
        MaybeUpdateMempoolForReorg(disconnectpool, true);
    }
    if (m_mempool) m_mempool->check(this->CoinsTip(), NextBlockHeightOrLimit(this->m_chain.Height()));

    CheckForkWarningConditions();

    if (!IsRetryableMatMulActivationError(state) &&
        this == &m_chainman.ActiveChainstate() &&
        !m_chainman.NormalizeReorgRecovery(m_chain.Tip())) {
        return state.Error("failed to persist completed reorg recovery transition");
    }

    return true;
}

static SynchronizationState GetSynchronizationState(bool init, bool blockfiles_indexed)
{
    if (!init) return SynchronizationState::POST_INIT;
    if (!blockfiles_indexed) return SynchronizationState::INIT_REINDEX;
    return SynchronizationState::INIT_DOWNLOAD;
}

bool ChainstateManager::NotifyHeaderTip()
{
    bool fNotify = false;
    bool fInitialBlockDownload = false;
    CBlockIndex* pindexHeader = nullptr;
    {
        LOCK(GetMutex());
        pindexHeader = m_best_header;

        if (pindexHeader != m_last_notified_header) {
            fNotify = true;
            fInitialBlockDownload = IsInitialBlockDownload();
            m_last_notified_header = pindexHeader;
        }
    }
    // Send block tip changed notifications without the lock held
    if (fNotify) {
        GetNotifications().headerTip(GetSynchronizationState(fInitialBlockDownload, m_blockman.m_blockfiles_indexed), pindexHeader->nHeight, pindexHeader->nTime, false);
    }
    return fNotify;
}

static void LimitValidationInterfaceQueue(ValidationSignals& signals) LOCKS_EXCLUDED(cs_main) {
    AssertLockNotHeld(cs_main);

    if (signals.CallbacksPending() > 10) {
        signals.SyncWithValidationInterfaceQueue();
    }
}

//! Drain the validation-interface queue without holding m_chainstate_mutex.
//! Syncing while that mutex is held deadlocks against net/RPC ActivateBestChain
//! (scheduler callbacks vs the thread blocked in Sync).
template <typename MutexType>
static void DrainValidationInterfaceQueue(ValidationSignals& signals, UniqueLock<MutexType>& chainstate_lock)
    LOCKS_EXCLUDED(cs_main)
{
    AssertLockNotHeld(cs_main);
    if (signals.CallbacksPending() <= 10) return;
    Assert(chainstate_lock.owns_lock());
    REVERSE_LOCK(chainstate_lock);
    signals.SyncWithValidationInterfaceQueue();
}

bool Chainstate::ActivateBestChain(BlockValidationState& state, std::shared_ptr<const CBlock> pblock)
{
    AssertLockNotHeld(m_chainstate_mutex);

    // Note that while we're often called here from ProcessNewBlock, this is
    // far from a guarantee. Things in the P2P/RPC will often end up calling
    // us in the middle of ProcessNewBlock - do not assume pblock is set
    // sanely for performance or correctness!
    AssertLockNotHeld(::cs_main);

    // ABC maintains a fair degree of expensive-to-calculate internal state
    // because this function periodically releases cs_main so that it does not lock up other threads for too long
    // during large connects - and to allow for e.g. the callback queue to drain
    // we use m_chainstate_mutex to enforce mutual exclusion so that only one caller may execute this function at a time
    WAIT_LOCK(m_chainstate_mutex, chainstate_lock);

    // Belt-and-suspenders check that we aren't attempting to advance the background
    // chainstate past the snapshot base block.
    if (WITH_LOCK(::cs_main, return m_disabled)) {
        LogPrintf("m_disabled is set - this chainstate should not be in operation. "
            "Please report this as a bug. %s\n", CLIENT_BUGREPORT);
        return false;
    }

    CBlockIndex *pindexMostWork = nullptr;
    CBlockIndex *pindexNewTip = nullptr;
    bool exited_ibd{false};
    bool attempted_step{false};
    bool retryable_matmul_deferred{false};
    do {
        // Drain the validation queue without holding m_chainstate_mutex. Syncing
        // while that mutex is held deadlocks against net-thread ActivateBestChain
        // (the Bitcoin Core comment below is the same failure mode).
        // Note that if a validationinterface callback ends up calling
        // ActivateBestChain this may lead to a deadlock! We should
        // probably have a DEBUG_LOCKORDER test for this in the future.
        if (m_chainman.m_options.signals) {
            DrainValidationInterfaceQueue(*m_chainman.m_options.signals, chainstate_lock);
            if (WITH_LOCK(::cs_main, return m_disabled)) {
                LogPrintf("m_disabled is set - this chainstate should not be in operation. "
                    "Please report this as a bug. %s\n", CLIENT_BUGREPORT);
                return false;
            }
        }

        {
            LOCK(cs_main);
            {
            // Lock transaction pool for at least as long as it takes for connectTrace to be consumed
            LOCK(MempoolMutex());
            const bool was_in_ibd = m_chainman.IsInitialBlockDownload();
            CBlockIndex* starting_tip = m_chain.Tip();
            bool blocks_connected = false;
            do {
                // We absolutely may not unlock cs_main until we've made forward progress
                // (with the exception of shutdown due to hardware issues, low disk space, etc).
                ConnectTrace connectTrace; // Destructed before cs_main is unlocked

                // Give ActivateBestChainStep one chance (LoadChainTip must still
                // connect genesis even if shutdown is already requested). After
                // that, honor m_interrupt BEFORE starting another ExactReplay/
                // connect so a retryable MatMul recovery cannot ignore shutdown.
                if (attempted_step && m_chainman.m_interrupt) {
                    break;
                }

                if (pindexMostWork == nullptr) {
                    // Stuck ABC never takes a step, so NormalizeReorgRecovery
                    // after ActivateBestChainStep never runs. Clear a stale
                    // record before selecting most-work (live 2026-08-15:
                    // armed 01:38 recovery kept 2fd67f18 out of the set).
                    if (this == &m_chainman.ActiveChainstate() &&
                        !m_chainman.NormalizeReorgRecovery(m_chain.Tip())) {
                        return state.Error("failed to persist completed reorg recovery transition");
                    }
                    pindexMostWork = FindMostWorkChain();
                }

                // Whether we have anything to do at all.
                if (pindexMostWork == nullptr || pindexMostWork == m_chain.Tip()) {
                    break;
                }

                bool fInvalidFound = false;
                std::shared_ptr<const CBlock> nullBlockPtr;
                // BlockConnected signals must be sent for the original role;
                // in case snapshot validation is completed during ActivateBestChainStep, the
                // result of GetRole() changes from BACKGROUND to NORMAL.
               const ChainstateRole chainstate_role{this->GetRole()};
                CBlockIndex* const tip_before_step{m_chain.Tip()};
                if (!ActivateBestChainStep(state, pindexMostWork, pblock && pblock->GetHash() == pindexMostWork->GetBlockHash() ? pblock : nullBlockPtr, fInvalidFound, connectTrace)) {
                    // A system error occurred
                    return false;
                }
                attempted_step = true;
                blocks_connected = true;
                pindexNewTip = m_chain.Tip();

                for (const PerBlockConnectTrace& trace : connectTrace.GetBlocksConnected()) {
                    assert(trace.pblock && trace.pindex);
                    if (m_chainman.m_options.signals) {
                        m_chainman.m_options.signals->BlockConnected(chainstate_role, trace.pblock, trace.pindex);
                    }
                }

                if (IsRetryableMatMulActivationError(state)) {
                    // Do not busy-loop the same pindexMostWork: after a reorg
                    // disconnect the tip is worse than starting_tip, so the
                    // inner comparator would retry forever. Leave the candidate
                    // in setBlockIndexCandidates; net/scheduler retries later.
                    LogWarning("ActivateBestChain: retryable MatMul failure toward %s (%s); "
                               "stopping activation so RPC/net can complete\n",
                               pindexMostWork->GetBlockHash().ToString(),
                               state.ToString());
                    state = BlockValidationState();
                    retryable_matmul_deferred = true;
                    break;
                }

                if (fInvalidFound) {
                    // Wipe cache, we may need another branch now.
                    pindexMostWork = nullptr;
                }

                // This will have been toggled in
                // ActivateBestChainStep -> ConnectTip -> MaybeCompleteSnapshotValidation,
                // if at all, so we should catch it here.
                //
                // Break this do-while to ensure we don't advance past the base snapshot.
                if (m_disabled) {
                    break;
                }
                // No chain movement and not a fork-choice wipe: stop. A
                // successful ABCStep that neither connects nor invalidates
                // would otherwise retry forever while tip is worse than
                // starting_tip (inner comparator), holding cs_main.
                if (m_chain.Tip() == tip_before_step && !fInvalidFound) {
                    // Inner do-while exits on an unchanged tip (equal work),
                    // but the outer loop is `pindexNewTip != pindexMostWork`.
                    // Leaving the unconnectable target set retries ABC with
                    // cs_main held (PR 105 comment 5301483741: restart
                    // livelock on a pending attested child).
                    LogWarning("ActivateBestChain: no progress toward %s; "
                               "stopping activation so RPC/net can complete\n",
                               pindexMostWork->GetBlockHash().ToString());
                    retryable_matmul_deferred = true;
                    break;
                }
            } while (!m_chain.Tip() || (starting_tip && CBlockIndexWorkComparator()(m_chain.Tip(), starting_tip)));
            if (!blocks_connected) return true;

            const CBlockIndex* pindexFork = m_chain.FindFork(starting_tip);
            bool still_in_ibd = m_chainman.IsInitialBlockDownload();

            if (was_in_ibd && !still_in_ibd) {
                // Active chainstate has exited IBD.
                exited_ibd = true;
            }

            // Notify external listeners about the new tip.
            // Enqueue while holding cs_main to ensure that UpdatedBlockTip is called in the order in which blocks are connected
            if (this == &m_chainman.ActiveChainstate() && pindexFork != pindexNewTip) {
                // Notify ValidationInterface subscribers
                if (m_chainman.m_options.signals) {
                    m_chainman.m_options.signals->UpdatedBlockTip(pindexNewTip, pindexFork, still_in_ibd);
                }

                if (kernel::IsInterrupted(m_chainman.GetNotifications().blockTip(GetSynchronizationState(still_in_ibd, m_chainman.m_blockman.m_blockfiles_indexed), *pindexNewTip))) {
                    // Just breaking and returning success for now. This could
                    // be changed to bubble up the kernel::Interrupted value to
                    // the caller so the caller could distinguish between
                    // completed and interrupted operations.
                    break;
                }
            }
            } // release MempoolMutex
            // Notify external listeners about the new tip, even if pindexFork == pindexNewTip.
            if (m_chainman.m_options.signals && this == &m_chainman.ActiveChainstate()) {
                m_chainman.m_options.signals->ActiveTipChange(*Assert(pindexNewTip), m_chainman.IsInitialBlockDownload());
            }
        } // release cs_main
        // When we reach this point, we switched to a new tip (stored in pindexNewTip).

        if (exited_ibd) {
            // If a background chainstate is in use, we may need to rebalance our
            // allocation of caches once a chainstate exits initial block download.
            LOCK(::cs_main);
            m_chainman.MaybeRebalanceCaches();
        }

        // Write changes periodically to disk, after relay.
        if (!FlushStateToDisk(state, FlushStateMode::PERIODIC)) {
            return false;
        }

        if (WITH_LOCK(::cs_main, return m_disabled)) {
            // Background chainstate has reached the snapshot base block, so exit.

            // Restart indexes to resume indexing for all blocks unique to the snapshot
            // chain. This resumes indexing "in order" from where the indexing on the
            // background validation chain left off.
            //
            // This cannot be done while holding cs_main (within
            // MaybeCompleteSnapshotValidation) or a cs_main deadlock will occur.
            if (m_chainman.snapshot_download_completed) {
                m_chainman.snapshot_download_completed();
            }
            break;
        }

        // Interrupt is also checked at the top of each subsequent inner
        // iteration (see attempted_step above). This trailing check still
        // lets the first step run so LoadChainTip can connect genesis.
        if (m_chainman.m_interrupt) break;
        if (retryable_matmul_deferred) break;
    } while (pindexNewTip != pindexMostWork);

    m_chainman.CheckBlockIndex();

    return true;
}

bool Chainstate::PreciousBlock(BlockValidationState& state, CBlockIndex* pindex)
{
    AssertLockNotHeld(m_chainstate_mutex);
    AssertLockNotHeld(::cs_main);
    {
        LOCK(cs_main);
        const CBlockIndex* tip = m_chain.Tip();
        if (!tip) {
            LogError("PreciousBlock: chain tip is null\n");
            return false;
        }
        if (pindex->nChainWork < tip->nChainWork) {
            // Nothing to do, this block is not at the tip.
            return true;
        }
        if (tip->nChainWork > m_chainman.nLastPreciousChainwork) {
            // The chain has been extended since the last call, reset the counter.
            m_chainman.nBlockReverseSequenceId = -1;
        }
        m_chainman.nLastPreciousChainwork = tip->nChainWork;
        setBlockIndexCandidates.erase(pindex);
        pindex->nSequenceId = m_chainman.nBlockReverseSequenceId;
        if (m_chainman.nBlockReverseSequenceId > std::numeric_limits<int32_t>::min()) {
            // We can't keep reducing the counter if somebody really wants to
            // call preciousblock 2**31-1 times on the same set of tips...
            m_chainman.nBlockReverseSequenceId--;
        }
        if (pindex->IsValid(BLOCK_VALID_TRANSACTIONS) &&
            pindex->HaveNumChainTxs() &&
            !m_chainman.IsOnParkedReorgBranch(pindex)) {
            TryAddBlockIndexCandidate(pindex);
            PruneBlockIndexCandidates();
        }
    }

    return ActivateBestChain(state, std::shared_ptr<const CBlock>());
}

bool Chainstate::InvalidateBlock(BlockValidationState& state, CBlockIndex* pindex)
{
    AssertLockNotHeld(m_chainstate_mutex);
    AssertLockNotHeld(::cs_main);

    // Genesis block can't be invalidated
    assert(pindex);
    if (pindex->nHeight == 0) return false;

    CBlockIndex* to_mark_failed = pindex;
    bool pindex_was_in_chain = false;
    int disconnected = 0;

    // We do not allow ActivateBestChain() to run while InvalidateBlock() is
    // running, as that could cause the tip to change while we disconnect
    // blocks.
    WAIT_LOCK(m_chainstate_mutex, chainstate_lock);

    // We'll be acquiring and releasing cs_main below, to allow the validation
    // callbacks to run. However, we should keep the block index in a
    // consistent state as we disconnect blocks -- in particular we need to
    // add equal-work blocks to setBlockIndexCandidates as we disconnect.
    // To avoid walking the block index repeatedly in search of candidates,
    // build a map once so that we can look up candidate blocks by chain
    // work as we go.
    std::multimap<const arith_uint256, CBlockIndex *> candidate_blocks_by_work;

    {
        LOCK(cs_main);
        for (auto& entry : m_blockman.m_block_index) {
            CBlockIndex* candidate = &entry.second;
            // We don't need to put anything in our active chain into the
            // multimap, because those candidates will be found and considered
            // as we disconnect.
            // Instead, consider only non-active-chain blocks that have at
            // least as much work as where we expect the new tip to end up.
            if (!m_chain.Contains(candidate) &&
                    !CBlockIndexWorkComparator()(candidate, pindex->pprev) &&
                    candidate->IsValid(BLOCK_VALID_TRANSACTIONS) &&
                    candidate->HaveNumChainTxs() &&
                    !m_chainman.IsOnParkedReorgBranch(candidate)) {
                candidate_blocks_by_work.insert(std::make_pair(candidate->nChainWork, candidate));
            }
        }
    }

    // Disconnect (descendants of) pindex, and mark them invalid.
    while (true) {
        if (m_chainman.m_interrupt) break;

        // Do not SyncWithValidationInterfaceQueue while m_chainstate_mutex is
        // held (deep invalidateblock deadlocked here: RPC held this mutex,
        // scheduler callbacks / net wanted ActivateBestChain). Queue growth is
        // drained after the mutex is released below. This loop stays
        // interruptible at the top of every disconnect; ActivateBestChain
        // after the RPC helper also honors m_interrupt.

        LOCK(cs_main);
        // Lock for as long as disconnectpool is in scope to make sure MaybeUpdateMempoolForReorg is
        // called after DisconnectTip without unlocking in between
        LOCK(MempoolMutex());
        if (!m_chain.Contains(pindex)) break;
        pindex_was_in_chain = true;
        CBlockIndex *invalid_walk_tip = m_chain.Tip();

        // ActivateBestChain considers blocks already in m_chain
        // unconditionally valid already, so force disconnect away from it.
        DisconnectedBlockTransactions disconnectpool{MAX_DISCONNECTED_TX_POOL_BYTES};
        bool ret = DisconnectTip(state, &disconnectpool);
        // DisconnectTip will add transactions to disconnectpool.
        // Adjust the mempool to be consistent with the new tip, adding
        // transactions back to the mempool if disconnecting was successful,
        // and we're not doing a very deep invalidation (in which case
        // keeping the mempool up to date is probably futile anyway).
        MaybeUpdateMempoolForReorg(disconnectpool, /* fAddToMempool = */ (++disconnected <= 10) && ret);
        if (!ret) return false;
        if (invalid_walk_tip->pprev != m_chain.Tip()) {
            LogError("InvalidateBlock: invalid_walk_tip->pprev (%s) != chain tip (%s) after disconnect\n",
                     invalid_walk_tip->pprev ? invalid_walk_tip->pprev->GetBlockHash().GetHex() : "null",
                     m_chain.Tip() ? m_chain.Tip()->GetBlockHash().GetHex() : "null");
            return false;
        }

        // We immediately mark the disconnected blocks as invalid.
        // This prevents a case where pruned nodes may fail to invalidateblock
        // and be left unable to start as they have no tip candidates (as there
        // are no blocks that meet the "have data and are not invalid per
        // nStatus" criteria for inclusion in setBlockIndexCandidates).
        invalid_walk_tip->nStatus |= BLOCK_FAILED_VALID;
        m_blockman.m_dirty_blockindex.insert(invalid_walk_tip);
        setBlockIndexCandidates.erase(invalid_walk_tip);
        TryAddBlockIndexCandidate(invalid_walk_tip->pprev);
        if (invalid_walk_tip == to_mark_failed->pprev && (to_mark_failed->nStatus & BLOCK_FAILED_VALID)) {
            // We only want to mark the last disconnected block as BLOCK_FAILED_VALID; its children
            // need to be BLOCK_FAILED_CHILD instead.
            to_mark_failed->nStatus = (to_mark_failed->nStatus ^ BLOCK_FAILED_VALID) | BLOCK_FAILED_CHILD;
            m_blockman.m_dirty_blockindex.insert(to_mark_failed);
        }

        // Add any equal or more work headers to setBlockIndexCandidates
        auto candidate_it = candidate_blocks_by_work.lower_bound(invalid_walk_tip->pprev->nChainWork);
        while (candidate_it != candidate_blocks_by_work.end()) {
            if (!CBlockIndexWorkComparator()(candidate_it->second, invalid_walk_tip->pprev)) {
                TryAddBlockIndexCandidate(candidate_it->second);
                candidate_it = candidate_blocks_by_work.erase(candidate_it);
            } else {
                ++candidate_it;
            }
        }

        // Track the last disconnected block, so we can correct its BLOCK_FAILED_CHILD status in future
        // iterations, or, if it's the last one, call InvalidChainFound on it.
        to_mark_failed = invalid_walk_tip;
    }

    m_chainman.CheckBlockIndex();

    {
        LOCK(cs_main);
        if (m_chain.Contains(to_mark_failed)) {
            // If the to-be-marked invalid block is in the active chain, something is interfering and we can't proceed.
            return false;
        }

        // Mark pindex (or the last disconnected block) as invalid, even when it never was in the main chain
        to_mark_failed->nStatus |= BLOCK_FAILED_VALID;
        m_blockman.m_dirty_blockindex.insert(to_mark_failed);
        setBlockIndexCandidates.erase(to_mark_failed);
        m_chainman.m_failed_blocks.insert(to_mark_failed);

        // If any new blocks somehow arrived while we were disconnecting
        // (above), then the pre-calculation of what should go into
        // setBlockIndexCandidates may have missed entries. This would
        // technically be an inconsistency in the block index, but if we clean
        // it up here, this should be an essentially unobservable error.
        // Loop back over all block index entries and add any missing entries
        // to setBlockIndexCandidates.
        for (auto& [_, block_index] : m_blockman.m_block_index) {
            if (block_index.IsValid(BLOCK_VALID_TRANSACTIONS) &&
                block_index.HaveNumChainTxs() &&
                !setBlockIndexCandidates.value_comp()(&block_index, m_chain.Tip()) &&
                !m_chainman.IsOnParkedReorgBranch(&block_index)) {
                TryAddBlockIndexCandidate(&block_index);
            }
        }

        InvalidChainFound(to_mark_failed);
    }

    // Only notify about a new block tip if the active chain was modified.
    if (pindex_was_in_chain) {
        // Ignoring return value for now, this could be changed to bubble up
        // kernel::Interrupted value to the caller so the caller could
        // distinguish between completed and interrupted operations. It might
        // also make sense for the blockTip notification to have an enum
        // parameter indicating the source of the tip change so hooks can
        // distinguish user-initiated invalidateblock changes from other
        // changes.
        (void)m_chainman.GetNotifications().blockTip(GetSynchronizationState(m_chainman.IsInitialBlockDownload(), m_chainman.m_blockman.m_blockfiles_indexed), *to_mark_failed->pprev);

        // Fire ActiveTipChange now for the current chain tip to make sure clients are notified.
        // ActivateBestChain may call this as well, but not necessarily.
        if (m_chainman.m_options.signals) {
            m_chainman.m_options.signals->ActiveTipChange(*Assert(m_chain.Tip()), m_chainman.IsInitialBlockDownload());
        }
    }
    if (m_chainman.m_options.signals) {
        REVERSE_LOCK(chainstate_lock);
        LimitValidationInterfaceQueue(*m_chainman.m_options.signals);
    }
    return true;
}

void Chainstate::SetBlockFailureFlags(CBlockIndex* invalid_block)
{
    AssertLockHeld(cs_main);

    for (auto& [_, block_index] : m_blockman.m_block_index) {
        if (invalid_block != &block_index && block_index.GetAncestor(invalid_block->nHeight) == invalid_block) {
            block_index.nStatus = (block_index.nStatus & ~BLOCK_FAILED_VALID) | BLOCK_FAILED_CHILD;
            m_blockman.m_dirty_blockindex.insert(&block_index);
        }
    }
}

void Chainstate::ResetBlockFailureFlags(CBlockIndex *pindex) {
    AssertLockHeld(cs_main);

    int nHeight = pindex->nHeight;

    // Remove the invalidity flag from this block and all its descendants.
    for (auto& [_, block_index] : m_blockman.m_block_index) {
        if ((block_index.nStatus & BLOCK_FAILED_MASK) && block_index.GetAncestor(nHeight) == pindex) {
            block_index.nStatus &= ~BLOCK_FAILED_MASK;
            m_blockman.m_dirty_blockindex.insert(&block_index);
            if (block_index.IsValid(BLOCK_VALID_TRANSACTIONS) &&
                block_index.HaveNumChainTxs() &&
                setBlockIndexCandidates.value_comp()(m_chain.Tip(), &block_index) &&
                !m_chainman.IsOnParkedReorgBranch(&block_index)) {
                TryAddBlockIndexCandidate(&block_index);
            }
            if (&block_index == m_chainman.m_best_invalid) {
                // Reset invalid block marker if it was pointing to one of those.
                m_chainman.m_best_invalid = nullptr;
            }
            m_chainman.m_failed_blocks.erase(&block_index);
        }
    }

    // Remove the invalidity flag from all ancestors too.
    while (pindex != nullptr) {
        if (pindex->nStatus & BLOCK_FAILED_MASK) {
            pindex->nStatus &= ~BLOCK_FAILED_MASK;
            m_blockman.m_dirty_blockindex.insert(pindex);
            m_chainman.m_failed_blocks.erase(pindex);
        }
        pindex = pindex->pprev;
    }
}

void Chainstate::TryAddBlockIndexCandidate(CBlockIndex* pindex)
{
    AssertLockHeld(cs_main);
    const CBlockIndex* const tip{m_chain.Tip()};
    const bool parked{m_chainman.IsOnParkedReorgBranch(pindex)};
    const bool defer_losing{m_chainman.ShouldDeferLosingTipExtension(pindex)};
    const bool consider{TrustedMirrorShouldConsiderMostWorkCandidate(tip, pindex)};
    if (parked || defer_losing || !consider) {
        if (pindex != nullptr && tip != nullptr && pindex->pprev == tip) {
            LogWarning("%s: refusing tip-child hash=%s height=%d parked=%s "
                       "defer_losing_tip=%s consider_most_work=%s\n",
                       __func__, pindex->GetBlockHash().ToString(),
                       pindex->nHeight, parked ? "yes" : "no",
                       defer_losing ? "yes" : "no",
                       consider ? "yes" : "no");
        }
        return;
    }

    // The block only is a candidate for the most-work-chain if it has the same
    // or more work than our current tip. Attested abandon-fork targets are the
    // exception: they must be selectable while the unattested tip still has
    // more work.
    if (tip != nullptr &&
        setBlockIndexCandidates.value_comp()(pindex, tip) &&
        !m_chainman.IsAttestedAbandonForkCandidate(pindex)) {
        if (pindex != nullptr && pindex->pprev == tip) {
            LogWarning("%s: refusing tip-child hash=%s height=%d less_work_than_tip=yes\n",
                       __func__, pindex->GetBlockHash().ToString(), pindex->nHeight);
        }
        return;
    }

    bool is_active_chainstate = this == &m_chainman.ActiveChainstate();
    if (is_active_chainstate) {
        // The active chainstate should always add entries that have more
        // work than the tip.
        setBlockIndexCandidates.insert(pindex);
    } else if (!m_disabled) {
        // For the background chainstate, we only consider connecting blocks
        // towards the snapshot base (which can't be nullptr or else we'll
        // never make progress).
        const CBlockIndex* snapshot_base{Assert(m_chainman.GetSnapshotBaseBlock())};
        if (snapshot_base->GetAncestor(pindex->nHeight) == pindex) {
            setBlockIndexCandidates.insert(pindex);
        }
    }
}

const CBlockIndex* ChainstateManager::FindParkedReorgBranchRoot(const CBlockIndex* pindex) const
{
    AssertLockHeld(::cs_main);
    if (pindex == nullptr) return nullptr;
    for (const uint256& root_hash : m_parked_reorg_branch_roots) {
        const CBlockIndex* root = m_blockman.LookupBlockIndex(root_hash);
        if (root != nullptr &&
            pindex->nHeight >= root->nHeight &&
            pindex->GetAncestor(root->nHeight) == root) {
            return root;
        }
    }
    return nullptr;
}

namespace {
bool BlockIndexComparable(const CBlockIndex* a, const CBlockIndex* b);
bool BlockIndexDescends(const CBlockIndex* block, const CBlockIndex* ancestor);
} // namespace

ChainRecoveryState ChainstateManager::GetChainRecoveryState() const
{
    AssertLockHeld(::cs_main);
    const CBlockIndex* const active_tip =
        m_active_chainstate != nullptr ? m_active_chainstate->m_chain.Tip() : nullptr;
    const CBlockIndex* const followed = m_best_header;
    if (active_tip == nullptr || followed == nullptr) {
        return {
            .phase = ChainRecoveryPhase::CHASING,
            .followed_target = followed,
            .fork = nullptr,
            .reorg_depth = 0,
            .reason = "chainstate-not-initialized",
        };
    }

    if (m_reorg_recovery.has_value()) {
        const CBlockIndex* const recovery_root{
            m_blockman.LookupBlockIndex(m_reorg_recovery->recovery_root_hash)};
        const CBlockIndex* const authenticated_tip{
            m_blockman.LookupBlockIndex(m_reorg_recovery->authenticated_tip_hash)};
        const CBlockIndex* recovery_target{
            BlockIndexDescends(followed, recovery_root)
                ? followed
                : authenticated_tip};
        const CBlockIndex* const recovery_fork{
            m_blockman.LookupBlockIndex(m_reorg_recovery->fork_hash)};
        const uint32_t depth{
            recovery_fork != nullptr && active_tip->nHeight > recovery_fork->nHeight
                ? static_cast<uint32_t>(active_tip->nHeight - recovery_fork->nHeight)
                : 0};
        return {
            .phase = FindParkedReorgBranchRoot(recovery_target) != nullptr
                ? ChainRecoveryPhase::PARKED_NEEDS_OPERATOR
                : ChainRecoveryPhase::RECOVERING_REORG,
            .followed_target = recovery_target,
            .fork = recovery_fork,
            .reorg_depth = depth,
            .reason = "authenticated-shallow-race-recovery",
        };
    }

    const CBlockIndex* const followed_at_active_height =
        followed->GetAncestor(active_tip->nHeight);
    if (followed == active_tip ||
        (followed->nHeight <= active_tip->nHeight &&
         active_tip->GetAncestor(followed->nHeight) == followed)) {
        return {
            .phase = ChainRecoveryPhase::CONVERGED,
            .followed_target = followed,
            .fork = followed,
            .reorg_depth = 0,
            .reason = "followed-header-on-active-chain",
        };
    }
    if (followed_at_active_height == active_tip) {
        return {
            .phase = ChainRecoveryPhase::CHASING,
            .followed_target = followed,
            .fork = active_tip,
            .reorg_depth = 0,
            .reason = "followed-header-extends-active-tip",
        };
    }

    const CBlockIndex* const fork = m_active_chainstate->m_chain.FindFork(followed);
    const uint32_t reorg_depth = fork != nullptr && active_tip->nHeight > fork->nHeight
        ? static_cast<uint32_t>(active_tip->nHeight - fork->nHeight)
        : 0;
    if (FindParkedReorgBranchRoot(followed) != nullptr) {
        return {
            .phase = ChainRecoveryPhase::PARKED_NEEDS_OPERATOR,
            .followed_target = followed,
            .fork = fork,
            .reorg_depth = reorg_depth,
            .reason = "followed-branch-parked",
        };
    }
    return {
        .phase = ChainRecoveryPhase::RECOVERING_REORG,
        .followed_target = followed,
        .fork = fork,
        .reorg_depth = reorg_depth,
        .reason = "followed-header-diverged",
    };
}

bool ChainstateManager::IsOnParkedReorgBranch(const CBlockIndex* pindex) const
{
    AssertLockHeld(::cs_main);
    if (m_options.deep_reorg_action != kernel::DeepReorgAction::PARK) return false;
    return FindParkedReorgBranchRoot(pindex) != nullptr;
}

bool ChainstateManager::PersistParkedReorgBranches()
{
    AssertLockHeld(::cs_main);
    if (!m_blockman.m_block_tree_db) return true;
    return m_blockman.m_block_tree_db->WriteParkedReorgBranches(m_parked_reorg_branch_roots);
}

bool ChainstateManager::ParkReorgBranch(CBlockIndex* branch_root)
{
    AssertLockHeld(::cs_main);
    if (branch_root == nullptr) return false;
    // A parked root ancestral to the active tip makes IsOnParkedReorgBranch(tip)
    // true and permanently rejects tip candidate insertion (PARK→WARN→PARK).
    // Never persist such a root; NormalizeParkedReorgBranches still repairs
    // legacy datadirs that already have one on disk.
    if (ActiveChain().Contains(branch_root)) {
        LogWarning("%s: refusing to park active-chain ancestor hash=%s height=%d\n",
                   __func__, branch_root->GetBlockHash().ToString(),
                   branch_root->nHeight);
        return false;
    }
    const uint256 root_hash = branch_root->GetBlockHash();
    if (!m_parked_reorg_branch_roots.insert(root_hash).second) return true;
    if (!PersistParkedReorgBranches()) {
        LogError("%s: failed to persist parked reorg branch root %s\n", __func__, root_hash.ToString());
        m_parked_reorg_branch_roots.erase(root_hash);
        return false;
    }
    LogWarning("%s: parked local reorg branch root hash=%s height=%d\n",
               __func__, root_hash.ToString(), branch_root->nHeight);
    return true;
}

bool ChainstateManager::UnparkReorgBranchContainingBlock(const CBlockIndex* pindex)
{
    AssertLockHeld(::cs_main);
    const CBlockIndex* root = FindParkedReorgBranchRoot(pindex);
    if (root == nullptr) return false;
    const uint256 root_hash = root->GetBlockHash();
    m_parked_reorg_branch_roots.erase(root_hash);
    if (!PersistParkedReorgBranches()) {
        LogError("%s: failed to persist removal of parked reorg branch root %s\n", __func__, root_hash.ToString());
        // LevelDB writes are atomic. Restore the in-memory view on a failed
        // commit so selection cannot disagree with what a restart would load.
        m_parked_reorg_branch_roots.insert(root_hash);
        return false;
    }
    for (Chainstate* chainstate : GetAll()) {
        for (auto& [_, block_index] : m_blockman.m_block_index) {
            if (block_index.IsValid(BLOCK_VALID_TRANSACTIONS) &&
                block_index.HaveNumChainTxs()) {
                chainstate->TryAddBlockIndexCandidate(&block_index);
            }
        }
    }
    LogWarning("%s: unparked local reorg branch root hash=%s height=%d\n",
               __func__, root_hash.ToString(), root->nHeight);
    return true;
}

bool ChainstateManager::NormalizeParkedReorgBranches(const CBlockIndex* active_tip)
{
    AssertLockHeld(::cs_main);
    std::set<uint256> normalized;

    if (m_options.deep_reorg_action == kernel::DeepReorgAction::PARK) {
        for (const uint256& root_hash : m_parked_reorg_branch_roots) {
            const CBlockIndex* root = m_blockman.LookupBlockIndex(root_hash);
            if (root == nullptr) {
                LogWarning("%s: dropping unknown parked reorg root %s\n",
                           __func__, root_hash.ToString());
                continue;
            }
            if (active_tip != nullptr &&
                active_tip->nHeight >= root->nHeight &&
                active_tip->GetAncestor(root->nHeight) == root) {
                LogWarning("%s: dropping parked reorg root %s at height %d because it is an ancestor of active tip %s at height %d\n",
                           __func__, root_hash.ToString(), root->nHeight,
                           active_tip->GetBlockHash().ToString(), active_tip->nHeight);
                continue;
            }

            // Descendant roots are redundant. Retain the shallowest root so a
            // single atomic unpark releases the whole refused branch.
            bool covered{false};
            for (auto it = normalized.begin(); it != normalized.end();) {
                const CBlockIndex* existing = m_blockman.LookupBlockIndex(*it);
                if (existing != nullptr && root->nHeight >= existing->nHeight &&
                    root->GetAncestor(existing->nHeight) == existing) {
                    covered = true;
                    break;
                }
                if (existing != nullptr && existing->nHeight > root->nHeight &&
                    existing->GetAncestor(root->nHeight) == root) {
                    it = normalized.erase(it);
                } else {
                    ++it;
                }
            }
            if (!covered) normalized.insert(root_hash);
        }
    } else if (!m_parked_reorg_branch_roots.empty()) {
        LogWarning("%s: retiring %u persisted parked reorg root(s) because active policy is warn-only\n",
                   __func__, static_cast<unsigned>(m_parked_reorg_branch_roots.size()));
    }

    if (normalized == m_parked_reorg_branch_roots) return true;
    if (m_blockman.m_block_tree_db &&
        !m_blockman.m_block_tree_db->WriteParkedReorgBranches(normalized)) {
        LogError("%s: failed to atomically persist normalized parked reorg roots\n", __func__);
        return false;
    }
    m_parked_reorg_branch_roots = std::move(normalized);
    return true;
}

std::vector<uint256> ChainstateManager::GetParkedReorgBranchRoots() const
{
    AssertLockHeld(::cs_main);
    return {m_parked_reorg_branch_roots.begin(), m_parked_reorg_branch_roots.end()};
}

namespace {

bool BlockIndexComparable(const CBlockIndex* a, const CBlockIndex* b)
{
    if (a == nullptr || b == nullptr) return false;
    if (a->nHeight >= b->nHeight) return a->GetAncestor(b->nHeight) == b;
    return b->GetAncestor(a->nHeight) == a;
}

bool BlockIndexDescends(const CBlockIndex* block, const CBlockIndex* ancestor)
{
    return block != nullptr && ancestor != nullptr &&
           block->nHeight >= ancestor->nHeight &&
           block->GetAncestor(ancestor->nHeight) == ancestor;
}

} // namespace

ChainstateManager::SignedFrontierStatus ChainstateManager::GetSignedFrontierStatus() const
{
    AssertLockHeld(::cs_main);
    SignedFrontierStatus out;
    if (!node::matmul_trusted::IsConfigured() || m_active_chainstate == nullptr) {
        return out;
    }
    const auto frontier_height{node::matmul_trusted::HighestAttestedHeight()};
    if (!frontier_height.has_value()) {
        return out;
    }
    out.available = true;
    out.height = *frontier_height;
    const CBlockIndex* const tip{m_active_chainstate->m_chain.Tip()};
    std::optional<uint256> on_chain_hash;
    std::optional<uint256> off_chain_hash;
    for (const auto& hint : node::matmul_trusted::AttestedFrontierHints()) {
        if (hint.height != out.height || hint.hash.IsNull()) continue;
        const CBlockIndex* const frontier_index{
            m_blockman.LookupBlockIndex(hint.hash)};
        const bool on_chain{
            tip != nullptr && frontier_index != nullptr &&
            tip->nHeight >= frontier_index->nHeight &&
            tip->GetAncestor(frontier_index->nHeight) == frontier_index};
        if (on_chain) {
            if (!on_chain_hash.has_value()) on_chain_hash = hint.hash;
        } else if (!off_chain_hash.has_value()) {
            off_chain_hash = hint.hash;
        }
    }
    // Dual-attested same-height siblings: prefer the off-chain hash so
    // FindUniqueCompetingAttestedIndex is not inverted by last-writer
    // (live 2026-08-15).
    if (off_chain_hash.has_value()) {
        out.hash = *off_chain_hash;
        out.hash_known = true;
    } else if (on_chain_hash.has_value()) {
        out.hash = *on_chain_hash;
        out.hash_known = true;
    }
    if (tip != nullptr) {
        for (const CBlockIndex* p{tip}; p != nullptr; p = p->pprev) {
            if (node::matmul_trusted::HasQuorum(
                    p->GetBlockHash(), p->nHeight)) {
                out.on_chain_attested_height = p->nHeight;
                break;
            }
        }
        if (out.hash_known) {
            const CBlockIndex* const frontier_index{
                m_blockman.LookupBlockIndex(out.hash)};
            out.on_active_chain =
                frontier_index != nullptr &&
                tip->nHeight >= frontier_index->nHeight &&
                tip->GetAncestor(frontier_index->nHeight) == frontier_index;
        } else if (out.on_chain_attested_height == out.height) {
            // Height-only: cannot prove a fork at equal height.
            out.on_active_chain = true;
        }
    }
    out.blocks_behind = node::matmul_trusted::BlocksBehindSignedFrontier(
        out.height, out.on_chain_attested_height);
    return out;
}

void ChainstateManager::NotifySignedFrontierStatus()
{
    AssertLockHeld(::cs_main);
    const SignedFrontierStatus st{GetSignedFrontierStatus()};
    if (!st.available || st.blocks_behind < 6) {
        GetNotifications().warningUnset(
            kernel::Warning::MATMUL_BEHIND_SIGNED_FRONTIER);
    } else {
        const bilingual_str warning{strprintf(
            _("Warning: this node is %d blocks behind the signed MatMul "
              "frontier (height %d%s). getmatmulattestedtip.hash only reflects "
              "HAVE_DATA on this chain; a stranded fork reports "
              "on_active_chain=true there. See "
              "getblockchaininfo.matmul_signed_frontier."),
            st.blocks_behind,
            st.height,
            st.hash_known ? strprintf(", hash %s", st.hash.ToString()) : "")};
        GetNotifications().warningSet(
            kernel::Warning::MATMUL_BEHIND_SIGNED_FRONTIER, warning,
            /*update=*/true);
    }
    if (st.available && st.blocks_behind >= 2) {
        static int32_t last_behind{-1};
        static bool last_on_chain{true};
        static std::chrono::steady_clock::time_point last_log{};
        const auto now{std::chrono::steady_clock::now()};
        const bool changed{
            st.blocks_behind != last_behind ||
            st.on_active_chain != last_on_chain};
        if (changed || last_log.time_since_epoch().count() == 0 ||
            now - last_log >= std::chrono::seconds{30}) {
            LogInfo("signed frontier height=%d hash=%s on_active_chain=%s "
                    "behind=%d on_chain_attested_height=%d\n",
                    st.height,
                    st.hash_known ? st.hash.ToString() : "unknown",
                    st.on_active_chain ? "true" : "false",
                    st.blocks_behind,
                    st.on_chain_attested_height);
            last_behind = st.blocks_behind;
            last_on_chain = st.on_active_chain;
            last_log = now;
        }
    }
}

const CBlockIndex* ChainstateManager::FindBestKnownAttestedIndex() const
{
    AssertLockHeld(::cs_main);
    if (!node::matmul_trusted::IsConfigured() || m_active_chainstate == nullptr) {
        return nullptr;
    }
    const CBlockIndex* best{nullptr};
    auto consider = [&](const CBlockIndex* idx) {
        if (idx == nullptr || (idx->nStatus & BLOCK_FAILED_MASK)) return;
        if (!(idx->nStatus & BLOCK_HAVE_DATA) ||
            !idx->IsValid(BLOCK_VALID_TRANSACTIONS) ||
            !idx->HaveNumChainTxs()) {
            return;
        }
        if (!node::matmul_trusted::HasQuorum(
                idx->GetBlockHash(), idx->nHeight)) {
            return;
        }
        if (best == nullptr || CBlockIndexWorkComparator()(best, idx)) {
            best = idx;
        }
    };
    for (const auto& hint : node::matmul_trusted::AttestedFrontierHints()) {
        consider(m_blockman.LookupBlockIndex(hint.hash));
    }
    for (CBlockIndex* idx : m_reorg_authenticated_candidate_tips) {
        consider(idx);
    }
    if (const CBlockIndex* tip{m_active_chainstate->m_chain.Tip()}) {
        for (const CBlockIndex* p{tip}; p != nullptr; p = p->pprev) {
            if (node::matmul_trusted::HasQuorum(
                    p->GetBlockHash(), p->nHeight)) {
                consider(p);
                break;
            }
        }
    }
    return best;
}

const CBlockIndex* ChainstateManager::FindUniqueCompetingAttestedIndex() const
{
    AssertLockHeld(::cs_main);
    if (!node::matmul_trusted::IsConfigured() || m_active_chainstate == nullptr) {
        return nullptr;
    }
    const CBlockIndex* const tip{m_active_chainstate->m_chain.Tip()};
    if (tip == nullptr) {
        return nullptr;
    }
    // Live 2026-08-15: signer attested both 189489 siblings; mirrors
    // connected the loser (quorum + HAVE_DATA first). The early "tip has
    // quorum → nullptr" return then froze ABC on unattested children of
    // that loser while the signed frontier ran 140 blocks up the other
    // fork. When the tip already has quorum, still adopt a unique
    // competing attested HAVE_DATA index on a short reorg (1–6) or the
    // unique attested HAVE_DATA suffix of this tip (catch-up). Scan
    // every frontier hint at a height (not last-writer) so a later
    // loser MMATTEST cannot hide the winner.
    const bool tip_has_quorum{node::matmul_trusted::HasQuorum(
        tip->GetBlockHash(), tip->nHeight)};

    std::vector<const CBlockIndex*> competing;
    auto consider = [&](const CBlockIndex* idx) {
        if (idx == nullptr || idx == tip ||
            (idx->nStatus & BLOCK_FAILED_MASK)) {
            return;
        }
        if (!(idx->nStatus & BLOCK_HAVE_DATA) ||
            !idx->IsValid(BLOCK_VALID_TRANSACTIONS) ||
            !idx->HaveNumChainTxs()) {
            return;
        }
        if (!node::matmul_trusted::HasQuorum(
                idx->GetBlockHash(), idx->nHeight)) {
            return;
        }
        // Pending-attestation extensions of the active chain must not be
        // disconnected: the signer typically attests ~1 behind the tip.
        if (m_active_chainstate->m_chain.Contains(idx)) return;
        if (tip_has_quorum) {
            const CBlockIndex* const lca{LastCommonAncestor(tip, idx)};
            if (lca == nullptr) return;
            const int lca_depth{tip->nHeight - lca->nHeight};
            // Live 2026-08-15: unique attested HAVE_DATA child of an
            // already-attested tip (LCA depth 0, suffix not yet connected).
            // Short-reorg is depth 1–6; depth 0 used to be dropped, so ABC
            // never selected the catch-up child while GBT kept mining a
            // competing sibling (signer tip 189675, attested 189676).
            const bool attested_suffix{
                lca == tip && idx->nHeight > tip->nHeight};
            if (!attested_suffix &&
                !node::matmul_trusted::TrustedMirrorIsShortTipReorg(lca_depth)) {
                return;
            }
        }
        competing.push_back(idx);
    };
    for (const auto& hint : node::matmul_trusted::AttestedFrontierHints()) {
        consider(m_blockman.LookupBlockIndex(hint.hash));
    }
    for (CBlockIndex* idx : m_reorg_authenticated_candidate_tips) {
        consider(idx);
    }

    if (tip_has_quorum) {
        std::vector<const CBlockIndex*> suffix;
        suffix.reserve(competing.size());
        for (const CBlockIndex* idx : competing) {
            const CBlockIndex* const lca{LastCommonAncestor(tip, idx)};
            if (lca == tip && idx->nHeight > tip->nHeight) {
                suffix.push_back(idx);
            }
        }
        // Live 2026-08-15: quorum tip 189675 893c7c9b had unique attested
        // HAVE_DATA child fecb5b4f, but attested HAVE_DATA 19625a1e (child
        // of competing 189675 1fbccf3f) sat in the short-reorg window.
        // Uniqueness then bailed, GBT kept mining, and ABC never caught
        // up. Prefer the attested suffix of this quorum tip over a
        // short-reorg away from it.
        if (!suffix.empty()) {
            competing = std::move(suffix);
        }
    }

    // Historical dual-attested siblings remain in the durable frontier hint
    // set. After preferring a direct attested suffix, rank the complete
    // remaining candidate set so a shorter dead-end sibling cannot mask a
    // strictly heavier attested descendant chain. Fail closed only when
    // incomparable candidates tie for the greatest accumulated work.
    const CBlockIndex* unique{nullptr};
    for (const CBlockIndex* idx : competing) {
        if (unique == nullptr || idx->nChainWork > unique->nChainWork) {
            unique = idx;
        }
    }
    if (unique == nullptr) return nullptr;
    for (const CBlockIndex* idx : competing) {
        if (idx == unique || idx->nChainWork != unique->nChainWork) continue;
        if (!BlockIndexComparable(unique, idx)) {
            return nullptr;
        }
    }
    // A quorum active tip is already an authenticated candidate. Competing
    // attestations can replace it only by proving strictly greater work.
    // Equal-work equivocations have no authenticated winner, while lower-work
    // historical siblings must never rewind the node. This also prevents ABC
    // from alternating between old dual-attested branches on each invocation.
    if (tip_has_quorum && unique->nChainWork <= tip->nChainWork) {
        return nullptr;
    }
    if (tip_has_quorum) {
        const CBlockIndex* const lca{LastCommonAncestor(tip, unique)};
        if (lca == nullptr) return nullptr;
        const CBlockIndex* const fork_child{
            unique->GetAncestor(lca->nHeight + 1)};
        if (fork_child == nullptr || fork_child == tip ||
            (fork_child->nStatus & BLOCK_FAILED_MASK) != 0) {
            return nullptr;
        }
        if (!(fork_child->nStatus & BLOCK_HAVE_DATA) ||
            !fork_child->IsValid(BLOCK_VALID_TRANSACTIONS) ||
            !fork_child->HaveNumChainTxs()) {
            return nullptr;
        }
        if (!node::matmul_trusted::HasQuorum(
                fork_child->GetBlockHash(), fork_child->nHeight)) {
            return nullptr;
        }
        // Return the attested frontier itself, not only its first child after
        // the fork. ActivateBestChain treats the selected index as the whole
        // activation target and otherwise stops after switching one block,
        // leaving an already-known signed descendant chain stranded until an
        // unrelated later event happens to invoke activation again.
        return unique;
    }
    // Equal-work attested sibling (lost race) and more-work attested
    // chain (signer pulled ahead) must switch; less-work attested
    // recovers a heavier unattested fork. Restricting to strictly-less
    // work left consensus miners stranded on their own unattested
    // sibling while the attested chain advanced (live 187847).
    return unique;
}

bool ChainstateManager::IsAttestedAbandonForkCandidate(
    const CBlockIndex* candidate) const
{
    AssertLockHeld(::cs_main);
    return candidate != nullptr &&
           candidate == FindUniqueCompetingAttestedIndex();
}

void ChainstateManager::NoteAuthenticatedRecoveryCandidate(CBlockIndex* candidate)
{
    AssertLockHeld(::cs_main);
    if (candidate == nullptr || (candidate->nStatus & BLOCK_FAILED_MASK)) return;

    const bool eligible{
        (m_options.matmul_validation_mode ==
             kernel::MatMulValidationMode::TRUSTED &&
         node::matmul_trusted::HasQuorum(
             candidate->GetBlockHash(), candidate->nHeight)) ||
        (m_options.matmul_validation_mode ==
             kernel::MatMulValidationMode::CONSENSUS &&
         (candidate->nStatus & BLOCK_HAVE_DATA) != 0 &&
         candidate->IsValid(BLOCK_VALID_TRANSACTIONS) &&
         candidate->HaveNumChainTxs() &&
         IsBlockAuthenticated(*candidate, GetConsensus()))};
    if (!eligible) return;

    // Keep only maximal authenticated tips per branch. The loop is bounded by
    // the number of competing branch tips, not total chain height/index size.
    for (auto it = m_reorg_authenticated_candidate_tips.begin();
         it != m_reorg_authenticated_candidate_tips.end();) {
        CBlockIndex* const indexed{*it};
        if (BlockIndexDescends(indexed, candidate)) {
            // A newer notification for an ancestor cannot replace the already
            // indexed descendant tip.
            return;
        }
        if (BlockIndexDescends(candidate, indexed)) {
            it = m_reorg_authenticated_candidate_tips.erase(it);
        } else {
            ++it;
        }
    }
    m_reorg_authenticated_candidate_tips.insert(candidate);
}

bool ChainstateManager::IsAutomaticReorgRecoveryCandidate(
    const CBlockIndex* candidate) const
{
    AssertLockHeld(::cs_main);
    if (!m_reorg_recovery.has_value() || candidate == nullptr ||
        m_active_chainstate == nullptr) {
        return false;
    }
    const node::ReorgRecoveryRecord& record{*m_reorg_recovery};
    const CBlockIndex* const active_tip{m_active_chainstate->m_chain.Tip()};
    const CBlockIndex* const recovery_root{
        m_blockman.LookupBlockIndex(record.recovery_root_hash)};
    const CBlockIndex* const losing_tip{
        m_blockman.LookupBlockIndex(record.losing_tip_hash)};
    if (active_tip == nullptr || recovery_root == nullptr || losing_tip == nullptr ||
        !BlockIndexDescends(active_tip, losing_tip) ||
        !BlockIndexDescends(candidate, recovery_root) ||
        !(candidate->nStatus & BLOCK_HAVE_DATA) ||
        !candidate->IsValid(BLOCK_VALID_TRANSACTIONS) ||
        !candidate->HaveNumChainTxs() ||
        !IsBlockAuthenticated(*candidate, GetConsensus()) ||
        candidate->nChainWork <= active_tip->nChainWork) {
        return false;
    }

    const bool trusted{
        record.mode == static_cast<uint8_t>(
            node::ReorgRecoveryRecord::Mode::TRUSTED_AUTHORITY)};
    if (trusted) {
        if (m_options.matmul_validation_mode !=
                kernel::MatMulValidationMode::TRUSTED ||
            (candidate->nStatus & BLOCK_TRUSTED_REPLAY_ATTESTED) == 0 ||
            !node::matmul_trusted::HasQuorum(
                candidate->GetBlockHash(), candidate->nHeight) ||
            m_best_header == nullptr ||
            !BlockIndexDescends(m_best_header, recovery_root)) {
            return false;
        }
    } else {
        if (m_options.matmul_validation_mode !=
                kernel::MatMulValidationMode::CONSENSUS ||
            candidate->nAuthenticatedChainWork <=
                active_tip->nAuthenticatedChainWork) {
            return false;
        }
    }

    // Authentication must identify one recovery branch, not merely one block.
    // A second incomparable branch with equal or greater authenticated work (or
    // any current authority quorum in trusted mode) makes the choice ambiguous
    // and therefore operator-only.
    for (const CBlockIndex* other : m_reorg_authenticated_candidate_tips) {
        if (other == nullptr || other == candidate ||
            (other->nStatus & BLOCK_FAILED_MASK) ||
            BlockIndexComparable(other, candidate) ||
            BlockIndexDescends(other, active_tip) ||
            (other->nHeight <= active_tip->nHeight &&
             active_tip->GetAncestor(other->nHeight) == other) ||
            (other->pprev == candidate->pprev &&
             other->nHeight == candidate->nHeight)) {
            continue;
        }
        if (trusted) {
            if (node::matmul_trusted::HasQuorum(
                    other->GetBlockHash(), other->nHeight)) {
                return false;
            }
        } else {
            if (!(other->nStatus & BLOCK_HAVE_DATA) ||
                !other->IsValid(BLOCK_VALID_TRANSACTIONS) ||
                !other->HaveNumChainTxs() ||
                !IsBlockAuthenticated(*other, GetConsensus())) {
                continue;
            }
            if (other->nAuthenticatedChainWork >=
                candidate->nAuthenticatedChainWork) {
                return false;
            }
        }
    }
    return true;
}

bool ChainstateManager::IndexIsFollowedTipChild(
    const CBlockIndex* tip, const CBlockIndex* index) const
{
    AssertLockHeld(::cs_main);
    if (tip == nullptr || index == nullptr || index->pprev != tip) return false;
    if ((index->nStatus & BLOCK_FAILED_MASK) != 0) return false;
    const CBlockIndex* const followed{m_best_header};
    return followed != nullptr &&
           followed->nHeight >= index->nHeight &&
           followed->GetAncestor(tip->nHeight) == tip &&
           followed->GetAncestor(index->nHeight) == index;
}

bool ChainstateManager::ShouldDeferLosingTipExtension(
    const CBlockIndex* candidate) const
{
    AssertLockHeld(::cs_main);
    if (!m_reorg_recovery.has_value() || candidate == nullptr ||
        m_active_chainstate == nullptr) {
        return false;
    }
    // Live 2026-08-15: unique attested HAVE_DATA suffix of the current
    // quorum tip (189676 fecb5b4f) was also classified as a losing-tip
    // extension because recovery was armed on a competing 189675.
    // FindMostWorkChain then erased it and looped, wedging ABC / RPC.
    // Catch-up / unique attested abandon must not be frozen by that
    // barrier.
    if (IsAttestedAbandonForkCandidate(candidate)) {
        return false;
    }
    const CBlockIndex* const active_tip{m_active_chainstate->m_chain.Tip()};
    const CBlockIndex* const losing_tip{
        m_blockman.LookupBlockIndex(m_reorg_recovery->losing_tip_hash)};
    const CBlockIndex* const recovery_root{
        m_blockman.LookupBlockIndex(m_reorg_recovery->recovery_root_hash)};
    // Followed validator-chain tip-child only. A blanket pprev==tip bypass
    // would also admit competing siblings (live 14c0d0e4 / e80753f4).
    if (IndexIsFollowedTipChild(active_tip, candidate)) {
        return false;
    }
    return active_tip != nullptr && candidate != active_tip &&
           BlockIndexDescends(active_tip, losing_tip) &&
           BlockIndexDescends(candidate, losing_tip) &&
           !BlockIndexDescends(candidate, recovery_root);
}

bool ChainstateManager::MaybeTrackReorgRecovery(const CBlockIndex* candidate)
{
    AssertLockHeld(::cs_main);
    if (m_active_chainstate == nullptr || candidate == nullptr) return true;
    NoteAuthenticatedRecoveryCandidate(const_cast<CBlockIndex*>(candidate));
    const CBlockIndex* const active_tip{m_active_chainstate->m_chain.Tip()};
    if (active_tip == nullptr) return true;

    if (!m_reorg_recovery.has_value()) {
        const auto profile_settings = kernel::GetReorgProtectionProfileSettings(
            m_options.reorg_protection_profile);
        const uint32_t park_depth = m_options.max_reorg_depth_park.value_or(
            profile_settings.park_depth);
        const CBlockIndex* const fork{m_active_chainstate->m_chain.FindFork(candidate)};
        if (m_options.deep_reorg_action != kernel::DeepReorgAction::PARK ||
            park_depth == kernel::REORG_PROTECTION_DEPTH_DISABLED ||
            fork == nullptr || fork == active_tip ||
            active_tip->nHeight < GetConsensus().nReorgProtectionStartHeight) {
            return true;
        }
        const int depth{active_tip->nHeight - fork->nHeight};
        if (depth <= 0 || depth > static_cast<int>(park_depth)) return true;

        uint8_t mode;
        if (m_options.matmul_validation_mode ==
            kernel::MatMulValidationMode::TRUSTED) {
            // Current quorum authenticates the authority's ExactReplay verdict,
            // but that verdict is validity provenance, not a canonical fork
            // choice. Only install the losing-tip growth barrier once the
            // certified branch has strictly more work than the active tip; equal
            // or stale side forks remain learnable/downloadable but cannot pin
            // honest growth.
            if (!node::matmul_trusted::HasQuorum(
                    candidate->GetBlockHash(), candidate->nHeight) ||
                candidate->nChainWork <= active_tip->nChainWork) {
                return true;
            }
            mode = static_cast<uint8_t>(
                node::ReorgRecoveryRecord::Mode::TRUSTED_AUTHORITY);
        } else if (m_options.matmul_validation_mode ==
                   kernel::MatMulValidationMode::CONSENSUS) {
            // An inferior authenticated side fork must not be able to freeze
            // honest tip growth. Equal authenticated work is the shallow-race
            // boundary; greater work is immediately actionable below.
            if (!(candidate->nStatus & BLOCK_HAVE_DATA) ||
                !candidate->IsValid(BLOCK_VALID_TRANSACTIONS) ||
                !candidate->HaveNumChainTxs() ||
                !IsBlockAuthenticated(*candidate, GetConsensus()) ||
                candidate->nAuthenticatedChainWork <=
                active_tip->nAuthenticatedChainWork) {
                return true;
            }
            mode = static_cast<uint8_t>(
                node::ReorgRecoveryRecord::Mode::CONSENSUS_AUTHENTICATED);
        } else {
            return true;
        }

        CBlockIndex* recovery_root{const_cast<CBlockIndex*>(candidate)};
        while (recovery_root != nullptr && recovery_root->pprev != fork) {
            recovery_root = recovery_root->pprev;
        }
        if (recovery_root == nullptr) return true;

        // Reject ambiguity before persisting a growth barrier. In consensus
        // mode only an equal-or-better independently authenticated branch is a
        // competitor; in trusted mode any other current authority branch is.
        for (const CBlockIndex* other : m_reorg_authenticated_candidate_tips) {
            if (other == nullptr || other == candidate ||
                (other->nStatus & BLOCK_FAILED_MASK) ||
                BlockIndexComparable(other, candidate) ||
                BlockIndexDescends(other, active_tip) ||
                (other->nHeight <= active_tip->nHeight &&
                 active_tip->GetAncestor(other->nHeight) == other) ||
                (other->pprev == candidate->pprev &&
                 other->nHeight == candidate->nHeight)) {
                continue;
            }
            if (mode == static_cast<uint8_t>(
                            node::ReorgRecoveryRecord::Mode::TRUSTED_AUTHORITY)) {
                if (node::matmul_trusted::HasQuorum(
                        other->GetBlockHash(), other->nHeight)) {
                    return true;
                }
            } else {
                if (!(other->nStatus & BLOCK_HAVE_DATA) ||
                    !other->IsValid(BLOCK_VALID_TRANSACTIONS) ||
                    !other->HaveNumChainTxs() ||
                    !IsBlockAuthenticated(*other, GetConsensus())) {
                    continue;
                }
                if (other->nAuthenticatedChainWork >=
                    candidate->nAuthenticatedChainWork) {
                    return true;
                }
            }
        }

        node::ReorgRecoveryRecord record{
            .version = node::ReorgRecoveryRecord::CURRENT_VERSION,
            .mode = mode,
            .fork_hash = fork->GetBlockHash(),
            .losing_tip_hash = active_tip->GetBlockHash(),
            .recovery_root_hash = recovery_root->GetBlockHash(),
            .authenticated_tip_hash = candidate->GetBlockHash(),
            .initial_reorg_depth = static_cast<uint32_t>(depth),
        };
        if (m_blockman.m_block_tree_db &&
            !m_blockman.m_block_tree_db->WriteReorgRecoveryRecord(record)) {
            LogError("%s: failed to persist shallow reorg recovery record\n",
                     __func__);
            return false;
        }
        m_reorg_recovery = std::move(record);
        if (mode == static_cast<uint8_t>(
                        node::ReorgRecoveryRecord::Mode::TRUSTED_AUTHORITY) &&
            !BlockIndexDescends(m_best_header, recovery_root)) {
            // The quorum itself is the current authority decision. The first
            // valid MMATTEST may arrive after this header was learned from a
            // then-unproven peer, so the header-follow path may not previously
            // have selected it. Make the durable recovery target the followed
            // target now; later authority headers can extend it normally.
            SetBestHeader(const_cast<CBlockIndex*>(candidate));
        }
        LogWarning("%s: armed automatic %s recovery at fork=%s initial_depth=%d recovery_root=%s authenticated_tip=%s\n",
                   __func__,
                   mode == static_cast<uint8_t>(
                               node::ReorgRecoveryRecord::Mode::TRUSTED_AUTHORITY)
                       ? "authority"
                       : "consensus-authenticated",
                   fork->GetBlockHash().ToString(), depth,
                   recovery_root->GetBlockHash().ToString(),
                   candidate->GetBlockHash().ToString());
    }

    const CBlockIndex* best_recovery{nullptr};
    const CBlockIndex* const root{
        m_blockman.LookupBlockIndex(m_reorg_recovery->recovery_root_hash)};
    for (const CBlockIndex* index : m_reorg_authenticated_candidate_tips) {
        if (!BlockIndexDescends(index, root) ||
            (index->nStatus & BLOCK_FAILED_MASK) ||
            !(index->nStatus & BLOCK_HAVE_DATA) ||
            !index->IsValid(BLOCK_VALID_TRANSACTIONS) ||
            !index->HaveNumChainTxs() ||
            !IsBlockAuthenticated(*index, GetConsensus())) {
            continue;
        }
        if (best_recovery == nullptr ||
            CBlockIndexWorkComparator()(best_recovery, index)) {
            best_recovery = index;
        }
    }
    // Uniqueness is evaluated once for the best maintained authenticated branch
    // tip. Neither this selection nor the ambiguity check scans the block index.
    if (best_recovery == nullptr ||
        !IsAutomaticReorgRecoveryCandidate(best_recovery)) {
        return true;
    }

    if (FindParkedReorgBranchRoot(best_recovery) != nullptr &&
        !UnparkReorgBranchContainingBlock(best_recovery)) {
        LogError("%s: failed to atomically unpark authenticated recovery branch %s\n",
                 __func__, best_recovery->GetBlockHash().ToString());
        return false;
    }
    for (Chainstate* chainstate : GetAll()) {
        chainstate->TryAddBlockIndexCandidate(
            const_cast<CBlockIndex*>(best_recovery));
    }
    return true;
}

bool ChainstateManager::NormalizeReorgRecovery(const CBlockIndex* active_tip)
{
    AssertLockHeld(::cs_main);
    if (!m_reorg_recovery.has_value()) return true;
    const node::ReorgRecoveryRecord& record{*m_reorg_recovery};
    const CBlockIndex* const fork{m_blockman.LookupBlockIndex(record.fork_hash)};
    const CBlockIndex* const losing_tip{
        m_blockman.LookupBlockIndex(record.losing_tip_hash)};
    const CBlockIndex* const recovery_root{
        m_blockman.LookupBlockIndex(record.recovery_root_hash)};
    const CBlockIndex* const authenticated_tip{
        m_blockman.LookupBlockIndex(record.authenticated_tip_hash)};
    const auto profile_settings = kernel::GetReorgProtectionProfileSettings(
        m_options.reorg_protection_profile);
    const uint32_t park_depth = m_options.max_reorg_depth_park.value_or(
        profile_settings.park_depth);
    const bool valid_mode{
        (record.mode == static_cast<uint8_t>(
                            node::ReorgRecoveryRecord::Mode::TRUSTED_AUTHORITY) &&
         m_options.matmul_validation_mode == kernel::MatMulValidationMode::TRUSTED) ||
        (record.mode == static_cast<uint8_t>(
                            node::ReorgRecoveryRecord::Mode::CONSENSUS_AUTHENTICATED) &&
         m_options.matmul_validation_mode == kernel::MatMulValidationMode::CONSENSUS)};
    const bool recovery_won{BlockIndexDescends(active_tip, recovery_root)};
    const bool structurally_valid{
        record.version == node::ReorgRecoveryRecord::CURRENT_VERSION &&
        valid_mode &&
        m_options.deep_reorg_action == kernel::DeepReorgAction::PARK &&
        park_depth != kernel::REORG_PROTECTION_DEPTH_DISABLED &&
        record.initial_reorg_depth > 0 &&
        record.initial_reorg_depth <= park_depth &&
        fork != nullptr && losing_tip != nullptr && recovery_root != nullptr &&
        authenticated_tip != nullptr && recovery_root->pprev == fork &&
        BlockIndexDescends(losing_tip, fork) &&
        static_cast<uint32_t>(losing_tip->nHeight - fork->nHeight) ==
            record.initial_reorg_depth &&
        BlockIndexDescends(authenticated_tip, recovery_root) &&
        (recovery_won || BlockIndexDescends(active_tip, losing_tip))};
    const bool trusted_record{
        record.mode == static_cast<uint8_t>(
                           node::ReorgRecoveryRecord::Mode::TRUSTED_AUTHORITY)};
    const bool provenance_valid{
        structurally_valid &&
        (trusted_record
             ? node::matmul_trusted::HasQuorum(
                   authenticated_tip->GetBlockHash(), authenticated_tip->nHeight)
             : (authenticated_tip->IsValid(BLOCK_VALID_TRANSACTIONS) &&
                (authenticated_tip->nStatus & BLOCK_HAVE_DATA) != 0 &&
                authenticated_tip->HaveNumChainTxs() &&
                IsBlockAuthenticated(*authenticated_tip, GetConsensus())))};

    // CONSENSUS only. TRUSTED records must keep reconstructing m_best_header
    // onto authenticated_tip while the authority body is still in flight.
    // Live 2026-08-15: CONSENSUS recovery armed at 01:38 on 1fbccf3f never
    // completed; the followed/quorum chain stayed on the "losing" side
    // through attested 189685, and every later tip-child was deferred.
    const bool followed_stayed_on_losing_side{
        m_best_header != nullptr && losing_tip != nullptr &&
        m_best_header->nHeight > losing_tip->nHeight &&
        BlockIndexDescends(m_best_header, losing_tip) &&
        !BlockIndexDescends(m_best_header, recovery_root)};
    bool quorum_on_losing_side{false};
    if (active_tip != nullptr && losing_tip != nullptr) {
        for (const CBlockIndex* walk{active_tip};
             walk != nullptr && walk->nHeight > losing_tip->nHeight;
             walk = walk->pprev) {
            if (node::matmul_trusted::HasQuorum(
                    walk->GetBlockHash(), walk->nHeight)) {
                quorum_on_losing_side = true;
                break;
            }
        }
    }
    const bool losing_side_now_canonical{
        !trusted_record && !recovery_won && active_tip != nullptr &&
        losing_tip != nullptr && BlockIndexDescends(active_tip, losing_tip) &&
        (quorum_on_losing_side || followed_stayed_on_losing_side)};
    if (recovery_won || !provenance_valid || losing_side_now_canonical) {
        if (m_blockman.m_block_tree_db &&
            !m_blockman.m_block_tree_db->WriteReorgRecoveryRecord(std::nullopt)) {
            LogError("%s: failed to clear stale reorg recovery record\n", __func__);
            return false;
        }
        LogWarning("%s: cleared %s reorg recovery record\n", __func__,
                   recovery_won ? "completed"
                   : losing_side_now_canonical ? "losing-side-became-canonical"
                                               : "stale or unauthenticated");
        m_reorg_recovery.reset();
        if (!recovery_won) {
            for (Chainstate* chainstate : GetAll()) {
                for (CBlockIndex* index : m_blockman.GetAllBlockIndices()) {
                    if (index->IsValid(BLOCK_VALID_TRANSACTIONS) &&
                        index->HaveNumChainTxs()) {
                        chainstate->TryAddBlockIndexCandidate(index);
                    }
                }
            }
        }
        return true;
    }
    if (trusted_record && !BlockIndexDescends(m_best_header, recovery_root)) {
        // Reconstruct the followed authority target on restart from the
        // durable, current-config-reverified quorum record.
        SetBestHeader(const_cast<CBlockIndex*>(authenticated_tip));
    }
    return MaybeTrackReorgRecovery(authenticated_tip);
}

bool ChainstateManager::LoadReorgRecoveryRecord()
{
    AssertLockHeld(::cs_main);
    if (!m_blockman.m_block_tree_db) {
        m_reorg_recovery.reset();
        return true;
    }
    if (!m_blockman.m_block_tree_db->ReadReorgRecoveryRecord(
            m_reorg_recovery)) {
        LogError("%s: failed to read durable reorg recovery record\n",
                 __func__);
        return false;
    }
    return true;
}

/** Mark a block as having its data received and checked (up to BLOCK_VALID_TRANSACTIONS). */
void ChainstateManager::ReceivedBlockTransactions(const CBlock& block, CBlockIndex* pindexNew, const FlatFilePos& pos)
{
    AssertLockHeld(cs_main);
    pindexNew->nTx = block.vtx.size();
    // Typically m_chain_tx_count will be 0 at this point, but it can be nonzero if this
    // is a pruned block which is being downloaded again, or if this is an
    // assumeutxo snapshot block which has a hardcoded m_chain_tx_count value from the
    // snapshot metadata. If the pindex is not the snapshot block and the
    // m_chain_tx_count value is not zero, assert that value is actually correct.
    auto prev_tx_sum = [](CBlockIndex& block) { return block.nTx + (block.pprev ? block.pprev->m_chain_tx_count : 0); };
    if (!Assume(pindexNew->m_chain_tx_count == 0 || pindexNew->m_chain_tx_count == prev_tx_sum(*pindexNew) ||
                pindexNew == GetSnapshotBaseBlock())) {
        LogWarning("Internal bug detected: block %d has unexpected m_chain_tx_count %i that should be %i (%s %s). Please report this issue here: %s\n",
            pindexNew->nHeight, pindexNew->m_chain_tx_count, prev_tx_sum(*pindexNew), CLIENT_NAME, FormatFullVersion(), CLIENT_BUGREPORT);
        pindexNew->m_chain_tx_count = 0;
    }
    pindexNew->nFile = pos.nFile;
    pindexNew->nDataPos = pos.nPos;
    pindexNew->nUndoPos = 0;
    pindexNew->nStatus |= BLOCK_HAVE_DATA;
    if (DeploymentActiveAt(*pindexNew, *this, Consensus::DEPLOYMENT_SEGWIT)) {
        pindexNew->nStatus |= BLOCK_OPT_WITNESS;
    }
    pindexNew->RaiseValidity(BLOCK_VALID_TRANSACTIONS);
    m_blockman.m_dirty_blockindex.insert(pindexNew);

    // Audit P0.1/C1: the block's body (and, at MatMul heights, its MatMul
    // product-committed proof) has now verified -- ContextualCheckBlock ran before
    // us. Promote this index's authenticated-work contribution from 0 to its full
    // GetBlockProof. Handles the case where pprev is not yet connected (the else
    // branch below); the descendant walk repeats this for every block that becomes
    // connected, in parent-before-child order.
    const arith_uint256 old_authenticated_work = pindexNew->nAuthenticatedChainWork;
    UpdateAuthenticatedChainWork(*pindexNew, GetConsensus());
    if (pindexNew->nAuthenticatedChainWork != old_authenticated_work) {
        auto consider_best_header = [this](CBlockIndex& candidate)
            EXCLUSIVE_LOCKS_REQUIRED(::cs_main) {
            AssertLockHeld(::cs_main);
            if (candidate.nStatus & BLOCK_FAILED_MASK) return;
            NoteAuthenticatedRecoveryCandidate(&candidate);
            if (m_best_header == nullptr ||
                PreferTrustAdjustedHeader(*m_best_header, candidate)) {
                SetBestHeader(&candidate);
            }
        };
        consider_best_header(*pindexNew);
        // Header-only descendants already in the index must inherit an updated
        // authenticated base. BlockManager's direct-child adjacency keeps this
        // proportional to the affected subtree. The same traversal updates the
        // best-header candidate incrementally, avoiding a second full-index scan.
        PropagateAuthenticatedChainWorkDescendants(*pindexNew, GetConsensus(),
            [this](CBlockIndex& parent,
                   const std::function<void(CBlockIndex&)>& visit)
                EXCLUSIVE_LOCKS_REQUIRED(::cs_main) {
                AssertLockHeld(::cs_main);
                m_blockman.ForEachBlockChild(parent, visit);
            },
            consider_best_header);
    }

    if (pindexNew->pprev == nullptr || pindexNew->pprev->HaveNumChainTxs()) {
        // If pindexNew is the genesis block or all parents are BLOCK_VALID_TRANSACTIONS.
        std::deque<CBlockIndex*> queue;
        queue.push_back(pindexNew);

        // Recursively process any descendant blocks that now may be eligible to be connected.
        while (!queue.empty()) {
            CBlockIndex *pindex = queue.front();
            queue.pop_front();
            // Before setting m_chain_tx_count, assert that it is 0 or already set to
            // the correct value. This assert will fail after receiving the
            // assumeutxo snapshot block if assumeutxo snapshot metadata has an
            // incorrect hardcoded AssumeutxoData::m_chain_tx_count value.
            if (!Assume(pindex->m_chain_tx_count == 0 || pindex->m_chain_tx_count == prev_tx_sum(*pindex))) {
                LogWarning("Internal bug detected: block %d has unexpected m_chain_tx_count %i that should be %i (%s %s). Please report this issue here: %s\n",
                   pindex->nHeight, pindex->m_chain_tx_count, prev_tx_sum(*pindex), CLIENT_NAME, FormatFullVersion(), CLIENT_BUGREPORT);
            }
            pindex->m_chain_tx_count = prev_tx_sum(*pindex);
            // Audit P0.1/C1: re-derive authenticated chainwork for each block that
            // just became connected. pprev is always processed before its children
            // here, so pprev->nAuthenticatedChainWork is final by the time we read it.
            UpdateAuthenticatedChainWork(*pindex, GetConsensus());
            pindex->nSequenceId = nBlockSequenceId++;
            for (Chainstate *c : GetAll()) {
                c->TryAddBlockIndexCandidate(pindex);
            }
            std::pair<std::multimap<CBlockIndex*, CBlockIndex*>::iterator, std::multimap<CBlockIndex*, CBlockIndex*>::iterator> range = m_blockman.m_blocks_unlinked.equal_range(pindex);
            while (range.first != range.second) {
                std::multimap<CBlockIndex*, CBlockIndex*>::iterator it = range.first;
                queue.push_back(it->second);
                range.first++;
                m_blockman.m_blocks_unlinked.erase(it);
            }
        }
    } else {
        if (pindexNew->pprev && pindexNew->pprev->IsValid(BLOCK_VALID_TREE)) {
            m_blockman.AddUnlinkedBlock(pindexNew);
        }
    }

    if (!MaybeTrackReorgRecovery(pindexNew)) {
        // Failure to persist this optional recovery policy does not invalidate
        // the block. The node simply retains the conservative legacy PARK
        // behavior, and the error remains visible to the operator.
        LogError("%s: failed to update durable reorg recovery state for block %s\n",
                 __func__, pindexNew->GetBlockHash().ToString());
    }
}

static bool CheckBlockHeader(const CBlockHeader& block, BlockValidationState& state, const Consensus::Params& consensusParams, bool fCheckPOW = true)
{
    // Check proof of work matches claimed amount
    if (fCheckPOW) {
        if (consensusParams.fMatMulPOW) {
            if (!CheckMatMulProofOfWork_Phase1(block, consensusParams)) {
                return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER, "high-hash", "matmul phase1 proof of work failed");
            }
        } else if (!consensusParams.fKAWPOW && !CheckProofOfWork(block.GetHash(), block.nBits, consensusParams)) {
            return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER, "high-hash", "proof of work failed");
        }
    }

    return true;
}

static bool CheckMerkleRoot(const CBlock& block, BlockValidationState& state)
{
    if (block.m_checked_merkle_root) return true;

    bool mutated;
    uint256 merkle_root = BlockMerkleRoot(block, &mutated);
    if (block.hashMerkleRoot != merkle_root) {
        return state.Invalid(
            /*result=*/BlockValidationResult::BLOCK_MUTATED,
            /*reject_reason=*/"bad-txnmrklroot",
            /*debug_message=*/"hashMerkleRoot mismatch");
    }

    // Check for merkle tree malleability (CVE-2012-2459): repeating sequences
    // of transactions in a block without affecting the merkle root of a block,
    // while still invalidating it.
    if (mutated) {
        return state.Invalid(
            /*result=*/BlockValidationResult::BLOCK_MUTATED,
            /*reject_reason=*/"bad-txns-duplicate",
            /*debug_message=*/"duplicate transaction");
    }

    block.m_checked_merkle_root = true;
    return true;
}

/** CheckWitnessMalleation performs checks for block malleation with regard to
 * its witnesses.
 *
 * Note: If the witness commitment is expected (i.e. `expect_witness_commitment
 * = true`), then the block is required to have at least one transaction and the
 * first transaction needs to have at least one input. */
static bool CheckWitnessMalleation(const CBlock& block, bool expect_witness_commitment, BlockValidationState& state)
{
    if (expect_witness_commitment) {
        if (block.m_checked_witness_commitment) return true;

        int commitpos = GetWitnessCommitmentIndex(block);
        if (commitpos != NO_WITNESS_COMMITMENT) {
            // Defence-in-depth: GetWitnessCommitmentIndex guarantees vtx is
            // non-empty, and CheckTransaction (run earlier in CheckBlock)
            // guarantees the coinbase has inputs.  Guard rather than assert
            // so a corrupted block cannot crash the node.
            if (block.vtx.empty() || block.vtx[0]->vin.empty()) {
                return state.Invalid(
                    /*result=*/BlockValidationResult::BLOCK_MUTATED,
                    /*reject_reason=*/"bad-witness-coinbase",
                    /*debug_message=*/strprintf("%s : coinbase missing for witness commitment", __func__));
            }
            const auto& witness_stack{block.vtx[0]->vin[0].scriptWitness.stack};

            if (witness_stack.size() != 1 || witness_stack[0].size() != 32) {
                return state.Invalid(
                    /*result=*/BlockValidationResult::BLOCK_MUTATED,
                    /*reject_reason=*/"bad-witness-nonce-size",
                    /*debug_message=*/strprintf("%s : invalid witness reserved value size", __func__));
            }

            // The malleation check is ignored; as the transaction tree itself
            // already does not permit it, it is impossible to trigger in the
            // witness tree.
            uint256 hash_witness = BlockWitnessMerkleRoot(block, /*mutated=*/nullptr);

            CHash256().Write(hash_witness).Write(witness_stack[0]).Finalize(hash_witness);
            if (memcmp(hash_witness.begin(), &block.vtx[0]->vout[commitpos].scriptPubKey[6], 32)) {
                return state.Invalid(
                    /*result=*/BlockValidationResult::BLOCK_MUTATED,
                    /*reject_reason=*/"bad-witness-merkle-match",
                    /*debug_message=*/strprintf("%s : witness merkle commitment mismatch", __func__));
            }

            block.m_checked_witness_commitment = true;
            return true;
        }
    }

    // No witness data is allowed in blocks that don't commit to witness data, as this would otherwise leave room for spam
    for (const auto& tx : block.vtx) {
        if (tx->HasWitness()) {
            return state.Invalid(
                /*result=*/BlockValidationResult::BLOCK_MUTATED,
                /*reject_reason=*/"unexpected-witness",
                /*debug_message=*/strprintf("%s : unexpected witness data found", __func__));
        }
    }

    return true;
}

static bool CheckReducedDataOutputLimits(const CTransaction& tx, BlockValidationState& state, const Consensus::Params& consensus_params)
{
    if (!consensus_params.fReducedDataLimits) return true;

    const auto is_p2mr_output = [](const CScript& script_pub_key) {
        int witness_version{-1};
        std::vector<unsigned char> witness_program;
        if (!script_pub_key.IsWitnessProgram(witness_version, witness_program)) return false;
        return witness_version == 2 && witness_program.size() == 32;
    };

    for (const auto& txout : tx.vout) {
        const size_t script_size{txout.scriptPubKey.size()};
        const bool is_op_return{script_size > 0 && txout.scriptPubKey[0] == OP_RETURN};

        if (is_op_return) {
            if (script_size > consensus_params.nMaxOpReturnBytes) {
                return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-txns-opreturn-size",
                                     strprintf("opreturn output script size %u exceeds max %u", static_cast<unsigned>(script_size), consensus_params.nMaxOpReturnBytes));
            }
            continue;
        }

        if (script_size > consensus_params.nMaxTxoutScriptPubKeyBytes) {
            return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-txns-scriptpubkey-size",
                                 strprintf("txout script size %u exceeds max %u", static_cast<unsigned>(script_size), consensus_params.nMaxTxoutScriptPubKeyBytes));
        }

        if (consensus_params.fEnforceP2MROnlyOutputs && !is_p2mr_output(txout.scriptPubKey)) {
            return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-txns-nonp2mr-output",
                                 "non-OP_RETURN outputs must be witness v2 P2MR");
        }
    }

    return true;
}

bool CheckBlock(const CBlock& block, BlockValidationState& state, const Consensus::Params& consensusParams, bool fCheckPOW, bool fCheckMerkleRoot)
{
    // These are checks that are independent of context.

    if (block.fChecked)
        return true;

    // Check that the header is valid (particularly PoW).  This is mostly
    // redundant with the call in AcceptBlockHeader.
    if (!CheckBlockHeader(block, state, consensusParams, fCheckPOW))
        return false;

    // Signet only: check block solution
    if (consensusParams.signet_blocks && fCheckPOW && !CheckSignetBlockSolution(block, consensusParams)) {
        return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-signet-blksig", "signet block signature validation failure");
    }

    if (consensusParams.fMatMulPOW) {
        const bool has_v2_payload = HasMatMulV2Payload(block);
        if (has_v2_payload && !IsMatMulV2PayloadSizeValid(block, consensusParams)) {
            // Payload vectors are not header-committed consensus fields; treat malformed
            // payloads as non-authoritative metadata and validate PoW from the header path.
            LogDebug(BCLog::NET, "Ignoring malformed matmul payload for block %s\n",
                     block.GetHash().ToString());
        }
    }

    // Check the merkle root.
    if (fCheckMerkleRoot && !CheckMerkleRoot(block, state)) {
        return false;
    }

    // All potential-corruption validation must be done before we do any
    // transaction validation, as otherwise we may mark the header as invalid
    // because we receive the wrong transactions for it.
    // Note that witness malleability is checked in ContextualCheckBlock, so no
    // checks that use witness data may be performed here.

    // Size limits
    if (block.vtx.empty()) {
        return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-blk-length", "size limits failed");
    }
    const uint64_t stripped_block_size = ::GetSerializeSize(TX_NO_WITNESS_WITH_SHIELDED(block));
    // MatMul v4.4 ENC-DR size accounting (tension-resolution §3.1-ii): at
    // DIGEST_RECOMPUTE heights the block body carries NO sketch (matrix_c_data
    // is empty by consensus rule — the miner emits a digest-only body and
    // ContextualCheckBlock rejects any inline sketch), so serialized_block_size
    // is headers + txs only and the 24 MB nMaxBlockSerializedSize ceiling is
    // untouched. On the regtest FLAT_SKETCH_INBLOCK replay path the in-block
    // sketch is counted here exactly as before.
    const uint64_t serialized_block_size = ::GetSerializeSize(TX_WITH_WITNESS(block));
    if (block.vtx.size() > consensusParams.nMaxBlockWeight / WITNESS_SCALE_FACTOR ||
        stripped_block_size > consensusParams.nMaxBlockWeight / WITNESS_SCALE_FACTOR ||
        serialized_block_size > consensusParams.nMaxBlockSerializedSize) {
        return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-blk-length", "size limits failed");
    }

    // First transaction must be coinbase, the rest must not be
    if (block.vtx.empty() || !block.vtx[0]->IsCoinBase())
        return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-cb-missing", "first tx is not coinbase");
    for (unsigned int i = 1; i < block.vtx.size(); i++)
        if (block.vtx[i]->IsCoinBase())
            return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-cb-multiple", "more than one coinbase");

    // Check transactions
    // Must check for duplicate inputs (see CVE-2018-17144)
    for (const auto& tx : block.vtx) {
        TxValidationState tx_state;
        if (!CheckTransaction(*tx, tx_state)) {
            // CheckBlock() does context-free validation checks. The only
            // possible failures are consensus failures.
            assert(tx_state.GetResult() == TxValidationResult::TX_CONSENSUS);
            return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, tx_state.GetRejectReason(),
                                 strprintf("Transaction check failed (tx hash %s) %s", tx->GetHash().ToString(), tx_state.GetDebugMessage()));
        }
        if (consensusParams.fReducedDataLimits &&
            !CheckReducedDataOutputLimits(*tx, state, consensusParams)) {
            return false;
        }
    }
    unsigned int nSigOps = 0;
    for (const auto& tx : block.vtx)
    {
        nSigOps += GetLegacySigOpCount(*tx);
    }
    if (nSigOps * WITNESS_SCALE_FACTOR > MAX_BLOCK_SIGOPS_COST)
        return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-blk-sigops", "out-of-bounds SigOpCount");

    if (fCheckPOW && fCheckMerkleRoot)
        block.fChecked = true;

    return true;
}

void ChainstateManager::UpdateUncommittedBlockStructures(CBlock& block, const CBlockIndex* pindexPrev) const
{
    int commitpos = GetWitnessCommitmentIndex(block);
    static const std::vector<unsigned char> nonce(32, 0x00);
    if (commitpos != NO_WITNESS_COMMITMENT && DeploymentActiveAfter(pindexPrev, *this, Consensus::DEPLOYMENT_SEGWIT) && !block.vtx[0]->HasWitness()) {
        CMutableTransaction tx(*block.vtx[0]);
        tx.vin[0].scriptWitness.stack.resize(1);
        tx.vin[0].scriptWitness.stack[0] = nonce;
        block.vtx[0] = MakeTransactionRef(std::move(tx));
    }
}

std::vector<unsigned char> ChainstateManager::GenerateCoinbaseCommitment(CBlock& block, const CBlockIndex* pindexPrev) const
{
    std::vector<unsigned char> commitment;
    int commitpos = GetWitnessCommitmentIndex(block);
    std::vector<unsigned char> ret(32, 0x00);
    if (commitpos == NO_WITNESS_COMMITMENT) {
        uint256 witnessroot = BlockWitnessMerkleRoot(block, nullptr);
        CHash256().Write(witnessroot).Write(ret).Finalize(witnessroot);
        CTxOut out;
        out.nValue = 0;
        out.scriptPubKey.resize(MINIMUM_WITNESS_COMMITMENT);
        out.scriptPubKey[0] = OP_RETURN;
        out.scriptPubKey[1] = 0x24;
        out.scriptPubKey[2] = 0xaa;
        out.scriptPubKey[3] = 0x21;
        out.scriptPubKey[4] = 0xa9;
        out.scriptPubKey[5] = 0xed;
        memcpy(&out.scriptPubKey[6], witnessroot.begin(), 32);
        commitment = std::vector<unsigned char>(out.scriptPubKey.begin(), out.scriptPubKey.end());
        CMutableTransaction tx(*block.vtx[0]);
        tx.vout.push_back(out);
        block.vtx[0] = MakeTransactionRef(std::move(tx));
    }
    UpdateUncommittedBlockStructures(block, pindexPrev);
    return commitment;
}

bool HasValidProofOfWork(const std::vector<CBlockHeader>& headers, const Consensus::Params& consensusParams)
{
    if (consensusParams.fMatMulPOW) {
        return std::all_of(headers.cbegin(), headers.cend(),
            [&](const auto& header) { return CheckMatMulProofOfWork_Phase1(header, consensusParams); });
    }
    if (consensusParams.fKAWPOW) {
        // KAWPOW verification is height-dependent and done in contextual header checks.
        return true;
    }
    return std::all_of(headers.cbegin(), headers.cend(),
            [&](const auto& header) { return CheckProofOfWork(header.GetHash(), header.nBits, consensusParams);});
}

bool IsBlockMutated(const CBlock& block, bool check_witness_root)
{
    BlockValidationState state;
    if (!CheckMerkleRoot(block, state)) {
        LogDebug(BCLog::VALIDATION, "Block mutated: %s\n", state.ToString());
        return true;
    }

    if (block.vtx.empty() || !block.vtx[0]->IsCoinBase()) {
        // Consider the block mutated if any transaction is 64 bytes in size (see 3.1
        // in "Weaknesses in Bitcoin’s Merkle Root Construction":
        // https://lists.linuxfoundation.org/pipermail/bitcoin-dev/attachments/20190225/a27d8837/attachment-0001.pdf).
        //
        // Note: This is not a consensus change as this only applies to blocks that
        // don't have a coinbase transaction and would therefore already be invalid.
        return std::any_of(block.vtx.begin(), block.vtx.end(),
                           [](auto& tx) { return GetSerializeSize(TX_NO_WITNESS_WITH_SHIELDED(tx)) == 64; });
    } else {
        // Theoretically it is still possible for a block with a 64 byte
        // coinbase transaction to be mutated but we neglect that possibility
        // here as it requires at least 224 bits of work.
    }

    if (!CheckWitnessMalleation(block, check_witness_root, state)) {
        LogDebug(BCLog::VALIDATION, "Block mutated: %s\n", state.ToString());
        return true;
    }

    return false;
}

arith_uint256 CalculateClaimedHeadersWork(std::span<const CBlockHeader> headers)
{
    arith_uint256 total_work{0};
    for (const CBlockHeader& header : headers) {
        CBlockIndex dummy(header);
        total_work += GetBlockProof(dummy);
    }
    return total_work;
}

std::optional<arith_uint256> CalculateClaimedHeadersWork(
    const CBlockIndex& chain_start,
    std::span<const CBlockHeader> headers,
    const Consensus::Params& consensusParams)
{
    if (headers.empty()) {
        return arith_uint256{0};
    }

    if (!consensusParams.fMatMulPOW) {
        return CalculateClaimedHeadersWork(headers);
    }

    std::list<CBlockIndex> recent_indices;
    CBlockIndex* previous_index = const_cast<CBlockIndex*>(&chain_start);
    int64_t next_height = static_cast<int64_t>(chain_start.nHeight) + 1;
    arith_uint256 total_work{0};

    for (const CBlockHeader& header : headers) {
        if (header.nBits != GetNextWorkRequired(previous_index, &header, consensusParams)) {
            return std::nullopt;
        }

        CBlockIndex dummy(header);
        total_work += GetBlockProof(dummy);

        if (next_height < 0 || next_height > std::numeric_limits<int>::max()) {
            return std::nullopt;
        }
        recent_indices.emplace_back(header);
        CBlockIndex& synthesized = recent_indices.back();
        synthesized.nHeight = static_cast<int>(next_height);
        synthesized.pprev = previous_index;
        previous_index = &synthesized;
        ++next_height;
    }

    return total_work;
}

/** Context-dependent validity checks.
 *  By "context", we mean only the previous block headers, but not the UTXO
 *  set; UTXO-related validity checks are done in ConnectBlock().
 *  NOTE: This function is not currently invoked by ConnectBlock(), so we
 *  should consider upgrade issues if we change which consensus rules are
 *  enforced in this function (eg by adding a new consensus rule). See comment
 *  in ConnectBlock().
 *  Note that -reindex-chainstate skips the validation that happens here!
 */
static bool ContextualCheckBlockHeader(const CBlockHeader& block,
                                       BlockValidationState& state,
                                       BlockManager& blockman,
                                       const ChainstateManager& chainman,
                                       const CBlockIndex* pindexPrev,
                                       bool fCheckPOW = true) EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);
    if (!pindexPrev) {
        return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER, "bad-prevblk-null",
            "ContextualCheckBlockHeader: pindexPrev is null");
    }
    if (pindexPrev->nHeight == std::numeric_limits<int>::max()) {
        return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER, "bad-blk-height-overflow", "block height overflow");
    }
    const int nHeight = pindexPrev->nHeight + 1;

    // Check proof of work
    const Consensus::Params& consensusParams = chainman.GetConsensus();
    if (block.nBits != GetNextWorkRequired(pindexPrev, &block, consensusParams))
        return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER, "bad-diffbits", "incorrect proof of work");

    // HeaderPoW bit-26 self-describing wire was WITHDRAWN: bit 26 was previously
    // legal, so gating 182↔186 parse / GetHash on it forks pre-activation peers.
    // Until a height-contextual wire design lands, do not require or forbid the
    // bit here. Activation of a commitment-format HeaderPoW remains a hard NO-GO.

    // Audit F1: header-PoW spam gate. At v4 heights the header-level PoW check is
    // only `matmul_digest <= target`, and matmul_digest is a self-declared field
    // (not a hash of the header), so header work is forgeable at zero cost -- a
    // header-flood / best-header-poisoning DoS vector. When enabled this requires
    // cheap UNFORGEABLE hash work per header. SINGLE ACTIVATION: no gate height of
    // its own -- it rides the v4 fork and is enabled by a non-sentinel
    // nMatMulHeaderPoWDiscountBits (default UINT32_MAX = disabled). NOT gated by
    // fSkipMatMulValidation: it is a one-hash relay/DoS defense, not an
    // expensive-verify correctness check.
    if (consensusParams.fMatMulPOW && consensusParams.IsMatMulV4Active(nHeight) &&
        consensusParams.IsMatMulHeaderPoWEnabled() &&
        !CheckMatMulHeaderSpamGate(block, consensusParams)) {
        return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER, "bad-matmul-header-pow",
                             "matmul header proof-of-work spam gate not satisfied");
    }

    // Deterministic seed enforcement: verify MatMul seeds match the active
    // height-gated derivation. This is done contextually (not in Phase 1)
    // because Phase 1 operates on header-only data without block index access.
    // Skip when fSkipMatMulValidation is true (default regtest without -test=matmulstrict).
    if (consensusParams.fMatMulPOW && !consensusParams.fSkipMatMulValidation && !block.hashPrevBlock.IsNull()) {
        // MatMul v4 (spec §I.1/§I.2): Phase1 (CheckBlockHeader) is
        // context-free and, when a v4 fork height is configured, leniently
        // accepts EITHER the v3 or v4 dimension. This is the first
        // height-aware check point, so enforce the exact height-gated
        // dimension here.
        if (consensusParams.IsMatMulV4Active(nHeight)) {
            // Spec §G.2/§G.4-#2: structural accepted-dimension bounds, enforced
            // ahead of the exact height-gated dimension below as defense in
            // depth (reject any out-of-range matmul_dim outright).
            if (block.matmul_dim < consensusParams.nMatMulV4MinDimension ||
                block.matmul_dim > consensusParams.nMatMulV4MaxDimension) {
                return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER,
                                     "bad-matmul-dim",
                                     "matmul v4 dimension out of range for this height");
            }
            if (block.matmul_dim != consensusParams.nMatMulV4Dimension) {
                return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER,
                                     "bad-matmul-dim",
                                     "matmul v4 dimension mismatch for this height");
            }
            // MatMul v4.2 / ENC-BMX4C (spec §1.3/§8.2): at BMX4C heights the
            // accepted-dim invariant additionally requires n % 32 == 0 (E8M0
            // block scales run along the contraction dim in blocks of 32).
            // Trivially true for the 4096/8192 (and regtest 256) dimensions,
            // enforced here as defense in depth. GetMatMulEncodingProfile is the
            // single profile selector (no second height compare, spec §8.2).
            // ENC-BMX4C-LT inherits the same E8M0 block-length gate (MatExpand
            // dequant still contracts over n with the BMX4 structural bound).
            {
                const auto enc = consensusParams.GetMatMulEncodingProfile(nHeight);
                if ((enc == Consensus::MatMulEncodingProfile::ENC_BMX4C ||
                     enc == Consensus::MatMulEncodingProfile::ENC_BMX4C_LT) &&
                    (block.matmul_dim % Consensus::BMX4C_SCALE_BLOCK_LENGTH) != 0) {
                    return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER,
                                         "bad-matmul-dim",
                                         "matmul bmx4c dimension not a multiple of the E8M0 block length");
                }
            }
        } else if (block.matmul_dim != consensusParams.nMatMulDimension) {
            return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER,
                                 "bad-matmul-dim",
                                 "matmul dimension mismatch for this height");
        }

        CBlockHeader expected_header{block};
        if (!SetDeterministicMatMulSeeds(expected_header, consensusParams, nHeight, pindexPrev->GetMedianTimePast())) {
            return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER,
                                 "bad-matmul-seeds",
                                 "matmul parent context unavailable for deterministic seed derivation");
        }
        if (block.seed_a != expected_header.seed_a || block.seed_b != expected_header.seed_b) {
            return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER,
                                 "bad-matmul-seeds",
                                 "matmul seeds do not match deterministic derivation for this height");
        }

        // MatMul v4 (spec §G.3): the pre-hash lottery gate is retired at v4
        // heights -- CheckMatMulPreHashGate is bypassed (returns true
        // unconditionally in effect) because v4 has no pre-hash step.
        if (fCheckPOW && !consensusParams.IsMatMulV4Active(nHeight) &&
            !CheckMatMulPreHashGate(block, consensusParams, nHeight)) {
            return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER,
                                 "high-hash",
                                 "matmul pre-hash proof failed");
        }
    }

    // MatMul phase2/Freivalds checks require full block payload and run in
    // ContextualCheckBlock(). ContextualCheckBlockHeader() remains header-only.
    if (fCheckPOW &&
        !consensusParams.fMatMulPOW &&
        consensusParams.fKAWPOW &&
        !consensusParams.fSkipKAWPOWValidation &&
        nHeight >= consensusParams.nKAWPOWHeight) {
        if (!CheckKAWPOWProofOfWork(block, nHeight, consensusParams)) {
            return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER, "high-hash", "kawpow proof of work failed");
        }
    }

    // Check against checkpoints
    if (chainman.m_options.checkpoints_enabled) {
        // Don't accept any forks from the main chain prior to last checkpoint.
        // GetLastCheckpoint finds the last checkpoint in MapCheckpoints that's in our
        // BlockIndex().
        const auto& checkpoint_data = chainman.GetParams().Checkpoints();
        const CBlockIndex* pcheckpoint = blockman.GetLastCheckpoint(checkpoint_data);
        if (pcheckpoint && nHeight < pcheckpoint->nHeight) {
            LogPrintf("ERROR: %s: forked chain older than last checkpoint (height %d)\n", __func__, nHeight);
            return state.Invalid(BlockValidationResult::BLOCK_CHECKPOINT, "bad-fork-prior-to-checkpoint");
        }

        // Check that the block chain matches the known block chain up to a checkpoint
        if (!checkpoint_data.CheckBlock(nHeight, block.GetHash())) {
            LogPrintf("ERROR: %s: rejected by checkpoint lock-in at %d\n", __func__, nHeight);
            return state.Invalid(BlockValidationResult::BLOCK_CHECKPOINT, "checkpoint-mismatch");
        }
    }

    // Check timestamp against prev
    if (block.GetBlockTime() <= pindexPrev->GetMedianTimePast())
        return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER, "time-too-old", "block's timestamp is too early");

    // Enforce BIP94-style timewarp protection on all relevant retarget heights.
    if (EnforceTimewarpProtectionAtHeight(consensusParams, nHeight) &&
        block.GetBlockTime() < pindexPrev->GetBlockTime() - MAX_TIMEWARP) {
        return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER, "time-timewarp-attack", "block's timestamp is too early for BIP94 timewarp protection");
    }

    if (auto max_time{consensusParams.MaxMatMulFutureBlockTime(nHeight, pindexPrev->GetMedianTimePast())};
        max_time.has_value()) {
        // a5 fix: never enforce a future-time cap below the BIP94 timewarp floor, or the legal
        // timestamp window can invert at the drift-cap activation boundary and wedge the chain.
        // Reconciliation only RAISES the cap and is flag-day gated.
        if (consensusParams.IsMatMulTimewarpReconcileActive(nHeight) &&
            EnforceTimewarpProtectionAtHeight(consensusParams, nHeight)) {
            max_time = std::max<int64_t>(*max_time, pindexPrev->GetBlockTime() - MAX_TIMEWARP);
        }
        if (block.GetBlockTime() > *max_time) {
            return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER, "time-mtp-too-new", "block timestamp too far ahead of median time past");
        }
    }

    // Check timestamp
    if (block.Time() > NodeClock::now() + std::chrono::seconds{MAX_FUTURE_BLOCK_TIME}) {
        return state.Invalid(BlockValidationResult::BLOCK_TIME_FUTURE, "time-too-new", "block timestamp too far in the future");
    }

    // Reject blocks with outdated version
    if ((block.nVersion < 2 && DeploymentActiveAfter(pindexPrev, chainman, Consensus::DEPLOYMENT_HEIGHTINCB)) ||
        (block.nVersion < 3 && DeploymentActiveAfter(pindexPrev, chainman, Consensus::DEPLOYMENT_DERSIG)) ||
        (block.nVersion < 4 && DeploymentActiveAfter(pindexPrev, chainman, Consensus::DEPLOYMENT_CLTV))) {
            return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER, strprintf("bad-version(0x%08x)", block.nVersion),
                                 strprintf("rejected nVersion=0x%08x block", block.nVersion));
    }

    if (IsThisSoftwareExpired(block.nTime)) {
        // Wait an extra day before we start rejecting blocks
        CBlockIndex const *blockindex_old = pindexPrev;
        for (int i{std::min<int>(144, pindexPrev->nHeight)}; i; --i) {
            if (!blockindex_old) {
                // Corrupted chain index; fail open (allow the block)
                LogWarning("ContextualCheckBlockHeader: broken pprev chain while walking software-expiry window\n");
                return true;
            }
            blockindex_old = blockindex_old->pprev;
        }
        if (!blockindex_old) {
            LogWarning("ContextualCheckBlockHeader: blockindex_old is null after expiry walk\n");
            return true;
        }
        if (IsThisSoftwareExpired(blockindex_old->GetMedianTimePast())) {
            return state.Invalid(BlockValidationResult::BLOCK_TIME_FUTURE, "node-expired", "node software has expired");
        }
    }

    return true;
}

namespace {
/** G.1: RAII scoped release of an already-held cs_main for the duration of an
 *  expensive, chainstate-INDEPENDENT computation, re-acquiring on scope exit
 *  (including on exception). Used to run the v4.4 ENC-DR reference recompute —
 *  a one-nonce O(W) GEMM (~seconds of CPU, ~100 MB transient at n=4096 on the
 *  cache/accel miss path) — WITHOUT serializing it under the node's global lock.
 *  Mirrors the LEAVE/ENTER_CRITICAL_SECTION longpoll precedent in
 *  rpc/mining.cpp, made exception-safe by RAII. Safe here because
 *  CheckMatMulProofOfWork_V4EncDr reads NO cs_main-guarded state (only the
 *  caller-owned block, immutable consensus params, and a local height; its only
 *  mutations are to the sketch cache and the phase2-budget counter, each under
 *  its OWN mutex), and only a pure bool crosses the release boundary. cs_main
 *  must be the innermost-held critical section at construction (it is, at the
 *  ENC-DR recompute point in ContextualCheckBlock). */
struct CsMainScopedRelease {
    CsMainScopedRelease() NO_THREAD_SAFETY_ANALYSIS { LEAVE_CRITICAL_SECTION(::cs_main); }
    ~CsMainScopedRelease() NO_THREAD_SAFETY_ANALYSIS { ENTER_CRITICAL_SECTION(::cs_main); }
    CsMainScopedRelease(const CsMainScopedRelease&) = delete;
    CsMainScopedRelease& operator=(const CsMainScopedRelease&) = delete;
};
} // namespace

bool ChainstateManager::IsMatMulRecomputeAssumeValidTrusted(const CBlockIndex* pindex_self, int nHeight) const
{
    AssertLockHeld(::cs_main);
    // Factored VERBATIM from ContextualCheckBlock's ASSUMEVALID BURIED-RECOMPUTE
    // TRUST clause (tension-resolution §3.1-vi); see the in-line rationale there.
    // Behavior must stay bit-identical to the historical in-line predicate: a
    // null pindex_self, unset -assumevalid, or missing best header always mean
    // "not trusted" (=> the recompute runs).
    if (AssumedValidBlock().IsNull() || m_best_header == nullptr || pindex_self == nullptr) return false;
    const auto av_it = m_blockman.m_block_index.find(AssumedValidBlock());
    if (av_it == m_blockman.m_block_index.end()) return false;
    if (av_it->second.GetAncestor(nHeight) != pindex_self) return false;
    if (m_best_header->GetAncestor(nHeight) != pindex_self) return false;
    if (m_best_header->nAuthenticatedChainWork < MinimumChainWork()) return false;
    return GetBlockProofEquivalentTime(*m_best_header, *pindex_self, *m_best_header, GetConsensus()) >
           static_cast<int64_t>(GetConsensus().nMatMulProofAssumeValidMinAge);
}

bool ChainstateManager::PersistMatMulExactReplayVerdict(
    const uint256& block_hash)
{
    AssertLockHeld(::cs_main);
    CBlockIndex* index{m_blockman.LookupBlockIndex(block_hash)};
    if (index == nullptr || (index->nStatus & BLOCK_FAILED_MASK)) return false;
    index->nStatus |= BLOCK_EXACT_REPLAY_VERIFIED;
    m_blockman.m_dirty_blockindex.insert(index);
    CacheMatMulEncDrVerdict(block_hash, true);
    if (GetMatMulValidationMode() ==
            kernel::MatMulValidationMode::CONSENSUS &&
        GetConsensus().IsMatMulTrustedReplayAttestationActive(
            index->nHeight) &&
        node::matmul_trusted::HasLocalSigner() &&
        ActiveChain().Contains(index)) {
        const auto result{
            node::matmul_trusted::SignAuthoritative(
                block_hash, index->nHeight)};
        if (result !=
                matmul::trusted::AddResult::Accepted &&
            result !=
                matmul::trusted::AddResult::Duplicate) {
            LogWarning(
                "Unable to create local ExactReplay attestation "
                "block=%s height=%d result=%s\n",
                block_hash.ToString(), index->nHeight,
                matmul::trusted::AddResultName(result));
        }
    }
    return true;
}

bool ChainstateManager::PersistMatMulTrustedReplayAttestation(
    const uint256& block_hash)
{
    AssertLockHeld(::cs_main);
    CBlockIndex* index{m_blockman.LookupBlockIndex(block_hash)};
    if (index == nullptr || (index->nStatus & BLOCK_FAILED_MASK) ||
        !node::matmul_trusted::IsConfigured() ||
        (GetMatMulValidationMode() != kernel::MatMulValidationMode::TRUSTED &&
         GetMatMulValidationMode() != kernel::MatMulValidationMode::CONSENSUS) ||
        !GetConsensus().IsMatMulTrustedReplayAttestationActive(
            index->nHeight) ||
        !node::matmul_trusted::HasQuorum(block_hash, index->nHeight)) {
        return false;
    }
    if ((index->nStatus & BLOCK_TRUSTED_REPLAY_ATTESTED) == 0) {
        index->nStatus |= BLOCK_TRUSTED_REPLAY_ATTESTED;
        m_blockman.m_dirty_blockindex.insert(index);
    }

    auto reconsider = [this](CBlockIndex& candidate)
        EXCLUSIVE_LOCKS_REQUIRED(::cs_main) {
        AssertLockHeld(::cs_main);
        if (candidate.nStatus & BLOCK_FAILED_MASK) return;
        NoteAuthenticatedRecoveryCandidate(&candidate);
        if ((candidate.nStatus & BLOCK_HAVE_DATA) != 0 &&
            candidate.IsValid(BLOCK_VALID_TRANSACTIONS) &&
            candidate.HaveNumChainTxs()) {
            for (Chainstate* chainstate : GetAll()) {
                chainstate->TryAddBlockIndexCandidate(&candidate);
            }
        }
        if (m_best_header == nullptr ||
            PreferTrustAdjustedHeader(*m_best_header, candidate)) {
            SetBestHeader(&candidate);
        }
    };

    // The body may have reached BLOCK_VALID_TRANSACTIONS before quorum. Its
    // authenticated-work contribution was therefore zero and descendants
    // inherited that stale total. Promote the complete subtree now, then put
    // every connectable block back into the candidate set: FindMostWorkChain
    // may have removed the branch while it was still unattested.
    UpdateAuthenticatedChainWork(*index, GetConsensus());
    reconsider(*index);
    PropagateAuthenticatedChainWorkDescendants(
        *index, GetConsensus(),
        [this](CBlockIndex& parent,
               const std::function<void(CBlockIndex&)>& visit)
            EXCLUSIVE_LOCKS_REQUIRED(::cs_main) {
            AssertLockHeld(::cs_main);
            m_blockman.ForEachBlockChild(parent, visit);
        },
        reconsider);

    // Deliberately do not populate the replay memo here. Callers must continue
    // to query current-config quorum; this persistent audit bit must never
    // recreate authority by itself after restart or key rotation.
    return true;
}

std::optional<ChainstateManager::MatMulEncDrClassifyResult>
ChainstateManager::ClassifyMatMulEncDrRecompute(const CBlock& block,
                                                bool* verdict_pinned,
                                                bool* assumevalid_trusted) const
{
    AssertLockHeld(::cs_main);
    if (verdict_pinned != nullptr) *verdict_pinned = false;
    if (assumevalid_trusted != nullptr) *assumevalid_trusted = false;
    const Consensus::Params& params = GetConsensus();
    if (!params.fMatMulPOW) return std::nullopt;
    const CBlockIndex* prev = m_blockman.LookupBlockIndex(block.hashPrevBlock);
    // An unknown or overflowing prev means AcceptBlock cannot reach the ENC-DR
    // seam for this delivery (it fails/queues upstream): keep it synchronous.
    if (prev == nullptr || prev->nHeight == std::numeric_limits<int>::max()) return std::nullopt;
    const int32_t nHeight = prev->nHeight + 1;
    if (!params.IsMatMulV4Active(nHeight)) return std::nullopt;
    if (params.GetMatMulProfileParams(nHeight).commitment !=
        Consensus::MatMulCommitmentScheme::DIGEST_RECOMPUTE) return std::nullopt;
    // Trusted Profile-1 tip-children must go through AcceptBlock so HAVE_DATA
    // is written even before quorum. Parking them in the async worker leaves
    // the body only in RAM; a consensus signer peered with this mirror cannot
    // getdata it, so never attests, so the mirror never connects (chicken-egg).
    if (GetMatMulValidationMode() == kernel::MatMulValidationMode::TRUSTED &&
        params.IsMatMulTrustedReplayAttestationActive(nHeight) &&
        prev == ActiveTip()) {
        return std::nullopt;
    }
    bool stage3_authority_job{false};
    if constexpr (matmul::v4::rc::kRCStage3SuccinctAuthorityReady) {
        stage3_authority_job = params.IsMatMulRCFamilyActive(nHeight);
    }
    // Do not enqueue an oversized proof body only to parse/verify it before
    // the ordinary contextual weight check. Stage-3 bytes are non-witness
    // block bytes and therefore receive the full weight multiplier.
    if (stage3_authority_job &&
        GetBlockWeight(block) >
            static_cast<int64_t>(params.nMaxBlockWeight)) {
        return std::nullopt;
    }
    // A/B payloads remain forbidden. A C payload is admissible only for the
    // durable Stage-3 authority: its proof body is verified off-thread and is
    // cached by (header, registry, payload), never by the header alone.
    if (!block.matrix_a_data.empty() || !block.matrix_b_data.empty() ||
        (!stage3_authority_job && !block.matrix_c_data.empty())) {
        return std::nullopt;
    }
    // Phase B seal-as-PoW: tip verify needs parent MTP for sibling-slot V3
    // seeds. Prev is known here, so MTP is always available — allow async
    // enqueue and thread MTP into the worker Job. CheckMatMulProofOfWork_V4EncDr
    // still fails closed if a Job somehow lacks MTP at seal heights.
    if (const CBlockIndex* existing = m_blockman.LookupBlockIndex(block.GetHash())) {
        // Already have the data: AcceptBlock short-circuits before the seam.
        if (existing->nStatus & BLOCK_HAVE_DATA) return std::nullopt;
        // Known-invalid: AcceptBlockHeader rejects it for free as "duplicate-invalid".
        // A failed block stores no data (BLOCK_HAVE_DATA stays unset), so without this
        // a re-delivered bad block would be enqueued and fully re-recomputed on EVERY
        // delivery (DoS). Route it synchronous — it fails cheaply, no worker slot spent.
        if (existing->nStatus & BLOCK_FAILED_MASK) return std::nullopt;
        // Assumevalid-buried: independent validators may use the historical
        // recompute shortcut. A Profile-1 trusted mirror must still obtain its
        // current configured signature quorum; generic assumevalid is not a
        // substitute for that explicit operator trust policy.
        const bool trusted_profile1{
            GetMatMulValidationMode() ==
                kernel::MatMulValidationMode::TRUSTED &&
            params.IsMatMulTrustedReplayAttestationActive(nHeight)};
        if (!trusted_profile1 &&
            IsMatMulRecomputeAssumeValidTrusted(existing, nHeight)) {
            if (assumevalid_trusted != nullptr) *assumevalid_trusted = true;
            return std::nullopt;
        }
    }
    // The legacy verdict memo is header-keyed. Stage-3 attachments are body
    // data, so only VerifyRCStage3ConsensusAttachment's proof-aware cache may
    // short-circuit them.
    if (!stage3_authority_job) {
        if (const CBlockIndex* existing{
                m_blockman.LookupBlockIndex(block.GetHash())};
            existing != nullptr &&
            (existing->nStatus & BLOCK_EXACT_REPLAY_VERIFIED) &&
            !(existing->nStatus & BLOCK_FAILED_MASK)) {
            CacheMatMulEncDrVerdict(block.GetHash(), true);
            return std::nullopt;
        }
        if (verdict_pinned != nullptr) {
            if (PinCachedMatMulEncDrVerdict(block.GetHash()).has_value()) {
                *verdict_pinned = true;
                return std::nullopt;
            }
        } else if (LookupMatMulEncDrVerdict(block.GetHash()).has_value()) {
            return std::nullopt;
        }
    }
    return MatMulEncDrClassifyResult{nHeight, prev->GetMedianTimePast()};
}

static bool ContextualCheckBlockBodyOnly(const CBlock& block,
                                         BlockValidationState& state,
                                         const ChainstateManager& chainman,
                                         const CBlockIndex* pindexPrev)
{
    if (pindexPrev != nullptr && pindexPrev->nHeight == std::numeric_limits<int>::max()) {
        return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER,
                             "bad-blk-height-overflow", "block height overflow");
    }
    const int nHeight{pindexPrev == nullptr ? 0 : pindexPrev->nHeight + 1};
    const Consensus::Params& consensus_params{chainman.GetConsensus()};
    const bool enforce_mtp{
        pindexPrev != nullptr && DeploymentActiveAfter(pindexPrev, chainman, Consensus::DEPLOYMENT_CSV)};
    const int64_t lock_time_cutoff{enforce_mtp ? pindexPrev->GetMedianTimePast()
                                               : block.GetBlockTime()};

    for (const auto& tx : block.vtx) {
        if (!IsFinalTx(*tx, nHeight, lock_time_cutoff)) {
            return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                 "bad-txns-nonfinal", "non-final transaction");
        }
    }

    if (DeploymentActiveAfter(pindexPrev, chainman, Consensus::DEPLOYMENT_HEIGHTINCB)) {
        const CScript expect{CScript() << nHeight};
        if (block.vtx[0]->vin[0].scriptSig.size() < expect.size() ||
            !std::equal(expect.begin(), expect.end(), block.vtx[0]->vin[0].scriptSig.begin())) {
            return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                 "bad-cb-height", "block height mismatch in coinbase");
        }
    }

    if (!CheckWitnessMalleation(
            block, DeploymentActiveAfter(pindexPrev, chainman, Consensus::DEPLOYMENT_SEGWIT), state)) {
        return false;
    }
    if (GetBlockWeight(block) > static_cast<int64_t>(consensus_params.nMaxBlockWeight)) {
        return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-blk-weight",
                             "ContextualCheckBlock : weight limit failed");
    }
    return true;
}

bool ChainstateManager::IsMatMulFollowedHistoricalHole(const CBlockIndex* index) const
{
    AssertLockHeld(::cs_main);
    if (index == nullptr) return false;
    const CBlockIndex* const tip{ActiveTip()};
    if (tip != nullptr && index->nHeight < tip->nHeight &&
        tip->GetAncestor(index->nHeight) == index) {
        return true;
    }
    const CBlockIndex* const snap{GetSnapshotBaseBlock()};
    if (snap != nullptr && index->nHeight <= snap->nHeight &&
        snap->GetAncestor(index->nHeight) == index) {
        return true;
    }
    return false;
}

bool ChainstateManager::CheckMatMulBlockAdmissionPreconditions(
    const CBlock& block,
    BlockValidationState& state,
    bool force_processing,
    bool min_pow_checked,
    bool& reaches_contextual_check)
{
    AssertLockHeld(::cs_main);
    reaches_contextual_check = false;
    if (!CheckBlock(block, state, GetConsensus())) return false;

    CBlockIndex* pindex{nullptr};
    if (!AcceptBlockHeader(block, state, &pindex, min_pow_checked)) return false;
    if (pindex == nullptr || !ContextualCheckBlockBodyOnly(block, state, *this, pindex->pprev)) return false;

    if (pindex->nStatus & BLOCK_HAVE_DATA) return true;
    if (!force_processing) {
        // Followed-chain historical holes are ancestors, so they always have
        // less work than the tip and may sit below nMinimumChainWork. Pruned
        // holes also have nTx != 0 with !HAVE_DATA. Those Bitcoin Core
        // unrequested anti-DoS gates must not drop a body we already know
        // belongs on the active / snapshot chain.
        if (!IsMatMulFollowedHistoricalHole(pindex)) {
            if (pindex->nTx != 0) return true;
            if (ActiveTip() != nullptr && pindex->nChainWork < ActiveTip()->nChainWork) return true;
            if (pindex->nHeight > ActiveHeight() + int(MIN_BLOCKS_TO_KEEP)) return true;
            if (pindex->nChainWork < MinimumChainWork()) return true;
        }
    }
    reaches_contextual_check = true;
    return true;
}

enum class MatMulRCAuthorityProvenance {
    NONE,
    LOCAL_EXACT_REPLAY,
    TRUSTED_SIGNER_QUORUM,
};

/** NOTE: This function is not currently invoked by ConnectBlock(), so we
 *  should consider upgrade issues if we change which consensus rules are
 *  enforced in this function (eg by adding a new consensus rule). See comment
 *  in ConnectBlock().
 *  Note that -reindex-chainstate skips the validation that happens here!
 */
/** Maximum coinbase scriptSig size once the content-elimination fork is active.
 *  Enough for the BIP34 serialized-height push plus a modest extranonce, but far
 *  below the 100-byte consensus limit that a miner could otherwise use as a data
 *  channel. See doc/btx-inscription-elimination-plan.md Pillar 5. */
static constexpr size_t MAX_CONTENT_COINBASE_SCRIPTSIG{40};

/** BTX content-elimination hard fork (doc/btx-inscription-elimination-plan.md).
 *  Height-gated block rules that remove the data-carriage channels that
 *  inscription/NFT/token meta-protocols depend on, while preserving every
 *  financial surface. Enforced from both ContextualCheckBlock and ConnectBlock
 *  so normal admission and -reindex-chainstate apply identical flag-day rules
 *  while leaving legacy history untouched. */
static bool CheckContentEliminationRules(const CBlock& block, int nHeight,
                                         const Consensus::Params& consensus_params,
                                         BlockValidationState& state)
{
    if (!consensus_params.IsContentEliminationActive(nHeight)) return true;
    if (block.vtx.empty()) return true;

    // Pillar 1: no OP_RETURN data channel. The coinbase may carry only the
    // mandatory witness-commitment OP_RETURN; every other OP_RETURN output --
    // coinbase or not -- is forbidden. This removes the <=83-byte marker lane
    // that off-chain content/token meta-protocols anchor on.
    const int commitpos = GetWitnessCommitmentIndex(block);
    for (size_t i = 0; i < block.vtx.size(); ++i) {
        const CTransaction& tx = *block.vtx[i];
        const bool is_coinbase = (i == 0);
        for (size_t o = 0; o < tx.vout.size(); ++o) {
            const CScript& spk = tx.vout[o].scriptPubKey;
            if (spk.empty() || spk[0] != OP_RETURN) continue;
            if (is_coinbase && commitpos != NO_WITNESS_COMMITMENT &&
                static_cast<int>(o) == commitpos) {
                continue; // the witness commitment is the only permitted OP_RETURN
            }
            return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                 is_coinbase ? "bad-cb-opreturn" : "bad-txns-opreturn-forbidden",
                                 "OP_RETURN data outputs are disabled on BTX");
        }
    }

    // Pillar 5: bound the coinbase scriptSig to the BIP34 height push plus a
    // small extranonce, removing the residual ~100-byte miner data channel. The
    // BIP34 height prefix itself is verified separately below in
    // ContextualCheckBlock; here we only cap the total size.
    const size_t cb_scriptsig{block.vtx[0]->vin[0].scriptSig.size()};
    if (cb_scriptsig > MAX_CONTENT_COINBASE_SCRIPTSIG) {
        return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-cb-scriptsig-content",
                             strprintf("coinbase scriptSig size %u exceeds content-elimination max %u",
                                       static_cast<unsigned>(cb_scriptsig),
                                       static_cast<unsigned>(MAX_CONTENT_COINBASE_SCRIPTSIG)));
    }

    return true;
}

static bool ContextualCheckBlock(const CBlock& block,
                                 BlockValidationState& state,
                                 const ChainstateManager& chainman,
                                 const CBlockIndex* pindexPrev,
                                 bool fCheckPOW = true,
                                 // E: gates whether the ENC-DR recompute below is permitted to
                                 // release cs_main (see CsMainScopedRelease). AcceptBlock (the hot
                                 // P2P path) passes true so the O(W) recompute never serializes
                                 // under the global lock. TestBlockValidity passes false: it holds
                                 // a stack-local CCoinsViewCache/CBlockIndex and a
                                 // pindexPrev==Tip() precondition ACROSS this call and then runs
                                 // ConnectBlock against them, so a mid-call tip advance by a
                                 // concurrent thread must not be possible. Holding cs_main across
                                 // the recompute is harmless there (not the hot path, and its
                                 // fCheckPOW=true callers are single-threaded).
                                 bool may_release_cs_main = true,
                                 MatMulRCAuthorityProvenance* rc_authority = nullptr) EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);
    if (rc_authority != nullptr) {
        *rc_authority = MatMulRCAuthorityProvenance::NONE;
    }
    if (pindexPrev != nullptr && pindexPrev->nHeight == std::numeric_limits<int>::max()) {
        return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "bad-blk-height-overflow", "block height overflow");
    }
    const int nHeight = pindexPrev == nullptr ? 0 : pindexPrev->nHeight + 1;
    const int best_known_height = chainman.m_best_header != nullptr
        ? chainman.m_best_header->nHeight
        : nHeight;
    const bool is_ibd = chainman.IsInitialBlockDownload();
    const Consensus::Params& consensusParams = chainman.GetConsensus();

    if (fCheckPOW && consensusParams.fMatMulPOW && consensusParams.IsMatMulV4Active(nHeight)) {
        // MatMul v4 (spec §I.2): exclusive cascade at and above nMatMulV4Height
        // -- no v3 Phase2/Freivalds/ProductCommitted fallback, no transcript
        // path, no dual-algorithm grace period.
        if (!block.matrix_a_data.empty() || !block.matrix_b_data.empty()) {
            return state.Invalid(BlockValidationResult::BLOCK_MUTATED, "v4-forbidden-ab-payload",
                "matmul v4 block carries forbidden legacy A/B matrix payload");
        }
        const Consensus::MatMulProfileParams matmul_profile = consensusParams.GetMatMulProfileParams(nHeight);
        if (matmul_profile.commitment == Consensus::MatMulCommitmentScheme::DIGEST_RECOMPUTE) {
            // Stage 3 replaces the header-only/sampled dispatch only after its
            // compile-time authority gate is deliberately closed. Its proof is
            // durable matrix_c_data, so all parse/binding/relation failures are
            // BLOCK_MUTATED: the header hash is unchanged and a valid full
            // block body may still exist (notably after compact reconstruction,
            // whose wire format carries no Stage-3 body).
            if constexpr (matmul::v4::rc::kRCStage3SuccinctAuthorityReady) {
                if (consensusParams.IsMatMulRCFamilyActive(nHeight)) {
                    // The complete proof parser is bounded independently, but
                    // a consensus-invalid oversized body must be rejected
                    // before any recursive verification work is attempted.
                    if (GetBlockWeight(block) >
                        static_cast<int64_t>(
                            consensusParams.nMaxBlockWeight)) {
                        return state.Invalid(
                            BlockValidationResult::BLOCK_CONSENSUS,
                            "bad-blk-weight",
                            "ContextualCheckBlock : weight limit failed");
                    }
                    const auto target = DeriveTarget(block.nBits, consensusParams.powLimit);
                    if (!target.has_value()) {
                        return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS,
                                             "bad-matmul-stage3-target",
                                             "matmul Stage-3 target is noncanonical");
                    }
                    matmul::v4::rc::RCStage3AttachmentStatus stage3_status;
                    std::string stage3_why;
                    if (may_release_cs_main) {
                        CsMainScopedRelease release_cs_main_for_stage3;
                        stage3_status =
                            matmul::v4::rc::VerifyRCStage3ConsensusAttachment(
                                block, consensusParams, nHeight,
                                ArithToUint256(*target), &stage3_why);
                    } else {
                        stage3_status =
                            matmul::v4::rc::VerifyRCStage3ConsensusAttachment(
                                block, consensusParams, nHeight,
                                ArithToUint256(*target), &stage3_why);
                    }
                    // The status -> verdict mapping lives in
                    // matmul_v4_rc_stage3_producer.h so it can be unit-tested
                    // while this whole branch is compiled out by the authority
                    // gate above (see RCStage3ConsensusVerdictFor). Behaviour is
                    // unchanged from the inline form it replaces, except that an
                    // unexpected status now fails closed to BLOCK_CONSENSUS
                    // instead of falling through.
                    const auto stage3_verdict =
                        matmul::v4::rc::RCStage3ConsensusVerdictFor(stage3_status);
                    switch (stage3_verdict.action) {
                    case matmul::v4::rc::RCStage3ConsensusAction::AcceptProceed:
                        return ContextualCheckBlockBodyOnly(
                            block, state, chainman, pindexPrev);
                    case matmul::v4::rc::RCStage3ConsensusAction::RejectMutation:
                        return state.Invalid(
                            BlockValidationResult::BLOCK_MUTATED,
                            stage3_verdict.reject_reason,
                            strprintf("matmul Stage-3 proof body rejected: %s",
                                      stage3_why));
                    case matmul::v4::rc::RCStage3ConsensusAction::RejectConsensus:
                        break;
                    }
                    return state.Invalid(
                        BlockValidationResult::BLOCK_CONSENSUS,
                        stage3_verdict.reject_reason,
                        strprintf("matmul Stage-3 authority failed closed: %s",
                                  stage3_why));
                }
            }
            // v4.4 ENC-DR path (doc/btx-matmul-v4.4-tension-resolution.md §4.1):
            // the block carries ZERO consensus proof bytes. The header's
            // matmul_digest = H(sigma||Chat_true) is the whole commitment; the
            // predicate is a pure function of the header, decided by exact
            // recompute (the consensus definition, epsilon = 0) or by the
            // cache-assisted Freivalds fast path (epsilon <= 2^-180) inside
            // CheckMatMulProofOfWork_V4EncDr.
            //
            // (1) The in-block body sketch MUST be empty (§4.1 clause 2). A
            // non-empty inline sketch at an ENC-DR height is classified as a
            // body mutation (BLOCK_MUTATED, non-permanent): matrix_c_data is
            // body data not covered by the header hash, so a relayer could
            // append garbage bytes to an honest digest-only block — the honest
            // empty-body block with the same header hash must not be poisoned.
            if (!block.matrix_c_data.empty()) {
                return state.Invalid(BlockValidationResult::BLOCK_MUTATED, "v4-encdr-nonempty-sketch",
                    "matmul v4.4 ENC-DR block carries a forbidden inline sketch payload");
            }
            // (2) ASSUMEVALID BURIED-RECOMPUTE TRUST (tension-resolution
            // §3.1-vi; retargeted byte-for-byte from the retired segregated
            // buried-proof trust, design §3.5-2). Below the configured
            // assumevalid block, a node TRUSTS that the buried chain's PoW
            // recomputes were verified by the network — the IDENTICAL trust
            // ConnectBlock already extends to buried scriptSigs — instead of
            // re-running the O(W) Chat recompute for every historical block.
            // This predicate is the SOLE bound on deep-history recompute cost
            // for a default node's IBD; a fully-verifying node
            // (-assumevalid=0 => AssumedValidBlock null) NEVER trusts and
            // always recomputes. The trust condition mirrors the ConnectBlock
            // fScriptChecks skip EXACTLY: the block is an assumed-valid
            // ancestor of the best header; the best header carries at least
            // MinimumChainWork of AUTHENTICATED work (C1 — a forged-header
            // chain cannot induce the skip); and it is buried more than the
            // 2-week equivalent-time DoS guard. When trusted, the recompute is
            // skipped for this block (every buried block stays re-auditable
            // forever by digest-only recompute — re-derivability underneath
            // the trust, §4.4).
            // (Predicate factored to ChainstateManager::IsMatMulRecomputeAssumeValidTrusted
            // so the WP-7 async-verify dispatcher and the WP-8 sketch-prefetch
            // guard share this single implementation; behavior unchanged.)
            const bool trusted_profile1{
                chainman.GetMatMulValidationMode() ==
                    kernel::MatMulValidationMode::TRUSTED &&
                consensusParams.IsMatMulTrustedReplayAttestationActive(
                    nHeight)};
            const bool recompute_assumevalid_trusted =
                !trusted_profile1 &&
                (chainman.IsMatMulRecomputeAssumeValidTrusted(
                    chainman.m_blockman.LookupBlockIndex(block.GetHash()), nHeight) ||
                // P2P admission may have made the same trust decision under
                // cs_main immediately before this call. Keep that decision
                // scoped across ProcessNewBlock so a concurrent best-header
                // branch change cannot turn the cheap path into an unbudgeted
                // recomputation. The pin is not an exact-verdict memo and is
                // released as soon as this validation re-entry completes.
                IsMatMulEncDrAssumeValidTrustPinned(block.GetHash()));
            // (3) Above assumevalid (or -assumevalid=0): decide the §4.1
            // predicate. Any failure is a header-level PERMANENT consensus
            // fault: with an empty body there are no proof bytes to mutate, so
            // the MUTATED/permanent classification collapses (§4.1 clause 2)
            // and "high-hash" is always correct here.
            if (!recompute_assumevalid_trusted) {
                // G.1: run the ENC-DR predicate WITHOUT cs_main held. On the
                // cache/accel-miss path it performs the full O(W) one-nonce
                // reference GEMM; holding the global lock across it would let a
                // header-PoW-paying attacker (or a near-tip IBD node) serialize
                // seconds of CPU under cs_main per non-cached block. The check is
                // a pure function of the header + immutable params and reads no
                // cs_main-guarded state (see CsMainScopedRelease), and every
                // cs_main-dependent decision above (empty-body classification,
                // assumevalid-trust) is already made; only this bool crosses the
                // release boundary.
                bool encdr_ok;
                bool rc_local_execution_failure{false};
                std::string rc_execution_detail;
                const auto check_rc = [&]() {
                    if (trusted_profile1) {
                        // Never block ProcessNewBlock on attestation quorum.
                        // Tip-extending bodies persist via AcceptBlock so a
                        // consensus signer can getdata them before quorum;
                        // ConnectTip still requires HasQuorum. Competing
                        // (non-tip-parent) deliveries stay retryable Errors.
                        const bool have_quorum{
                            node::matmul_trusted::HasQuorum(
                                block.GetHash(), nHeight)};
                        if (!have_quorum) {
                            if (pindexPrev != nullptr &&
                                pindexPrev == chainman.ActiveTip()) {
                                return true;
                            }
                            // Followed-chain historical hole (active-tip
                            // ancestor or assumeutxo snapshot-base ancestor):
                            // persist HAVE_DATA without GPU so background
                            // backfill is not a getdata livelock. ConnectTip
                            // still requires HasQuorum. Competing off-path
                            // bodies stay retryable Errors.
                            bool followed_hole{false};
                            {
                                LOCK(::cs_main);
                                followed_hole =
                                    chainman.IsMatMulFollowedHistoricalHole(
                                        chainman.m_blockman.LookupBlockIndex(
                                            block.GetHash()));
                            }
                            if (followed_hole) return true;
                            rc_local_execution_failure = true;
                            rc_execution_detail = strprintf(
                                "trusted attestation quorum %s",
                                matmul::trusted::WaitResultName(
                                    matmul::trusted::WaitResult::Timeout));
                            return false;
                        }
                        CacheMatMulEncDrVerdict(
                            block.GetHash(), true);
                        if (rc_authority != nullptr) {
                            *rc_authority =
                                MatMulRCAuthorityProvenance::
                                    TRUSTED_SIGNER_QUORUM;
                        }
                        return true;
                    }
                    // Consensus-mode node with -matmultrustedpubkey: a verified
                    // quorum is fork-choice authority for this hash. Skip the
                    // redundant local ExactReplay so mining + a sibling flood
                    // cannot wedge the 2-slot accelerator. Unattested
                    // tip-children and our own candidates still ExactReplay.
                    if (node::matmul_trusted::IsConfigured() &&
                        consensusParams.IsMatMulTrustedReplayAttestationActive(
                            nHeight) &&
                        node::matmul_trusted::HasQuorum(
                            block.GetHash(), nHeight)) {
                        CacheMatMulEncDrVerdict(block.GetHash(), true);
                        if (rc_authority != nullptr) {
                            *rc_authority =
                                MatMulRCAuthorityProvenance::
                                    TRUSTED_SIGNER_QUORUM;
                        }
                        return true;
                    }
                    const matmul::v4::rc::ScopedExactReplayCancellation
                        shutdown_cancel{&chainman.m_interrupt.Flag()};
                    const auto outcome{
                        CheckMatMulProofOfWork_RCOutcome(
                            block, consensusParams, nHeight,
                            /*carrier_missing=*/nullptr,
                            &rc_execution_detail)};
                    rc_local_execution_failure =
                        outcome ==
                            MatMulRCValidationOutcome::
                                LOCAL_ACCELERATOR_FAILURE ||
                        outcome ==
                            MatMulRCValidationOutcome::CANCELLED;
                    const bool valid{
                        outcome ==
                        MatMulRCValidationOutcome::VALID};
                    if (valid && rc_authority != nullptr) {
                        *rc_authority =
                            MatMulRCAuthorityProvenance::
                                LOCAL_EXACT_REPLAY;
                    }
                    return valid;
                };
                std::string winner_authority_provider;
                const bool winner_reseal_authority{
                    !trusted_profile1 &&
                    consensusParams.IsMatMulRCProfile1Active(nHeight) &&
                    !consensusParams.IsMatMulRCCoupledActive(nHeight) &&
                    ConsumeMatMulRCWinnerResealAuthority(
                        block, nHeight, consensusParams,
                        &winner_authority_provider)};
                const CBlockIndex* exact_index{
                    chainman.m_blockman.LookupBlockIndex(block.GetHash())};
                if (winner_reseal_authority) {
                    // This consumes exactly one process-local epsilon-zero
                    // result produced by the strict winner reseal. It skips
                    // only the duplicate replay: the function continues to
                    // ContextualCheckBlockBodyOnly below, and ProcessNewBlock /
                    // ConnectBlock retain all ordinary block and script checks.
                    encdr_ok = true;
                    if (rc_authority != nullptr) {
                        *rc_authority =
                            MatMulRCAuthorityProvenance::
                                LOCAL_EXACT_REPLAY;
                    }
                    LogPrintf(
                        "matmul RC local winner authority consumed: "
                        "block=%s height=%d provider=%s\n",
                        block.GetHash().ToString(), nHeight,
                        winner_authority_provider);
                } else if (exact_index != nullptr &&
                    (exact_index->nStatus & BLOCK_EXACT_REPLAY_VERIFIED) &&
                    // A durable bit must never outrank a recorded failure.
                    !(exact_index->nStatus & BLOCK_FAILED_MASK)) {
                    // Block-index startup accepts persisted authority only
                    // when its versioned MatMul activation/profile context
                    // matches the current immutable consensus parameters. A
                    // mismatch atomically clears local and trusted replay bits
                    // before validation begins.
                    encdr_ok = true;
                    if (rc_authority != nullptr) {
                        *rc_authority =
                            MatMulRCAuthorityProvenance::
                                LOCAL_EXACT_REPLAY;
                    }
                } else if (const auto memo{LookupMatMulEncDrVerdict(block.GetHash())}) {
                    // WP-7 / C5: the async verify worker (net_processing's
                    // node::MatMulVerifyWorker) already ran the pure ENC-DR
                    // predicate for this header off-thread and memoized the
                    // verdict. The predicate is a pure function of the header +
                    // process-constant params (the hash pins prev => height =>
                    // profile), so replaying the verdict here is
                    // consensus-equivalent to recomputing — and it keeps the
                    // completion's re-entry through ProcessNewBlock O(1) even
                    // when the 8-entry sketch cache already evicted the entry
                    // or the block is INVALID (nothing was ever cached).
                    encdr_ok = *memo;
                    if (encdr_ok && rc_authority != nullptr &&
                        consensusParams.IsMatMulRCActive(nHeight) &&
                        !consensusParams.IsMatMulRCCoupledActive(
                            nHeight)) {
                        *rc_authority = trusted_profile1
                            ? MatMulRCAuthorityProvenance::
                                  TRUSTED_SIGNER_QUORUM
                            : MatMulRCAuthorityProvenance::
                                  LOCAL_EXACT_REPLAY;
                    }
                } else if (may_release_cs_main) {
                    CsMainScopedRelease release_cs_main_for_recompute;
                    // DO NOT add cs_main-requiring code in this scope: TSA still
                    // believes cs_main is held here (the RAII guard is
                    // NO_THREAD_SAFETY_ANALYSIS), so a guarded call would compile
                    // yet run unlocked. Keep it to the single pure recompute.
                    // Parent MTP is a pure scalar from pindexPrev (already
                    // resolved under cs_main above); Phase B seal-as-PoW needs
                    // it for sibling-slot V3 seed re-derivation (LT-Q2).
                    const std::optional<int64_t> parent_mtp =
                        pindexPrev != nullptr
                            ? std::optional<int64_t>{pindexPrev->GetMedianTimePast()}
                            : std::nullopt;
                    if (consensusParams.IsMatMulRCCoupledActive(nHeight)) {
                        encdr_ok = CheckMatMulProofOfWork_RCCoupled(block, consensusParams, nHeight);
                    } else if (consensusParams.IsMatMulRCActive(nHeight)) {
                        encdr_ok = check_rc();
                    } else {
                        encdr_ok = CheckMatMulProofOfWork_V4EncDr(block, consensusParams, nHeight,
                                                                  parent_mtp);
                    }
                } else {
                    // E: TestBlockValidity path — keep cs_main held across the
                    // recompute so the caller's Tip()-pinned viewNew/indexDummy
                    // cannot be invalidated by a concurrent tip advance. The
                    // verdict is identical; only the locking discipline differs.
                    const std::optional<int64_t> parent_mtp =
                        pindexPrev != nullptr
                            ? std::optional<int64_t>{pindexPrev->GetMedianTimePast()}
                            : std::nullopt;
                    if (consensusParams.IsMatMulRCCoupledActive(nHeight)) {
                        encdr_ok = CheckMatMulProofOfWork_RCCoupled(block, consensusParams, nHeight);
                    } else if (consensusParams.IsMatMulRCActive(nHeight)) {
                        encdr_ok = check_rc();
                    } else {
                        encdr_ok = CheckMatMulProofOfWork_V4EncDr(block, consensusParams, nHeight,
                                                                  parent_mtp);
                    }
                }
                if (rc_local_execution_failure) {
                    return state.Error(strprintf(
                        "matmul RC ExactReplay local execution incomplete: %s",
                        rc_execution_detail));
                }
                if (!encdr_ok) {
                    return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "high-hash",
                        "matmul v4 proof of work failed");
                }
            }
        } else {
            // LEGACY IN-BLOCK path (FLAT_SKETCH_INBLOCK) — v4.4: reachable ONLY
            // via the regtest-only fMatMulV4FlatSketchReplay differential-
            // testing switch (tension-resolution §4.5); byte-for-byte the
            // pre-ENC-DR carriage.
            if (block.matrix_c_data.empty()) {
                return state.Invalid(BlockValidationResult::BLOCK_MUTATED, "missing-product-payload",
                    "matmul v4 block missing required product sketch payload");
            }
            if (!IsMatMulV4PayloadSizeValid(block, consensusParams)) {
                return state.Invalid(BlockValidationResult::BLOCK_MUTATED, "invalid-product-payload",
                    "matmul v4 block carries malformed product sketch payload");
            }
            if (!CheckMatMulProofOfWork_V4ProductCommitted(block, consensusParams, nHeight)) {
                // Separate a body MUTATION from a header-level CONSENSUS fault. The
                // sketch payload lives in the block body while matmul_digest is in
                // the 182-byte header, so a corrupted payload leaves the block hash
                // unchanged and a correct payload for that same hash still exists.
                // Marking such a block permanently invalid (BLOCK_CONSENSUS) would
                // let an attacker poison a valid header's hash by relaying a
                // corrupted-payload copy first, blocking the honest block as
                // "duplicate-invalid". Classify it as BLOCK_MUTATED (non-permanent),
                // matching the A/B and payload-shape checks above. Only a payload
                // that DOES reconstruct the committed digest but still fails
                // (Freivalds mismatch or digest over target) is a real, permanent
                // PoW failure.
                if (!MatMulV4PayloadMatchesCommitment(block)) {
                    return state.Invalid(BlockValidationResult::BLOCK_MUTATED, "bad-matmul-v4-payload",
                        "matmul v4 sketch payload does not match committed digest");
                }
                return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "high-hash",
                    "matmul v4 proof of work failed");
            }
        }
    } else if (fCheckPOW && consensusParams.fMatMulPOW) {
        const bool has_v2_payload = HasMatMulV2Payload(block);
        const bool payload_shape_valid =
            !has_v2_payload || IsMatMulV2PayloadSizeValid(block, consensusParams);
        if (has_v2_payload && !payload_shape_valid) {
            LogDebug(BCLog::NET, "Ignoring malformed matmul payload for block %s\n",
                     block.GetHash().ToString());
        }

        const bool should_run_phase2 =
            ShouldRunMatMulPhase2Validation(
                nHeight,
                best_known_height,
                consensusParams,
                chainman.GetMatMulValidationMode() ==
                        kernel::MatMulValidationMode::CONSENSUS ||
                    chainman.GetMatMulValidationMode() ==
                        kernel::MatMulValidationMode::TRUSTED,
                is_ibd);
        const bool should_run_matmul_validation =
            should_run_phase2 || consensusParams.IsMatMulProductDigestActive(nHeight);

        // Optional v2 payload vectors are relay metadata and not header-committed.
        // If payload-derived verification fails, rely on header-based phase2
        // validation performed in ContextualCheckBlockHeader().
        if (payload_shape_valid &&
            should_run_phase2 &&
            has_v2_payload &&
            !CheckMatMulProofOfWork_Phase2WithPayload(block, consensusParams, nHeight)) {
            LogDebug(BCLog::NET, "Ignoring non-canonical matmul payload for block %s\n",
                     block.GetHash().ToString());
        }

        // MatMul Phase2 and Freivalds validation: requires full CBlock with
        // matrix_c_data payload, so it lives here in ContextualCheckBlock
        // rather than ContextualCheckBlockHeader (which only has CBlockHeader).
        if (should_run_matmul_validation) {
            bool matmul_verified = false;
            const bool has_product_payload = HasMatMulFreivaldsPayload(block);
            const bool payload_size_valid =
                has_product_payload && IsMatMulFreivaldsPayloadSizeValid(block, consensusParams);
            const bool product_digest_active = consensusParams.IsMatMulProductDigestActive(nHeight);
            const bool payload_required = consensusParams.IsMatMulProductPayloadRequired(nHeight);

            // Product-committed digest: pure O(n^2) verification with no
            // transcript recomputation. Available at and above nMatMulProductDigestHeight.
            if (!matmul_verified &&
                product_digest_active &&
                payload_size_valid) {
                matmul_verified = CheckMatMulProofOfWork_ProductCommitted(block, consensusParams, nHeight);
            }

            // Primary path: Freivalds' O(n^2) verification. The C' payload
            // lets us verify A'*B'==C' probabilistically (error < 2^-62)
            // without recomputing the O(n^3) transcript.
            if (!matmul_verified &&
                consensusParams.fMatMulFreivaldsEnabled &&
                payload_size_valid) {
                matmul_verified = CheckMatMulProofOfWork_Freivalds(block, consensusParams, nHeight);
            }

            // Reject blocks missing the product payload when consensus requires it.
            if (!matmul_verified && payload_required &&
                consensusParams.fMatMulFreivaldsEnabled &&
                !has_product_payload) {
                return state.Invalid(BlockValidationResult::BLOCK_MUTATED, "missing-product-payload",
                    "block missing required Freivalds product matrix payload");
            }

            if (!matmul_verified && has_product_payload && !payload_size_valid) {
                return state.Invalid(BlockValidationResult::BLOCK_MUTATED, "invalid-product-payload",
                    "block carries malformed Freivalds product matrix payload");
            }

            if (!matmul_verified && consensusParams.IsMatMulProductDigestActive(nHeight) && has_product_payload) {
                return state.Invalid(BlockValidationResult::BLOCK_MUTATED, "invalid-product-payload",
                    "block carries non-canonical Freivalds product matrix payload");
            }

            // Fallback: full O(n^3) transcript verification for blocks
            // without a product matrix payload.
            if (!matmul_verified) {
                matmul_verified = CheckMatMulProofOfWork_Phase2(block, consensusParams, nHeight);
            }

            if (!matmul_verified) {
                return state.Invalid(BlockValidationResult::BLOCK_CONSENSUS, "high-hash", "matmul phase2 proof of work failed");
            }
        }
    }

    return ContextualCheckBlockBodyOnly(block, state, chainman, pindexPrev);
}

bool ChainstateManager::AcceptBlockHeader(const CBlockHeader& block, BlockValidationState& state, CBlockIndex** ppindex, bool min_pow_checked)
{
    AssertLockHeld(cs_main);

    // Check for duplicate
    uint256 hash = block.GetHash();
    BlockMap::iterator miSelf{m_blockman.m_block_index.find(hash)};
    if (hash != GetConsensus().hashGenesisBlock) {
        if (miSelf != m_blockman.m_block_index.end()) {
            // Block header is already known.
            CBlockIndex* pindex = &(miSelf->second);
            if (ppindex)
                *ppindex = pindex;
            if (pindex->nStatus & BLOCK_FAILED_MASK) {
                LogDebug(BCLog::VALIDATION, "%s: block %s is marked invalid\n", __func__, hash.ToString());
                return state.Invalid(BlockValidationResult::BLOCK_CACHED_INVALID, "duplicate-invalid",
                                     strprintf("block %s was previously marked invalid", hash.ToString()));
            }
            return true;
        }

        if (!CheckBlockHeader(block, state, GetConsensus())) {
            LogDebug(BCLog::VALIDATION, "%s: Consensus::CheckBlockHeader: %s, %s\n", __func__, hash.ToString(), state.ToString());
            return false;
        }

        // Get prev block index
        CBlockIndex* pindexPrev = nullptr;
        BlockMap::iterator mi{m_blockman.m_block_index.find(block.hashPrevBlock)};
        if (mi == m_blockman.m_block_index.end()) {
            LogDebug(BCLog::VALIDATION, "header %s has prev block not found: %s\n", hash.ToString(), block.hashPrevBlock.ToString());
            return state.Invalid(BlockValidationResult::BLOCK_MISSING_PREV, "prev-blk-not-found");
        }
        pindexPrev = &((*mi).second);
        if (pindexPrev->nStatus & BLOCK_FAILED_MASK) {
            LogDebug(BCLog::VALIDATION, "header %s has prev block invalid: %s\n", hash.ToString(), block.hashPrevBlock.ToString());
            return state.Invalid(BlockValidationResult::BLOCK_INVALID_PREV, "bad-prevblk");
        }
        if (!ContextualCheckBlockHeader(block, state, m_blockman, *this, pindexPrev)) {
            LogDebug(BCLog::VALIDATION, "%s: Consensus::ContextualCheckBlockHeader: %s, %s\n", __func__, hash.ToString(), state.ToString());
            return false;
        }

        /* Determine if this block descends from any block which has been found
         * invalid (m_failed_blocks), then mark pindexPrev and any blocks between
         * them as failed. For example:
         *
         *                D3
         *              /
         *      B2 - C2
         *    /         \
         *  A             D2 - E2 - F2
         *    \
         *      B1 - C1 - D1 - E1
         *
         * In the case that we attempted to reorg from E1 to F2, only to find
         * C2 to be invalid, we would mark D2, E2, and F2 as BLOCK_FAILED_CHILD
         * but NOT D3 (it was not in any of our candidate sets at the time).
         *
         * In any case D3 will also be marked as BLOCK_FAILED_CHILD at restart
         * in LoadBlockIndex.
         */
        if (!pindexPrev->IsValid(BLOCK_VALID_SCRIPTS)) {
            // The above does not mean "invalid": it checks if the previous block
            // hasn't been validated up to BLOCK_VALID_SCRIPTS. This is a performance
            // optimization, in the common case of adding a new block to the tip,
            // we don't need to iterate over the failed blocks list.
            for (const CBlockIndex* failedit : m_failed_blocks) {
                if (pindexPrev->GetAncestor(failedit->nHeight) == failedit) {
                    assert(failedit->nStatus & BLOCK_FAILED_VALID);
                    CBlockIndex* invalid_walk = pindexPrev;
                    while (invalid_walk && invalid_walk != failedit) {
                        invalid_walk->nStatus |= BLOCK_FAILED_CHILD;
                        m_blockman.m_dirty_blockindex.insert(invalid_walk);
                        invalid_walk = invalid_walk->pprev;
                    }
                    LogDebug(BCLog::VALIDATION, "header %s has prev block invalid: %s\n", hash.ToString(), block.hashPrevBlock.ToString());
                    return state.Invalid(BlockValidationResult::BLOCK_INVALID_PREV, "bad-prevblk");
                }
            }
        }
    }
    if (!min_pow_checked) {
        LogDebug(BCLog::VALIDATION, "%s: not adding new block header %s, missing anti-dos proof-of-work validation\n", __func__, hash.ToString());
        return state.Invalid(BlockValidationResult::BLOCK_HEADER_LOW_WORK, "too-little-chainwork");
    }
    CBlockIndex* const prev_best{m_best_header};
    CBlockIndex* pindex{m_blockman.AddToBlockIndex(block, m_best_header)};

    // PreferTrustAdjustedHeader's unauth allowance is denominated in the
    // candidate's own nBits. A high-difficulty stale fork (live 1883xx)
    // therefore outranks the ASERT-floor attested suffix, and AddToBlockIndex
    // writes that into m_best_header. Configured nodes (trusted mirrors and
    // consensus+pubkey) must not keep that choice: competing better-work
    // branches are followed only from attestation-authority peers in
    // net_processing. Blocks still require M-of-N quorum.
    if (node::matmul_trusted::IsConfigured() && pindex != nullptr) {
        const CBlockIndex* tip{ActiveChain().Tip()};
        if (tip != nullptr) {
            const bool extends_tip{
                pindex->nHeight >= tip->nHeight &&
                pindex->GetAncestor(tip->nHeight) == tip};
            const bool prev_extends_tip{
                prev_best != nullptr &&
                prev_best->nHeight >= tip->nHeight &&
                prev_best->GetAncestor(tip->nHeight) == tip};
            const bool extends_prev{
                prev_best != nullptr &&
                pindex->nHeight > prev_best->nHeight &&
                pindex->GetAncestor(prev_best->nHeight) == prev_best};
            if (node::matmul_trusted::PreferTrustedMirrorTipChainHeader({
                    .extends_active_tip_chain = extends_tip,
                    .on_parked_reorg_branch = IsOnParkedReorgBranch(pindex),
                    .candidate_height = pindex->nHeight,
                    .tip_height = tip->nHeight,
                    .current_best_height =
                        prev_best ? prev_best->nHeight : -1,
                    .current_best_extends_tip = prev_extends_tip,
                    .candidate_extends_current_best = extends_prev,
                })) {
                SetBestHeader(pindex);
            } else if (m_best_header == pindex && prev_best != nullptr &&
                       prev_best != pindex) {
                // Undo only this AddToBlockIndex promotion. Do not snap an
                // authority-followed short reorg back to ActiveTip().
                SetBestHeader(prev_best);
            }
        }
    }

    // AddToBlockIndex may have changed m_best_header through its legacy
    // reference parameter even when the trusted overlay kept that choice.
    SetBestHeader(m_best_header);

    if (ppindex)
        *ppindex = pindex;

    // Since this is the earliest point at which we have determined that a
    // header is both new and valid, log here.
    //
    // These messages are valuable for detecting potential selfish mining behavior;
    // if multiple displacing headers are seen near simultaneously across many
    // nodes in the network, this might be an indication of selfish mining. Having
    // this log by default when not in IBD ensures broad availability of this data
    // in case investigation is merited.
    const auto msg = strprintf(
        "Saw new header hash=%s height=%d", hash.ToString(), pindex->nHeight);

    if (IsInitialBlockDownload()) {
        LogPrintLevel(BCLog::VALIDATION, BCLog::Level::Debug, "%s\n", msg);
    } else {
        LogPrintf("%s\n", msg);
    }

    return true;
}

// Exposed wrapper for AcceptBlockHeader
bool ChainstateManager::ProcessNewBlockHeaders(std::span<const CBlockHeader> headers, bool min_pow_checked, BlockValidationState& state, const CBlockIndex** ppindex)
{
    AssertLockNotHeld(cs_main);
    {
        LOCK(cs_main);
        for (const CBlockHeader& header : headers) {
            CBlockIndex *pindex = nullptr; // Use a temp pindex instead of ppindex to avoid a const_cast
            bool accepted{AcceptBlockHeader(header, state, &pindex, min_pow_checked)};
            CheckBlockIndex();

            if (!accepted) {
                return false;
            }
            if (ppindex) {
                *ppindex = pindex;
            }
        }
    }
    if (NotifyHeaderTip()) {
        if (IsInitialBlockDownload() && ppindex && *ppindex) {
            const CBlockIndex& last_accepted{**ppindex};
            int64_t blocks_left{(NodeClock::now() - last_accepted.Time()) / GetConsensus().PowTargetSpacing()};
            blocks_left = std::max<int64_t>(0, blocks_left);
            const double progress{100.0 * last_accepted.nHeight / (last_accepted.nHeight + blocks_left)};
            LogInfo("Synchronizing blockheaders, height: %d (~%.2f%%)\n", last_accepted.nHeight, progress);
        }
    }
    return true;
}

void ChainstateManager::ReportHeadersPresync(const arith_uint256& work, int64_t height, int64_t timestamp)
{
    AssertLockNotHeld(cs_main);
    {
        LOCK(cs_main);
        // Don't report headers presync progress if we already have a post-minchainwork header chain.
        // This means we lose reporting for potentially legitimate, but unlikely, deep reorgs, but
        // prevent attackers that spam low-work headers from filling our logs.
        // Audit P0.1/C1: gate on AUTHENTICATED work so a forged matmul-header chain
        // cannot suppress/flip presync reporting. Equal to nChainWork pre-fork.
        if (m_best_header->nAuthenticatedChainWork >= UintToArith256(GetConsensus().nMinimumChainWork)) return;
        // Rate limit headers presync updates to 4 per second, as these are not subject to DoS
        // protection.
        auto now = std::chrono::steady_clock::now();
        if (now < m_last_presync_update + std::chrono::milliseconds{250}) return;
        m_last_presync_update = now;
    }
    bool initial_download = IsInitialBlockDownload();
    GetNotifications().headerTip(GetSynchronizationState(initial_download, m_blockman.m_blockfiles_indexed), height, timestamp, /*presync=*/true);
    if (initial_download) {
        int64_t blocks_left{(NodeClock::now() - NodeSeconds{std::chrono::seconds{timestamp}}) / GetConsensus().PowTargetSpacing()};
        blocks_left = std::max<int64_t>(0, blocks_left);
        const double progress{100.0 * height / (height + blocks_left)};
        LogInfo("Pre-synchronizing blockheaders, height: %d (~%.2f%%)\n", height, progress);
    }
}

/** Store block on disk. If dbp is non-nullptr, the file is known to already reside on disk */
bool ChainstateManager::AcceptBlock(const std::shared_ptr<const CBlock>& pblock, BlockValidationState& state, CBlockIndex** ppindex, bool fRequested, const FlatFilePos* dbp, bool* fNewBlock, bool min_pow_checked)
{
    const CBlock& block = *pblock;

    if (fNewBlock) *fNewBlock = false;
    AssertLockHeld(cs_main);

    CBlockIndex *pindexDummy = nullptr;
    CBlockIndex *&pindex = ppindex ? *ppindex : pindexDummy;

    bool accepted_header{AcceptBlockHeader(block, state, &pindex, min_pow_checked)};
    CheckBlockIndex();

    if (!accepted_header)
        return false;

    // Check all requested blocks that we do not already have for validity and
    // save them to disk. Skip processing of unrequested blocks as an anti-DoS
    // measure, unless the blocks have more work than the active chain tip, and
    // aren't too far ahead of it, so are likely to be attached soon.
    bool fAlreadyHave = pindex->nStatus & BLOCK_HAVE_DATA;
    bool fHasMoreOrSameWork = (ActiveTip() ? pindex->nChainWork >= ActiveTip()->nChainWork : true);
    // Blocks that are too out-of-order needlessly limit the effectiveness of
    // pruning, because pruning will not delete block files that contain any
    // blocks which are too close in height to the tip.  Apply this test
    // regardless of whether pruning is enabled; it should generally be safe to
    // not process unrequested blocks.
    bool fTooFarAhead{pindex->nHeight > ActiveHeight() + int(MIN_BLOCKS_TO_KEEP)};

    // NOTE: Decouple this function from the block download logic by removing fRequested
    // This requires some new chain data structure to efficiently look up if a
    // block is in a chain leading to a candidate for best tip, despite not
    // being such a candidate itself.
    // Note that this would break the getblockfrompeer RPC

    // NOTE: deal better with return value and error conditions for duplicate
    // and unrequested blocks.
    // Live 2026-08-15: HEADER_ONLY / persist-without-GPU left 189686
    // 2fd67f18 HAVE_DATA. A later header ExactReplay persisted
    // BLOCK_EXACT_REPLAY_VERIFIED without ReceivedBlockTransactions /
    // ConnectTip, so submitblock still returned "duplicate" and ABC
    // never selected the validator-chain child of attested 189685.
    // Re-enter ContextualCheck + (if needed) ReceivedBlockTransactions
    // for any requested/forced unconnected tip-child.
    const bool reverify_tip_child{
        fAlreadyHave && fRequested &&
        pindex->pprev != nullptr &&
        pindex->pprev == ActiveTip() &&
        !ActiveChain().Contains(pindex) &&
        (pindex->nStatus & BLOCK_FAILED_MASK) == 0};
    if (fAlreadyHave && !reverify_tip_child) return true;
    if (!fRequested) {  // If we didn't ask for it:
        // Followed-chain historical holes (active-tip / snapshot-base
        // ancestors) are less-work by construction and may be pruned
        // (nTx != 0, !HAVE_DATA). Persist a delivered body even when
        // unrequested; competing forks, pruned junk, and too-far-ahead
        // spam keep the Bitcoin Core anti-DoS early returns.
        // ReceivedBlockTransactions already accepts a pruned re-download.
        if (!IsMatMulFollowedHistoricalHole(pindex)) {
            if (pindex->nTx != 0) return true;    // previously-processed, pruned
            if (!fHasMoreOrSameWork) return true; // Don't process less-work chains
            if (fTooFarAhead) return true;        // Block height is too high

            // Protect against DoS attacks from low-work chains.
            // If our tip is behind, a peer could try to send us
            // low-work blocks on a fake chain that we would never
            // request; don't process these.
            if (pindex->nChainWork < MinimumChainWork()) return true;
        }
    }

    const CChainParams& params{GetParams()};
    MatMulRCAuthorityProvenance rc_authority{
        MatMulRCAuthorityProvenance::NONE};

    if (!CheckBlock(block, state, params.GetConsensus()) ||
        !ContextualCheckBlock(block, state, *this, pindex->pprev,
                              /*fCheckPOW=*/true,
                              /*may_release_cs_main=*/true,
                              &rc_authority)) {
        if (state.IsInvalid() && state.GetResult() != BlockValidationResult::BLOCK_MUTATED) {
            pindex->nStatus |= BLOCK_FAILED_VALID;
            m_blockman.m_dirty_blockindex.insert(pindex);
        }
        LogError("%s: %s\n", __func__, state.ToString());
        return false;
    }

    // Persist only explicit authority provenance. In particular, a generic
    // assumevalid skip must never create an exact-replay bit, a trusted audit
    // bit, or an archive signature.
    if (rc_authority ==
        MatMulRCAuthorityProvenance::LOCAL_EXACT_REPLAY) {
        (void)PersistMatMulExactReplayVerdict(
            pindex->GetBlockHash());
    } else if (
        rc_authority ==
        MatMulRCAuthorityProvenance::TRUSTED_SIGNER_QUORUM) {
        (void)PersistMatMulTrustedReplayAttestation(
            pindex->GetBlockHash());
    }

    // G.1 duplicate-processing guard: ContextualCheckBlock may have RELEASED
    // cs_main during the v4.4 ENC-DR recompute, opening a window in which
    // another thread (a local submitblock/generateblock RPC, or reindex racing
    // net) could have fully processed this same block — write +
    // ReceivedBlockTransactions (which sets BLOCK_HAVE_DATA). fAlreadyHave was
    // sampled BEFORE that window (above), so re-check now under the re-acquired
    // lock to avoid a duplicate disk write / NewPoWValidBlock /
    // ReceivedBlockTransactions for the same block. (A remote peer cannot
    // trigger this — the message handler is single-threaded — but a concurrent
    // RPC/reindex can.)
    // Followed HAVE_DATA tip-child catch-up: the body is already on disk but
    // nTx / VALID_TRANSACTIONS may never have been raised. Do not skip
    // ReceivedBlockTransactions in that case — otherwise ABC cannot select it.
    if (pindex->nStatus & BLOCK_HAVE_DATA) {
        if (reverify_tip_child && pindex->nTx == 0) {
            const FlatFilePos pos{pindex->GetBlockPos()};
            if (!pos.IsNull()) {
                ReceivedBlockTransactions(block, pindex, pos);
            }
        }
        if (reverify_tip_child) {
            ActiveChainstate().TryAddBlockIndexCandidate(pindex);
        }
        return true;
    }

    // Header is valid/has work, merkle tree and segwit merkle tree are good...RELAY NOW
    // (but if it does not build on our best tip, let the SendMessages loop relay it)
    if (!IsInitialBlockDownload() && ActiveTip() == pindex->pprev && m_options.signals) {
        m_options.signals->NewPoWValidBlock(pindex, pblock);
    }

    // Write block to history file
    if (fNewBlock) *fNewBlock = true;
    try {
        FlatFilePos blockPos{};
        if (dbp) {
            blockPos = *dbp;
            m_blockman.UpdateBlockInfo(block, pindex->nHeight, blockPos);
        } else {
            blockPos = m_blockman.WriteBlock(block, pindex->nHeight);
            if (blockPos.IsNull()) {
                state.Error(strprintf("%s: Failed to find position to write new block to disk", __func__));
                return false;
            }
        }
        ReceivedBlockTransactions(block, pindex, blockPos);
    } catch (const std::runtime_error& e) {
        return FatalError(GetNotifications(), state, strprintf(_("System error while saving block to disk: %s"), e.what()));
    }

    // NOTE: FlushStateToDisk() handles flushing of both block and chainstate
    // data, so we should move this to ChainstateManager so that we can be more
    // intelligent about how we flush.
    // For now, since FlushStateMode::NONE is used, all that can happen is that
    // the block files may be pruned, so we can just call this on one
    // chainstate (particularly if we haven't implemented pruning with
    // background validation yet).
    // Flush failures are fatal operational errors, not block-validation
    // failures. Keep them out of the caller's validation state while the
    // notification still initiates shutdown.
    BlockValidationState flush_state_ignore;
    (void)ActiveChainstate().FlushStateToDisk(flush_state_ignore, FlushStateMode::NONE);

    CheckBlockIndex();

    return true;
}

bool ChainstateManager::ProcessNewBlock(const std::shared_ptr<const CBlock>& block, bool force_processing, bool min_pow_checked, bool* new_block)
{
    AssertLockNotHeld(cs_main);

    {
        CBlockIndex *pindex = nullptr;
        if (new_block) *new_block = false;
        BlockValidationState state;

        // CheckBlock() does not support multi-threaded block validation because CBlock::fChecked can cause data race.
        // Therefore, the following critical section must include the CheckBlock() call as well.
        LOCK(cs_main);

        // Skipping AcceptBlock() for CheckBlock() failures means that we will never mark a block as invalid if
        // CheckBlock() fails.  This is protective against consensus failure if there are any unknown forms of block
        // malleability that cause CheckBlock() to fail; see e.g. CVE-2012-2459 and
        // https://lists.linuxfoundation.org/pipermail/bitcoin-dev/2019-February/016697.html.  Because CheckBlock() is
        // not very expensive, the anti-DoS benefits of caching failure (of a definitely-invalid block) are not substantial.
        bool ret = CheckBlock(*block, state, GetConsensus());
        if (ret) {
            // Store to disk
            ret = AcceptBlock(block, state, &pindex, force_processing, nullptr, new_block, min_pow_checked);
        }
        if (!ret) {
            if (m_options.signals) {
                m_options.signals->BlockChecked(*block, state);
            }
            LogError("%s: AcceptBlock FAILED (%s)\n", __func__, state.ToString());

            return false;
        }
    }

    NotifyHeaderTip();

    BlockValidationState state; // Only used to report errors, not invalidity - ignore it
    if (!ActiveChainstate().ActivateBestChain(state, block)) {
        LogError("%s: ActivateBestChain failed (%s)\n", __func__, state.ToString());

        return false;
    }

    Chainstate* bg_chain{WITH_LOCK(cs_main, return BackgroundSyncInProgress() ? m_ibd_chainstate.get() : nullptr)};
    BlockValidationState bg_state;
    if (bg_chain && !bg_chain->ActivateBestChain(bg_state, block)) {
        LogError("%s: [background] ActivateBestChain failed (%s)\n", __func__, bg_state.ToString());
        return false;
     }

    return true;
}

MempoolAcceptResult ChainstateManager::ProcessTransaction(const CTransactionRef& tx, bool test_accept, const ignore_rejects_type& ignore_rejects)
{
    AssertLockHeld(cs_main);
    Chainstate& active_chainstate = ActiveChainstate();
    if (!active_chainstate.GetMempool()) {
        TxValidationState state;
        state.Invalid(TxValidationResult::TX_NO_MEMPOOL, "no-mempool");
        return MempoolAcceptResult::Failure(state);
    }
    auto result = AcceptToMemoryPool(active_chainstate, tx, GetTime(), ignore_rejects, test_accept);
    active_chainstate.GetMempool()->check(
        active_chainstate.CoinsTip(),
        NextBlockHeightOrLimit(active_chainstate.m_chain.Height()));
    return result;
}

bool TestBlockValidity(BlockValidationState& state,
                       const CChainParams& chainparams,
                       Chainstate& chainstate,
                       const CBlock& block,
                       CBlockIndex* pindexPrev,
                       bool fCheckPOW,
                       bool fCheckMerkleRoot)
{
    AssertLockHeld(cs_main);
    if (!pindexPrev) {
        LogDebug(BCLog::VALIDATION, "%s: pindexPrev is null\n", __func__);
        return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER, "bad-prevblk-null", "pindexPrev is null");
    }
    if (pindexPrev != chainstate.m_chain.Tip()) {
        LogDebug(BCLog::VALIDATION, "%s: pindexPrev (height=%d, hash=%s) is not the current chain tip (hash=%s)\n",
                 __func__, pindexPrev->nHeight, pindexPrev->GetBlockHash().GetHex(),
                 chainstate.m_chain.Tip() ? chainstate.m_chain.Tip()->GetBlockHash().GetHex() : "null");
        return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER, "bad-prevblk-not-tip",
            "pindexPrev is not the current chain tip");
    }
    if (pindexPrev->nHeight == std::numeric_limits<int>::max()) {
        return state.Invalid(BlockValidationResult::BLOCK_INVALID_HEADER, "bad-blk-height-overflow", "block height overflow");
    }
    CCoinsViewCache viewNew(&chainstate.CoinsTip());
    uint256 block_hash(block.GetHash());
    CBlockIndex indexDummy(block);
    indexDummy.pprev = pindexPrev;
    indexDummy.nHeight = pindexPrev->nHeight + 1;
    indexDummy.phashBlock = &block_hash;

    // NOTE: CheckBlockHeader is called by CheckBlock
    if (!ContextualCheckBlockHeader(block, state, chainstate.m_blockman, chainstate.m_chainman, pindexPrev, fCheckPOW)) {
        LogError("%s: Consensus::ContextualCheckBlockHeader: %s\n", __func__, state.ToString());
        return false;
    }
    if (!CheckBlock(block, state, chainparams.GetConsensus(), fCheckPOW, fCheckMerkleRoot)) {
        LogError("%s: Consensus::CheckBlock: %s\n", __func__, state.ToString());
        return false;
    }
    // E: pass may_release_cs_main=false. TestBlockValidity holds viewNew /
    // indexDummy and the pindexPrev==Tip() precondition across this call and
    // ConnectBlock below, so ContextualCheckBlock must NOT release cs_main
    // mid-call even on the fCheckPOW=true ENC-DR recompute branch.
    if (!ContextualCheckBlock(block, state, chainstate.m_chainman, pindexPrev, fCheckPOW, /*may_release_cs_main=*/false)) {
        LogError("%s: Consensus::ContextualCheckBlock: %s\n", __func__, state.ToString());
        return false;
    }
    if (!chainstate.ConnectBlock(block, state, &indexDummy, viewNew, true)) {
        return false;
    }
    assert(state.IsValid());

    return true;
}

/* This function is called from the RPC code for pruneblockchain */
void PruneBlockFilesManual(Chainstate& active_chainstate, int nManualPruneHeight)
{
    BlockValidationState state;
    if (!active_chainstate.FlushStateToDisk(
            state, FlushStateMode::NONE, nManualPruneHeight)) {
        LogPrintf("%s: failed to flush state (%s)\n", __func__, state.ToString());
    }
}

bool Chainstate::LoadChainTip()
{
    AssertLockHeld(cs_main);
    const CCoinsViewCache& coins_cache = CoinsTip();
    assert(!coins_cache.GetBestBlock().IsNull()); // Never called when the coins view is empty
    CBlockIndex* tip = m_chain.Tip();

    if (tip && tip->GetBlockHash() == coins_cache.GetBestBlock()) {
        return true;
    }

    // Load pointer to end of best chain
    CBlockIndex* pindex = m_blockman.LookupBlockIndex(coins_cache.GetBestBlock());
    if (!pindex) {
        return false;
    }
    m_chain.SetTip(*pindex);
    m_last_flushed_block = pindex;
    tip = m_chain.Tip();

    // Park roots are local policy, not block validity. A root which became an
    // ancestor of the active tip (for example after PARK -> WARN -> PARK) must
    // never make that tip and every future child ineligible. A warn-only
    // startup likewise retires prior PARK decisions instead of resurrecting
    // them on a later profile change.
    if (this == &m_chainman.ActiveChainstate() &&
        !m_chainman.NormalizeParkedReorgBranches(tip)) {
        LogError("%s: failed to normalize persisted parked reorg roots\n", __func__);
        return false;
    }
    if (this == &m_chainman.ActiveChainstate() &&
        !m_chainman.NormalizeReorgRecovery(tip)) {
        LogError("%s: failed to normalize persisted reorg recovery state\n", __func__);
        return false;
    }

    // nSequenceId is a sort key for setBlockIndexCandidates and CBlockIndex
    // objects are shared between chainstates. All candidate sets must therefore
    // be empty before changing any sequence id.
    for (Chainstate* chainstate : m_chainman.GetAll()) {
        assert(chainstate->setBlockIndexCandidates.empty());
    }

    // Make sure our chain tip before shutting down scores better than any other candidate
    // to maintain a consistent best tip over reboots in case of a tie.
    auto target = tip;
    while (target) {
        target->nSequenceId = SEQ_ID_BEST_CHAIN_FROM_DISK;
        target = target->pprev;
    }

    tip = m_chain.Tip();
    LogPrintf("Loaded best chain: hashBestChain=%s height=%d date=%s progress=%f\n",
              tip->GetBlockHash().ToString(),
              m_chain.Height(),
              FormatISO8601DateTime(tip->GetBlockTime()),
              m_chainman.GuessVerificationProgress(tip));

    // Ensure KernelNotifications m_tip_block is set even if no new block arrives.
    if (this->GetRole() != ChainstateRole::BACKGROUND) {
        // Ignoring return value for now.
        (void)m_chainman.GetNotifications().blockTip(GetSynchronizationState(/*init=*/true, m_chainman.m_blockman.m_blockfiles_indexed), *pindex);
    }

    CheckForkWarningConditions();

    return true;
}

CVerifyDB::CVerifyDB(Notifications& notifications)
    : m_notifications{notifications}
{
    m_notifications.progress(_("Verifying blocks…"), 0, false);
}

CVerifyDB::~CVerifyDB()
{
    m_notifications.progress(bilingual_str{}, 100, false);
}

VerifyDBResult CVerifyDB::VerifyDB(
    Chainstate& chainstate,
    const Consensus::Params& consensus_params,
    CCoinsView& coinsview,
    int nCheckLevel, int nCheckDepth)
{
    AssertLockHeld(cs_main);

    if (chainstate.m_chain.Tip() == nullptr || chainstate.m_chain.Tip()->pprev == nullptr) {
        return VerifyDBResult::SUCCESS;
    }

    // Verify blocks in the best chain
    if (nCheckDepth <= 0 || nCheckDepth > chainstate.m_chain.Height()) {
        nCheckDepth = chainstate.m_chain.Height();
    }
    nCheckLevel = std::max(0, std::min(4, nCheckLevel));
    LogPrintf("Verifying last %i blocks at level %i\n", nCheckDepth, nCheckLevel);
    CCoinsViewCache coins(&coinsview);
    CBlockIndex* pindex;
    CBlockIndex* pindexFailure = nullptr;
    int nGoodTransactions = 0;
    BlockValidationState state;
    int reportDone = 0;
    bool skipped_no_block_data{false};
    bool skipped_l3_checks{false};
    const auto verify_start = std::chrono::steady_clock::now();
    auto last_progress_log = verify_start;
    LogPrintf("Verification progress: 0%% (checking last %i blocks at level %i from height %d)\n",
              nCheckDepth,
              nCheckLevel,
              chainstate.m_chain.Height());

    const bool is_snapshot_cs{chainstate.m_from_snapshot_blockhash};

    for (pindex = chainstate.m_chain.Tip(); pindex && pindex->pprev; pindex = pindex->pprev) {
        const int percentageDone = std::max(1, std::min(99, (int)(((double)(chainstate.m_chain.Height() - pindex->nHeight)) / (double)nCheckDepth * (nCheckLevel >= 4 ? 50 : 100))));
        const auto now = std::chrono::steady_clock::now();
        if (reportDone < percentageDone / 10 || now - last_progress_log >= std::chrono::seconds{15}) {
            // report every 10% step, and at least every 15s so a long -checkblocks=0 archive
            // VerifyDB cannot look wedged (the production "Verifying blocks…" silence).
            last_progress_log = now;
            const auto elapsed_s = std::chrono::duration_cast<std::chrono::seconds>(now - verify_start).count();
            LogPrintf("Verification progress: %d%% (height=%d, elapsed=%llds)\n",
                      percentageDone,
                      pindex->nHeight,
                      static_cast<long long>(elapsed_s));
            reportDone = percentageDone / 10;
        }
        m_notifications.progress(_("Verifying blocks…"), percentageDone, false);
        if (pindex->nHeight <= chainstate.m_chain.Height() - nCheckDepth) {
            break;
        }
        if ((chainstate.m_blockman.IsPruneMode() || is_snapshot_cs) && !(pindex->nStatus & BLOCK_HAVE_DATA)) {
            // If pruning or running under an assumeutxo snapshot, only go
            // back as far as we have data.
            LogPrintf("VerifyDB(): block verification stopping at height %d (no data). This could be due to pruning or use of an assumeutxo snapshot.\n", pindex->nHeight);
            skipped_no_block_data = true;
            break;
        }
        CBlock block;
        // check level 0: read from disk
        if (!chainstate.m_blockman.ReadBlock(block, *pindex, /*lowprio=*/true)) {
            LogPrintf("Verification error: ReadBlock failed at %d, hash=%s\n", pindex->nHeight, pindex->GetBlockHash().ToString());
            return VerifyDBResult::CORRUPTED_BLOCK_DB;
        }
        // check level 1: verify block validity
        if (nCheckLevel >= 1 && !CheckBlock(block, state, consensus_params)) {
            LogPrintf("Verification error: found bad block at %d, hash=%s (%s)\n",
                      pindex->nHeight, pindex->GetBlockHash().ToString(), state.ToString());
            return VerifyDBResult::CORRUPTED_BLOCK_DB;
        }
        // check level 2: verify undo validity
        if (nCheckLevel >= 2 && pindex) {
            CBlockUndo undo;
            if (!pindex->GetUndoPos().IsNull()) {
                if (!chainstate.m_blockman.ReadBlockUndo(undo, *pindex)) {
                    LogPrintf("Verification error: found bad undo data at %d, hash=%s\n", pindex->nHeight, pindex->GetBlockHash().ToString());
                    return VerifyDBResult::CORRUPTED_BLOCK_DB;
                }
            }
        }
        // check level 3: check for inconsistencies during memory-only disconnect of tip blocks
        size_t curr_coins_usage = coins.DynamicMemoryUsage() + chainstate.CoinsTip().DynamicMemoryUsage();

        if (nCheckLevel >= 3) {
            // A snapshot chainstate cannot disconnect its base: its UTXO
            // database starts at that block and contains no ancestor state.
            // Shielded state is likewise loaded from the BTX snapshot section,
            // so do not mutate it while performing this memory-only UTXO check.
            if (is_snapshot_cs && pindex == chainstate.SnapshotBase()) {
                break;
            }
            if (curr_coins_usage <= chainstate.m_coinstip_cache_size_bytes) {
                assert(coins.GetBestBlock() == pindex->GetBlockHash());
                DisconnectResult res = chainstate.DisconnectBlock(block, pindex, coins, /*apply_shielded_state=*/false);
                if (res == DISCONNECT_FAILED) {
                    LogPrintf("Verification error: irrecoverable inconsistency in block data at %d, hash=%s\n", pindex->nHeight, pindex->GetBlockHash().ToString());
                    return VerifyDBResult::CORRUPTED_BLOCK_DB;
                }
                if (res == DISCONNECT_UNCLEAN) {
                    nGoodTransactions = 0;
                    pindexFailure = pindex;
                } else {
                    nGoodTransactions += block.vtx.size();
                }
            } else {
                skipped_l3_checks = true;
            }
        }
        if (chainstate.m_chainman.m_interrupt) return VerifyDBResult::INTERRUPTED;
    }
    if (pindexFailure) {
        const int64_t inconsistent_blocks{
            static_cast<int64_t>(chainstate.m_chain.Height()) -
            static_cast<int64_t>(pindexFailure->nHeight) + 1};
        LogPrintf("Verification error: coin database inconsistencies found (last %lld blocks, %i good transactions before that)\n", static_cast<long long>(inconsistent_blocks), nGoodTransactions);
        return VerifyDBResult::CORRUPTED_BLOCK_DB;
    }
    if (skipped_l3_checks) {
        LogPrintf("Skipped verification of level >=3 (insufficient database cache size). Consider increasing -dbcache.\n");
    }

    // store block count as we move pindex at check level >= 4
    int block_count = chainstate.m_chain.Height() - pindex->nHeight;

    // check level 4: try reconnecting blocks
    if (nCheckLevel >= 4 && !skipped_l3_checks) {
        while (pindex != chainstate.m_chain.Tip()) {
            const int percentageDone = std::max(1, std::min(99, 100 - (int)(((double)(chainstate.m_chain.Height() - pindex->nHeight)) / (double)nCheckDepth * 50)));
            if (reportDone < percentageDone / 10) {
                // report every 10% step
                LogPrintf("Verification progress: %d%%\n", percentageDone);
                reportDone = percentageDone / 10;
            }
            m_notifications.progress(_("Verifying blocks…"), percentageDone, false);
            pindex = chainstate.m_chain.Next(pindex);
            CBlock block;
            if (!chainstate.m_blockman.ReadBlock(block, *pindex, /*lowprio=*/true)) {
                LogPrintf("Verification error: ReadBlock failed at %d, hash=%s\n", pindex->nHeight, pindex->GetBlockHash().ToString());
                return VerifyDBResult::CORRUPTED_BLOCK_DB;
            }
            if (!chainstate.ConnectBlock(block, state, pindex, coins, /*fJustCheck=*/false, /*apply_shielded_state=*/false)) {
                LogPrintf("Verification error: found unconnectable block at %d, hash=%s (%s)\n", pindex->nHeight, pindex->GetBlockHash().ToString(), state.ToString());
                return VerifyDBResult::CORRUPTED_BLOCK_DB;
            }
            if (chainstate.m_chainman.m_interrupt) return VerifyDBResult::INTERRUPTED;
        }
    }

    const bool run_full_shielded_audit{
        nCheckLevel >= 4 &&
        nCheckDepth >= chainstate.m_chain.Height() &&
        !skipped_no_block_data &&
        !skipped_l3_checks};
    if (run_full_shielded_audit) {
        std::string shielded_error;
        if (!AuditShieldedStateAgainstChain(chainstate, shielded_error)) {
            LogPrintf("Verification error: shielded state audit failed (%s)\n", shielded_error);
            return VerifyDBResult::CORRUPTED_BLOCK_DB;
        }
    }

    LogPrintf("Verification: No coin database inconsistencies in last %i blocks (%i transactions)\n", block_count, nGoodTransactions);

    if (skipped_l3_checks) {
        return VerifyDBResult::SKIPPED_L3_CHECKS;
    }
    if (skipped_no_block_data) {
        return VerifyDBResult::SKIPPED_MISSING_BLOCKS;
    }
    return VerifyDBResult::SUCCESS;
}

/** Apply the effects of a block on the utxo cache, ignoring that it may already have been applied.
 *  Also applies shielded state effects (nullifiers, commitments, pool balance) so that
 *  crash-recovery replay keeps shielded state in sync with the UTXO set (R5-400). */
bool Chainstate::RollforwardBlock(const CBlockIndex* pindex, CCoinsViewCache& inputs)
{
    AssertLockHeld(cs_main);
    const auto& consensus = m_chainman.GetConsensus();
    CBlock block;
    if (!m_blockman.ReadBlock(block, *pindex, /*lowprio=*/true)) {
        LogError("ReplayBlock(): ReadBlock failed at %d, hash=%s\n", pindex->nHeight, pindex->GetBlockHash().ToString());
        return false;
    }

    std::vector<Nullifier> block_nullifiers;
    std::vector<uint256> block_recovery_exit_commitments;
    std::vector<shielded::registry::ShieldedAccountLeaf> block_account_leaves;
    std::vector<std::pair<uint256, smile2::CompactPublicAccount>> block_smile_public_accounts;
    std::vector<std::pair<uint256, uint256>> block_account_leaf_entries;
    shielded::ShieldedMerkleTree projected_tree = m_chainman.m_shielded_merkle_tree;
    ShieldedPoolBalance projected_pool = m_chainman.m_shielded_pool_balance;
    auto projected_account_registry = m_chainman.m_shielded_account_registry;
    auto projected_public_accounts = m_chainman.m_shielded_smile_public_accounts;
    auto projected_account_leaf_commitments = m_chainman.m_shielded_account_leaf_commitments;
    for (const CTransactionRef& tx : block.vtx) {
        if (!tx->IsCoinBase()) {
            for (const CTxIn &txin : tx->vin) {
                inputs.SpendCoin(txin.prevout);
            }
        }
        // Pass check = true as every addition may be an overwrite.
        AddCoins(inputs, *tx, pindex->nHeight, true);

        // Apply shielded state effects to keep in sync with UTXO set.
        if (tx->HasShieldedBundle()) {
            const CShieldedBundle& bundle = tx->GetShieldedBundle();
            std::string height_gate_reject;
            if (!RejectShieldedHeightGateViolation(*tx, consensus, pindex->nHeight, height_gate_reject)) {
                LogError("RollforwardBlock(): shielded height-gate violation at %d: %s\n",
                         pindex->nHeight,
                         height_gate_reject);
                return false;
            }
            const bool use_nonced_bridge_tag =
                UseNoncedShieldedBridgeTags(m_chainman.GetConsensus(), pindex->nHeight);
            const auto bundle_nullifiers = CollectShieldedNullifiers(bundle);
            block_nullifiers.insert(block_nullifiers.end(),
                                    bundle_nullifiers.begin(),
                                    bundle_nullifiers.end());
            if (!CollectRecoveryExitRetirements(bundle,
                                                &block_nullifiers,
                                                &block_recovery_exit_commitments)) {
                LogError("RollforwardBlock(): failed to derive recovery-exit retirements at %d\n",
                         pindex->nHeight);
                return false;
            }
            const auto bundle_smile_accounts = CollectShieldedOutputSmileAccounts(bundle);
            block_smile_public_accounts.insert(block_smile_public_accounts.end(),
                                              bundle_smile_accounts.begin(),
                                              bundle_smile_accounts.end());
            const auto bundle_account_leaves =
                shielded::registry::CollectShieldedOutputAccountLeaves(bundle,
                                                                      use_nonced_bridge_tag);
            if (!bundle_account_leaves.has_value()) {
                LogError("RollforwardBlock(): failed to collect account leaves at %d\n",
                         pindex->nHeight);
                return false;
            }
            block_account_leaves.insert(block_account_leaves.end(),
                                        bundle_account_leaves->begin(),
                                        bundle_account_leaves->end());
            const auto bundle_account_leaf_entries =
                CollectShieldedOutputAccountLeafEntries(bundle, use_nonced_bridge_tag);
            if (!bundle_account_leaf_entries.has_value()) {
                LogError("RollforwardBlock(): failed to collect account-leaf entries at %d\n",
                         pindex->nHeight);
                return false;
            }
            block_account_leaf_entries.insert(block_account_leaf_entries.end(),
                                              bundle_account_leaf_entries->begin(),
                                              bundle_account_leaf_entries->end());
            try {
                for (const auto& commitment : CollectShieldedOutputCommitments(bundle)) {
                    projected_tree.Append(commitment);
                }
            } catch (const std::exception& e) {
                LogError("RollforwardBlock(): shielded tree append exception at %d: %s\n",
                         pindex->nHeight,
                         e.what());
                return false;
            }
            // R6-410: Check return value to prevent corrupted pool balance during crash recovery.
            std::string reject_reason;
            const auto value_balance = TryGetShieldedStateValueBalance(bundle, reject_reason);
            if (!value_balance.has_value()) {
                LogError("RollforwardBlock(): failed to derive pool balance delta at %d: %s\n",
                         pindex->nHeight,
                         reject_reason);
                return false;
            }
            if (!projected_pool.ApplyValueBalance(*value_balance)) {
                LogError("RollforwardBlock(): shielded pool balance transition rejected at %d\n", pindex->nHeight);
                return false;
            }
        }
    }

    if (m_chainman.m_shielded_nullifiers) {
        if (!block_account_leaves.empty()) {
            try {
                if (!projected_account_registry.Append(
                        Span<const shielded::registry::ShieldedAccountLeaf>{block_account_leaves.data(),
                                                                            block_account_leaves.size()})) {
                    LogError("RollforwardBlock(): shielded account-registry append failed at %d\n", pindex->nHeight);
                    return false;
                }
            } catch (const std::exception& e) {
                LogError("RollforwardBlock(): shielded account-registry append exception at %d: %s\n",
                         pindex->nHeight,
                         e.what());
                return false;
            }
            if (UseAccountRegistryEntryCountLimit(consensus, pindex->nHeight) &&
                projected_account_registry.Size() > consensus.nMaxShieldedAccountRegistryEntries) {
                LogError("RollforwardBlock(): shielded account-registry size limit exceeded at %d\n",
                         pindex->nHeight);
                return false;
            }
        }
        for (const auto& [commitment, account] : block_smile_public_accounts) {
            projected_public_accounts[commitment] = account;
        }
        for (const auto& [commitment, account_leaf_commitment] : block_account_leaf_entries) {
            projected_account_leaf_commitments[commitment] = account_leaf_commitment;
        }
        if (BlockHasShieldedBundle(block)) {
            const auto prepared_marker = MakePreparedShieldedMutationMarker(
                pindex->pprev,
                pindex,
                projected_tree,
                projected_pool,
                projected_account_registry);
            if (!prepared_marker.has_value() ||
                !WriteShieldedMutationMarker(m_chainman.m_shielded_nullifiers.get(), *prepared_marker)) {
                LogError("RollforwardBlock(): shielded mutation marker write failed at %d\n", pindex->nHeight);
                return false;
            }
        }
        if (!block_nullifiers.empty() && !m_chainman.m_shielded_nullifiers->Insert(block_nullifiers)) {
            LogError("RollforwardBlock(): shielded nullifier insert failed at %d\n", pindex->nHeight);
            return false;
        }
        if (!block_recovery_exit_commitments.empty() &&
            !m_chainman.m_shielded_nullifiers->InsertRecoveryExitCommitments(block_recovery_exit_commitments)) {
            LogError("RollforwardBlock(): recovery-exit commitment insert failed at %d\n", pindex->nHeight);
            return false;
        }
        if (!m_chainman.m_shielded_nullifiers->WritePoolBalance(projected_pool.GetBalance())) {
            LogError("RollforwardBlock(): shielded pool balance write failed at %d\n", pindex->nHeight);
            return false;
        }
        m_chainman.m_shielded_merkle_tree = std::move(projected_tree);
        m_chainman.m_shielded_account_registry = std::move(projected_account_registry);
        m_chainman.m_shielded_smile_public_accounts = std::move(projected_public_accounts);
        m_chainman.m_shielded_account_leaf_commitments =
            std::move(projected_account_leaf_commitments);
        if (!block_smile_public_accounts.empty() || !block_account_leaf_entries.empty()) {
            m_chainman.InvalidateShieldedAccountStateSnapshotCaches();
        }
        m_chainman.m_shielded_pool_balance = projected_pool;
    }
    return true;
}

bool Chainstate::ReplayBlocks()
{
    LOCK(cs_main);

    CCoinsView& db = this->CoinsDB();
    CCoinsViewCache cache(&db);

    std::vector<uint256> hashHeads = db.GetHeadBlocks();
    if (hashHeads.empty()) return true; // We're already in a consistent state.
    if (hashHeads.size() != 2) {
        LogError("ReplayBlocks(): unknown inconsistent state\n");
        return false;
    }

    m_chainman.GetNotifications().progress(_("Replaying blocks…"), 0, false);
    LogPrintf("Replaying blocks\n");

    const CBlockIndex* pindexOld = nullptr;  // Old tip during the interrupted flush.
    const CBlockIndex* pindexNew;            // New tip during the interrupted flush.
    const CBlockIndex* pindexFork = nullptr; // Latest block common to both the old and the new tip.

    if (m_blockman.m_block_index.count(hashHeads[0]) == 0) {
        LogError("ReplayBlocks(): reorganization to unknown block requested\n");
        return false;
    }
    pindexNew = &(m_blockman.m_block_index[hashHeads[0]]);

    if (!hashHeads[1].IsNull()) { // The old tip is allowed to be 0, indicating it's the first flush.
        if (m_blockman.m_block_index.count(hashHeads[1]) == 0) {
            LogError("ReplayBlocks(): reorganization from unknown block requested\n");
            return false;
        }
        pindexOld = &(m_blockman.m_block_index[hashHeads[1]]);
        pindexFork = LastCommonAncestor(pindexOld, pindexNew);
        assert(pindexFork != nullptr);
    }

    // Rollback along the old branch.
    while (pindexOld != pindexFork) {
        if (pindexOld->nHeight > 0) { // Never disconnect the genesis block.
            CBlock block;
            if (!m_blockman.ReadBlock(block, *pindexOld, /*lowprio=*/true)) {
                LogError("RollbackBlock(): ReadBlock() failed at %d, hash=%s\n", pindexOld->nHeight, pindexOld->GetBlockHash().ToString());
                return false;
            }
            LogPrintf("Rolling back %s (%i)\n", pindexOld->GetBlockHash().ToString(), pindexOld->nHeight);
            // Replay must keep shielded state in lock-step with the UTXO set.
            DisconnectResult res = DisconnectBlock(block, pindexOld, cache, /*apply_shielded_state=*/true);
            if (res == DISCONNECT_FAILED) {
                LogError("RollbackBlock(): DisconnectBlock failed at %d, hash=%s\n", pindexOld->nHeight, pindexOld->GetBlockHash().ToString());
                return false;
            }
            // If DISCONNECT_UNCLEAN is returned, it means a non-existing UTXO was deleted, or an existing UTXO was
            // overwritten. It corresponds to cases where the block-to-be-disconnect never had all its operations
            // applied to the UTXO set. However, as both writing a UTXO and deleting a UTXO are idempotent operations,
            // the result is still a version of the UTXO set with the effects of that block undone.
        }
        pindexOld = pindexOld->pprev;
    }

    // Roll forward from the forking point to the new tip.
    int nForkHeight = pindexFork ? pindexFork->nHeight : 0;
    for (int nHeight = nForkHeight + 1; nHeight <= pindexNew->nHeight; ++nHeight) {
        const CBlockIndex& pindex{*Assert(pindexNew->GetAncestor(nHeight))};

        LogPrintf("Rolling forward %s (%i)\n", pindex.GetBlockHash().ToString(), nHeight);
        m_chainman.GetNotifications().progress(_("Replaying blocks…"), (int)((nHeight - nForkHeight) * 100.0 / (pindexNew->nHeight - nForkHeight)), false);
        if (!RollforwardBlock(&pindex, cache)) return false;
    }

    cache.SetBestBlock(pindexNew->GetBlockHash());
    cache.Flush();
    m_chainman.GetNotifications().progress(bilingual_str{}, 100, false);
    return true;
}

bool Chainstate::NeedsRedownload() const
{
    AssertLockHeld(cs_main);

    // At and above m_params.SegwitHeight, segwit consensus rules must be validated
    CBlockIndex* block{m_chain.Tip()};

    while (block != nullptr && DeploymentActiveAt(*block, m_chainman, Consensus::DEPLOYMENT_SEGWIT)) {
        if (!(block->nStatus & BLOCK_OPT_WITNESS)) {
            // block is insufficiently validated for a segwit client
            return true;
        }
        block = block->pprev;
    }

    return false;
}

void Chainstate::ClearBlockIndexCandidates()
{
    AssertLockHeld(::cs_main);
    setBlockIndexCandidates.clear();
}

void Chainstate::PopulateBlockIndexCandidates()
{
    AssertLockHeld(::cs_main);
    const CBlockIndex* active_snapshot_base{m_chainman.GetSnapshotBaseBlock()};

    for (CBlockIndex* pindex : m_blockman.GetAllBlockIndices()) {
        // An assumeutxo chainstate needs its own snapshot base as an immediate
        // candidate even when that block has not been downloaded yet. Do not
        // add it to the historical/background chainstate: the latter will add
        // it normally after processing the preceding transactions.
        const bool background_snapshot_base_parent_unprocessed{
            this != &m_chainman.ActiveChainstate() &&
            pindex == active_snapshot_base &&
            pindex->pprev &&
            !pindex->pprev->HaveNumChainTxs()};
        if (background_snapshot_base_parent_unprocessed) continue;

        if (pindex == SnapshotBase() ||
            (pindex->IsValid(BLOCK_VALID_TRANSACTIONS) &&
             (pindex->HaveNumChainTxs() || pindex->pprev == nullptr))) {
            TryAddBlockIndexCandidate(pindex);
        }
    }
}

bool ChainstateManager::LoadBlockIndex()
{
    AssertLockHeld(cs_main);
    // Load block index from databases
    if (m_blockman.m_blockfiles_indexed) {
        bool ret{m_blockman.LoadBlockIndexDB(SnapshotBlockhash(), m_attested_assumeutxo)};
        if (!ret) return false;
        if (m_blockman.m_block_tree_db &&
            !m_blockman.m_block_tree_db->ReadParkedReorgBranches(m_parked_reorg_branch_roots)) {
            return false;
        }
        if (!LoadReorgRecoveryRecord()) {
            // A present-but-malformed recovery record is not safe to ignore:
            // doing so could silently re-enable the losing branch after a
            // crash. Fail startup before candidate selection instead.
            return false;
        }
        if (!m_parked_reorg_branch_roots.empty()) {
            LogWarning("%s: loaded %u locally parked reorg branch root(s)\n",
                       __func__, static_cast<unsigned>(m_parked_reorg_branch_roots.size()));
        }

        m_blockman.ScanAndUnlinkAlreadyPrunedFiles();

        std::vector<CBlockIndex*> vSortedByHeight{m_blockman.GetAllBlockIndices()};
        std::sort(vSortedByHeight.begin(), vSortedByHeight.end(),
                  CBlockIndexHeightOnlyComparator());

        for (CBlockIndex* pindex : vSortedByHeight) {
            if (m_interrupt) return false;
            NoteAuthenticatedRecoveryCandidate(pindex);
            if (pindex->nStatus & BLOCK_FAILED_MASK && (!m_best_invalid || pindex->nChainWork > m_best_invalid->nChainWork)) {
                m_best_invalid = pindex;
            }
            if (pindex->IsValid(BLOCK_VALID_TREE) &&
                (m_best_header == nullptr || PreferTrustAdjustedHeader(*m_best_header, *pindex))) {
                SetBestHeader(pindex);
            }
        }
        SetBestHeader(m_best_header);
    }
    return true;
}

bool Chainstate::LoadGenesisBlock()
{
    LOCK(cs_main);

    const CChainParams& params{m_chainman.GetParams()};

    // Check whether we're already initialized by checking for genesis in
    // m_blockman.m_block_index. Note that we can't use m_chain here, since it is
    // set based on the coins db, not the block index db, which is the only
    // thing loaded at this point.
    if (m_blockman.m_block_index.count(params.GenesisBlock().GetHash()))
        return true;

    try {
        const CBlock& block = params.GenesisBlock();
        FlatFilePos blockPos{m_blockman.WriteBlock(block, 0)};
        if (blockPos.IsNull()) {
            LogError("%s: writing genesis block to disk failed\n", __func__);
            return false;
        }
        CBlockIndex* pindex = m_blockman.AddToBlockIndex(block, m_chainman.m_best_header);
        m_chainman.ReceivedBlockTransactions(block, pindex, blockPos);
    } catch (const std::runtime_error& e) {
        LogError("%s: failed to write genesis block: %s\n", __func__, e.what());
        return false;
    }

    return true;
}

void ChainstateManager::LoadExternalBlockFile(
    AutoFile& file_in,
    FlatFilePos* dbp,
    std::multimap<uint256, FlatFilePos>* blocks_with_unknown_parent)
{
    // Either both should be specified (-reindex), or neither (-loadblock).
    assert(!dbp == !blocks_with_unknown_parent);

    const auto start{SteadyClock::now()};
    const CChainParams& params{GetParams()};

    int nLoaded = 0;
    try {
        IOPRIO_IDLER(/*lowprio=*/true);
        file_in.SetIdlePriority();

        BufferedFile blkdat{file_in, 2 * MAX_BLOCK_SERIALIZED_SIZE, MAX_BLOCK_SERIALIZED_SIZE + 8};
        // nRewind indicates where to resume scanning in case something goes wrong,
        // such as a block fails to deserialize.
        uint64_t nRewind = blkdat.GetPos();
        while (!blkdat.eof()) {
            if (m_interrupt) return;

            blkdat.SetPos(nRewind);
            nRewind++; // start one byte further next time, in case of failure
            blkdat.SetLimit(); // remove former limit
            unsigned int nSize = 0;
            try {
                // locate a header
                MessageStartChars buf;
                blkdat.FindByte(std::byte(params.MessageStart()[0]));
                nRewind = blkdat.GetPos() + 1;
                blkdat >> buf;
                if (buf != params.MessageStart()) {
                    continue;
                }
                // read size
                blkdat >> nSize;
                if (nSize < 80 || nSize > MAX_BLOCK_SERIALIZED_SIZE)
                    continue;
            } catch (const std::exception&) {
                // no valid block header found; don't complain
                // (this happens at the end of every blk.dat file)
                break;
            }
            try {
                // read block header
                const uint64_t nBlockPos{blkdat.GetPos()};
                if (dbp)
                    dbp->nPos = nBlockPos;
                blkdat.SetLimit(nBlockPos + nSize);
                CBlockHeader header;
                blkdat >> header;
                const uint256 hash{header.GetHash()};
                // Skip the rest of this block (this may read from disk into memory); position to the marker before the
                // next block, but it's still possible to rewind to the start of the current block (without a disk read).
                nRewind = nBlockPos + nSize;
                blkdat.SkipTo(nRewind);

                std::shared_ptr<CBlock> pblock{}; // needs to remain available after the cs_main lock is released to avoid duplicate reads from disk

                {
                    LOCK(cs_main);
                    // detect out of order blocks, and store them for later
                    if (hash != params.GetConsensus().hashGenesisBlock && !m_blockman.LookupBlockIndex(header.hashPrevBlock)) {
                        LogDebug(BCLog::REINDEX, "%s: Out of order block %s, parent %s not known\n", __func__, hash.ToString(),
                                 header.hashPrevBlock.ToString());
                        if (dbp && blocks_with_unknown_parent) {
                            blocks_with_unknown_parent->emplace(header.hashPrevBlock, *dbp);
                        }
                        continue;
                    }

                    // process in case the block isn't known yet
                    const CBlockIndex* pindex = m_blockman.LookupBlockIndex(hash);
                    if (!pindex || (pindex->nStatus & BLOCK_HAVE_DATA) == 0) {
                        // This block can be processed immediately; rewind to its start, read and deserialize it.
                        blkdat.SetPos(nBlockPos);
                        pblock = std::make_shared<CBlock>();
                        blkdat >> TX_WITH_WITNESS(*pblock);
                        nRewind = blkdat.GetPos();

                        BlockValidationState state;
                        if (AcceptBlock(pblock, state, nullptr, true, dbp, nullptr, true)) {
                            nLoaded++;
                        }
                        if (state.IsError()) {
                            break;
                        }
                    } else if (hash != params.GetConsensus().hashGenesisBlock && pindex->nHeight % 1000 == 0) {
                        LogDebug(BCLog::REINDEX, "Block Import: already had block %s at height %d\n", hash.ToString(), pindex->nHeight);
                    }
                }

                // Activate the genesis block so normal node progress can continue
                // During first -reindex, this will only connect Genesis since
                // ActivateBestChain only connects blocks which are in the block tree db,
                // which only contains blocks whose parents are in it.
                // But do this only if genesis isn't activated yet, to avoid connecting many blocks
                // without assumevalid in the case of a continuation of a reindex that
                // was interrupted by the user.
                if (hash == params.GetConsensus().hashGenesisBlock && WITH_LOCK(::cs_main, return ActiveHeight()) == -1) {
                    BlockValidationState state;
                    if (!ActiveChainstate().ActivateBestChain(state, nullptr)) {
                        break;
                    }
                }

                if (m_blockman.IsPruneMode() && m_blockman.m_blockfiles_indexed && pblock) {
                    // must update the tip for pruning to work while importing with -loadblock.
                    // this is a tradeoff to conserve disk space at the expense of time
                    // spent updating the tip to be able to prune.
                    // otherwise, ActivateBestChain won't be called by the import process
                    // until after all of the block files are loaded. ActivateBestChain can be
                    // called by concurrent network message processing. but, that is not
                    // reliable for the purpose of pruning while importing.
                    bool activation_failure = false;
                    for (auto c : GetAll()) {
                        BlockValidationState state;
                        if (!c->ActivateBestChain(state, pblock)) {
                            LogDebug(BCLog::REINDEX, "failed to activate chain (%s)\n", state.ToString());
                            activation_failure = true;
                            break;
                        }
                    }
                    if (activation_failure) {
                        break;
                    }
                }

                NotifyHeaderTip();

                if (!blocks_with_unknown_parent) continue;

                // Recursively process earlier encountered successors of this block
                std::deque<uint256> queue;
                queue.push_back(hash);
                while (!queue.empty()) {
                    uint256 head = queue.front();
                    queue.pop_front();
                    auto range = blocks_with_unknown_parent->equal_range(head);
                    while (range.first != range.second) {
                        std::multimap<uint256, FlatFilePos>::iterator it = range.first;
                        std::shared_ptr<CBlock> pblockrecursive = std::make_shared<CBlock>();
                        if (m_blockman.ReadBlock(*pblockrecursive, it->second)) {
                            LogDebug(BCLog::REINDEX, "%s: Processing out of order child %s of %s\n", __func__, pblockrecursive->GetHash().ToString(),
                                    head.ToString());
                            LOCK(cs_main);
                            BlockValidationState dummy;
                            if (AcceptBlock(pblockrecursive, dummy, nullptr, true, &it->second, nullptr, true)) {
                                nLoaded++;
                                queue.push_back(pblockrecursive->GetHash());
                            }
                        }
                        range.first++;
                        blocks_with_unknown_parent->erase(it);
                        NotifyHeaderTip();
                    }
                }
            } catch (const std::exception& e) {
                // historical bugs added extra data to the block files that does not deserialize cleanly.
                // commonly this data is between readable blocks, but it does not really matter. such data is not fatal to the import process.
                // the code that reads the block files deals with invalid data by simply ignoring it.
                // it continues to search for the next {4 byte magic message start bytes + 4 byte length + block} that does deserialize cleanly
                // and passes all of the other block validation checks dealing with POW and the merkle root, etc...
                // we merely note with this informational log message when unexpected data is encountered.
                // we could also be experiencing a storage system read error, or a read of a previous bad write. these are possible, but
                // less likely scenarios. we don't have enough information to tell a difference here.
                // the reindex process is not the place to attempt to clean and/or compact the block files. if so desired, a studious node operator
                // may use knowledge of the fact that the block files are not entirely pristine in order to prepare a set of pristine, and
                // perhaps ordered, block files for later reindexing.
                LogDebug(BCLog::REINDEX, "%s: unexpected data at file offset 0x%x - %s. continuing\n", __func__, (nRewind - 1), e.what());
            }
        }
    } catch (const std::runtime_error& e) {
        GetNotifications().fatalError(strprintf(_("System error while loading external block file: %s"), e.what()));
    }
    LogPrintf("Loaded %i blocks from external file in %dms\n", nLoaded, Ticks<std::chrono::milliseconds>(SteadyClock::now() - start));
}

bool ChainstateManager::ShouldCheckBlockIndex() const
{
    // Assert to verify Flatten() has been called.
    if (!*Assert(m_options.check_block_index)) return false;
    if (FastRandomContext().randrange(*m_options.check_block_index) >= 1) return false;
    return true;
}

void ChainstateManager::CheckBlockIndex()
{
    if (!ShouldCheckBlockIndex()) {
        return;
    }

    LOCK(cs_main);

    // During a reindex, we read the genesis block and call CheckBlockIndex before ActivateBestChain,
    // so we have the genesis block in m_blockman.m_block_index but no active chain. (A few of the
    // tests when iterating the block tree require that m_chain has been initialized.)
    if (ActiveChain().Height() < 0) {
        assert(m_blockman.m_block_index.size() <= 1);
        return;
    }

    // Build forward-pointing data structure for the entire block tree.
    // For performance reasons, indexes of the best header chain are stored in a vector (within CChain).
    // All remaining blocks are stored in a multimap.
    // The best header chain can differ from the active chain: E.g. its entries may belong to blocks that
    // are not yet validated.
    CChain best_hdr_chain;
    assert(m_best_header);
    best_hdr_chain.SetTip(*m_best_header);
    const size_t best_hdr_chain_nodes = best_hdr_chain.Height() < 0
        ? 0
        : static_cast<size_t>(best_hdr_chain.Height()) + 1;

    std::multimap<CBlockIndex*,CBlockIndex*> forward;
    for (auto& [_, block_index] : m_blockman.m_block_index) {
        // Only save indexes in forward that are not part of the best header chain.
        if (!best_hdr_chain.Contains(&block_index)) {
            // Only genesis, which must be part of the best header chain, can have a nullptr parent.
            assert(block_index.pprev);
            forward.emplace(block_index.pprev, &block_index);
        }
    }
    assert(forward.size() + best_hdr_chain_nodes == m_blockman.m_block_index.size());

    CBlockIndex* pindex = best_hdr_chain[0];
    assert(pindex);
    // Iterate over the entire block tree, using depth-first search.
    // Along the way, remember whether there are blocks on the path from genesis
    // block being explored which are the first to have certain properties.
    size_t nNodes = 0;
    int nHeight = 0;
    CBlockIndex* pindexFirstInvalid = nullptr; // Oldest ancestor of pindex which is invalid.
    CBlockIndex* pindexFirstMissing = nullptr; // Oldest ancestor of pindex which does not have BLOCK_HAVE_DATA, since assumeutxo snapshot if used.
    CBlockIndex* pindexFirstNeverProcessed = nullptr; // Oldest ancestor of pindex for which nTx == 0, since assumeutxo snapshot if used.
    CBlockIndex* pindexFirstNotTreeValid = nullptr; // Oldest ancestor of pindex which does not have BLOCK_VALID_TREE (regardless of being valid or not).
    CBlockIndex* pindexFirstNotTransactionsValid = nullptr; // Oldest ancestor of pindex which does not have BLOCK_VALID_TRANSACTIONS (regardless of being valid or not), since assumeutxo snapshot if used.
    CBlockIndex* pindexFirstNotChainValid = nullptr; // Oldest ancestor of pindex which does not have BLOCK_VALID_CHAIN (regardless of being valid or not), since assumeutxo snapshot if used.
    CBlockIndex* pindexFirstNotScriptsValid = nullptr; // Oldest ancestor of pindex which does not have BLOCK_VALID_SCRIPTS (regardless of being valid or not), since assumeutxo snapshot if used.

    // After checking an assumeutxo snapshot block, reset pindexFirst pointers
    // to earlier blocks that have not been downloaded or validated yet, so
    // checks for later blocks can assume the earlier blocks were validated and
    // be stricter, testing for more requirements.
    const CBlockIndex* snap_base{GetSnapshotBaseBlock()};
    CBlockIndex *snap_first_missing{}, *snap_first_notx{}, *snap_first_notv{}, *snap_first_nocv{}, *snap_first_nosv{};
    auto snap_update_firsts = [&] {
        if (pindex == snap_base) {
            std::swap(snap_first_missing, pindexFirstMissing);
            std::swap(snap_first_notx, pindexFirstNeverProcessed);
            std::swap(snap_first_notv, pindexFirstNotTransactionsValid);
            std::swap(snap_first_nocv, pindexFirstNotChainValid);
            std::swap(snap_first_nosv, pindexFirstNotScriptsValid);
        }
    };

    while (pindex != nullptr) {
        nNodes++;
        if (pindexFirstInvalid == nullptr && pindex->nStatus & BLOCK_FAILED_VALID) pindexFirstInvalid = pindex;
        if (pindexFirstMissing == nullptr && !(pindex->nStatus & BLOCK_HAVE_DATA)) {
            pindexFirstMissing = pindex;
        }
        if (pindexFirstNeverProcessed == nullptr && pindex->nTx == 0) pindexFirstNeverProcessed = pindex;
        if (pindex->pprev != nullptr && pindexFirstNotTreeValid == nullptr && (pindex->nStatus & BLOCK_VALID_MASK) < BLOCK_VALID_TREE) pindexFirstNotTreeValid = pindex;

        if (pindex->pprev != nullptr) {
            if (pindexFirstNotTransactionsValid == nullptr &&
                    (pindex->nStatus & BLOCK_VALID_MASK) < BLOCK_VALID_TRANSACTIONS) {
                pindexFirstNotTransactionsValid = pindex;
            }

            if (pindexFirstNotChainValid == nullptr &&
                    (pindex->nStatus & BLOCK_VALID_MASK) < BLOCK_VALID_CHAIN) {
                pindexFirstNotChainValid = pindex;
            }

            if (pindexFirstNotScriptsValid == nullptr &&
                    (pindex->nStatus & BLOCK_VALID_MASK) < BLOCK_VALID_SCRIPTS) {
                pindexFirstNotScriptsValid = pindex;
            }
        }

        // Begin: actual consistency checks.
        if (pindex->pprev == nullptr) {
            // Genesis block checks.
            assert(pindex->GetBlockHash() == GetConsensus().hashGenesisBlock); // Genesis block's hash must match.
            for (auto c : GetAll()) {
                if (c->m_chain.Genesis() != nullptr) {
                    assert(pindex == c->m_chain.Genesis()); // The chain's genesis block must be this block.
                }
            }
        }
        // nSequenceId can't be set higher than SEQ_ID_INIT_FROM_DISK{1} for blocks that aren't linked
        // (negative is used for preciousblock, SEQ_ID_BEST_CHAIN_FROM_DISK{0} for active chain when loaded from disk)
        if (!pindex->HaveNumChainTxs()) assert(pindex->nSequenceId <= SEQ_ID_INIT_FROM_DISK);
        // VALID_TRANSACTIONS is equivalent to nTx > 0 for all nodes (whether or not pruning has occurred).
        // HAVE_DATA is only equivalent to nTx > 0 (or VALID_TRANSACTIONS) if no pruning has occurred.
        if (!m_blockman.m_have_pruned) {
            // If we've never pruned, then HAVE_DATA should be equivalent to nTx > 0
            assert(!(pindex->nStatus & BLOCK_HAVE_DATA) == (pindex->nTx == 0));
            assert(pindexFirstMissing == pindexFirstNeverProcessed);
        } else {
            // If we have pruned, then we can only say that HAVE_DATA implies nTx > 0
            if (pindex->nStatus & BLOCK_HAVE_DATA) assert(pindex->nTx > 0);
        }
        if (pindex->nStatus & BLOCK_HAVE_UNDO) assert(pindex->nStatus & BLOCK_HAVE_DATA);
        if (snap_base && snap_base->GetAncestor(pindex->nHeight) == pindex) {
            // Assumed-valid blocks should connect to the main chain.
            assert((pindex->nStatus & BLOCK_VALID_MASK) >= BLOCK_VALID_TREE);
        }
        // There should only be an nTx value if we have
        // actually seen a block's transactions.
        assert(((pindex->nStatus & BLOCK_VALID_MASK) >= BLOCK_VALID_TRANSACTIONS) == (pindex->nTx > 0)); // This is pruning-independent.
        // All parents having had data (at some point) is equivalent to all parents being VALID_TRANSACTIONS, which is equivalent to HaveNumChainTxs().
        // HaveNumChainTxs will also be set in the assumeutxo snapshot block from snapshot metadata.
        assert((pindexFirstNeverProcessed == nullptr || pindex == snap_base) == pindex->HaveNumChainTxs());
        assert((pindexFirstNotTransactionsValid == nullptr || pindex == snap_base) == pindex->HaveNumChainTxs());
        assert(pindex->nHeight == nHeight); // nHeight must be consistent.
        assert(pindex->pprev == nullptr || pindex->nChainWork >= pindex->pprev->nChainWork); // For every block except the genesis block, the chainwork must be larger than the parent's.
        // Authenticated-chainwork invariants (audit: contiguous authenticated prefix).
        // Authenticated work is a monotone sub-total of claimed work: it never exceeds
        // nChainWork (the GetTrustAdjustedChainWork subtraction relies on this) and never
        // decreases parent->child. UpdateAuthenticatedChainWork maintains both; asserting
        // here surfaces any future desync (e.g. a stale value) in tests/regtest.
        assert(pindex->nAuthenticatedChainWork <= pindex->nChainWork);
        assert(pindex->pprev == nullptr || pindex->nAuthenticatedChainWork >= pindex->pprev->nAuthenticatedChainWork);
        assert(nHeight < 2 || (pindex->pskip && (pindex->pskip->nHeight < nHeight))); // The pskip pointer must point back for all but the first 2 blocks.
        assert(pindexFirstNotTreeValid == nullptr); // All m_blockman.m_block_index entries must at least be TREE valid
        if ((pindex->nStatus & BLOCK_VALID_MASK) >= BLOCK_VALID_TREE) assert(pindexFirstNotTreeValid == nullptr); // TREE valid implies all parents are TREE valid
        if ((pindex->nStatus & BLOCK_VALID_MASK) >= BLOCK_VALID_CHAIN) assert(pindexFirstNotChainValid == nullptr); // CHAIN valid implies all parents are CHAIN valid
        if ((pindex->nStatus & BLOCK_VALID_MASK) >= BLOCK_VALID_SCRIPTS) assert(pindexFirstNotScriptsValid == nullptr); // SCRIPTS valid implies all parents are SCRIPTS valid
        if (pindexFirstInvalid == nullptr) {
            // Checks for not-invalid blocks.
            assert((pindex->nStatus & BLOCK_FAILED_MASK) == 0); // The failed mask cannot be set for blocks without invalid parents.
        }
        // Make sure m_chain_tx_count sum is correctly computed.
        if (!pindex->pprev) {
            // If no previous block, nTx and m_chain_tx_count must be the same.
            assert(pindex->m_chain_tx_count == pindex->nTx);
        } else if (pindex->pprev->m_chain_tx_count > 0 && pindex->nTx > 0) {
            // If previous m_chain_tx_count is set and number of transactions in block is known, sum must be set.
            assert(pindex->m_chain_tx_count == pindex->nTx + pindex->pprev->m_chain_tx_count);
        } else {
            // Otherwise m_chain_tx_count should only be set if this is a snapshot
            // block, and must be set if it is.
            assert((pindex->m_chain_tx_count != 0) == (pindex == snap_base));
        }

        // Chainstate-specific checks on setBlockIndexCandidates
        for (auto c : GetAll()) {
            if (c->m_chain.Tip() == nullptr) continue;
            // Two main factors determine whether pindex is a candidate in
            // setBlockIndexCandidates:
            //
            // - If pindex has less work than the chain tip, it should not be a
            //   candidate, and this will be asserted below. Otherwise it is a
            //   potential candidate.
            //
            // - If pindex or one of its parent blocks back to the genesis block
            //   or an assumeutxo snapshot never downloaded transactions
            //   (pindexFirstNeverProcessed is non-null), it should not be a
            //   candidate, and this will be asserted below. The only exception
            //   is if pindex itself is an assumeutxo snapshot block. Then it is
            //   also a potential candidate.
            if (!CBlockIndexWorkComparator()(pindex, c->m_chain.Tip()) && (pindexFirstNeverProcessed == nullptr || pindex == snap_base)) {
                // If pindex was detected as invalid (pindexFirstInvalid is
                // non-null), it is not required to be in
                // setBlockIndexCandidates.
                if (pindexFirstInvalid == nullptr) {
                    // If pindex and all its parents back to the genesis block
                    // or an assumeutxo snapshot block downloaded transactions,
                    // and the transactions were not pruned (pindexFirstMissing
                    // is null), it is a potential candidate. The check
                    // excludes pruned blocks, because if any blocks were
                    // pruned between pindex and the current chain tip, pindex will
                    // only temporarily be added to setBlockIndexCandidates,
                    // before being moved to m_blocks_unlinked. This check
                    // could be improved to verify that if all blocks between
                    // the chain tip and pindex have data, pindex must be a
                    // candidate.
                    //
                    // If pindex is the chain tip, it also is a potential
                    // candidate.
                    //
                    // If the chainstate was loaded from a snapshot and pindex
                    // is the base of the snapshot, pindex is also a potential
                    // candidate.
                    if (pindexFirstMissing == nullptr || pindex == c->m_chain.Tip() || pindex == c->SnapshotBase()) {
                        // If this chainstate is the active chainstate, pindex
                        // must be in setBlockIndexCandidates. Otherwise, this
                        // chainstate is a background validation chainstate, and
                        // pindex only needs to be added if it is an ancestor of
                        // the snapshot that is being validated.
                        //
                        // Parked branches, armed losing-tip extensions, and
                        // trusted-mirror gate refusals are not candidates
                        // (FindMostWorkChain / TryAddBlockIndexCandidate).
                        if (!c->m_chainman.IsOnParkedReorgBranch(pindex) &&
                            !c->m_chainman.ShouldDeferLosingTipExtension(pindex) &&
                            TrustedMirrorShouldConsiderMostWorkCandidate(c->m_chain.Tip(), pindex) &&
                            (c == &ActiveChainstate() || snap_base->GetAncestor(pindex->nHeight) == pindex)) {
                            assert(c->setBlockIndexCandidates.count(pindex));
                        }
                    }
                    // If some parent is missing, then it could be that this block was in
                    // setBlockIndexCandidates but had to be removed because of the missing data.
                    // In this case it must be in m_blocks_unlinked -- see test below.
                }
            } else { // If this block sorts worse than the current tip or some ancestor's block has never been seen, it cannot be in setBlockIndexCandidates.
                // Attested abandon-fork targets are inserted so ActivateBestChain
                // can switch to them (heavier unattested tip, or equal-work
                // lost sibling).
                if (!c->m_chainman.IsAttestedAbandonForkCandidate(pindex)) {
                    assert(c->setBlockIndexCandidates.count(pindex) == 0);
                }
            }
        }
        // Check whether this block is in m_blocks_unlinked.
        std::pair<std::multimap<CBlockIndex*,CBlockIndex*>::iterator,std::multimap<CBlockIndex*,CBlockIndex*>::iterator> rangeUnlinked = m_blockman.m_blocks_unlinked.equal_range(pindex->pprev);
        bool foundInUnlinked = false;
        while (rangeUnlinked.first != rangeUnlinked.second) {
            assert(rangeUnlinked.first->first == pindex->pprev);
            if (rangeUnlinked.first->second == pindex) {
                foundInUnlinked = true;
                break;
            }
            rangeUnlinked.first++;
        }
        if (pindex->pprev && (pindex->nStatus & BLOCK_HAVE_DATA) && pindexFirstNeverProcessed != nullptr && pindexFirstInvalid == nullptr) {
            // If this block has block data available, some parent was never received, and has no invalid parents, it must be in m_blocks_unlinked.
            assert(foundInUnlinked);
        }
        if (!(pindex->nStatus & BLOCK_HAVE_DATA)) assert(!foundInUnlinked); // Can't be in m_blocks_unlinked if we don't HAVE_DATA
        if (pindexFirstMissing == nullptr) assert(!foundInUnlinked); // We aren't missing data for any parent -- cannot be in m_blocks_unlinked.
        if (pindex->pprev && (pindex->nStatus & BLOCK_HAVE_DATA) && pindexFirstNeverProcessed == nullptr && pindexFirstMissing != nullptr) {
            // We HAVE_DATA for this block, have received data for all parents at some point, but we're currently missing data for some parent.
            assert(m_blockman.m_have_pruned);
            // This block may have entered m_blocks_unlinked if:
            //  - it has a descendant that at some point had more work than the
            //    tip, and
            //  - we tried switching to that descendant but were missing
            //    data for some intermediate block between m_chain and the
            //    tip.
            // So if this block is itself better than any m_chain.Tip() and it wasn't in
            // setBlockIndexCandidates, then it must be in m_blocks_unlinked.
            for (auto c : GetAll()) {
                const bool is_active = c == &ActiveChainstate();
                if (!CBlockIndexWorkComparator()(pindex, c->m_chain.Tip()) &&
                    c->setBlockIndexCandidates.count(pindex) == 0 &&
                    !c->m_chainman.IsOnParkedReorgBranch(pindex) &&
                    !c->m_chainman.ShouldDeferLosingTipExtension(pindex) &&
                    TrustedMirrorShouldConsiderMostWorkCandidate(c->m_chain.Tip(), pindex)) {
                    if (pindexFirstInvalid == nullptr) {
                        if (is_active || snap_base->GetAncestor(pindex->nHeight) == pindex) {
                            assert(foundInUnlinked);
                        }
                    }
                }
            }
        }
        // assert(pindex->GetBlockHash() == pindex->GetBlockHeader().GetHash()); // Perhaps too slow
        // End: actual consistency checks.


        // Try descending into the first subnode. Always process forks first and the best header chain after.
        snap_update_firsts();
        std::pair<std::multimap<CBlockIndex*,CBlockIndex*>::iterator,std::multimap<CBlockIndex*,CBlockIndex*>::iterator> range = forward.equal_range(pindex);
        if (range.first != range.second) {
            // A subnode not part of the best header chain was found.
            pindex = range.first->second;
            nHeight++;
            continue;
        } else if (best_hdr_chain.Contains(pindex)) {
            // Descend further into best header chain.
            nHeight++;
            pindex = best_hdr_chain[nHeight];
            if (!pindex) break; // we are finished, since the best header chain is always processed last
            continue;
        }
        // This is a leaf node.
        // Move upwards until we reach a node of which we have not yet visited the last child.
        while (pindex) {
            // We are going to either move to a parent or a sibling of pindex.
            snap_update_firsts();
            // If pindex was the first with a certain property, unset the corresponding variable.
            if (pindex == pindexFirstInvalid) pindexFirstInvalid = nullptr;
            if (pindex == pindexFirstMissing) pindexFirstMissing = nullptr;
            if (pindex == pindexFirstNeverProcessed) pindexFirstNeverProcessed = nullptr;
            if (pindex == pindexFirstNotTreeValid) pindexFirstNotTreeValid = nullptr;
            if (pindex == pindexFirstNotTransactionsValid) pindexFirstNotTransactionsValid = nullptr;
            if (pindex == pindexFirstNotChainValid) pindexFirstNotChainValid = nullptr;
            if (pindex == pindexFirstNotScriptsValid) pindexFirstNotScriptsValid = nullptr;
            // Find our parent.
            CBlockIndex* pindexPar = pindex->pprev;
            // Find which child we just visited.
            std::pair<std::multimap<CBlockIndex*,CBlockIndex*>::iterator,std::multimap<CBlockIndex*,CBlockIndex*>::iterator> rangePar = forward.equal_range(pindexPar);
            while (rangePar.first->second != pindex) {
                assert(rangePar.first != rangePar.second); // Our parent must have at least the node we're coming from as child.
                rangePar.first++;
            }
            // Proceed to the next one.
            rangePar.first++;
            if (rangePar.first != rangePar.second) {
                // Move to a sibling not part of the best header chain.
                pindex = rangePar.first->second;
                break;
            } else if (pindexPar == best_hdr_chain[nHeight - 1]) {
                // Move to pindex's sibling on the best-chain, if it has one.
                pindex = best_hdr_chain[nHeight];
                // There will not be a next block if (and only if) parent block is the best header.
                assert((pindex == nullptr) == (pindexPar == best_hdr_chain.Tip()));
                break;
            } else {
                // Move up further.
                pindex = pindexPar;
                nHeight--;
                continue;
            }
        }
    }

    // Check that we actually traversed the entire block index.
    assert(nNodes == forward.size() + best_hdr_chain_nodes);
}

std::string Chainstate::ToString()
{
    AssertLockHeld(::cs_main);
    CBlockIndex* tip = m_chain.Tip();
    return strprintf("Chainstate [%s] @ height %d (%s)",
                     m_from_snapshot_blockhash ? "snapshot" : "ibd",
                     tip ? tip->nHeight : -1, tip ? tip->GetBlockHash().ToString() : "null");
}

bool Chainstate::ResizeCoinsCaches(size_t coinstip_size, size_t coinsdb_size)
{
    AssertLockHeld(::cs_main);
    if (coinstip_size == m_coinstip_cache_size_bytes &&
            coinsdb_size == m_coinsdb_cache_size_bytes) {
        // Cache sizes are unchanged, no need to continue.
        return true;
    }
    size_t old_coinstip_size = m_coinstip_cache_size_bytes;
    m_coinstip_cache_size_bytes = coinstip_size;
    m_coinsdb_cache_size_bytes = coinsdb_size;
    CoinsDB().ResizeCache(coinsdb_size);

    LogPrintf("[%s] resized coinsdb cache to %.1f MiB\n",
        this->ToString(), coinsdb_size * (1.0 / 1024 / 1024));
    LogPrintf("[%s] resized coinstip cache to %.1f MiB\n",
        this->ToString(), coinstip_size * (1.0 / 1024 / 1024));

    BlockValidationState state;
    bool ret;

    if (coinstip_size > old_coinstip_size) {
        // Likely no need to flush if cache sizes have grown.
        ret = FlushStateToDisk(state, FlushStateMode::IF_NEEDED);
    } else {
        // Otherwise, flush state to disk and deallocate the in-memory coins map.
        ret = FlushStateToDisk(state, FlushStateMode::FORCE_FLUSH);
    }
    return ret;
}

//! Guess how far we are in the verification process at the given block index
//! require cs_main if pindex has not been validated yet (because m_chain_tx_count might be unset)
double ChainstateManager::GuessVerificationProgress(const CBlockIndex* pindex) const
{
    const ChainTxData& data{GetParams().TxData()};
    if (pindex == nullptr) {
        return 0.0;
    }

    if (pindex->m_chain_tx_count == 0) {
        LogDebug(BCLog::VALIDATION, "Block %d has unset m_chain_tx_count. Unable to estimate verification progress.\n", pindex->nHeight);
        return 0.0;
    }

    int64_t nNow = time(nullptr);

    double fTxTotal;

    if (pindex->m_chain_tx_count <= data.tx_count) {
        fTxTotal = data.tx_count + (nNow - data.nTime) * data.dTxRate;
    } else {
        fTxTotal = pindex->m_chain_tx_count + (nNow - pindex->GetBlockTime()) * data.dTxRate;
    }

    return std::min<double>(pindex->m_chain_tx_count / fTxTotal, 1.0);
}

std::optional<uint256> ChainstateManager::SnapshotBlockhash() const
{
    LOCK(::cs_main);
    if (m_active_chainstate && m_active_chainstate->m_from_snapshot_blockhash) {
        // If a snapshot chainstate exists, it will always be our active.
        return m_active_chainstate->m_from_snapshot_blockhash;
    }
    return std::nullopt;
}

std::vector<Chainstate*> ChainstateManager::GetAll()
{
    LOCK(::cs_main);
    std::vector<Chainstate*> out;

    for (Chainstate* cs : {m_ibd_chainstate.get(), m_snapshot_chainstate.get()}) {
        if (this->IsUsable(cs)) out.push_back(cs);
    }

    return out;
}

Chainstate& ChainstateManager::InitializeChainstate(CTxMemPool* mempool)
{
    AssertLockHeld(::cs_main);
    assert(!m_ibd_chainstate);
    assert(!m_active_chainstate);

    m_ibd_chainstate = std::make_unique<Chainstate>(mempool, m_blockman, *this);
    m_active_chainstate = m_ibd_chainstate.get();
    return *m_active_chainstate;
}

[[nodiscard]] static bool DeleteCoinsDBFromDisk(const fs::path db_path, bool is_snapshot)
    EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);

    if (is_snapshot) {
        fs::path base_blockhash_path = db_path / node::SNAPSHOT_BLOCKHASH_FILENAME;
        fs::path attested_path = db_path / node::SNAPSHOT_ATTESTED_ASSUMEUTXO_FILENAME;

        try {
            bool existed = fs::remove(base_blockhash_path);
            if (!existed) {
                LogPrintf("[snapshot] snapshot chainstate dir being removed lacks %s file\n",
                          fs::PathToString(node::SNAPSHOT_BLOCKHASH_FILENAME));
            }
        } catch (const fs::filesystem_error& e) {
            LogPrintf("[snapshot] failed to remove file %s: %s\n",
                    fs::PathToString(base_blockhash_path), fsbridge::get_filesystem_error_message(e));
        }
        try {
            fs::remove(attested_path);
        } catch (const fs::filesystem_error& e) {
            LogPrintf("[snapshot] failed to remove file %s: %s\n",
                    fs::PathToString(attested_path), fsbridge::get_filesystem_error_message(e));
        }
    }

    std::string path_str = fs::PathToString(db_path);
    LogPrintf("Removing leveldb dir at %s\n", path_str);

    // We have to destruct before this call leveldb::DB in order to release the db
    // lock, otherwise `DestroyDB` will fail. See `leveldb::~DBImpl()`.
    const bool destroyed = DestroyDB(path_str);

    if (!destroyed) {
        LogPrintf("error: leveldb DestroyDB call failed on %s\n", path_str);
    }

    // Datadir should be removed from filesystem; otherwise initialization may detect
    // it on subsequent statups and get confused.
    //
    // If the base_blockhash_path removal above fails in the case of snapshot
    // chainstates, this will return false since leveldb won't remove a non-empty
    // directory.
    return destroyed && !fs::exists(db_path);
}

bool IsAssumeUtxoSnapshotHeaderCompatible(
    const CBlockIndex* best_header,
    const CBlockIndex* snapshot_start_block,
    bool known_assumeutxo_hash)
{
    if (best_header == nullptr || snapshot_start_block == nullptr) {
        return false;
    }
    if (best_header->nHeight >= snapshot_start_block->nHeight &&
        best_header->GetAncestor(snapshot_start_block->nHeight) ==
            snapshot_start_block) {
        return true;
    }
    return known_assumeutxo_hash &&
           snapshot_start_block->nChainWork > best_header->nAuthenticatedChainWork;
}

bool IsAttestedSnapshotHeaderCompatible(
    const CBlockIndex* best_header,
    const CBlockIndex* snapshot_start_block)
{
    if (best_header == nullptr || snapshot_start_block == nullptr) {
        return false;
    }
    if ((snapshot_start_block->nStatus & BLOCK_FAILED_MASK) != 0) {
        return false;
    }
    if (best_header->nHeight >= snapshot_start_block->nHeight) {
        if (best_header->GetAncestor(snapshot_start_block->nHeight) ==
            snapshot_start_block) {
            return true;
        }
    } else if (snapshot_start_block->GetAncestor(best_header->nHeight) ==
               best_header) {
        return true;
    }
    // Live 2026-08-14: competing 1883xx has more claimed work than attested
    // 187798, so m_best_header is the flood. The snapshot manifest already
    // authenticated the base. Refuse only if the competing header itself
    // carries a quorum or extra authenticated work past the fork.
    const CBlockIndex* fork{
        LastCommonAncestor(best_header, snapshot_start_block)};
    if (fork == nullptr) return false;
    if (node::matmul_trusted::HasQuorum(
            best_header->GetBlockHash(), best_header->nHeight)) {
        return false;
    }
    return best_header->nAuthenticatedChainWork <=
           fork->nAuthenticatedChainWork;
}

util::Result<CBlockIndex*> ChainstateManager::ActivateSnapshot(
        AutoFile& coins_file,
        const SnapshotMetadata& metadata,
        bool in_memory,
        const std::optional<matmul::trusted::UtxoSnapshotManifest>& attested_manifest)
{
    uint256 base_blockhash = metadata.m_base_blockhash;
    std::optional<ShieldedSnapshotSectionHeader> shielded_snapshot_section;
    std::optional<AssumeutxoData> attested_au;
    if (attested_manifest) {
        const auto verified{
            node::matmul_trusted::VerifyUtxoSnapshotManifest(*attested_manifest)};
        if (verified != matmul::trusted::UtxoSnapshotVerifyResult::Valid) {
            return util::Error{Untranslated(strprintf(
                "Attested snapshot manifest rejected: %s",
                matmul::trusted::UtxoSnapshotVerifyResultName(verified)))};
        }
        if (!metadata.HasShieldedSection()) {
            return util::Error{Untranslated(
                "Attested snapshot must contain a shielded state section")};
        }
        const auto& statement{attested_manifest->statement};
        attested_au = AssumeutxoData{
            .height = statement.block_height,
            .hash_serialized = AssumeutxoHash{statement.hash_serialized},
            .m_chain_tx_count = statement.m_chain_tx_count,
            .blockhash = statement.block_hash,
            .shielded_state_commitment = statement.shielded_state_commitment,
        };
    }

    if (this->SnapshotBlockhash()) {
        return util::Error{Untranslated("Can't activate a snapshot-based chainstate more than once")};
    }

    CBlockIndex* snapshot_start_block{};
    const auto snapshot_base_outworks_authenticated_headers{[&]() EXCLUSIVE_LOCKS_REQUIRED(::cs_main) {
        AssertLockHeld(::cs_main);
        if (!snapshot_start_block || !m_best_header) return false;
        const bool same_header_chain{
            m_best_header->nHeight >= snapshot_start_block->nHeight
                ? m_best_header->GetAncestor(snapshot_start_block->nHeight) == snapshot_start_block
                : snapshot_start_block->GetAncestor(m_best_header->nHeight) == m_best_header};
        return same_header_chain ||
               m_best_header->nAuthenticatedChainWork < snapshot_start_block->nChainWork;
    }};

    {
        LOCK(::cs_main);

        snapshot_start_block = m_blockman.LookupBlockIndex(base_blockhash);

        const bool using_attested_au{attested_au.has_value()};
        if (using_attested_au) {
            if (attested_au->blockhash != base_blockhash) {
                return util::Error{Untranslated("Attested assumeutxo blockhash does not match snapshot metadata")};
            }
            if (attested_au->shielded_state_commitment.IsNull()) {
                return util::Error{Untranslated("Attested assumeutxo must include a shielded state commitment")};
            }
            if (!snapshot_start_block) {
                return util::Error{Untranslated(strprintf(
                    "The base block header (%s) must appear in the headers chain. Make sure all headers are syncing, and call loadtxoutsetattested again",
                    base_blockhash.ToString()))};
            }
            if (attested_au->height != snapshot_start_block->nHeight) {
                return util::Error{Untranslated("Attested assumeutxo height does not match the base block header")};
            }
            if (attested_au->m_chain_tx_count == 0 && snapshot_start_block->nHeight > 0) {
                return util::Error{Untranslated("Attested assumeutxo m_chain_tx_count must be non-zero above genesis")};
            }
            // Attested fast-forward is authenticated by the snapshot manifest,
            // not by claimed-heaviest m_best_header. A competing 1883xx
            // headers-only tree must not strand loadtxoutsetattested. Two
            // attested histories, or a competing suffix that already has
            // authenticated work, still refuse.
            if (!IsAttestedSnapshotHeaderCompatible(
                    m_best_header, snapshot_start_block)) {
                return util::Error{Untranslated(
                    "Attested snapshot base is incompatible with the current "
                    "best-header chain (competing header is attested or has "
                    "authenticated work past the fork)")};
            }
            if (IsOnParkedReorgBranch(snapshot_start_block)) {
                return util::Error{Untranslated(
                    "Attested snapshot base is on a parked reorg branch")};
            }
        } else {
            const bool known_assumeutxo_hash{GetParams().AssumeutxoForBlockhash(base_blockhash).has_value()};
            if (!known_assumeutxo_hash) {
                const bool allow_mockable_height_match{
                    GetParams().IsMockableChain() &&
                    snapshot_start_block &&
                    GetParams().AssumeutxoForHeight(snapshot_start_block->nHeight).has_value()};

                if (!allow_mockable_height_match) {
                    auto available_heights = GetParams().GetAvailableSnapshotHeights();
                    std::string heights_formatted = util::Join(available_heights, ", ", [&](const auto& i) { return util::ToString(i); });
                    return util::Error{Untranslated(strprintf("assumeutxo block hash in snapshot metadata not recognized (hash: %s). The following snapshot heights are available: %s",
                        base_blockhash.ToString(),
                        heights_formatted))};
                }

                LogPrintf("[snapshot] mockable chain accepting assumeutxo base hash %s at height %d via height-only match\n",
                    base_blockhash.ToString(),
                    snapshot_start_block->nHeight);
            }

            if (!snapshot_start_block) {
                return util::Error{Untranslated(strprintf("The base block header (%s) must appear in the headers chain. Make sure all headers are syncing, and call loadtxoutset again",
                              base_blockhash.ToString()))};
            }

            bool start_block_invalid = snapshot_start_block->nStatus & BLOCK_FAILED_MASK;
            if (start_block_invalid) {
                return util::Error{Untranslated(strprintf("The base block header (%s) is part of an invalid chain", base_blockhash.ToString()))};
            }

            // A compiled-in base hash pins the production header, ancestry, nBits,
            // and claimed chainwork. Compare it with authenticated work so a
            // divergent header-only MatMul suffix cannot deny snapshot activation.
            // Height-only matching remains confined to mockable test chains.
            if (!IsAssumeUtxoSnapshotHeaderCompatible(
                    m_best_header, snapshot_start_block,
                    known_assumeutxo_hash)) {
                return util::Error{Untranslated("A forked headers-chain with more work than the chain with the snapshot base block header exists. Please proceed to sync without AssumeUtxo.")};
            }
        }

        bool start_block_invalid = snapshot_start_block->nStatus & BLOCK_FAILED_MASK;
        if (start_block_invalid) {
            return util::Error{Untranslated(strprintf("The base block header (%s) is part of an invalid chain", base_blockhash.ToString()))};
        }

        auto mempool{m_active_chainstate->GetMempool()};
        if (mempool && mempool->size() > 0) {
            return util::Error{Untranslated("Can't activate a snapshot when mempool not empty")};
        }
    }

    int64_t current_coinsdb_cache_size{0};
    int64_t current_coinstip_cache_size{0};

    // Cache percentages to allocate to each chainstate.
    //
    // These particular percentages don't matter so much since they will only be
    // relevant during snapshot activation; caches are rebalanced at the conclusion of
    // this function. We want to give (essentially) all available cache capacity to the
    // snapshot to aid the bulk load later in this function.
    static constexpr double IBD_CACHE_PERC = 0.01;
    static constexpr double SNAPSHOT_CACHE_PERC = 0.99;

    {
        LOCK(::cs_main);
        // Resize the coins caches to ensure we're not exceeding memory limits.
        //
        // Allocate the majority of the cache to the incoming snapshot chainstate, since
        // (optimistically) getting to its tip will be the top priority. We'll need to call
        // `MaybeRebalanceCaches()` once we're done with this function to ensure
        // the right allocation (including the possibility that no snapshot was activated
        // and that we should restore the active chainstate caches to their original size).
        //
        current_coinsdb_cache_size = this->ActiveChainstate().m_coinsdb_cache_size_bytes;
        current_coinstip_cache_size = this->ActiveChainstate().m_coinstip_cache_size_bytes;

        // Temporarily resize the active coins cache to make room for the newly-created
        // snapshot chain.
        this->ActiveChainstate().ResizeCoinsCaches(
            static_cast<size_t>(current_coinstip_cache_size * IBD_CACHE_PERC),
            static_cast<size_t>(current_coinsdb_cache_size * IBD_CACHE_PERC));
    }

    auto snapshot_chainstate = WITH_LOCK(::cs_main,
        return std::make_unique<Chainstate>(
            /*mempool=*/nullptr, m_blockman, *this, base_blockhash));

    // LoadChainTip() may update CBlockIndex::nSequenceId, which is part of the
    // setBlockIndexCandidates comparator. Track whether activation has cleared
    // the shared candidate sets so a failed BTX shielded-state handoff can
    // restore the still-active IBD chainstate before returning.
    bool candidate_sets_cleared{false};

    {
        LOCK(::cs_main);
        snapshot_chainstate->InitCoinsDB(
            static_cast<size_t>(current_coinsdb_cache_size * SNAPSHOT_CACHE_PERC),
            in_memory, false, "chainstate");
        snapshot_chainstate->InitCoinsCache(
            static_cast<size_t>(current_coinstip_cache_size * SNAPSHOT_CACHE_PERC));
    }

    auto cleanup_bad_snapshot = [&](bilingual_str reason) EXCLUSIVE_LOCKS_REQUIRED(::cs_main) {
        // PopulateAndValidateSnapshot can return (in error) before the leveldb datadir
        // has been created, so only attempt removal if we got that far.
        const auto snapshot_datadir = node::FindSnapshotChainstateDir(m_options.datadir);
        if (snapshot_datadir) {
            // We have to destruct leveldb::DB in order to release the db lock, otherwise
            // DestroyDB() (in DeleteCoinsDBFromDisk()) will fail. See `leveldb::~DBImpl()`.
            // Destructing the chainstate (and so resetting the coinsviews object) does this.
            //
            // BTX may fail after the snapshot chainstate has already been moved into
            // m_snapshot_chainstate, for example while loading the shielded snapshot
            // appendix. In that case the managed snapshot chainstate owns the DB handle,
            // not the local temporary.
            if (m_snapshot_chainstate) {
                assert(m_active_chainstate != m_snapshot_chainstate.get());
                m_snapshot_chainstate.reset();
            } else {
                snapshot_chainstate.reset();
            }
            bool removed = DeleteCoinsDBFromDisk(*snapshot_datadir, /*is_snapshot=*/true);
            if (!removed) {
                GetNotifications().fatalError(strprintf(_("Failed to remove snapshot chainstate dir (%s). "
                    "Manually remove it before restarting.\n"), fs::PathToString(*snapshot_datadir)));
            }
        } else if (m_snapshot_chainstate) {
            // In-memory snapshots have no directory, but a delayed shielded
            // snapshot failure must still discard the managed chainstate.
            assert(m_active_chainstate != m_snapshot_chainstate.get());
            m_snapshot_chainstate.reset();
        } else {
            snapshot_chainstate.reset();
        }
        if (candidate_sets_cleared) {
            for (Chainstate* chainstate : GetAll()) {
                chainstate->PopulateBlockIndexCandidates();
            }
            candidate_sets_cleared = false;
        }
        m_attested_assumeutxo.reset();
        this->MaybeRebalanceCaches();
        return util::Error{std::move(reason)};
    };

    if (auto res{this->PopulateAndValidateSnapshot(
            *snapshot_chainstate, coins_file, metadata,
            attested_au ? &*attested_au : nullptr)}; !res) {
        LOCK(::cs_main);
        return cleanup_bad_snapshot(Untranslated(strprintf("Population failed: %s", util::ErrorString(res).original)));
    }

    if (metadata.HasShieldedSection()) {
        try {
            ShieldedSnapshotSectionHeader header;
            header.m_snapshot_version = metadata.Version();
            coins_file >> header;
            shielded_snapshot_section = std::move(header);
        } catch (const std::ios_base::failure& e) {
            LOCK(::cs_main);
            return cleanup_bad_snapshot(Untranslated(strprintf("BTX shielded snapshot section parse failed: %s", e.what())));
        }
    }

    LOCK(::cs_main);  // cs_main required for rest of snapshot activation.

    // Snapshot deserialization can be lengthy. Recheck under the final
    // activation lock so a competing branch that completed body validation
    // while the file was being loaded cannot race the initial check.
    // Attested loads use IsAttestedSnapshotHeaderCompatible (competing
    // unauthenticated most-work is not a veto).
    if (attested_au) {
        if (!IsAttestedSnapshotHeaderCompatible(
                m_best_header, snapshot_start_block)) {
            return cleanup_bad_snapshot(Untranslated(
                "Attested snapshot base is incompatible with the current "
                "best-header chain (competing header is attested or has "
                "authenticated work past the fork)"));
        }
        if (IsOnParkedReorgBranch(snapshot_start_block)) {
            return cleanup_bad_snapshot(Untranslated(
                "Attested snapshot base is on a parked reorg branch"));
        }
    } else if (!snapshot_base_outworks_authenticated_headers()) {
        return cleanup_bad_snapshot(Untranslated("A forked headers-chain with more work than the chain with the snapshot base block header exists. Please proceed to sync without AssumeUtxo."));
    }

    // Do a final check to ensure that the snapshot chainstate is actually a more
    // work chain than the active chainstate; a user could have loaded a snapshot
    // very late in the IBD process, and we wouldn't want to load a useless chainstate.
    if (!CBlockIndexWorkComparator()(ActiveTip(), snapshot_chainstate->m_chain.Tip())) {
        return cleanup_bad_snapshot(Untranslated("work does not exceed active chainstate"));
    }
    assert(!m_snapshot_chainstate);
    m_snapshot_chainstate.swap(snapshot_chainstate);
    if (attested_au) {
        m_attested_assumeutxo = *attested_au;
        LogPrintf("[snapshot] activated attested-fast-forward snapshot at height %d (%s)\n",
                  attested_au->height, base_blockhash.ToString());
    }

    // BTX cannot make the snapshot active until its shielded appendix and
    // consensus pin have been validated. Clear every chainstate's candidate
    // set before LoadChainTip() can update the shared block-index sequence ids,
    // and leave them empty until the active/background roles are final.
    for (Chainstate* chainstate : GetAll()) {
        chainstate->ClearBlockIndexCandidates();
    }
    candidate_sets_cleared = true;
    const bool chaintip_loaded = m_snapshot_chainstate->LoadChainTip();
    if (!chaintip_loaded) {
        m_attested_assumeutxo.reset();
        return cleanup_bad_snapshot(Untranslated(
            "could not load snapshot chain tip or normalize local park state"));
    }

    // DS-3 fix: the shielded snapshot section (pool balance + nullifier set + commitment tree) is
    // attacker-supplied and is otherwise never validated against consensus. Without this check a
    // malicious snapshot could set an arbitrary pool balance and OMIT spent nullifiers, enabling a
    // double-spend on the bootstrapped node. Reject loading unless the loaded shielded state hashes to
    // the consensus-pinned commitment for this snapshot's base height. (Null pin == not yet computed
    // == legacy behavior, so existing snapshots are unaffected until their pin is filled in.)
    // Attested fast-forward always supplies a pin via the signed manifest.
    if (shielded_snapshot_section) {
        const auto& shielded_au =
            attested_au ? std::optional<AssumeutxoData>{*attested_au}
                        : GetParams().AssumeutxoForHeight(snapshot_start_block->nHeight);
        const bool pinned = shielded_au && !shielded_au->shielded_state_commitment.IsNull();
        if (!pinned && !m_options.allow_unpinned_shielded_snapshot) {
            // DS-3 fail-closed: an unpinned shielded section is attacker-supplied and otherwise never
            // validated against consensus, so loading it can seed a double-spend (omitted nullifiers) or
            // a forged pool balance. Refuse unless the operator explicitly opts in to trusting it.
            m_attested_assumeutxo.reset();
            return cleanup_bad_snapshot(Untranslated("BTX shielded snapshot section has no consensus pin for this height; refusing to load (set -allowunpinnedshieldedsnapshot to override)"));
        } else if (!pinned) {
            LogPrintf("[snapshot] WARNING: loading an UNPINNED shielded snapshot section at height %d "
                      "(-allowunpinnedshieldedsnapshot); the shielded pool balance and nullifier set are "
                      "trusted from the snapshot and not validated against consensus\n",
                      snapshot_start_block->nHeight);
        }

        // The loader validates and durably publishes the accepted pin while
        // its backup rollback guard is still live. Thus a mismatch or pin
        // write failure restores the pre-snapshot shielded_state directories.
        std::function<bool(bool)> finish_shielded_snapshot;
        ShieldedSnapshotPinPolicy pin_policy{
            .required_pin = pinned
                ? std::optional<uint256>{shielded_au->shielded_state_commitment}
                : std::nullopt,
            .persist_accepted_pin = true,
            .deferred_completion = &finish_shielded_snapshot,
        };
        auto shielded_section_result{LoadShieldedSnapshotSection(
            coins_file, *shielded_snapshot_section, snapshot_start_block,
            std::move(pin_policy))};
        if (!shielded_section_result) {
            m_attested_assumeutxo.reset();
            return cleanup_bad_snapshot(Untranslated(strprintf(
                "could not load BTX shielded snapshot section: %s",
                util::ErrorString(shielded_section_result).original)));
        }

        // Keep rollback ownership live until the final discovery metadata has
        // committed below. A scope guard handles every intervening error path.
        auto rollback_shielded_snapshot = std::unique_ptr<void, std::function<void(void*)>>{
            reinterpret_cast<void*>(1),
            [&](void*) {
                if (finish_shielded_snapshot) {
                    (void)finish_shielded_snapshot(false);
                }
            }};

        if (!in_memory) {
            if (attested_manifest &&
                !node::WriteAttestedAssumeutxoData(*m_snapshot_chainstate,
                                                   *attested_manifest)) {
                m_attested_assumeutxo.reset();
                return cleanup_bad_snapshot(Untranslated(
                    "could not write attested assumeutxo data"));
            }
            if (!node::WriteSnapshotBaseBlockhash(*m_snapshot_chainstate)) {
                m_attested_assumeutxo.reset();
                return cleanup_bad_snapshot(Untranslated("could not write base blockhash"));
            }
        }
        if (!finish_shielded_snapshot || !finish_shielded_snapshot(true)) {
            m_attested_assumeutxo.reset();
            return cleanup_bad_snapshot(Untranslated(
                "could not commit BTX shielded snapshot state transaction"));
        }
        finish_shielded_snapshot = {};
        rollback_shielded_snapshot.reset();
    }

    // Commit restart discovery only after UTXO population, shielded-section
    // loading, and the complete shielded pin check have all succeeded. The
    // signed manifest sidecar is durable first; base_blockhash is the final
    // commit point. A crash at any earlier point leaves an undiscoverable
    // staging directory rather than a partially activated snapshot.
    if (!in_memory && !shielded_snapshot_section) {
        if (attested_manifest &&
            !node::WriteAttestedAssumeutxoData(*m_snapshot_chainstate,
                                               *attested_manifest)) {
            m_attested_assumeutxo.reset();
            return cleanup_bad_snapshot(Untranslated(
                "could not write attested assumeutxo data"));
        }
        if (!node::WriteSnapshotBaseBlockhash(*m_snapshot_chainstate)) {
            m_attested_assumeutxo.reset();
            return cleanup_bad_snapshot(Untranslated("could not write base blockhash"));
        }
    }

    // Transfer possession of the mempool to the snapshot chainstate.
    // Mempool is empty at this point because we're still in IBD.
    Assert(m_active_chainstate->m_mempool->size() == 0);
    Assert(!m_snapshot_chainstate->m_mempool);
    m_snapshot_chainstate->m_mempool = m_active_chainstate->m_mempool;
    m_active_chainstate->m_mempool = nullptr;
    m_active_chainstate = m_snapshot_chainstate.get();
    m_blockman.m_snapshot_height = this->GetSnapshotBaseHeight();

    // Candidate eligibility depends on whether a chainstate is active or
    // background, so rebuild both sets only after the delayed BTX role switch.
    for (Chainstate* chainstate : GetAll()) {
        chainstate->PopulateBlockIndexCandidates();
    }
    candidate_sets_cleared = false;

    // LoadBlockIndex / AddToBlockIndex may have left m_best_header on a
    // claimed-heaviest 1883xx flood. Configured nodes must chase the snapshot
    // tip-chain (and any already-known attested suffix) rather than that
    // flood, or IBD downloads the competing tree after a successful load.
    if (node::matmul_trusted::IsConfigured()) {
        RecalculateBestHeader();
    }

    LogPrintf("[snapshot] successfully activated snapshot %s\n", base_blockhash.ToString());
    LogPrintf("[snapshot] (%.2f MB)\n",
        m_snapshot_chainstate->CoinsTip().DynamicMemoryUsage() / (1000 * 1000));

    this->MaybeRebalanceCaches();
    return snapshot_start_block;
}

static void FlushSnapshotToDisk(CCoinsViewCache& coins_cache, bool snapshot_loaded)
{
    LOG_TIME_MILLIS_WITH_CATEGORY_MSG_ONCE(
        strprintf("%s (%.2f MB)",
                  snapshot_loaded ? "saving snapshot chainstate" : "flushing coins cache",
                  coins_cache.DynamicMemoryUsage() / (1000 * 1000)),
        BCLog::LogFlags::ALL);

    coins_cache.Flush();
}

struct StopHashingException : public std::exception
{
    const char* what() const noexcept override
    {
        return "ComputeUTXOStats interrupted.";
    }
};

static void SnapshotUTXOHashBreakpoint(const util::SignalInterrupt& interrupt)
{
    if (interrupt) throw StopHashingException();
}

util::Result<void> ChainstateManager::PopulateAndValidateSnapshot(
    Chainstate& snapshot_chainstate,
    AutoFile& coins_file,
    const SnapshotMetadata& metadata,
    const AssumeutxoData* attested_au)
{
    // It's okay to release cs_main before we're done using `coins_cache` because we know
    // that nothing else will be referencing the newly created snapshot_chainstate yet.
    CCoinsViewCache& coins_cache = *WITH_LOCK(::cs_main, return &snapshot_chainstate.CoinsTip());

    uint256 base_blockhash = metadata.m_base_blockhash;

    CBlockIndex* snapshot_start_block = WITH_LOCK(::cs_main, return m_blockman.LookupBlockIndex(base_blockhash));

    if (!snapshot_start_block) {
        // Needed for ComputeUTXOStats to determine the
        // height and to avoid a crash when base_blockhash.IsNull()
        return util::Error{Untranslated(strprintf("Did not find snapshot start blockheader %s",
                  base_blockhash.ToString()))};
    }

    int base_height = snapshot_start_block->nHeight;
    std::optional<AssumeutxoData> maybe_au_data;
    if (attested_au) {
        maybe_au_data = *attested_au;
    } else {
        maybe_au_data = GetParams().AssumeutxoForHeight(base_height);
    }

    if (!maybe_au_data) {
        return util::Error{Untranslated(strprintf("Assumeutxo height in snapshot metadata not recognized "
                  "(%d) - refusing to load snapshot", base_height))};
    }

    const AssumeutxoData& au_data = *maybe_au_data;

    // This work comparison is a duplicate check with the one performed later in
    // ActivateSnapshot(), but is done so that we avoid doing the long work of staging
    // a snapshot that isn't actually usable.
    if (WITH_LOCK(::cs_main, return !CBlockIndexWorkComparator()(ActiveTip(), snapshot_start_block))) {
        return util::Error{Untranslated("Work does not exceed active chainstate")};
    }

    const uint64_t coins_count = metadata.m_coins_count;
    uint64_t coins_left = metadata.m_coins_count;

    LogPrintf("[snapshot] loading %d coins from snapshot %s\n", coins_left, base_blockhash.ToString());
    int64_t coins_processed{0};

    while (coins_left > 0) {
        try {
            Txid txid;
            coins_file >> txid;
            size_t coins_per_txid{0};
            coins_per_txid = ReadCompactSize(coins_file);

            if (coins_per_txid > coins_left) {
                return util::Error{Untranslated("Mismatch in coins count in snapshot metadata and actual snapshot data")};
            }

            for (size_t i = 0; i < coins_per_txid; i++) {
                COutPoint outpoint;
                Coin coin;
                outpoint.n = static_cast<uint32_t>(ReadCompactSize(coins_file));
                outpoint.hash = txid;
                coins_file >> coin;
                if (coin.nHeight > base_height ||
                    outpoint.n >= std::numeric_limits<decltype(outpoint.n)>::max() // Avoid integer wrap-around in coinstats.cpp:ApplyHash
                ) {
                    return util::Error{Untranslated(strprintf("Bad snapshot data after deserializing %d coins",
                              coins_count - coins_left))};
                }
                if (!MoneyRange(coin.out.nValue)) {
                    return util::Error{Untranslated(strprintf("Bad snapshot data after deserializing %d coins - bad tx out value",
                              coins_count - coins_left))};
                }
                coins_cache.EmplaceCoinInternalDANGER(std::move(outpoint), std::move(coin));

                --coins_left;
                ++coins_processed;

                if (coins_processed % 1000000 == 0) {
                    LogPrintf("[snapshot] %d coins loaded (%.2f%%, %.2f MB)\n",
                        coins_processed,
                        static_cast<float>(coins_processed) * 100 / static_cast<float>(coins_count),
                        coins_cache.DynamicMemoryUsage() / (1000 * 1000));
                }

                // Batch write and flush (if we need to) every so often.
                //
                // If our average Coin size is roughly 41 bytes, checking every 120,000 coins
                // means <5MB of memory imprecision.
                if (coins_processed % 120000 == 0) {
                    if (m_interrupt) {
                        return util::Error{Untranslated("Aborting after an interrupt was requested")};
                    }

                    const auto snapshot_cache_state = WITH_LOCK(::cs_main,
                        return snapshot_chainstate.GetCoinsCacheSizeState());

                    if (snapshot_cache_state >= CoinsCacheSizeState::CRITICAL) {
                        // This is a hack - we don't know what the actual best block is, but that
                        // doesn't matter for the purposes of flushing the cache here. We'll set this
                        // to its correct value (`base_blockhash`) below after the coins are loaded.
                        coins_cache.SetBestBlock(GetRandHash());

                        // No need to acquire cs_main since this chainstate isn't being used yet.
                        FlushSnapshotToDisk(coins_cache, /*snapshot_loaded=*/false);
                    }
                }
            }
        } catch (const std::ios_base::failure&) {
            return util::Error{Untranslated(strprintf("Bad snapshot format or truncated snapshot after deserializing %d coins",
                      coins_processed))};
        }
    }

    // Important that we set this. This and the coins_cache accesses above are
    // sort of a layer violation, but either we reach into the innards of
    // CCoinsViewCache here or we have to invert some of the Chainstate to
    // embed them in a snapshot-activation-specific CCoinsViewCache bulk load
    // method.
    coins_cache.SetBestBlock(base_blockhash);

    const int64_t post_coins_position = coins_file.tell();
    bool out_of_coins{false};
    try {
        std::byte left_over_byte;
        coins_file >> left_over_byte;
    } catch (const std::ios_base::failure&) {
        // We expect an exception since we should be out of coins for legacy
        // snapshots, or positioned at the BTX shielded appendix for v3.
        out_of_coins = true;
    }
    if (metadata.HasShieldedSection()) {
        if (out_of_coins) {
            return util::Error{Untranslated("Missing BTX shielded snapshot appendix")};
        }
        coins_file.seek(post_coins_position, SEEK_SET);
    } else if (!out_of_coins) {
        return util::Error{Untranslated(strprintf("Bad snapshot - coins left over after deserializing %d coins",
            coins_count))};
    }

    LogPrintf("[snapshot] loaded %d (%.2f MB) coins from snapshot %s\n",
        coins_count,
        coins_cache.DynamicMemoryUsage() / (1000 * 1000),
        base_blockhash.ToString());

    // No need to acquire cs_main since this chainstate isn't being used yet.
    FlushSnapshotToDisk(coins_cache, /*snapshot_loaded=*/true);

    assert(coins_cache.GetBestBlock() == base_blockhash);

    // As above, okay to immediately release cs_main here since no other context knows
    // about the snapshot_chainstate.
    CCoinsViewDB* snapshot_coinsdb = WITH_LOCK(::cs_main, return &snapshot_chainstate.CoinsDB());

    std::optional<CCoinsStats> maybe_stats;

    try {
        maybe_stats = ComputeUTXOStats(
            CoinStatsHashType::HASH_SERIALIZED, snapshot_coinsdb, m_blockman, [&interrupt = m_interrupt] { SnapshotUTXOHashBreakpoint(interrupt); });
    } catch (StopHashingException const&) {
        return util::Error{Untranslated("Aborting after an interrupt was requested")};
    }
    if (!maybe_stats.has_value()) {
        return util::Error{Untranslated("Failed to generate coins stats")};
    }

    // Assert that the deserialized chainstate contents match the expected assumeutxo value.
    if (!GetParams().AssumeutxoHashMatches(au_data, maybe_stats->hashSerialized)) {
        return util::Error{Untranslated(strprintf("Bad snapshot content hash: expected %s, got %s",
            au_data.hash_serialized.ToString(), maybe_stats->hashSerialized.ToString()))};
    }

    snapshot_chainstate.m_chain.SetTip(*snapshot_start_block);

    // The remainder of this function requires modifying data protected by cs_main.
    LOCK(::cs_main);

    // Fake various pieces of CBlockIndex state:
    CBlockIndex* index = nullptr;

    // Don't make any modifications to the genesis block since it shouldn't be
    // necessary, and since the genesis block doesn't have normal flags like
    // BLOCK_VALID_SCRIPTS set.
    constexpr int AFTER_GENESIS_START{1};

    for (int i = AFTER_GENESIS_START; i <= snapshot_chainstate.m_chain.Height(); ++i) {
        index = snapshot_chainstate.m_chain[i];

        // Fake BLOCK_OPT_WITNESS so that Chainstate::NeedsRedownload()
        // won't ask for -reindex on startup.
        if (DeploymentActiveAt(*index, *this, Consensus::DEPLOYMENT_SEGWIT)) {
            index->nStatus |= BLOCK_OPT_WITNESS;
        }

        m_blockman.m_dirty_blockindex.insert(index);
        // Changes to the block index will be flushed to disk after this call
        // returns in `ActivateSnapshot()`, when `MaybeRebalanceCaches()` is
        // called, since we've added a snapshot chainstate and therefore will
        // have to downsize the IBD chainstate, which will result in a call to
        // `FlushStateToDisk(ALWAYS)`.
    }

    assert(index);
    assert(index == snapshot_start_block);
    index->m_chain_tx_count = au_data.m_chain_tx_count;

    LogPrintf("[snapshot] validated snapshot (%.2f MB)\n",
        coins_cache.DynamicMemoryUsage() / (1000 * 1000));
    return {};
}

// Currently, this function holds cs_main for its duration, which could be for
// multiple minutes due to the ComputeUTXOStats call. This hold is necessary
// because we need to avoid advancing the background validation chainstate
// farther than the snapshot base block - and this function is also invoked
// from within ConnectTip, i.e. from within ActivateBestChain, so cs_main is
// held anyway.
//
// Eventually (NOTE), we could somehow separate this function's runtime from
// maintenance of the active chain, but that will either require
//
//  (i) setting `m_disabled` immediately and ensuring all chainstate accesses go
//      through IsUsable() checks, or
//
//  (ii) giving each chainstate its own lock instead of using cs_main for everything.
SnapshotCompletionResult ChainstateManager::MaybeCompleteSnapshotValidation()
{
    AssertLockHeld(cs_main);
    if (m_ibd_chainstate.get() == &this->ActiveChainstate() ||
            !this->IsUsable(m_snapshot_chainstate.get()) ||
            !this->IsUsable(m_ibd_chainstate.get()) ||
            !m_ibd_chainstate->m_chain.Tip()) {
       // Nothing to do - this function only applies to the background
       // validation chainstate.
       return SnapshotCompletionResult::SKIPPED;
    }
    const int snapshot_tip_height = this->ActiveHeight();
    const int snapshot_base_height = *Assert(this->GetSnapshotBaseHeight());
    const CBlockIndex& index_new = *Assert(m_ibd_chainstate->m_chain.Tip());

    if (index_new.nHeight < snapshot_base_height) {
        // Background IBD not complete yet.
        return SnapshotCompletionResult::SKIPPED;
    }

    assert(SnapshotBlockhash());
    uint256 snapshot_blockhash = *Assert(SnapshotBlockhash());

    auto handle_invalid_snapshot = [&]() EXCLUSIVE_LOCKS_REQUIRED(::cs_main) {
        bilingual_str user_error = strprintf(_(
            "%s failed to validate the -assumeutxo snapshot state. "
            "This indicates a hardware problem, or a bug in the software, or a "
            "bad software modification that allowed an invalid snapshot to be "
            "loaded. As a result of this, the node will shut down and stop using any "
            "state that was built on the snapshot, resetting the chain height "
            "from %d to %d. On the next "
            "restart, the node will resume syncing from %d "
            "without using any snapshot data. "
            "Please report this incident to %s, including how you obtained the snapshot. "
            "The invalid snapshot chainstate will be left on disk in case it is "
            "helpful in diagnosing the issue that caused this error."),
            CLIENT_NAME, snapshot_tip_height, snapshot_base_height, snapshot_base_height, CLIENT_BUGREPORT
        );

        LogError("[snapshot] !!! %s\n", user_error.original);
        LogError("[snapshot] deleting snapshot, reverting to validated chain, and stopping node\n");

        m_active_chainstate = m_ibd_chainstate.get();
        m_snapshot_chainstate->m_disabled = true;
        assert(!this->IsUsable(m_snapshot_chainstate.get()));
        assert(this->IsUsable(m_ibd_chainstate.get()));

        auto rename_result = m_snapshot_chainstate->InvalidateCoinsDBOnDisk();
        if (!rename_result) {
            user_error += Untranslated("\n") + util::ErrorString(rename_result);
        }

        GetNotifications().fatalError(user_error);
    };

    if (index_new.GetBlockHash() != snapshot_blockhash) {
        LogPrintf("[snapshot] supposed base block %s does not match the "
          "snapshot base block %s (height %d). Snapshot is not valid.\n",
          index_new.ToString(), snapshot_blockhash.ToString(), snapshot_base_height);
        handle_invalid_snapshot();
        return SnapshotCompletionResult::BASE_BLOCKHASH_MISMATCH;
    }

    assert(index_new.nHeight == snapshot_base_height);

    int curr_height = m_ibd_chainstate->m_chain.Height();

    assert(snapshot_base_height == curr_height);
    assert(snapshot_base_height == index_new.nHeight);
    assert(this->IsUsable(m_snapshot_chainstate.get()));
    assert(this->GetAll().size() == 2);

    CCoinsViewDB& ibd_coins_db = m_ibd_chainstate->CoinsDB();
    m_ibd_chainstate->ForceFlushStateToDisk();

    const auto& maybe_au_data =
        m_attested_assumeutxo ? m_attested_assumeutxo
                              : m_options.chainparams.AssumeutxoForHeight(curr_height);
    if (!maybe_au_data) {
        LogPrintf("[snapshot] assumeutxo data not found for height "
            "(%d) - refusing to validate snapshot\n", curr_height);
        handle_invalid_snapshot();
        return SnapshotCompletionResult::MISSING_CHAINPARAMS;
    }

    const AssumeutxoData& au_data = *maybe_au_data;
    std::optional<CCoinsStats> maybe_ibd_stats;
    LogPrintf("[snapshot] computing UTXO stats for background chainstate to validate "
        "snapshot - this could take a few minutes\n");
    try {
        maybe_ibd_stats = ComputeUTXOStats(
            CoinStatsHashType::HASH_SERIALIZED,
            &ibd_coins_db,
            m_blockman,
            [&interrupt = m_interrupt] { SnapshotUTXOHashBreakpoint(interrupt); });
    } catch (StopHashingException const&) {
        return SnapshotCompletionResult::STATS_FAILED;
    }

    // NOTE note that this function is slow and will hold cs_main for potentially minutes.
    if (!maybe_ibd_stats) {
        LogPrintf("[snapshot] failed to generate stats for validation coins db\n");
        // While this isn't a problem with the snapshot per se, this condition
        // prevents us from validating the snapshot, so we should shut down and let the
        // user handle the issue manually.
        handle_invalid_snapshot();
        return SnapshotCompletionResult::STATS_FAILED;
    }
    const auto& ibd_stats = *maybe_ibd_stats;

    // Compare the background validation chainstate's UTXO set hash against the hard-coded
    // assumeutxo hash we expect.
    //
    // NOTE: For belt-and-suspenders, we could cache the UTXO set
    // hash for the snapshot when it's loaded in its chainstate's leveldb. We could then
    // reference that here for an additional check.
    if (!GetParams().AssumeutxoHashMatches(au_data, ibd_stats.hashSerialized)) {
        LogPrintf("[snapshot] hash mismatch: actual=%s, expected=%s\n",
            ibd_stats.hashSerialized.ToString(),
            au_data.hash_serialized.ToString());
        handle_invalid_snapshot();
        return SnapshotCompletionResult::HASH_MISMATCH;
    }

    LogPrintf("[snapshot] snapshot beginning at %s has been fully validated\n",
        snapshot_blockhash.ToString());

    m_ibd_chainstate->m_disabled = true;
    this->MaybeRebalanceCaches();

    return SnapshotCompletionResult::SUCCESS;
}

Chainstate& ChainstateManager::ActiveChainstate() const
{
    LOCK(::cs_main);
    assert(m_active_chainstate);
    return *m_active_chainstate;
}

bool ChainstateManager::IsSnapshotActive() const
{
    LOCK(::cs_main);
    return m_snapshot_chainstate && m_active_chainstate == m_snapshot_chainstate.get();
}

void ChainstateManager::MaybeRebalanceCaches()
{
    AssertLockHeld(::cs_main);
    bool ibd_usable = this->IsUsable(m_ibd_chainstate.get());
    bool snapshot_usable = this->IsUsable(m_snapshot_chainstate.get());
    assert(ibd_usable || snapshot_usable);

    if (ibd_usable && !snapshot_usable) {
        // Allocate everything to the IBD chainstate. This will always happen
        // when we are not using a snapshot.
        m_ibd_chainstate->ResizeCoinsCaches(m_total_coinstip_cache, m_total_coinsdb_cache);
    }
    else if (snapshot_usable && !ibd_usable) {
        // If background validation has completed and snapshot is our active chain...
        LogPrintf("[snapshot] allocating all cache to the snapshot chainstate\n");
        // Allocate everything to the snapshot chainstate.
        m_snapshot_chainstate->ResizeCoinsCaches(m_total_coinstip_cache, m_total_coinsdb_cache);
    }
    else if (ibd_usable && snapshot_usable) {
        // If both chainstates exist, determine who needs more cache based on IBD status.
        //
        // Note: shrink caches first so that we don't inadvertently overwhelm available memory.
        if (IsInitialBlockDownload()) {
            m_ibd_chainstate->ResizeCoinsCaches(
                m_total_coinstip_cache * 0.05, m_total_coinsdb_cache * 0.05);
            m_snapshot_chainstate->ResizeCoinsCaches(
                m_total_coinstip_cache * 0.95, m_total_coinsdb_cache * 0.95);
        } else {
            m_snapshot_chainstate->ResizeCoinsCaches(
                m_total_coinstip_cache * 0.05, m_total_coinsdb_cache * 0.05);
            m_ibd_chainstate->ResizeCoinsCaches(
                m_total_coinstip_cache * 0.95, m_total_coinsdb_cache * 0.95);
        }
    }
}

void ChainstateManager::ResetChainstates()
{
    m_ibd_chainstate.reset();
    m_snapshot_chainstate.reset();
    m_active_chainstate = nullptr;
    m_shielded_nullifiers.reset();
    shielded::ShieldedMerkleTree::ResetCommitmentIndexStore();
    shielded::registry::ShieldedAccountRegistryState::ResetPayloadStore();
    m_shielded_merkle_tree = shielded::ShieldedMerkleTree{};
    m_shielded_account_registry = shielded::registry::ShieldedAccountRegistryState::WithConfiguredPayloadStore();
    m_shielded_smile_public_accounts.clear();
    m_shielded_account_leaf_commitments.clear();
    InvalidateShieldedAccountStateSnapshotCaches();
    m_shielded_anchor_roots.clear();
    m_shielded_account_registry_roots.clear();
    m_shielded_pool_balance = ShieldedPoolBalance{};
    m_shielded_unshield_velocity.Clear();
    m_shielded_state_initialized = false;
}

/**
 * Apply default chain params to nullopt members.
 * This helps to avoid coding errors around the accidental use of the compare
 * operators that accept nullopt, thus ignoring the intended default value.
 */
static ChainstateManager::Options&& Flatten(ChainstateManager::Options&& opts)
{
    if (!opts.check_block_index.has_value()) opts.check_block_index = opts.chainparams.DefaultConsistencyChecks();
    if (!opts.minimum_chain_work.has_value()) opts.minimum_chain_work = UintToArith256(opts.chainparams.GetConsensus().nMinimumChainWork);
    if (!opts.assumed_valid_block.has_value()) opts.assumed_valid_block = opts.chainparams.GetConsensus().defaultAssumeValid;
    return std::move(opts);
}

ChainstateManager::ChainstateManager(const util::SignalInterrupt& interrupt, Options options, node::BlockManager::Options blockman_options)
    : m_script_check_queue{/*batch_size=*/128, std::clamp(options.worker_threads_num, 0, MAX_SCRIPTCHECK_THREADS)},
      m_shielded_proof_check_queue{/*batch_size=*/128, std::clamp(options.worker_threads_num, 0, MAX_SHIELDEDCHECK_THREADS)},
      m_shielded_spendauth_check_queue{/*batch_size=*/128, std::clamp(options.worker_threads_num, 0, MAX_SHIELDEDCHECK_THREADS)},
      m_interrupt{interrupt},
      m_options{Flatten(std::move(options))},
      m_blockman{interrupt, std::move(blockman_options)},
      m_validation_cache{m_options.script_execution_cache_bytes, m_options.signature_cache_bytes}
{
}

ChainstateManager::~ChainstateManager()
{
    LOCK(::cs_main);

    m_versionbitscache.Clear();
}

void ChainstateManager::RecordShieldedAnchorRoot(const uint256& root)
{
    AssertLockHeld(::cs_main);
    if (root.IsNull()) return;
    m_shielded_anchor_roots.push_front(root);
    const size_t max_roots = static_cast<size_t>(SHIELDED_ANCHOR_DEPTH) + 1;
    while (m_shielded_anchor_roots.size() > max_roots) {
        m_shielded_anchor_roots.pop_back();
    }
}

void ChainstateManager::RecordShieldedAccountRegistryRoot(const uint256& root)
{
    AssertLockHeld(::cs_main);
    if (root.IsNull()) return;
    m_shielded_account_registry_roots.push_front(root);
    const size_t max_roots = static_cast<size_t>(SHIELDED_ANCHOR_DEPTH) + 1;
    while (m_shielded_account_registry_roots.size() > max_roots) {
        m_shielded_account_registry_roots.pop_back();
    }
}

bool ChainstateManager::RebuildShieldedAnchorHistory(const Chainstate& chainstate, const CBlockIndex* tip)
{
    AssertLockHeld(::cs_main);
    std::deque<uint256> rebuilt_roots;
    if (!CollectShieldedAnchorHistoryFromTree(chainstate,
                                              tip,
                                              m_shielded_merkle_tree,
                                              rebuilt_roots)) {
        return false;
    }
    m_shielded_anchor_roots = std::move(rebuilt_roots);
    return true;
}

bool ChainstateManager::RepairShieldedAnchorHistoryFromActiveChain()
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_state_initialized || m_active_chainstate == nullptr) {
        return false;
    }

    const CBlockIndex* const tip = m_active_chainstate->m_chain.Tip();
    const auto previous_roots = m_shielded_anchor_roots;
    if (!RebuildShieldedAnchorHistory(*m_active_chainstate, tip)) {
        return false;
    }
    if (previous_roots != m_shielded_anchor_roots && !PersistShieldedState(tip)) {
        LogPrintf("RepairShieldedAnchorHistoryFromActiveChain: repaired in-memory anchor history but failed to persist tip=%s height=%d\n",
                  tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString(),
                  tip != nullptr ? tip->nHeight : -1);
    }
    return true;
}

void ChainstateManager::AutoReconsiderShieldedInvalidBlocks(
    const std::function<bool(const CBlockIndex&)>& include_candidate,
    const char* log_context,
    const char* log_reason)
{
    AssertLockHeld(::cs_main);
    if (m_active_chainstate == nullptr) {
        return;
    }

    std::vector<CBlockIndex*> candidates;
    for (auto& [_, block_index] : m_blockman.m_block_index) {
        if ((block_index.nStatus & BLOCK_FAILED_VALID) == 0) continue;
        if ((block_index.nStatus & BLOCK_HAVE_DATA) == 0) continue;
        if (!include_candidate(block_index)) continue;
        if (block_index.pprev == nullptr || !m_active_chainstate->m_chain.Contains(block_index.pprev)) {
            continue;
        }

        CBlock block;
        if (!ReadShieldedRebuildBlock(*m_active_chainstate, block_index, block)) {
            LogPrintf("%s: failed to read candidate block %s at height=%d\n",
                      log_context,
                      block_index.GetBlockHash().ToString(),
                      block_index.nHeight);
            continue;
        }
        if (!BlockHasShieldedBundle(block)) continue;
        candidates.push_back(&block_index);
    }

    if (candidates.empty()) {
        return;
    }

    std::sort(candidates.begin(), candidates.end(), CBlockIndexHeightOnlyComparator{});

    size_t reconsidered{0};
    for (CBlockIndex* candidate : candidates) {
        if ((candidate->nStatus & BLOCK_FAILED_VALID) == 0) continue;
        LogPrintf("%s: clearing failure flags for block %s at height=%d %s\n",
                  log_context,
                  candidate->GetBlockHash().ToString(),
                  candidate->nHeight,
                  log_reason);
        m_active_chainstate->ResetBlockFailureFlags(candidate);
        ++reconsidered;
    }

    if (reconsidered > 0) {
        RecalculateBestHeader();
        LogPrintf("%s: scheduled %u shielded block(s) for reconsideration\n",
                  log_context,
                  static_cast<unsigned int>(reconsidered));
    }
}

void ChainstateManager::AutoReconsiderShieldedInvalidBlocksAfterStartupRepair()
{
    AutoReconsiderShieldedInvalidBlocks(
        [](const CBlockIndex&) { return true; },
        "AutoReconsiderShieldedInvalidBlocksAfterStartupRepair",
        "after startup shielded repair");
}

void ChainstateManager::AutoReconsiderShieldedInvalidBlocksAfterConsensusRetune()
{
    AssertLockHeld(::cs_main);
    const int32_t pool_credit_disable_height{GetConsensus().nShieldedPoolCreditDisableHeight};
    if (pool_credit_disable_height == std::numeric_limits<int32_t>::max()) {
        return;
    }

    AutoReconsiderShieldedInvalidBlocks(
        [pool_credit_disable_height](const CBlockIndex& block_index) {
            return block_index.nHeight < pool_credit_disable_height;
        },
        "AutoReconsiderShieldedInvalidBlocksAfterConsensusRetune",
        "after shielded pool-credit disable height retune");
}

bool ChainstateManager::MarkShieldedAutoRepairAttempt(ShieldedAutoRepairKind kind)
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_state_initialized || m_active_chainstate == nullptr) {
        return false;
    }

    const ShieldedAutoRepairGeneration current_generation{
        m_shielded_merkle_tree.Root(),
        static_cast<uint64_t>(m_shielded_merkle_tree.Size()),
        m_shielded_account_registry.Root(),
    };
    auto& last_generation = kind == ShieldedAutoRepairKind::ANCHOR_HISTORY
        ? m_last_shielded_anchor_auto_repair_generation
        : m_last_shielded_state_rebuild_generation;
    if (last_generation.has_value() && *last_generation == current_generation) {
        return false;
    }

    last_generation = current_generation;
    if (kind == ShieldedAutoRepairKind::ANCHOR_HISTORY) {
        ++m_shielded_anchor_auto_repair_attempts;
    } else {
        ++m_shielded_state_rebuild_attempts;
    }
    return true;
}

bool ChainstateManager::RebuildShieldedAccountRegistryHistory(const Chainstate& chainstate, const CBlockIndex* tip)
{
    AssertLockHeld(::cs_main);
    std::deque<uint256> rebuilt_roots;
    if (!CollectShieldedAccountRegistryHistoryFromState(chainstate,
                                                        tip,
                                                        m_shielded_account_registry,
                                                        rebuilt_roots)) {
        return false;
    }
    m_shielded_account_registry_roots = std::move(rebuilt_roots);
    return true;
}

bool ChainstateManager::RebuildShieldedStateFromActiveChain()
{
    AssertLockHeld(::cs_main);
    if (m_active_chainstate == nullptr) {
        return false;
    }

    const fs::path shielded_db_path = m_options.datadir / "shielded_state" / "nullifiers";
    const CBlockIndex* const tip = m_active_chainstate->m_chain.Tip();

    // One pass feeds every chain-derived consumer. build_nullifier_state below must NOT call
    // SyncShielded* (each Sync is another full-history read); load the already-rebuilt bridge
    // metadata into both the memory-only and persistent nullifier stores.
    ShieldedChainDerivedRebuildResult rebuilt;
    if (!RebuildShieldedChainDerivedState(
            *m_active_chainstate,
            tip,
            rebuilt,
            ShieldedChainDerivedRebuildParts::ALL,
            shielded::ShieldedMerkleTree::IndexStorageMode::MEMORY_ONLY,
            /*account_registry_use_payload_store=*/false,
            &GetNotifications())) {
        return false;
    }
    if (tip != nullptr &&
        UseAccountRegistryEntryCountLimit(GetConsensus(), tip->nHeight) &&
        rebuilt.account_registry.Size() > GetConsensus().nMaxShieldedAccountRegistryEntries) {
        return false;
    }

    std::deque<uint256> rebuilt_anchor_roots;
    if (!CollectShieldedAnchorHistoryFromTree(*m_active_chainstate,
                                              tip,
                                              rebuilt.tree,
                                              rebuilt_anchor_roots)) {
        return false;
    }
    std::deque<uint256> rebuilt_account_registry_roots;
    if (!CollectShieldedAccountRegistryHistoryFromState(*m_active_chainstate,
                                                        tip,
                                                        rebuilt.account_registry,
                                                        rebuilt_account_registry_roots)) {
        return false;
    }

    const auto build_nullifier_state =
        [&](bool memory_only) EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
            -> std::unique_ptr<NullifierSet> {
        AssertLockHeld(::cs_main);
        std::unique_ptr<NullifierSet> rebuilt_state;
        try {
            rebuilt_state = std::make_unique<NullifierSet>(shielded_db_path,
                                                           8 << 20,
                                                           memory_only,
                                                           /*wipe_data=*/!memory_only);
        } catch (const std::exception& e) {
            LogPrintf("RebuildShieldedStateFromActiveChain: failed to open %s nullifier state store at tip=%s height=%d: %s\n",
                      memory_only ? "memory-only" : "persistent",
                      tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString(),
                      tip != nullptr ? tip->nHeight : -1,
                      e.what());
            return nullptr;
        }
        if (!rebuilt.nullifiers.empty() && !rebuilt_state->Insert(rebuilt.nullifiers)) {
            LogPrintf("RebuildShieldedStateFromActiveChain: failed to load nullifiers into %s rebuild state at tip=%s height=%d\n",
                      memory_only ? "memory-only" : "persistent",
                      tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString(),
                      tip != nullptr ? tip->nHeight : -1);
            return nullptr;
        }
        if (!rebuilt.recovery_exit_commitments.empty() &&
            !rebuilt_state->InsertRecoveryExitCommitments(rebuilt.recovery_exit_commitments)) {
            LogPrintf("RebuildShieldedStateFromActiveChain: failed to load recovery-exit commitments into %s rebuild state at tip=%s height=%d\n",
                      memory_only ? "memory-only" : "persistent",
                      tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString(),
                      tip != nullptr ? tip->nHeight : -1);
            return nullptr;
        }
        if (!LoadRebuiltShieldedBridgeMetadata(*rebuilt_state,
                                               rebuilt.settlement_anchors,
                                               rebuilt.netting_manifests)) {
            LogPrintf("RebuildShieldedStateFromActiveChain: failed to load bridge metadata into %s rebuild state at tip=%s height=%d\n",
                      memory_only ? "memory-only" : "persistent",
                      tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString(),
                      tip != nullptr ? tip->nHeight : -1);
            return nullptr;
        }
        if (!rebuilt_state->WritePoolBalance(rebuilt.pool.GetBalance())) {
            LogPrintf("RebuildShieldedStateFromActiveChain: failed to rebuild pool balance in %s state at tip=%s height=%d\n",
                      memory_only ? "memory-only" : "persistent",
                      tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString(),
                      tip != nullptr ? tip->nHeight : -1);
            return nullptr;
        }
        if (!rebuilt_state->WriteUnshieldVelocity(rebuilt.unshield_velocity)) {
            LogPrintf("RebuildShieldedStateFromActiveChain: failed to rebuild unshield-velocity log in %s state at tip=%s height=%d\n",
                      memory_only ? "memory-only" : "persistent",
                      tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString(),
                      tip != nullptr ? tip->nHeight : -1);
            return nullptr;
        }
        if (!rebuilt_state->WriteSnapshotBridgeMetadataHint(false)) {
            LogPrintf("RebuildShieldedStateFromActiveChain: failed to reset snapshot metadata hint in %s state at tip=%s height=%d\n",
                      memory_only ? "memory-only" : "persistent",
                      tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString(),
                      tip != nullptr ? tip->nHeight : -1);
            return nullptr;
        }
        if (!memory_only &&
            !rebuilt_state->WriteMutationMarker(MakeShieldedMutationMarker(tip))) {
            LogPrintf("RebuildShieldedStateFromActiveChain: failed to stage persistent mutation marker at tip=%s height=%d\n",
                      tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString(),
                      tip != nullptr ? tip->nHeight : -1);
            return nullptr;
        }
        return rebuilt_state;
    };

    auto rebuilt_live_nullifiers = build_nullifier_state(/*memory_only=*/true);
    if (!rebuilt_live_nullifiers) {
        return false;
    }

    m_shielded_merkle_tree = std::move(rebuilt.tree);
    m_shielded_account_registry = std::move(rebuilt.account_registry);
    m_shielded_smile_public_accounts = std::move(rebuilt.public_accounts);
    m_shielded_account_leaf_commitments = std::move(rebuilt.account_leaf_commitments);
    InvalidateShieldedAccountStateSnapshotCaches();
    m_shielded_nullifiers = std::move(rebuilt_live_nullifiers);
    m_shielded_anchor_roots = std::move(rebuilt_anchor_roots);
    m_shielded_account_registry_roots = std::move(rebuilt_account_registry_roots);
    m_shielded_pool_balance = rebuilt.pool;
    m_shielded_unshield_velocity = rebuilt.unshield_velocity;
    m_shielded_state_initialized = true;

    if (auto rebuilt_persistent_nullifiers = build_nullifier_state(/*memory_only=*/false)) {
        m_shielded_nullifiers = std::move(rebuilt_persistent_nullifiers);
        if (!PersistShieldedState(tip)) {
            LogPrintf("RebuildShieldedStateFromActiveChain: rebuilt live shielded state but failed to persist tip=%s height=%d\n",
                      tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString(),
                      tip != nullptr ? tip->nHeight : -1);
        }
    } else {
        LogPrintf("RebuildShieldedStateFromActiveChain: rebuilt live shielded state at tip=%s height=%d but kept nullifier persistence in memory-only fallback; durable rebuild will be retried on restart if needed\n",
                  tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString(),
                  tip != nullptr ? tip->nHeight : -1);
    }
    return true;
}

uint64_t ChainstateManager::GetShieldedAutoRepairAttemptCountForTest(ShieldedAutoRepairKind kind) const
{
    AssertLockHeld(::cs_main);
    return kind == ShieldedAutoRepairKind::ANCHOR_HISTORY
        ? m_shielded_anchor_auto_repair_attempts
        : m_shielded_state_rebuild_attempts;
}

void ChainstateManager::SetShieldedAutoRepairHookForTest(
    std::function<void(ShieldedAutoRepairKind)> hook)
{
    AssertLockHeld(::cs_main);
    m_shielded_auto_repair_hook_for_test = std::move(hook);
}

void ChainstateManager::RunShieldedAutoRepairHookForTest(ShieldedAutoRepairKind kind)
{
    AssertLockHeld(::cs_main);
    if (m_shielded_auto_repair_hook_for_test) {
        m_shielded_auto_repair_hook_for_test(kind);
    }
}

bool ChainstateManager::InjectShieldedTransitionWriteFailureForTest(
    ShieldedTransitionWriteSeam seam)
{
    AssertLockHeld(::cs_main);
    return m_shielded_transition_write_fault_hook_for_test &&
           m_shielded_transition_write_fault_hook_for_test(seam);
}

ShieldedSnapshotSectionHeader ChainstateManager::GetShieldedSnapshotSectionHeader(
    const Chainstate& chainstate,
    const CBlockIndex* tip) const
{
    AssertLockHeld(::cs_main);
    ShieldedSnapshotSectionHeader header;
    header.m_commitment_count = m_shielded_merkle_tree.Size();
    header.m_nullifier_count = m_shielded_nullifiers ? m_shielded_nullifiers->CountNullifiers() : 0;
    if (!ForEachShieldedRecoveryExitCommitment([&](const uint256&) {
            if (header.m_recovery_exit_commitment_count == std::numeric_limits<uint64_t>::max()) return false;
            ++header.m_recovery_exit_commitment_count;
            return true;
        })) {
        throw std::runtime_error("Failed to count BTX recovery-exit commitments for snapshot");
    }
    if (!ForEachShieldedSettlementAnchor([&](const uint256&) {
            if (header.m_settlement_anchor_count == std::numeric_limits<uint64_t>::max()) return false;
            ++header.m_settlement_anchor_count;
            return true;
        })) {
        throw std::runtime_error("Failed to count BTX settlement anchors for snapshot");
    }
    if (!ForEachShieldedNettingManifest([&](const uint256&) {
            if (header.m_netting_manifest_count == std::numeric_limits<uint64_t>::max()) return false;
            ++header.m_netting_manifest_count;
            return true;
        })) {
        throw std::runtime_error("Failed to count BTX netting manifests for snapshot");
    }
    header.m_account_registry_entry_count = m_shielded_account_registry.Size();
    header.m_pool_balance = m_shielded_pool_balance.GetBalance();
    header.m_unshield_velocity = m_shielded_unshield_velocity;
    if (!CollectRecentShieldedOutputCounts(chainstate, tip, header.m_recent_output_counts)) {
        throw std::runtime_error("Failed to collect BTX shielded snapshot output counts");
    }
    std::deque<uint256> account_registry_roots;
    if (!CollectShieldedAccountRegistryHistoryFromState(chainstate,
                                                        tip,
                                                        m_shielded_account_registry,
                                                        account_registry_roots)) {
        throw std::runtime_error("Failed to collect BTX shielded account-registry snapshot roots");
    }
    if (account_registry_roots.size() != header.m_recent_output_counts.size() + 1U) {
        throw std::runtime_error("BTX shielded account-registry snapshot root/count mismatch");
    }
    header.m_account_registry_roots.assign(account_registry_roots.begin(),
                                           account_registry_roots.end());
    return header;
}

std::optional<shielded::registry::ShieldedStateCommitment> ChainstateManager::GetShieldedStateCommitment() const
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return std::nullopt;

    std::vector<uint256> nullifiers;
    nullifiers.reserve(m_shielded_nullifiers->CountNullifiers());
    if (!ForEachShieldedNullifier([&](const Nullifier& nullifier) {
            nullifiers.push_back(nullifier);
            return true;
        })) {
        return std::nullopt;
    }
    std::vector<uint256> recovery_exit_commitments;
    if (!ForEachShieldedRecoveryExitCommitment([&](const uint256& commitment) {
            recovery_exit_commitments.push_back(commitment);
            return true;
        })) {
        return std::nullopt;
    }

    std::vector<uint256> settlement_anchors;
    if (!ForEachShieldedSettlementAnchor([&](const uint256& anchor) {
            settlement_anchors.push_back(anchor);
            return true;
        })) {
        return std::nullopt;
    }

    std::vector<ConfirmedNettingManifestState> netting_manifests;
    if (!ForEachShieldedNettingManifestState([&](const ConfirmedNettingManifestState& manifest_state) {
            netting_manifests.push_back(manifest_state);
            return true;
        })) {
        return std::nullopt;
    }

    shielded::registry::ShieldedStateCommitment commitment;
    commitment.note_commitment_root = m_shielded_merkle_tree.Root();
    commitment.account_registry_root = m_shielded_account_registry.Root();
    commitment.nullifier_root = shielded::registry::ComputeNullifierSetCommitment(
        Span<const uint256>{nullifiers.data(), nullifiers.size()});
    HashWriter bridge_commitment;
    bridge_commitment << std::string{"BTX_SHIELDED_BRIDGE_STATE_COMMITMENT_V2"}
                      << recovery_exit_commitments
                      << settlement_anchors
                      << netting_manifests;
    commitment.bridge_settlement_root = bridge_commitment.GetSHA256();
    if (!commitment.IsValid()) return std::nullopt;
    return commitment;
}

std::optional<uint256> ChainstateManager::ComputeShieldedSnapshotStatePin() const
{
    AssertLockHeld(::cs_main);
    const auto commitment = GetShieldedStateCommitment();
    if (!commitment.has_value()) return std::nullopt;
    HashWriter pin;
    pin << std::string{"BTX_ShieldedSnapshotStatePin_V3"}
        << commitment->note_commitment_root
        << commitment->account_registry_root
        << commitment->nullifier_root
        << commitment->bridge_settlement_root
        << GetShieldedPoolBalance()
        << m_shielded_unshield_velocity;
    return pin.GetSHA256();
}

std::optional<uint256> ChainstateManager::ComputeShieldedSnapshotStatePinV2ForMigration() const
{
    AssertLockHeld(::cs_main);
    const auto commitment = GetShieldedStateCommitment();
    if (!commitment.has_value()) return std::nullopt;
    HashWriter pin;
    pin << std::string{"BTX_ShieldedSnapshotStatePin_V2"}
        << commitment->note_commitment_root
        << commitment->account_registry_root
        << commitment->nullifier_root
        << commitment->bridge_settlement_root
        << GetShieldedPoolBalance();
    return pin.GetSHA256();
}

std::optional<uint256> ChainstateManager::ComputeShieldedSnapshotStatePinEmptyVelocityV3ForMigration() const
{
    AssertLockHeld(::cs_main);
    const auto commitment = GetShieldedStateCommitment();
    if (!commitment.has_value()) return std::nullopt;
    ShieldedUnshieldVelocity empty_velocity;
    HashWriter pin;
    pin << std::string{"BTX_ShieldedSnapshotStatePin_V3"}
        << commitment->note_commitment_root
        << commitment->account_registry_root
        << commitment->nullifier_root
        << commitment->bridge_settlement_root
        << GetShieldedPoolBalance()
        << empty_velocity;
    return pin.GetSHA256();
}

bool ChainstateManager::PersistShieldedState(const CBlockIndex* tip,
                                             bool fast_startup_trusted)
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return false;
    if (!fast_startup_trusted &&
        (InjectShieldedTransitionWriteFailureForTest(
             ShieldedTransitionWriteSeam::STATE_PIN) ||
         !m_shielded_nullifiers->ClearPersistedShieldedStatePin())) {
        return false;
    }
    const auto commitment_index_digest = m_shielded_merkle_tree.CommitmentIndexDigest();
    if (!commitment_index_digest.has_value()) {
        LogPrintf("PersistShieldedState: missing commitment index digest tree_size=%u root=%s\n",
                  static_cast<unsigned int>(m_shielded_merkle_tree.Size()),
                  m_shielded_merkle_tree.Root().ToString());
        return false;
    }

    std::vector<uint256> anchor_roots;
    anchor_roots.reserve(m_shielded_anchor_roots.size());
    for (const auto& root : m_shielded_anchor_roots) {
        anchor_roots.push_back(root);
    }

    std::vector<uint256> account_registry_roots;
    account_registry_roots.reserve(m_shielded_account_registry_roots.size());
    for (const auto& root : m_shielded_account_registry_roots) {
        account_registry_roots.push_back(root);
    }

    const auto account_registry_snapshot = m_shielded_account_registry.ExportPersistedSnapshot();
    if (!account_registry_snapshot.IsValid()) {
        LogPrintf("PersistShieldedState: invalid account registry snapshot size=%u root=%s\n",
                  static_cast<unsigned int>(m_shielded_account_registry.Size()),
                  m_shielded_account_registry.Root().ToString());
        return false;
    }
    const auto shielded_state_pin = ComputeShieldedSnapshotStatePin();
    if (!shielded_state_pin.has_value()) {
        LogPrintf("PersistShieldedState: failed to compute full shielded state pin at height=%d hash=%s\n",
                  tip ? tip->nHeight : -1,
                  tip ? tip->GetBlockHash().ToString() : uint256{}.ToString());
        return false;
    }

    LogDebug(BCLog::VALIDATION, "PersistShieldedState: writing snapshot height=%d hash=%s tree_size=%u registry_entries=%u\n",
              tip ? tip->nHeight : -1,
              tip ? tip->GetBlockHash().ToString() : uint256{}.ToString(),
              static_cast<unsigned int>(m_shielded_merkle_tree.Size()),
              static_cast<unsigned int>(m_shielded_account_registry.Size()));

    if (InjectShieldedTransitionWriteFailureForTest(
            ShieldedTransitionWriteSeam::PERSISTED_STATE) ||
        !m_shielded_nullifiers->WritePersistedState(
        m_shielded_merkle_tree,
        anchor_roots,
        tip ? tip->GetBlockHash() : uint256{},
        tip ? tip->nHeight : -1,
        m_shielded_pool_balance.GetBalance(),
        commitment_index_digest,
        account_registry_snapshot,
        account_registry_roots)) {
        return false;
    }
    // Persist the nullifier-set MuHash accumulator digest at this tip so a restart can cheaply
    // verify the loaded nullifier set has not drifted (without re-deriving it from chain). Written
    // only here -- direct test/diagnostic nullifier insertions that bypass PersistShieldedState
    // leave the persisted digest unchanged, so the restart drift check still catches them.
    if (InjectShieldedTransitionWriteFailureForTest(
            ShieldedTransitionWriteSeam::NULLIFIER_ACCUMULATOR) ||
        !m_shielded_nullifiers->PersistNullifierAccumulator()) {
        return false;
    }
    if (fast_startup_trusted &&
        (InjectShieldedTransitionWriteFailureForTest(
             ShieldedTransitionWriteSeam::STATE_PIN) ||
         !m_shielded_nullifiers->PersistShieldedStatePin(*shielded_state_pin))) {
        return false;
    }
    if (InjectShieldedTransitionWriteFailureForTest(
            ShieldedTransitionWriteSeam::MARKER_CLEAR) ||
        !m_shielded_nullifiers->ClearMutationMarker()) {
        return false;
    }
    if (tip) m_last_persisted_shielded_tip_hash = tip->GetBlockHash();
    return true;
}

bool ChainstateManager::HasDurableShieldedSnapshotAt(const CBlockIndex* tip) const
{
    AssertLockHeld(::cs_main);
    return tip != nullptr &&
           m_last_persisted_shielded_tip_hash.has_value() &&
           *m_last_persisted_shielded_tip_hash == tip->GetBlockHash();
}

bool ChainstateManager::ReadPersistedShieldedState(
    shielded::ShieldedMerkleTree& tree,
    std::vector<uint256>& anchor_roots,
    uint256& tip_hash,
    int32_t& tip_height,
    CAmount& balance,
    std::optional<uint256>& commitment_index_digest,
    std::optional<shielded::registry::ShieldedAccountRegistryPersistedSnapshot>&
        account_registry_snapshot,
    std::vector<uint256>* account_registry_roots) const
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return false;
    return m_shielded_nullifiers->ReadPersistedState(tree,
                                                     anchor_roots,
                                                     tip_hash,
                                                     tip_height,
                                                     balance,
                                                     commitment_index_digest,
                                                     account_registry_snapshot,
                                                     account_registry_roots);
}

bool ChainstateManager::WritePersistedShieldedState(
    const shielded::ShieldedMerkleTree& tree,
    const std::vector<uint256>& anchor_roots,
    const uint256& tip_hash,
    int32_t tip_height,
    CAmount balance,
    std::optional<uint256> commitment_index_digest,
    std::optional<shielded::registry::ShieldedAccountRegistryPersistedSnapshot>
        account_registry_snapshot,
    std::vector<uint256> account_registry_roots)
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return false;
    return m_shielded_nullifiers->WritePersistedState(tree,
                                                      anchor_roots,
                                                      tip_hash,
                                                      tip_height,
                                                      balance,
                                                      std::move(commitment_index_digest),
                                                      std::move(account_registry_snapshot),
                                                      std::move(account_registry_roots));
}

util::Result<void> ChainstateManager::LoadShieldedSnapshotSection(
    AutoFile& file,
    const ShieldedSnapshotSectionHeader& header,
    const CBlockIndex* tip)
{
    return LoadShieldedSnapshotSection(
        file, header, tip, ShieldedSnapshotPinPolicy{});
}

util::Result<void> ChainstateManager::LoadShieldedSnapshotSection(
    AutoFile& file,
    const ShieldedSnapshotSectionHeader& header,
    const CBlockIndex* tip,
    ShieldedSnapshotPinPolicy pin_policy)
{
    AssertLockHeld(::cs_main);

    auto fail = [](const std::string& reason) -> util::Result<void> {
        LogPrintf("LoadShieldedSnapshotSection: %s\n", reason);
        return util::Error{Untranslated(reason)};
    };
    const Chainstate* rebuild_chainstate = m_snapshot_chainstate ? m_snapshot_chainstate.get()
                                                                 : m_active_chainstate;
    const auto require_full_rebuild_blocks =
        [&](const std::string& reason) EXCLUSIVE_LOCKS_REQUIRED(::cs_main) -> util::Result<void> {
        if (rebuild_chainstate != nullptr &&
            ShieldedFullRebuildBlocksAvailable(*rebuild_chainstate, tip)) {
            return {};
        }
        return fail(reason);
    };

    if (header.m_snapshot_version < node::SHIELDED_SNAPSHOT_ACCOUNT_REGISTRY_VERSION) {
        auto result{require_full_rebuild_blocks(strprintf(
            "snapshot version %u lacks account-registry payload state and this node does not have the historical blocks needed to rebuild it; use a v%u+ BTX shielded snapshot, a non-pruned datadir, or reindex/redownload",
            header.m_snapshot_version,
            node::SHIELDED_SNAPSHOT_ACCOUNT_REGISTRY_VERSION))};
        if (!result) return result;
    }
    if (header.m_snapshot_version < node::SHIELDED_SNAPSHOT_RECOVERY_EXIT_COMMITMENTS_VERSION &&
        tip != nullptr &&
        GetConsensus().IsShieldedRecoveryExitActive(tip->nHeight)) {
        auto result{require_full_rebuild_blocks(strprintf(
            "snapshot version %u does not contain recovery-exit commitments and this node does not have the historical blocks needed to rebuild them at height %d; use a v%u+ BTX shielded snapshot, a non-pruned datadir, or reindex/redownload",
            header.m_snapshot_version,
            tip->nHeight,
            node::SHIELDED_SNAPSHOT_RECOVERY_EXIT_COMMITMENTS_VERSION))};
        if (!result) return result;
    }
    const bool velocity_state_required_at_tip =
        tip != nullptr && ShieldedUnshieldVelocityStateRequiredAtTip(GetConsensus(), tip->nHeight);

    const fs::path shielded_db_path = m_options.datadir / "shielded_state" / "nullifiers";
    const fs::path commitment_index_db_path = m_options.datadir / "shielded_state" / "commitments";
    const fs::path account_registry_db_path = m_options.datadir / "shielded_state" / "account_registry";

    m_shielded_nullifiers.reset();
    // Snapshot activation may run while the manager already has a live
    // shielded state bound to shared LevelDB stores. Release those handles
    // before moving/wiping/reopening the stores or LevelDB will reject the
    // second open with the same LOCK file still held in-process.
    m_shielded_merkle_tree = shielded::ShieldedMerkleTree{
        shielded::ShieldedMerkleTree::IndexStorageMode::MEMORY_ONLY};
    shielded::ShieldedMerkleTree::ResetCommitmentIndexStore();
    m_shielded_account_registry = shielded::registry::ShieldedAccountRegistryState{};
    shielded::registry::ShieldedAccountRegistryState::ResetPayloadStore();

    auto backup = BackupShieldedStateDirectories(m_options.datadir);
    if (!backup.has_value()) {
        // The live LevelDB handles were released before taking the backup. If
        // journalling or a backup rename failed, restore the manager's
        // in-memory view of the still-authoritative state before returning.
        // EnsureShieldedStateInitialized also completes any recoverable
        // PREPARING transaction left by BackupShieldedStateDirectories.
        m_shielded_state_initialized = false;
        if (!EnsureShieldedStateInitialized()) {
            GetNotifications().fatalError(_(
                "Failed to reopen previous shielded state after snapshot backup failure; shutting down to avoid using incomplete consensus state."));
        }
        return fail("failed to back up existing shielded_state directories before snapshot load");
    }
    auto release_partial_shielded_snapshot_state = [&]() EXCLUSIVE_LOCKS_REQUIRED(::cs_main) {
        m_shielded_nullifiers.reset();
        m_shielded_merkle_tree = shielded::ShieldedMerkleTree{
            shielded::ShieldedMerkleTree::IndexStorageMode::MEMORY_ONLY};
        shielded::ShieldedMerkleTree::ResetCommitmentIndexStore();
        m_shielded_account_registry = shielded::registry::ShieldedAccountRegistryState{};
        shielded::registry::ShieldedAccountRegistryState::ResetPayloadStore();
        m_shielded_smile_public_accounts.clear();
        m_shielded_account_leaf_commitments.clear();
        InvalidateShieldedAccountStateSnapshotCaches();
        m_shielded_anchor_roots.clear();
        m_shielded_account_registry_roots.clear();
        m_shielded_pool_balance = ShieldedPoolBalance{};
        m_shielded_unshield_velocity.Clear();
        m_shielded_state_initialized = false;
    };
    auto restore_backup = [this, release_partial_shielded_snapshot_state](
                              ShieldedStateDirectoryBackup& backup_to_restore)
        EXCLUSIVE_LOCKS_REQUIRED(::cs_main) {
        release_partial_shielded_snapshot_state();
        ShieldedSnapshotTxnMarker rolling{
            .phase = static_cast<uint8_t>(
                ShieldedSnapshotTxnPhase::ROLLING_BACK),
            .backup_name = fs::PathToString(
                backup_to_restore.backup_root.filename()),
        };
        if (!WriteShieldedSnapshotTxnMarker(m_options.datadir, rolling)) {
            GetNotifications().fatalError(_(
                "Failed to journal shielded snapshot rollback; shutting down to avoid using incomplete consensus state."));
            return false;
        }
        if (!RestoreShieldedStateDirectoryBackup(backup_to_restore)) {
            LogPrintf("LoadShieldedSnapshotSection: failed to restore previous shielded state directories after snapshot load failure\n");
            GetNotifications().fatalError(_(
                "Failed to restore previous shielded state after rejected snapshot; shutting down to avoid using incomplete consensus state."));
            return false;
        }
        if (!ClearShieldedSnapshotTxnMarker(m_options.datadir)) {
            GetNotifications().fatalError(_(
                "Failed to clear completed shielded snapshot rollback marker; shutting down to avoid ambiguous consensus state."));
            return false;
        }
        if (!EnsureShieldedStateInitialized()) {
            LogPrintf("LoadShieldedSnapshotSection: restored previous directories but failed to reopen previous shielded state\n");
            GetNotifications().fatalError(_(
                "Failed to reopen previous shielded state after rejected snapshot; shutting down to avoid using incomplete consensus state."));
            return false;
        }
        return true;
    };
    auto rollback_backup = [restore_backup](ShieldedStateDirectoryBackup* backup_to_restore)
        EXCLUSIVE_LOCKS_REQUIRED(::cs_main) {
        if (backup_to_restore == nullptr) return;
        (void)restore_backup(*backup_to_restore);
        delete backup_to_restore;
    };
    std::unique_ptr<ShieldedStateDirectoryBackup, decltype(rollback_backup)> rollback_guard{
        new ShieldedStateDirectoryBackup{std::move(*backup)},
        rollback_backup};

    if (!PrepareShieldedCommitmentIndex(commitment_index_db_path,
                                        m_options.retain_shielded_commitment_index,
                                        /*wipe_data=*/true)) {
        return fail("failed to prepare shielded commitment index for snapshot load");
    }
    shielded::registry::ShieldedAccountRegistryState::ResetPayloadStore();
    m_shielded_account_registry = shielded::registry::ShieldedAccountRegistryState::WithConfiguredPayloadStore();
    if (!PrepareShieldedAccountRegistryPayloadStore(account_registry_db_path, /*wipe_data=*/true)) {
        return fail("failed to prepare shielded account-registry payload store for snapshot load");
    }

    m_shielded_merkle_tree = shielded::ShieldedMerkleTree{};
    m_shielded_account_registry = shielded::registry::ShieldedAccountRegistryState::WithConfiguredPayloadStore();
    m_shielded_smile_public_accounts.clear();
    m_shielded_account_leaf_commitments.clear();
    InvalidateShieldedAccountStateSnapshotCaches();
    m_shielded_anchor_roots.clear();
    m_shielded_account_registry_roots.clear();
    m_shielded_pool_balance = ShieldedPoolBalance{};
    m_shielded_unshield_velocity.Clear();
    m_shielded_state_initialized = false;
    m_shielded_nullifiers = std::make_unique<NullifierSet>(shielded_db_path,
                                                           8 << 20,
                                                           /*memory_only=*/false,
                                                           /*wipe_data=*/true);
    if (!m_shielded_nullifiers->WriteMutationMarker(MakeShieldedMutationMarker(tip))) {
        return fail("failed to write shielded mutation marker before snapshot load");
    }

    try {
        for (uint64_t i = 0; i < header.m_commitment_count; ++i) {
            uint256 commitment;
            file >> commitment;
            m_shielded_merkle_tree.Append(commitment);
        }

        std::vector<Nullifier> batch;
        batch.reserve(8192);
        for (uint64_t i = 0; i < header.m_nullifier_count; ++i) {
            Nullifier nullifier;
            file >> nullifier;
            batch.push_back(nullifier);
            if (batch.size() == 8192) {
                if (!m_shielded_nullifiers->Insert(batch)) {
                    return fail("failed to insert shielded nullifier batch from snapshot");
                }
                batch.clear();
            }
        }
        if (!batch.empty() && !m_shielded_nullifiers->Insert(batch)) {
            return fail("failed to insert final shielded nullifier batch from snapshot");
        }

        if (header.m_snapshot_version >= node::SHIELDED_SNAPSHOT_RECOVERY_EXIT_COMMITMENTS_VERSION) {
            std::vector<uint256> recovery_exit_commitments;
            recovery_exit_commitments.reserve(std::min<uint64_t>(header.m_recovery_exit_commitment_count, 8192));
            for (uint64_t i = 0; i < header.m_recovery_exit_commitment_count; ++i) {
                uint256 commitment;
                file >> commitment;
                recovery_exit_commitments.push_back(commitment);
                if (recovery_exit_commitments.size() == 8192) {
                    if (!m_shielded_nullifiers->InsertRecoveryExitCommitments(recovery_exit_commitments)) {
                        return fail("failed to insert recovery-exit commitment batch from snapshot");
                    }
                    recovery_exit_commitments.clear();
                }
            }
            if (!recovery_exit_commitments.empty() &&
                !m_shielded_nullifiers->InsertRecoveryExitCommitments(recovery_exit_commitments)) {
                return fail("failed to insert final recovery-exit commitment batch from snapshot");
            }
        }

        if (header.m_snapshot_version >= 4) {
            std::vector<uint256> settlement_anchors;
            settlement_anchors.reserve(std::min<uint64_t>(header.m_settlement_anchor_count, 8192));
            for (uint64_t i = 0; i < header.m_settlement_anchor_count; ++i) {
                uint256 anchor;
                file >> anchor;
                settlement_anchors.push_back(anchor);
                if (settlement_anchors.size() == 8192) {
                    if (!m_shielded_nullifiers->InsertSettlementAnchors(settlement_anchors)) {
                        return fail("failed to insert settlement-anchor batch from snapshot");
                    }
                    settlement_anchors.clear();
                }
            }
            if (!settlement_anchors.empty() &&
                !m_shielded_nullifiers->InsertSettlementAnchors(settlement_anchors)) {
                return fail("failed to insert final settlement-anchor batch from snapshot");
            }

            std::vector<ConfirmedNettingManifestState> netting_manifests;
            netting_manifests.reserve(std::min<uint64_t>(header.m_netting_manifest_count, 8192));
            for (uint64_t i = 0; i < header.m_netting_manifest_count; ++i) {
                if (header.m_snapshot_version >= 6) {
                    ConfirmedNettingManifestState manifest_state;
                    file >> manifest_state;
                    netting_manifests.push_back(std::move(manifest_state));
                    if (netting_manifests.size() == 8192) {
                        if (!m_shielded_nullifiers->InsertNettingManifests(netting_manifests)) {
                            return fail("failed to insert netting-manifest batch from snapshot");
                        }
                        netting_manifests.clear();
                    }
                } else {
                    uint256 manifest_id;
                    file >> manifest_id;
                }
            }
            if (header.m_snapshot_version >= 6 &&
                !netting_manifests.empty() &&
                !m_shielded_nullifiers->InsertNettingManifests(netting_manifests)) {
                return fail("failed to insert final netting-manifest batch from snapshot");
            }
        }

        if (header.m_snapshot_version >= node::SHIELDED_SNAPSHOT_ACCOUNT_REGISTRY_VERSION) {
            shielded::registry::ShieldedAccountRegistrySnapshot snapshot;
            snapshot.entries.reserve(std::min<uint64_t>(header.m_account_registry_entry_count, 8192));
            for (uint64_t i = 0; i < header.m_account_registry_entry_count; ++i) {
                shielded::registry::ShieldedAccountRegistryEntry entry;
                file >> entry;
                snapshot.entries.push_back(entry);
            }
            const auto restored_registry =
                shielded::registry::ShieldedAccountRegistryState::Restore(snapshot);
            if (!restored_registry.has_value()) {
                return fail("invalid shielded account-registry snapshot section");
            }
            m_shielded_account_registry = std::move(*restored_registry);
        }
    } catch (const std::ios_base::failure&) {
        return fail("truncated or malformed BTX shielded snapshot section");
    }

    if (header.m_snapshot_version < node::SHIELDED_SNAPSHOT_RECOVERY_EXIT_COMMITMENTS_VERSION &&
        tip != nullptr &&
        GetConsensus().IsShieldedRecoveryExitActive(tip->nHeight)) {
        shielded::ShieldedMerkleTree rebuilt_tree{
            shielded::ShieldedMerkleTree::IndexStorageMode::MEMORY_ONLY};
        ShieldedPoolBalance rebuilt_pool;
        std::vector<uint256> rebuilt_recovery_exit_commitments;
        if (!RebuildShieldedState(*rebuild_chainstate,
                                  tip,
                                  rebuilt_tree,
                                  rebuilt_pool,
                                  /*all_nullifiers=*/nullptr,
                                  /*public_accounts=*/nullptr,
                                  /*account_leaf_commitments=*/nullptr,
                                  &rebuilt_recovery_exit_commitments)) {
            return fail("failed to rebuild recovery-exit commitments for legacy shielded snapshot");
        }
        if (!rebuilt_recovery_exit_commitments.empty() &&
            !m_shielded_nullifiers->InsertRecoveryExitCommitments(rebuilt_recovery_exit_commitments)) {
            return fail("failed to persist rebuilt recovery-exit commitments for legacy shielded snapshot");
        }
    }

    if (!m_shielded_pool_balance.SetBalance(header.m_pool_balance)) {
        return fail("invalid shielded pool balance in snapshot section");
    }
    ShieldedUnshieldVelocity snapshot_unshield_velocity;
    if (header.m_snapshot_version >= node::SHIELDED_SNAPSHOT_UNSHIELD_VELOCITY_VERSION) {
        snapshot_unshield_velocity = header.m_unshield_velocity;
    } else if (velocity_state_required_at_tip) {
        if (rebuild_chainstate == nullptr) {
            return fail("legacy shielded snapshot lacks unshield velocity and no chainstate is available to rebuild it");
        }
        std::string rebuild_error;
        if (!RebuildRecentShieldedUnshieldVelocity(*rebuild_chainstate,
                                                   tip,
                                                   GetConsensus(),
                                                   snapshot_unshield_velocity,
                                                   rebuild_error)) {
            return fail(strprintf(
                "snapshot version %u lacks unshield velocity and local recent blocks are unavailable to rebuild it at height %d (%s); use a v%u+ BTX shielded snapshot, a non-pruned datadir, or reindex/redownload",
                header.m_snapshot_version,
                tip->nHeight,
                rebuild_error,
                node::SHIELDED_SNAPSHOT_UNSHIELD_VELOCITY_VERSION));
        }
    }
    if (header.m_snapshot_version < node::SHIELDED_SNAPSHOT_ACCOUNT_REGISTRY_VERSION) {
        if (m_active_chainstate != nullptr &&
            !RebuildShieldedSmilePublicAccountState(*m_active_chainstate,
                                                    tip,
                                                    m_shielded_smile_public_accounts)) {
            return fail("failed to rebuild SMILE public-account state for legacy shielded snapshot");
        }
        if (m_active_chainstate != nullptr &&
            !RebuildShieldedAccountLeafCommitmentState(*m_active_chainstate,
                                                       tip,
                                                       m_shielded_account_leaf_commitments)) {
            return fail("failed to rebuild account-leaf commitment state for legacy shielded snapshot");
        }
        if (m_active_chainstate == nullptr ||
            !RebuildShieldedAccountRegistryState(*m_active_chainstate,
                                                 tip,
                                                 m_shielded_account_registry)) {
            return fail("failed to rebuild account-registry state for legacy shielded snapshot");
        }
    } else if (!shielded::registry::BuildRegistryAccountState(m_shielded_account_registry,
                                                              m_shielded_smile_public_accounts,
                                                              m_shielded_account_leaf_commitments)) {
        return fail("failed to build account-registry lookup state from snapshot section");
    }
    if (tip != nullptr &&
        UseAccountRegistryEntryCountLimit(GetConsensus(), tip->nHeight) &&
        m_shielded_account_registry.Size() > GetConsensus().nMaxShieldedAccountRegistryEntries) {
        return fail("shielded account-registry snapshot exceeds the active entry limit");
    }
    if (!m_shielded_nullifiers->WritePoolBalance(m_shielded_pool_balance.GetBalance())) {
        return fail("failed to persist shielded pool balance from snapshot section");
    }
    if (!m_shielded_nullifiers->WriteUnshieldVelocity(snapshot_unshield_velocity)) {
        return fail("failed to persist unshield-velocity log from snapshot section");
    }
    m_shielded_unshield_velocity = snapshot_unshield_velocity;

    m_shielded_anchor_roots.clear();
    RecordShieldedAnchorRoot(m_shielded_merkle_tree.Root());
    shielded::ShieldedMerkleTree cursor_tree = m_shielded_merkle_tree;
    if (!cursor_tree.DetachToMemoryOnly()) {
        return fail("failed to detach shielded tree while reconstructing anchor history");
    }
    for (const uint64_t output_count : header.m_recent_output_counts) {
        if (output_count > 0 && !cursor_tree.RemoveLast(output_count)) {
            return fail("shielded snapshot output-count history is inconsistent with the commitment tree");
        }
        RecordShieldedAnchorRoot(cursor_tree.Root());
    }

    m_shielded_account_registry_roots.clear();
    if (header.m_snapshot_version >= node::SHIELDED_SNAPSHOT_ACCOUNT_REGISTRY_HISTORY_VERSION) {
        const size_t expected_recent_history = ExpectedShieldedRecentHistoryCount(tip);
        if (header.m_recent_output_counts.size() != expected_recent_history) {
            LogPrintf("LoadShieldedSnapshotSection: v7 account-registry snapshot has %u recent output-count entries, expected %u at height=%d hash=%s\n",
                      static_cast<unsigned int>(header.m_recent_output_counts.size()),
                      static_cast<unsigned int>(expected_recent_history),
                      tip != nullptr ? tip->nHeight : -1,
                      tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
            return fail(strprintf("shielded v7+ snapshot has %u recent output-count entries, expected %u at height %d",
                                  static_cast<unsigned int>(header.m_recent_output_counts.size()),
                                  static_cast<unsigned int>(expected_recent_history),
                                  tip != nullptr ? tip->nHeight : -1));
        }
        if (!LoadShieldedAccountRegistryRootWindow(
                header.m_account_registry_roots,
                m_shielded_account_registry.Root(),
                expected_recent_history + 1U,
                "LoadShieldedSnapshotSection",
                m_shielded_account_registry_roots)) {
            return fail("invalid shielded account-registry root history in snapshot section");
        }
    } else if (m_active_chainstate != nullptr &&
               ShieldedRebuildHistoryBlocksAvailable(*m_active_chainstate, tip)) {
        if (!RebuildShieldedAccountRegistryHistory(*m_active_chainstate, tip)) {
            return fail("failed to rebuild shielded account-registry history from local blocks");
        }
    } else if (header.m_snapshot_version >= node::SHIELDED_SNAPSHOT_ACCOUNT_REGISTRY_VERSION) {
        const size_t expected_recent_history = ExpectedShieldedRecentHistoryCount(tip);
        if (header.m_recent_output_counts.size() != expected_recent_history) {
            LogPrintf("LoadShieldedSnapshotSection: v6 account-registry snapshot has %u recent output-count entries, expected %u at height=%d hash=%s\n",
                      static_cast<unsigned int>(header.m_recent_output_counts.size()),
                      static_cast<unsigned int>(expected_recent_history),
                      tip != nullptr ? tip->nHeight : -1,
                      tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
            return fail(strprintf("shielded snapshot has %u recent output-count entries, expected %u at height %d",
                                  static_cast<unsigned int>(header.m_recent_output_counts.size()),
                                  static_cast<unsigned int>(expected_recent_history),
                                  tip != nullptr ? tip->nHeight : -1));
        }
        std::deque<uint256> derived_roots;
        if (!DeriveShieldedAccountRegistryHistoryFromV6Snapshot(m_shielded_merkle_tree,
                                                                m_shielded_account_registry,
                                                                header.m_recent_output_counts,
                                                                derived_roots)) {
            LogPrintf("LoadShieldedSnapshotSection: v6 account-registry snapshot lacks enough data to derive recent root history at height=%d hash=%s\n",
                      tip != nullptr ? tip->nHeight : -1,
                      tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
            return fail("shielded v6 snapshot lacks enough data to derive recent account-registry root history");
        }
        m_shielded_account_registry_roots = std::move(derived_roots);
    } else {
        return fail("legacy shielded snapshot lacks account-registry history and local blocks are unavailable to rebuild it");
    }
    if (header.m_snapshot_version < 4) {
        // Pre-v4 snapshots lack both settlement and netting metadata; one fused walk fills both.
        if (!SyncShieldedBridgeMetadataState(*m_active_chainstate, tip, *m_shielded_nullifiers)) {
            return fail("failed to sync settlement-anchor/netting state for legacy shielded snapshot");
        }
    } else if (header.m_snapshot_version < 6) {
        if (m_active_chainstate == nullptr ||
            !SyncShieldedNettingManifestState(*m_active_chainstate,
                                              tip,
                                              *m_shielded_nullifiers,
                                              PersistedShieldedMetadataSyncMode::STRICT_CHAIN_CONVERGENCE,
                                              &GetNotifications())) {
            return fail("failed to sync netting-manifest state for legacy shielded snapshot");
        }
    }
    const bool preserve_snapshot_bridge_metadata_extras =
        header.m_snapshot_version >= 4 &&
        (header.m_settlement_anchor_count > 0 || header.m_netting_manifest_count > 0);
    if (!m_shielded_nullifiers->WriteSnapshotBridgeMetadataHint(
            preserve_snapshot_bridge_metadata_extras)) {
        return fail("failed to persist snapshot bridge-metadata hint");
    }

    m_shielded_state_initialized = true;
    const auto accepted_state_pin = ComputeShieldedSnapshotStatePin();
    if (pin_policy.required_pin.has_value() &&
        (!accepted_state_pin.has_value() ||
         *accepted_state_pin != *pin_policy.required_pin)) {
        return fail("BTX shielded snapshot state does not match the consensus-pinned commitment");
    }
    if (!PersistShieldedState(tip, /*fast_startup_trusted=*/false)) {
        return fail("failed to persist loaded shielded snapshot state");
    }
    if (pin_policy.persist_accepted_pin &&
        (!accepted_state_pin.has_value() || !m_shielded_nullifiers ||
         !m_shielded_nullifiers->PersistShieldedStatePin(*accepted_state_pin))) {
        return fail("failed to persist verified BTX shielded snapshot provenance");
    }
    if (pin_policy.deferred_completion != nullptr) {
        auto backup_owner = std::shared_ptr<ShieldedStateDirectoryBackup>{
            rollback_guard.release()};
        *pin_policy.deferred_completion =
            [backup_owner = std::move(backup_owner), restore_backup,
             datadir = m_options.datadir](bool commit) {
                AssertLockHeld(::cs_main);
                if (!commit) return restore_backup(*backup_owner);
                if (!DiscardShieldedStateDirectoryBackup(*backup_owner)) {
                    LogPrintf("LoadShieldedSnapshotSection: loaded snapshot but failed to remove previous shielded state backup at %s\n",
                              fs::PathToString(backup_owner->backup_root));
                    // The base hash is already the durable commit record.
                    // Retain the marker and backup so startup can retry the
                    // cleanup without making the transaction ambiguous.
                    return true;
                }
                if (!ClearShieldedSnapshotTxnMarker(datadir)) {
                    // The base hash is already committed and the old backup
                    // is gone. A stale marker with no backup root is safely
                    // cleared by startup recovery.
                    LogPrintf("LoadShieldedSnapshotSection: committed snapshot but failed to clear shielded transaction marker\n");
                }
                return true;
            };
    } else {
        ShieldedStateDirectoryBackup* committed_backup = rollback_guard.release();
        if (!DiscardShieldedStateDirectoryBackup(*committed_backup)) {
            LogPrintf("LoadShieldedSnapshotSection: loaded snapshot but failed to remove previous shielded state backup at %s\n",
                      fs::PathToString(committed_backup->backup_root));
            // Direct section loads have no later base-hash commit. Restore the
            // previous state now; a partial discard is fatal if it destroyed
            // an entry needed for that rollback.
            (void)restore_backup(*committed_backup);
            delete committed_backup;
            return fail("failed to discard previous shielded state backup after snapshot load");
        }
        if (!ClearShieldedSnapshotTxnMarker(m_options.datadir)) {
            delete committed_backup;
            return fail("failed to clear committed shielded snapshot transaction marker");
        }
        delete committed_backup;
    }
    return {};
}

std::optional<shielded::registry::ShieldedAccountRegistrySnapshot>
ChainstateManager::ExportShieldedAccountRegistrySnapshot(
    const Chainstate& chainstate,
    const CBlockIndex* tip)
{
    AssertLockHeld(::cs_main);

    auto snapshot = m_shielded_account_registry.ExportSnapshot();
    if (snapshot.IsValid()) {
        return snapshot;
    }

    LogPrintf("ExportShieldedAccountRegistrySnapshot: rebuilding payload-backed registry state at height=%d hash=%s\n",
              tip != nullptr ? tip->nHeight : -1,
              tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());

    shielded::registry::ShieldedAccountRegistryState rebuilt_registry =
        shielded::registry::ShieldedAccountRegistryState::WithConfiguredPayloadStore();
    if (!RebuildShieldedAccountRegistryState(chainstate, tip, rebuilt_registry)) {
        LogPrintf("ExportShieldedAccountRegistrySnapshot: failed to rebuild account registry from chain\n");
        return std::nullopt;
    }

    if (rebuilt_registry.Size() != m_shielded_account_registry.Size() ||
        rebuilt_registry.Root() != m_shielded_account_registry.Root()) {
        LogPrintf(
            "ExportShieldedAccountRegistrySnapshot: rebuilt registry mismatch size=%u/%u root=%s/%s\n",
            static_cast<unsigned int>(rebuilt_registry.Size()),
            static_cast<unsigned int>(m_shielded_account_registry.Size()),
            rebuilt_registry.Root().ToString(),
            m_shielded_account_registry.Root().ToString());
        return std::nullopt;
    }

    m_shielded_account_registry = std::move(rebuilt_registry);
    snapshot = m_shielded_account_registry.ExportSnapshot();
    if (!snapshot.IsValid()) {
        LogPrintf("ExportShieldedAccountRegistrySnapshot: rebuilt registry still failed snapshot export\n");
        return std::nullopt;
    }
    return snapshot;
}

bool ChainstateManager::EnsureShieldedStateInitialized()
{
    AssertLockHeld(::cs_main);
    if (m_shielded_state_initialized) {
        return m_shielded_nullifiers != nullptr;
    }
    if (m_active_chainstate == nullptr) {
        LogPrintf("EnsureShieldedStateInitialized: no active chainstate\n");
        return false;
    }
    if (!RecoverInterruptedShieldedSnapshotTransaction(m_options.datadir)) {
        GetNotifications().fatalError(_(
            "Failed to recover an interrupted shielded snapshot transaction; refusing to open possibly mixed consensus state."));
        return false;
    }

    GetNotifications().progress(_("Initializing shielded state…"), 0, false);

    // -resetshieldedstate: supported one-shot repair. Wipe the whole shielded_state directory before
    // any DB is opened so a stale in-flight mutation marker or a partially-rebuilt/corrupt store cannot
    // wedge startup; the normal path below then opens fresh stores and does one clean full rebuild from
    // local block data. Block files and wallets are untouched.
    if (m_options.reset_shielded_state) {
        const fs::path shielded_state_dir = m_options.datadir / "shielded_state";
        LogPrintf("EnsureShieldedStateInitialized: -resetshieldedstate set; wiping %s to force a clean rebuild\n",
                  fs::PathToString(shielded_state_dir));
        std::error_code ec;
        fs::remove_all(shielded_state_dir, ec);
        if (ec) {
            LogPrintf("EnsureShieldedStateInitialized: failed to wipe shielded_state for -resetshieldedstate (%s)\n",
                      ec.message());
            return false;
        }
    }

    const fs::path shielded_db_path = m_options.datadir / "shielded_state" / "nullifiers";
    const fs::path commitment_index_db_path = m_options.datadir / "shielded_state" / "commitments";
    const fs::path account_registry_db_path = m_options.datadir / "shielded_state" / "account_registry";
    if (!PrepareShieldedCommitmentIndex(commitment_index_db_path,
                                        m_options.retain_shielded_commitment_index,
                                        /*wipe_data=*/false)) {
        return false;
    }
    shielded::registry::ShieldedAccountRegistryState::ResetPayloadStore();
    if (!PrepareShieldedAccountRegistryPayloadStore(account_registry_db_path, /*wipe_data=*/false)) {
        return false;
    }
    m_shielded_merkle_tree = shielded::ShieldedMerkleTree{};
    m_shielded_account_registry = shielded::registry::ShieldedAccountRegistryState::WithConfiguredPayloadStore();
    m_shielded_smile_public_accounts.clear();
    m_shielded_account_leaf_commitments.clear();
    InvalidateShieldedAccountStateSnapshotCaches();
    m_shielded_nullifiers = std::make_unique<NullifierSet>(shielded_db_path,
                                                            8 << 20,
                                                            /*memory_only=*/false,
                                                            /*wipe_data=*/false);
    const CBlockIndex* const tip = m_active_chainstate->m_chain.Tip();
    bool startup_shielded_repair_performed{false};
    // Recent anchor / account-registry root windows are derived from the tip frontier + the last
    // SHIELDED_ANCHOR_DEPTH blocks. Refreshing those windows is cheap and does not invalidate the
    // nullifier-accumulator / full-state-pin evidence used by -fastshieldedstartup. Treat it as a
    // distinct class from substantive repairs (full rebuild, registry rewrite, commitment-index
    // rebuild, etc.) so a history-window mismatch cannot force a silent full-chain settlement /
    // netting / audit scan (the production 224 GB / 15+ minute restart stall).
    bool startup_history_window_refreshed{false};
    bool startup_velocity_repair_performed{false};
    auto load_persisted_unshield_velocity = [&]() EXCLUSIVE_LOCKS_REQUIRED(::cs_main) -> bool {
        AssertLockHeld(::cs_main);
        if (m_shielded_nullifiers &&
            !m_shielded_nullifiers->ReadUnshieldVelocity(m_shielded_unshield_velocity)) {
            LogPrintf("EnsureShieldedStateInitialized: failed to load unshield-velocity window\n");
            return false;
        }
        return true;
    };
    bool startup_velocity_checked{false};
    bool startup_velocity_verified_or_inactive{false};
    bool startup_velocity_rebuild_deferred_to_state_pin{false};
    auto verify_or_repair_startup_velocity = [&]() EXCLUSIVE_LOCKS_REQUIRED(::cs_main) -> bool {
        AssertLockHeld(::cs_main);
        if (startup_velocity_checked) {
            return startup_velocity_verified_or_inactive;
        }
        startup_velocity_checked = true;
        startup_velocity_verified_or_inactive = true;
        startup_velocity_rebuild_deferred_to_state_pin = false;
        if (tip == nullptr) {
            return true;
        }
        const auto& consensus = GetConsensus();
        const int32_t next_block_height =
            tip->nHeight == std::numeric_limits<int32_t>::max() ? tip->nHeight : tip->nHeight + 1;
        const bool velocity_state_required_at_tip =
            ShieldedUnshieldVelocityStateRequiredAtTip(consensus, tip->nHeight);
        const bool velocity_log_present =
            m_shielded_nullifiers && m_shielded_nullifiers->HasUnshieldVelocity();
        if (!velocity_state_required_at_tip &&
            !consensus.IsShieldedUnshieldVelocityCapActive(next_block_height)) {
            return true;
        }

        ShieldedUnshieldVelocity rebuilt_velocity;
        std::string rebuild_error;
        if (!RebuildRecentShieldedUnshieldVelocity(*m_active_chainstate,
                                                   tip,
                                                   consensus,
                                                   rebuilt_velocity,
                                                   rebuild_error)) {
            if (velocity_log_present &&
                consensus.IsShieldedRecoveryExitActive(tip->nHeight) &&
                m_active_chainstate != nullptr &&
                m_active_chainstate->m_from_snapshot_blockhash.has_value() &&
                !ShieldedFullRebuildBlocksAvailable(*m_active_chainstate, tip)) {
                // AssumeUTXO snapshot nodes do not necessarily have the snapshot base block on disk.
                // After they catch up, the persisted velocity window can still reference that base
                // height, so a by-block reconstruction may be impossible even though the v9 snapshot
                // loaded and persisted the velocity log. Defer the velocity check to the full
                // recovery-exit state pin below. That pin covers the velocity log, nullifier root,
                // note root, account-registry root, bridge metadata, and pool balance; mismatches
                // still fail closed when full audit blocks are unavailable.
                LogPrintf("EnsureShieldedStateInitialized: recent unshield velocity rebuild unavailable at height=%d hash=%s (%s); deferring verification to persisted full shielded state pin for pruned assumeutxo snapshot\n",
                          tip->nHeight,
                          tip->GetBlockHash().ToString(),
                          rebuild_error);
                startup_velocity_rebuild_deferred_to_state_pin = true;
                return true;
            }
            LogPrintf("EnsureShieldedStateInitialized: failed to verify persisted unshield velocity at height=%d hash=%s (%s)\n",
                      tip->nHeight,
                      tip->GetBlockHash().ToString(),
                      rebuild_error);
            startup_velocity_verified_or_inactive = false;
            return false;
        }
        const uint32_t window_blocks = consensus.nShieldedUnshieldVelocityWindowBlocks;
        const CAmount persisted_window =
            m_shielded_unshield_velocity.WindowTotal(next_block_height, window_blocks);
        const CAmount rebuilt_window =
            rebuilt_velocity.WindowTotal(next_block_height, window_blocks);
        if (!velocity_log_present || persisted_window != rebuilt_window) {
            LogPrintf("EnsureShieldedStateInitialized: repaired persisted unshield velocity at height=%d hash=%s (present=%d db_window=%lld expected_window=%lld)\n",
                      tip->nHeight,
                      tip->GetBlockHash().ToString(),
                      velocity_log_present ? 1 : 0,
                      static_cast<long long>(persisted_window),
                      static_cast<long long>(rebuilt_window));
            if (!m_shielded_nullifiers ||
                !m_shielded_nullifiers->WriteUnshieldVelocity(rebuilt_velocity)) {
                startup_velocity_verified_or_inactive = false;
                return false;
            }
            m_shielded_unshield_velocity = std::move(rebuilt_velocity);
            startup_velocity_repair_performed = true;
        }
        return true;
    };
    auto finish_success = [&]() EXCLUSIVE_LOCKS_REQUIRED(::cs_main) -> bool {
        AssertLockHeld(::cs_main);
        // v0.32.0 unshield velocity cap: load and verify the persisted trailing-window egress log so
        // the rule is evaluated identically on every node (incl. pruned ones).
        if (!load_persisted_unshield_velocity() ||
            !verify_or_repair_startup_velocity()) {
            return false;
        }
        if (startup_shielded_repair_performed ||
            startup_history_window_refreshed ||
            startup_velocity_repair_performed) {
            AutoReconsiderShieldedInvalidBlocksAfterStartupRepair();
        }
        // Register the shielded prune-retention lock at startup, before any FindFilesToPrune can run,
        // even if no new block has connected yet (no-op unless pruning + shielded).
        UpdateShieldedPruneRetentionLock(*m_active_chainstate);
        GetNotifications().progress(bilingual_str{}, 100, false);
        return true;
    };
    auto persisted_mutation_marker = m_shielded_nullifiers->ReadMutationMarker();
    auto rebuild_from_chain = [&]() EXCLUSIVE_LOCKS_REQUIRED(::cs_main) -> bool {
        AssertLockHeld(::cs_main);
        // Wipe is irreversible. A snapshot-bootstrapped node has no blocks
        // below GetSnapshotBaseBlock(); walking genesis OpenBlockFile-fails
        // after destroying hundreds of MB of otherwise recoverable state
        // (assumeutxo brick after unclean stop). Check first.
        if (!ShieldedFullRebuildBlocksAvailable(*m_active_chainstate, tip)) {
            const CBlockIndex* const snap{GetSnapshotBaseBlock()};
            LogError("EnsureShieldedStateInitialized: fused shielded rebuild "
                     "needs block files this node does not have (active tip "
                     "height=%d hash=%s snapshot_base height=%d). Refusing to "
                     "wipe shielded_state. Reload the assumeutxo snapshot "
                     "while snapshot.dat is still on disk, or restore "
                     "shielded_state from backup.\n",
                     tip ? tip->nHeight : -1,
                     tip ? tip->GetBlockHash().ToString() : uint256{}.ToString(),
                     snap ? snap->nHeight : -1);
            return false;
        }
        m_shielded_nullifiers.reset();
        m_shielded_nullifiers = std::make_unique<NullifierSet>(shielded_db_path,
                                                               8 << 20,
                                                               /*memory_only=*/false,
                                                               /*wipe_data=*/true);
        if (!m_shielded_nullifiers->WriteMutationMarker(MakeShieldedMutationMarker(tip))) {
            return false;
        }
        shielded::registry::ShieldedAccountRegistryState::ResetPayloadStore();
        m_shielded_account_registry = shielded::registry::ShieldedAccountRegistryState::WithConfiguredPayloadStore();
        if (!PrepareShieldedAccountRegistryPayloadStore(account_registry_db_path, /*wipe_data=*/true)) {
            return false;
        }

        // One disk pass rebuilds note tree / nullifiers / pool / velocity, account registry,
        // settlement anchors, and netting manifests. Previously this was four full-history reads
        // (~4x archive block-store IO on MatMul-sized blocks).
        ShieldedChainDerivedRebuildResult rebuilt;
        if (!RebuildShieldedChainDerivedState(
                *m_active_chainstate,
                tip,
                rebuilt,
                ShieldedChainDerivedRebuildParts::ALL,
                shielded::ShieldedMerkleTree::IndexStorageMode::AUTO,
                /*account_registry_use_payload_store=*/true,
                &GetNotifications())) {
            return false;
        }
        if (tip != nullptr &&
            UseAccountRegistryEntryCountLimit(GetConsensus(), tip->nHeight) &&
            rebuilt.account_registry.Size() > GetConsensus().nMaxShieldedAccountRegistryEntries) {
            return false;
        }
        if (!rebuilt.nullifiers.empty() && !m_shielded_nullifiers->Insert(rebuilt.nullifiers)) {
            return false;
        }
        if (!rebuilt.recovery_exit_commitments.empty() &&
            !m_shielded_nullifiers->InsertRecoveryExitCommitments(rebuilt.recovery_exit_commitments)) {
            return false;
        }
        if (!LoadRebuiltShieldedBridgeMetadata(*m_shielded_nullifiers,
                                               rebuilt.settlement_anchors,
                                               rebuilt.netting_manifests)) {
            return false;
        }
        if (!m_shielded_nullifiers->WritePoolBalance(rebuilt.pool.GetBalance())) {
            return false;
        }
        if (!m_shielded_nullifiers->WriteUnshieldVelocity(rebuilt.unshield_velocity)) {
            return false;
        }

        m_shielded_merkle_tree = std::move(rebuilt.tree);
        m_shielded_account_registry = std::move(rebuilt.account_registry);
        m_shielded_smile_public_accounts = std::move(rebuilt.public_accounts);
        m_shielded_account_leaf_commitments = std::move(rebuilt.account_leaf_commitments);
        InvalidateShieldedAccountStateSnapshotCaches();
        m_shielded_pool_balance = rebuilt.pool;
        m_shielded_unshield_velocity = rebuilt.unshield_velocity;
        m_shielded_state_initialized = true;
        if (!RebuildShieldedAnchorHistory(*m_active_chainstate, tip)) {
            m_shielded_state_initialized = false;
            return false;
        }
        if (!RebuildShieldedAccountRegistryHistory(*m_active_chainstate, tip)) {
            m_shielded_state_initialized = false;
            return false;
        }
        if (!PersistShieldedState(tip)) {
            m_shielded_state_initialized = false;
            return false;
        }
        startup_velocity_checked = false;
        startup_velocity_verified_or_inactive = false;
        startup_velocity_rebuild_deferred_to_state_pin = false;
        startup_shielded_repair_performed = true;
        LogPrintf("EnsureShieldedStateInitialized: initialized tree_size=%u root=%s nullifiers=%u pool=%lld\n",
                  static_cast<unsigned int>(m_shielded_merkle_tree.Size()),
                  m_shielded_merkle_tree.Root().ToString(),
                  static_cast<unsigned int>(rebuilt.nullifiers.size()),
                  static_cast<long long>(m_shielded_pool_balance.GetBalance()));
        return true;
    };

    auto reset_shielded_state = [&]() EXCLUSIVE_LOCKS_REQUIRED(::cs_main) {
        AssertLockHeld(::cs_main);
        m_shielded_merkle_tree = shielded::ShieldedMerkleTree{};
        m_shielded_account_registry = shielded::registry::ShieldedAccountRegistryState{};
        m_shielded_smile_public_accounts.clear();
        m_shielded_account_leaf_commitments.clear();
        InvalidateShieldedAccountStateSnapshotCaches();
        m_shielded_anchor_roots.clear();
        m_shielded_account_registry_roots.clear();
        m_shielded_pool_balance = ShieldedPoolBalance{};
        m_shielded_unshield_velocity.Clear();
        m_shielded_state_initialized = false;
    };

    // Complete the idempotent, disk-backed portion of a PREPARED transition
    // from block/undo data. The marker owns the exact target frontier,
    // registry and pool state; this routine makes the remaining stores agree
    // with that target. Every operation below is safe to repeat, so a crash at
    // any point leaves PREPARED in place and the next startup resumes instead
    // of discarding 100k+ blocks of otherwise valid persisted state.
    auto redo_prepared_transition_stores =
        [&](const ShieldedStateMutationMarker& marker) EXCLUSIVE_LOCKS_REQUIRED(::cs_main) -> bool {
            AssertLockHeld(::cs_main);
            if (!marker.IsPreparedTransitionJournal() || !m_shielded_nullifiers) return false;

            const CBlockIndex* const source = marker.source_tip_height < 0
                ? nullptr
                : m_blockman.LookupBlockIndex(marker.source_tip_hash);
            const CBlockIndex* const target = marker.target_tip_height < 0
                ? nullptr
                : m_blockman.LookupBlockIndex(marker.target_tip_hash);
            if ((source == nullptr) != (marker.source_tip_height < 0) ||
                (target == nullptr) != (marker.target_tip_height < 0) ||
                (source != nullptr && source->nHeight != marker.source_tip_height) ||
                (target != nullptr && target->nHeight != marker.target_tip_height)) {
                return false;
            }

            const bool forward = target != nullptr &&
                (source == nullptr || (target->nHeight >= source->nHeight &&
                 target->GetAncestor(source->nHeight) == source));
            const bool reverse = source != nullptr &&
                (target == nullptr || (source->nHeight >= target->nHeight &&
                 source->GetAncestor(target->nHeight) == target));
            const CBlockIndex* const transition_fork =
                source != nullptr && target != nullptr
                    ? LastCommonAncestor(source, target)
                    : nullptr;
            if (!forward && !reverse && source != target && transition_fork == nullptr) return false;

            std::vector<Nullifier> nullifiers_to_insert;
            std::vector<Nullifier> nullifiers_to_remove;
            std::vector<uint256> recovery_commitments_to_insert;
            std::vector<uint256> recovery_commitments_to_remove;
            std::map<uint256, ConfirmedSettlementAnchorState> settlement_to_insert;
            std::set<uint256> settlement_to_remove;
            std::map<uint256, ConfirmedNettingManifestState> manifests_to_insert;
            std::set<uint256> manifests_to_remove;

            auto schedule_settlement_insert =
                [&](const ConfirmedSettlementAnchorState& state) {
                    settlement_to_remove.erase(state.anchor);
                    settlement_to_insert.insert_or_assign(state.anchor, state);
                };
            auto schedule_settlement_remove = [&](const uint256& anchor) {
                settlement_to_insert.erase(anchor);
                settlement_to_remove.insert(anchor);
            };

            auto collect_forward_block =
                [&](const CBlockIndex* pindex) EXCLUSIVE_LOCKS_REQUIRED(::cs_main) -> bool {
                if (ShieldedGapRecoveryCanSkipBlockBody(*pindex)) return true;
                CBlock block;
                if (!ReadShieldedRebuildBlock(*m_active_chainstate, *pindex, block)) return false;
                std::set<uint256> created_anchors;
                std::set<uint256> consumed_anchors;
                for (const auto& txref : block.vtx) {
                    std::string reject_reason;
                    const auto created =
                        ExtractCreatedShieldedSettlementAnchors(*txref, pindex->nHeight, reject_reason);
                    const auto manifests =
                        ExtractCreatedShieldedNettingManifestStates(*txref, pindex->nHeight, reject_reason);
                    if (!created.has_value() || !manifests.has_value()) return false;
                    created_anchors.insert(created->begin(), created->end());
                    for (const auto& manifest : *manifests) {
                        manifests_to_remove.erase(manifest.manifest_id);
                        manifests_to_insert.insert_or_assign(manifest.manifest_id, manifest);
                    }
                    if (!txref->HasShieldedBundle()) continue;
                    const CShieldedBundle& bundle = txref->GetShieldedBundle();
                    const auto block_nullifiers = CollectShieldedNullifiers(bundle);
                    nullifiers_to_insert.insert(nullifiers_to_insert.end(),
                                                block_nullifiers.begin(),
                                                block_nullifiers.end());
                    if (IsRecoveryExitBundle(bundle)) {
                        shielded::recovery::RecoveryExitClaim claim;
                        shielded::recovery::RecoveryExitIdentifiers ids;
                        if (!GetRecoveryExitIdentifiersFromBundle(bundle, claim, ids)) return false;
                        nullifiers_to_insert.push_back(ids.nullifier);
                        recovery_commitments_to_insert.push_back(ids.commitment);
                    }
                    if (UseSingleUseSettlementAnchors(GetConsensus(), pindex->nHeight)) {
                        const auto consumed = CollectShieldedSettlementAnchorRefs(bundle);
                        consumed_anchors.insert(consumed.begin(), consumed.end());
                    }
                }
                for (const auto& anchor : created_anchors) {
                    if (consumed_anchors.count(anchor) == 0) {
                        schedule_settlement_insert({anchor, pindex->nHeight});
                    }
                }
                for (const auto& anchor : consumed_anchors) schedule_settlement_remove(anchor);
                return true;
            };

            auto collect_reverse_block =
                [&](const CBlockIndex* pindex) EXCLUSIVE_LOCKS_REQUIRED(::cs_main) -> bool {
                if (ShieldedGapRecoveryCanSkipBlockBody(*pindex)) return true;
                CBlock block;
                CBlockUndo undo;
                if (!ReadShieldedRebuildBlock(*m_active_chainstate, *pindex, block) ||
                    !m_blockman.ReadBlockUndo(undo, *pindex)) {
                    return false;
                }
                std::set<uint256> created_anchors;
                std::set<uint256> consumed_anchors;
                for (const auto& txref : block.vtx) {
                    std::string reject_reason;
                    const auto created =
                        ExtractCreatedShieldedSettlementAnchors(*txref, pindex->nHeight, reject_reason);
                    const auto manifests =
                        ExtractCreatedShieldedNettingManifestStates(*txref, pindex->nHeight, reject_reason);
                    if (!created.has_value() || !manifests.has_value()) return false;
                    created_anchors.insert(created->begin(), created->end());
                    for (const auto& manifest : *manifests) {
                        manifests_to_insert.erase(manifest.manifest_id);
                        manifests_to_remove.insert(manifest.manifest_id);
                    }
                    if (!txref->HasShieldedBundle()) continue;
                    const CShieldedBundle& bundle = txref->GetShieldedBundle();
                    const auto block_nullifiers = CollectShieldedNullifiers(bundle);
                    nullifiers_to_remove.insert(nullifiers_to_remove.end(),
                                                block_nullifiers.begin(),
                                                block_nullifiers.end());
                    if (IsRecoveryExitBundle(bundle)) {
                        shielded::recovery::RecoveryExitClaim claim;
                        shielded::recovery::RecoveryExitIdentifiers ids;
                        if (!GetRecoveryExitIdentifiersFromBundle(bundle, claim, ids)) return false;
                        nullifiers_to_remove.push_back(ids.nullifier);
                        recovery_commitments_to_remove.push_back(ids.commitment);
                    }
                    if (UseSingleUseSettlementAnchors(GetConsensus(), pindex->nHeight)) {
                        const auto consumed = CollectShieldedSettlementAnchorRefs(bundle);
                        consumed_anchors.insert(consumed.begin(), consumed.end());
                    }
                }
                for (const auto& anchor : created_anchors) {
                    if (consumed_anchors.count(anchor) == 0) schedule_settlement_remove(anchor);
                }
                std::map<uint256, ConfirmedSettlementAnchorState> undo_states;
                for (const auto& state : undo.consumed_settlement_anchor_states) {
                    if (!state.IsValid() || !undo_states.emplace(state.anchor, state).second) return false;
                }
                for (const auto& anchor : consumed_anchors) {
                    if (created_anchors.count(anchor) != 0) continue;
                    const auto it = undo_states.find(anchor);
                    if (it == undo_states.end()) return false;
                    schedule_settlement_insert(it->second);
                }
                return true;
            };

            if (forward) {
                std::vector<const CBlockIndex*> path;
                for (const CBlockIndex* cursor = target; cursor != source; cursor = cursor->pprev) {
                    if (cursor == nullptr) return false;
                    path.push_back(cursor);
                }
                std::reverse(path.begin(), path.end());
                for (const CBlockIndex* pindex : path) {
                    if (!collect_forward_block(pindex)) return false;
                }
            } else if (reverse) {
                for (const CBlockIndex* cursor = source; cursor != target; cursor = cursor->pprev) {
                    if (cursor == nullptr || !collect_reverse_block(cursor)) return false;
                }
            } else if (source != target) {
                for (const CBlockIndex* cursor = source; cursor != transition_fork;
                     cursor = cursor->pprev) {
                    if (cursor == nullptr || !collect_reverse_block(cursor)) return false;
                }
                std::vector<const CBlockIndex*> path;
                for (const CBlockIndex* cursor = target; cursor != transition_fork;
                     cursor = cursor->pprev) {
                    if (cursor == nullptr) return false;
                    path.push_back(cursor);
                }
                std::reverse(path.begin(), path.end());
                for (const CBlockIndex* pindex : path) {
                    if (!collect_forward_block(pindex)) return false;
                }
            }

            if (!nullifiers_to_remove.empty() &&
                !m_shielded_nullifiers->Remove(nullifiers_to_remove)) return false;
            if (!recovery_commitments_to_remove.empty() &&
                !m_shielded_nullifiers->RemoveRecoveryExitCommitments(
                    recovery_commitments_to_remove)) return false;
            if (!nullifiers_to_insert.empty() &&
                !m_shielded_nullifiers->Insert(nullifiers_to_insert)) return false;
            if (!recovery_commitments_to_insert.empty() &&
                !m_shielded_nullifiers->InsertRecoveryExitCommitments(
                    recovery_commitments_to_insert)) return false;

            std::vector<uint256> settlement_removals(settlement_to_remove.begin(),
                                                     settlement_to_remove.end());
            if (!settlement_removals.empty() &&
                !m_shielded_nullifiers->RemoveSettlementAnchors(settlement_removals)) return false;
            std::vector<ConfirmedSettlementAnchorState> settlement_insertions;
            settlement_insertions.reserve(settlement_to_insert.size());
            for (const auto& [_, state] : settlement_to_insert) {
                settlement_insertions.push_back(state);
            }
            if (!settlement_insertions.empty() &&
                !m_shielded_nullifiers->InsertSettlementAnchors(settlement_insertions)) return false;

            std::vector<uint256> manifest_removals(manifests_to_remove.begin(),
                                                   manifests_to_remove.end());
            if (!manifest_removals.empty() &&
                !m_shielded_nullifiers->RemoveNettingManifests(manifest_removals)) return false;
            std::vector<ConfirmedNettingManifestState> manifest_insertions;
            manifest_insertions.reserve(manifests_to_insert.size());
            for (const auto& [_, state] : manifests_to_insert) manifest_insertions.push_back(state);
            if (!manifest_insertions.empty() &&
                !m_shielded_nullifiers->InsertNettingManifests(manifest_insertions)) return false;

            ShieldedUnshieldVelocity target_velocity;
            std::string velocity_error;
            if (target != nullptr &&
                !RebuildRecentShieldedUnshieldVelocity(*m_active_chainstate,
                                                       target,
                                                       GetConsensus(),
                                                       target_velocity,
                                                       velocity_error)) {
                LogPrintf("EnsureShieldedStateInitialized: prepared transition could not rebuild recent unshield velocity at target height=%d hash=%s (%s)\n",
                          target->nHeight, target->GetBlockHash().ToString(), velocity_error);
                return false;
            }
            if (!m_shielded_nullifiers->WriteUnshieldVelocity(target_velocity) ||
                !m_shielded_nullifiers->WritePoolBalance(
                    marker.prepared_target_snapshot.pool_balance)) {
                return false;
            }
            m_shielded_unshield_velocity = std::move(target_velocity);
            return true;
        };

    auto try_restore_prepared_transition =
        [&](const ShieldedStateMutationMarker& marker) EXCLUSIVE_LOCKS_REQUIRED(::cs_main) -> bool {
            AssertLockHeld(::cs_main);
            if (!marker.IsPreparedTransitionJournal()) {
                return false;
            }
            const uint256 active_tip_hash = tip ? tip->GetBlockHash() : uint256{};
            const int32_t active_tip_height = tip ? tip->nHeight : -1;
            if (marker.target_tip_hash != active_tip_hash ||
                marker.target_tip_height != active_tip_height) {
                return false;
            }

            auto restored_registry =
                shielded::registry::ShieldedAccountRegistryState::RestorePersisted(
                    marker.prepared_target_snapshot.account_registry_snapshot);
            if (!restored_registry.has_value()) {
                LogPrintf("EnsureShieldedStateInitialized: prepared mutation marker carried invalid account registry snapshot; rebuilding full state from chain\n");
                return false;
            }
            if (!restored_registry->CommitJournaledPayloads(
                    marker.prepared_target_snapshot.journaled_account_payloads)) {
                LogPrintf("EnsureShieldedStateInitialized: prepared mutation marker account payload redo failed; retaining PREPARED for retry\n");
                return false;
            }
            if (!redo_prepared_transition_stores(marker)) {
                LogPrintf("EnsureShieldedStateInitialized: prepared mutation marker could not deterministically redo target stores; rebuilding full state from chain\n");
                return false;
            }
            if (!restored_registry->CanMaterializeAllEntries()) {
                LogPrintf("EnsureShieldedStateInitialized: prepared mutation marker account registry payload store is incomplete; rebuilding full state from chain\n");
                return false;
            }

            m_shielded_merkle_tree = marker.prepared_target_snapshot.tree;
            if (!m_shielded_merkle_tree.HasCommitmentIndex() &&
                !m_shielded_merkle_tree.AttachConfiguredCommitmentIndexStore()) {
                LogPrintf("EnsureShieldedStateInitialized: prepared mutation marker could not attach the commitment index store\n");
                return false;
            }
            const auto restored_commitment_digest = m_shielded_merkle_tree.CommitmentIndexDigest();
            if (!restored_commitment_digest.has_value() ||
                *restored_commitment_digest != marker.prepared_target_snapshot.commitment_index_digest) {
                LogPrintf("EnsureShieldedStateInitialized: prepared mutation marker commitment digest mismatch at height=%d hash=%s expected=%s actual=%s; rebuilding full state from chain\n",
                          active_tip_height,
                          active_tip_hash.ToString(),
                          marker.prepared_target_snapshot.commitment_index_digest.ToString(),
                          restored_commitment_digest.has_value() ? restored_commitment_digest->ToString() : "missing");
                return false;
            }
            if (!m_shielded_pool_balance.SetBalance(marker.prepared_target_snapshot.pool_balance)) {
                return false;
            }
            m_shielded_account_registry = std::move(*restored_registry);
            m_shielded_smile_public_accounts.clear();
            m_shielded_account_leaf_commitments.clear();
            if (!shielded::registry::BuildRegistryAccountState(
                    m_shielded_account_registry,
                    m_shielded_smile_public_accounts,
                    m_shielded_account_leaf_commitments)) {
                return false;
            }
            InvalidateShieldedAccountStateSnapshotCaches();
            m_shielded_anchor_roots.clear();
            m_shielded_account_registry_roots.clear();

            if (tip != nullptr &&
                UseAccountRegistryEntryCountLimit(GetConsensus(), tip->nHeight) &&
                m_shielded_account_registry.Size() > GetConsensus().nMaxShieldedAccountRegistryEntries) {
                return false;
            }
            m_shielded_state_initialized = true;
            if (!RebuildShieldedAnchorHistory(*m_active_chainstate, tip)) {
                m_shielded_state_initialized = false;
                return false;
            }
            if (ShieldedRebuildHistoryBlocksAvailable(*m_active_chainstate, tip)) {
                if (!RebuildShieldedAccountRegistryHistory(*m_active_chainstate, tip)) {
                    m_shielded_state_initialized = false;
                    return false;
                }
            } else {
                LogPrintf("EnsureShieldedStateInitialized: prepared mutation marker cannot restore account-registry root history without recent blocks at height=%d hash=%s\n",
                          tip != nullptr ? tip->nHeight : -1,
                          tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                m_shielded_state_initialized = false;
                return false;
            }
            if (!load_persisted_unshield_velocity() ||
                !verify_or_repair_startup_velocity()) {
                m_shielded_state_initialized = false;
                return false;
            }
            // The target structures came from the already validated block
            // transition and every auxiliary store was just idempotently
            // replayed from the same block/undo path. A genesis audit here
            // defeats the purpose of the journal and turned a single local
            // write interruption into a multi-hour outage.
            if (!PersistShieldedState(tip)) {
                m_shielded_state_initialized = false;
                return false;
            }
            startup_shielded_repair_performed = true;
            LogPrintf("EnsureShieldedStateInitialized: finalized prepared shielded transition at height=%d hash=%s tree_size=%u root=%s\n",
                      active_tip_height,
                      active_tip_hash.ToString(),
                      static_cast<unsigned int>(m_shielded_merkle_tree.Size()),
                      m_shielded_merkle_tree.Root().ToString());
            return true;
        };

    if (persisted_mutation_marker.has_value()) {
        if (try_restore_prepared_transition(*persisted_mutation_marker)) {
            return finish_success();
        }
        // §11 / production restart tax: a clean stop (or a ConnectBlock that
        // already PersistShieldedState'd the tip) can leave a stale marker whose
        // target already matches the active tip AND the durable tip snapshot.
        // Falling through to rebuild_from_chain then costs ~1–2h of archive IO
        // even though there is nothing to repair. If the tip-matched snapshot
        // still carries a verifying full-state pin (and nullifier accumulator),
        // clear the stale marker and continue into the ordinary restore path.
        //
        // Intentionally require pin+accumulator evidence: a LEGACY marker on a
        // tip-matched snapshot WITHOUT a pin still means "unknown prefix of a
        // multi-store mutation may have committed" and must fail closed / rebuild
        // (see chainstatemanager_rejects_stale_legacy_marker_when_pruned).
        if (tip != nullptr &&
            persisted_mutation_marker->target_tip_height == tip->nHeight &&
            persisted_mutation_marker->target_tip_hash == tip->GetBlockHash()) {
            shielded::ShieldedMerkleTree stale_marker_tree;
            std::vector<uint256> stale_marker_anchor_roots;
            uint256 stale_marker_tip_hash;
            int32_t stale_marker_tip_height{-1};
            CAmount stale_marker_balance{0};
            std::optional<uint256> stale_marker_commitment_digest;
            std::optional<shielded::registry::ShieldedAccountRegistryPersistedSnapshot>
                stale_marker_registry_snapshot;
            if (m_shielded_nullifiers->ReadPersistedState(
                    stale_marker_tree,
                    stale_marker_anchor_roots,
                    stale_marker_tip_hash,
                    stale_marker_tip_height,
                    stale_marker_balance,
                    stale_marker_commitment_digest,
                    stale_marker_registry_snapshot) &&
                stale_marker_tip_hash == tip->GetBlockHash() &&
                stale_marker_tip_height == tip->nHeight) {
                const auto persisted_accumulator =
                    m_shielded_nullifiers->ReadPersistedNullifierAccumulator();
                const auto persisted_pin =
                    m_shielded_nullifiers->ReadPersistedShieldedStatePin();
                // Load the frontier into the live managers just long enough to
                // recompute the pin against the durable tip snapshot. This does
                // not publish HasShieldedState() until finish_success / restore.
                // Pin V3 covers unshield velocity, so load it before hashing.
                m_shielded_merkle_tree = stale_marker_tree;
                if (m_shielded_pool_balance.SetBalance(stale_marker_balance) &&
                    load_persisted_unshield_velocity() &&
                    stale_marker_registry_snapshot.has_value()) {
                    auto restored_registry =
                        shielded::registry::ShieldedAccountRegistryState::RestorePersisted(
                            *stale_marker_registry_snapshot);
                    if (restored_registry.has_value() &&
                        restored_registry->CanMaterializeAllEntries()) {
                        m_shielded_account_registry = std::move(*restored_registry);
                        const auto current_pin = ComputeShieldedSnapshotStatePin();
                        const bool tip_matched_credential_ok =
                            persisted_accumulator.has_value() &&
                            *persisted_accumulator ==
                                m_shielded_nullifiers->NullifierAccumulatorDigest() &&
                            persisted_pin.has_value() &&
                            current_pin.has_value() &&
                            *persisted_pin == *current_pin;
                        // Only PREPARED journals are eligible: a LEGACY marker on a
                        // tip-matched snapshot still means an unknown multi-store
                        // mutation prefix and must rebuild / fail closed when pruned.
                        if (tip_matched_credential_ok &&
                            persisted_mutation_marker->IsPreparedTransitionJournal() &&
                            m_shielded_nullifiers->ClearMutationMarker()) {
                            LogPrintf(
                                "EnsureShieldedStateInitialized: clearing stale tip-matched "
                                "PREPARED mutation marker at height=%d hash=%s; durable "
                                "snapshot already matches the active tip with verifying "
                                "pin+accumulator (skipping from-genesis rebuild)\n",
                                tip->nHeight,
                                tip->GetBlockHash().ToString());
                            // Drop the temporary live loads; the tip-matched restore
                            // path below re-reads and validates the same snapshot.
                            reset_shielded_state();
                            persisted_mutation_marker.reset();
                        } else {
                            reset_shielded_state();
                        }
                    } else {
                        reset_shielded_state();
                    }
                } else {
                    reset_shielded_state();
                }
            }
        }
        if (persisted_mutation_marker.has_value()) {
            // A legacy marker only says that some unknown prefix of a multi-store
            // mutation may have committed. A PREPARED marker additionally pins the
            // target tree/pool/registry, but does not yet journal target nullifier,
            // bridge, or velocity state. If the exact prepared target could not be
            // finalized above and the historical blocks needed for a full rebuild
            // are pruned, neither source nor target state is provable. Never clear
            // the marker and continue on a potentially mixed view. Fail closed and
            // retain all recovery evidence for reindex/snapshot repair.
            if (!ShieldedFullRebuildBlocksAvailable(*m_active_chainstate, tip)) {
                LogError("EnsureShieldedStateInitialized: in-flight %s mutation marker target_height=%d "
                         "target_hash=%s cannot be proven/finalized because blocks required for a full "
                         "shielded rebuild are pruned at tip height=%d hash=%s. Refusing to clear the "
                         "marker or expose mixed shielded state. Recover with -reindex on an un-pruned "
                         "node or load a verified archival shielded snapshot.\n",
                          persisted_mutation_marker->IsPreparedTransitionJournal()
                              ? "PREPARED" : "legacy",
                          persisted_mutation_marker->target_tip_height,
                          persisted_mutation_marker->target_tip_hash.ToString(),
                          tip ? tip->nHeight : -1,
                          tip ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                return false;
            } else {
                LogPrintf("EnsureShieldedStateInitialized: found in-flight mutation marker target_height=%d target_hash=%s; rebuilding full shielded state from chain\n",
                          persisted_mutation_marker->target_tip_height,
                          persisted_mutation_marker->target_tip_hash.ToString());
                if (!rebuild_from_chain()) {
                    return false;
                }
                return finish_success();
            }
        }
    }

    LogPrintf("EnsureShieldedStateInitialized: rebuilding from tip height=%d hash=%s retention=%s\n",
              tip ? tip->nHeight : -1,
              tip ? tip->GetBlockHash().ToString() : uint256{}.ToString(),
              m_options.retain_shielded_commitment_index ? "full" : "externalized");
    shielded::ShieldedMerkleTree persisted_tree;
    std::vector<uint256> persisted_anchor_roots;
    uint256 persisted_tip_hash;
    int32_t persisted_tip_height{-1};
    CAmount persisted_balance{0};
    std::optional<uint256> persisted_commitment_index_digest;
    std::optional<shielded::registry::ShieldedAccountRegistryPersistedSnapshot>
        persisted_account_registry_snapshot;
    std::vector<uint256> persisted_account_registry_roots;
    const bool preserve_snapshot_bridge_metadata_extras =
        m_shielded_nullifiers->ReadSnapshotBridgeMetadataHint();
    if (tip != nullptr &&
        m_shielded_nullifiers->ReadPersistedState(
            persisted_tree,
            persisted_anchor_roots,
            persisted_tip_hash,
            persisted_tip_height,
            persisted_balance,
            persisted_commitment_index_digest,
            persisted_account_registry_snapshot,
            &persisted_account_registry_roots) &&
        persisted_tip_hash == tip->GetBlockHash() &&
        persisted_tip_height == tip->nHeight) {
        if (!m_shielded_pool_balance.SetBalance(persisted_balance)) {
            return false;
        }
        m_shielded_merkle_tree = std::move(persisted_tree);
        m_shielded_anchor_roots.clear();
        for (const auto& root : persisted_anchor_roots) {
            m_shielded_anchor_roots.push_back(root);
        }
        if (m_shielded_anchor_roots.empty()) {
            m_shielded_anchor_roots.push_front(m_shielded_merkle_tree.Root());
        }
        if (!load_persisted_unshield_velocity()) {
            return false;
        }
        if (!verify_or_repair_startup_velocity()) {
            if (ShieldedFullRebuildBlocksAvailable(*m_active_chainstate, tip)) {
                LogPrintf("EnsureShieldedStateInitialized: rebuilding full shielded state from chain after unshield velocity verification failed\n");
                reset_shielded_state();
                if (!rebuild_from_chain()) {
                    return false;
                }
                return finish_success();
            }
            LogPrintf("EnsureShieldedStateInitialized: refusing persisted shielded state at height=%d hash=%s because unshield velocity verification failed and full rebuild blocks are unavailable\n",
                      tip != nullptr ? tip->nHeight : -1,
                      tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
            return false;
        }
        auto restore_rebuilt_commitment_index = [&]() EXCLUSIVE_LOCKS_REQUIRED(::cs_main) -> bool {
            AssertLockHeld(::cs_main);
            shielded::ShieldedMerkleTree rebuilt_tree;
            ShieldedPoolBalance rebuilt_pool;
            if (!RebuildShieldedState(*m_active_chainstate, tip, rebuilt_tree, rebuilt_pool)) {
                return false;
            }
            if (rebuilt_tree.Size() != m_shielded_merkle_tree.Size() ||
                rebuilt_tree.Root() != m_shielded_merkle_tree.Root() ||
                rebuilt_pool.GetBalance() != m_shielded_pool_balance.GetBalance()) {
                LogPrintf("EnsureShieldedStateInitialized: persisted frontier mismatch at height=%d hash=%s persisted_root=%s rebuilt_root=%s persisted_size=%u rebuilt_size=%u persisted_pool=%lld rebuilt_pool=%lld; rebuilding full state from chain\n",
                          persisted_tip_height,
                          persisted_tip_hash.ToString(),
                          m_shielded_merkle_tree.Root().ToString(),
                          rebuilt_tree.Root().ToString(),
                          static_cast<unsigned int>(m_shielded_merkle_tree.Size()),
                          static_cast<unsigned int>(rebuilt_tree.Size()),
                          static_cast<long long>(m_shielded_pool_balance.GetBalance()),
                          static_cast<long long>(rebuilt_pool.GetBalance()));
                return false;
            }
            m_shielded_merkle_tree = std::move(rebuilt_tree);
            if (!RebuildShieldedAnchorHistory(*m_active_chainstate, tip)) {
                return false;
            }
            if (!PersistShieldedState(tip)) {
                return false;
            }
            startup_shielded_repair_performed = true;
            return true;
        };

        bool restored_index{false};
        const auto actual_commitment_index_digest = m_shielded_merkle_tree.CommitmentIndexDigest();
        if (persisted_commitment_index_digest.has_value() &&
            actual_commitment_index_digest.has_value() &&
            *persisted_commitment_index_digest == *actual_commitment_index_digest) {
            restored_index = true;
        } else if (!ShieldedFullRebuildBlocksAvailable(*m_active_chainstate, tip)) {
            if (!actual_commitment_index_digest.has_value()) {
                // Frontier-only externalized retention is safe under pruning: shielded spend
                // verification that needs a leaf-position lookup fails closed while the index
                // is absent. A present-but-drifted index is not safe, and is refused below.
                LogPrintf("EnsureShieldedStateInitialized: commitment index rebuild needs pruned blocks at height=%d hash=%s; retaining frontier-only shielded tree with no commitment index\n",
                          persisted_tip_height,
                          persisted_tip_hash.ToString());
                restored_index = true;
            } else {
                LogPrintf("EnsureShieldedStateInitialized: refusing persisted commitment index at height=%d hash=%s because digest verification failed and rebuild needs pruned blocks (expected=%s actual=%s)\n",
                          persisted_tip_height,
                          persisted_tip_hash.ToString(),
                          persisted_commitment_index_digest.has_value() ? persisted_commitment_index_digest->ToString() : "missing",
                          actual_commitment_index_digest->ToString());
                return false;
            }
        } else {
            if (!persisted_commitment_index_digest.has_value()) {
                LogPrintf("EnsureShieldedStateInitialized: persisted state missing commitment index digest at height=%d hash=%s tree_size=%u root=%s; rebuilding commitment index from chain\n",
                          persisted_tip_height,
                          persisted_tip_hash.ToString(),
                          static_cast<unsigned int>(m_shielded_merkle_tree.Size()),
                          m_shielded_merkle_tree.Root().ToString());
            } else {
                LogPrintf("EnsureShieldedStateInitialized: commitment index digest mismatch at height=%d hash=%s expected=%s actual=%s tree_size=%u root=%s; rebuilding commitment index from chain\n",
                          persisted_tip_height,
                          persisted_tip_hash.ToString(),
                          persisted_commitment_index_digest->ToString(),
                          actual_commitment_index_digest.has_value() ? actual_commitment_index_digest->ToString() : "missing",
                          static_cast<unsigned int>(m_shielded_merkle_tree.Size()),
                          m_shielded_merkle_tree.Root().ToString());
            }
            restored_index = restore_rebuilt_commitment_index();
        }

        // issue #35: if the commitment index is not retained (for example when
        // -retainshieldedcommitmentindex=0), a tree loaded from its persisted frontier has no in-memory
        // commitment index, and the index cannot be reconstructed without reading (pruned) blocks.
        // Still enter the recovery block in that case so the node comes up on its validated persisted
        // frontier. This is consensus-safe:
        // a frontier-only node is a correct VALIDATOR -- every shielded-spend verification that needs
        // a leaf-position lookup fails closed (CommitmentAt -> nullopt -> bad-shielded-ring-member-
        // position) when the index is absent; it simply cannot itself PRODUCE witnesses until it can
        // rebuild the index from un-pruned blocks.
        const bool commitment_index_present = m_shielded_merkle_tree.HasCommitmentIndex();
        const bool commitment_index_unrebuildable_under_pruning =
            !commitment_index_present &&
            !ShieldedFullRebuildBlocksAvailable(*m_active_chainstate, tip);
        if (restored_index &&
            (commitment_index_present || commitment_index_unrebuildable_under_pruning)) {
            const auto loaded_anchor_roots = m_shielded_anchor_roots;
            const auto loaded_account_registry_roots = persisted_account_registry_roots;
            bool anchor_history_rebuilt{false};
            bool account_registry_rebuilt_from_chain{false};
            bool fast_startup_nullifier_accumulator_checked{false};
            bool fast_startup_nullifier_accumulator_verified{false};
            auto verify_fast_startup_nullifier_accumulator =
                [&]() EXCLUSIVE_LOCKS_REQUIRED(::cs_main) -> bool {
                    AssertLockHeld(::cs_main);
                    if (fast_startup_nullifier_accumulator_checked) {
                        return fast_startup_nullifier_accumulator_verified;
                    }
                    fast_startup_nullifier_accumulator_checked = true;
                    const auto persisted_accumulator =
                        m_shielded_nullifiers->ReadPersistedNullifierAccumulator();
                    const uint256 current_accumulator =
                        m_shielded_nullifiers->NullifierAccumulatorDigest();
                    fast_startup_nullifier_accumulator_verified =
                        persisted_accumulator.has_value() &&
                        *persisted_accumulator == current_accumulator;
                    return fast_startup_nullifier_accumulator_verified;
                };
            bool fast_startup_state_pin_checked{false};
            bool fast_startup_state_pin_verified{false};
            bool fast_startup_state_pin_present{false};
            bool fast_startup_state_pin_current_available{false};
            auto verify_fast_startup_state_pin =
                [&]() EXCLUSIVE_LOCKS_REQUIRED(::cs_main) -> bool {
                    AssertLockHeld(::cs_main);
                    if (fast_startup_state_pin_checked) {
                        return fast_startup_state_pin_verified;
                    }
                    fast_startup_state_pin_checked = true;
                    const auto persisted_pin =
                        m_shielded_nullifiers->ReadPersistedShieldedStatePin();
                    const auto current_pin = ComputeShieldedSnapshotStatePin();
                    fast_startup_state_pin_present = persisted_pin.has_value();
                    fast_startup_state_pin_current_available = current_pin.has_value();
                    fast_startup_state_pin_verified =
                        persisted_pin.has_value() &&
                        current_pin.has_value() &&
                        *persisted_pin == *current_pin;
                    if (!fast_startup_state_pin_verified &&
                        persisted_pin.has_value() &&
                        current_pin.has_value() &&
                        startup_velocity_checked &&
                        startup_velocity_verified_or_inactive &&
                        !startup_velocity_rebuild_deferred_to_state_pin) {
                        const auto legacy_v2_pin = ComputeShieldedSnapshotStatePinV2ForMigration();
                        const auto empty_velocity_v3_pin =
                            ComputeShieldedSnapshotStatePinEmptyVelocityV3ForMigration();
                        const bool matched_legacy_v2 =
                            legacy_v2_pin.has_value() && *persisted_pin == *legacy_v2_pin;
                        const bool matched_empty_velocity_v3 =
                            empty_velocity_v3_pin.has_value() &&
                            *persisted_pin == *empty_velocity_v3_pin;
                        if (matched_legacy_v2 || matched_empty_velocity_v3) {
                            LogPrintf("EnsureShieldedStateInitialized: migrating persisted %s shielded state pin at height=%d hash=%s after verifying unshield velocity\n",
                                      matched_legacy_v2 ? "legacy-v2" : "empty-velocity-v3",
                                      tip != nullptr ? tip->nHeight : -1,
                                      tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                            if (!PersistShieldedState(tip)) {
                                return false;
                            }
                            fast_startup_state_pin_verified = true;
                        }
                    }
                    return fast_startup_state_pin_verified;
                };
            auto build_account_registry_views =
                [&]() EXCLUSIVE_LOCKS_REQUIRED(::cs_main) -> bool {
                    AssertLockHeld(::cs_main);
                    if (!shielded::registry::BuildRegistryAccountState(
                            m_shielded_account_registry,
                            m_shielded_smile_public_accounts,
                            m_shielded_account_leaf_commitments)) {
                        return false;
                    }
                    InvalidateShieldedAccountStateSnapshotCaches();
                    return true;
                };
            auto rebuild_account_registry_from_chain =
                [&](const char* reason) EXCLUSIVE_LOCKS_REQUIRED(::cs_main) -> bool {
                    AssertLockHeld(::cs_main);
                    LogPrintf("EnsureShieldedStateInitialized: %s at height=%d hash=%s; rebuilding account registry from chain\n",
                              reason,
                              persisted_tip_height,
                              persisted_tip_hash.ToString());
                    shielded::registry::ShieldedAccountRegistryState::ResetPayloadStore();
                    m_shielded_account_registry = shielded::registry::ShieldedAccountRegistryState::WithConfiguredPayloadStore();
                    if (!PrepareShieldedAccountRegistryPayloadStore(account_registry_db_path,
                                                                    /*wipe_data=*/true)) {
                        return false;
                    }
                    m_shielded_account_registry = shielded::registry::ShieldedAccountRegistryState::WithConfiguredPayloadStore();
                    if (!RebuildShieldedAccountRegistryState(*m_active_chainstate,
                                                             tip,
                                                             m_shielded_account_registry)) {
                        return false;
                    }
                    if (!build_account_registry_views()) {
                        return false;
                    }
                    startup_shielded_repair_performed = true;
                    account_registry_rebuilt_from_chain = true;
                    return true;
                };
            if (ShieldedRebuildHistoryBlocksAvailable(*m_active_chainstate, tip)) {
                if (!RebuildShieldedAnchorHistory(*m_active_chainstate, tip)) {
                    LogPrintf("EnsureShieldedStateInitialized: failed to rebuild recent shielded anchor history at height=%d hash=%s; rebuilding full state from chain\n",
                              persisted_tip_height,
                              persisted_tip_hash.ToString());
                    reset_shielded_state();
                    if (!rebuild_from_chain()) {
                        return false;
                    }
                    return finish_success();
                }
                if (loaded_anchor_roots != m_shielded_anchor_roots) {
                    LogPrintf("EnsureShieldedStateInitialized: rebuilt shielded anchor history at height=%d hash=%s replacing %u persisted roots with %u chain-derived roots\n",
                              persisted_tip_height,
                              persisted_tip_hash.ToString(),
                              static_cast<unsigned int>(loaded_anchor_roots.size()),
                              static_cast<unsigned int>(m_shielded_anchor_roots.size()));
                    // History-window refresh only — do not set startup_shielded_repair_performed.
                    startup_history_window_refreshed = true;
                    anchor_history_rebuilt = true;
                }
            } else {
                LogPrintf("EnsureShieldedStateInitialized: snapshot anchor history blocks unavailable at height=%d hash=%s; retaining persisted anchor roots\n",
                          tip != nullptr ? tip->nHeight : -1,
                          tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
            }
            if (persisted_account_registry_snapshot.has_value()) {
                auto restored_registry =
                    shielded::registry::ShieldedAccountRegistryState::RestorePersisted(
                        *persisted_account_registry_snapshot);
                if (!restored_registry.has_value()) {
                    if (!rebuild_account_registry_from_chain(
                            "invalid persisted account registry snapshot")) {
                        return false;
                    }
                } else if (!restored_registry->CanMaterializeAllEntries()) {
                    // Release the stale payload-store handle before wiping and
                    // rebuilding the on-disk registry.
                    restored_registry.reset();
                    if (!rebuild_account_registry_from_chain(
                            "persisted account registry payload store is incomplete")) {
                        return false;
                    }
                } else {
                    m_shielded_account_registry = std::move(*restored_registry);
                    if (!build_account_registry_views()) {
                        if (!rebuild_account_registry_from_chain(
                                "persisted account registry views could not be restored")) {
                            return false;
                        }
                    }
                }
            } else {
                if (!rebuild_account_registry_from_chain(
                        "persisted state missing account registry snapshot")) {
                    return false;
                }
            }
            // Hash the pin only after the persisted registry is live. The V3 pin
            // covers account_registry_root; computing it on the empty default
            // registry (the previous order) mismatches any non-empty archive
            // snapshot, caches the failure, and forces a multi-hour recovery-exit
            // audit. Capture before history-window rewrite/persist so a later
            // refresh cannot mint a fast-start credential for repaired state.
            const bool pre_repair_state_pin_verified{
                verify_fast_startup_state_pin()};
            bool account_registry_roots_changed{false};
            if (ShieldedRebuildHistoryBlocksAvailable(*m_active_chainstate, tip)) {
                if (!RebuildShieldedAccountRegistryHistory(*m_active_chainstate, tip)) {
                    if (!rebuild_account_registry_from_chain(
                            "persisted account registry history mismatch") ||
                        !RebuildShieldedAccountRegistryHistory(*m_active_chainstate, tip)) {
                        return false;
                    }
                }
                std::vector<uint256> rebuilt_account_registry_roots;
                rebuilt_account_registry_roots.reserve(m_shielded_account_registry_roots.size());
                for (const auto& root : m_shielded_account_registry_roots) {
                    rebuilt_account_registry_roots.push_back(root);
                }
                account_registry_roots_changed =
                    loaded_account_registry_roots != rebuilt_account_registry_roots;
                if (account_registry_roots_changed) {
                    LogPrintf("EnsureShieldedStateInitialized: rebuilt account-registry history at height=%d hash=%s replacing %u persisted roots with %u chain-derived roots\n",
                              persisted_tip_height,
                              persisted_tip_hash.ToString(),
                              static_cast<unsigned int>(loaded_account_registry_roots.size()),
                              static_cast<unsigned int>(rebuilt_account_registry_roots.size()));
                    // History-window refresh only — do not set startup_shielded_repair_performed.
                    startup_history_window_refreshed = true;
                }
                if (account_registry_rebuilt_from_chain || account_registry_roots_changed) {
                    if (!PersistShieldedState(
                            tip,
                            /*fast_startup_trusted=*/
                                pre_repair_state_pin_verified &&
                                !account_registry_rebuilt_from_chain)) {
                        return false;
                    }
                }
            } else {
                const size_t expected_account_registry_roots =
                    ExpectedShieldedRecentHistoryCount(tip) + 1U;
                if (!LoadShieldedAccountRegistryRootWindow(
                        persisted_account_registry_roots,
                        m_shielded_account_registry.Root(),
                        expected_account_registry_roots,
                        "EnsureShieldedStateInitialized",
                        m_shielded_account_registry_roots)) {
                    LogPrintf("EnsureShieldedStateInitialized: snapshot account-registry history blocks unavailable and persisted root window is incomplete at height=%d hash=%s\n",
                              tip != nullptr ? tip->nHeight : -1,
                              tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                    return false;
                }
                LogPrintf("EnsureShieldedStateInitialized: snapshot account-registry history blocks unavailable at height=%d hash=%s; retaining %u persisted account-registry roots\n",
                          tip != nullptr ? tip->nHeight : -1,
                          tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString(),
                          static_cast<unsigned int>(m_shielded_account_registry_roots.size()));
            }
            if (anchor_history_rebuilt &&
                !account_registry_rebuilt_from_chain &&
                !account_registry_roots_changed &&
                !PersistShieldedState(
                    tip,
                    /*fast_startup_trusted=*/pre_repair_state_pin_verified)) {
                return false;
            }
            const bool nullifier_accumulator_verified =
                verify_fast_startup_nullifier_accumulator();
            if (!nullifier_accumulator_verified) {
                const bool persisted_accumulator_present =
                    m_shielded_nullifiers->ReadPersistedNullifierAccumulator().has_value();
                LogPrintf("EnsureShieldedStateInitialized: nullifier accumulator %s at height=%d hash=%s; persisted shielded state may be drifted or tampered\n",
                          persisted_accumulator_present ? "mismatch" : "absent",
                          tip != nullptr ? tip->nHeight : -1,
                          tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                if (ShieldedFullRebuildBlocksAvailable(*m_active_chainstate, tip)) {
                    LogPrintf("EnsureShieldedStateInitialized: rebuilding full shielded state from chain after nullifier accumulator verification failed\n");
                    reset_shielded_state();
                    if (!rebuild_from_chain()) {
                        return false;
                    }
                    return finish_success();
                }
                LogPrintf("EnsureShieldedStateInitialized: refusing to restore persisted shielded state because nullifier accumulator verification failed and full rebuild blocks are unavailable\n");
                return false;
            }
            const bool recovery_exit_active_at_tip =
                tip != nullptr && GetConsensus().IsShieldedRecoveryExitActive(tip->nHeight);
            const bool full_state_pin_verified =
                startup_velocity_verified_or_inactive &&
                verify_fast_startup_state_pin();
            const bool recovery_exit_state_pin_verified =
                recovery_exit_active_at_tip &&
                full_state_pin_verified;
            const bool preserve_persisted_bridge_metadata =
                m_options.fast_shielded_startup &&
                !startup_shielded_repair_performed &&
                !preserve_snapshot_bridge_metadata_extras &&
                nullifier_accumulator_verified &&
                full_state_pin_verified;
            if (preserve_persisted_bridge_metadata) {
                if (startup_history_window_refreshed) {
                    LogPrintf("EnsureShieldedStateInitialized: refreshed recent shielded history windows at height=%d hash=%s without disabling -fastshieldedstartup (history refresh is not a substantive shielded-state repair)\n",
                              tip != nullptr ? tip->nHeight : -1,
                              tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                }
                if (recovery_exit_active_at_tip) {
                    LogPrintf("EnsureShieldedStateInitialized: preserving persisted settlement-anchor and netting-manifest metadata at height=%d hash=%s because -fastshieldedstartup=1, persisted nullifier accumulator verified, and full shielded state pin verified\n",
                              tip != nullptr ? tip->nHeight : -1,
                              tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                } else {
                    LogPrintf("EnsureShieldedStateInitialized: preserving persisted settlement-anchor and netting-manifest metadata at height=%d hash=%s because -fastshieldedstartup=1, persisted nullifier accumulator verified, and full shielded state pin verified\n",
                              tip != nullptr ? tip->nHeight : -1,
                              tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                }
            } else if (ShieldedFullRebuildBlocksAvailable(*m_active_chainstate, tip)) {
                if (preserve_snapshot_bridge_metadata_extras) {
                    LogPrintf("EnsureShieldedStateInitialized: full shielded rebuild blocks available at height=%d hash=%s; converging snapshot bridge metadata to chain state\n",
                              tip != nullptr ? tip->nHeight : -1,
                              tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                } else if (startup_shielded_repair_performed) {
                    LogPrintf("EnsureShieldedStateInitialized: converging settlement-anchor and netting-manifest metadata from chain after substantive shielded-state repair at height=%d hash=%s (full-chain scan)\n",
                              tip != nullptr ? tip->nHeight : -1,
                              tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                } else if (!m_options.fast_shielded_startup) {
                    LogPrintf("EnsureShieldedStateInitialized: converging settlement-anchor and netting-manifest metadata from chain because -fastshieldedstartup=0 at height=%d hash=%s (full-chain scan)\n",
                              tip != nullptr ? tip->nHeight : -1,
                              tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                }
                GetNotifications().progress(_("Rebuilding shielded bridge metadata…"), 0, false);
                if (!SyncShieldedBridgeMetadataState(*m_active_chainstate,
                                                     tip,
                                                     *m_shielded_nullifiers,
                                                     PersistedShieldedMetadataSyncMode::STRICT_CHAIN_CONVERGENCE,
                                                     &GetNotifications())) {
                    LogPrintf("EnsureShieldedStateInitialized: failed to sync persisted settlement-anchor/netting metadata; rebuilding full state from chain\n");
                    reset_shielded_state();
                    if (!rebuild_from_chain()) {
                        return false;
                    }
                    return finish_success();
                }
                if (preserve_snapshot_bridge_metadata_extras &&
                    !m_shielded_nullifiers->WriteSnapshotBridgeMetadataHint(false)) {
                    LogPrintf("EnsureShieldedStateInitialized: failed to clear snapshot bridge metadata hint after strict chain convergence\n");
                    return false;
                }
            } else {
                if (preserve_snapshot_bridge_metadata_extras) {
                    LogPrintf("EnsureShieldedStateInitialized: refusing to restore snapshot-seeded settlement-anchor/netting metadata at height=%d hash=%s because full shielded rebuild blocks are unavailable\n",
                              tip != nullptr ? tip->nHeight : -1,
                              tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                    return false;
                }
                LogPrintf("EnsureShieldedStateInitialized: full shielded rebuild blocks unavailable at height=%d hash=%s; retaining persisted settlement-anchor and netting-manifest state\n",
                          tip != nullptr ? tip->nHeight : -1,
                          tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
            }
            m_shielded_state_initialized = true;

            // Zero-downtime restart: skip the expensive cross-chain audit (the genesis re-derivation in
            // RebuildShieldedState) when persisted state has enough cheap restart evidence. After
            // A nullifier accumulator alone is never enough: note-tree, account-registry, pool,
            // velocity, settlement, and netting state can drift independently while leaving that
            // accumulator unchanged. The full shielded state pin covers all of those structures, so a
            // matching pin is the fast-path evidence for every clean restart. Missing or mismatched
            // pins still force the chain-derived audit or fail closed under pruning.
            const bool fast_audit_skip_eligible =
                m_options.fast_shielded_startup &&
                !startup_shielded_repair_performed;
            if (!full_state_pin_verified) {
                LogPrintf("EnsureShieldedStateInitialized: full shielded state pin %s at height=%d hash=%s tree_size=%u registry_entries=%u; cross-chain audit is required\n",
                          !fast_startup_state_pin_present ? "absent" :
                          !fast_startup_state_pin_current_available ? "unavailable" : "mismatch",
                          tip != nullptr ? tip->nHeight : -1,
                          tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString(),
                          static_cast<unsigned int>(m_shielded_merkle_tree.Size()),
                          static_cast<unsigned int>(m_shielded_account_registry.Size()));
            }
            std::string audit_error;
            const bool full_rebuild_blocks_available =
                ShieldedFullRebuildBlocksAvailable(*m_active_chainstate, tip);
            if (recovery_exit_active_at_tip) {
                if (fast_audit_skip_eligible && recovery_exit_state_pin_verified) {
                    LogPrintf("EnsureShieldedStateInitialized: -fastshieldedstartup verified persisted full shielded state pin at height=%d hash=%s after recovery-exit activation; skipping cross-chain audit\n",
                              tip != nullptr ? tip->nHeight : -1,
                              tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                } else {
                    if (!full_rebuild_blocks_available) {
                        LogPrintf("EnsureShieldedStateInitialized: refusing persisted shielded state at height=%d hash=%s because recovery-exit state requires chain-derived audit and full audit blocks are unavailable\n",
                                  tip != nullptr ? tip->nHeight : -1,
                                  tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                        return false;
                    }
                    if (!AuditShieldedStateAgainstChain(*m_active_chainstate,
                                                       audit_error,
                                                       /*include_proof_audit=*/false)) {
                        LogPrintf("EnsureShieldedStateInitialized: recovery-exit persisted shielded state audit failed (%s); rebuilding full state from chain\n",
                                  audit_error);
                        reset_shielded_state();
                        if (!rebuild_from_chain()) {
                            return false;
                        }
                        return finish_success();
                    }
                    if (!recovery_exit_state_pin_verified ||
                        startup_shielded_repair_performed ||
                        !preserve_persisted_bridge_metadata) {
                        LogPrintf("EnsureShieldedStateInitialized: recovery-exit cross-chain audit verified persisted shielded state; refreshing full shielded state pin at height=%d hash=%s\n",
                                  tip != nullptr ? tip->nHeight : -1,
                                  tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                        if (!PersistShieldedState(tip)) {
                            return false;
                        }
                    }
                }
            } else if (fast_audit_skip_eligible && full_state_pin_verified) {
                LogPrintf("EnsureShieldedStateInitialized: -fastshieldedstartup verified persisted full shielded state pin at height=%d hash=%s; skipping cross-chain audit for zero-downtime restart\n",
                          tip != nullptr ? tip->nHeight : -1,
                          tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
            } else if (!m_options.shielded_startup_audit) {
                LogPrintf("EnsureShieldedStateInitialized: skipped persisted shielded state startup audit at height=%d hash=%s because -shieldedstartupaudit=0\n",
                          tip != nullptr ? tip->nHeight : -1,
                          tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
            } else if (full_rebuild_blocks_available) {
                if (!AuditShieldedStateAgainstChain(*m_active_chainstate,
                                                   audit_error,
                                                   /*include_proof_audit=*/false)) {
                    LogPrintf("EnsureShieldedStateInitialized: persisted shielded state audit failed (%s); rebuilding full state from chain\n",
                              audit_error);
                    reset_shielded_state();
                    if (!rebuild_from_chain()) {
                        return false;
                    }
                    return finish_success();
                }
                if (!full_state_pin_verified ||
                    startup_shielded_repair_performed ||
                    !preserve_persisted_bridge_metadata) {
                    LogPrintf("EnsureShieldedStateInitialized: cross-chain audit verified persisted shielded state; refreshing fast-start provenance at height=%d hash=%s\n",
                              tip != nullptr ? tip->nHeight : -1,
                              tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                    if (!PersistShieldedState(tip)) {
                        return false;
                    }
                }
            } else if (!full_state_pin_verified) {
                LogPrintf("EnsureShieldedStateInitialized: refusing persisted shielded state at height=%d hash=%s because the full state pin requires a chain-derived audit and full audit blocks are unavailable\n",
                          tip != nullptr ? tip->nHeight : -1,
                          tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
                return false;
            } else {
                // issue #35: the cross-check audit re-derives the full shielded state from genesis
                // (RebuildShieldedState) plus recent-history reads. Under pruning those blocks are
                // gone, so the audit would spuriously fail and force the destructive rebuild below
                // into the same pruned-block read, crash-looping the node. The persisted snapshot
                // reaching this point already had its frontier root/size matched to the active tip,
                // its commitment index digest-verified or retained-under-pruning, and its
                // anchor/registry root windows validated. Skipping a verification that cannot run is
                // fail-closed: the audit can only reject, never accept, so any retained structure
                // still surfaces later as a proof-verification rejection -- never silent acceptance.
                LogPrintf("EnsureShieldedStateInitialized: full shielded rebuild blocks unavailable at height=%d hash=%s; skipping cross-chain audit and retaining validated persisted state\n",
                          tip != nullptr ? tip->nHeight : -1,
                          tip != nullptr ? tip->GetBlockHash().ToString() : uint256{}.ToString());
            }
            LogPrintf("EnsureShieldedStateInitialized: restored persisted state at height=%d hash=%s tree_size=%u root=%s\n",
                      persisted_tip_height,
                      persisted_tip_hash.ToString(),
                      static_cast<unsigned int>(m_shielded_merkle_tree.Size()),
                      m_shielded_merkle_tree.Root().ToString());
            return finish_success();
        }

        LogPrintf("EnsureShieldedStateInitialized: persisted state could not restore commitment index at height=%d hash=%s tree_size=%u root=%s; rebuilding full state from chain\n",
                  persisted_tip_height,
                  persisted_tip_hash.ToString(),
                  static_cast<unsigned int>(m_shielded_merkle_tree.Size()),
                  m_shielded_merkle_tree.Root().ToString());
        m_shielded_merkle_tree = shielded::ShieldedMerkleTree{};
        m_shielded_account_registry = shielded::registry::ShieldedAccountRegistryState{};
        m_shielded_smile_public_accounts.clear();
        m_shielded_account_leaf_commitments.clear();
        InvalidateShieldedAccountStateSnapshotCaches();
        m_shielded_anchor_roots.clear();
        m_shielded_account_registry_roots.clear();
        m_shielded_pool_balance = ShieldedPoolBalance{};
        m_shielded_unshield_velocity.Clear();
        m_shielded_state_initialized = false;
    }

    // Tip mismatch: restore a cryptographically pinned persisted ancestor and
    // project only the missing active-chain suffix. This deliberately uses the
    // same PREPARED redo path as crash recovery, so the gap operation itself is
    // resumable at every durable write boundary.
    if (tip != nullptr &&
        !persisted_tip_hash.IsNull() &&
        persisted_tip_height >= 0 &&
        (persisted_tip_hash != tip->GetBlockHash() || persisted_tip_height != tip->nHeight)) {
        const CBlockIndex* persisted_index = m_blockman.LookupBlockIndex(persisted_tip_hash);
        // Trust the block-index height when pin metadata disagrees (unclean
        // SIGKILL can tear the height field while the hash is still valid).
        if (persisted_index != nullptr &&
            persisted_index->nHeight != persisted_tip_height) {
            LogPrintf("EnsureShieldedStateInitialized: persisted pin height=%d disagrees with index height=%d for hash=%s; using index height\n",
                      persisted_tip_height,
                      persisted_index->nHeight,
                      persisted_tip_hash.ToString());
        }
        if (persisted_index != nullptr &&
            tip->GetAncestor(persisted_index->nHeight) == persisted_index &&
            persisted_index->nHeight < tip->nHeight) {
            const int gap_blocks = tip->nHeight - persisted_index->nHeight;
            LogPrintf("EnsureShieldedStateInitialized: persisted shielded tip height=%d hash=%s is behind active tip height=%d hash=%s; catching up %d block(s) without from-genesis rebuild\n",
                      persisted_index->nHeight,
                      persisted_tip_hash.ToString(),
                      tip->nHeight,
                      tip->GetBlockHash().ToString(),
                      gap_blocks);

            bool base_trusted{false};
            auto restored_registry = persisted_account_registry_snapshot.has_value()
                ? shielded::registry::ShieldedAccountRegistryState::RestorePersisted(
                      *persisted_account_registry_snapshot)
                : std::nullopt;
            if (restored_registry.has_value() &&
                restored_registry->CanMaterializeAllEntries() &&
                m_shielded_pool_balance.SetBalance(persisted_balance)) {
                m_shielded_merkle_tree = persisted_tree;
                const auto actual_digest = m_shielded_merkle_tree.CommitmentIndexDigest();
                const bool digest_matches =
                    persisted_commitment_index_digest.has_value() &&
                    actual_digest.has_value() &&
                    *persisted_commitment_index_digest == *actual_digest;
                if (digest_matches &&
                    (m_shielded_merkle_tree.HasCommitmentIndex() ||
                     m_shielded_merkle_tree.AttachConfiguredCommitmentIndexStore())) {
                    m_shielded_account_registry = std::move(*restored_registry);
                    m_shielded_anchor_roots.assign(persisted_anchor_roots.begin(),
                                                   persisted_anchor_roots.end());
                    m_shielded_account_registry_roots.assign(
                        persisted_account_registry_roots.begin(),
                        persisted_account_registry_roots.end());
                    m_shielded_smile_public_accounts.clear();
                    m_shielded_account_leaf_commitments.clear();
                    if (shielded::registry::BuildRegistryAccountState(
                            m_shielded_account_registry,
                            m_shielded_smile_public_accounts,
                            m_shielded_account_leaf_commitments) &&
                        load_persisted_unshield_velocity()) {
                        InvalidateShieldedAccountStateSnapshotCaches();
                        m_shielded_state_initialized = true;
                        const auto persisted_accumulator =
                            m_shielded_nullifiers->ReadPersistedNullifierAccumulator();
                        const auto persisted_pin =
                            m_shielded_nullifiers->ReadPersistedShieldedStatePin();
                        const auto current_pin = ComputeShieldedSnapshotStatePin();
                        base_trusted =
                            persisted_accumulator.has_value() &&
                            *persisted_accumulator ==
                                m_shielded_nullifiers->NullifierAccumulatorDigest() &&
                            persisted_pin.has_value() &&
                            current_pin.has_value() &&
                            *persisted_pin == *current_pin;
                    }
                }
            }

            if (base_trusted) {
                shielded::ShieldedMerkleTree target_tree = m_shielded_merkle_tree;
                ShieldedPoolBalance target_pool = m_shielded_pool_balance;
                shielded::registry::ShieldedAccountRegistryState target_registry =
                    m_shielded_account_registry;
                if (!target_tree.DetachToMemoryOnly()) {
                    base_trusted = false;
                }

                std::vector<const CBlockIndex*> gap_path;
                for (const CBlockIndex* cursor = tip; cursor != persisted_index;
                     cursor = cursor->pprev) {
                    if (cursor == nullptr) {
                        base_trusted = false;
                        break;
                    }
                    gap_path.push_back(cursor);
                }
                std::reverse(gap_path.begin(), gap_path.end());
                for (const CBlockIndex* pindex : gap_path) {
                    if (!base_trusted) break;
                    if (ShieldedGapRecoveryCanSkipBlockBody(*pindex)) continue;
                    CBlock block;
                    if (!ReadShieldedRebuildBlock(*m_active_chainstate, *pindex, block)) {
                        base_trusted = false;
                        break;
                    }
                    const bool use_nonced_bridge_tag =
                        UseNoncedShieldedBridgeTags(GetConsensus(), pindex->nHeight);
                    for (const auto& txref : block.vtx) {
                        if (!txref->HasShieldedBundle()) continue;
                        const CShieldedBundle& bundle = txref->GetShieldedBundle();
                        std::string height_gate_reject;
                        if (!RejectShieldedHeightGateViolation(*txref,
                                                               GetConsensus(),
                                                               pindex->nHeight,
                                                               height_gate_reject) ||
                            !ApplyShieldedStateEffects(bundle,
                                                       use_nonced_bridge_tag,
                                                       pindex->GetBlockHash(),
                                                       target_tree,
                                                       target_pool)) {
                            base_trusted = false;
                            break;
                        }
                        const auto leaves =
                            shielded::registry::CollectShieldedOutputAccountLeaves(
                                bundle, use_nonced_bridge_tag);
                        if (!leaves.has_value() ||
                            !target_registry.AppendPrepared(
                                Span<const shielded::registry::ShieldedAccountLeaf>{
                                    leaves->data(), leaves->size()})) {
                            base_trusted = false;
                            break;
                        }
                    }
                }

                if (base_trusted) {
                    const auto marker = MakePreparedShieldedMutationMarker(
                        persisted_index, tip, target_tree, target_pool, target_registry);
                    if (marker.has_value() &&
                        target_registry.CommitPreparedPayloads() &&
                        ::WriteShieldedMutationMarker(m_shielded_nullifiers.get(), *marker) &&
                        try_restore_prepared_transition(*marker)) {
                        LogPrintf("EnsureShieldedStateInitialized: completed gap-only shielded catch-up of %d block(s) to height=%d hash=%s\n",
                                  gap_blocks, tip->nHeight,
                                  tip->GetBlockHash().ToString());
                        return finish_success();
                    }
                }
            }
            LogPrintf("EnsureShieldedStateInitialized: persisted ancestor failed gap-only provenance or transition checks; using single-pass fused rebuild\n");
            reset_shielded_state();
        } else if (persisted_index != nullptr &&
                   persisted_index->GetAncestor(tip->nHeight) == tip &&
                   tip->nHeight < persisted_index->nHeight) {
            const int gap_blocks = persisted_index->nHeight - tip->nHeight;
            LogPrintf("EnsureShieldedStateInitialized: persisted shielded tip height=%d hash=%s is ahead of active tip height=%d hash=%s (e.g. interrupted invalidateblock / unclean stop); unwinding %d block(s) from persisted state\n",
                      persisted_index->nHeight,
                      persisted_tip_hash.ToString(),
                      tip->nHeight,
                      tip->GetBlockHash().ToString(),
                      gap_blocks);

            bool base_trusted{false};
            auto restored_registry = persisted_account_registry_snapshot.has_value()
                ? shielded::registry::ShieldedAccountRegistryState::RestorePersisted(
                      *persisted_account_registry_snapshot)
                : std::nullopt;
            if (restored_registry.has_value() &&
                restored_registry->CanMaterializeAllEntries() &&
                m_shielded_pool_balance.SetBalance(persisted_balance)) {
                m_shielded_merkle_tree = persisted_tree;
                const auto actual_digest = m_shielded_merkle_tree.CommitmentIndexDigest();
                if (persisted_commitment_index_digest.has_value() &&
                    actual_digest.has_value() &&
                    *persisted_commitment_index_digest == *actual_digest &&
                    (m_shielded_merkle_tree.HasCommitmentIndex() ||
                     m_shielded_merkle_tree.AttachConfiguredCommitmentIndexStore())) {
                    m_shielded_account_registry = std::move(*restored_registry);
                    m_shielded_anchor_roots.assign(persisted_anchor_roots.begin(),
                                                   persisted_anchor_roots.end());
                    m_shielded_account_registry_roots.assign(
                        persisted_account_registry_roots.begin(),
                        persisted_account_registry_roots.end());
                    m_shielded_smile_public_accounts.clear();
                    m_shielded_account_leaf_commitments.clear();
                    if (shielded::registry::BuildRegistryAccountState(
                            m_shielded_account_registry,
                            m_shielded_smile_public_accounts,
                            m_shielded_account_leaf_commitments) &&
                        load_persisted_unshield_velocity()) {
                        InvalidateShieldedAccountStateSnapshotCaches();
                        m_shielded_state_initialized = true;
                        const auto persisted_accumulator =
                            m_shielded_nullifiers->ReadPersistedNullifierAccumulator();
                        const auto persisted_pin =
                            m_shielded_nullifiers->ReadPersistedShieldedStatePin();
                        const auto current_pin = ComputeShieldedSnapshotStatePin();
                        base_trusted =
                            persisted_accumulator.has_value() &&
                            *persisted_accumulator ==
                                m_shielded_nullifiers->NullifierAccumulatorDigest() &&
                            persisted_pin.has_value() && current_pin.has_value() &&
                            *persisted_pin == *current_pin;
                    }
                }
            }

            if (base_trusted) {
                shielded::ShieldedMerkleTree target_tree = m_shielded_merkle_tree;
                ShieldedPoolBalance target_pool = m_shielded_pool_balance;
                shielded::registry::ShieldedAccountRegistryState target_registry =
                    m_shielded_account_registry;
                if (!target_tree.DetachToMemoryOnly()) base_trusted = false;
                for (const CBlockIndex* cursor = persisted_index;
                     base_trusted && cursor != tip;
                     cursor = cursor->pprev) {
                    if (cursor == nullptr) {
                        base_trusted = false;
                        break;
                    }
                    if (ShieldedGapRecoveryCanSkipBlockBody(*cursor)) continue;
                    CBlock block;
                    if (!ReadShieldedRebuildBlock(*m_active_chainstate, *cursor, block)) {
                        base_trusted = false;
                        break;
                    }
                    uint64_t output_count{0};
                    uint64_t registry_leaf_count{0};
                    for (const auto& txref : block.vtx) {
                        if (!txref->HasShieldedBundle()) continue;
                        const CShieldedBundle& bundle = txref->GetShieldedBundle();
                        const uint64_t bundle_outputs = bundle.GetShieldedOutputCount();
                        if (output_count > std::numeric_limits<uint64_t>::max() - bundle_outputs) {
                            base_trusted = false;
                            break;
                        }
                        output_count += bundle_outputs;
                        uint64_t appends{0};
                        if (!TryCountShieldedAccountRegistryAppends(
                                bundle, GetConsensus(), cursor->nHeight, appends) ||
                            registry_leaf_count >
                                std::numeric_limits<uint64_t>::max() - appends) {
                            base_trusted = false;
                            break;
                        }
                        registry_leaf_count += appends;
                        std::string value_balance_reject;
                        const auto value_balance =
                            TryGetShieldedStateValueBalance(bundle, value_balance_reject);
                        if (!value_balance.has_value() ||
                            !target_pool.UndoValueBalance(*value_balance)) {
                            base_trusted = false;
                            break;
                        }
                    }
                    if (!base_trusted || output_count > target_tree.Size() ||
                        registry_leaf_count > target_registry.Size() ||
                        !target_tree.Truncate(target_tree.Size() - output_count) ||
                        !target_registry.Truncate(
                            target_registry.Size() - registry_leaf_count,
                            shielded::registry::ShieldedAccountRegistryState::PayloadPruneMode::KEEP)) {
                        base_trusted = false;
                    }
                }
                if (base_trusted) {
                    const auto marker = MakePreparedShieldedMutationMarker(
                        persisted_index, tip, target_tree, target_pool, target_registry);
                    if (marker.has_value() &&
                        ::WriteShieldedMutationMarker(m_shielded_nullifiers.get(), *marker) &&
                        try_restore_prepared_transition(*marker)) {
                        LogPrintf("EnsureShieldedStateInitialized: completed gap-only shielded unwind of %d block(s) to height=%d hash=%s\n",
                                  gap_blocks, tip->nHeight,
                                  tip->GetBlockHash().ToString());
                        return finish_success();
                    }
                }
            }
            LogPrintf("EnsureShieldedStateInitialized: persisted descendant failed gap-only provenance or transition checks; using single-pass fused rebuild\n");
            reset_shielded_state();
        } else if (persisted_index != nullptr) {
            const CBlockIndex* const transition_fork{
                LastCommonAncestor(persisted_index, tip)};
            const int disconnect_blocks = transition_fork
                ? persisted_index->nHeight - transition_fork->nHeight
                : std::numeric_limits<int>::max();
            const int connect_blocks = transition_fork
                ? tip->nHeight - transition_fork->nHeight
                : std::numeric_limits<int>::max();
            bool base_trusted{
                transition_fork != nullptr && disconnect_blocks > 0 &&
                connect_blocks > 0 &&
                disconnect_blocks + connect_blocks <= SHIELDED_ANCHOR_DEPTH};
            auto restored_registry = persisted_account_registry_snapshot.has_value()
                ? shielded::registry::ShieldedAccountRegistryState::RestorePersisted(
                      *persisted_account_registry_snapshot)
                : std::nullopt;
            if (!restored_registry.has_value() ||
                !restored_registry->CanMaterializeAllEntries() ||
                !m_shielded_pool_balance.SetBalance(persisted_balance)) {
                base_trusted = false;
            }
            if (base_trusted) {
                m_shielded_merkle_tree = persisted_tree;
                const auto actual_digest =
                    m_shielded_merkle_tree.CommitmentIndexDigest();
                base_trusted =
                    persisted_commitment_index_digest.has_value() &&
                    actual_digest.has_value() &&
                    *persisted_commitment_index_digest == *actual_digest &&
                    (m_shielded_merkle_tree.HasCommitmentIndex() ||
                     m_shielded_merkle_tree.AttachConfiguredCommitmentIndexStore());
            }
            if (base_trusted) {
                m_shielded_account_registry = std::move(*restored_registry);
                m_shielded_anchor_roots.assign(persisted_anchor_roots.begin(),
                                               persisted_anchor_roots.end());
                m_shielded_account_registry_roots.assign(
                    persisted_account_registry_roots.begin(),
                    persisted_account_registry_roots.end());
                m_shielded_smile_public_accounts.clear();
                m_shielded_account_leaf_commitments.clear();
                base_trusted =
                    shielded::registry::BuildRegistryAccountState(
                        m_shielded_account_registry,
                        m_shielded_smile_public_accounts,
                        m_shielded_account_leaf_commitments) &&
                    load_persisted_unshield_velocity();
                if (base_trusted) {
                    InvalidateShieldedAccountStateSnapshotCaches();
                    m_shielded_state_initialized = true;
                    const auto persisted_accumulator =
                        m_shielded_nullifiers->ReadPersistedNullifierAccumulator();
                    const auto persisted_pin =
                        m_shielded_nullifiers->ReadPersistedShieldedStatePin();
                    const auto current_pin = ComputeShieldedSnapshotStatePin();
                    base_trusted =
                        persisted_accumulator.has_value() &&
                        *persisted_accumulator ==
                            m_shielded_nullifiers->NullifierAccumulatorDigest() &&
                        persisted_pin.has_value() && current_pin.has_value() &&
                        *persisted_pin == *current_pin;
                }
            }

            if (base_trusted) {
                LogPrintf("EnsureShieldedStateInitialized: persisted shielded tip height=%d hash=%s is on a short fork from active tip height=%d hash=%s; disconnecting %d and connecting %d block(s) without from-genesis rebuild\n",
                          persisted_tip_height, persisted_tip_hash.ToString(),
                          tip->nHeight, tip->GetBlockHash().ToString(),
                          disconnect_blocks, connect_blocks);
                shielded::ShieldedMerkleTree target_tree = m_shielded_merkle_tree;
                ShieldedPoolBalance target_pool = m_shielded_pool_balance;
                shielded::registry::ShieldedAccountRegistryState target_registry =
                    m_shielded_account_registry;
                if (!target_tree.DetachToMemoryOnly()) base_trusted = false;

                for (const CBlockIndex* cursor = persisted_index;
                     base_trusted && cursor != transition_fork;
                     cursor = cursor->pprev) {
                    if (ShieldedGapRecoveryCanSkipBlockBody(*cursor)) continue;
                    CBlock block;
                    if (!ReadShieldedRebuildBlock(*m_active_chainstate,
                                                  *cursor, block)) {
                        base_trusted = false;
                        break;
                    }
                    // Undo is best-effort provenance. A SIGKILL after ConnectTip
                    // can persist shielded state before the matching undo file is
                    // durable; requiring undo here forced a from-genesis rebuild
                    // for a 2-block short fork. Disconnect uses the block's
                    // shielded output counts, same as the persisted-ahead path.
                    uint64_t output_count{0};
                    uint64_t registry_leaf_count{0};
                    for (const auto& txref : block.vtx) {
                        if (!txref->HasShieldedBundle()) continue;
                        const CShieldedBundle& bundle = txref->GetShieldedBundle();
                        const uint64_t outputs{bundle.GetShieldedOutputCount()};
                        uint64_t appends{0};
                        if (output_count >
                                std::numeric_limits<uint64_t>::max() - outputs ||
                            !TryCountShieldedAccountRegistryAppends(
                                bundle, GetConsensus(), cursor->nHeight, appends) ||
                            registry_leaf_count >
                                std::numeric_limits<uint64_t>::max() - appends) {
                            base_trusted = false;
                            break;
                        }
                        output_count += outputs;
                        registry_leaf_count += appends;
                        std::string reject;
                        const auto value_balance =
                            TryGetShieldedStateValueBalance(bundle, reject);
                        if (!value_balance.has_value() ||
                            !target_pool.UndoValueBalance(*value_balance)) {
                            base_trusted = false;
                            break;
                        }
                    }
                    if (!base_trusted || output_count > target_tree.Size() ||
                        registry_leaf_count > target_registry.Size() ||
                        !target_tree.Truncate(target_tree.Size() - output_count) ||
                        !target_registry.Truncate(
                            target_registry.Size() - registry_leaf_count,
                            shielded::registry::ShieldedAccountRegistryState::
                                PayloadPruneMode::KEEP)) {
                        base_trusted = false;
                    }
                }

                std::vector<const CBlockIndex*> connect_path;
                for (const CBlockIndex* cursor = tip;
                     base_trusted && cursor != transition_fork;
                     cursor = cursor->pprev) {
                    connect_path.push_back(cursor);
                }
                std::reverse(connect_path.begin(), connect_path.end());
                for (const CBlockIndex* pindex : connect_path) {
                    if (ShieldedGapRecoveryCanSkipBlockBody(*pindex)) continue;
                    CBlock block;
                    if (!ReadShieldedRebuildBlock(*m_active_chainstate,
                                                  *pindex, block)) {
                        base_trusted = false;
                        break;
                    }
                    const bool use_nonced_bridge_tag =
                        UseNoncedShieldedBridgeTags(GetConsensus(), pindex->nHeight);
                    for (const auto& txref : block.vtx) {
                        if (!txref->HasShieldedBundle()) continue;
                        const CShieldedBundle& bundle = txref->GetShieldedBundle();
                        std::string reject;
                        if (!RejectShieldedHeightGateViolation(
                                *txref, GetConsensus(), pindex->nHeight, reject) ||
                            !ApplyShieldedStateEffects(
                                bundle, use_nonced_bridge_tag,
                                pindex->GetBlockHash(), target_tree, target_pool)) {
                            base_trusted = false;
                            break;
                        }
                        const auto leaves =
                            shielded::registry::CollectShieldedOutputAccountLeaves(
                                bundle, use_nonced_bridge_tag);
                        if (!leaves.has_value() ||
                            !target_registry.AppendPrepared(
                                Span<const shielded::registry::ShieldedAccountLeaf>{
                                    leaves->data(), leaves->size()})) {
                            base_trusted = false;
                            break;
                        }
                    }
                    if (!base_trusted) break;
                }
                if (base_trusted) {
                    const auto marker = MakePreparedShieldedMutationMarker(
                        persisted_index, tip, target_tree, target_pool,
                        target_registry);
                    if (marker.has_value() &&
                        target_registry.CommitPreparedPayloads() &&
                        ::WriteShieldedMutationMarker(
                            m_shielded_nullifiers.get(), *marker) &&
                        try_restore_prepared_transition(*marker)) {
                        LogPrintf("EnsureShieldedStateInitialized: completed gap-only shielded short-reorg transition (disconnect=%d connect=%d) to height=%d hash=%s\n",
                                  disconnect_blocks, connect_blocks,
                                  tip->nHeight, tip->GetBlockHash().ToString());
                        return finish_success();
                    }
                }
            }
            LogPrintf("EnsureShieldedStateInitialized: persisted short-fork state failed bounded gap-only provenance or transition checks; using single-pass fused rebuild\n");
            reset_shielded_state();
        } else {
            LogPrintf("EnsureShieldedStateInitialized: persisted shielded tip height=%d hash=%s is not in the block index (active tip height=%d hash=%s); performing single-pass fused rebuild from chain\n",
                      persisted_tip_height,
                      persisted_tip_hash.ToString(),
                      tip->nHeight,
                      tip->GetBlockHash().ToString());
        }
    }

    if (!rebuild_from_chain()) {
        return false;
    }
    return finish_success();
}

bool ChainstateManager::IsShieldedAnchorValid(const uint256& anchor) const
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_state_initialized) return false;
    if (anchor.IsNull()) return false;
    const uint256 root = m_shielded_merkle_tree.Root();
    const bool valid = std::find(m_shielded_anchor_roots.begin(),
                                 m_shielded_anchor_roots.end(),
                                 anchor) != m_shielded_anchor_roots.end();
    if (!valid) {
        LogPrintf("ChainstateManager::IsShieldedAnchorValid mismatch: provided=%s current_root=%s tree_size=%u anchors_tracked=%u\n",
                  anchor.ToString(),
                  root.ToString(),
                  static_cast<unsigned int>(m_shielded_merkle_tree.Size()),
                  static_cast<unsigned int>(m_shielded_anchor_roots.size()));
    }
    return valid;
}

bool ChainstateManager::IsShieldedAccountRegistryRootValid(const uint256& root) const
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_state_initialized) return false;
    if (root.IsNull()) return false;
    const uint256 current_root = m_shielded_account_registry.Root();
    const bool valid = std::find(m_shielded_account_registry_roots.begin(),
                                 m_shielded_account_registry_roots.end(),
                                 root) != m_shielded_account_registry_roots.end();
    if (!valid) {
        LogPrintf("ChainstateManager::IsShieldedAccountRegistryRootValid mismatch: provided=%s current_root=%s registry_size=%u roots_tracked=%u\n",
                  root.ToString(),
                  current_root.ToString(),
                  static_cast<unsigned int>(m_shielded_account_registry.Size()),
                  static_cast<unsigned int>(m_shielded_account_registry_roots.size()));
    }
    return valid;
}

std::optional<std::pair<uint256, shielded::registry::ShieldedAccountRegistrySpendWitness>>
ChainstateManager::GetShieldedAccountRegistryWitness(const uint256& account_leaf_commitment) const
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_state_initialized || account_leaf_commitment.IsNull()) {
        return std::nullopt;
    }
    const auto proof = m_shielded_account_registry.BuildSpendWitnessByCommitment(account_leaf_commitment);
    if (!proof.has_value()) {
        return std::nullopt;
    }
    const uint256 root = m_shielded_account_registry.Root();
    if (root.IsNull()) {
        return std::nullopt;
    }
    return std::make_pair(root, std::move(*proof));
}

bool ChainstateManager::IsShieldedSettlementAnchorValid(const uint256& anchor) const
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_state_initialized || !m_shielded_nullifiers) return false;
    return m_shielded_nullifiers->ContainsSettlementAnchor(anchor);
}

std::optional<ConfirmedSettlementAnchorState> ChainstateManager::GetShieldedSettlementAnchorState(
    const uint256& anchor) const
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_state_initialized || !m_shielded_nullifiers) return std::nullopt;
    return m_shielded_nullifiers->GetSettlementAnchorState(anchor);
}

bool ChainstateManager::HasShieldedNettingManifest(const uint256& manifest_id) const
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_state_initialized || !m_shielded_nullifiers) return false;
    return m_shielded_nullifiers->ContainsNettingManifest(manifest_id);
}

bool ChainstateManager::IsShieldedNettingManifestValid(const uint256& manifest_id) const
{
    AssertLockHeld(::cs_main);
    const CBlockIndex* const tip = m_active_chainstate != nullptr ? m_active_chainstate->m_chain.Tip() : nullptr;
    return IsShieldedNettingManifestValid(manifest_id, tip != nullptr ? tip->nHeight : -1);
}

bool ChainstateManager::IsShieldedNettingManifestValid(const uint256& manifest_id,
                                                       int32_t validation_height) const
{
    AssertLockHeld(::cs_main);
    if (!HasShieldedNettingManifest(manifest_id)) return false;
    if (m_active_chainstate == nullptr) return true;
    if (!UseNettingManifestFreshnessBinding(GetConsensus(), validation_height)) {
        return true;
    }
    const auto manifest_state = m_shielded_nullifiers->GetNettingManifestState(manifest_id);
    return manifest_state.has_value() && IsNettingManifestFresh(*manifest_state, validation_height);
}

std::optional<ConfirmedNettingManifestState> ChainstateManager::GetShieldedNettingManifestState(
    const uint256& manifest_id) const
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_state_initialized || !m_shielded_nullifiers) return std::nullopt;
    return m_shielded_nullifiers->GetNettingManifestState(manifest_id);
}

bool ChainstateManager::IsShieldedNullifierSpent(const Nullifier& nullifier) const
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_state_initialized || !m_shielded_nullifiers) return false;
    return m_shielded_nullifiers->Contains(nullifier);
}

uint64_t ChainstateManager::GetShieldedNullifierCount() const
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_state_initialized || !m_shielded_nullifiers) return 0;
    return m_shielded_nullifiers->CountNullifiers();
}

std::optional<ShieldedStateMutationMarker> ChainstateManager::ReadShieldedMutationMarker() const
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return std::nullopt;
    return m_shielded_nullifiers->ReadMutationMarker();
}

bool ChainstateManager::WriteShieldedMutationMarker(const ShieldedStateMutationMarker& marker)
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return false;
    return m_shielded_nullifiers->WriteMutationMarker(marker);
}

bool ChainstateManager::ClearShieldedMutationMarker()
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return false;
    return m_shielded_nullifiers->ClearMutationMarker();
}

bool ChainstateManager::InsertShieldedNullifiersForTest(const std::vector<Nullifier>& nullifiers)
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return false;
    return m_shielded_nullifiers->Insert(nullifiers);
}

bool ChainstateManager::InsertShieldedRecoveryExitCommitmentsForTest(const std::vector<uint256>& commitments)
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return false;
    return m_shielded_nullifiers->InsertRecoveryExitCommitments(commitments);
}

bool ChainstateManager::AppendShieldedAccountRegistryForTest(
    Span<const shielded::registry::ShieldedAccountLeaf> leaves)
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_state_initialized) return false;
    if (!m_shielded_account_registry.Append(leaves)) return false;
    if (!shielded::registry::BuildRegistryAccountState(m_shielded_account_registry,
                                                       m_shielded_smile_public_accounts,
                                                       m_shielded_account_leaf_commitments)) {
        return false;
    }
    InvalidateShieldedAccountStateSnapshotCaches();
    return PersistShieldedState(ActiveTip(), /*fast_startup_trusted=*/true);
}

bool ChainstateManager::InsertShieldedSettlementAnchorsForTest(const std::vector<uint256>& anchors)
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return false;
    return m_shielded_nullifiers->InsertSettlementAnchors(anchors);
}

bool ChainstateManager::InsertShieldedSettlementAnchorsForTest(
    const std::vector<ConfirmedSettlementAnchorState>& anchors)
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return false;
    return m_shielded_nullifiers->InsertSettlementAnchors(anchors);
}

bool ChainstateManager::InsertShieldedNettingManifestsForTest(
    const std::vector<ConfirmedNettingManifestState>& manifests)
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return false;
    return m_shielded_nullifiers->InsertNettingManifests(manifests);
}

bool ChainstateManager::WriteShieldedPoolBalanceForTest(CAmount balance)
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return false;
    return m_shielded_nullifiers->WritePoolBalance(balance);
}

bool ChainstateManager::WriteShieldedUnshieldVelocityForTest(const ShieldedUnshieldVelocity& velocity)
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return false;
    return m_shielded_nullifiers->WriteUnshieldVelocity(velocity);
}

bool ChainstateManager::SetShieldedPoolBalanceForTest(CAmount balance)
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return false;
    if (!m_shielded_pool_balance.SetBalance(balance)) return false;
    return m_shielded_nullifiers->WritePoolBalance(balance);
}

bool ChainstateManager::IsShieldedRecoveryExitCommitmentRetired(const uint256& commitment) const
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return false;
    return m_shielded_nullifiers->ContainsRecoveryExitCommitment(commitment);
}

bool ChainstateManager::WriteSnapshotBridgeMetadataHintForTest(bool preserve_snapshot_extras)
{
    AssertLockHeld(::cs_main);
    if (!m_shielded_nullifiers) return false;
    return m_shielded_nullifiers->WriteSnapshotBridgeMetadataHint(preserve_snapshot_extras);
}

void ChainstateManager::SetShieldedAnchorRootsForTest(const std::vector<uint256>& roots)
{
    AssertLockHeld(::cs_main);
    m_shielded_anchor_roots.clear();
    for (const auto& root : roots) {
        m_shielded_anchor_roots.push_back(root);
    }
}

bool ChainstateManager::DropShieldedCommitmentIndexForTest()
{
    AssertLockHeld(::cs_main);
    DataStream stream;
    stream << m_shielded_merkle_tree;
    shielded::ShieldedMerkleTree frontier_only{
        shielded::ShieldedMerkleTree::IndexStorageMode::MEMORY_ONLY};
    stream >> frontier_only;
    m_shielded_merkle_tree = std::move(frontier_only);
    return m_shielded_merkle_tree.Size() == 0 || !m_shielded_merkle_tree.HasCommitmentIndex();
}

bool ChainstateManager::DetectSnapshotChainstate()
{
    assert(!m_snapshot_chainstate);
    std::optional<fs::path> path = node::FindSnapshotChainstateDir(m_options.datadir);
    if (!path) {
        return false;
    }
    std::optional<uint256> base_blockhash = node::ReadSnapshotBaseBlockhash(*path);
    if (!base_blockhash) {
        return false;
    }
    LogPrintf("[snapshot] detected active snapshot chainstate (%s) - loading\n",
        fs::PathToString(*path));

    const bool attested_sidecar_exists{node::AttestedAssumeutxoDataExists(*path)};
    if (auto attested = node::ReadAttestedAssumeutxoData(*path)) {
        if (attested->assumeutxo.blockhash != *base_blockhash) {
            GetNotifications().fatalError(_(
                "Attested snapshot manifest does not match the persisted snapshot base blockhash."));
            return false;
        }
        m_attested_assumeutxo = attested->assumeutxo;
        LogPrintf("[snapshot] restored attested-fast-forward assumeutxo override at height %d\n",
                  attested->assumeutxo.height);
    } else if (attested_sidecar_exists) {
        GetNotifications().fatalError(_(
            "Attested snapshot manifest is present but failed verification under the current authority configuration."));
        return false;
    }

    this->ActivateExistingSnapshot(*base_blockhash);
    return true;
}

Chainstate& ChainstateManager::ActivateExistingSnapshot(uint256 base_blockhash)
{
    assert(!m_snapshot_chainstate);
    m_snapshot_chainstate =
        std::make_unique<Chainstate>(nullptr, m_blockman, *this, base_blockhash);
    LogPrintf("[snapshot] switching active chainstate to %s\n", m_snapshot_chainstate->ToString());

    // Mempool is empty at this point because we're still in IBD.
    Assert(m_active_chainstate->m_mempool->size() == 0);
    Assert(!m_snapshot_chainstate->m_mempool);
    m_snapshot_chainstate->m_mempool = m_active_chainstate->m_mempool;
    m_active_chainstate->m_mempool = nullptr;
    m_active_chainstate = m_snapshot_chainstate.get();
    return *m_snapshot_chainstate;
}

bool IsBIP30Repeat(const CBlockIndex& block_index)
{
    return (block_index.nHeight==91842 && block_index.GetBlockHash() == uint256{"00000000000a4d0a398161ffc163c503763b1f4360639393e0e4c8e300e0caec"}) ||
           (block_index.nHeight==91880 && block_index.GetBlockHash() == uint256{"00000000000743f190a18c5577a3c2d2a1f610ae9601ac046a38084ccb7cd721"});
}

bool IsBIP30Unspendable(const CBlockIndex& block_index)
{
    return (block_index.nHeight==91722 && block_index.GetBlockHash() == uint256{"00000000000271a2dc26e7667f8419f2e15416dc6955e5a6c6cdf3f2574dd08e"}) ||
           (block_index.nHeight==91812 && block_index.GetBlockHash() == uint256{"00000000000af0aed4792b1acee3d966af36cf5def14935db8de83d6f9306f2f"});
}

static fs::path GetSnapshotCoinsDBPath(Chainstate& cs) EXCLUSIVE_LOCKS_REQUIRED(::cs_main)
{
    AssertLockHeld(::cs_main);
    // Should never be called on a non-snapshot chainstate.
    assert(cs.m_from_snapshot_blockhash);
    auto storage_path_maybe = cs.CoinsDB().StoragePath();
    // Should never be called with a non-existent storage path.
    assert(storage_path_maybe);
    return *storage_path_maybe;
}

util::Result<void> Chainstate::InvalidateCoinsDBOnDisk()
{
    fs::path snapshot_datadir = GetSnapshotCoinsDBPath(*this);

    // Coins views no longer usable.
    m_coins_views.reset();

    auto invalid_path = snapshot_datadir + "_INVALID";
    std::string dbpath = fs::PathToString(snapshot_datadir);
    std::string target = fs::PathToString(invalid_path);
    LogPrintf("[snapshot] renaming snapshot datadir %s to %s\n", dbpath, target);

    // The invalid snapshot datadir is simply moved and not deleted because we may
    // want to do forensics later during issue investigation. The user is instructed
    // accordingly in MaybeCompleteSnapshotValidation().
    try {
        fs::rename(snapshot_datadir, invalid_path);
    } catch (const fs::filesystem_error& e) {
        auto src_str = fs::PathToString(snapshot_datadir);
        auto dest_str = fs::PathToString(invalid_path);

        LogPrintf("%s: error renaming file '%s' -> '%s': %s\n",
                __func__, src_str, dest_str, e.what());
        return util::Error{strprintf(_(
            "Rename of '%s' -> '%s' failed. "
            "You should resolve this by manually moving or deleting the invalid "
            "snapshot directory %s, otherwise you will encounter the same error again "
            "on the next startup."),
            src_str, dest_str, src_str)};
    }
    return {};
}

bool ChainstateManager::DeleteSnapshotChainstate()
{
    AssertLockHeld(::cs_main);
    Assert(m_snapshot_chainstate);
    Assert(m_ibd_chainstate);

    fs::path snapshot_datadir = Assert(node::FindSnapshotChainstateDir(m_options.datadir)).value();
    if (!DeleteCoinsDBFromDisk(snapshot_datadir, /*is_snapshot=*/ true)) {
        LogPrintf("Deletion of %s failed. Please remove it manually to continue reindexing.\n",
                  fs::PathToString(snapshot_datadir));
        return false;
    }
    m_active_chainstate = m_ibd_chainstate.get();
    m_active_chainstate->m_mempool = m_snapshot_chainstate->m_mempool;
    m_snapshot_chainstate.reset();
    return true;
}

ChainstateRole Chainstate::GetRole() const
{
    if (m_chainman.GetAll().size() <= 1) {
        return ChainstateRole::NORMAL;
    }
    return (this != &m_chainman.ActiveChainstate()) ?
               ChainstateRole::BACKGROUND :
               ChainstateRole::ASSUMEDVALID;
}

const CBlockIndex* ChainstateManager::GetSnapshotBaseBlock() const
{
    return m_active_chainstate ? m_active_chainstate->SnapshotBase() : nullptr;
}

std::optional<int> ChainstateManager::GetSnapshotBaseHeight() const
{
    const CBlockIndex* base = this->GetSnapshotBaseBlock();
    return base ? std::make_optional(base->nHeight) : std::nullopt;
}

void ChainstateManager::RecalculateBestHeader()
{
    AssertLockHeld(cs_main);
    const CBlockIndex* const active_tip{ActiveChain().Tip()};
    SetBestHeader(ActiveChain().Tip());
    if (!node::matmul_trusted::IsConfigured() || active_tip == nullptr) {
        for (auto& entry : m_blockman.m_block_index) {
            if (entry.second.nStatus & BLOCK_FAILED_MASK) continue;
            // Authenticated-work selection with a bounded unauth allowance: a short
            // unverified MatMul suffix may displace a losing tip for chase, but a
            // forged flood beyond the allowance cannot outrank authenticated work.
            if (m_best_header == nullptr || PreferTrustAdjustedHeader(*m_best_header, entry.second)) {
                SetBestHeader(&entry.second);
            }
        }
        return;
    }

    // Recalculation runs after load/invalidate/reconsider and has no peer
    // provenance. Preserve the live configured-node predicate: ordinary
    // headers may only advance the active-tip branch, while a competing
    // branch requires a current-config M-of-N quorum on itself or an
    // ancestor. This prevents an unordered generic rescan from following
    // unattestable fork spam.
    //
    // PreferTrustAdjustedHeader on every tip-chain descendant is the wrong
    // overlay after attested snapshot load: nAuthenticatedChainWork often
    // covers only the first attested child (headers=H+1) while H+2..H+N
    // stay HEADER_ONLY, so m_best_header stalls and getblockchaininfo
    // rewinds (follow_forward: 11 < 18). Snap to the furthest HasQuorum
    // descendant of the active tip, then to the unique suffix of that
    // attested frontier — competing unattested siblings of H are not in
    // that subtree.
    CBlockIndex* attested_frontier{
        node::matmul_trusted::HasQuorum(active_tip->GetBlockHash(),
                                        active_tip->nHeight)
            ? const_cast<CBlockIndex*>(active_tip)
            : nullptr};
    CBlockIndex* authority_best{nullptr};
    for (auto& [_, candidate] : m_blockman.m_block_index) {
        if ((candidate.nStatus & BLOCK_FAILED_MASK) ||
            IsOnParkedReorgBranch(&candidate) ||
            candidate.nHeight < active_tip->nHeight) {
            continue;
        }

        if (BlockIndexDescends(&candidate, active_tip)) {
            if (node::matmul_trusted::HasQuorum(candidate.GetBlockHash(),
                                                candidate.nHeight)) {
                if (attested_frontier == nullptr ||
                    CBlockIndexWorkComparator()(attested_frontier, &candidate)) {
                    attested_frontier = &candidate;
                }
            }
            continue;
        }
        if (candidate.nChainWork < active_tip->nChainWork) continue;

        bool has_authority_ancestor{false};
        for (const CBlockIndex* cursor = &candidate;
             cursor != nullptr && cursor->nHeight >= active_tip->nHeight;
             cursor = cursor->pprev) {
            if (node::matmul_trusted::HasQuorum(
                    cursor->GetBlockHash(), cursor->nHeight)) {
                has_authority_ancestor = true;
                break;
            }
        }
        if (!has_authority_ancestor) continue;
        if (authority_best == nullptr ||
            CBlockIndexWorkComparator()(authority_best, &candidate)) {
            authority_best = &candidate;
        }
    }
    if (attested_frontier != nullptr) {
        CBlockIndex* followed{attested_frontier};
        for (auto& [_, candidate] : m_blockman.m_block_index) {
            if ((candidate.nStatus & BLOCK_FAILED_MASK) ||
                IsOnParkedReorgBranch(&candidate) ||
                !BlockIndexDescends(&candidate, attested_frontier)) {
                continue;
            }
            if (candidate.nHeight > followed->nHeight ||
                (candidate.nHeight == followed->nHeight &&
                 CBlockIndexWorkComparator()(followed, &candidate))) {
                followed = &candidate;
            }
        }
        SetBestHeader(followed);
    }
    if (authority_best != nullptr) {
        // Current quorum is stronger than peer provenance and therefore may
        // displace the active-tip branch on reconstruction.
        SetBestHeader(authority_best);
    }
}

bool ChainstateManager::ValidatedSnapshotCleanup()
{
    AssertLockHeld(::cs_main);
    auto get_storage_path = [](auto& chainstate) EXCLUSIVE_LOCKS_REQUIRED(::cs_main) -> std::optional<fs::path> {
        if (!(chainstate && chainstate->HasCoinsViews())) {
            return {};
        }
        return chainstate->CoinsDB().StoragePath();
    };
    std::optional<fs::path> ibd_chainstate_path_maybe = get_storage_path(m_ibd_chainstate);
    std::optional<fs::path> snapshot_chainstate_path_maybe = get_storage_path(m_snapshot_chainstate);

    if (!this->IsSnapshotValidated()) {
        // No need to clean up.
        return false;
    }
    // If either path doesn't exist, that means at least one of the chainstates
    // is in-memory, in which case we can't do on-disk cleanup. You'd better be
    // in a unittest!
    if (!ibd_chainstate_path_maybe || !snapshot_chainstate_path_maybe) {
        LogPrintf("[snapshot] snapshot chainstate cleanup cannot happen with "
                  "in-memory chainstates. You are testing, right?\n");
        return false;
    }

    const auto& snapshot_chainstate_path = *snapshot_chainstate_path_maybe;
    const auto& ibd_chainstate_path = *ibd_chainstate_path_maybe;

    // Since we're going to be moving around the underlying leveldb filesystem content
    // for each chainstate, make sure that the chainstates (and their constituent
    // CoinsViews members) have been destructed first.
    //
    // The caller of this method will be responsible for reinitializing chainstates
    // if they want to continue operation.
    this->ResetChainstates();

    // No chainstates should be considered usable.
    assert(this->GetAll().size() == 0);

    LogPrintf("[snapshot] deleting background chainstate directory (now unnecessary) (%s)\n",
              fs::PathToString(ibd_chainstate_path));

    fs::path tmp_old{ibd_chainstate_path + "_todelete"};

    auto rename_failed_abort = [this](
                                   fs::path p_old,
                                   fs::path p_new,
                                   const fs::filesystem_error& err) {
        LogError("[snapshot] Error renaming path (%s) -> (%s): %s\n",
                  fs::PathToString(p_old), fs::PathToString(p_new), err.what());
        GetNotifications().fatalError(strprintf(_(
            "Rename of '%s' -> '%s' failed. "
            "Cannot clean up the background chainstate leveldb directory."),
            fs::PathToString(p_old), fs::PathToString(p_new)));
    };

    try {
        fs::rename(ibd_chainstate_path, tmp_old);
    } catch (const fs::filesystem_error& e) {
        rename_failed_abort(ibd_chainstate_path, tmp_old, e);
        throw;
    }

    LogPrintf("[snapshot] moving snapshot chainstate (%s) to "
              "default chainstate directory (%s)\n",
              fs::PathToString(snapshot_chainstate_path), fs::PathToString(ibd_chainstate_path));

    try {
        fs::rename(snapshot_chainstate_path, ibd_chainstate_path);
    } catch (const fs::filesystem_error& e) {
        rename_failed_abort(snapshot_chainstate_path, ibd_chainstate_path, e);
        throw;
    }

    if (!DeleteCoinsDBFromDisk(tmp_old, /*is_snapshot=*/false)) {
        // No need to FatalError because once the unneeded bg chainstate data is
        // moved, it will not interfere with subsequent initialization.
        LogPrintf("Deletion of %s failed. Please remove it manually, as the "
                  "directory is now unnecessary.\n",
                  fs::PathToString(tmp_old));
    } else {
        LogPrintf("[snapshot] deleted background chainstate directory (%s)\n",
                  fs::PathToString(ibd_chainstate_path));
    }
    return true;
}

Chainstate& ChainstateManager::GetChainstateForIndexing()
{
    // We can't always return `m_ibd_chainstate` because after background validation
    // has completed, `m_snapshot_chainstate == m_active_chainstate`, but it can be
    // indexed.
    return (this->GetAll().size() > 1) ? *m_ibd_chainstate : *m_active_chainstate;
}

std::pair<int, int> ChainstateManager::GetPruneRange(const Chainstate& chainstate, int last_height_can_prune)
{
    if (chainstate.m_chain.Height() <= 0) {
        return {0, 0};
    }
    int prune_start{0};

    if (this->GetAll().size() > 1 && m_snapshot_chainstate.get() == &chainstate) {
        // Leave the blocks in the background IBD chain alone if we're pruning
        // the snapshot chain.
        prune_start = *Assert(GetSnapshotBaseHeight()) + 1;
    }

    int max_prune = std::max<int>(
        0, chainstate.m_chain.Height() - static_cast<int>(MIN_BLOCKS_TO_KEEP));

    // last block to prune is the lesser of (caller-specified height, MIN_BLOCKS_TO_KEEP from the tip)
    //
    // While you might be tempted to prune the background chainstate more
    // aggressively (i.e. fewer MIN_BLOCKS_TO_KEEP), this won't work with index
    // building - specifically blockfilterindex requires undo data, and if
    // we don't maintain this trailing window, we hit indexing failures.
    int prune_end = std::min(last_height_can_prune, max_prune);

    return {prune_start, prune_end};
}
